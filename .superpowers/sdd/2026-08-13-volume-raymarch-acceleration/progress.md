# SDD ledger — plan: docs/superpowers/plans/2026-08-13-volume-raymarch-acceleration.md

Branch: worktree-volume-raymarch-acceleration · Draft PR: #556 · Spec: docs/superpowers/specs/2026-08-12-volume-raymarch-acceleration-design.md
Plan commits: ffefa64b4 (initial) + 1d5662b99 (amendment: dev-space max pyramid — divergent palettes skip safely; cf4-density exists TODAY).
Execution mode: subagent-driven, strictly sequential (heavy file overlap; see plan Global Constraints).
User checkpoints: Tasks 5 and 6 visual checkpoints need the USER's eyes — pause dispatch and ask.
Dev server: main session starts/owns it (bg subagents don't run npm; implementers get the port in their brief).
Baseline note: pre-change refs measured on OTHER poses — full-survey scalar-volume 0.6 ms / local-group 1.4 ms (per-layer, 30f, dpr2). Task 1 records the volume-inside baseline.

Task 1 baseline (volume-inside, 30f dpr2): TOTAL merged 8.6 ms; scalar-volume per-layer 1.4 ms; volume-upsample per-layer 0.3 ms; volume·COSMO MERGED group 1.1 ms.
Task 1 concern: the ≥2x-local-group PER-LAYER acceptance bar was NOT cleanly met — clean, paired, contention-controlled trials against a matched local-group run (same session, same command) put local-group's own per-layer scalar-volume at 1.1-1.5 ms depending on noise, statistically overlapping volume-inside's 1.4 ms. See task-1-report.md for the full trial log (14 candidate poses: center, near-wall facing, near-corner, void-seeking far octant) and the root-cause finding: PER-LAYER carries a ~0.6-1.5 ms fixed per-pass floor (see the perf skill) that dominates this pass's small absolute cost at EVERY position tried, damping any real geometric signal below the 2x threshold. The MERGED `volume·COSMO` group is a cleaner signal: it reads a clean, standalone 1.1 ms for volume-inside vs folding into `hdr·COSMO` (~0.2 ms) for local-group — a real, qualitative, reproducible difference. Recommend Stage 1-3 authors compare against volume·COSMO MERGED rather than the per-layer row, or accept this row's real-but-modest (~25-30%) per-layer margin over the noise floor.
Task 1 review: Spec PASS; quality approved w/ 1 Important — pose visual density unverified (no screenshot; frame cost profile suggests point-sprite-dominated). Enters fix loop round 1 (QUEUED behind Task 2 implementer — serial-implementer rule). Fix scope: screenshot at committed pose via ?perf setPose; if not volumetric-dense, adjust pose + re-baseline paired. Also resolves reviewer's two ⚠️ items (visual density, envelope-boundary-for-view-direction).
Task 1: minor (deferred): perfScenarios.ts header docstring claims all poses captured via logState; this row was derived numerically (disclosed in row comment only).
Task 1: minor (deferred): 14-pose trial log lives only in git-ignored task-1-report.md.
Task 1 Important finding RESOLVED without fix dispatch: Task 2's visual-gate artifact (task-2-visual/before.png, captured at the volume-inside pose) shows the volume layer live and contributing frame-wide (filamentary glow), camera inside the cube, full-viewport raymarch coverage. Frame is point-sprite-dominated visually — acceptable: the gate metric is volume·COSMO MERGED (1.1 ms standalone), not visual share. Both reviewer ⚠️ items resolved by the same evidence.
Task 1: complete (commits 1d5662b99..679de1bef, review clean after evidence ruling; 2 minors deferred)
Task 2 implementer: DONE (commit 59656570d; suite 1023 files/6901 tests green; visual delta 0.601/255). Review dispatched.
Task 2 review: Spec PASS; quality CHANGES REQUESTED — 1 Critical: fs_max 2k/2k+1 tap pairs drop the trailing parent slice/row/col on odd dims (floor-sized dst ⇒ edge clamp unreachable; verified numerically 91→45 drops z=90, 89→44 drops x=88). Box chain unaffected (continuous-ratio blend). Max pyramid would under-report maxima ⇒ unsafe over-skip in Task 5. Fix round 1 dispatched (resume implementer). Minor deferred: generateMipChain3d.ts header 18 lines vs ≤10 budget (matches 2D sibling precedent).
Task 2: fix round 1/5 (1 addressed, 0 open — fs_max tapRange covers odd-dim trailing indices; MipBlit3dUniforms shrank to 16B via textureDimensions on single-level views; commits 59656570d..17c3b28b9)
Task 2: complete (commits 679de1bef..17c3b28b9, review clean after 1 fix round; 1 minor deferred: header line budget)
Task 3 implementer: DONE (commit 6e93d14af; suite green; visual delta 0.268/255; zero WebGPU validation errors). Concerns carried: (a) Task 5 visual check must be deliberately composed (this pose's captures read as starfield-dominated); (b) buildMaxPyramid dims = chained ceil-halving, agrees with ceil(dims/8) for shipped grids but not arbitrary odd dims — reviewer to assess. Review dispatched.
Task 3 review: Spec PASS, quality Approve, 0 Critical/Important. Ceil-halving concern proven non-issue (ceiling-division composes; verified d∈[1,2000]). ⚠️ resolved by controller: task-3 screenshots NOT preserved on disk (report documents the run; risk accepted — unused BGL entry is inert, suite green). RULE for Tasks 5-6: visual artifacts MUST be saved into the SDD workspace.
Task 3: complete (commits 17c3b28b9..6e93d14af, review clean; 1 minor deferred: FieldEntry docblock density matches local convention)
Task 4 implementer: DONE (commit 7ecdd6905; suite 6902 green; both tsc clean — controller re-verified after stale editor diagnostics). Two extra test files touched as signature fallout (scalarVolumeLayer.test.ts, renderFrameSplitBaseline.test.ts snapshot) — reviewer to judge necessity. Review dispatched.
Task 4 review: Spec PASS; quality approve-with-findings — 2 Important (scratch[66]/[67] pad zeroing missing vs in-file convention; scalarVolumeLayer.test.ts pixelConeTan assertion is a mirror test) + 1 Minor (comment claims small-angle approx; formula is exact). Fix round 1 dispatched (resumed original implementer).
Task 4: fix round 1/5 (3 addressed incl. optional minor, 0 open; commits 7ecdd6905..7a0521f06)
Task 4: complete (commits 6e93d14af..7a0521f06, review clean after 1 fix round)
Task 5 implementer: DONE (commit 34d612772; visuals identical ≤0.88/255 all compositions incl. volume-only; sliders retune live; suite green). PERF REGRESSION at volume-inside: volume·COSMO 1.5→2.2 ms (+47%), reproduced — pose is skip-adversarial (dense structure, skips rarely fire). Controller ruling: proceed to Task 6 (loop restructure rebalances cost/payoff), add full-survey as skip-favorable pose at Task 6 gate; fix loop only if still net-negative post-Task-6. PENDING USER RATIFICATION.
Task 5 review: Spec PASS, Approve; 4 minors deferred (hoistable textureDimensions in pyramidCellQuery; report wording ×2; stale binding-5 comment in volumeFieldRenderer.ts:168).
Task 5: complete (commits 7a0521f06..34d612772; USER ratified visual checkpoint 2026-08-13 "visual is ok" + no objection to proceed-to-Task-6 ruling; 4 minors deferred). Perf context for Task 6 gate: volume-inside volume·COSMO 1.5→2.2 ms (+47%), skip-adversarial pose; measure BOTH volume-inside AND full-survey at Task 6's gate; fix-loop the skip policy only if still net-negative post-Task-6.
Task 6 BASE: 34d612772 — implementer dispatched
Resume 2026-08-18: prior session died mid-Task-6. Working tree (base 34d612772) holds the implementer's UNCOMMITTED implementation incl. a TEMP DEBUG heatmap return in fragment.wesl; visuals captured 08-13 in task-6-visual/; no report, no commit. Dev server for this worktree started on :5175 (controller-owned). Continuation state written to task-6-continuation.md; fresh implementer dispatched (sonnet) to verify/finish/measure/commit.
Ruling (user request 2026-08-18): Task 6 perf evidence must ALSO include `--tier large` runs of both poses (deeper volume grids stress cone-LOD/mip depth). Like-for-like gate vs T1/T5 stays at the boot tier (medium) for comparability; large-tier is an additional data point. If the implementer's report lacks it, resume the agent for the extra runs before review dispatch.
Ruling (user request 2026-08-19): expanded perf matrix AFTER the 2MRS MCPM field lands on main — merge main into this branch, then run --tier large with BOTH mcpm fields resident, all three poses; numbers feed Task 7 (spend the savings) and the deferred multi-cube-single-march spec. T6's own gate is unchanged (medium like-for-like vs T1/T5 + large-tier single-field). If the 2mrs entry lands default-off, add a setVolumeField seam to window.__skymapPerf first (mcpm precedent: visible:true default-on needs no seam). T6 implementer confirmed still RUNNING 2026-08-19; debug return reverted + comments trimmed in its working diff.
Ruling (user request 2026-08-19b): scale-3 numbers sit near the pass floor and under-stress the algorithm. Add a STRESS perf pass: temporarily patch renderTargets.ts 'volume' row scale 3->1 (IDENTICALLY in both A/B worktrees, uncommitted, reverted after) + --tier large, volume-inside + full-survey. Queued BEHIND the running large-tier measurement agent (concurrent perf runs corrupt each other). Stress numbers feed Task 7 (spend-the-savings = likely raising volume resolution). Full matrix (two MCPM fields x large tier x scale 1&3) still pending 2MRS field landing on main.
Task 6 implementer: DONE (commit 0218c610e; 1023 files/6903 tests green; volume-inside volume·COSMO 2.2->1.4ms paired A/B vs T5, TOTAL 9.8->8.8ms; full-survey floor-dominated unchanged; STEP_QUALITY=2.0 kept vs 1.0 spot-check +68% cost for indistinguishable visuals). Review dispatched (opus) + large-tier measurement agent dispatched (sonnet) in parallel; SendMessage resume of original implementer failed (no transcript) — fresh measurement agent instead.
Large-tier addendum (measurement agent): volume-inside volume·COSMO T5 2.2 -> T6 2.8ms (+27%, stable 3 rounds) — OPPOSITE of medium tier's 2.2->1.4 improvement; genuine tier-dependent effect. full-survey floor-dominated both tiers. Tier read-back `large` confirmed on all 12 reports.
Stress three-way dispatched (sonnet): legs C=679de1bef pre-accel / A=34d612772 T5 / B=0218c610e T6, --tier large, scale 3 (C only + B sentinel) and scale 1 (all legs, renderTargets.ts volume row patched 3->1 identically, reverted after). Results -> task-6-report.md Stress addendum.
Task 6 review: Spec 14/16 (2 ⚠️: wesl-skill invocation unverifiable — dead first session; visual evidence static/medium/two-pose). Quality CHANGES REQUESTED — 3 Important: (1) volumeFieldRenderer.ts:129-135 sampler omits mipmapFilter -> 'nearest', fractional LOD snaps between mips (hard shells; violates no-popping criterion); (2) fragment.wesl:394 STEP_COUNT=256 truncates rays at tier large (dims 600: 256 steps reach t≈0.853 < cube span) — far half unsampled; fix = step floor (tMax-t)/(STEP_COUNT-i) or raise cap; (3) :427 direct skip jump resets jitter phase -> correlated banding. 4 minors deferred (diff comment budget 71c/27code w/ concrete trims, "provably empty" needs at-LOD-0 qualifier, anisotropic-cube approx undocumented, last-step overshoot unclamped). Reviewer also notes: at volume-inside/medium lod pins to 0 cube-wide, so T6's medium win is step-sizing+skip-throttle, NOT mip sampling. Skip-stride/phase questions came back clean (integral-neutral, deferral ≤3 iters).
Task 6: fix round 1/5 QUEUED behind stress measurement agent (same worktree + :5175 HMR — concurrent edit would corrupt leg B). Original implementer unresumable (no transcript); fix goes to a fresh implementer w/ brief+report+review paths. NOTE for fix: cap fix will CHANGE large-tier perf (currently truncating = artificially cheap); re-measure large tier after fix round.
Stress three-way (large tier, volume-inside): scale 3 — C(pre-accel 679de1bef) 1.25ms / A(T5) 2.2 / B(T6) 2.8~1.4 (B sentinel drifted 2x, noise ceiling). scale 1 — C 8.78 / A 16.94 / B 10.35 (4/5 rounds tight; 1 outlier 19.89 discarded). C < B < A at BOTH scales: the acceleration stack is net-NEGATIVE at the dense pose (A +93% over C under stress; B recovers ~half, still +18% over C). full-survey: floor-pinned, undifferentiated, all builds/scales. Tier read-back large confirmed (34 reports). Cleanup verified (scale patch reverted, scratch trees removed). EVIDENCE GAP flagged: no skip-FAVORABLE in-volume pose exists (camera-in-void); volume-inside is deliberately adversarial, full-survey can't differentiate. Add void-inside pose to the post-fix matrix before T7 rules on the stack's value.
2MRS MCPM field MERGED to main (#577, per memory index polyphorm-2mrs) — two-field matrix unblocked; merge main into this branch AFTER fix round 1 + re-review.
Task 6 fix round 1 dispatched (fresh implementer, sonnet — original unresumable): 3 Importants (trilinear sampler, step-floor for STEP_COUNT truncation, post-skip jitter re-decorrelation) + large-tier re-measure (truncation made 2.8ms artificially cheap). Minors stay deferred.
Task 6: fix round 1/5 (3 addressed, 0 open — trilinear sampler; per-iter step floor (tMax-t)/(STEP_COUNT-i) kills large-tier truncation; post-skip jitter re-decorrelation; commits 0218c610e..ffad99458; suite 1023/6903 green; large-tier volume·COSMO back to ~parity w/ T5, median 2.13ms but BIMODAL 1.4/2.8/3.8 clusters — residual flagged, not in scope). Scoped re-review dispatched.
Task 6 re-review: all 3 ADDRESSED (trilinear volumeFieldRenderer.ts:132; step floor fragment.wesl:398-410 provably reaches tMax, alpha uses floored length; per-skip re-jitter :446-447 unbiased). New breakage: none. 2 nits (non-defects, noted): skip comment overclaims "already proved" past skipExitT; skipJitter scales by lodStepLength not floored stepLength.
Task 6: review clean after fix round 1. AWAITING USER visual ratification (plan-mandated checkpoint) before completion line + merge-from-main (merge held so :5175 keeps serving ffad99458 for the user's check).
Data variant note (2026-08-19): user clamped public/data/scalar-field/v3/polyphorm-2mrs-{small,medium,large}.scfd IN PLACE (~99% zero voxels; originals beside them as .scfd.bak, same size — distinguish by content only). mcpm-*.scfd untouched. 2mrs not on this branch yet, so :5175 / the T6 checkpoint is unaffected. All post-merge 2mrs numbers are CLAMPED-variant; label tables accordingly. Clamp rationale: TF-adaptive cutoff sits 0.05 under the deadband, so sub-deadband noise voxels held bricks unskippable — clamping frees them; expect skip-favorable behavior (and weaker saturation early-out for the pre-accel reference). .bak swap = same-build clamped-vs-original A/B recipe.
Task 6: complete (commits 34d612772..ffad99458, review clean after 1 fix round; USER ratified visual checkpoint 2026-08-19 "visuals are ok"; 4 minors deferred + 2 re-review nits noted). Next: merge origin/main (2 known conflicts scalarVolumeLayer.ts + test), then two-field matrix, then Task 7 with full dataset.
Merge origin/main: 88e34fb07 (conflicts scalarVolumeLayer.ts + test resolved — main's sizeOf('volume') structure kept, branch pixelConeTan re-grafted, formula verified identical; branch's min-1px-clamp test dropped, covered in renderTargets.test.ts). Suite 1046 files/7025 tests green, typecheck clean. polyphorm-2mrs: default-OFF (visible:false), id 'polyphorm-2mrs', loads polyphorm-2mrs-{tier}.scfd.
Two-field matrix dispatched: legs C'=origin/main a5c3527ae (pre-accel + 2mrs) vs B=88e34fb07, tier large, scales 3+1 (patched), fields mcpm + polyphorm-2mrs (visible:true patched BOTH legs, uncommitted), poses volume-inside + NEW void-inside scenario (committed to perfScenarios.ts; full-survey dropped — proven undifferentiating). 2mrs data = CLAMPED variant. Plus leg-B 2mrs-off-vs-on delta to isolate second-field cost. Results -> matrix-report.md.
PR #580 landed on main (6d8edfd26): tools-side --clamp for scfd imports, zeroing SUB-DEADBAND voxels (confirms clamp threshold = deadband, visually lossless by construction). Runtime-inert (tools/volumes + DATA.md only) — matrix legs unaffected; origin/main deliberately NOT re-fetched until matrix agent completes (its C' leg pins origin/main at creation). Freshening merge before final whole-branch review picks it up.
Matrix results (commit a3834fb9a adds void-inside scenario, target [-437.6,-405.3,405.7], derived by decoding mcpm-large.scfd — 0% nonzero on 5/6 axis rays to 300Mpc): volume·COSMO median, C'(pre-accel main) vs B(branch), two fields, tier large: void-inside — B WINS 44% (1.25 vs 2.23ms) at scale 3, 35% (18.22 vs 27.85) at scale 1. volume-inside — B LOSES 38% (2.97 vs 2.16) / 44% (24.58 vs 17.01). Direction consistent all rounds under contention. 2mrs on/off (B, scale3, n=2): +3.96ms total, lands in hdr·COSMO — directional only. VERDICT: stack pays for itself on sparse/clamped data, taxes dense poses; net value is pose-dependent. Full writeup matrix-report.md. Ruling: proceed to Task 7 per plan (its own decision rule + paired technique); adaptivity/dense-pose tax + multi-cube = follow-up spec territory, noted for final review + T8.
Task 7 BASE: a3834fb9a — implementer dispatched
Task 7 implementer: DONE (commit e8f26076e). Decision: REVERT (keep scale 3) per rule — volume-inside TOTAL merged +1.343ms (>1ms budget, stable across 6 rounds); local-group -0.131 / full-survey -1.475 pass. Diff = landmine comment only. Suite 1046/7025 green. Concern noted: cold-start perf chains spike 6-7x for ~2 rounds before settling; decision on post-settle regime. Review dispatched (sonnet) + Task 8 implementer dispatched in parallel (Rule 2, disjoint files).
Task 7 review: Spec PASS, quality Approve, 0 findings — rule application, arithmetic (incl. all-8-rounds sensitivity +1.16ms), and the 7-line landmine comment all verified.
Task 7: complete (commits a3834fb9a..e8f26076e, review clean; decision REVERT, scale stays 3)
Task 8 implementer: DONE (commit 074269dc8, docs-only, claims cross-checked vs code; typecheck green). Minor (deferred): renderTargets.ts pre-existing "192 raymarch steps" comment stale (actual 256, never matched) — flagged for final review fix wave. Review dispatched.
Task 8 review: Spec/style PASS, quality CHANGES REQUESTED — 1 Important ("CF-4 flow" misnames the field; renderer draws CF-4 DENSITY; Flow is a different subsystem) + 1 Minor (max pyramid base = 3 direct downsampleLevel3d calls, not generateMipChain3d). All other claims verified verbatim vs code. Ruling: the Minor rides along in fix round 1 — same docs-accuracy concern, same bullet; cost if wrong: none (docs-only). Fix round 1 dispatched (original implementer RESUMED via SendMessage — works on finished agents).
Task 8: fix round 1/5 (2 addressed, 0 open — CF-4 density rename + max-pyramid build-path attribution; commits 074269dc8..7acc2ce2d). Scoped re-review dispatched.
DECISION GATE (2026-08-19): user challenged whether landing is justified — at shipped defaults the branch is perf-neutral-to-negative (volume-inside 1.1 pre-plan -> ~1.4ms; wins only in sparse regimes; shimmer improvement claimed but unverified). Close-out chain HALTED. Options presented: (1) LOD-only ablation (skip machinery disabled) to isolate what's worth keeping — controller recommendation; (2) land as-is; (3) park branch, go to representation pivot (splatting / sparse-brick / skeleton-LOD). Awaiting user ruling.
Task 8 re-review: both ADDRESSED, verified vs source, no new breakage.
Task 8: complete (commits e8f26076e..7acc2ce2d, review clean after 1 fix round)
ALL 8 TASKS COMPLETE. Close-out chain (freshening merge, comment audit, final whole-branch review, /feature-done) remains HALTED at the DECISION GATE above — land / LOD-only ablation / park is the user's ruling.
RULING (user, 2026-08-19): PARKED. Draft PR #556 CLOSED with full measured conclusion written into it (numbers table, why-parked, reusable yield). Branch left unmerged on origin at 7acc2ce2d; worktree + this ledger left on disk. Dev server :5175 stopped. Salvage (perf-pose cherry-pick, archived patch in specs/completed) NOT executed — available on request. Companion outcome: CLAUDE.md code-is-liability convention drafted as PR #582 (b393f969e) + feedback_code_is_liability memory.
