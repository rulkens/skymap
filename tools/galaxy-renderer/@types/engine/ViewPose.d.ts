/**
 * ViewPose — spherical camera coordinates: azimuth, elevation, and distance
 * from the scene origin.
 */

export type ViewPose = { readonly az: number; readonly el: number; readonly dist: number };
