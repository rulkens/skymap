/** Keys of the raymarch layer's four ParamSlider rows — shared by ControlsPanel's
 * RAYMARCH_SLIDERS spec table and RAYMARCH_SETTERS lookup, so both stay total
 * over the same key set. */
export type RaymarchSliderKey = 'opticalThickness' | 'sampleWeight' | 'trimDensity' | 'stepVoxels';
