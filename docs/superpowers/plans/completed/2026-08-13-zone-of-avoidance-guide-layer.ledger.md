# SDD ledger — plan: docs/superpowers/plans/2026-08-13-zone-of-avoidance-guide-layer.md

Branch: worktree-zone-of-avoidance-guide-layer · Draft PR: #555 · Base at execution start: 4286db106

Pre-flight ruling (plan vs testing convention, user AFK with drive-to-gate mandate):
- Task 5's two `ZONE_OF_AVOIDANCE_INFO` constant-restatement tests and Task 6's
  `DETAIL_CARD['zoneOfAvoidance']` table-row test violate
  docs/superpowers/conventions/testing.md (no constant/registry restatements).
  Convention governs — those assertions are DROPPED. Task 6's behavioral tests
  (InfoCard renders the card; no Focus affordance) stay. Surface to user at the gate.

Execution scope this session: Tasks 1–8, then STOP at Task 9 (visual gate).
Subagent rules in force: sonnet implementers/reviewers (haiku for Task 1), never
Fable; sequential bash; no npm in subagents (npx tsc/vitest directly); main
session runs the npm suite between tasks; absolute worktree path in every brief.
Task 1: complete (commits 4286db1..628662a, review clean)
Task 2: fix round 1/5 (1 addressed — galaxyType exhaustive switch; commit d546176)
Task 2: fix round 2/5 dispatched (2 Important — stale bearsLabel claim in type header; header over 10-line budget)
Task 2: minor (deferred): zone-of-avoidance.ts divergence comment over half-code budget — trim to plan pointer
Task 2: fix round 2/5 (2 addressed, 0 open; commit 4d6ae03)
Task 2: complete (commits 628662a..4d6ae03, review clean, 1 minor deferred)
Task 3: complete (commit f5df516, review clean)
Task 4: complete (commit c8045f9, review clean, 1 minor deferred: ZoneOfAvoidanceSettings header 15 lines >10 budget)
Task 5: fix round 1/5 dispatched (1 Important — focusFraming throw reachable via wireInput onDoubleClick once Task 12 lands; fix = benign no-op, single enforcement site)
Task 5: fix round 1/5 (crash path addressed via saga-funnel filter, commit 8b34de7)
Task 5: fix round 2/5 dispatched (exhaustive ROW_FOCUSABLE table + no-op regression test)
Task 5: fix round 2/5 (2 addressed, 0 open; commit a1c8d0f — ROW_FOCUSABLE table + saga regression test)
Task 5: complete (commits a092c48..a1c8d0f, review clean)
Task 6: complete (commit 123b5af, review clean)
Task 7: complete (commit a4b518dd3, additional cascade beyond correction paragraph closed same-task — fadeRegistry switch, VISIBILITY_ACTION_ROW, 3 test fixtures; full suite 1025/1025 green; see task-7-report.md)
Task 7: complete (commit a4b518d, review clean — widen cascade into fadeRegistry/visibilityActionRow + 3 fixture leaves, all mirrored)
Task 8: fix round 1/5 dispatched (1 Important — radialFalloff normalized-vs-Mpc currency mismatch; CPU-side conversion, names track units)
Task 8: fix round 1/5 (1 addressed — radialFalloffMpc conversion at draw boundary; commit 1d18c6b)
Task 8: complete (commits 17155d0..1d18c6b, review clean)
Tasks 1-8 done; STOPPED at Task 9 visual gate per user mandate.
Task 8: fix round 2/5 dispatched (20 failures / 3 files — TIMED_SLOTS pin, renderFrame fixture undefined-vs-null guard, destroyReachability coverage). NOTE: controller's earlier 'suite green' before gate was premature — tail cut the failure summary; verify counts, not tails.
Task 8: fix round 2/5 (3 addressed — TIMED_SLOTS pin, fixture null completion + guard convention, destroyReachability mock; commit e5232c7)
Task 8: complete (commits 17155d0..e5232c7, review clean). Controller-verified suite: 1025 files / 6909 tests green.
GATE STAGED: dev server http://localhost:5174 (shell bl779llji, this worktree). Awaiting user visual pass = plan Task 9. Post-gate remainder: 10 (curved text) -> gate 11 -> 12/13/14 -> 15. Compact-safe (resume: .superpowers/sdd/2026-08-13-zone-of-avoidance-guide-layer/progress.md)
Task 9 GATE FEEDBACK (2026-08-13): (a) DEFECT — band reads as dome-painted sky-band, not extruded wedge: fragment.wesl evaluates ONE sphere-surface hit; fix = 32-step ray march accumulating latMask*radialRim along the shell segment, per-sample radial rims replace the camera-distance radialMask (brief: gate-fix-wedge-brief.md, BASE e5232c7ac). (b) User pulled remaining tasks forward: run 13 (debug GUI), 10 (lettering), 14 (settings toggle), 12 (pick) now, one combined visual pass at the end (collapses gates 9+11). Picking/text/toggle "missing" = tasks not yet run, not defects. Source.ZoneOfAvoidance question answered (identity row, not data source); no objection raised.
Gate-fix wedge: complete (commit 2b6088b9f, review clean 0C/0I/2M; npm run build green = WESL links). Minors (deferred): io.wesl docblock still says "two concentric spheres" (inner cavity is now a per-sample mask); smoothstep zero-width UB if radialFalloffMpc==0 (unreachable, pre-existing).
Task 13: complete (commit ee518fc4e, review clean 0C/0I/2M; parity tests 3/3, tsc clean both configs). Minors (deferred): ZoneOfAvoidanceTuningSection header 15 lines >10 budget (mirrors MilkyWay's over-budget header); color-channel onChange copies full tuple per keystroke (correct, heavier pattern).
Task 10: implemented DONE_WITH_CONCERNS (commit 909814c80; vite build + full vitest 1026/6912 green per implementer; concern = visual pass, covered by combined gate). Design calls: reading direction galacticLonRad - arcRad (sign flip is one line if mirrored); additive label blend (Apple-Silicon OVER/HDR bug); placeholders LABEL_RADIUS_MPC=40, LABEL_EM_MPC=2. Reviewer dispatched bg. Order pulled forward per gate feedback: 13 -> 10 -> 14 -> 12, one combined visual pass at end.
Task 14: complete (commit 227d2fbbe, review clean 0C/0I/2M; suite 1026/6912 + tsc independently re-verified by reviewer; fixture edits verified legitimate compile-closure). Minors (deferred): labelCategories.ts "Five kinds" doc now stale (sixth kind); band + label toggles both render "Zone of Avoidance" — check distinguishability at visual pass.
Task 10 review: spec all-pass (bandAxes sign independently re-derived; 112-byte ABI + 36-byte stride byte-checked), 1 Important + 2 Minor. IMPORTANT (fix round 1 queued behind gate-fix-2 implementer): comment budget blown in label/{io,vertex,fragment}.wesl (headers 27/12/10 lines, 71-74% comment-to-code vs <=50% cap) — trim to why-only + link spec. Minors (deferred): no parity test on glyph-instance/uniform byte-offset arithmetic (zoneOfAvoidanceRenderer.ts:495-524); layer enabled()/draw() recompute both opacities (4 calls/frame, harmless, pre-existing pattern).
Gate-fix 2 (live user feedback, ONE commit, brief gate-fix-radial-fade-brief.md): (1) radial density = exponential decay with distance, radialFalloff knob becomes e-folding fraction, default 0.1->0.35; (2) approach fade derives from MILKY_WAY_RADIUS_MPC (goneAt x2, fullAt x10) replacing 0.3/8; (3) half-widths bulge 15->10 deg, anticenter 5->3 deg (user likes the cosine variation, keep it). Implemented DONE (commit 2cf85e3ee; tsc + 2 targeted vitest files + vite build green). Reviewer dispatched bg.
Task 10: complete (commits 909814c80 + 9d00b9b93; review spec all-pass, Important budget finding ADDRESSED in re-review, comments-only confirmed, load-bearing notes preserved). Minors (deferred): no parity test on glyph-instance/uniform byte-offset arithmetic; layer recomputes opacities in enabled()+draw().
Gate-fix 2: complete (commits 2cf85e3ee + b37fb1f58; review 10/10 spec pass; both comment findings ADDRESSED in re-review, comments-only confirmed). Reviewer's "3 dirty label files" note = timing artifact (trim was mid-flight, since committed; tree verified clean).
Gate-fix 3 (user): color picker replaces RGB slider rows (plan-sanctioned alternative; sRGB<->linear at widget boundary). Implemented DONE (commit 74f22aa8a; tsc + 13/13 tests incl. 2 new util test files). Reviewer dispatched bg. Concern noted: 8-bit hex quantization of dim default not browser-checked (sub-perceptual expected).
Gate-fix 4: complete (commit c51b316b6, review CLEAN 0C/0I/0M, independently verified in isolated worktree; lowercase confirmed covered by ASCII_PRINTABLE atlas charset).
Gate-fix 7 (user): band fades OUT once Local Group in view — visibility WINDOW via new zoneOfAvoidanceRecede band row x approach x layerFade in zoneOfAvoidanceLayerOpacity (derive from regionById local-group extent if present, else 2/6 Mpc literals). Brief gate-fix-fade-window-brief.md. Dispatch order after single-toggle: DebugPanel round -> fade-window -> lowres -> Task 12.
Gate-fix 5 (user): "two toggles, neither works, want ONE". ROOT CAUSE DIAGNOSED BY CONTROLLER: FADE_ROW in watchFadesSaga.ts has no zoneOfAvoidance rows — Task 7 added fadeLayers.ts rows but never the trigger; toggles write store, syncFades never fires, opacity stays boot-seeded 1. Fix = one FADE_ROW entry + DELETE labelEnabled entirely (state/fade row/'zoneOfAvoidanceLabel' key/'zoneOfAvoidance' LabelLayerId/Task 14 label-machinery reversal, bearsLabel back to false; hand-authored band row stays as THE toggle). Grill Q7's independent-toggles decision superseded by this gate feedback. Implemented DONE (commit 8fffc59eb, 25 files +46/-109; suite 1028/6926 + tsc both configs; FADE_ROW regression test MUTATION-VERIFIED). Review: spec all-pass, 2 Important (stale headers: ZoneOfAvoidanceSourceEntry.d.ts still says label-bearing; ZoneOfAvoidanceTuning.d.ts still names labelEnabled) + 1 Minor (watchFadesSaga.test stray blank line, prettier). Fix round 1/5 queued behind DebugPanel round.
DebugPanel round: DONE (f5b3582c3 review fixes, 5a9e05b57 edgeSharpness 5.0, 9f86f3c96 CopyButton+formatter; tsc both configs, 93/93 targeted tests). Accepted deviation: Copy button always-enabled whole-cluster emit (coordinator-specified, unlike MilkyWay's diffed button). Re-review: alignment + stale-sentence ADDRESSED; edgeSharpness + CopyButton commits spec-pass (round-trip test genuinely falsifiable; 93/93 re-verified). 1 Important SURVIVES: linearRgbToHex.ts comment:code 0.692 > 0.5 (report claimed ~0.5 — papering). Minor: TuningSection header 16 lines, grew when touched. Round 2 (two-file comment trim) queued behind fade-window. Deferred minor: no rendered-DOM test for the tuning section (pre-existing). LESSON for briefs: implementers must COUNT budget ratios, not estimate.
Gate-fix 5: complete (commits 8fffc59eb + d314a28b2; doc sweep controller-verified in the diff — comments/whitespace only, all 3 findings fixed).
Gate-fix 7 (fade window): implemented DONE (commit 58c83b71a; suite 1029/6933 + tsc both configs; no local-group region exists -> literal {fullAt: 2, goneAt: 6} Mpc fallback, needs visual tune). Reviewer dispatched bg.

== RESUME MAP (2026-08-13, pre-compact) ==
HEAD 58c83b71a on branch worktree-zone-of-avoidance-guide-layer, PR #555 draft, pushed through e5232c7ac only — PUSH PENDING. Dev server http://localhost:5174 (shell bl779llji — do not kill).
Picker round 2: complete (commit 6aafed22e, counted ratios 0.308 + 5-line header, tsc+prettier clean; no re-review needed, counts comply). Gate-fix 3 CLOSED.
Gate-fix 7 review: spec 5/5 pass (no-local-group verified; edge order + one-home + both-edge tests confirmed). 1 Important: zoneOfAvoidanceLayerOpacity.ts header 12 lines (2 over). ⚠️ same file has PRE-EXISTING false sentence ("two different layerFadeOpacity inputs — band's vs label's" — draw reuses one). MICRO-ROUND queued after lowres (one haiku agent, TWO commits): (a) comments-only — zoneOfAvoidanceLayerOpacity.ts header trim <=10 + fix false "two different layerFadeOpacity inputs" sentence; (b) USER-PASTED DEFAULTS (2026-08-13, from Copy button) into DEFAULT_ZONE_OF_AVOIDANCE_TUNING in src/data/defaults.ts, verbatim: intensity 0.37, radialFalloff 0.46, edgeSharpness 5, color [0.5333, 0.5089, 1], labelColor [0.2307, 0.2502, 0.6795] — commit `feat(zoneOfAvoidance): user-tuned defaults from the visual pass`; slider parity test must still span the new values (widen ranges if needed, never the defaults). Other ⚠️: scaleFadeBands/zoneOfAvoidanceLayer already over budget pre-existing (defer to final review); {2,6} literals await visual tune.
LIVE AGENT: lowres implementer (brief gate-fix-lowres-brief.md; on completion -> review-package BASE 6aafed22e -> sonnet reviewer -> opacity-header micro-round -> Task 12).
 3. Task 12 analytic pick: brief task-12-brief.md (+ note: band opacity now windowed; drawPick discards where alpha<=0 — unchanged contract). sonnet + review.
 4. Push branch, then COMBINED VISUAL PASS (task list #10): wedge look, window fade both ends, lettering (chording!), single toggle works, picker/Copy button, pick->InfoCard, perf acceptable.
 5. User tuning loop: user pastes Copy-defaults blob -> write into DEFAULT_ZONE_OF_AVOIDANCE_TUNING (+ scaleFadeBands/layer consts if included) as its own commit.
 6. Then Task 15 finalization (spec gate-amendment appendix: single toggle, labelColor, mixed-case, e-folding falloff, MW-scaled fade-in, LG fade-out window, lowres offscreen, picker; entanglement-radar; /feature-done) + final whole-branch review (opus) + finishing-a-development-branch.
STANDING RULES THIS SESSION: max delegation (user: run as much as possible in subagents — memory feedback_minimize_main_context); implementers sequential; reviews pipelined bg; briefs on disk are the single source; report counts not tails.
Gate-fix 6 (user): "performance is pretty horrendous" — band is fullscreen 32-step march at full res. Scout mapped the route; brief WRITTEN (gate-fix-lowres-brief.md): dedicated 'zoa' offscreen (scale 5, rgba16float) + third createAdditiveUpsample instance + liveness helper + producer/upsample layer split; lettering STAYS full-res in the hdr pass; do NOT share the 'volume' target (deriveVolumeLiveness braid). Dispatch after gate-fix 5 + color-picker fix round.
Color-picker review: spec 10/10 pass, 3 Important (picker row missing .readout spacer breaks slider alignment; both new color utils over comment budget; stale "three bespoke rows" sentence in zoneOfAvoidanceSliderFields.ts header) + 2 Minor (redundant cast; no component-level picker test — deferred). Fix round 1/5 queued behind gate-fix 5; SAME dispatch also carries user tuning: edgeSharpness default -> 5.0, and UN-DEFER the CopyButton/formatZoneOfAvoidanceTuningDefaults mirror (user asked "how do i easily give you defaults for the colours" — copy-paste loop), as separate commits.

- 2026-08-13: task-12 premise scout dispatched (read-only, parallel to live lowres implementer). On completion: fold task-12-premise-check.md corrections into task-12-brief.md (stale claims + post-lowres re-verifies), then Task 12 dispatches after lowres review + haiku micro-round clear. Implementers stay strictly sequential (shared git index; lowres is restructuring zoneOfAvoidanceLayer.ts, which Task 12's pick draw touches).
- 2026-08-13: task-12 premise scout DONE — 22 verified / 0 stale / 3 re-verify-post-lowres / 7 silent-brief symbols; persisted to task-12-premise-check.md (scout was read-only, orchestrator wrote it). Task 12 dispatch must hand the implementer BOTH task-12-brief.md and task-12-premise-check.md, with the 3 re-verify items called out (drawPick lands on the PRODUCER layer post-split; shape constants may move; opacity gate relocates to zoneOfAvoidanceLiveness.ts).
- 2026-08-13: lowres DONE — commit 520fbdcac (BASE 6aafed22e), full suite 1032 files/6950 tests green, vite build clean. Implementer flags: liveness mirrors deriveMilkyWayCloudAlpha (no renderer-null gate); zoa pass opens whenever camera in window → renderFrame/renderTargets count assertions updated (10→11 offscreen); PASS_GROUP_TITLES 'zoa·COSMO' added. Report: gate-fix-lowres-report.md.
- 2026-08-13: DISPATCHED in parallel: (a) sonnet reviewer on review-6aafed22e..520fbdcac.diff → gate-fix-lowres-review.md; (b) haiku micro-round implementer (BASE 520fbdcac, brief gate-fix-micro-round-brief.md: commit A opacity-header comment fix, commit B user defaults verbatim) → gate-fix-micro-round-report.md. Handling: lowres review findings → fix loop as usual (resume lowres agent rounds 1-3); micro-round report → verify 2 commits + defaults verbatim, no separate reviewer needed beyond a diff glance unless tests changed. After both clear: Task 12 dispatch (task-12-brief.md + task-12-premise-check.md, BASE recorded at dispatch), then push (remote at e5232c7ac), then combined visual pass.
- 2026-08-16 (new session, resumed): micro-round verified complete in git (78b84b776 comments + 9ad74db6c user defaults verbatim) — gate-fix micro-round CLOSED. Lowres reviewer from 08-13 never landed its report (session ended); RE-DISPATCHED sonnet reviewer bg on review-6aafed22e..520fbdcac.diff → gate-fix-lowres-review.md. Task 12 dispatches after that review clears (shared-file sequencing per resume map). Push still pending (remote at e5232c7ac, 19 commits behind); gh auth currently broken (401) — user must gh auth login before PR ops.
- Lowres review LANDED (gate-fix-lowres-review.md): Needs fixes. 2 Important: (a) comment budget 2-5x over on all three new ZoA files (counted); (b) deriveZoneOfAvoidanceLiveness missing renderer-null gate — brief mandated volumeLiveness.ts mirror, implementer swapped in deriveMilkyWayCloudAlpha shape (disclosed). Finding agrees with the brief — straight fix, no ruling needed. Minors (deferred): report texture-count claim said 10→11, diff shows 11→12 (doc-only); zoa producer layer not excluded from passOverrides.allNames (matches pre-existing star-aggregates/mw-aggregate inconsistency); visual acceptance covered by combined pass. Fix round 1/5 dispatching: FRESH sonnet implementer (original 08-13 agent lost with its session), FIX_BASE 9ad74db6c.
- STANDING RULE (user, 2026-08-16): implementer agents are writing task numbers / gate-fix names into code comments — forbidden (comments timeless; history is git's job). Every future dispatch brief must state: no task/plan/round references in comments. Fix-round-1 implementer (live) predates the rule — strip such comments in its round before re-review; re-reviewer checks for them explicitly.
- Fix round 1/5 LANDED (fresh sonnet): 2 addressed — comment budgets counted (liveness 0.455, layer 0.429, upsample 0.483, headers ≤10) commit 6b988c6c7; renderer-null gate + falsifiable null test + 4 exact-count assertion updates commit 66c280c55. tsc both configs + 146 targeted + 1778 engine-sweep green. Concern noted: budget+gate entangled in liveness commit split (accepted — header claim changed with the gate).
- Round 2 (user rule, same agent resumed): strip branch-introduced task/gate-fix references from comments across ~17 files (list in dispatch; pre-existing other-plan references left alone). Comments-only, one commit. Re-review after covers BOTH rounds: FIX_BASE 9ad74db6c.
- Round 2 LANDED (commit 1af7e26e4): 28 branch-introduced task/round references rewritten timeless across 17 files, comments/strings only; pre-existing other-plan references verified against base diff and left (incl. 2 extra hits beyond the dispatch list). tsc both configs + 7 test files / 87 tests + prettier clean; budget ratios unchanged. Scoped re-review DISPATCHED (sonnet bg) on review-9ad74db6c..1af7e26e4.diff covering both rounds. On clear: run full suite in main session, then Task 12 dispatch (brief + premise-check, no-task-numbers rule in brief).
- Controller-verified full suite at 1af7e26e4: 1032 files / 6951 tests green (counts, not tail).
- Re-review CLEAN: budget counts independently recounted (10/10/10 headers at cap; ratios 0.455/0.457/0.483); renderer gate mirrors volumeLiveness with falsifiable null test (verified by running it); renderFrame 5→4 pass-count updates encode real new behavior (null-renderer fixture); round-2 commit verified comments/strings-only with full straggler grep — no content loss.
- Gate-fix lowres: COMPLETE (commits 520fbdcac + 6b988c6c7 + 66c280c55 + 1af7e26e4, review clean after 2 rounds). Suite 1032/6951 green at 1af7e26e4.
- Task 12 dispatching: BASE 1af7e26e4, sonnet implementer, inputs task-12-brief.md + task-12-premise-check.md (3 re-verify items: drawPick lands on PRODUCER layer post-split; shape constants location; opacity gate now in zoneOfAvoidanceLiveness.ts WITH renderer-null gate). Brief carries no-task-numbers-in-comments rule.
- Task 12: implemented DONE_WITH_CONCERNS (commit 0b863eeeb; 2199 tests / 288 files targeted + both tsc configs green, controller re-verified tsc BOTH_CLEAN; editor drawPick diagnostic was stale). Implementer's key find: pick program groups by registry order, band layer sat before pointSpritesLayer → drawPick would read unbound @group(0) camera; fixed by CONTENT_LAYERS reorder + locked-test update (12 pickables). Concerns (both covered by combined visual pass): no browser acceptance of click-to-select; fragmentPick.wesl runtime shader compile unproven by build. Reviewer dispatched bg (sonnet) on review-1af7e26e4..0b863eeeb.diff; full suite running in main session.
- Controller-verified full suite at 0b863eeeb: 1033 files / 6958 tests green (counts).
- Task 12 review: spec mostly-compliant (both named risks verified: pick-pass @group(0) reorder correct+necessary; selection round-trip decodes to the zoneOfAvoidance arm); visual acceptance honestly deferred to combined pass. 2 Important (both comment-convention): "the brief" reference in zoneOfAvoidanceRenderer.test.ts:71; renderer module header ~68 lines vs ≤10 (grew +10 this diff). Minors (deferred): pick-depth tStart occlusion untested; pre-existing over-budget ratios in io.wesl + passes/index.ts. Fix round 1/5: original implementer resumed with both findings.
- Task 12 fix round 1/5 (2 addressed per implementer — test comment made timeless, renderer header to budget; commit b3fa2dbc7, comments-only, renderer test 3/3 + tsc both + prettier + 46/46 sanity). Scoped re-review dispatched bg (sonnet) on review-0b863eeeb..b3fa2dbc7.diff. On clear: Task 12 complete → push branch (remote at e5232c7ac) → combined visual pass (user gate).
- Task 12: complete (commits 0b863eeeb..b3fa2dbc7, review clean after 1 round; re-reviewer independently recounted: header exactly 10, ratio 0.263, comments-only hunk-by-hunk; io.wesl byte-table pointer verified non-dangling). ALL implementation tasks done. Next: push → COMBINED VISUAL PASS (user gate: wedge look, window fade both ends incl {2,6} Mpc literals, curved lettering chording, single toggle, picker/Copy, pick→InfoCard incl fragmentPick.wesl runtime compile, perf) → tuning loop → Task 15 finalization.
- COMBINED VISUAL PASS feedback 1 (user, 2026-08-17): ZoA FLICKERS in the pick debug overlay while orbiting — present some frames, absent others. DEFECT. Systematic-debugging in force: diagnosis scout dispatched (read-only, sonnet) before any fix; suspects to check (not conclusions): pick program registry-order grouping + camera bind, zoa offscreen liveness/stale-offscreen gating vs pick iteration, ping-pong/dual-pulse frame landmines, enabled() recompute with time-dependent inputs. Scout writes zoa-pick-flicker-diagnosis.md.
- VISUAL PASS feedback 2 (user): artifact LINES converging at the ZENITH. Second read-only scout dispatched in parallel → zoa-zenith-lines-diagnosis.md (hypotheses ranked by predicted convergence point: world zenith vs galactic pole vs camera-dependent — user-checkable discriminator; candidates: pole singularity in atan2/asin lat mask, unjittered march samples aliasing into ray-aligned streaks, degenerate label glyph quads, upsample streaking).
- User CORRECTION on feedback 2: the zenith artifact lines are IN THE PICK OVERLAY too (not the hdr band). Both visual-pass findings are pick-path. Raises single-root-cause hypothesis: drawPick reading stale/mis-bound camera or uniforms → flicker (presence nondeterminism) + zenith streaks (garbage geometry) together. Zenith scout redirect queued for when it stops (no messaging running subagents): retarget fragmentPick.wesl + pick uniform write path; its hdr-march analysis becomes secondary.
- User observation: on present frames the pick wedge SHAPE IS CORRECT, zenith lines are extras. Rules out wholesale uniform corruption → back to TWO distinct causes: flicker = pick-draw scheduling/binding per frame; zenith lines = edge case in fragmentPick's analytic test (pole singularity / near-tangent shell hits), consistent with hdr march being clean.
- Zenith-lines diagnosis LANDED (zoa-zenith-lines-diagnosis.md): asin(z/r) domain violation in a <0.01 deg cone at the galactic pole — f32 rounding pushes |z/r| to 1.00000008, WGSL asin → NaN, and `alpha <= 0.0` discard is IEEE-false for NaN so poisoned fragments write full pick ids. Pick-only because full-res (25x fragments) + r32uint winner-take-all vs diluted 1/5-res additive HDR. Ruled out: camera-basis rays (byte-identical to horizonShell, clean), labels, jitter, upsample. FIX DISPATCHED (sonnet, gate-fix-pick-pole-clamp): clamp asin args in both fragments + TS-twin check.
- Flicker diagnosis LANDED (zoa-pick-flicker-diagnosis.md): pick treats continuous fade opacity as binary alpha>0 with no floor — razor-thin oscillation at the 2-6 Mpc recede edge is invisible in HDR (blend weight) but hard on/off in pick. Structurally explains ZoA-only asymmetry (sole faded COSMO layer with drawPick). NOT YET CONFIRMED: needs user answer — was the camera near the Local-Group frame-up when flickering? If far out at full opacity, hypothesis fails → instrument per diagnosis doc. Fix design (pending confirm): perceptual pick floor (alpha >= small epsilon), keeps opacity-0 house rule.
- Pole-clamp fix LANDED (commit 139553b0c): asin args clamped at fragment.wesl:79 + fragmentPick.wesl:74 (only two latitude sites); TS twin has no asin (longitude-only cosine) — untouched. vite build green. Reviewer dispatched bg (sonnet, review-b3fa2dbc7..139553b0c.diff). Minor (deferred): fragment.wesl ratio 0.56 pre-existing → 0.59 with the why-comment. USER TO RE-CHECK pick overlay near the pole after refresh. Flicker fix still awaits user's distance-regime answer.
- Pole-clamp review CLEAN (0C/0I/0M): call-site completeness grep-verified (2 of 2), clamp identity inside domain, timeless comments, TS twin verdict confirmed by direct read; matches analyticSphere.wesl:170 precedent. Gate-fix pole-clamp COMPLETE (commit 139553b0c). Awaiting user: (a) pick-overlay re-check near pole post-refresh; (b) flicker distance-regime answer.
- USER (mid-pass): flicker persists AT FULL OPACITY anywhere the band is visible — knife-edge-opacity hypothesis DEAD. Lines also unchanged post-clamp, BUT dev-server log shows no shader reload event after 139553b0c: pipelines built once at initGpu, soft HMR likely never rebuilt the pick pipeline → clamp possibly not live in user's session; alternatively lines are FOSSIL ids in an uncleared pick target written by the old NaN path. User asked for hard-reload discriminator (fresh session: lines gone = stale pipeline/fossil; lines present = clamp hypothesis wrong). ALSO server log shows repeated React max-update-depth errors killing glade/milliquas/gaiaStars/mcpm loads — possible separate defect, user asked if visible. ESCALATED diagnosis dispatching (fresh agent, opus): pick-pass per-frame lifecycle — ping-pong targets, clear-vs-load ops, what gates band drawPick per frame; dual-pulse landmine in scope.
- USER: hard reload did NOT remove the pole lines — fresh session still shows them. Fossil-texture and stale-pipeline premises DEAD; the asin/NaN clamp (139553b0c, kept — real latent edge case, review-clean) was NOT the cause of the visible lines. Lines have a live cause in the current pick path. Failed-fix count for the lines: 1. Opus deep-diagnosis (in flight) covers the remaining candidates: pick-target lifecycle/clear, pick cadence + band drawPick gating, depth state, uniform sharing draw()/drawPick().

== RESUME MAP (2026-08-17, pre-compact) ==
HEAD 139553b0c on worktree-zone-of-avoidance-guide-layer, tree clean. Pushed through b3fa2dbc7 (PR #555 draft); 139553b0c NOT yet pushed. Dev server http://localhost:5174 (shell b38mm8s5k — do not kill). gh CLI auth broken (401) — user runs `gh auth login` before PR ops; plain git push works.
LIVE AGENT (only one): opus deep-diagnosis of the two pick-path defects → writes zoa-pick-path-diagnosis-2.md. HANDLING on completion: read its final message + doc; fold in the user data it predates (hard reload did NOT clear the lines — fossil/stale-pipeline dead, live cause); verify each root cause explains BAND-ONLY flicker at full opacity + persistent pole lines + clean HDR; then write gate-fix brief(s) in the SDD workspace and dispatch fix implementers SEQUENTIALLY (sonnet, no-task-numbers-in-comments rule in every brief), each with scoped review; user re-checks overlay after each.
QUEUE after pick defects clear: (1) push accumulated commits; (2) user continues combined visual pass checklist (wedge, fade window {2,6} Mpc literals, lettering chording, single toggle, picker/Copy, pick→InfoCard, perf); (3) user pastes Copy-defaults blob → own commit into DEFAULT_ZONE_OF_AVOIDANCE_TUNING; (4) Task 15 finalization: spec gate-amendment appendix, entanglement-radar, final whole-branch review (opus), /feature-done, finishing-a-development-branch.
OPEN with user: does the max-update-depth loading failure (glade/milliquas/gaiaStars/mcpm, repeating in dev-server log) show on screen? Possible separate defect, not yet triaged — if confirmed visible, diagnose on its own track (may predate branch; check main).
Failed-fix counts: lines 1 (asin clamp kept, not the cause), flicker 0. Systematic-debugging in force: no fix without the diagnosis pinning root cause.
- OPUS DIAGNOSIS LANDED (zoa-pick-path-diagnosis-2.md): FLICKER = depth bug, HIGH confidence — tStart=max(outerHits.x,0)=0 with camera always inside the 380 Mpc shell → hitPoint degenerates to camera → worldToClip w≈0 → depth ±Inf → 0.0/1.0 chaotic per pose (19% fail sim, scratchpad/depthSim.mjs); depth 1.0 loses `less` vs 1.0 clear → whole band absent that frame; single shared depth explains all-or-nothing/band-only/opacity-independent/HDR-clean. LINES top hypothesis (med-high): NOT the band's — another pick layer (band can't partially survive); user discriminator pending: toggle ZoA off, lines persist? SEPARATE confirmed bug: no alpha floor → 1.6e-9 tail clickable to |b|≈48° vs ~10° wedge + band depth-0 steals picks from disks/markers. asin/NaN DEAD as line cause (0/1.2M f32 sim). Fossil texture DEAD (clears every open, no ping-pong); stale pipeline DEAD. Latent: shared draw/drawPick uniform buffer → pick silhouette on slightly different aspect (inactive).
- Gate-fix pick-depth-floor DISPATCHED (sonnet; brief gate-fix-pick-depth-floor-brief.md, BASE 139553b0c): (1) constant far depth — band = last-resort background pick; (2) density-mask floor for footprint; (3) latent uniform-sharing if trivial. Lines triage awaits user's ZoA-off toggle check.
- Pick depth+floor fix LANDED (2c68a326d depth: PICK_FALLBACK_DEPTH=0.999999 last-resort background pick; ab8ed7d4c footprint floor). Latent uniform-sharing: report-only, re-verified inactive (existing test pins draw's downsampled-viewport contract). vite build + both tsc clean. Reviewer dispatched bg (sonnet, review-139553b0c..ab8ed7d4c.diff). USER re-checks after hard reload: flicker gone all poses; band pickable; foreground galaxy wins; footprint hugs wedge. STILL OPEN: ZoA-off toggle → lines persist? (decides separate-layer triage). Implementer concern noted: 0.999999 loses to real objects only out to ~10,000 Mpc (fine for content).
- USER CONFIRMS: flicker AND pole lines both GONE after depth+floor fix. Lines were the band's own no-floor tails after all (opus runner-up hypothesis correct, not the separate-layer top hypothesis) — jitter-stippled 1e-9 density tails to |b|≈48° reading as pole-convergent streaks; ZoA-off toggle check no longer needed. Both pick defects CLOSED pending review + remaining browser checks (foreground-galaxy-wins, click→InfoCard).
- Copy commit 8f3bf9386: both InfoCard strings rewritten (user-approved verbatim) — distanceNote now says sightlines-blocked-at-any-depth; LLM tells removed. InfoCard ZoA test 2/2 + prettier clean; no test pins strings. STANDING: copy gets a tells check before landing.
- Depth+floor review: APPROVED (0C/0I/2M). Independently re-verified: degenerate path deleted, depthCompare 'less' vs clear 1.0 chain read from source, DENSITY_MASK_FLOOR=0.02 floors PRE-intensity with alpha<=0 discard kept for toggle-off, commits cleanly split, uniform-deferral argument holds (test pins both viewports as deliberate contracts). Minors (deferred): fragmentPick.wesl header 11 lines (1 over budget, grew this fix); PICK_FALLBACK_DEPTH crossover ~10,000 Mpc has only ~15-18% headroom over milliquas real max ~8,600 Mpc — revisit if a higher-z catalog lands. Reviewer disclosed briefly spawning a subagent against rules before self-correcting (noted; review otherwise direct). Gate-fix pick-depth-floor COMPLETE.
- USER (visual pass): remove distanceNote ENTIRELY (irrelevant for ZoA — supersedes the eab69dcc6 shortening) + remove the detail card's thumb block (adds nothing). Sonnet implementer dispatched: type field + record + card markup/CSS + tests, one commit. ZoA-only; shared thumb machinery untouched.

== RESUME MAP (2026-08-17b, pre-compact) ==
HEAD eab69dcc6 (copy: one-sentence distance note — about to be superseded); pushed through 8f3bf9386; eab69dcc6 + the in-flight slimming commit NOT pushed. Dev server http://localhost:5174 (shell b38mm8s5k). gh CLI 401 (plain git push works).
LIVE AGENT (only one): card-slimming implementer (sonnet) — removes distanceNote (type+record+cards+tests) and ZoA thumb block, one commit, appends to task-12-report.md. HANDLING: verify final message + `git status` clean + read its report section; controller diff-glances the commit (small user-directed deletion — no separate reviewer; final whole-branch review covers it); user eyeballs the slimmed card via HMR; then push both unpushed commits.
VISUAL PASS state: CLOSED — pick flicker (depth fix 2c68a326d), pole lines (floor ab8ed7d4c), InfoCard copy (8f3bf9386+eab69dcc6+slimming). Review of depth+floor APPROVED 0C/0I/2M (minors ledgered above). REMAINING checklist: foreground-galaxy-beats-band click, click→InfoCard, wedge look, fade window ({2,6} Mpc literals await tune), lettering chording, single toggle, picker/Copy, perf feel. Then: user tuning blob → own commit; Task 15 finalization (spec gate-amendment appendix incl. pick depth/floor + card slimming, entanglement-radar, final whole-branch opus review, /feature-done, finishing-a-development-branch).
OPEN with user: max-update-depth loading errors (glade/milliquas/gaiaStars/mcpm in server log) — visible on screen? Separate triage if so.

== Card slimming (user-directed) complete — 2026-08-17 ==
Commit 92fb0c8d9 (sonnet implementer): distanceNote removed from ZoneOfAvoidanceInfo type + record + both cards; detail card's glyph/thumb block (cardTopRow + .glyph CSS) removed. Compact card's small "Hover" title icon deliberately kept (label icon, not a thumb slot; agent's call, controller agrees). tsc both configs clean; InfoCard.zoneOfAvoidance.test.ts 2/2; report appended to task-12-report.md. Controller diff-glanced: coherent, no strays. USER CONFIRMED the fix visually ("ok the fix is verified"). Pushed: remote now at 92fb0c8d9.
Copy/card arc CLOSED. Remaining queue unchanged: rest of combined visual-pass checklist, user tuning-defaults blob (if provided), Task 15 finalization (spec gate-amendment appendix incl. card slimming, entanglement-radar, final whole-branch review on opus, /feature-done, finishing-a-development-branch).

== Wikipedia link (user-directed) dispatched — 2026-08-17 ==
User asked for a Wikipedia link on the ZoA info card, layout mirrored from the sibling cards. Brief: card-wikipedia-link-brief.md (agent finds the sibling link pattern in MilkyWayDetailCard/GalaxyDetailCard and copies it exactly; one commit "feat(zoneOfAvoidance): detail card links the Wikipedia article"; report appends to task-12-report.md). LIVE AGENT: sonnet implementer, BASE 92fb0c8d9. Handling on completion: verify final message + clean tree, diff-glance the commit, push, user eyeballs via HMR. No separate reviewer — final whole-branch review covers it.

== Visual pass: fade window CONFIRMED — 2026-08-17 ==
User: "fade window feel feels good". The {2, 6} Mpc fade literals in DEFAULT_ZONE_OF_AVOIDANCE_TUNING are ACCEPTED AS-IS — no tuning-blob commit needed for the fade window. Checklist item closed; do not re-open or re-tune.
Visual-pass remaining: foreground-galaxy-beats-band click, band click -> InfoCard opens, wedge look (already confirmed earlier), lettering chording, single toggle, picker/Copy button, perf feel.

Fade-window literals located (for the record, controller-verified):
- src/services/engine/presentation/scaleFadeBands.ts:209 zoneOfAvoidance (approach) = { fullAt: MILKY_WAY_RADIUS_MPC*10, goneAt: MILKY_WAY_RADIUS_MPC*2 }
- src/services/engine/presentation/scaleFadeBands.ts:221 zoneOfAvoidanceRecede = { fullAt: 2, goneAt: 6 } Mpc
NOT in DEFAULT_ZONE_OF_AVOIDANCE_TUNING (that holds intensity/radialFalloff/edgeSharpness/color/labelColor only) — an earlier ledger line implying otherwise is corrected here.
DEFERRED MINOR (for final whole-branch review): both comments describe their numbers as an "eye-tuning STARTING POINT"; the values are now user-confirmed settled. Reword to drop the provisional hedge WITHOUT adding process/history ("confirmed in visual pass" would violate the timeless-comment rule). Not dispatched now: an implementer is live and implementers run strictly sequential.

== Wikipedia link complete — 2026-08-17 ==
Commit a98461d15 (sonnet implementer). Sibling pattern mirrored: WikipediaRow (src/components/InfoCard/WikipediaRow/WikipediaRow.tsx) as used by BodyDetailCard, driven by an explicit article-title field (BodyFacts.wikiTitle). Added `wikiTitle: 'Zone_of_Avoidance'` to the ZoneOfAvoidanceInfo type + record; WikipediaRow rendered in its own cardSection directly ABOVE the description. Compact hover card untouched (no sibling compact card shows the link).
Controller VERIFIED independently (not just agent-reported): diff read in full — coherent, no strays; `npx vitest run tests/components/InfoCard/InfoCard.zoneOfAvoidance.test.ts` -> 1 file / 2 tests passed, run by the controller itself. Test now asserts the rendered href is https://en.wikipedia.org/wiki/Zone_of_Avoidance. Pushed: remote at a98461d15.
AWAITING: user's visual eyeball of the link row via HMR.

== Task 15 finalization IN PROGRESS — 2026-08-17 ==
USER: "all visuals pass" — the ENTIRE manual smoke-test / visual checklist is CLOSED (band+lettering toggles, fade-in, no far-edge pop, band click -> InfoCard, DebugPanel knobs, wedge look, foreground-galaxy-beats-band, fade window, perf feel). Do not re-ask the user to look at any of these.
Branch scope for reviews: merge-base dbd93b98122fbaf47360b5ea00b148de961a05dc..HEAD = 50 commits, 101 files, +3883/-155.
Gates run by controller: `npm run typecheck` (both tsconfigs) CLEAN; `npm test` 1033 files / 6958 tests ALL PASSED; `npm run build` running.
LIVE AGENT: sonnet docs implementer (brief: task-15-finalization-brief.md) — Unit 1 scaleFadeBands comment reword (closes the deferred minor above), Unit 2 spec `## Amendments (as built)` appendix, verified-not-assumed, two commits. Handling: diff-glance, push, then dispatch the two reviews.
Briefs PRE-WRITTEN and ready to dispatch once docs commits land (substitute REVIEW_PACKAGE_PATH in each): final-review-brief.md (opus, whole-branch, LAST gate) and entanglement-radar-brief.md (opus, invokes the entanglement-radar skill). Both read-only, dispatched in parallel.
Then: adjudicate findings -> one fix dispatch if any must-fix -> /feature-done -> finishing-a-development-branch. NOTE gh CLI auth is BROKEN (HTTP 401); user must run `gh auth login` before any PR operation. Plain `git push` works.

PRE-EXISTING defect spotted while watching the docs agent edit (NOT this branch's, NOT to be fixed here):
scaleFadeBands.ts ~:44-51 — the "shape starBackdrop and bodyGlintBackdrop share" comment block is glued to the R₀ comment and sits above `const SGR_A_STAR_R0_MPC`, while the constants it actually describes (BACKDROP_FULL_AT_EXTENT_MULTIPLE / BACKDROP_GONE_AT_EXTENT_MULTIPLE) sit below with no comment. Controller VERIFIED it is present verbatim at merge-base dbd93b981 (i.e. it came from main), so per the never-sweep convention it stays out of this branch. Candidate one-line BACKLOG nit.
Gates: build ✓ (15.46s, exit 0). typecheck ✓. tests ✓ 1033/6958.
LIVE AGENTS (2): sonnet docs implementer (task-15-finalization-brief.md) + opus entanglement-radar (entanglement-radar-brief.md, read-only, diff at review-dbd93b981..a98461d15.diff). Final whole-branch review (opus, final-review-brief.md) dispatches AFTER the docs agent lands the spec appendix — it reads the appendix to tell ruled divergences from real findings.

== Task 15 docs commits landed — 2026-08-17 ==
73928cc4e scaleFadeBands comment reword (deferred minor CLOSED — controller read the diff: timeless, no process refs, derivation kept).
ce4388454 spec `## Amendments (as built)` — 11 amendments, each cited to a file/symbol; agent dropped ZERO candidates (all verified true) and ADDED two from the ledger: b_limit half-widths tightened to BULGE_DEG=10 / ANTICENTER_DEG=3 (spec said ~15/~5), and the radial profile is exponential decay not flat-plateau-with-eased-rims. Commit also carries incidental prettier markdown normalization of the spec.
NOTABLE for future readers: the shipped design has ONE toggle (settings.zoneOfAvoidance.enabled gates band AND lettering; SOURCE_REGISTRY row has bearsLabel:false) — this SUPERSEDES the spec's grill-Q7 independent-toggle call and the plan's two-FadeId text. The spec appendix now records it.
Pushed: remote at ce4388454.
LIVE AGENTS (2, both opus, both read-only): entanglement-radar (older diff a98461d15 — code-only scope, unaffected by the docs commits) and FINAL whole-branch review (diff review-dbd93b981..ce4388454.diff, 52 commits; report -> final-review-report.md).
NEXT after both land: adjudicate findings against this ledger (must-fix -> ONE fix dispatch + scoped re-review; optional -> BACKLOG or park with a ruling), then /feature-done (relocates spec+plan to completed/), then finishing-a-development-branch. gh auth is BROKEN (401) — user must `gh auth login` before PR ops.

== Entanglement-radar landed + ADJUDICATED — 2026-08-17 ==
Report persisted by the CONTROLLER at entanglement-radar-report.md (the agent wrote none: my dispatch's READ-ONLY rule overrode the brief's write instruction — controller error, note for future dispatches: say "read-only for SOURCE, write your report file").
Verdicts: 4 fix-now / 6 backlog / 1 accepted.

CONTROLLER VERIFIED each load-bearing claim itself (not agent-reported):
- F1 duplication: READ both shaders. fragment.wesl:26-94 and fragmentPick.wesl:46-96 are verbatim identical modulo comments (ray build, raySphere, jitter, 32-step loop, innerRim/distanceFade/outerRim, worldToGalactic, bLimitDeg cosine bump, bAxis/edgeFadeAxis/edgeBandMask). CONFIRMED.
- F2 dead TS twin: `rg -l zoneOfAvoidanceBLimitDeg|bLimitDeg` over src/ tests/ tools/ = the util, its OWN test, the two .wesl copies, and a .d.ts comment. ZERO production TS callers. CONFIRMED. Aggravating: fragment.wesl:74-76 says "WGSL port of the TS twin — keep in lockstep", a lockstep contract with a file nothing runs.
- F3 dead binding: READ vertex.wesl. Line 12 declares `@group(0) @binding(0) var<uniform> u: Uniforms`, never read by `vs`; line 9 imports Uniforms only for it. CONFIRMED (safe to delete: renderer uses EXPLICIT bind-group layouts, not 'auto').
- F4 stale prose / "called once": `rg -l zoneOfAvoidanceLayerOpacity` = its own file+test, scaleFadeBands comment, ZoneOfAvoidanceRenderer.d.ts comment, and ONE caller (zoneOfAvoidanceLiveness.ts). CONFIRMED called once, header claims "called twice".

RULINGS:
- FIX NOW (one bundled dispatch, after the final review lands so both reviews' must-fixes go in ONE unit): F1 extract shared bandDensity into zoneOfAvoidance/density.wesl; F2 delete the dead util + its test; F3 delete vertex.wesl's unused binding/import + the three comment blocks defending it; F4 fix the three stale-prose sites; F10 comment drift (defaults.ts stale 0.1..0.35 vs 0.46, sliderFields "0.5 default" vs 0.37, label/io.wesl all-caps after mixed-case); F11 reword the two "where the band would be visible" claims to "where the band's DENSITY is perceptible" (behaviour stays); F7 formatter walks Object.keys like its milkyWay sibling.
  Why F1/F3 despite the user's visuals sign-off: unmerged branch, fresh duplication, project precedent (lib/masks.wesl exists BECAUSE copy-pasted masks diverged silently). Cost if wrong: reintroduces the flicker/footprint bugs just fixed -> mitigated by mechanical extraction + a REQUIRED user re-check of band render + pick.
- BACKLOG (own items, not this branch): F5 ZONE_OF_AVOIDANCE_SHELL record + 4 positional same-typed number args (bulgeDeg/anticenterDeg swappable); F6 focusability double-encoded (ROW_FOCUSABLE vs focusFraming throwing arm); F9 third copy of Math.max(1, floor(size/scale)) -> renderTargets.sizeOf().
- F8 (two homes for default-enabled): ACCEPTED — defaults.ts ALREADY documents the choice deliberately and names milkyWay as the contrast. The general "SourceEntryBase.visible mandatory on non-catalog rows" question goes to BACKLOG, not a branch fix.

== USER RAISED: TS constants importable in WESL — 2026-08-17 ==
User, on seeing the dead-bLimitDeg-twin finding: "this points at a problem that we'd love to have TS constants directly importable in WESL". Treated as an ASSESSMENT request, not a change request. No implementation without an explicit ask.
THIS PUTS F2 ON HOLD. F2's ruling was "delete the dead TS util". If TS-to-WESL sharing is feasible, the right move flips to "TS twin becomes the single source, shader side generated". F1, F3, F4, F7, F10, F11 are unaffected and still go in the fix bundle.
LIVE AGENT, opus, research, writes one report: ts-constants-in-wesl-research.md. Scope: what wesl 0.7.26 and wesl-plugin 0.6.74 actually support, grounded in node_modules source not recollection; alternatives including codegen, build-time substitution, and a pinning test; must respect the load-bearing no-JS-linker-in-production constraint at vite.config.ts:66; must cover the separate tool builds; must find 2 or 3 OTHER real TS-to-WGSL duplication instances; and must answer the narrow ZoA choice between deleting the twin, generating from the twin, or pinning both with a test.

== FINAL REVIEW landed + ADJUDICATED — 2026-08-17 ==
Verdict APPROVE WITH FIXES, 3 must-fix / 7 optional. Report: final-review-report.md (398 lines, agent-written).

CONTROLLER VERIFICATION of the must-fixes (opened the files myself):
- MF1 CONFIRMED and it is a REAL correctness bug. zoneOfAvoidanceSliderFields.ts:43 has edgeSharpness min:0. At 0, edgeFadeAxis = clamp(0/(2*bLimit),0,0.5) = 0, so lib/masks.wesl:86 evaluates smoothstep(0.0, 0.0, axis). bAxis is EXACTLY 0.0 for every march sample past the southern b-limit, so that is 0/0 = NaN. NaN then survives `densityMask <= DENSITY_MASK_FLOOR` and `alpha <= 0.0` (all NaN comparisons are false) and PAINTS THE PICK TARGET — the identical mechanism as the zenith-lines bug already fixed on this branch. Reachable only by dragging the DebugPanel slider to 0 (default 5), so latent, not user-visible today. Fix = the same `max(x, 1e-3)` guard the sibling knob has three lines up.
- MF2 = the radar's F1+F2, already adjudicated. Extraction proceeds; the delete-the-TS-twin half stays ON HOLD pending the TS-in-WESL research.
- MF3 PARTIALLY CONFIRMED: 3 real sites, not 4. FadeId.d.ts:37-41 and ZoneOfAvoidanceRenderer.d.ts:76 verified verbatim; zoneOfAvoidanceLayerOpacity.ts:8 already known. The cited `src/@types/store/EngineSettingsState.d.ts:180` DOES NOT EXIST — no such file, and `rg` for the claim text across all of src/ returns only the FadeId lines. FABRICATED CITATION; the report's own MF3 heading says "Three doc sites", so the inflation is in the summary message only. Standing lesson reconfirmed: open the file, never relay a subagent citation unchecked.

RULINGS on the optionals:
- FIX NOW (join the bundle): O1 five timeless-comment survivors of the branch's own sweep; O2 defaults.ts docblock calls a LAVENDER veil "warm amber" and narrates falloff 0.1..0.35 for a 0.46 default; O5's io.wesl only (40-line header = 4x budget, ratio 1.79 — trim the narrative, KEEP the byte table, it is a real cross-file ABI contract); O6 delete both low-value tests (renderer.label constant restatement; pickEnabled toBeUndefined runtime type assertion).
- O3 edgeSharpness default 5 == slider max 5: FIX by widening max to 8. The parity test asserts max >= default and passes on EQUALITY, so it never caught the end-stop state it exists to prevent. Widening changes no rendered default. MUST TELL THE USER: they tuned at the range boundary, so the perceptual optimum may lie above 5 and is currently unreachable.
- O4 DebugPanel disable freezes the band in SCREEN SPACE and keeps compositing it (producer toggled off so 'zoa' is never re-cleared, consumer still composites): pre-existing star-aggregates/mw-aggregate shape, ledger already adjudicated it, never-sweep applies. BACKLOG one line covering all THREE producers.
- O7 draw vs drawPick aspect delta (~0.3%, 1.7847 vs 1.7801): ACCEPTED. Each viewport is correct for the target it draws into; the delta is the floor() in the 1/5-res allocation. Both pinned by tests. This retires the old "latent uniform-sharing" note by naming the residual.

== TS-constants-in-WESL research landed — 2026-08-17 ==
Report: ts-constants-in-wesl-research.md. ANSWER: YES, injection is possible and the repo's 12 "?static has no value injection" comments are HALF WRONG. wesl's link() accepts `constants` and `virtualLibs` (node_modules/wesl/src/Linker.ts:74-92, :279-292); the builtin staticBuildExtension simply never passes them (wesl-plugin/src/extensions/StaticExtension.ts:44-50). A ~30-line custom PluginExtension NAMED `static` shadows the builtin, so none of the 132 `?static` import sites would change. Agent VERIFIED by running the installed linker: constants work from non-root lib modules, virtualLibs can emit whole functions, unused constants are dropped, missing ones are a loud link error.
LANDMINE if anyone does this: `constants: { X: 15.0 }` emits `const X = 15;` — a WGSL AbstractInt, so `X / 2` silently becomes INTEGER DIVISION. Floats must be passed as STRINGS.
General guidance from the report: prefer a shared .wesl module; if TS genuinely needs the number too, prefer CODEGEN (a checked-in generated .wesl) over the plugin extension, because injection couples FOUR separate wesl-plugin registrations (app, vitest, galaxy-renderer, flow-workbench) and loses HMR on the TS side.
ALSO FOUND, live and unguarded: labels/vertex.wesl:27-32 mirrors src/data/fonts.ts with no parity test. -> BACKLOG.

F2 HOLD RESOLVED -> option (a). bulgeDeg/anticenterDeg are UNIFORMS, so there is no compile-time value to inject: (b) solves a problem this file does not have and (c) cannot pin a formula, only a number. Delete the dead twin + its test; the single home is a shared .wesl function.

== Final-gate fix bundle part A DISPATCHED — 2026-08-17 ==
LIVE AGENT: opus implementer, brief gate-fix-final-code-brief.md, 7 units / 7 commits: (1) extract shared band.wesl march, (2) guard the zero-width smoothstep NaN, (3) delete dead bLimitDeg util+test + stale doc ref + the lockstep comment, (4) delete vertex.wesl's unread binding + its three prose defences, (5) edgeSharpness max 5->8 + strict slider parity assertion, (6) formatter walks Object.keys, (7) delete two low-value tests. Told to invoke the wesl-shaders skill before any .wesl edit, and warned that units 1-2 carry the previously-debugged flicker/pole-line fixes.
PART B still to dispatch after A lands (prose only, zero risk): MF3 three stale two-toggle doc sites, O1 five timeless-comment survivors, O2 defaults.ts "warm amber" for a lavender veil + stale falloff narration, O5 io.wesl 40-line header trim (KEEP the byte table), F10 comment drift (sliderFields "0.5 default" vs 0.37; label/io.wesl all-caps after mixed-case), F11 reword the two "where the band would be visible" claims to "where the band's DENSITY is perceptible".
THEN: scoped re-review of the whole fix bundle, USER visual re-check (band renders, still pickable, foreground galaxy still wins, no pole lines/flicker), /feature-done, finishing-a-development-branch.

== Fix bundle part A LANDED — 2026-08-17 ==
8 commits: 1e1df55bd extract band.wesl · 12cb51f5a NaN floor · c41dc5aa2 delete dead TS twin+test · cb855ba2a drop vertex.wesl unread binding · 40920ed42 edgeSharpness max 5->8 + strict slider assertion · daed089e3 formatter walks Object.keys · b4c1be301 drop two low-value tests · b5db84ae5 comment budget (EXTRA, agent-initiated: the extraction halved both fragments' code so their comment ratios crossed 0.50).
Agent's own verification: vite build clean after every shader edit PLUS direct inspection of the LINKED WGSL in dist/ (fn inlined into both modules, no surviving `import package::`, vertex has no @group, pick has only @group(1)); both tsc projects clean; suite 1032 files / 6950 tests. Bind-group layouts confirmed explicit via createPipelineLayout, no 'auto'.

CONTROLLER VERIFIED THE RISKY UNIT MYSELF (read all three shader files end to end):
- band.wesl exports `bandDensityMask(u: Uniforms, ndc: vec2<f32>, fragCoordXY: vec2<f32>) -> f32` returning the mask ALREADY divided by outerRadiusMpc (same unit DENSITY_MASK_FLOOR is expressed in).
- Both fragments call it with `input.ndc, input.clipPos.xy` — identical jitter input to before, so the dissolve pattern is unchanged.
- Visual tail `applyFade(u.intensity * densityMask, u.fadeAlpha)` is algebraically identical to the old `applyFade(u.intensity * opticalLength / u.outerRadiusMpc, ...)`.
- Ray-miss now RETURNS 0.0 where it used to `discard`: visual path 0.0 -> alpha 0 -> `alpha <= 0.0` discards; pick path 0.0 <= 0.02 discards. Behaviour identical, and `discard` stays in the entry points where it belongs.
- The `if (radialMask <= 0.0) continue;` guard and its asin-protection comment survived intact.
- NaN FIX IS BETTER THAN BRIEFED: the floor went on the CLAMP'S LOW BOUND — `clamp(u.edgeSharpness / (2.0 * bLimitDeg), 1e-3, 0.5)` — so no route to zero width exists at all (not just edgeSharpness=0, but also huge bLimitDeg or denormal flush). Only affects the near-zero regime; at the default 5/(2*bLimit) the value is orders of magnitude above 1e-3, so no visible change.
- fragmentPick.wesl header now correctly says "where the band's density is perceptible" (radar F11, partially done early).
TEST DELTA EXPLAINED: 1033->1032 files and 6958->6950 tests = the deleted bLimitDeg test file (6 cases) + the 2 deleted low-value cases. Consistent, no silent losses.

AGENT-FLAGGED LEFTOVERS, both routed to part B: ZoneOfAvoidanceRenderer.d.ts:15-16 still claims a click registers "where the band is actually visible" (F11's third site); slider test's LOWER bound still inclusive — RULING: ACCEPT, min:0 with default 5 is not an end-stop state, strictness there would be cargo-culted.

== Fix bundle part B DISPATCHED — 2026-08-17 ==
LIVE AGENT: sonnet, brief gate-fix-final-prose-brief.md, prose-only, 6 commits: MF3 three abandoned-two-toggle doc sites, O2 defaults.ts "warm amber" for a LAVENDER veil + falloff narration anchored on the old default, O5 io.wesl 40-line header trim (KEEP the byte table — real ABI contract), F10 sliderFields "0.5 default" + label/io.wesl all-caps, F11 the d.ts pick-footprint claim, O1 the five timeless-comment survivors.
AFTER B LANDS: controller runs the full gates once (typecheck/test/build), scoped re-review of the WHOLE fix bundle, push, USER visual re-check, /feature-done, finishing-a-development-branch.

== Fix bundle part B LANDED + whole bundle pushed — 2026-08-17 ==
5 commits: a3c9d73af two-toggle prose (3 sites; unit 5's pick-"visible" claim rode along, same docblock) · 944e8d741 defaults.ts colour+falloff facts · 420cb48aa io.wesl header 15->10 lines, ratio 1.79->1.58 (byte table kept, so >0.5 BY DESIGN — accepted) · 49b0330bd slider ceiling math + stale all-caps · 598c474cf timeless-comment survivors (3 fixed in defaults.ts and renderTargets.ts x2; the other 2 were already gone via part A's shader rewrite, grep-confirmed; agent also fixed a bonus "half-res"->"1/5-res" inaccuracy).
Controller spot-check: FadeId.d.ts:37-40 now reads "gates both the band and its curved lettering — a single toggle, not a band/label split" = TRUE against the shipped design.
FIX BUNDLE = 13 commits ce4388454..598c474cf. PUSHED, remote at 598c474cf.
Gates re-running (typecheck already CLEAN; tests+build in flight, background id bi3ogj3sr).
LIVE AGENT: opus scoped re-review of the whole bundle -> fix-bundle-rereview.md. Three questions only: is each finding actually FIXED (not merely reworded — the "called twice" claim already survived one rewording), did the fixes introduce anything (highest risk = 1e1df55bd's extraction, told to diff the extracted body against BOTH pre-extraction originals via `git show 1e1df55bd^:<path>` and to check 12cb51f5a's clamp-low-bound NaN floor for default-appearance perturbation), and anything missed WITHIN scope.
REMAINING AFTER RE-REVIEW: user visual re-check (shaders changed since sign-off) -> /feature-done -> finishing-a-development-branch. gh auth still BROKEN (401): user must `gh auth login` before PR ops.

== Scoped re-review of the fix bundle: ISSUES — 2026-08-17 ==
Report: fix-bundle-rereview.md. Gates all green first (typecheck both configs, 1032 files / 6950 tests, build 13.71s).
CLEARED by the re-review, independently of my own check: the extraction 1e1df55bd is behaviour-preserving (comment-stripped statement diff vs BOTH pre-extraction originals: identical but for param renames, the discard->return 0.0 change which is outcome-equivalent at both tails, and ONE fp re-association in the visual tail worth <=1 ulp). Both previously-debugged bugs survive intact: asin(clamp(z/r,-1,1)) and PICK_FALLBACK_DEPTH + pre-intensity DENSITY_MASK_FLOOR in original order. 12cb51f5a's clamp lower bound is sound: raw ratio at defaults is 0.25..0.833, so the 1e-3 floor binds ONLY below edgeSharpness 0.02, i.e. only the slider's exact 0 — zero default-appearance delta.

FOUR ISSUES, ALL CONTROLLER-VERIFIED BY OPENING THE FILES:
1. REGRESSION in daed089e3 (caused by MY brief saying "match the sibling"): formatZoneOfAvoidanceTuningDefaults.ts:29 walks Object.keys(TUNING) = the LIVE object, but the call site ZoneOfAvoidanceTuningSection.tsx:78 passes `zoneOfAvoidance` typed ZoneOfAvoidanceSettings = `{ enabled: boolean } & ZoneOfAvoidanceTuning` (VERIFIED in ZoneOfAvoidanceSettings.d.ts:19-22). TS accepts the intersection, so at runtime the walk sees `enabled` and emits `enabled: 1,` — String(round(true)) — an invalid paste. The milkyWay sibling walks Object.keys(DEFAULTS), not the live bag; that is the distinction that was missed. Its test is TAUTOLOGICAL (pure Tuning fixture, never sees `enabled`).
2. MF3(c) NOT FIXED: EngineSettingsState.d.ts:180-186 still says "the two independent visibility axes (band / label)".
   *** CONTROLLER CORRECTION, recorded for honesty: my earlier ruling that this citation was "FABRICATED" was WRONG. The CLAIM was real; only the PATH was wrong (the reviewer said src/@types/store/, the file is at src/@types/settings/). My verification then compounded it by text-searching phrases that do not occur in the file ("toggle independently", "band and label") instead of the phrasing it actually uses ("two independent visibility axes"). LESSON: when a cited path is wrong, locate the FILE BY NAME before concluding the finding is false. ***
3. MF3(a) WRONG A THIRD TIME: zoneOfAvoidanceLayerOpacity.ts now says "Called once per frame" — one call SITE, but ~4 calls/frame (both layers' enabled() + draw()), 5 on pick frames. RULING: stop counting calls; state what the function IS and drop the frequency claim, so no recount is ever needed again.
4. Stale statements: band.wesl:20 "0.0 means the ray never crosses the shell" is false (a pole-ward ray crosses the shell and still accumulates 0.0); zoneOfAvoidanceLabelText.ts:3 still carries a `grill Q5` process ref that survived the sweep.
RULED OUT OF SCOPE (do not sweep): fragmentPick.wesl ratio 0.34->0.69 and io.wesl >0.5 are ACCEPTED — the extraction shrank the CODE, not the comments, and both hold load-bearing ABI/derivation contracts. zoneOfAvoidanceLayerOpacity.ts 1.00 and zoneOfAvoidanceSliderFields.ts header 11 stay.

LIVE AGENT: sonnet, brief gate-fix-round2-brief.md, 4 commits. Unit 1 must ALSO add a non-tautological test passing a Settings-shaped object and asserting `enabled` is absent from the output.
AFTER: re-verify unit 1 myself, run gates, push, USER visual re-check (already requested), /feature-done, finishing-a-development-branch.

== Round 2 LANDED — 2026-08-17 ==
4 commits: 314c3e431 formatter walks the canonical DEFAULTS keys · 90b8b2d5b EngineSettingsState two-axis claim · 678864b4f dropped the call-frequency claim · 46e733cb3 band.wesl + label-text stale comments.
CONTROLLER VERIFIED:
- formatZoneOfAvoidanceTuningDefaults.ts now iterates Object.keys(DEFAULT_ZONE_OF_AVOIDANCE_TUNING) and reads values from the passed object — complete key set AND live values AND no `enabled` leak. Docblock accurately explains why.
- EngineSettingsState.d.ts:180-186 now reads "one visibility toggle (`enabled` gates the band and its lettering together)". Correctly left the MILKY WAY docblock above it saying "two independent visibility axes" — milkyWay genuinely HAS both enabled and labelEnabled.
- MUTATION-VERIFIED the new test (per the standing rule, not just trusted): reverted the loop to Object.keys(tuning), re-ran -> `expect(output).not.toContain('enabled')` FAILED (1 failed | 2 passed); restored via git checkout -> 3/3 pass. The test genuinely catches this regression.
- Agent self-caught and corrected one error before committing: its first label-text comment claimed the 3-way repeat hits both bulge AND anticenter, false since 0/120/240 never lands on 180.
Final full gates running (bj68f44wl).
REMAINING: push · USER visual re-check (requested, not yet answered) · /feature-done · finishing-a-development-branch. gh auth BROKEN 401 — user runs `gh auth login` first.

== Deferred findings captured in BACKLOG — 2026-08-17 ==
Commit 3c5cf6c92, pushed. 7 entries, 5 detail files under docs/backlog/2026-08-17-*: ts-constants-in-wesl (the big one, 126 lines), zone-of-avoidance-shape-constants, focusability-double-encoded, debugpanel-producer-toggle-freezes-overlay, labels-vertex-wesl-fonts-parity. Index-only: third copy of the reduced-res viewport formula; scaleFadeBands comment misplacement.
Controller checked the index lines against the terse-index convention and the file's existing 141 entries — format and voice match.
Agent judgment calls, all accepted: TS-in-WESL + the fonts parity item filed under Rendering not Docs (shader-specific); focusability under Engine & State; DebugPanel freeze tagged `ready` not `needs-design` since the fix is one predicate; and it DROPPED the radar's `io.wesl:9 "3..380 Mpc"` citation because it could not re-verify the phrase after the file shifted, keeping only citations it confirmed itself (defaults.ts:250, renderer.ts:53 drift). That is the right instinct — an unverifiable citation in a durable doc is worse than a missing one.

== /feature-done AUDIT run — 2026-08-17 ==
Baseline: npm test 1032 files / 6951 tests PASS; npm run typecheck (both configs) PASS; npm run build PASS.
TODO/FIXME/HACK/XXX across every new zone-of-avoidance file: ZERO.
Test parity: zoneOfAvoidanceRenderer.test.ts 78 lines vs its template horizonShellRenderer.test.ts 44 — parity BETTER than the comparable, not worse. zoneOfAvoidanceLayer.test.ts 176 lines. OK.
Branch footprint: 105 files changed vs main.
FINDING (minor, non-blocking): the plan has NO `## File Structure` section, so audit step 3's created/modified cross-reference could not run. Not a failure — noted so the next plan author includes one.
BLOCKING ITEM: plan checkboxes were 52 ticked / 54 OPEN despite the work shipping. Delegated to a sonnet agent with the spec's Amendments section as the authority, because ~6 open boxes describe SUPERSEDED design (labelEnabled, "independently toggle band vs lettering", "the 4 tuning knobs") and blind-ticking would make the plan lie; those get ticked WITH a correction clause. Three boxes already annotated DROPPED per earlier rulings get ticked with their reasons intact.
SMOKE ATTESTATION = PARTIAL, and this is what holds the audit at NOT READY: the user's "all visuals pass" predates the fix bundle, which rewrote both fragment shaders (extraction + NaN floor + widened edgeSharpness range). Two independent statement-level diffs proved the extraction behaviour-preserving and the NaN floor inert at default tuning, so risk is LOW — but shader behaviour is runtime-only and the re-check was requested and not yet answered. Do NOT relocate plan/spec until the user confirms.

== Checkboxes reconciled — 2026-08-17 ==
Commit 60b33f822, pushed. 53 ticked-as-written · 18 ticked-WITH-CORRECTION · 3 ticked-as-dropped (reasons kept) · 1 deliberately left OPEN.
The open one is CORRECT and must stay open until the housekeeping runs: Task 15's "Confirm the DoD, then run /feature-done before merge". The agent's reasoning is the right test — the plan and spec are still at their original paths, and their relocation to plans/completed//specs/completed/ IS the observable proof that box is done.
Controller spot-checked the correction clauses: accurate and specific (fragment.wesl now describes the 32-step march + shared band.wesl extraction rather than the planned two-sphere single-sample shade; the ContentLayer box records the 'zoa' offscreen + producer/consumer split instead of drawing straight to 'hdr'; the drawLabels box records the single-toggle gate; the DebugPanel box records that the CopyButton mirror the plan said to SKIP was built after all).

== AUDIT VERDICT: NOT READY — one item — 2026-08-17 ==
Everything passes except smoke attestation, which is PARTIAL: the user's "all visuals pass" predates the fix bundle's shader rewrite. Housekeeping (plan+spec relocation to completed/, ledger archive to docs/superpowers/plans/completed/2026-08-13-zone-of-avoidance-guide-layer.ledger.md, backlog sweep, completion commit) is DELIBERATELY NOT RUN.
TO RESUME: when the user confirms the post-refactor visuals, re-run /feature-done. Expect it to go straight to READY — every other check is green and recorded above, so only the attestation line changes. Then tick that last box as part of the completion commit, then finishing-a-development-branch. gh auth BROKEN (401): user runs `gh auth login` before any PR operation.
