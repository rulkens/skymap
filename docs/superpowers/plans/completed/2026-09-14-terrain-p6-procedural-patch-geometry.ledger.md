# SDD ledger — plan: docs/superpowers/plans/2026-09-14-terrain-p6-procedural-patch-geometry.md

Execution branch: `terrain-p6-patch-geometry` on origin (created 2026-09-14 from main 35463c237). Controller session is pinned to worktree `per-planet-terrain` (spec branch); implementers run in `isolation: "worktree"` agents that reset to `origin/terrain-p6-patch-geometry`, commit, rebase, push. Reports live in the scratchpad `sdd/p6/` dir; briefs + review packages here. No harness task-list tool in this session — this file IS the task list.

## Task list (waves)

- Wave A: Task 1 (perf baseline) ∥ Task 2+3 (ONE dispatch) ∥ Task 4
- Wave B: Task 5+6+7 (ONE dispatch — the atomic trio)
- Wave C: Task 8 (delete the CPU mesh path)
- Wave D: Task 9 (perf after + eye-check — USER attests)
- Wave E: Task 10 (docs)

## Preflight scan

| pair / task | produces → consumes                                                                                 | finding                                                                                                                                                                          |
| ----------- | --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T2 → T3     | `SurfacePatchAnchor` type → `surfacePatchAnchor`, `patchOriginRelEyeM`                              | T3 needs T2's type file; parallel would duplicate it → batched                                                                                                                   |
| T3 → T5/T7  | `SurfaceCutTile.anchor`, `patchOriginRelEyeM(anchor, radiusM, eyeRelBodyM)`                         | names consistent across T5 table + T7                                                                                                                                            |
| T4 → T7     | `surfacePatchIndices(resolution): Uint16Array`                                                      | consistent                                                                                                                                                                       |
| T5 → T6/T7  | `PatchInstance` 64 B, `PATCH_INSTANCE_BYTES`, `writePatchInstance`, `vertsPerTile`→`meshResolution` | consistent; T7 must write `meshResolution = resolution`                                                                                                                          |
| T6 → T7     | shader reads binding 1 `array<PatchInstance>`, binding 2 gone                                       | T7 layout removes binding 2, keeps 3–9                                                                                                                                           |
| T4/T6 → T8  | T4/T6 cite `bakeSurfaceTileMesh.ts` lines that T8 deletes                                           | citations only, no code dep                                                                                                                                                      |
| T1 → T9     | same flags, same scenarios                                                                          | with isolated worktrees T1 and T9 run on DIFFERENT dev servers (same machine, headless) — noise noted; T9 may re-run a `main` baseline side-by-side if the delta is within noise |
| T1 self     | "record before-shots"                                                                               | the before-picture is `main` itself, unchanged — the user compares main's dev server vs the branch at T9                                                                         |

Ruling: T2+T3 batched (shared new type, same author). Cost if wrong: one fix loop spans both.
Ruling: T5+T6+T7 batched — the plan already declares them an atomic trio that must never be handed over mid-way. Cost if wrong: a larger review surface (3 commits) in one seat.
Ruling: T1 runs in parallel with T2–T4 — the ordering "baseline before code moves" is about one working tree; isolated worktrees at origin/main make it moot. Cost if wrong: none (T1 touches no code).
Ruling: no before-shot captures; `main`'s dev server is the before-picture at T9. Cost if wrong: user must have main's server up at attestation time.
Ruling: no harness task list exists in this session; ledger waves stand in.

## Dispatch log

- 2026-09-14 Wave A dispatched, all BASE 35463c237, isolated worktrees: Task 1 perf baseline (opus, agent acdeb561541e8c2c9, report → scratchpad/sdd/p6/task-1-report.md, perf-before.txt); Task 2+3 (opus, agent a9e1b634142fb51d0, report task-2-3-report.md); Task 4 (sonnet, agent affad474071d2b508, report task-4-report.md)
- Task 1: complete (no code; perf baseline at 35463c237 — earth-surface 22.1 ms MERGED median / 28.0 p90; solar-system 22.2 / 29.5; noise floor ~0.7 ms; dev server :5174 left running in worktree agent-acdeb561541e8c2c9; perf-before.txt in scratchpad sdd/p6). Ruling: Task 9 treats deltas < 1 ms as noise (measured floor 0.7 > skill's 0.5). Cost if wrong: a 0.5–1 ms regression passes the gate.
- Task 4: complete (commits 35463c2..86a4995, review clean — sonnet reviewer, winding hand-verified). Draft PR #705 opened. ⚠️ implementer saw 3 unrelated full-suite timeouts (slider UI, ISM arm-forcing) under shared-machine load — recheck at Task 8's full run.
- T2 landed as 3b8f93b49 (SurfacePatchAnchor + patchVertexOffsetM + test); T3 in progress when the agent died on the usage limit (test file written, utils not yet) — resumed via SendMessage. Review of T2+T3 waits for T3's push.
- T2+T3 DONE (resumed agent): 3b8f93b49 + 40116841a, suite 9252/0, typecheck clean, both tests mutation-verified. Renderer kept bit-identical by composing the origin inline from the UNROUNDED anchor (not `patchOriginRelEyeM`) because baked meshes are still rooted at the unrounded corner — Task 7 switches. Registration test adds a z6 corner (z0's v0 is always the pole). Review dispatched (opus) on review-86a4995c3..40116841a.diff. Concern carried to T6/T8: stale `originLocal` prose at `bakeSurfaceTileMesh.ts:11,13` (deleted in T8) and `vertex.wesl:21` (rewritten in T6).
- T2+3 review (opus): Approved with 1 Important — `dLonRad`/`dLatRad` untested (surfacePatchAnchor spans never asserted). Derivation re-derived by reviewer, mutation sweep reproduced (z7 case is load-bearing for the lat0-vs-lat distinction). minor (deferred): `patchOriginRelEyeM.ts` header 9 prose lines vs ~17 code (DataView paragraph droppable); `SurfaceCutTile.d.ts:9` "derivable via patchOriginRelEyeM" loose phrasing; `patchVertexOffsetM.test.ts:75` says all three −0 (z is +0); `patchOriginRelEyeM.ts:25-27` groups `(r·cosLat0)·cos` vs renderer's `r·(cosLat0·cos)` — sub-ulp f64, but T7's switch is NOT bit-identical to today (note for T9 eye-check, not a bug); stale `originLocal` prose in bakeSurfaceTileMesh.ts:11,13 (T8 deletes) and vertex.wesl:21 (T6 rewrites).
- T2+3 fix round 1/5 dispatched: resumed implementer (cumulative ~124k tokens) with the Important + the z0-comment minor (same file).
- T2+3: fix round 1/5 (2 addressed, 0 open — spans pinned against equirectUvToDirection([u1,v1]); z0 rows dropped for [2,3,0]; commits 40116841a..ad28106f4). Re-review (sonnet) verified "swapped pair uncatchable" is true by construction (rows = cols/2 at every level).
- Task 2+3: complete (commits 86a4995c..ad28106f, review clean after 1 fix round).
- Trio T5+6+7 dispatched off ad28106f4 (opus, isolated; report task-5-6-7-report.md; includes the shader-compile check via a headless perf boot, dev server left running).
- LANDMINE: `git checkout -B <exec-branch>` fails when another agent worktree holds the branch — protocol now says `git reset --hard origin/<exec>` on the agent's own branch and push by ref.
- Trio T5+6+7 DONE_WITH_CONCERNS: 70d8ea1a0 (T5 record) + 911e897ce (T6 shader) + f53778cc3 (T7 renderer), 11 files +275/−421; suite 1339/9250 green, tsc clean at HEAD; shader compiles headless, HEAD vs BASE same picture at 53 km / 7 km. Dev server for this branch: http://localhost:5177 (worktree agent-a4fd578fd931f142b) — T9 perf must pass `--url http://localhost:5177`. Ruling: red intermediate commits (5, 6 not tsc-green alone) accepted — PR squash-merges, BASE..HEAD identical. Cost if wrong: bisect lands on a red commit. LANDMINE: `patch` is a WGSL reserved word (record named `inst`) — add to WESL landmine memory at wrap-up. T9 note: `__skymapPerf.setPose` looked inert while Earth is focused (follow driver re-commits the arm) — pre-existing, may affect the measurement; wheel-zoom worked. 3 out-of-list test edits (earthPass.test.ts args.frame assertion deleted, frameFilePurity earthPass 6→5, initGpu stub queue) — reviewer to confirm. Review dispatched (opus, agent a2c3c75850863ff5b) on review-ad28106f4..f53778cc3.diff → scratchpad/sdd/p6/task-5-6-7-review.md.
- Trio review (opus): A Approved — 64 B walked field-by-field in io.wesl and layout.ts, stride 64, meshResolution u32 @92, uniform 176 B; vertex.wesl is a line-for-line transcription of patchVertexOffsetM.ts; haversine vs deleted bake's R·(dir−dir0) agrees to ≤9.4e-10 m; index template byte-identical winding; binding 2 gone, 3–9 unmoved, explicit layout; parity test green. Out-of-list test edits legitimate (frameFilePurity 6→5 is a genuine ratchet down: earthFrameCounter export deleted). B Changes required, 2 Important, both comments: io.wesl:14-16 + layout.ts:21-23 "move fadeWeight → 80 B" derivation is FALSE (f32 align 4: stride stays 64, offsets 12–31 re-map silently) — the error originates in the PLAN at line 210 (spec-branch edit, do at T10 with the pivotRadiusMpc census row for P1); io.wesl:83-86 "vertex stage applies rot" untrue (position path has no rotation; normal/viewDir still invert it).
- Carry to T8: delete surfaceTileMeshCache.ts, bakeSurfaceTileMesh.ts, SurfaceTileMesh.d.ts + 2 tests; EARTH_SURFACE_TILE_MESH_CACHE_CAPACITY (earthTileParams.ts:58-66) dead with no import error; EARTH_SURFACE_TILE_MESH_RESOLUTION doc (:50-56) false on three counts — rewrite; new device stubs need a `queue`. Carry to T10: docs/RENDERER.md:14,:27 phrasing.
- Trio fix round 1/5 dispatched: fresh isolated sonnet agent a35f750c0cc8d001d, comment-only, off f53778cc3 → report task-5-6-7-fix1-report.md.
- T8 dispatched off f53778cc3 in parallel with the trio's comment fix round (disjoint files; Rule 2 pipelining): opus, isolated, agent a78caffae3ed75c50, report task-8-report.md. Carries the review's T8 list.
- Servers: :5174 = P6 T1 baseline worktree at 35463c237 (= main at branch time, the before-picture); :5177 = P6 HEAD.
- Trio: fix round 1/5 (2 addressed, 0 open — f53778cc3..bb5236ad4, comment-only). Re-review (sonnet) verified both comments true against vertex.wesl and the layout test's per-field assertions. io.wesl comment-heavy (113/40) is the pre-existing byte-layout-contract exemption, not this diff's.
- Task 5+6+7: complete (commits ad28106f..bb5236ad, review clean after 1 fix round).
- T8 DONE: 8e95cd4c9 (6 files +4/−340), refs clean, suite 1337/9241 green, tsc clean, no timeouts. Stale citations for T10: surfacePatchIndices.ts:4 and fragment.wesl:53 cite the deleted bake. Review dispatched (sonnet, agent a3d07feb6f23b5751) on review-bb5236ad4..8e95cd4c9.diff → task-8-review.md.
- T9 dispatched in parallel with T8's review (pure deletion; Rule 2): opus, isolated, agent a05bdf6e61e94123b; own server on :5183 at 8e95cd4c9; paired A-B-A-B against :5174 (main at branch time); halt rule carried verbatim → task-9-report.md, perf-after.txt.
- LANDMINE (protocol): sandbox denied `git reset --hard` in two agent worktrees; `git checkout -B <own-branch> origin/<exec>` is the working fallback (the branch is the agent's own, so no cross-worktree refusal).
- Task 8: complete (commits bb5236ad..8e95cd4c, review clean). Carry to T10: stale citations surfacePatchIndices.ts:4 and fragment.wesl:53 (non-load-bearing); RENDERER.md:14,:27; spec §3.1/§3.5/§12 status (spec branch).
- Ruling: T10 split like P1's T8 — RENDERER.md:14,:27 + the two stale citations land on the exec branch now (sonnet isolated agent a41723fbb07b27ecf, off 8e95cd4c9, in parallel with T9's measurement; report task-10-report.md); spec §3.1/§3.5/§12 status waits for the T9 land/park call and rides the spec-branch docs commit with P1's §3.3 + census + the P6 plan line-210 correction. Cost if wrong: a docs commit on a parked branch.
- Task 10 (exec-branch half): complete (commits 8e95cd4c..2acb865c, docs/comment-only 3 files +12/−10, controller inspection: RENDERER.md:14,:27 rewritten with the tie sentence kept; both dead citations repointed). minor (deferred → final review): fragment.wesl:53-57 still carries process prose ("this task creates new files only") — plan-phase content in a shader comment, pre-existing; final reviewer to rule on dropping it.
- Remaining: T9 verdict (agent a05bdf6e61e94123b) → USER eye-check four poses (:5183 vs :5174) + land/park call → final whole-branch review → spec-branch docs commit → archive → finish (#705).
- T9 DONE: NEUTRAL. Paired A-B-A-B at --frames 90: earth-surface 21.28 → 20.78 ms (−0.50), solar-system 20.73 → 20.47 (−0.27); 30-frame runs spread 3–4 ms against the SAME server (box loaded) so the 1 ms noise ruling did not hold — the paired 90-frame series is the evidence. No validation errors or shader warnings in 23 runs. Server :5183 at 8e95cd4c9 left running. HALT per spec §11/§3.5: land/park is the USER's call. Pipeline state at halt: T1–T8 + T10(exec half) complete; final whole-branch review dispatched anyway as input to the call (reversible, no landing step).
- Ruling: the noise floor for this box is ~3 ms at 30 frames under load; future gates on this machine use paired ≥90-frame runs. Cost if wrong: none (stricter).
- Final whole-branch review dispatched (opus, agent abb415f85f6a2af25) on review-35463c237..2acb865c6.diff (10 commits) → scratchpad/sdd/p6/final-review.md; asked for an explicit buys/costs paragraph for the user's land/park call.
- USER GATES OPEN: (1) four-pose eye-check :5183 (P6 at 8e95cd4c9) vs :5174 (main) — orbit, 300 km fade band, Søndermarken z19, limb; (2) land/park call on neutral perf.
- Final review (opus): Changes required — 1 Important, comment-only (fragment.wesl:53-57 false "CPU twin" claim + process prose [ruling: drop]; :102 still cites NodeParams → PatchInstance); 0 Blocking; 5 Minors (albedoRect merge narrowed the parity test's lane-swap coverage; indexCount restates indices.length; vertex.wesl header 11 lines; setFloat32 fact in two files). DoD verified item by item. Buys: bake + LRU cache + type + 2 tests gone (−128 net), per-frame upload ~1.97 MB → ~6.8 KB, corner stated once, the only viable path to F2's n=64 (~126 MB/frame on the old path). Costs: a second two-language contract; patchVertexOffsetM.ts has zero production callers (reference only); three shader-only facts (i/j split, ENU handedness, 1−t uv flip) lost their CPU tests — the eye-check is their whole suite. Reviewer's framing for the user: the call is "is terrain going ahead".
- Final fix dispatch (the ONE allowed): sonnet isolated agent af8ad6e3aaf5fb084 off 2acb865c6 — Important + 3 cheap minors + a lane-swap assertion if the narrowing holds; report final-fix1-report.md. Then scoped re-review → USER land/park + eye-check → spec-branch docs → archive → finish (#705).
- Final fix round 1/1 DONE: c5d9a2590 (5 files +42/−13): fragment.wesl comments only; indexCount → indices.length; vertex.wesl header 10; setFloat32 duplicate dropped; one per-lane asymmetric assertion added to the layout test, mutation-verified. Suite 9242 green, both typecheck gates clean. Scoped re-review dispatched (sonnet, agent a14d0f7034f25a059) on review-2acb865c6..c5d9a2590.diff.
- NOTE: :5183 serves 8e95cd4c9 (pre docs/comment commits) — render-identical to HEAD c5d9a2590 (only comments, a test, and indexCount alias changed since).
- Final review: clean after 1 fix round. Branch HEAD c5d9a2590 = review-clean. PIPELINE HALTED on neutral perf — awaiting USER: eye-check (:5183 vs :5174) + land/park. On LAND: spec-branch §3.1/§3.5/§12 status commit → archive → finishing-a-development-branch (#705). On PARK: archive ledger with the halt, leave #705 draft, retire :5183/:5177.
- PR #705 body updated (rulings, perf table, NEUTRAL verdict, buys/costs, eye-check PENDING). HALTED awaiting USER land/park + eye-check.
- Eye-check by user 2026-09-14 (:5183 vs :5174): PASS, four poses. User ruling on NEUTRAL perf: LAND (PR #705 ready for user code review). Ruling: deletion audit deferred to the terrain feature's /feature-done — P1 is ground prep and P6 is net-deleting (−128); cost if wrong: one audit pass later.
