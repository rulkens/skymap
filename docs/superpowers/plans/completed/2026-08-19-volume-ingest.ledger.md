# SDD ledger — plan: docs/superpowers/plans/2026-08-19-volume-ingest.md

Branch: refactor/volume-ingest (draft PR #583). Base at execution start: 18e2cdf23 (plan commit; code base df590f5d3).
No separate spec — decisions.md is the authority (plan header; rung-2/3 precedent).
User approved at checkpoint ("ok" after the side-door keep-vs-delete question was answered; keep ruling stands).

## Pre-flight conflict scan (2026-08-19)

Task-pair rows (shared files/interfaces):

| pair | shared surface | produce vs consume | finding |
| --- | --- | --- | --- |
| T1→T2 | uploadVolumeField.ts location + name | T1 renames addVolumeField→uploadVolumeField and moves to services/engine/volume/; T2 modifies it there + new test | consistent; order binding 1→2→3 declared in plan |
| T1→T3 | import path of the shared fn | T3's four slot files import from the post-T1 location | consistent, order enforced |
| T2→T3 | normalized body semantics | T3's cross-file id-wiring test drives factories through the T2 body | consistent; T3 after T2 |
| T3/T4 | none shared (T4 = renderer tests + assetWiring comment; T3 never touches either) | — | independent, any order after T3 per plan |
| T5/T6 | decision #14 wording | T6's :249 cell and IMP-node rewrite "must match #14", which T5 writes | plan says 4-6 any order — see Ruling 1 |
| T1/T5 | EngineVolumesHandle docblock vs #14's door ruling | T1 writes the docblock from D3; T5 records D3 in #14 | same source (plan D3), no drift risk |
| T2/T7 | wake behaviour | T2 deliberately writes NO wake test (redundant line, rung 5 owns deletion); T7 smoke observes fades not wakes | consistent |

Per-task self-consistency:

| task | check | finding |
| --- | --- | --- |
| T1 | rename commands use <file>#<symbol> form; disambiguation from settingsSlice action creators noted; move uses post-rename filenames; 3 commits | self-consistent; finding 9 carried in task text |
| T2 | 3 tests vs contract vs findings 2-4 | agrees; null-renderer test asserts absence (no dispatch/fade/wake) — matches "guard the whole body"; no vacuous wake test, justified in task text |
| T3 | 4 call sites + orphan deletion gated on refs re-verify with fallback instruction | self-consistent; cross-file test precedent cited (demandTable.test.ts) |
| T4 | 2 renderer tests + 1 comment line | agrees; tests assert destroy counts + listIds — observables |
| T5 | #14 content list vs plan D1-D7 + findings 1-2 | complete: contract, door, re-open FAILS with the count that adds up, site-4 close+rehand, wake finding, D6, D7, #9 amend |
| T6 | 10 doc-sweep bullets, locate by quoted text | self-consistent; line numbers stamped as-of df590f5d3 |
| T7 | gate + 4-part smoke incl. the DevTools two-dispatch fixture recipe | smoke cannot self-certify — user-eyes checkpoint |

Ruling 1: T6 executes AFTER T5 (batched into one dispatch, T5 first) — T6's cells must match decision #14 as written; the plan's "4-6 any order" is internally inconsistent with T6's own text. Same shape as rung-3 Ruling 1. Cost if wrong: none material (docs-only, same PR).
Ruling 2: dispatch shape — T1 (sonnet, mechanical CLI ops), T2 (sonnet, TDD), T3 (sonnet, migration), T4 (sonnet, TDD small), batch T5+T6 (opus, T5 first), T7 controller gate + USER-EYES smoke, final whole-branch review (opus). Implementers sequential, never parallel.

## Progress

- BASE for Task 1: 18e2cdf23. Briefs task-1..task-7 extracted in this directory.
- Task 1: implementer DONE_WITH_CONCERNS (3 commits 28803ebbb / 334b16537 / 3b3546f42; typecheck clean, full suite 1045 files / 7024 tests green; agent a6979c7a31757215a resumable rounds 1-3). Accepted deviation: alias collapse applied to BOTH renamed files (symmetric twin). Ruling 3: the 3 stale prose comments it flagged (defaults.ts:400, volumeFieldDefaults.ts:57, syntheticVolumeSlots.ts:20) fold into Task 3's comment sweep — carry in T3 dispatch. Review: spec ✅ all items, quality approved, zero behaviour-changing hunks; both typechecks re-run clean. Two nits → T3 sweep: (a) EngineVolumesHandle.d.ts:14 ALL-CAPS "SAME" emphasis style; (b) VolumeFieldRowData.d.ts:14,30 — a 4th stale addVolumeField prose ref. Task 1: complete.
- T3 comment-sweep carry list (Rulings 3 + review nits): defaults.ts:400, volumeFieldDefaults.ts:57, syntheticVolumeSlots.ts:20 (already in plan), VolumeFieldRowData.d.ts:14,30, EngineVolumesHandle.d.ts:14 caps.
- BASE for Task 2: 3b3546f42. Implementer DONE (commit 69d24c197; 3/3 new tests, full suite 1046 files / 7027 green, typecheck clean; agent a91ddd7b582308c48 resumable rounds 1-3). Accepted deviation: param fieldId→id per the plan's contract signature (sole caller positional). Review: spec ✅ (order pinned via invocationCallOrder, delta boundary verified — null-renderer no-op is the only change), quality approved, ZERO findings; both gates re-run clean. Task 2: complete.
- BASE for Task 3: 69d24c197. Implementer DONE (3 commits c422b95e2 migration+sweep+test / d7ac44c6a orphan deletion / db1f8a91d carry-list; typecheck clean, full suite 1045 files / 7024 green; agent a48e6d73b595cc7a0 resumable rounds 1-3). PARKED for final-review fix round: volumeFieldDefaults.ts:57 "the volume slots" clause now slightly stale post-dead-seed-deletion (naming itself is accurate). Review: spec ✅ all items (ids verified per-site vs SOURCE_REGISTRY; unreachable-seed argument confirmed via fadeLayers.ts:314 gate; comments 142→77 lines −46%; carry-list verdicts all correct), quality approved. One nit PARKED for final-review fix round: settingsSlice.ts:326 comment still references deleted writeVolumeFieldSetting. Task 3: complete.
- Final-review parking list so far: volumeFieldDefaults.ts:57 stale "the volume slots" clause; settingsSlice.ts:326 dangling helper ref.
- BASE for Task 4: db1f8a91d. Implementer DONE (commit 537bcf8a4; volumeFieldRenderer.test.ts 10/10 incl. 2 new; renderer untouched; assetWiring one comment only; agent a34ed1d11df3df670). Review: spec ✅ (per-instance destroy spies verified non-vacuous; .slice(-2) index trick validated vs constructor buffer order), quality approved; one non-deviation nit ("one line" read as "one comment", 3 wrapped lines matching file style — accepted). Task 4: complete.
- BASE for T5+T6 batch: 537bcf8a4. Batch implementer DONE (2 commits: 58d1599ef decision #14 + #9 amend / 4d19142b9 four-map sweep; agent af06ac2bf4706dacd; report task-5-6-report.md). 3 concerns in report: (1) prettier corrupts identifiers in engine-composition-map.md + subsystem-sweep.md (CONTENT*LAYERS) — implementer reverted format, content-only edits; CI has no format:check gate; (2) filamentRenderer.clear "no callers outside module" vs one test-file caller — precise version used in outliers row; (3) unloadVolumeField.ts:20 false "// Essential wake" comment (removeVolumeField on same wake route) — code file, out of batch scope → carried to final-review parking list / rung 5. Reviewer dispatched (sonnet, review-537bcf8a4..4d19142b9.diff, verdict → task-5-6-review.md). Review: spec ✅ lenses 1/2/4/5, lens 3 ⚠️ ONE blocking doc-only finding — renderer-layer-outliers.md:180 mermaid F4 node still says "volume ×3", contradicting the swept :201 table row ("×5 (not ×3)"). All code spot-checks confirmed; implementer's 3 concerns all check out. Fix round 1: implementer resumed, DONE (commit acab0e55f — F4 node → "divergence + volume ×5", one file one line). Scoped re-review: ADDRESSED (1 file, 1 line, no new problems). Tasks 5+6: complete.
- BASE for Task 7 gate: acab0e55f. Gate PASSED: npm run typecheck clean, galaxy-renderer tsc clean, full suite 1045 files / 7026 tests green. Smoke checklist presented to user (toggle 3 volumes off/on, tier switch w/ mcpm, DevTools debug-gaussian two-dispatch recipe) — awaiting USER EYES.
- Final whole-branch review dispatched IN PARALLEL with the user smoke (opus, review-18e2cdf23..acab0e55f.diff, 11 commits; pointed at parking list a: volumeFieldDefaults.ts:57, b: settingsSlice.ts:326, c: unloadVolumeField.ts:20 false wake comment; verdict → final-review.md). Merge still gated on BOTH smoke pass + review clean + user's explicit merge word.
- Final review verdict: FINDINGS, none blocking (behaviour-neutral verified: reevaluateDemand/makeRunTierTransition/reevaluateDemand.test byte-identical to base; seed dispatch traced neutral through watchFadesSaga/watchHashWriteSaga/watchWakeSaga; no action-creator rename leak). 5 should-fix + 7 nits, ALL zero-runtime (stale/false comments, twin param name id vs fieldId, test-name overclaim). Parked a/b/c all CONFIRMED → fix now. Ruling: ONE fix dispatch covers all 12 findings + c's wording (relabel "redundant-but-local, rung 5 owns wake accounting"; keep the requestRender() line) — cost if wrong: comment churn only. Full report final-review.md. Fix-round implementer dispatched (sonnet, all 12 findings, one commit, report → final-fix-report.md); scoped re-review to follow, then push + report to user. Smoke: USER CONFIRMED PASS ("smoke test done", 2026-08-19). Task 7: complete pending final fix round + scoped re-review. Remaining: fix round lands → re-review → /feature-done on branch → push → user's explicit merge word for PR #583.
- Final fix round DONE (commit 11badd382, all 12 findings, no deviations; typecheck clean, 9 files / 26 targeted tests green; report final-fix-report.md). Scoped re-review: ADDRESSED with ONE residual — finding 1's rewrite copied the TWIN's caller list ("the volume slot commits") into unloadVolumeField.ts:3-4; only real caller is createEngine via handle.volumes.remove. All 11 others verified clean (requestRender intact, rename call-site-neutral, headers ≤10, gates reproduce). Implementer resumed for the one-clause reword; controller verifies the diff directly (no third review round — one comment line).
- Residual fix DONE (commit 9d4dd9912, 1 file, comment-only; controller verified the diff by inspection — header now names the real caller set). Final review: CLEAN.
- /feature-done audit at HEAD 9d4dd9912: typecheck PASS, full suite 1045 files / 7026 tests PASS, checkboxes 54/54 ticked (DoD present), file inventory matches plan (rename R050 detected for removeVolumeField→unloadVolumeField), zero TODO/FIXME markers in touched files, backlog clean (dust-volume detail file is a separate effort, left), smoke USER-ATTESTED this session, no spec by design (decisions.md is authority, rung-2/3 precedent). OVERALL: READY. Completion moves: plan → plans/completed/, ledger archived beside it. Merge of PR #583 still awaits the user's explicit word.
- Final-review parking list addition: unloadVolumeField.ts:20 "// Essential wake" comment is false (same wake-route argument as difference 2) — fix in final-review fix round or hand to rung 5.
- Queued sequence (binding): T1 → T2 → T3 → T4 → batch T5+T6 → T7 gate + smoke → final review (opus, review-package 18e2cdf23..HEAD) → report with "Rulings I made".
- Merge policy: PR #583 (draft) merges ONLY on the user's explicit word, via gh api PUT squash, announced. /feature-done runs on the branch first.
- Session artifacts: board https://claude.ai/code/artifact/4027c9f0-17bf-42e4-a686-7aa096283852 (republish same scratchpad file engine-ladder-board.html); surveys scratchpad/rung4-{mandates,ingest-copies,assetslot-contract}.md.
- Pre-execution history: 3 surveys → plan authored (opus) → adversarial review COMMIT-WITH-AMENDMENTS (5 blockers + 6 should-fix) → all applied → committed 18e2cdf23 → user checkpoint passed (side-door keep ruling explained and accepted).
