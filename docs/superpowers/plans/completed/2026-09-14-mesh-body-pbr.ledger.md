# SDD ledger — plan: docs/superpowers/plans/2026-09-14-mesh-body-pbr.md

## User rulings (2026-09-14, user AFK — run to completion)
- Feature on its OWN branch/PR stacked on the prep branch: worktree .claude/worktrees/mesh-body-pbr-feature, branch worktree-mesh-body-pbr-feature, cut from prep HEAD 90b3eadb2 (before the prep fix wave; REBASE onto the final prep branch before opening the PR). PR #708 becomes the prep PR.
- Perf gates SKIPPED (task 1 perf + task 11 perf). Task 1 baseline screenshots skipped (user AFK); the task-10 visual gate compares against the deployed version, as for #697.
- No deletion audit on prep; the feature's /feature-done runs it once for the whole stack.
- Parallelism: wave 1 = T2 (bake, agent runs Blender in its worktree), T3 (LUT tool), T5 (capture rows) in per-task worktrees pbr-feat-t2/t3/t5 (branches wt-pbr-feat-*), cherry-picked onto the feature branch; wave 2 = T4 (after T3) → T6 (after T4+T5) → T7 → T8 → T9 sequential in the feature worktree.

## Pre-flight conflict scan
| pair | shared surface | finding |
|---|---|---|
| T2 × prep fix wave | meshPrebake.py comments | possible textual conflict at rebase; resolve by hand. |
| T3 × T5 | none | — |
| T4 × T6 | meshBodyRenderer.ts, fragment.wesl, MeshBodyRenderer.d.ts | sequential T4 → T6. |
| T5 × T7/T8 | CaptureStepSpec, scheduleCubemapCaptures, frame/ | sequential T5 → T7 → T8. |
| T9 × T6 | fragment.wesl, io.wesl | sequential, T9 last. |

## Progress
- Task 3 (LUT): implementer DONE_WITH_CONCERNS (could not git in its worktree — controller committed 24028b83e on wt-pbr-feat-t3 via scratchpad/wt-git.py; ruling: f32ToF16Bits RNE instead of floatToF16). Review dispatched. LANDMINE: subagents cannot run git outside the session worktree even with EnterWorktree(path) — commit on their behalf with wt-git.py; wave 2 runs in the feature worktree with the session switched there.
- PREP DONE: completion commit 0c0b4bc7d pushed; PR #708 retitled as prep PR, marked ready — USER merges. Feature branch reset to 0c0b4bc7d (no feature commits yet); per-task worktrees still at 90b3eadb2 → cherry-picks may conflict on READMEs/meshPrebake.py (T2).
- Task 3 (LUT): complete — review APPROVED A+B (decoded LUT values match independent quadrature), 0 blocking, 3 minor (dead voH guard :55; radicalInverseVdC home; CLI-guard comment) → fix wave. Cherry-picked onto feature branch.
- Task 4 (LUT at boot): dispatched in worktree pbr-feat-t4 (branch wt-pbr-feat-t4 off feature 8d8eda62f); controller commits via wt-git.py.
- LANDMINE: T2 agent edits the SESSION (prep) worktree, not pbr-feat-t2 (relative paths). Plan: when it reports, `git diff` + untracked in prep → apply onto feature worktree via wt-git.py apply, commit there, then `git checkout --` the prep files. Its build-meshes writes through public/data symlink into MAIN public/data (new _normal/_mr hashed files + manifest) — acceptable, same as the post-merge bake.
- Task 5 (capture rows): implementer DONE_WITH_CONCERNS (uncommitted → controller committed bf9d86da3 on wt-pbr-feat-t5; suite 1342/9269 green); cherry-picked onto feature; review dispatched (fable). Concerns: rosterSettling re-bakes rebakeOnSettings:false rows during a fade (as briefed); captureFaceAttachment throws for probe rows until T7; 12 unused view slots until T7–8; unlisted fix passTimingSlotName carries the capture key.
- Task 4 (LUT at boot): implementer DONE_WITH_CONCERNS (uncommitted → controller committed 3062a081e on wt-pbr-feat-t4; extra touches engine.ts/wireInput/buildSwapRenderers justified by GpuHandleConstructDeps); cherry-picked onto feature as 975102a38; review dispatched. T6 dispatched in pbr-feat-t6 off 975102a38.
- Task 2 (bake): DONE_WITH_CONCERNS — agent committed 9e6572ba6 on wt-pbr-feat-t2-bake (worked in the session worktree); cherry-picked onto feature as c43bc8467. Findings: Blender exporter packs MR; UV re-point bug fixed (perseverance normals were flat); only Perseverance authors metal; METAL ALBEDO BAKES BLACK under DIFFUSE/COLOR → CONTROLLER RULING: bake the albedo row as EMIT of Base Color (F0 tint for metals is the albedo; dielectrics identical) — follow-up task T2b; rovers get authored roughness (~0.5) instead of forced 0.7 = expected look change for the visual gate. SESSION LIMIT hit ~19:20: T6 + T4/T5 reviews died at start → re-dispatch. Session switching into the feature worktree; per-task worktrees removed; ledger copied to feature worktree .superpowers/sdd/2026-09-14-mesh-body-pbr/progress.md (authoritative from here).
- Task 4 (LUT at boot): complete — review APPROVED A+B, 0 blocking, 4 minor (engine.ts:194 comment scope; redundant asserts meshBodyRenderer.test.ts:127-128; stale title :114; BYTES_PER_TEXEL note) → fix wave. Ownership shape kept (fontAtlases precedent).

## In flight + handling (2026-09-14 ~22:15, after session-limit restart)
- T2b implementer (opus, shared tree): albedo row → emitted Base Color + rebake ×4 + regenerated table; on report → review package `git diff c43bc8467..<sha>`, reviewer opus → ledger; blocking → one fix round.
- T6 implementer (fable, shared tree): MeshProbe/MeshResources.probe/prefilterCubeGgx; on report → review (fable) → then dispatch T7 in the shared tree (brief task-7-brief.md), then T8, then T9 sequentially, each with a review.
- Reviews in flight: T2 (opus), T5 (fable) → ledger; blocking findings → fix round resuming a fresh opus implementer in the shared tree.
- Queue after T9: fix wave over all feature minors → final whole-branch review (fable) → real `npm run build-meshes` in this worktree (writes through public/data symlink into MAIN public/data + manifest; note in PR) → dev server for this worktree (new port; :5179 is the prep worktree) → open DRAFT PR --base worktree-mesh-body-pbr → user T10 visual gate → /feature-done (deletion audit once, whole stack) → user merges #708 then the feature PR → post-merge R2 sync of public/data/meshes from main.
- Perf gates skipped (user). Baseline screenshots skipped (user AFK).
- Task 2 (bake rows): complete — review APPROVED A+B, 0 blocking, 6 minor (repoint call inside log arg; sweeps all materials vs docstring; curiosity/mer README "roughness 0.5" wrong; perseverance README "11 materials" is the re-point count; GLB log line absent; mip-bleed of black atlas margin into roughness/metallic at distance → watch at visual gate) → fix wave.
- Task 6 (probe + prefilter): implementer DONE_WITH_CONCERNS — committed acfc4433b (shared tree, path-scoped). Rulings: PREFILTER_SAMPLES 512; uniform `params`; PROBE_BINDING module const; renderer imports CUBEMAP_CAPTURES directly (controller: accept — seam via factory input only if the reviewer objects); params buffer destroyed post-submit; pipeline per call. Depth usage RENDER_ATTACHMENT only (T7 may add TEXTURE_BINDING). vite build tree-shakes the prefilter module → browser console check at T9 USER-RUN. Review dispatched (fable), package review-t6.diff.
- Task 5 (capture rows): complete — review APPROVED A+B, 0 blocking, 8 minor (CaptureStepSpec.d.ts / cubemapCaptures.ts headers over 10 lines; duplicate 1.5 margin const; single-use SOLAR_SYSTEM_SKY_FACE_PX + rationale ×3; five stale scheduleCubemapCaptures/CubemapCaptureRuntime pointers incl. a backlog doc; two stale "1..6" slot docs; cubemapFaceContext docblock omits pose distance sizes NEAR0 near plane (P1 lens faces 100 pc → ~1.5 km, flag for lens smoke); pre-existing roster mis-list renderTargets.ts:289) → fix wave. Reviewer rulings: rosterSettling re-bake of rebakeOnSettings:false row KEEP; captureFaceAttachment throw fine until T7; 19 slots correct (solarSystem 7–12 live now, probe 13–18 idle until T8). ADJACENT for T8 dispatch: frameContext.ts:195 subtracts the focus pivot radius from the synthetic capture distance — negative at the probe's 1 m, survives only on frustum floors → T8 brief must address.
- T7 dispatched (fable, shared tree, frame files only) in parallel with the T6 review and T2b — disjoint paths. On report → review package `git diff -U10 --stat -p acfc4433b <sha>` (T7 base is acfc4433b; if T2b commits first, diff only T7's paths) → reviewer (fable) → T8 (brief task-8-brief.md + the frameContext.ts:195 pivot-radius adjacent finding) → T9.
- T2b (albedo as emitted Base Color): implementer DONE_WITH_CONCERNS — 419d8745c. swap_to_emission(socket) generalisation; NEW FINDING: both .blend rovers have a rival Cycles-only Material Output (bare Diffuse BSDF) that won over the Principled — unify_shader_outputs() keeps the active output; T2's roughness on those faces was 1.0 not the authored 0.5. Dielectric albedo shift ×1.037 (specular no longer subtracted); perseverance meanAlbedo 0.029 → 0.069. public/data/meshes still holds the T2 bake → real build-meshes before the visual gate (already queued). Review dispatched (opus), package review-t2b.diff.
- Task 6 (probe + prefilter): complete — review APPROVED A+B (reviewer linked the WESL and hand-typed every line; face table = inverse texture_cube convention; attachment views/dynamic offsets/destroy-after-submit legal; Karis math OK), 0 blocking, 5 minor (α=0 comment muddled; params-vs-p comment records a non-hazard; "128 px" restated in a comment; stale layout-test title; single-level no-op test unreachable) → fix wave. Rulings 1–7 accepted (CUBEMAP_CAPTURES direct import stays). ADJACENT: 512-tap mip-0-only sampling may sparkle at the roughness-1 mip over a star field → watch at T10; shader untested by a real WGSL compiler until T9 console check.
- T2b: complete — review APPROVED A+B (reviewer re-measured atlases; ratio 1.037 ≈ 1/(1−F0); meanAlbedo's only consumer is label tint hue → inert), 0 blocking, 6 minor (is_active_output can be True on both outputs → pick falls to node order, silent if wrong: log the selector; undo-order no longer guarded by "metallic is last"; curiosity README "roughness 0.5" still false (10,761 texels at 255); perseverance README past tense; "Seven materials" is a node count; no-Principled log understates albedo bakes black) → fix wave. Ruling unify_shader_outputs UPHELD (in-memory only; Blender --background never saves; .blend is a symlink into main). ADJACENT: plan Task 2 contract block stale (says DIFFUSE/COLOR) → fix in plan at wrap; metals render bright-diffuse until T9; T10 baselines expect 3 shifts (roughness 0.7→authored, rival-output faces, metal tint).
- Task 7 (capture faces per command buffer): implementer DONE_WITH_CONCERNS — 47dfaaa37 (on 419d8745c). Rulings to adjudicate via review: finishCubemapCapture takes device as 3rd param; two in-function arms in captureFaceAttachment; state.cubemapCaptures.probe direct read; sky line gained bodyPasses: []; no probe·NEAR0 title row. Review dispatched (fable), package review-t7.diff. T8 waits for the review (shares frameOrder.ts / passGroupTitles.ts with a possible T7 fix round). T8 dispatch must carry: pivotSurfaceRangeMpc (src/services/engine/camera/) subtracts the REAL focus's pivot radius for any 'absolute' pose → a probe face ctx at nearMpc (~1 m) gets a hugely negative altitudeMpc into deriveSlabs when Mars/rover is focused; T8 must make the capture ctx's altitude focus-free at the smallest seam + test.
- Task 7: complete — review APPROVED A+B, 0 blocking, 6 minor (CaptureFaceAttachment.d.ts single-site → inline/delete; renderFrame.ts header 12 lines; CaptureStepSpec.d.ts header 11 + restated field doc; submit-i↔encoder-i assertion; renderFrame.test.ts "exactly one encoder" titles conditional; duplicated subject→probe→throw in two files) → fix wave. All 5 implementer rulings upheld. ADJACENT: executeFrame.ts 71-line header pre-existing; at T8 check unfired probe·NEAR0 slot rows + the 4096 timestamp-query budget; Sgr A* USER-RUN gate owed.
- REVIEW POLICY (controller proposal to user 2026-09-15, six reviews / 0 blocking): T8 gets NO separate task review; T9 keeps its review (shader, no compiler gate); then ONE final whole-branch review + one fix wave. T8 dispatched (fable).
- Task 8 (probe scheduling + sky blit + probe line): complete (NO separate review per policy) — 987a2cd50 (on 47dfaaa37). Rulings accepted: (1) capture-face altitude = optional trailing `altitudeMpc?` on deriveFrameContext, cubemapFaceContext passes nearMpc (+test); (B) blit gate = `skyCaptureBandAlpha('solarSystem', state, ctx) > 0` (generalised from sgrAStarBandAlpha; PassState excludes cubemapCaptures per P1) instead of the brief's bakedSettings read — ACCEPTED, sounder (bakedSettings nulls during every fade); (C) three renderFrame fixtures gained meshBodyRenderer: null; finish-order test now expects one finish per row. Timing budget 1533 slots → 3066 queries ≤ 4096; probe row bills 1146 (≈ +8 body rows would breach → note in PR). For final review: no skyCubemapBlitPass unit test; band evaluated at the face eye vs row allocated on the camera band. USER-RUN gates owed: cubeFaceBlit console compile, ?gpuTimings probe cadence at Curiosity/Voyager/away, "Mars under the rover" blit sign. T9 dispatched (fable, shared tree, base 987a2cd50).

## Task 9: implemented — 6568432ff (fable, shared tree, base 987a2cd50), DONE_WITH_CONCERNS
Rulings by implementer, accepted by controller: (1) env diffuse weight = 1 − (f0·scale + bias) from the LUT, no F90 formula; (2) alpha' = clamp(alpha + 0.5·sin, alpha, 1) with MIN_ROUGHNESS-floored alpha is the guard; (3) brief deviation — MPC_TO_M forbidden in frame/ by oneMpcSeam gate, so new `src/utils/scene/sinSunAngularRadius.ts` owns the conversion (mirrors sunVisibleFraction), pass consumes the scalar; (4) MeshBodyRenderer.d.ts untouched (already correct); (5) sunRadiusM hoisted in draw.
Concerns: dist/ rebuilt (gitignored, harmless); night side has no floor by design (T10 watch item); pbr.wesl over comment budget (pre-existing). Report: scratchpad/feat-task-9-report.md.
T9 review dispatched (fable, shader-focused); package review-t9.diff.

## Task 9: review — SPEC ✅, QUALITY APPROVED, 0 blocking, 4 minor (comment-level) → fix-wave-brief.md §Task 9
Reviewer also linked all four WGSL modules with the project's wesl linker and validated with naga 22: OK. Uniformity hand-checked clean.
Report: .superpowers/sdd/2026-09-14-mesh-body-pbr/review-t9-report.md
## Task 9: complete — 6568432ff
NEXT: fix wave (fresh opus implementer, shared tree, fix-wave-brief.md, `fix(mesh-body-pbr):` commits) → final whole-branch review (fable, diff 0c0b4bc7d..HEAD, covers T8) → one fix dispatch + scoped re-review → plan Task 2 contract block update → `npm run build-meshes` → dev server → draft PR --base worktree-mesh-body-pbr.
SESSION HANDOFF 2026-09-15: user switching account ludens → personal; new session resumes from this ledger at "fix wave".

## Fix wave: complete — e6c4ac39b (T3/T4/T2/T2b), d4eafb449 (T5/T7), 60e1233c8 (T6/T9); report fix-wave-report.md. Deviations: T2 GLB log line → README note; T8 no test (restates band gate); T5 item 4 two pointers valid again post-T8.
Ruling: pre-existing red test tests/visual/renderFrameSplitBaseline.test.ts (from T8: fixture omitted meshBodyRenderer → undefined ≠ null → probe scheduler ran on a mock with no state.data) — fixed in the fixture, 70b95017a; cost if wrong: none, the scheduler's null gate is unchanged.
Plan Task 2 contract block updated 9f759160b. build-meshes run (tree unchanged — hashes already current); build-data-manifest run via `npm --prefix <main>` because the symlinked worktree skips it: 24 renamed / 12 removed in MAIN public/data (note in PR). Dev server: http://localhost:5181/ (bg task in personal session). User T10 visual gate can start now.
Final whole-branch review dispatched (fable): package review-0c0b4bc7d..9f759160b.diff (14 commits).

## Final review (fable): 1 Critical, 2 Important, 9 Minor — final-review-report.md
## Final fix wave: complete — C1 e1495ca2a (probe captured in host axes via cubemapFaceContext `axes`), I1+I2 971f0eeea (probe `due` → shouldKeepTicking vote; capture-line names ⊆ CONTENT_PASSES test), minors 4bca3d568 (M1, M6–M9 left, reasons in final-fix-report.md). Suite 1349/9307 green.
Backlog entry 4badad34f: mesh sources as editable .blend + R2 backup (user request; follow-up after this PR).
NEW DEFECT (found by fixer, load-bearing): probe face pose distance = nearMpc (1 m) → target − eye collapses at a solar-system eye → host body basis zero, face vp NaN → Mars never draws into the probe.
Ruling: forward from the face BASIS, nearMpc stays near plane only; test at a Mars-distance eye — dispatched (opus) together with user-requested display fold of `·FACE[n]` timing rows (pure util + GpuTimingsSection). Cost if wrong: one more scoped re-review round.
NEXT: scoped re-review of e1495ca2a..HEAD (fable) → draft PR --base worktree-mesh-body-pbr → user T10 visual gate ("Mars under the rover" = C1 + NaN fix acceptance) → /feature-done.
Data facts for the PR body: NASA sources author metallic 0 / roughness 0.5 everywhere (atlases faithful) — "dish reads as metal" needs the .blend edit path (backlog).
Probe-face fix + display fold: complete — e04f88fc8 (forward from pose basis via src/utils/camera/orbitForwardOf.ts at 4 shared sites: frameContext, computeViewProj, slabs, cameraBillboardBasis; main view shifts at rounding level; Voyager-distance test), 804d2e631 (foldCaptureFaceRows + GpuTimingsSection). Suite 1350/9317 green. FINDING: collapse is real at ≥1e-6 Mpc eyes (Voyager skew 4e-4..0.18), NOT at Mars — a missing Mars glow at T10 has another cause.
Scoped re-review dispatched (fable): package review-9f759160b..804d2e631.diff; asked for main-view forward equivalence proof (poles, non-identity upBasis) + slabs.test.ts relaxation + unused normalize3 import in frameContext.ts.
Scoped re-review (fable): ALL ADDRESSED, (a) orbitForwardOf identity PROVEN for the main view (poles, any poseBasis; upBasis unread), (b) fold correct; no new C/I; minors: per-face Mat3 alloc, no direct orbitForwardOf test. Report final-rereview-report.md.
DRAFT PR #714 opened 2026-09-15 --base worktree-mesh-body-pbr, HEAD 804d2e631 pushed. Diff vs prep: src +1359/−337, comments +518/−266, tests +1128/−134, tools +263/−22, docs +106/−39.
NEXT: user T10 visual gate on :5181 → fixes if any → /feature-done (deletion audit ONCE over prep+feature; none on prep alone) → user merges #708 then #714 → post-merge from main: build-meshes + manifest + R2 sync public/data/meshes. Cleanup owed: prep dev server :5179, this dev server :5181, agent worktrees (none created this session).
2026-09-15 late: user asked "ready to merge?" — answered NOT YET. Order: user T10 attestation on :5181 → /feature-done (deletion audit once over prep+feature) → user merges #708 → GitHub retargets #714 to main → CI runs (workflow triggers ONLY on PRs to main, so #714 has no CI yet) → merge #714. No agents in flight. Tree clean at 804d2e631 (pushed).

2026-09-15 late: T10 GATE PASSED (user). #708 squash-merged via API → main d1ab1b3ca; #714 retargeted to main BEFORE remote branch worktree-mesh-body-pbr deleted. #714 CONFLICTING: branch forked at 536329ea8, main gained #702 #707 #705 #703 #704 (radiusM→surface) #700 #712 + prep squash. Merging origin/main INTO the feature branch (no force-push); one Opus agent resolving per merge-main-brief.md → merge-main-report.md. lensBodySlabs.ts was renamed bodyRowSlabs.ts on main (feature edit to be ported). NEXT after merge commit: push → CI on #714 → /feature-done → user merges #714 → from MAIN: build-meshes + build-data-manifest + R2 sync. Prep worktree :5179 server still owed cleanup.
