# SDD ledger — plan: docs/superpowers/plans/2026-09-15-terrain-f2-displacement.md

Branch `terrain-f2-displacement` off `terrain-f1-height-products` (937182c68), worktree `.claude/worktrees/terrain-f2-displacement`, STACKED draft PR #719 (`--base terrain-f1-height-products`). Lean protocol. Controller in this worktree (entered via EnterWorktree from the F1 session); implementers are plain agents here, serial. `public/data` → main's (F1's v8 bake). Plan commit a1089a84c.

## Two questions (defaults, user asleep — overnight run approved 2026-09-15)

- Parallelism: single lane, serial dispatches D1 → D2 → D3.
- Perf gate: **USER-RUN only** (F2-R6; user: "dont run the perf test, thats for sure"). Agents never run `npm run perf`.

## Dispatches

| D   | tasks | model  | review                     |
| --- | ----- | ------ | -------------------------- |
| D1  | 1–2   | opus   | yes (edge code + 80 B rec) |
| D2  | 3–6   | opus   | yes (shader + numerics)    |
| D3  | 7–8   | sonnet | no                         |
| T9  | —     | user   | eye-check + USER perf      |

Then one final whole-branch review + one fix round, no re-review. STOP at T9 (visual gate).

## Stacking rules

- Rebase onto `main` only after F1 (#713) squash-merges, at a dispatch boundary. Never merge from the worktree.
- If F1 gets fix commits from the user's eye-check, `git rebase terrain-f1-height-products` here at the next dispatch boundary.

## Dispatch log

- 2026-09-15 D1 (T1–T2) dispatched, opus, plain agent in this worktree, BASE = a1089a84c. Handling: read reply, `git log --oneline a1089a84c..` = 2 commits + pushed, typecheck:fast, `npx vitest run tests/utils/scene tests/services/gpu` green, `npm run build` passes (WESL link gate); then dispatch the D1 REVIEW (opus, read-only) of T1+T2 vs spec §6 R12 / §7 record table / F2-R1; sonnet fix round if findings; then D2.
- 2026-09-15 F1 CI GREEN on #713 (typecheck·test·format pass 4m50s, Workers build pass).
- 2026-09-15 D1 LANDED: a1089a84c → 40250e177 (7f6c4ab40 T1 edge code, 40250e177 T2 80 B record + height atlas binding 2), 74 turns, ~141k tokens. typecheck:fast + 1325 tests + npm run build green. Deviations: cutSurfaceTiles untouched (NO_COARSER_EDGES typed via SurfaceCutTile); no 'one bit' sentence existed in balanceSurfaceCut header; earthPass.test.ts stub gained getHeightAtlasView + a height-gate test; layout lane test extended for x↔y swap.
- 2026-09-15 D1 REVIEW dispatched (opus, read-only): T1+T2 vs spec §6 R12 / §7 record table / F2-R1. Handling: findings → sonnet fix round; then D2 (T3–T6, opus).
- 2026-09-15 D1 REVIEW back: CLEAN. Both test expectations re-derived by hand ✓; offsets 64/72/80 ✓; no behaviour change ✓. Deferred to D3: (1) HEIGHT_SLOTS_PER_ROW duplicated in earthSurfaceTileRenderer.ts:41 vs HEIGHT_ATLAS_SLOTS_PER_ROW in surfaceTileSubsystem.ts:58 → one constant in earthTileParams.ts; (2) spec lines 118/154/441/508 still say edgeCoarser is 1 bit per edge → amend in the D3 docs commit. NITs (3) height-gate conjunct unreachable-but-plan-mandated, (4) >>>0 no-ops — leave.
- 2026-09-15 D2 (T3–T6) dispatched, opus, plain agent in this worktree, BASE = 40250e177. Handling: read reply, `git log --oneline 40250e177..` = 4 commits + pushed, typecheck:fast, `npx vitest run tests/utils/scene tests/services/gpu tests/services/engine/frame` green, `npm run build` passes; then dispatch the D2 REVIEW (opus, read-only) vs spec §5.5/§7.1–7.3 + F2-R2/R3/R7; sonnet fix round if findings; then D3 (T7–T8 + deferred D1 findings, sonnet).
- 2026-09-15 D2 LANDED: 40250e177 → 2f1c5eb07 (a8b09ef6b T3 displacement n=64, 26782c983 T4 edge collapse, d0063558d T5 skirt ring, 2f1c5eb07 T6 fragment normals + normal map off tiles), 114 turns, ~215k tokens. typecheck:fast, 1345 tests, npm run build green; WGSL validated with wesl link + naga (no tint on this machine). Deviations: SURFACE_TILE_SKIRT_DEPTH_FRACTION parity test added; validation = wesl+naga. EYE-CHECK NOTES: binding 8 is now a hole (cloud stays at 9); skirt ring always present (degenerate on non-seam edges) — dark rim at the limb ⇒ the 0.05 knob; collapsed-edge vertices take the snapped (s,t) for uv/tangent too ⇒ look for SHADING seams at one-level LOD boundaries; shading discontinuity at base-globe/tile boundary inside the fade band is by design (globe keeps its normal map).
- 2026-09-15 D2 REVIEW dispatched (opus, read-only): T3–T6 vs spec §5.5/§7.1–7.3, F2-R2/R3/R7. Handling: findings → sonnet fix round (one); then D3 (T7–T8 + deferred D1 findings, sonnet).
- 2026-09-15 D2 REVIEW back: CLEAN. T3–T6 all OK by hand derivation (§7.1 distribution, row flip, collapse, all four skirt windings, normal frame handedness). NITs → D3: (1) fragment.wesl:144 eHat not Gram-Schmidt'd vs Ng / nHat unnormalized (residue ~1e-7) → orthogonalize or one 'why not' line; (2) surfacePatchIndices.test.ts:53-55 two literal-arithmetic assertions → keep only indices.length === 26112; (3) vertex.wesl 12-line header → fold to ≤5 or declare the exemption.
- 2026-09-15 D3 (T7–T8 + deferred D1 findings 1–2 + D2 nits 1–3) dispatched, sonnet, BASE = 2f1c5eb07. Handling: read reply, `git log --oneline 2f1c5eb07..` ≥2 commits + pushed, typecheck:fast, `npx vitest run tests/services/engine tests/services/gpu tests/utils tests/data` green, npm run build; then FINAL whole-branch review (opus) → one sonnet fix round → STOP at T9 (user eye-check).
- 2026-09-15 D3 LANDED: 2f1c5eb07 → d1affcec0 (62e150bfb T7 base globe at inner bound + reliefM [-430,8849]; a81eaa947 follow-ups: one HEIGHT_ATLAS_SLOTS_PER_ROW in earthTileParams, fragment tangent Gram-Schmidt, test/header trims; d1affcec0 T8 RENDERER.md + spec F2-R1 amendments). 4771 tests + build green. USER FLAG: golden traces driverGoldenTrace.json + settleGoldenTrace.json RE-RECORDED — reliefM's +8.8 km outer bound moves body-framing distances by ~0.14% through bodyFootprintRadiusM (F2-R4 consequence); fixtures in focusFraming/focusReleaseWhileEngaged/bodyDrawRadiusM/selectionHaloTable tests now derive from the real bounds.
- 2026-09-15 FINAL REVIEW dispatched (opus, read-only, whole branch a1089a84c..d1affcec0 vs plan DoD + spec §7 + rulings F2-R1–R7). Handling: findings → one sonnet fix round, no re-review → start this worktree's dev server → STOP at T9 (user eye-check + USER-RUN perf).
- 2026-09-15 FINAL REVIEW back: READY FOR FIX ROUND, no BLOCKERs, DoD all DONE, first frame clean (bind group {0..7,9} matches layout, one VSOut, uint16 26112 idx). SHOULD: (1) atmosphereParams.ts:31 stale 'zero relief' clause; (3) surfacePatchIndices.test.ts:60,66 derived-length assertion + uint16 test never asserts uint16 → Math.max(...indices) < 65536; (6) plan checkboxes untouched. NIT: (2) AtmosphereParams.d.ts:17 literal 6371; (4) twins over comment budget (surfacePatchPostIndex 7/5, collapseTemplateIndex 10/10, surfaceNormalFromHeightCell 10/16); (5) patchVertexOffsetM.ts:20-23 third copy of §7.1 rule; (7) fragment.wesl header exemption only partly warranted; (8) settleGoldenTrace reflowed 1→768 lines, deltas verified 0.135–0.139% = 8849/6371000.
- 2026-09-15 FINAL FIX ROUND dispatched (sonnet, findings 1–7). Handling: read reply, `git log --oneline d1affcec0..` ≥1 commit + pushed, typecheck:fast, `npx vitest run tests/utils/scene tests/services/gpu tests/data` green, npm run build; then start dev server (background, log scratchpad/f2-dev.log), record port, update PR #719 body, memory; STOP at T9.
- 2026-09-15 F2 DEV SERVER: http://localhost:5176/ (this worktree, background, log scratchpad/f2-dev.log). PR #719 body written (scratchpad pr719.md).
- 2026-09-15 FINAL FIX ROUND LANDED: 1389917cd (all 7 findings). F2 STATE = STOPPED AT T9 (visual gate). HEAD 1389917cd pushed, PR #719 draft (base terrain-f1-height-products), dev server http://localhost:5176/. USER GATES: (1) four-pose eye-check + band seams (EOX box edge, Søndermarken edge) + limb for skirt walls; (2) USER-RUN perf A-B-A-B with --url http://localhost:5176 (n=64 ≈ 2.2M tris/frame; lever EARTH_SURFACE_TILE_MESH_RESOLUTION); (3) confirm F2-R1/R3/R4 and the golden-trace re-record; (4) after #713 squash-merges: rebase this branch onto main, then /feature-done. Not done here: no perf run, no rebase onto main.
- 2026-09-15 DEV SERVERS DEAD: the background `npm run dev` tasks for :5175 (F1) and :5176 (F2) were killed (exit 137) after the controller's turn ended; ports no longer answer. USER: start `npm run dev` yourself in each worktree (F1: .claude/worktrees/terrain-f1-height-products, F2: .claude/worktrees/terrain-f2-displacement) and read the Local: port; the perf run needs --url of the F2 one.
- 2026-09-15 REBASED onto terrain-f1-height-products @ 48cbe9910 (R13 refine-onto-ready-children fix from the user's F1 eye-check: flicker on deeper levels). RENDERER.md conflict resolved keeping F2's bullet + R13 wording. Force-pushed with lease. F1 server :5176 (user-run), F2 server :5175 (bgswqiz7i).
- 2026-09-15 R14 MERGE: rebase over 11 commits conflicted per commit → merged terrain-f1-height-products (bf10e4657) instead → 49a9a6bce. Resolutions: balance + test = F1's (height-level R14, R12 on height level, edge bit 1 only — code 2 NOT emitted yet); SurfaceCutTile = F1 `height {slot, levelDelta, originPosts}` + F2 `edgeCoarser 0|1|2`; RENDERER.md F2 bullet 1 + F1 bullets; spec = F1 + F2-R1 note; renderer reads `height.slot` only (levelDelta/originPosts ignored → wrong heights on inherited leaves until B1). typecheck clean, 3784 tests green, pushed --force-with-lease. Plan amendment R14 (B1 sampler + heightCells@76, B2 base globe always drawn, B3 docs, B4 displacement headroom from resident ancestor's subtree bounds) appended, 073a284f5.
- NEXT: dispatch B1–B4 (opus, this worktree, base 073a284f5). Handling: read reply, `git log --oneline 073a284f5..` = 4 commits + pushed, typecheck:fast, vitest scene/engine/gpu/utils-gpu green, `npm run build` (WESL link), then ONE final review (opus) → fix round → PR #719 body (scratchpad pr719.md) → STOP at the user's F2 eye-check on :5175 + USER-RUN perf.
- 2026-09-15 B1–B4 LANDED (opus, 2nd dispatch — 1st stalled on an API error with no changes): 1b14215cb B1 (lattice.wesl `latticeHeightM`/`latticeCellM`, heightCells @76, twins in utils/scene, code 2 in balance), bad27fe0b B2 (fade deleted; base pipeline opaque, OVER blend removed), b7012e934 B4 (subtreeRangeM on the residency record → probe headroom; test on survivors not cut), 8922e3b62 B3 docs. Deviations 1–9 in the agent reply (notable: fragment uses latticeCellM 4 loads not 16; collapseStride scalar; VSOut heightLattice vec3u; surfacePatchPostIndex row-flip guard gone, flip lives in vertex.wesl + RENDERER landmine). typecheck clean, build (WESL link) clean, 4038 tests green. FINAL REVIEW dispatched (opus, read-only, 49a9a6bce..8922e3b62 + WGSL). Handling: findings → one sonnet fix round → PR #719 body → STOP at user eye-check :5175 + USER-RUN perf.
- 2026-09-15 FINAL REVIEW back: BLOCKER — R14 balances height levels but the cut's MESH levels are unbalanced (balance never removes leaves), so a one-level cut step is a T-junction the stride-2 collapse cannot fix (z13 own-tile leaf: stride 2 is a no-op at even posts); spec §7.3 'same vertex count per edge' premise false. SHOULD: B4 headroom inflates during streaming (base-level ancestor range = Earth relief → 8.8 km margin at ground level); headroom test can't catch a units bug; reliefHeadroom per-probe cost (user perf). NIT: stride 2 at cells=1 reads outside the sub-rect; surfaceNormalFromHeightCell 3-arg signature. CONTROLLER RULING R15 (user to confirm): skirts on EVERY edge (Cesium/Maps), edge code back to 0|1 = lattice step only, code 2 + 3-valued type deleted, stride clamp min(2,cells), headroom clamp min(range/radiusM, chord) + pinned test, normal twin 2-spacing. Plan amendment + Task B5 committed. B5 dispatched (opus). Handling: read reply, commits pushed, typecheck, vitest scene/engine/gpu/utils-gpu, `npm run build`; NO further review round unless B5 deviates — then PR #719 body (pr719.md), STOP at user eye-check :5175 + USER perf.

## B5 LANDED (R15) — f41b4f2c3, pushed, tree clean, typecheck:fast clean (agent: vitest 3778 pass, build green, naga OK)
- Deviations accepted: edgeLatticeStride kept (1-bit), code-2 balance tests re-pointed, F2-R2 amended.
- NEXT: user eye-check :5175 (R15 skirts + headroom) + USER-RUN perf; PR #719 body refresh (scratchpad pr719.md).
- OPEN 2026-09-15 ~13:00: user reports NO tiles on :5176 (F1) and :5175. Data + HTTP verified fine (v8 albedo/height 20684 each, tile URLs 200). Headless probe cannot engage tiles even on the site-rung baseline (:5173) — not a repro. Awaiting user: Earth Tile Atlas debug readout at Søndermarken + what is drawn.
- RESOLVED 15:08: user saw "Not engaged — no manifest, or camera outside Earth" + base globe. Cause = my 11:07 `--product albedo` rerun wrote an albedo-only manifest; runtime gate (surfaceTileSubsystem ~L178) needs height provenance per band. Fixed by `--product height` rerun from the F1 wt (npm --prefix); manifest = 20 bands albedo+height. Tool-fix ask (keep prior provenance on product-scoped runs) pending with user.
- QUEUE (15:20): no agents in flight; F2 tree clean at f41b4f2c3 (pushed). Next in order: (1) USER eye-check :5175 — Søndermarken z19 (flat, small relief only), EOX relief bands Everest `86.92, 27.99` / Grand Canyon `-112.11, 36.10` / Cape Town `18.42, -33.96` / Rio `-43.16, -22.95` / Moorea `-151.70, -16.50` for skirts, edge steps, headroom, R3 water; confirm R15 (+R11/R12/R14) — (2) USER-RUN perf A-B-A-B `--url http://localhost:5175` — (3) refresh PR #719 body from scratchpad pr719.md (+ #713 if needed) — (4) F1 gates on :5176 then merge #713 on user's word, /feature-done, v8 R2 deploy incl. the five regenerated z14 albedo tiles + manifest/index — (5) tool-fix ruling: albedo-only rebake must keep height provenance (failing test first), F1 branch or backlog.

## EYE-CHECK FEEDBACK 16:00 (user on :5175, Everest) — three items
- (1) "quads, stretched texture" at Everest → shading quilting (spec 7.2 forward difference is discontinuous at every post) + n=64 mesh chords over 128 cells + inherent nadir-imagery stretch. SPIKE LANDED b95d565ac (pushed): per-post central differences interpolated across the cell (lattice.wesl `latticePostGradient`, twin utils/scene/latticePostGradient.ts; surfaceNormalFromHeightCell re-signed). One-sided at the sub-rect edge = seam risk along tile edges the spec warned about → USER eye-check decides: keep, or escalate to the spec's one-post apron (height format v2 + rebake). Mesh n=128 = one constant (EARTH_SURFACE_TILE_MESH_RESOLUTION), 4× vertices, USER perf gate. Not yet in spec/plan docs — amend at landing if kept.
- (2) camera sinks to sea level / clips into the mountain → F3 (SurfaceHeightField, ceilingHeightM), explicitly deferred. Nothing to do in F2.
- (3) grey empty rectangles at screen edges → my frustum-cull hypothesis DISPROVED by a brute-force oracle (z13 displaced samples on screen vs. drawn coverage, 8 poses incl. sea-level looking up): zero holes under the old bound too, because a culled child is drawn by its parent. Slab-bound patch (sphere at 1+(min+max)/2, radius 1.5·chord + (max−min)/2, no clamp; tighter and correct in shape; re-points 2 headroom tests) PARKED in scratchpad slab.patch — user ruling keep/drop. Grey rectangles still OPEN: ask the user (a) does the rectangle fill after ~1 s (fetch latency: base globe now sits at datum + reliefM[0], so an unlanded ancestor shows the sunk globe instead of BMNG), (b) debug panel "alloc dropped" / misses while visible, (c) does the earth-lod-overlay tint it (drawn patch) or not (no patch).
- 17:10 USER: Everest looks good, smooth normals WORK (spike b95d565ac accepted; amend spec 7.2 + plan at landing). NEW: Søndermarken cream-white on every slope = city-lights emissive gated on the TERRAIN normal's NoL (slopes facing away from the sun read as night → Black Marble glow). FIXED ad7ab0f69 (pushed): nightLights keyed on dot(Ng, l). Awaiting user re-check on :5175.
- 17:15 USER: atmosphere clips Everest's summit (hard horizontal seam: crisp summit above the analytic ground sphere's horizon, over-hazed terrain below). = spec §2 EXTERNAL DEPENDENCY (depth-blind inside-atmosphere composite; ground sphere = innerBoundRadiusM via atmosphereParams.ts). NOT an F2 item. Froxel attempt (#709) abandoned this morning; Bruneton-tables item was parked "until the terrain mesh lands" → unpark as the follow-on after F2. Note as a known limitation in the #719 body.
- 17:20 USER CONFIRMED: cream fixed (ad7ab0f69). STILL OPEN from the eye-check: grey rectangles at screen edges (three questions above), slab-bound patch ruling (scratchpad slab.patch), mesh n=128 (perf-gated), R15 skirts/edge-step confirm, USER-RUN perf on :5175, then PR #719 body (pr719.md + known limitation: atmosphere seam over terrain, spec §2).
- 17:45 GREY RECTANGLES — investigation state (user: they never fill, also around Søndermarken, only at higher tilt; user suspects the screen-info load decision). FACTS: (a) walk EXONERATED — coverage oracle (pixel rays → datum uv vs cut leaves) with the REAL manifest bands + ideal in-band residency = 0 holes at 30 poses (Søndermarken 300/1000/3000 m, Everest 9/12 km, tilt 60/75/85, yaw 0/90); temp test deleted. (b) headless probe now ENGAGES on :5175 (55/256 albedo slots, height x/1024, all tile responses 200) but the canvas renders black headless and a drag at 1.5 m altitude barely tilts → no visual repro (scratchpad probeTilt.mjs). (c) atlas capacity unlikely: plan ≤ ~80 req/product in the oracle vs 256/1024 slots. LEADING HYPOTHESIS (unverified): a leaf is DROPPED from the cut when NO albedo (or height) ancestor above baseLevel is resident — and the tile sets are SPARSE over water ("land-oriented, most of the grid doesn't exist", fetchSurfaceTileBitmap.ts; a 404 → stream.isFailed → never retried). Over the Øresund/Baltic (and lakes near Everest) the z5–7 global tiles may not exist → leaf dropped → F2's base globe is SUNK to innerBoundRadiusM (B2), so the hole shows sky/haze grey instead of F1's datum BMNG. NEXT CHECK (was refused by the worktree guard mid-command; run as plain separate commands): count files under public/data/images/earth-tiles/v8/{albedo,height}/{5,6,7} vs full grid 2^z·2^z/2; check SCENE_EARTH reliefM/innerBoundRadiusM. If sparse: fix options = (1) base globe back at the datum where no tile covers (draw order/depth), (2) bake global z5–7 water tiles (ETOPO bathymetry + BMNG exist everywhere), (3) leaf with no resident ancestor draws flat at datum from the base texture. Ask the user for LOD-overlay tint (tinted = drawn patch, untinted = dropped leaf) + "plan: N req · N miss · alloc dropped".
- OTHER USER ANSWERS 17:20–17:30: R15 skirts "not really noticeable" (= confirmed, no seams). Smooth normals confirmed. Cream fixed confirmed. Atmosphere seam over Everest = spec §2 external dependency (noted above).

## 2026-09-15 18:10 — grey rectangles: user readout + delegation
- User readout at Everest (86.955E 27.932N, deepest band 13): "height 373/1024 · plan: 124 req · zWin 13 · 0 miss · cut: 38 tiles drawn". 0 miss = not a 404/failed-tile problem.
- User observation: rectangles sit at the BOTTOM of the screen (ground nearest the camera) under high tilt; only z13 EOX areas, never lower-z; also around Søndermarken (z13 ring, tiny relief) — so relief alone does not explain it.
- Camera sinks target to sea level (F3), so camera height above datum ≈ 0 while the walk's horizon cap / frustum / 9-sample bbox all test the DATUM surface.
- Delegated (Sonnet, background): reproduce the pose through cutSurfaceTiles with real bands + ideal residency, instrument which step drops the near z13 leaves, write one failing test, fix only if ≤10 obvious lines. Controller rules on the <10-line reply.
- Asked user: toggle "Earth LOD overlay" (debug panel, key d) on a rectangle — tinted = drawn patch missing texture, untinted = leaf not in the cut.
- 18:20 USER: LOD overlay shows NO tint on the rectangles → no patch drawn at all → leaf absent from the cut (walk cull or heightOf/residency drop), not a texture miss.
- 18:40 PARKED by user ("we don't have the tokens"). Agent found root cause before stop: horizon cull (cutSurfaceTiles step 1) ignores reliefHeadroom → near z13 patches dropped at camera ≈ datum + high tilt; also camera below datum plans NOTHING. Backlog: docs/backlog/2026-09-15-terrain-horizon-cull-ignores-relief.md (+ index line). Repro test patch: scratchpad horizon-relief-repro.patch. Tree cleaned of agent probes; test file reverted.
- 18:50 UNPARKED on user "go": Sonnet agent fixing horizon cull (fold reliefHeadroom into cap + camera-inside-datum branch), applying scratchpad repro patch, deleting the backlog item, committing (no push). On reply: controller checks diff size/expectation changes, pushes, asks user to eye-check :5175 at Everest high tilt + Søndermarken ring, then USER-RUN perf A-B.
- 19:00 USER: new eye-check finding, relief coarsens as tilt rises low over Søndermarken → backlog detail written docs/backlog/2026-09-15-terrain-relief-coarsens-under-tilt.md; INDEX LINE + commit deferred until the horizon-cull agent's commit lands (it edits docs/BACKLOG.md).
- 19:05 USER: also backlog a height-LOD debug overlay (albedo overlay exists, no relief one). Folded into the relief-coarsens detail file as tooling prerequisite; needs ITS OWN index line in docs/BACKLOG.md alongside the relief-coarsens line (both deferred until the agent's commit lands).
- 19:15 USER RULINGS: perf A-B waived for now ("ok for now"); slab-cull patch DROPPED (scratchpad patch deleted); mesh resolution 128 DEFERRED → needs a backlog index line "Terrain mesh resolution 128 `needs-perf`" (queued with the other two lines until BACKLOG.md is free); F1 #713 CAN MERGE → Sonnet agent dispatched to run /feature-done on the F1 worktree, merge main, push, wait CI; controller squash-merges on its green reply, then F2 retargets to main.
- 19:25 F1 LANDING BLOCKED from this session: worktree guard refuses any git/shell targeting terrain-f1-height-products (incl. git -C); subagent deadlocked, nothing done. #713 is DRAFT + CONFLICTING with main. Needs a session in the F1 worktree or main: merge origin/main (resolve), typecheck+tests, /feature-done F1, push, gh pr ready 713, CI, squash-merge. Offered user option 1 (other session) vs 2 (isolated detached agent); awaiting pick.
- 19:40 HORIZON FIX LANDED d8636f2f1 (pushed): step-1 cap widened by acos(1/(1+relief)) using the node's reliefHeadroom; camera-inside-datum early return removed (capCamLen clamp). 34/34 file, 8073 suite green, no expectations loosened. CAVEAT from agent: the parked repro tests pass pre-fix too (their node was step-2 governed); only the new -50 m test is load-bearing. Proof of the user's symptom = eye-check on :5175. Backlog lines committed (relief coarsens, height-LOD overlay, mesh res 128).
- 19:55 USER: horizon fix d8636f2f1 did NOT change the grey holes; "leave for now, record what you tried". Fix KEPT (correct on its own, under-datum test load-bearing). Findings + next steps in docs/backlog/2026-09-15-terrain-grey-holes-high-tilt.md (+ index line). Ships with F2 as known issue → goes in the PR #719 body known-limitations list with atmosphere seam + sea-level camera.
- 20:05 USER: "fix F1 then F2, main deploy is broken (v8 on R2 in prod), fixes as follow-ups". DECISION: land F1+F2 together via #719 (this worktree; the guard blocks F1's). Sonnet agent: merge origin/main (2 conflicts), typecheck+test, push, retarget #719 → main, un-draft, refresh body (scratchpad pr719.md + landed-during-eye-check + known-limitations), CI watch, NO merge. On green reply: controller squash-merges #719, closes #713 as superseded (comment), then tells user the code deploy from main is the remaining step (docs/DEPLOY.md). /feature-done filing for F1+F2 = docs follow-up PR. Skipped for speed: final whole-branch review + deletion audit (user chose landing over fixes).
- 20:45 LANDED: #719 squash-merged → main 343fd14c0 (carries F1); #713 CLOSED as superseded. Merge commit bf822deec (engine.ts logCameraState relocation + runFrame.ts type imports), orphan import dropped 297a500e8. CI: typecheck·test·format + Workers Builds green. Code deploy is automatic on push to main (DEPLOY.md). FOLLOW-UPS: docs PR = /feature-done filing for F1+F2 plans+specs (+ final review + deletion audit skipped at landing); five regenerated z14 albedo tiles + manifest/index R2 sync if still pending; backlog items (grey holes, relief LOD, height-LOD overlay, mesh res 128); Bruneton/depth-aware atmosphere follow-on; F3 surface camera. Worktrees terrain-f1-height-products (:5176) + terrain-f2-displacement (:5175) → wt-clean when servers are done.
- 21:00 USER: "run the sync from latest main, then the docs PR". R2 sync running in background from this worktree (public/data → main's dir via symlink; tools code == main after the merge); log scratchpad/sync-r2.log (creds from keychain, never print). Docs agent (Sonnet) on branch terrain-feature-done-docs off origin/main in this worktree: /feature-done F1+F2 filing, spec 7.2 as shipped, RENDERER/DATA pointers, PR opened non-draft; controller reads reply, merges on green CI (user's word covers "do the docs pr").
- 21:30 R2 SYNC DONE (exit 0): rclone attempt 1 hit a DNS outage (2868 errors), attempt 2 clean — 122138 tile checks, 0 B transferred (tiles already current), 1 file uploaded via wrangler (manifest), 198 unchanged, CDN purge 1 URL; 3 raw-data files skipped (not present locally, expected). Docs agent #1 STALLED (600 s watchdog) with nothing committed; #2 dispatched on the same clean branch terrain-feature-done-docs with commit-per-milestone.
- 21:45 USER: PROD REGRESSION on mobile after #719 — "triangles sticking out of earth surface" (desktop fine). Asked user for device/browser + orbit-vs-near + screenshot. Sonnet audit agent (read-only) ranking mobile-only causes (tier base level without height tiles, atlas size vs limits, unwritten slot reads, skirt/edge NaN, index format) + smallest hotfix. Priority over the docs PR (agent #2 still running on terrain-feature-done-docs).
