# SDD ledger — plan: docs/superpowers/plans/2026-09-10-mesh-bodies-02-feature.md

Execution branch: worktree-whale-petunias-mesh-bodies (wt .claude/worktrees/whale-petunias-mesh-bodies), rebased on main d46fd6911 (prep #677 merged). BASE for wave 1: 0b08382f9.
Parallelism 3 (user ruling): per-task worktrees .claude/worktrees/mesh-feat-tN (branch mesh-feat-tN) off the execution branch; commits cherry-picked onto it in task order. Draft PR: see gh pr list.
Spec: docs/superpowers/specs/2026-09-10-mesh-bodies-design.md (this branch). Assets: data/raw/meshes/{whale,petunias}/*.glb present (gitignored).
Task list (harness has no todo tool this session; this ledger is the list): T1..T18 per plan dependency table.

## Pre-flight scan

Read-only scan run 2026-09-10 against the worktree tree (branch `worktree-whale-petunias-mesh-bodies`, HEAD `0b08382f9`, merge-base `d46fd6911`). Authority order: spec > plan. No files changed other than this append.

### 0. Base-commit and rename-notice check

| Item | Plan says | Found | Verdict |
| --- | --- | --- | --- |
| Rename notice (`ContentLayer`→`ContentPass`, #674) | verified at `89d716438`; re-run `git log origin/main -- src/services/engine/frame/` before executing | `79f1f8063` is in; `*Pass.ts`, `CONTENT_PASSES`, `pickEnabled`/`drawPick` all present; `ContentPass.d.ts:85-120` and `:103-120` cite correctly (the doc names `planetsPass`/`bodyGlintsPass`/`starPointsPass` as the three legitimate overriders) | consistent — no second rename in `frame/` |
| `origin/main` position | "main is now `d46fd6911`" | `origin/main` = **`69eede86f`** (#675 boot de-coupling), one commit ahead of the worktree's base | **mismatch (low)** — see F1 |
| Prep P1 (`rotationRowById` gate) | `orientationForBody` gates on rotation-row presence | shipped in `d46fd6911`; `tests/data/bodies/rotationElements.test.ts` carries **no** `toHaveLength` | consistent — Task 6 step 6 is a verified no-op |
| Prep P2 (`BODY_PICK_ROWS`) | `Readonly<Record<BodyId, readonly { readonly id: string }[]>>`, 5 rows, value IS the seed array | exact match, `src/data/bodies/bodyPickRows.ts:15-21` | consistent |
| Prep P2 (`sceneBodyPickId` scan) | scans `type:'body'` SOURCE_ENTRIES, skips `sun`, `starPickId` tail fallback, no pack-side edit for Task 7 | exact: `PACKABLE_BODY_ENTRIES = SOURCE_ENTRIES.filter(e => e.type==='body' && e.id!=='sun')`, then `starPickId(id)` | consistent — Task 7's "ONE row, no pack-side edit" holds |
| Prep P2 (`resolvePickTable` body arm) | `BODY_PICK_ROWS[entry.id as BodyId][pick.localIdx]` | exact | consistent |
| Only `Record<BodyId, …>` total literal | `BODY_PICK_ROWS` is the only one; every other is a `Object.fromEntries(...) as Record<BodyId,…>` cast | confirmed (`initialState.ts:245`, `makeSettingsFixture.ts:179`, `makeBodyItems.ts` derives from `BODY_IDS`) | consistent — Task 7's "the totality check IS the compiler" holds, and no fixture breaks |

### 1. Task-pair scan (tasks sharing a file or an interface)

| Pair | Producer states | Consumer states | Found |
| --- | --- | --- | --- |
| 1 → 13 | `rotateByTranspose(orientation: Readonly<Mat3>, v: Readonly<Vec3>): Vec3` in `src/utils/math/` | `bodyStateInHostFrame` uses it for `posM` and (optionally) `rotM` | consistent. Source at `bodyRelativePose.ts:21-27`, 4 call sites at `:47,:49-53,:54,:55-59` — all verified |
| 2 → 13 | `bodySlabRow` input gains `attachedBodies?: readonly {posM, radiusM}[]`; `deriveSlabs`/its call site UNCHANGED in T2 | T13 adds one `deriveSlabs` input field and threads `attachedBodiesByHostId.get(body.id)` into `:453` | consistent. Both touch `slabs.ts` but T13 depends on T2, so no parallel conflict |
| 2 ↔ 2 (internal) | near = `Math.max(viewZ-marginM, (dM-radiusM)*NEAR_RATIO, MIN_NEAR_M)` at `:284`; `eyeRelBodyM` destructured at `:252` | — | consistent, both citations exact |
| 3 → 4 | `partitionBodiesByPresentation` returns `glints: (PlanetBody\|MeshBody)[]` + `meshes` | `sceneBodyPartition` widens `bodies` to `[...planets, ...meshBodies]` and its return with `meshes` | consistent, and **T3 compiles standalone** — `MeshBody` is a structural supertype-compatible shape of `PlanetBody` (`{id,label,radiusM,albedo}`), so the widened `glints` is still assignable to the un-widened `readonly PlanetBody[]` annotation. See F7 for the flip side |
| 3 → 17 | `meshes: readonly MeshBody[]` branch | `meshBodiesPass.draw` intersects `meshBodiesAttachedTo(bodyId)` with `.meshes` | consistent |
| 3 → glint consumers (unlisted) | a sub-pixel `MeshBody` routes to `glints`; spec: "no change to that layer needed" | `bodyGlintsPass.draw` reads only `body.albedo`/`body.radiusM` (both on `MeshBody`) — OK; **but `drawPick` at `:430` does `seedIndexOfBody(body.id, SCENE_PLANETS)` and `continue`s on −1** | **gap (F2)** — a sub-pixel mesh body draws as a glint but is not clickable at glint scale |
| 4 → 5 | `MESH_ASSETS: Readonly<Record<string, MeshAssetRow>>` (empty) | `meshBody(seed)` looks up `MESH_ASSETS[seed.meshKey]`, throws on miss | consistent |
| 4 → 7 | `SCENE_MESH_BODIES: readonly MeshBody[] = []` | `BODY_PICK_ROWS['mesh-body'] = SCENE_MESH_BODIES` | consistent; row type is `readonly {readonly id: string}[]`, satisfied |
| 4 → 10 | `meshAssets.generated.ts` shape + header | tool writes real rows into it | consistent (`attribution` present on both sides) |
| 4 → 12 | `SCENE_MESH_BODIES`, `BodyStore.meshBodies` | one demand row + one slot per entry | consistent |
| 4 → 13 | `SCENE_MESH_BODIES` | `meshBodiesAttachedTo` filters it by `elementsById(id).focusId` | consistent; `elementsById` at `orbitalElements.ts:38` verified |
| 4 → 18 | `sceneMeshBodies.ts` exports bare `[]` | T18 rewrites to `SEED_MESH_BODIES.map(meshBody)` | consistent |
| 5 → 18 | `MeshBodySeed = {id,label,meshKey,description}` | `SEED_MESH_BODIES: readonly MeshBodySeed[]` | consistent |
| 6 → 13 | two `ORBITAL_ELEMENTS` rows, `focusId:'earth'` | `meshBodiesAttachedTo` test mocks `sceneMeshBodies` but reads the REAL element table | consistent, and the plan says so explicitly |
| 6 → 18 | `every SCENE_BODIES id resolves a BodyState` | earns its keep when the seed rows land | consistent |
| 6 → existing suite | appends 2 rows to `ORBITAL_ELEMENTS` before any `SCENE_BODIES` row exists | `tests/data/bodies/orbitalElements.test.ts` asserts unique ids, `focusId` resolves (via `ORBITAL_ELEMENTS ∪ SCENE_BODIES`), `e ∈ [0,1)`, `a > 0`; `deriveBodyStates.test.ts:19` is dynamic (`SCENE_ANCHORS.length + ORBITAL_ELEMENTS.length`) | consistent — no test breaks on the orphan rows |
| 7 → 17 | `Source.MeshBody = 32`, `MESH_BODY_ENTRY`, `BODY_PICK_ROWS` row | `packSelection(Source.MeshBody, seedIndexOfBody(meshId, SCENE_MESH_BODIES) + PICK_SENTINEL_OFFSET)` | consistent. `MESH_BODY_ENTRY` field-for-field matches `planet.ts` |
| 7 → caption pipeline (unlisted) | `bearsLabel: true`, `labelLayer:'body'`, `detailLabel`/`shortLabel`/`plural` | nothing emits a mesh-body caption: `sceneBodyLabels.ts:187-207` enumerates `SCENE_EARTH`/`SCENE_STARS`/`SCENE_PLANETS`/`SGR_A_STAR` only; `CaptionKind` is a closed union (`captionPriority.ts:38`); `s-star.ts:30-32` sets `bearsLabel:false` with the comment "those are present iff `bearsLabel`" | **conflict (F3)** — highest-value finding |
| 8 → — | `description?` on `BodyInfo`/`SelectionRow`, `'description' in body` idiom, new `BodyDetailCard` block | no consumer inside the plan | consistent; all five citations exact, and the new block at `:227/:228` sits inside the `{!isFamousStar && (<>` fragment opened at `:127`, so a mesh body reaches it |
| 9 → 10 | `MESH_MAGIC`/`MESH_VERSION`/`MESH_HEADER_BYTES`/`MESH_VERTEX_STRIDE_BYTES`, `DecodedMeshGeometry` | `writeMeshBinary` imports the constants; round-trips through `decodeMesh` | consistent. Header arithmetic checks out: 4+2+4+4+4 = 18, stride 3+3+4+2 f32 = 48; the "payload is not 4-byte aligned" landmine is real |
| 9 → 11 | `decodeMesh`, `MeshAsset`, `MeshReq` | `Fetcher<MeshAsset, MeshReq>` | consistent |
| 9 → 15 | `MeshAsset` (5 typed arrays + 3 `ImageBitmap`) | `setMesh(id, asset)` | consistent; but see F4 (texture formats unstated) |
| 11 → 12 | `meshFetcher` | slot `commit` → `state.gpu.meshBodyRenderer?.setMesh` | consistent |
| 12 ↔ 17 | T12 file list AND step: "`wireSlots.ts` — add `wireMeshBodySlots(state)` beside `wireBodyTextureSlots(state)` at line 117" | T17 file list AND step: "Modify `src/services/engine/phases/wireSlots.ts` — call `wireMeshBodySlots(state)` (Task 12)" | **duplicate ownership (F5)** — two tasks claim the same one-line edit |
| 12 → 15 | needs `state.gpu.meshBodyRenderer` declared | T15 declares the field, seeded `null` | consistent, and the plan states the reason |
| 12 → existing fixtures | 8 files carry `bodyTextures: new Map()` | grep confirms exactly those 8 files — but **11 occurrences** (`bodyGlintsPass.test.ts` ×3, `engineSliceDispatches.test.ts` ×2) | minor (F6) |
| 12 → `assetWiring.ts` | new `loadRadiusMpc` in `meshBodyLoadRadius.ts` | `assetWiring.ts:36` already does `import { loadRadiusMpc } from '../frame/bodyTextureLoadRadius'` | **name collision (F8)** — the second import needs an alias; no task says so |
| 13 → 17 | `bodyStateInHostFrame(body, host) → {posM, rotM}` in host fixed axes | model = `translate(posM − hostPose.eyeRelBodyM) · rotate(rotM)`; pick = `bodySlabFlooredPick(view.slab.vp, hostPose.eyeRelBodyM − posM, radiusM, ctx.drawPxPerRad)` | consistent, and both signs check out against `bodyRelativePose`'s `eyeRelBodyM = R^T(camPos − bodyPos)` convention. `bodySlabFlooredPick.ts:25-35` returns `{mvp, camPosLocal}` as cited |
| 14 → 17 | `sunVisibleFraction({bodyPosMpc, sunPosMpc, hostPosMpc, sunRadiusM, hostRadiusM})` | fed Sun/host/mesh positions and radii | consistent |
| 15 → 16 | 176-byte `MeshBodyUniforms`, 44 f32 | `io.wesl` struct field order | consistent, and the layout is WGSL-legal: mvp@0, vec3@64, f32@76, mat3x3@80 (48 B), vec3@128, f32@140, vec3@144, f32@156, vec3@160, f32@172 → 176 (16-aligned). Test indices 16/19/35 are correct |
| 15 → 16/17 | `camPosLocal` packed as `hostPose.eyeRelBodyM − posM` (HOST axes) | `vertex.wesl` emits `localPos` = **un-transformed authored position**; `fragment.wesl` does `normalize(u.camPosLocal - localPos)` | **frame mismatch (F9)** — mixes host axes with mesh-authored axes |
| 15 ↔ `packLitBodyUniforms` | "MATCHES that packer's first 80 bytes byte-for-byte" | that packer zeroes bytes 76..79; the mesh packer writes `sunVisibleFraction` there — the same sentence then says so | minor self-contradiction (F10); intent is "same field order for the first 76 bytes" |
| 16 → 17 | 3 `.wesl` files | pipeline created in T17 | consistent, but T16 has no verification that can fail (F11) |
| 17 → `passes.test.ts` | plan lists three sites: `:348-357` (edit), `:529-537` (leave alone), `:333-346` (out of scope) | all three verified exactly as described — the `foreground:0` pin really is a last-element check and survives. **But `:970-985` asserts the exact 14-name `drawPick` roster** ("this test is the ONLY place the fourteen names are asserted") and `meshBodiesPass` carries `drawPick` | **missed site (F12)** |
| 17 → 18 | orbit-trail rings arrive as a side effect of T6 | user ruling keep/suppress | consistent; `orbitTrailsPass.ts:141` (`limit = ORBITAL_ELEMENTS.length`) and the unconditional loop at `:157` verified |

### 2. Per-task self-consistency (1–18)

| # | Files list vs steps | Tests vs code | testing.md / rubric | Verdict |
| --- | --- | --- | --- | --- |
| 1 | consistent | 2 new tests specified; **spec says P5 needs none** ("a pure extraction, so `bodyRelativePose`'s existing tests staying green is its test") — the plan quotes that line and then adds tests anyway | the "returns v unchanged under the identity" case is a near-vacuous property | **F13** |
| 1 | step "trim the module header's rotate-by-transpose parenthetical" | the header (`:1-9`) carries no such parenthetical — the doc that moves is the comment at `:18-20` | — | cosmetic no-op step |
| 2 | consistent | 2 failing tests, then implement, then no-op proof | sound (hand-built fixture, non-mirror) | clean |
| 3 | consistent; correctly defers `sceneBodyPartition.ts` to T4 | 3 tests, all in the spec's own list | "four branches disjoint and covering" is load-bearing precisely because the union is not discriminated (F7) | clean |
| 4 | "grep for `setPlanets(`" resolves to `createEngineData.ts:35` (call) + `createBodyStore.ts:39` (impl) — neither named | explicitly no new test, justified | correct call: nothing observable changes | clean (name the two files in the brief) |
| 5 | consistent | 2 tests, `vi.mock` of the generated table | crossed-field-mapping bug is real and compiler-invisible | clean |
| 6 | consistent | 4 tests; 2 are the spec's own, 2 are additions (`periodDaysFromSemiMajorKm`) | additions are hand-computed pure-function tests (keep-rule) | clean; the `apsidalPrecessionYears: 0` safety claim is verified (`moonRatesFromPeriods.ts:53,64-67` floors to 0, no divide) |
| 7 | 5 files listed, 6 steps, all consistent | explicitly no new test, with the reasoning | correct: `Record<BodyId,…>` totality is the compiler's job; the "delete any body-items key-set test" step finds nothing (none exists) | clean except F3 |
| 8 | consistent | 3 tests | sound | the `vi.mock('data/bodies/sceneBodies')` fixture must also satisfy `deriveBodyStates(simDays).get(id)!` at `extractSelectionRow.ts:56` — mock `orbitalElements` too, or reuse a real id (F14, practicality) |
| 9 | consistent | 3 tests, hand-built `DataView` fixtures | on-disk-format keep-rule; explicitly not a mirror | clean |
| 10 | 12 files + package.json; 7 tests | 7 tests vs spec's 2 (+the round-trip) | additions are behavioral, not restatements — but the plan's Global Constraint claims the test set "mirrors the spec's Testing section exactly", which is false here (F13) | clean on substance |
| 11 | 1 file, no test, precedent cited (`filamentFetcher`/`bodyTextureFetcher` carry none) | — | correct | clean |
| 12 | file list correct (8 files), but 11 edit sites | no new test, precedent cited | correct | F5, F6, F8 |
| 13 | 6 files, 6 commits-worth | 2 tests + a no-new-test wiring step | hand-computed rotation expectation, explicitly not via the function's own helper | clean |
| 14 | 2 files | the spec's 3 cases verbatim | clean | clean |
| 15 | 5 deliverables | 1 uniform byte-layout test | explicitly the WGSL/TS keep-rule, `packTexturedBodyUniforms` precedent verified to exist | clean; F4, F9, F10 |
| 16 | 3 `.wesl` files | no test (spec: none) | — | F11: `npm run build` cannot fail here — nothing imports these files until T17, so the WESL linker never sees them. The step verifies nothing |
| 17 | 5 modify targets + 1 create | no new pass test; `passes.test.ts` update called a "structural-registry fix" | correct framing | F5, F9, F12 |
| 18 | large but internally coherent; the 4 credit surfaces, the pre-bake, the two rulings all line up with the DoD | full-suite + perf + visual | the "keep or suppress" and "land or park" gates are correctly the user's | clean |

### 3. Dependency table vs task bodies

| Row | Table says | Body says | Verdict |
| --- | --- | --- | --- |
| 4 | depends 3 | reads `MeshBody`, edits `sceneBodyPartition` (T3's deferred half) | real |
| 5 | depends 4 | `MESH_ASSETS`, `MeshAssetRow`, `MeshBody` | real |
| 6 | prep only | appends element rows; test iterates `SCENE_BODIES` | real (trivially green pre-T18, as stated) |
| 7 | depends 4 | `SCENE_MESH_BODIES` for the pick row | real |
| 8 | none | confirmed: the fixture is `MeshBody`-shaped, not `MeshBody`-typed | real |
| 9 | none | "no dependency on Task 4: `MeshAsset` does not reference `MeshAssetRow`" | verified |
| 10 | depends 4, 9 | writes `meshAssets.generated.ts`, imports format constants | real |
| 12 | depends **5**, 11, 15 | body reads `SCENE_MESH_BODIES` (T4) and `meshFetcher` (T11) and `state.gpu.meshBodyRenderer` (T15); the **maker** (T5) is never referenced | **listed dep 5 is not real** — should be 4 (T5 pulls it in transitively, so harmless, but it over-serialises) |
| 13 | depends 1, 2, 4, 6 | all four used; T3 not listed but arrives via 4 | real |
| 15 | depends 4, 9 | `MeshAsset` (9) — 4 only for `MeshBody`/nothing directly; harmless | real enough |
| 16 | depends 15 | the `.wesl` struct must match the packer; direction could be reversed | conservative, fine |
| 17 | depends 7, 12, 13, 14, 15, 16 | all six used | real |
| 18 | depends 5, 6, 10, 17 | all four used | real |

Missing edges: none found (every symbol a task reads is produced by a listed or transitively-listed dependency).

### 4. Wave 1 disjointness (tasks 1, 2, 3)

| Task | Creates | Modifies | Overlap |
| --- | --- | --- | --- |
| 1 | `src/utils/math/rotateByTranspose.ts`, its test | `src/services/engine/camera/bodyRelativePose.ts` | — |
| 2 | — | `src/services/engine/frame/slabs.ts`, `tests/.../slabs.test.ts` | — |
| 3 | `src/@types/scene/MeshBody.d.ts` | `src/services/engine/frame/partitionBodiesByPresentation.ts`, its test | — |

**Disjoint, and no task reads another's output.** T3 must import `Vec3` into `MeshBody.d.ts` (the plan's contract block omits the import line — cosmetic). T2 explicitly imports nothing new. Safe to run in three worktrees.

### 5. Citation spot-checks against the live tree (14 of the plan's cites, tasks 1–17)

| Citation | Verdict |
| --- | --- |
| `bodyRelativePose.ts:18-27` (`rotateByTranspose` decl), call sites `:47,:49-53,:54,:55-59` | exact |
| `slabs.ts:234` `bodySlabRow`, `:284` the `near` `Math.max`, `:252` `eyeRelBodyM`, `:175` `SLAB_REVERSED_Z`, `:368` `deriveSlabs`, `:453` the call site | all exact |
| `partitionBodiesByPresentation.ts:48` `BODY_GLINT_MAX_PX = 3`, function `:72-114`, file 114 lines | exact |
| `sceneBodyPartition.ts:44` `bodies: state.data.bodies.planets`, file 57 lines | exact |
| `satellite.ts:38-53` signature, `:57` `semiMajorKm * KM_TO_MPC`; `sceneEarth.ts:21` `radiusM: 6371000`; `moonRatesFromPeriods.ts:53,64-67` | all exact |
| `source.ts:250` `McpmWorkbench: 31`; `bodySearchNames.ts:20-25` `AUTHORED`; `BodyId.d.ts:11`; `bodyIds.ts:14`; `initialState.ts:239-246` | all exact |
| `BodyInfo.d.ts:31`; `SelectionRow.d.ts:28-41`; `extractSelectionRow.ts:51-67` and `:56` (the `!`); `buildFocusable.ts:39-49`; `BodyDetailCard.tsx:223-227` + `</>` at `:228` | all exact |
| `EngineAssetSlots.d.ts:85`; `assetWiring.ts:144-162` `bodyTextureRow`; `bodyTextureLoadRadius.ts:60` `LOAD_RADIUS_BODY_RADII = 1e4` | all exact |
| `wireSlots.ts:117`, `engine.ts:567` | exact **in this worktree**; on `origin/main` (`69eede86f`) they are `:107` and `:572` — see F1 |
| `frameContext.ts:192` `bodyStates`, `:204-207` `slabBodyCandidates`, `:236-240` `bodyPose`, `:265-273` `deriveSlabs` | all exact |
| `pbr.wesl:282-288` — `fn pbrDirect(n, v, l, albedo, roughness, f0: f32)` | exact; `f0` really is a scalar, so the plan's "do not invent a metalness mix inside `pbrDirect`" instruction is well-founded |
| `passes/index.ts:398` `texturedBodiesPass`, `:404` `ringsPass`, `:426` `atmosphereShellPass` | exact |
| `passes.test.ts:348-357` (fixed `[earthPass, planetsPass, texturedBodiesPass]` array at `:352`), `:529-537` (`idxAtmosphere > idxRings` at `:533`, last-element check at `:537`), `:333-346` (`FOREGROUND_NAMES` filter) | all exact, including the claim that the ordering pin survives |
| `composeBodyMvp.ts:113-120` (the padded-mat3 landmine), `frameProgram.ts:89` `BODY_SLAB_CAPACITY`, `planetsPass.ts:111` `sunDirLocal(...)`, `bodySlabFlooredPick.ts:25-35`, `bodyPickRenderer.ts:384` `drawSphere`, `atmosphereParams.ts:59` `groundAlbedo: [0.3,0.3,0.3]`, `EarthBody.d.ts:17-21` (no `albedo`), `orbitTrailsPass.ts:141/157` | all exact |

**Zero stale citations found relative to the worktree base.** The only drift is F1 (relative to today's `origin/main`).

### 6. Contradictions with the spec or the plan's own Global Constraints

| # | Finding |
| --- | --- |
| F13 | Global Constraints: "tests only where listed per-task below — **this mirrors the spec's own Testing section exactly**." It does not. The plan adds: 2 `rotateByTranspose` tests (spec explicitly says P5 needs none), 2 `meshBody`-maker tests, 1 `periodDaysFromSemiMajorKm` test, 5 extra tool tests (spec lists 2 + the round-trip), 1 `packMeshBodyUniforms` test (spec says "no renderer or shader unit tests"). Every addition is individually defensible under testing.md; the *claim* is what is false, and it is the sentence a reviewer will lean on. |
| F3 | Spec §Settings ends with "**This needs a ruling**" on the shared caption toggle. Plan Task 7 opens with "**RULED — not an open question**" and asserts `bearsLabel: true`. Spec is authority; no user ruling is recorded in this ledger. |
| F15 | Spec §"Model matrix" places the pass "after `texturedBodiesLayer` and before `atmosphereShellLayer`"; plan narrows to "before `ringsPass`". Compatible and better justified (opaque before translucent) — noting only that the plan tightened a spec statement. |
| F16 | Spec §Selection puts the new `BodyDetailCard` block "beside the existing `orbit` block (`:201-217`)"; plan puts it after the `facts?.description` block (`:223-227`). Both inside the same `{!isFamousStar}` fragment; cosmetic. |
| F17 | Spec §Pick names `PICK_SEEDS_BY_BODY_ID` / `resolvePickTable.ts:36-49`, and §Model matrix names `meshBodiesLayer.ts`/`texturedBodiesLayer`/`atmosphereShellLayer`. Both are pre-prep / pre-rename spellings; the plan translates them correctly. The spec is the stale document here, not the plan. |

### 7. Findings and proposed rulings

Legend: **B** = must be ruled before wave 1 (tasks 1/2/3) starts; **L** = affects later tasks only.

| # | Finding | Proposed ruling (spec is the authority) | Cost if the ruling is wrong |
| --- | --- | --- | --- |
| **F1** | `origin/main` is `69eede86f` (#675 boot de-coupling), not `d46fd6911`. The worktree is based on `d46fd6911`. #675 touched `wireSlots.ts` (cite `:117` → `:107`), `engine.ts` (`:567` → `:572`), and 3 of Task 12's 8 fixture files. The plan's re-verify instruction greps only `src/services/engine/frame/`, which would **not** have caught #675. | **L.** Do not rebase now — wave 1 is unaffected and rebasing invalidates the three task briefs. Before Task 12/17 dispatch, rebase the execution branch onto `origin/main` and re-derive the two line numbers + re-run `git grep -l 'bodyTextures: new Map()'`. Also widen the plan's re-verify instruction from `frame/` to `src/services/engine/`. | Task 12 or 17 edits the wrong line or misses a fixture; caught by `tsc` within minutes. Low. |
| **F3** | `MESH_BODY_ENTRY` sets `bearsLabel: true` + `labelLayer`/`detailLabel`/`shortLabel`/`plural`, but **no task emits a mesh-body caption**. `sceneBodyLabels.ts:187-207` enumerates Earth/stars/planets/Sgr A* by hand; `CaptionKind` (`captionPriority.ts:38`) is a closed 6-member union with matching `CAPTION_FADE_RULES` and `CAPTION_PRIORITY` rows and a `FOREGROUND_LABEL_CAPACITY` (`:69`). `s-star.ts:30-32` sets `bearsLabel: false` for exactly this reason and states the invariant: those four fields "are present iff `bearsLabel`". So Task 7 as written ships a Labels & Guides checkbox that controls nothing — the same dead-knob shape the spec's own §Settings warns about for `.enabled`. It also makes prep P2's mesh-body pack row dead (`bodyLabel` at `sceneBodyLabels.ts:144` is the only `sceneBodyPickId` caller). | **L, but ask the user before Task 7.** Two options, user's call: **(A)** `bearsLabel: false`, drop the four label fields — smallest diff, matches the `s-star` precedent, no dead checkbox, no caption on approach (the spec's DoD/observable list does not require one). **(B)** Add mesh bodies to `sceneBodyLabels.ts` with a new `CaptionKind`, a `CAPTION_FADE_RULES` row, a `CAPTION_PRIORITY` row and `+2` capacity — makes the toggle and P2 real, but is roughly half a task of unplanned scope on a user-visible surface. Default recommendation if the user does not want to decide: **(A)**, and backlog (B). | (A) wrong → no name floats beside the whale on approach; surfaces at the T18 visual pass, ~1h to add. (B) wrong → a new UI row and a new caption kind nobody asked for, plus the capacity constant. Medium either way; cheap to defer, expensive to discover at T18. |
| **F9** | Frame mismatch between Tasks 15/16/17. Task 16's vertex carries `localPos` as the **un-transformed authored position** (mesh-local axes) while Task 17 packs `camPosLocal = hostPose.eyeRelBodyM − posM` (**host** axes), and the fragment computes `normalize(u.camPosLocal - localPos)`. Everything else in the fragment (`worldNormal`/`worldTangent` via `u.model = rotM`, `sunDirLocal` fed the **host's** orientation, `dirToHost = normalize(-posM)`) is host-axes, so `localPos` is the one operand in the wrong frame. The error is proportional to the tumble rotation and to `|localPos|` (metres) — i.e. it is worst exactly at the close approach the feature exists for. `planetsPass`'s `bodySlabCamLocal` precedent does not carry, because a planet has no rotation between its slab frame and its own authored frame. | **L.** Fix in Task 16: emit `localPos = u.model * position` (host-axes offset from the body's centre) rather than the raw authored position. That keeps every fragment operand in host axes, keeps `dirToHost` a uniform, and needs no change to Task 15's packing or Task 17's `camPosLocal`. Record it in the Task 16 brief as a contract line, not a comment. | Wrong-looking specular/view-dependent shading on a tumbling body, with no test and no compiler check — the classic "looks slightly off, nobody knows why" bug. Would likely survive the T18 visual pass. High. |
| **F12** | Task 17 misses `passes.test.ts:970-985`: `expect(CONTENT_PASSES.filter(l => l.drawPick).map(l => l.name)).toEqual([...14 names...])`. `meshBodiesPass` carries `drawPick`, so this fails. The plan enumerated three `passes.test.ts` sites and claimed origin/main verification. | **L.** Add `'mesh-bodies'` to that list between `'planets'` and `'foreground-labels'` (registry order). Do **not** delete the test: its own comment records that it is the only place the pick roster's ordering is asserted, and the ordering carries a real `@group(0)` constraint for the two label rows. Add the site to the Task 17 brief. | `npm test -- passes` fails at the end of Task 17; 10 minutes. Low — but it is evidence the "all citations verified" claim was not exhaustive for test files. |
| **F5** | Tasks 12 and 17 both list, and both step, the same one-line `wireSlots.ts` edit (`wireMeshBodySlots(state)`). | **L.** Give it to Task 12 (which owns the function and its dependencies); strike the file and the step from Task 17. Task 12 already depends on 15, so ordering is fine. | A merge conflict between two per-task worktrees, or a double-call that wires slots twice. Low but noisy. |
| **F2** | A sub-pixel mesh body draws as a glint but is **not pickable** there: `bodyGlintsPass.drawPick` (`:430`) does `seedIndexOfBody(body.id, SCENE_PLANETS)` and skips on −1. Spec says "below 3 px it is a tinted glint, exactly like a planet", and a planet at glint scale *is* pickable via that path. Neither spec nor plan mentions it. | **L.** Accept and record — the spec's named observable behaviours are fly-to-from-search, sunrise, glint handoff, offset, InfoCard; none needs a glint-scale click, and the failure is graceful (no mis-pack, no crash). Note it in the PR body. If the user wants it: it is a `Source.MeshBody`/`SCENE_MESH_BODIES` arm in that same loop, ~10 lines, and it should ride Task 17, not a follow-up. | Clicking a distant whale does nothing; discovered at the T18 visual pass. Low. |
| **F13** | The Global Constraints' claim that the per-task test set "mirrors the spec's own Testing section exactly" is false (see §6). | **B (cheap).** Fix the sentence rather than the tests: replace with "the spec's Testing section is the floor; the additions below are per-task and justified in place." Then apply one substantive cut per the spec's own P5 ruling: in Task 1, drop `rotateByTranspose returns v unchanged under the identity` (a vacuous property) and keep the single hand-computed non-identity case. | If the sentence stands, a downstream reviewer will read every listed test as spec-mandated and refuse to cut any. Low, but it is the exact bias `leanness.md` exists to counter. |
| **F7** | `MeshBody` is structurally assignable to `PlanetBody` (`{id, label, radiusM, albedo}` ⊂ `MeshBody`). The `PlanetBody \| MeshBody` union is therefore **not** discriminated, and `tsc` will not catch a mesh body leaking into a planet-only path (`flat`, `textured`, `planetsPass`). | **B (record only, no change).** Keep the design as spec'd — adding a tag field to buy the compiler check is exactly the "extra knob" `simplicity.md` forbids, and the routing (`'meshKey' in body`, type-before-residency) is correct. But this makes Task 3's `keeps the four branches disjoint and covering` test the **only** guard, so it must not be trimmed by a later leanness pass. Note it in Task 3's brief. | A future refactor drops that test as a "registry-ish" case and a mesh body silently renders as a flat sphere. Medium, deferred. |
| **F4** | Neither Task 15 nor Task 16 states the GPU texture formats. `_albedo` must be created `rgba8unorm-srgb` and `_mr`/`_normal` `rgba8unorm`; the plan's landmine 2 covers only the fetcher half (`colorSpaceConversion: 'none'`). This is the standing "normal maps decode LINEAR, never sRGB" trap in its GPU-side form. | **L.** Add one contract line to Task 15: albedo `rgba8unorm-srgb`, mr and normal `rgba8unorm`. No code beyond the format argument. | Washed-out or over-dark normal/roughness response with no error anywhere. Medium — and historically this exact trap has cost a debug loop here before. |
| **F8** | `assetWiring.ts:36` already imports `loadRadiusMpc` from `bodyTextureLoadRadius`; Task 12 adds a second `loadRadiusMpc` (from `meshBodyLoadRadius`) into the same file. | **L.** Import the mesh one aliased (`loadRadiusMpc as meshLoadRadiusMpc`) rather than renaming the new export — the file-name-equals-symbol-name convention for `frame/` helpers is already set by `bodyTextureLoadRadius`. One line in the Task 12 brief. | `tsc` duplicate-identifier error on first compile; 2 minutes. Negligible, but it will otherwise burn a fix loop mid-task. |
| **F6** | Task 12's fixture list names 8 files but there are **11** `bodyTextures: new Map()` sites (`bodyGlintsPass.test.ts` ×3, `engineSliceDispatches.test.ts` ×2). | **L.** Change the step wording from "every fixture file listed" to "every occurrence — re-run `git grep -n 'bodyTextures: new Map()'`". | Two fixtures under-satisfy the widened type; `tsc` catches it. Negligible. |
| **F11** | Task 16's only verification is `npm run build` "confirms the WESL relinks" — but nothing imports the three `.wesl` files until Task 17 creates the pipeline, so the linker never sees them. The step cannot fail. | **L.** Either accept it as a write-only task and say so plainly in the brief (first real check is Task 17's `createRenderPipeline`), or fold Task 16 into Task 17. Recommend the former: keeping the shader diff its own commit is worth more than a fake gate. | An agent reports "verified" on a step that verified nothing; the shader error surfaces one task later. Low, but it is the rubric's "step that asserts nothing". |
| **F14** | Task 8's test recipe (`vi.mock` `data/bodies/sceneBodies` with a synthetic `MeshBody`-shaped row) must also satisfy `extractSelectionRow.ts:56`'s `deriveBodyStates(simDays).get(body.id)!` — the fixture body has no `BodyState`, so the `!` throws unless `orbitalElements` is mocked too (the plan says "plus its `ORBITAL_ELEMENTS` counterpart", which implies it but does not say it). | **L.** Spell it out in the brief: mock both modules, or add `description` to a real body id in the fixture. | One failed-test debug loop. Negligible. |
| **F10** | Task 15's docblock says the layout "MATCHES `packLitBodyUniforms`'s first 80 bytes byte-for-byte" and, one clause earlier, that it writes `sunVisibleFraction` into a pad slot that packer never writes. Both cannot be true. Verified: `packLitBodyUniforms` is 20 f32, zeroing bytes 76..79. | **L.** Reword to "the first 76 bytes match field-for-field; byte 76 is that struct's trailing pad, reused here for `sunVisibleFraction`". Comment-only. | A future reader trusts a false byte-identity claim. Negligible. |
| **F18** | Dependency table lists Task 12 as depending on **5**; the body never references the maker (it reads `SCENE_MESH_BODIES`, from Task 4). | **L.** Change the edge to 4. Harmless as-is (5 depends on 4), but it needlessly serialises 12 behind 5. | Slightly less parallelism. Negligible. |
| **F19** | Task 4 says "grep for `setPlanets(`" without naming the sites. They are `src/services/engine/data/createEngineData.ts:35` (the boot call) and `src/services/engine/data/createBodyStore.ts:39` (the store implementation). | **L.** Put both paths in the Task 4 brief. | An agent widens the type but misses the store implementation; `tsc` catches it. Negligible. |

**Nothing blocks tasks 1, 2 or 3.** The only pre-wave item is F13's one-sentence correction (and, if the user wants it applied, dropping Task 1's identity case) — neither changes what the three agents do to the code.

## Controller rulings on the scan (2026-09-10)
Ruling F13: Global Constraints "mirrors the spec's Testing section exactly" sentence is false → fix wording in a plan-edit commit; T1 drops the identity case (asserts nothing testing.md values) — cost if wrong: one trivial test missing.
Ruling F3: user's earlier ruling was "one shared Labels & Guides row, bearsLabel: true" — intent = mesh bodies DO get captions. Rule (B): add a mesh-body caption kind so the toggle is live; lands as Task 7's second half (or 7b) with the smallest CaptionKind extension; user may override to (A) before T7 dispatch — cost if wrong: ~half a task of label code to delete.
Ruling F9: vertex shader emits localPos = u.model * position (host axes) so it matches camPosLocal — carried into T15/T16/T17 briefs; cost if wrong: none (it is the correct frame).
Ruling F12: T17 also updates the 14-name drawPick roster in passes.test.ts:970-985 — carried into T17 dispatch.
Ruling F1: rebase execution branch onto origin/main (69eede86f, #675) after wave 1 lands, before T4 dispatch; re-derive T12/T17 line cites then. Wave-1 files untouched by #675.
Ruling F5: T12 and T17 are sequential (17 depends on 12) → no worktree conflict; T17 brief notes wireSlots already edited.
F2/F4/F7/F8/F11/F14/F18/F19: carried as dispatch notes for their tasks (glint-scale pick, texture formats sRGB/linear, MeshBody discriminant 'meshKey' in body — T3 already uses it, loadRadiusMpc import collision T12, T16 needs a failing check (probe/tsc on WESL link), T8 mock adds orbitalElements, dep 12→5 dropped, T4 names boot sites).
Wave 1 dispatched: T1/T2/T3 (sonnet) in mesh-feat-t1/t2/t3, BASE 0b08382f9.
Task 1: implementer DONE (t1 bf3ba9cbc → cherry-picked 5848938d4); review dispatched (sonnet), package review-0b08382f9..5848938d4.diff
Task 3: implementer DONE (t3 9b7c6c5a3 → cherry-picked 0d74670c3); review dispatched (sonnet), package review-5848938d4..0d74670c3.diff
Task 2: implementer DONE (t2 416079c29 → cherry-picked c0b6770d5); review dispatched (sonnet), package review-0d74670c3..c0b6770d5.diff
Task 1: minor (deferred): rotateByTranspose.ts 6 comment / 9 code (precedented in utils/math) — final review triages.
Task 1: complete (commits 0b08382f9..5848938d4, review clean)
Task 2: minor (deferred): bodySlabRow gained `export` for direct unit testing (not in brief's snippet).
Task 2: complete (commits 0d74670c3..c0b6770d5, review clean)
Task 3: minor (deferred): partitionBodiesByPresentation.ts header 33 lines / ratio ~1.6 — PRE-EXISTING debt, not introduced; comment-audit at the end.
Task 3: complete (commits 5848938d4..0d74670c3, review clean)
Wave 1 landed; rebased onto origin/main 69eede86f (#675) per ruling F1 → HEAD d9841f170. t1-t3 worktrees removed. Task 4: dispatched (sonnet) in mesh-feat-t4, BASE d9841f170.
Task 9 + Task 14: dispatched (sonnet) in mesh-feat-t9/t14, BASE d9841f170 (parallel with T4; no shared files).
Task 9: implementer DONE (t9 aa226b20f → cherry-picked 8d5cb40d7); review dispatched (sonnet), package review-d9841f170..8d5cb40d7.diff. Slot freed → Task 6 dispatched.
Task 14: implementer DONE (t14 fd2d6db1c → cherry-picked b9696e608; note: acos clamp added beyond brief); review dispatched (sonnet), package review-8d5cb40d7..b9696e608.diff. Slot freed → Task 8 dispatched.
Task 4: implementer DONE (t4 8ca13d09d → cherry-picked a46d9f133; boot site = createEngineData.ts post-#675; 3 pass-test fixtures gained meshBodies: []); review dispatched (sonnet), package review-b9696e608..a46d9f133.diff.
Task 9: minor (deferred): meshBinaryFormat.ts header 16 lines under the wire-format exception (precedent filamentBinaryFormat.ts); tiny type files comment-heavy per FilamentReq precedent.
Task 9: complete (commits d9841f170..8d5cb40d7, review clean)
Task 5: dispatched (sonnet) in mesh-feat-t5, BASE a46d9f133. Task 7 queued (needs slot; carries ruling F3 caption kind, opus).
Task 14: review Needs fixes — Important: unclamped ramp negative in annular regime (sun disc > host disc). Ruling: clamp return to [0,1], one comment, one test; no annular branch (unreachable at 400 km Earth orbit) — cost if wrong: occlusion overstated only in a regime no in-scene body reaches. Minor accepted: acos clamp is a real NaN guard (dot rounds to 1+ε), keep with a one-line comment.
Task 14: fix round 1/5 dispatched (resume implementer), FIX_BASE b9696e608.
Task 4: complete (commits b9696e608..a46d9f133, review clean)
Task 5: implementer DONE (t5 563ce2063 → cherry-picked a72d53225); review dispatched (sonnet), package review-a46d9f133..a72d53225.diff. Slot freed → Task 7 dispatched (opus, carries ruling F3).
Task 14: fix round 1/5 landed (t14 538742850 → cherry-picked 53a2f4b97); scoped re-review dispatched (sonnet), package review-a72d53225..53a2f4b97.diff.
Task 5: minor (deferred): meshBody.ts error message lacks a build-meshes hint; comment budget at exact ceiling.
Task 5: complete (commits a46d9f133..a72d53225, review clean)
Task 14: fix round 1/5 (2 addressed, 0 open; commits a72d53225..53a2f4b97)
Task 14: minor (deferred): sunVisibleFraction.ts comment ratio 18/35 (one line over half).
Task 14: complete (commits 8d5cb40d7..53a2f4b97, review clean after 1 fix round)
Task 10: dispatched (opus) in mesh-feat-t10, BASE 53a2f4b97.
Task 6: implementer DONE (t6 647189aab → cherry-picked 4121e8e11; bodyStatesJ2000.json fixture gained 2 keys); review dispatched (sonnet), package review-53a2f4b97..4121e8e11.diff.
Task 13: QUEUED (worktree mesh-feat-t13 ready at 4121e8e11; deps 1,2,4,6 landed); dispatch when a slot frees (T7/T8/T10 running = parallelism 3).
Task 8: implementer DONE (t8 d2544fd28 → cherry-picked 4108294b5; used a cast instead of the 'in' idiom because its worktree predated T4's union widening); review dispatched (sonnet), package review-4121e8e11..4108294b5.diff. Slot freed → Task 13 dispatched (sonnet).
Task 6: review Needs fixes — Important: orbitalElements.meshBodies.test.ts:228 radius check toBeCloseTo(x,15) tolerance 5e-16 > x≈2.2e-16 → vacuous. Minor: periodDays header 9/6 (derivation carve-out), report inaccuracy, offset test is plumbing (plan-mandated).
Task 6: fix round 1/5 dispatched (resume implementer in fresh wt mesh-feat-t6fix off 4108294b5), FIX_BASE 4121e8e11.
Task 8: review Needs fixes — Important: extractSelectionRow.ts:69 structural cast instead of the 'in' idiom (compiles here post-T4) + stale comment. Minor: report mis-cites vi.hoisted precedent.
Task 8: fix round 1/5 dispatched (resume implementer in fresh wt mesh-feat-t8fix off 4108294b5), FIX_BASE 4108294b5.
Task 6: fix round 1/5 landed (t6fix 5a617586f → cherry-picked 1669a088e); scoped re-review dispatched (sonnet), package review-4108294b5..1669a088e.diff.
Task 8: fix round 1/5 landed (t8fix 7441a37b3 → cherry-picked 9aaa90e13); scoped re-review dispatched, package review-1669a088e..9aaa90e13.diff.
Task 7: implementer DONE (t7 8ad6a8643 + d6a1cd8ff → cherry-picked ..e2ca30d0b; extra file galaxyType.ts exhaustive switch; kind→gate route = CAPTION_FADE_RULES row referencing MESH_BODY_ENTRY.id; concern for T17: subjectVisible UNGATED copies planets); review dispatched (sonnet), package review-9aaa90e13..e2ca30d0b.diff.
Task 11: dispatched (sonnet) in mesh-feat-t11, BASE e2ca30d0b (dep 9 landed; T10 parallel, disjoint files).
Task 6: fix round 1/5 (1 addressed, 0 open; commits 4108294b5..1669a088e)
Task 6: complete (commits 53a2f4b97..4121e8e11 + fix 1669a088e, review clean after 1 fix round)
Task 8: fix round 1/5 (2 addressed, 0 open; commits 1669a088e..9aaa90e13)
Task 8: complete (commits 4121e8e11..4108294b5 + fix 9aaa90e13, review clean after 1 fix round)
Task 7: minor (deferred): mesh-body.ts and bodySearchNames.ts comment ratios inherited from siblings. Carry to T17: fade rule subjectVisible UNGATED mirrors planet; if the mesh pass gates on bodies.items['mesh-body'].enabled, the fade row needs the gate.
Task 7: complete (commits 9aaa90e13..e2ca30d0b, review clean)
Task 13: implementer DONE (t13 205bb4964 → cherry-picked c47f448d0); review dispatched (sonnet), package review-e2ca30d0b..c47f448d0.diff. Slot freed → Task 15 dispatched (opus).
Task 11: implementer DONE (t11 dfbab6b5f → cherry-picked 41ba9b6ca); review dispatched (sonnet), package review-c47f448d0..41ba9b6ca.diff.
Task 15: dispatched (opus) in mesh-feat-t15, BASE c47f448d0. Ruling: T15 may create placeholder .wesl files so the renderer compiles; T16 replaces them (interface = the 176-byte layout pinned in both briefs) — cost if wrong: one merge conflict on three shader files resolved in T16's favour.
Task 13 + Task 11 reviews dispatched.
Task 11: minor (deferred): meshFetcher.ts header 13 lines (≤10 budget) — comment-audit at the end.
Task 11: complete (commits c47f448d0..41ba9b6ca, review clean)
Task 13: review Needs fixes — Important: bodyStateInHostFrame.ts header 12 lines (≤10). Minor taken into the same round (ruling: per-frame work, one-line fix): frameContext loop filters SCENE_MESH_BODIES per candidate per frame → short-circuit when empty. Minor deferred: bodyStates.get(id)! invariant proven by T6's every-SCENE_BODIES test once T18 seeds rows.
Task 13: fix round 1/5 dispatched (resume implementer in fresh wt mesh-feat-t13fix off 41ba9b6ca), FIX_BASE c47f448d0.
Task 13: fix round 1/5 landed (t13fix 87898c618 → 7aa7dc5b4); scoped re-review dispatched, package review-41ba9b6ca..7aa7dc5b4.diff.
Task 10: implementer DONE (t10 1e650c886 → 18e830543; hand-rolled merge instead of joinPrimitives/weld/prune; MeshSourceEntry gained licence+attribution; budgets 150k tris / 2048 px; simplify path untested on real data); review dispatched (opus), package review-7aa7dc5b4..18e830543.diff. Infra: gltf-transform 4.5.0 + meshoptimizer 1.2.0 installed --no-save into main node_modules (worktree symlinks were one level too high; Node parent-walk masked it).
Task 13: fix round 1/5 (2 addressed, 0 open; commits 41ba9b6ca..7aa7dc5b4)
Task 13: complete (commits e2ca30d0b..c47f448d0 + fix 7aa7dc5b4, review clean after 1 fix round)
Task 15: implementer DONE (t15 bb2f59fc9 → 71132444f; 4 vertex slots SoA; placeholder .wesl ×3 with FINAL io struct; GPU_HANDLE_ROWS totality forced the boot-construction row here — T17 must NOT add a second); review dispatched (opus), package review-18e830543..71132444f.diff. T12 + T16 dispatched (disjoint files), BASE 71132444f.
Task 10: review Needs fixes — Important: (1) tangents transformed by cofactor (must be plain 3x3 + Gram-Schmidt); (2) mirrored nodes (det<0) silently mis-baked: normals inside-out, TANGENT.w not flipped, winding not reversed; (3) zero test coverage of non-identity node transforms; (4) listPrimitives walks Root.listNodes() not the scene (orphans count toward the material refusal). Minors taken into the round (ruling: cheap, per-asset correctness): post-simplify recount+warn, skin test asserts uvs/normals, substitution test reads 1x1 pixel values, non-indexed fallback. Minors deferred: meshSources comment ratio, buildMeshes header 11, file structure (transform+merge seam), sRGB curve triplication, quote() heuristic.
Task 10: fix round 1/5 dispatched (resume implementer in fresh wt mesh-feat-t10fix off 71132444f), FIX_BASE 18e830543.
Task 12 (sonnet, wt mesh-feat-t12) + Task 16 (opus, wt mesh-feat-t16) dispatched, BASE 71132444f. Running: T10fix, T12, T16 implementers; T15 review.
Task 15: minor (deferred to final fix wave): (1) meshes.delete(id) after releaseResources in setMesh (stale entry on throw); (2) keyof MeshAsset casts could be value-typed; (3) docs/RENDERER.md:31 flipY paragraph needs the unflipped meshBodyRenderer case; (4) EngineGpuHandles.d.ts:512 says meshBodiesLayer → meshBodiesPass; (5) minBindingSize on the uniform BGL entry (bodyPickRenderer precedent). Adjacent: mirrored-node winding → covered by T10 fix round.
Task 15: complete (commits 18e830543..71132444f, review clean)
Task 10: fix round 1/5 landed (t10fix 98abf5dcf → 45960846b); scoped re-review dispatched (opus), package review-71132444f..45960846b.diff.
Task 18a (asset half: provenance, RAW_DATA/MESH_SOURCES rows, Blender pre-bake, build-meshes run, seed rows, credit surface) dispatched (opus) in mesh-feat-t18, BASE 45960846b — ruling: split T18 so the asset half runs ahead of T17; T18b (perf, visual pass, trail ruling, Earthshine tone, DEPLOY note) after T17. data/raw/meshes symlinked to main.
Task 16: implementer DONE (t16 4d5307831 → 8450d6dc1; concerns for review: Earthshine not albedo-tinted, no irradiance scale/ambient, tangent not Gram-Schmidt'd); review dispatched (opus), package review-45960846b..8450d6dc1.diff.
Running: T12 impl (sonnet), T18a impl (opus), T10 re-review (opus), T16 review (opus). T17 waits on T12 + T16 review.

## RESUME MAP (written for compaction, 2026-09-10 — supersedes earlier status lines)
Branch worktree-whale-petunias-mesh-bodies HEAD 8450d6dc1 (pushed, draft PR #678). Parallelism 3. Per-task worktrees .claude/worktrees/mesh-feat-tN (absolute symlinks node_modules / public/data; t18 also data/raw/meshes → main). Controller cherry-picks task commits onto this branch, then `review-package PLAN BASE HEAD` + reviewer.
COMPLETE (review clean): T1 T2 T3 T4 T5 T6 T7 T8 T9 T11 T13 T14 T15.
IN FLIGHT:
- T10 re-review (opus) of fix 45960846b [findings: tangent matrix, mirrored nodes, transform tests, scene traversal]. On ADDRESSED → ledger "Task 10: complete". On open → fix round 2 (resume T10 implementer in a fresh wt off HEAD).
- T16 review (opus) of 8450d6dc1. Concerns to RULE on with the verdict: (1) Earthshine × albedo — take it if reviewer agrees it is physically right; (2) irradiance scale vs sibling shaders — Important if brightness mismatches planets; (3) tangent Gram-Schmidt — take if lib/normalMap.wesl does it. Fix round via a fresh wt mesh-feat-t16fix.
- T12 implementer (sonnet, wt mesh-feat-t12): on DONE → cherry-pick, review (sonnet).
- T18a implementer (opus, wt mesh-feat-t18): provenance, RAW_DATA/MESH_SOURCES rows, Blender pre-bake + run, build-meshes run, seed rows, credit surface; 2 commits. On DONE → cherry-pick both, review (opus; check attribution strings + inspect numbers).
QUEUED, in order:
1. T17 (opus, wt off HEAD after T12 + T16 land): brief task-17-brief.md + carry: ruling F12 (update the 14-name drawPick roster in passes.test.ts ~:970-985); GPU_HANDLE_ROWS boot row ALREADY exists from T15 — do not add a second, only fill/verify it; wireSlots.ts call wireMeshBodySlots (T12 may have added it — check); fade rule subjectVisible UNGATED mirrors planet — if the pass gates on bodies.items['mesh-body'].enabled, gate the fade row too; earthshineColor = ATMOSPHERE_PARAMS[hostId].groundAlbedo, earthshineStrength local knob; localPos/camPosLocal host-axes frame contract (F9).
2. T18b (needs dev server + USER): perf before/after per perf skill (--url of THIS worktree's dev server), visual pass checklist (brief), orbit-trail rings KEEP/SUPPRESS = user ruling, Earthshine tone, docs/DEPLOY.md R2 note. Backlog detail files already exist (spec commit) — skip that step.
3. Final whole-branch review (opus) over 69eede86f..HEAD → ONE fix wave (include deferred minors: T15's five items incl. docs/RENDERER.md flipY paragraph + meshBodiesLayer→Pass docblock; T11 header 13 lines; T9/T14/T5 comment ratios; T7 inherited ratios; plan Global Constraints sentence fix F13) → scoped re-review → deletion audit (opus, legacy framing) → safe-now commit → /feature-done (ledger archive to plans/completed, spec → specs/completed) → user squash-merges #678.
USER DIRECTIVES this session: parallelism 3; prep stayed at spec scope; assets = Livyatan by Major + Goudriaan petunias with Blender pre-bake; .mesh header 18 B (DataView copy); Earthshine albedo = ATMOSPHERE_PARAMS; raw GLBs gitignored; orbit-trail decision at visual pass; deletion-audit R1-R4 from the prep PR still open for the user.
Task 10: fix round 1/5 (8 addressed, 0 open; commits 71132444f..45960846b). Minor (deferred to final fix wave): index loop overruns on non-multiple-of-3 counts (guard or throw); transformTangent may collapse to zero on a degenerate frame (one-line fallback like generateTangents); `det` param carries a sign not a determinant (rename). Note: this worktree (execution branch) has NO node_modules dir — Node parent-walk resolves main's; reviewers that need vitest should run from main or a task wt.
Task 10: complete (commits 7aa7dc5b4..18e830543 + fix 45960846b, review clean after 1 fix round)
Task 16: review Needs fixes — Important: (1) pbrDirect not scaled by sun irradiance (~3x darker than host; pbr.wesl contract says caller scales). Ruling: shader const SUN_IRRADIANCE = 3.0 mirroring EARTH_SURFACE_PARAMS.sunIrradiance + a line in constants.parity.test.ts — not a uniform (frozen layout, one TS home already exists) — cost if wrong: one const to re-home. (2) Earthshine fill lacks surface albedo (plan-mandated expression) → Ruling: take `albedo * earthshineColor * strength * saturate(dot(n, dirToHost))`; spec wording + every sibling fill agree; strength is an untuned knob (T18b). Minors taken into the round: Gram-Schmidt tangent against normal (lib/normalMap.wesl precedent), io.wesl FRAME CONTRACT names sunDirLocal as host-axes, fragment header 11→≤10. Not defects: no AMBIENT floor (T18b visual call), metallic unread (brief-pinned).
Task 16: fix round 1/5 dispatched (fresh opus implementer — original agent id lost to compaction — in wt mesh-feat-t16fix off 8450d6dc1), FIX_BASE 8450d6dc1.
Task 12: implementer DONE (t12 08c2f14e5 → 5bb62511e; added AssetKey mesh: family + isMeshBodyKey/meshBodySlotKey/slotFor plumbing beyond brief, bodyTextures precedent; wireSlots.ts touched — T17 must check wireMeshBodySlots already wired); review dispatched (sonnet), package review-8450d6dc1..5bb62511e.diff.
Task 16: fix round 1/5 landed (t16fix 9dc82bbdf → a9c0b1766); scoped re-review dispatched (sonnet), package review-5bb62511e..a9c0b1766.diff. Carry to T18b: exposure check of direct term (now x3) + albedo-tinted Earthshine vs sphere bodies; no AMBIENT floor (visual call).
Task 17: dispatched (opus) in mesh-feat-t17, BASE a9c0b1766 (T12 + T16 fix landed on branch; their reviews pending — a fix round on either = small rebase for T17). Running: T18a impl, T12 review, T16 re-review, T17 impl.
PAUSED by user (2026-09-10): T17 worktree mesh-feat-t17 created at a9c0b1766 (symlinks set) but T17 NOT dispatched. Branch HEAD a9c0b1766 (T12 5bb62511e + T16 fix a9c0b1766 on branch, NOT yet pushed). Live agents: T18a implementer (opus, wt mesh-feat-t18), T12 review (sonnet, package review-8450d6dc1..5bb62511e.diff), T16 re-review (sonnet, package review-5bb62511e..a9c0b1766.diff). On resume: handle those three results per the RESUME MAP, then dispatch T17 with the carry list (drawPick roster test passes.test.ts:970-985 gains 'mesh-bodies' in registry order; GPU_HANDLE_ROWS meshBodyRenderer row exists at gpuHandleRegistry.ts:450 — fill/verify only; wireMeshBodySlots already wired at wireSlots.ts:111; earthshineColor = ATMOSPHERE_PARAMS[hostId].groundAlbedo; fade-rule gate check; F9 host-axes frame contract), then push.
Task 16: fix round 1/5 (5 addressed, 0 open; commits 5bb62511e..a9c0b1766)
Task 16: complete (commits 45960846b..8450d6dc1 + fix a9c0b1766, review clean after 1 fix round)
STILL PAUSED: T12 review + T18a implementer live; T17 not dispatched.
Task 12: minor (deferred to final fix wave): meshBodyLoadRadius.ts header 10 lines vs ~7 code (trim to rationale + pointer); meshSlotRegistry.ts 13 comment / 16 code with duplicated sentence (fold). Two keyed families un-factored in installLoadProgress/installSlots/slotFor — third family is the abstraction trigger, leave.
Task 12: complete (commits 8450d6dc1..5bb62511e, review clean)
STILL PAUSED: only T18a implementer live; T17 not dispatched.
RATE-LIMIT HALT 2026-09-10 13:48 UTC (ludens 5-hour limit, resets 14:50 UTC / 16:50 CEST): the T18a implementer (opus, wt mesh-feat-t18) was KILLED mid-task — its task notification reads "failed", which is the 429, NOT a real failure. Its work is UNCOMMITTED in that worktree: modified package.json, src/data/bodies/meshAssets.generated.ts, tools/utils/io/meshSources.ts, tools/utils/io/rawDataRegistry.ts, plus untracked tools/meshes/prebake/. On resume: inspect that tree FIRST and re-dispatch T18a as a resume-from-here brief (fresh agent, the old id is dead) rather than restarting the task. Branch HEAD a9c0b1766 still NOT pushed. T12 review + T16 re-review both landed clean before the halt (see the two lines above); only T18a and the undispatched T17 remain per the RESUME MAP.
Task 18a: implementer DIED (rate limit 429) mid-task, NOTHING committed in wt mesh-feat-t18 (at 8450d6dc1). On disk: modified package.json (prebake-petunias script), meshAssets.generated.ts (whale + petunias rows populated), meshSources.ts (2 rows w/ licence+attribution; petunias native = pre-baked GLB, petuniasSource registered), rawDataRegistry.ts; untracked tools/meshes/prebake/petuniasPrebake.py (259 lines); pre-bake RAN (data/raw/meshes/petunias/petunias.prebaked.glb 8.4 MB + .prebaked.albedo.png); build-meshes RAN (public/data/meshes/{whale,petunias}.{mesh,_albedo,_mr,_normal}; whale 5598 tris, petunias 149999 tris = at the 150k budget, normalMapSubstituted true). LICENSE + README.md present in data/raw/meshes/{whale,petunias} (main's tree via symlink — check they are not gitignored). NOT done: sceneMeshBodies.ts seed rows, credit surface (Splash/ATTRIBUTIONS/README table), commits. OPEN FINDING (agent's last words): boundingRadiusM inflated by off-origin authored pivots — whale 8.915 m (12.9 m bbox → expect ~6.5-7), petunias 1.823 m (0.87 m object → expect ~0.5). Agent intended to recenter in the prebake; the whale needs the same → ruling on resume: recenter to bbox centre INSIDE buildMeshes (one path, both assets), not per-asset; then re-run build-meshes. On resume: fresh opus implementer in mesh-feat-t18 with this state summary (do not redo the pre-bake or provenance).
STILL PAUSED: no agents live.
RESUMED 2026-09-11 (user: continue). Dispatching T17 (opus, mesh-feat-t17 @ a9c0b1766) + T18a restart (opus, mesh-feat-t18, resumes from on-disk state; ruling: recenter to bbox centre inside buildMeshes).
Carry to T18b (from eyeballing the bake): petunias atlas = per-face islands over black (~half the 2048^2 is few-px dust) → expect mip black-bleed on leaves + meanAlbedo understated (averages black gaps). If visible: dilate/pad in the prebake or make meanAlbedo average covered texels only. Whale atlas fine.
Task 18a: implementer DONE_WITH_CONCERNS (t18 2be42db62 + fb027450e → 06576f7f8 + 60b0b29bc; + controller commit f215658c9 = raw READMEs + meshes.sha256 staged from main tree since data/raw/meshes is a symlink in t18). Radii recentred in mergeGeometry: whale 8.915→6.773 m, petunias 1.823→0.461 m. public/data/meshes/* gitignored (12.3 MB, R2 in 18b); build-data-manifest skipped in symlinked wt → RE-RUN from main before R2 sync; seed labels/descriptions authored by agent (user may rewrite); serializeMeshAssets gained printWidth wrap. Review dispatched (opus), package review-a9c0b1766..f215658c9.diff.
Dev server for the execution worktree STARTED 2026-09-11: http://localhost:5174/ (log scratchpad/dev-server.log). Perf in 18b: --url http://localhost:5174. The wt has a real node_modules dir holding only .vite cache; packages resolve via parent-walk to main.
Task 17: implementer DONE (t17 1df9a73bb → 387d88071; frameProgram.test.ts timedSlotsOf list gained mesh-bodies·BODY[0]; AIRLESS_HOST_ALBEDO=[0.1]^3 fallback for non-Earth hosts; earthshineStrength=0.4 knob for 18b); review dispatched (opus), package review-f215658c9..387d88071.diff. Seeds already on branch (T18a) → the pass is live on the 5174 dev server.
Task 18a: review Approved. Deferred to final fix wave (all no-behaviour-change): (1) petuniasPrebake.py centre_on_origin duplicates the tool's recentre + false docstring → delete fn + log line (no re-bake: output identical); (2) rawDataRegistry.ts meshes.dir description stale (LICENSE / 'entries land later') → one-line rewrite; (3) .py docstring 15 lines + plan-history framing → 2-line why; (4) spec :224 still says LICENSE per key → carry the gitignore ruling; (5) raw READMEs say 'falls' but orbit is circular; (6) join_meshes unguarded meshes[0] (leave). Provenance sha256 verified clean by reviewer; attribution byte-identical x4.
Task 18a: complete (commits a9c0b1766..f215658c9, review clean)
Task 17: minor (deferred): AIRLESS_HOST_ALBEDO fallback → leanness seat may prefer ?? [0,0,0]; meshBodiesAttachedTo reads static SCENE_MESH_BODIES vs partition reading state list (two tables, one truth — residuals note); eyeRelBodyM − posM spelled twice (extract at a third use).
Task 17: complete (commits f215658c9..387d88071, review clean)
ALL 18 TASKS CODE-COMPLETE except T18b (user-gated). NEXT: T18b on dev server http://localhost:5174 — perf before/after (perf skill, --url), visual checklist from task-18-brief.md, orbit-trail KEEP/SUPPRESS ruling, Earthshine tone (earthshineStrength=0.4 knob in meshBodiesPass.ts:46), seed copy review (sceneMeshBodies.ts), docs/DEPLOY.md R2 note + build-data-manifest from main + R2 sync of public/data/meshes; then final whole-branch review → fix wave (deferred minors list in ledger) → deletion audit → /feature-done.
T18b STARTED (user viewing 5174). User found EXISTING bug on main: orbit trails not occluded by their host (drawn through Earth) → separate session; prompt at .claude/rps/orbit-trail-occlusion-prompt.md (own worktree off main). NOT this PR. Orbit-trail KEEP/SUPPRESS ruling for whale/petunias rings still open.
T18b VISUAL FINDING 1 (user): petunias inside out. Diagnosed: prebaked GLB has 34,428/149,999 tris wound against their authored normal (SketchUp two-sided faces), both materials doubleSided:true, renderer cullMode 'back'. Ruling: (a) tool makes winding follow the authored normal (swap indices when cross(e1,e2)·n < 0) + one synthetic test; (b) renderer cullMode 'none' + fragment flips n/t when !front_facing (glTF doubleSided semantics), no flag; re-run build-meshes. Fix dispatched (opus) in wt mesh-feat-t18fix off 387d88071.
T18b PERF: NEUTRAL/noise-dominated (earth-surface scenario never frames the whale → mesh-bodies pass absent; merged ms before 20.5/15.2, after 17.2/14.0, drift 5 ms > band; user tab shared GPU). Report scratchpad/perf-mesh-bodies.md. No common-path regression; pass cost unmeasured. User rules at landing.
T18b VISUAL FINDING 2 (user): after search fly-to, wheel zoom parks the camera far away. Investigating camera drivers (followBody activation = bodyMovesThisFrame).
T18b FINDING 2 diagnosed: MIN_DISTANCE_MPC=1e-17 Mpc (309 km) floors every body smaller than 309 km (its own comment says so); follow's initial approach never clamps (reaches bodyFocusDistance ≈30 m) but the first wheel event clamps to 309 km. Near plane is NOT the blocker (T2 widening + MIN_NEAR_M). Ruling: MIN_DISTANCE_MPC → 1e-24 (≈3 cm) + MeshBody gains standoffRadii (maker sets 2.0, Sgr A* precedent) so the wheel floor = 2 radii. Fix dispatched (opus) in wt mesh-feat-t18fix2 off 387d88071.
T18b FINDING 3 (user): whale/petunias labels not visible. Cause: sceneBodyLabels tints from albedo; baked mean albedo ≈0.10/0.17 (dark skin; atlas black gaps) → near-black caption. Ruling: mesh-body label tint = albedo scaled so max channel = 1 (hue kept, no new data); offer seed-authored tint only if user wants evocative colours. Fix dispatched (sonnet) in wt mesh-feat-t18fix3 off 387d88071.
T18b fix1 (inside-out) landed: t18fix bc2384e5c → 7f3581fab (winding normalised at bake: petunias 34,428→0 disagreeing; cullMode none + front_facing flip; meshes rebaked, radii unchanged). BRANCH-LEVEL TEST BREAK found: T18a seeds landing after T12/T17 tests → assetWiring.test.ts membership (mesh:whale/mesh:petunias) + earthPass.test.ts fixture lacks data.bodies.meshBodies. Fix dispatched (sonnet) in wt mesh-feat-t18fix4 off 7f3581fab.
T18b fix3 (label tint) landed: t18fix3 d06fff6b1 → 3b4b5d2a4; scaleToUnitMax util; whale tint [1,.93,.88], petunias [.96,1,.86].
T18b fix4 (test breaks) landed: t18fix4 a7e77dab3 → 25ea90a6c; suite 8269/8269 green in that wt.
T18b fix2 (zoom floor) landed: t18fix2 2331f272b → b4c784e87. Note for final fix wave: docs/backlog/2026-07-29-analytic-pick-inside-body.md premise (309 km wall) now false → one-line update. All 4 T18b fixes on branch; run full suite + push, then user re-checks 5174.
T18b FINDING 4 (user): whale upside down. Diagnosed: whale mesh extents x4.27 y3.90 z12.88 (long axis = Z, dorsal = Y, glTF Y-up); rotationFromIau spins about local Z → whale rolls about its length (belly-up half of every 3-min turn); petunias (tall along Y) cartwheels. Options for user: A bake-time glTF Y-up→Z-up so pole = dorsal (yaw spin, never rolls; belly still away from Earth half an orbit); B nadir-locked orientation kind (belly always to Earth) = the backlogged tagged-union trigger; C keep tumble. Awaiting ruling. Scoped review of fixes 387d88071..b4c784e87 dispatched (sonnet).
Fix-wave review (387d88071..b4c784e87): all 4 approved; minors deferred: cullMode none perf unmeasured, fragment.wesl ratio 0.78.
T18b FINDING 5 (user): selection ring way too small. Cause: near0RingRadiusPx safeDist floor 1e-18 Mpc = 31 km (comment claims 3 cm — unit slip) → use MIN_DISTANCE_MPC. Fix dispatched (sonnet) wt mesh-feat-t18fix6.
T18b FINDING 6 (user): petunias textures mis-mapped, same in Blender on the prebaked GLB. Cause: petuniasPrebake.py unwrap() sets uv.active_render=True on the BAKE layer before baking → source Image Texture nodes sample through the bake UVs → atlas painted from wrong UVs. Fix: keep active_render on the source layer during bake (bake target = uv_layer= arg); re-run prebake + build-meshes; headless preview render. Dispatched (opus) wt mesh-feat-t18fix5.
T18b FINDING 7 (user): labels too small — cause NOT yet found (anchors camera-relative f64; worldEm = radius; asked user for context).
User said 'attitude fix works' — ambiguous (no attitude change landed); whale roll ruling A/B/C still open.
T18b fix6 (ring floor) landed: t18fix6 861dff431 → 188bcf866, pushed. Waiting: fix5 prebake (opus).
USER REQUEST: descriptions → direct Douglas Adams ch.18 quotes with attribution (short excerpts). Dispatched (sonnet) wt mesh-feat-t18fix7 off 188bcf866. Live: fix5 prebake (opus), fix7 copy (sonnet). Open user rulings: whale roll A/B/C; labels-too-small context; orbit-trail rings keep/suppress; Earthshine tone.
fix7 (Adams quotes) landed: 617206c8a → 2591a7eea, pushed. User re-reported tiny ring — likely pre-fix state (HMR); told to hard-reload.
USER REQUEST: honour line breaks in info text → DescriptionBlock pre-line + \n in the Adams quotes. Dispatched (sonnet) wt mesh-feat-t18fix8 off 2591a7eea. Live: fix5 prebake (opus), fix8 (sonnet).
- fix8 (line breaks: DescriptionBlock pre-line + \n in Adams quotes) LANDED 48465d4b5 (cherry-pick of ba441c5fb), pushed. Note from implementer: only NGC 147's famous blurb carries \n (one mid-sentence hard-wrap artifact + one intentional pre-quote break + one trailing) — data untouched, now renders those breaks; check NGC 147 in the visual pass. Live: fix5 prebake (opus).
- fix5 (petunias atlas through source UVs) LANDED 238342f7a (cherry-pick of bfb94dfd3), pushed. Preview confirms leaves green/flowers white. Residual found by the agent: source carries a coincident untextured duplicate bowl shell (`material`, 560 faces) that wins after material flattening → white pot. → fix9 dispatched (RESUMED fix5 agent, wt mesh-feat-t18fix5 on bfb94dfd3): drop `material` faces before join, loud guard, re-bake.
- USER FINDING: pot's selection ring "towards the right, not on the pot". Mesh bbox centre IS at origin (verified petunias.mesh min/max symmetric) → ring is on the body centre; the basket's visible mass (white bowl + flowers) sits off the bbox centre and the dark leaves vanish against space, so the ring looks displaced. Expect fix9 (tan bowl) to help; re-check with user after. Not a code defect so far.
- USER REQUEST: whale head in the direction of motion (+ earlier "upside down"). RULING: orbit-locked via synchronous rotation row (pole = orbit normal 0/90, rate 360/MESH_BODY_PERIOD_DAYS, W₀ for head-prograde) + bake-time source→body frame remap in MESH_SOURCES; resolves the open whale-roll A/B/C question (= A+, no new orientation kind). Dispatched (opus) wt mesh-feat-t18fix10 off 238342f7a, brief t18fix10-brief.md. Live: fix9 (opus, resumed), fix10 (opus). After both land: one final `npm run build-meshes` + commit generated file.
- USER: ring offset IS a real bug, whale too. Re-diagnosed: the ring projects through the NEAR0 slab in heliocentric Mpc (`centreWorld − camPos`, `rebaseViewProj` cancellation) where 1 AU f64 ulp ≈ 0.2 m; the mesh projects through the host body-m row in metres. Residual ~0.2–1 m = most of the pot's FOV at 2-radii standoff. fix11 dispatched (opus) wt mesh-feat-t18fix11 off 238342f7a, brief t18fix11-brief.md: failing-test repro first, then remove the Mpc cancellation from the ring path for body rows (metre seam + exact P·R rebase); report captions + posM jitter. Live: fix9, fix10, fix11.
- fix9 (drop duplicate white bowl shell before join, loud guard, correct_aspect=False to keep square atlas packing) LANDED (cherry-pick of 5798a1761), pushed. meanAlbedo 0.100/0.115/0.082 → 0.095/0.107/0.067, tris 150000. fix5 agent retired (174k). Live: fix10 (whale heading), fix11 (ring offset).
- USER: whale head points down + same spin axis on the live server = half-applied fix10 (agent re-baked the SHARED whale.mesh at 02:31 with the axis remap while its rotation-row change is still uncommitted in wt t18fix10). Explained; resolves when fix10 lands.
- USER REQUEST: info-card Radius row must go down to m/cm/mm. fix12 dispatched (sonnet) wt mesh-feat-t18fix12 off 20ec500d3: `formatRadiusM` util + test, BodyDetailCard uses it. Live: fix10, fix11, fix12.
- fix12 (formatRadiusM km/m/cm/mm + BodyDetailCard) LANDED (cherry-pick of afd5dffc1), pushed. Live: fix10, fix11.
  DEFERRED MINOR (final fix wave): formatRadiusM.ts header 14 lines / 13 code lines — over both budgets; trim to ≤ 5 lines (why the ladder + why km keeps toLocaleString).
- fix10 (whale orbit-locked: pole 0/90, W₀=90° (brief's 0° premise was wrong — epoch whale sits at +Y), rate 360/MESH_BODY_PERIOD_DAYS now exported; bake remap bodyFromSource [0,0,1,0,-1,0,1,0,0] in MESH_SOURCES; head/dorsal = source +Z/+Y from six-axis Blender renders; orbit-lock test + remap test, mutation-verified) LANDED 9239d7b10 (cherry-pick of 2b32af9d9; generated-file hunk was a no-op after fix9), 737 tests green, pushed. Whale-roll A/B/C ruling CLOSED. fix10 agent retired (189k). Live: fix11 (ring offset).
  Notes for final review: rotationElements.ts stays hand-formatted (was unformatted on main); flaky galaxyIsmMapArmForcing.test.ts seen once by the agent (passes alone).
- USER FINDING: mesh-body labels displaced from the body (pick-proxy quad correct) and shrinking on approach. Same root as the ring: caption anchor = worldPos − camPos in Mpc + near0LabelProjection's rebase → residual dominates clip.w at metre range (label size = worldEm/clip.w). Folded into fix11 by message (captions fixed with the same helper; test extended). Live: fix11 only.
- fix11 LANDED 75097bc28 (cherry-pick of ffd7e5b82), pushed. MY HYPOTHESIS REFUTED by measurement: 1 AU = 4.85e-12 Mpc (f64 ulp 25 µm) — ring and caption project onto the mesh origin to 0.014 px. Real defects: (1) NEAR0 near plane floored at MIN_NEAR_MPC (6.17 m) > pot standoff 0.92 m → ring quad discarded whole; FIXED (pinInsideNearPlane, NEAR_PIN_MARGIN 1.001) + test near0SelectionRingPass.meshBody.test.ts. (2) What the user SEES = affordance sizing at 2-radii standoff: ring radius 1.5×apparent = 686 px, caption lift 1.5×subjectSizePx = 1373 px on a 1000 px viewport (glyphs capped 64 px) → looks like "ring beside the body" / "label shrinks". Shared look knobs (Earth/planets/stars) → USER RULING requested (fit-to-viewport clamp / hide when body fills view / bigger standoff). (3) whale bbox-centre anchor 3.58 m (0.53 r) from its visible mass → fix13 dispatched (sonnet) wt mesh-feat-t18fix13 off 75097bc28: recentre on area-weighted surface centroid at bake, re-bake both. fix11 agent retired (303k). DEFERRED MINOR: NEAR_PIN_MARGIN knob — reviewer to judge.
- fix13 (area-weighted surface-centroid recentre at bake; whale r 6.773→7.237 m, centroid moved 0.52 m; petunias r 0.461→0.498 m, moved 0.054 m; both re-baked) LANDED b3dcdad05 (cherry-pick of e4ba33353), pushed. Agent note: no totalArea===0 guard (matches prior posture).
- USER RULING (ring/caption when the body overflows the viewport): FADE OUT when it overflows (not fit-to-viewport, not bigger standoff). One rule for all NEAR0 body kinds. fix14 dispatched (opus) wt mesh-feat-t18fix14 off b3dcdad05, brief t18fix14-brief.md: overflowFade helper + ring alpha uniform (io.wesl/fragment×2/renderer) + caption fadeAlpha multiply + tests. Live: fix14 only.
- fix14 (overflowFade helper src/utils/scene/overflowFade.ts, band: apparent diameter 2/3→5/6 of viewport short side; ring alpha uniform (io.wesl grown, shadeRing multiplies once, fragmentOcclude inherits); near0RingRadiusPx returns {ringRadiusPx, apparentRadiusPx}; caption fadeAlpha × overflowFade; tests) LANDED 290971773 (cherry-pick of 51c0b8338), 1074 tests green in affected dirs, full suite 8304 green in the agent's wt, pushed. Agent notes: Earth/planet captions now fade on final descent (ruled: one rule); pot's ring never visible at its own standoff (caption faded too) — user to judge visually; CAPTION_FADE_RULES has no redundancy. fix14 agent retired (217k). No agents live.
  NEXT: user visual re-check (ring/caption fade, whale heading, pot tan, recentre, radius units) → remaining open rulings (orbit-trail rings keep/suppress; Earthshine tone; prep deletion-audit R1-R4) → T18b remainder (DEPLOY.md R2 note, build-data-manifest from main, R2 sync) → final whole-branch review (opus) 69eede86f..HEAD with the deferred-minors list → fix wave → deletion audit → /feature-done.
- T18b VISUAL PASS ATTESTED by user 2026-09-11: whale heading ✓, bowl ✓, radius units ✓, ring+caption fade ✓. Rulings defaulted absent complaint: orbit-trail rings KEEP, Earthshine tone KEEP (user may still override). Proceeding to final whole-branch review.
- USER RULING: SUPPRESS the whale/petunias orbit-trail rings ("we dont need them for now"). fix15 dispatched (sonnet) wt mesh-feat-t18fix15 off 290971773: orbitTrailsPass trail-row list = ORBITAL_ELEMENTS minus mesh-body ids (module constant), test. Final review WAITS for fix15 to land (package must include it). Review package for 69eede86f..290971773 already written (review-69eede86f..290971773.diff, 611 KB) — regenerate after fix15. Deferred-minors extract: final-review-deferred-minors.md.
- fix15 (orbitTrailsPass TRAIL_ELEMENTS = ORBITAL_ELEMENTS minus mesh-body ids; reach untouched; test) LANDED 2feef94b9 (cherry-pick of 897d847f1), pushed. All T18b rulings closed except Earthshine (default keep) + prep deletion-audit R1-R4 (user). FINAL WHOLE-BRANCH REVIEW dispatched (opus): package review-69eede86f..2feef94b9.diff (46 commits, 621 KB), report final-review-report.md. No other agents live.
- T18b REMAINDER FOUND A REAL GAP: tools/deploy/r2/allowDataFile.ts is a name allow-list without meshes → buildDataManifest never hashes/lists `meshes/*` and collectDataFiles never syncs them → prod 404 on mesh bodies. fix16 dispatched (sonnet) wt mesh-feat-t18fix16 off 2feef94b9: allow `meshes/<key>.mesh` + `<key>_(albedo|mr|normal).png`, tests, DEPLOY.md note (plan DoD item). Deploy sequence after merge (USER, from the main checkout): `npm run build-meshes` (or just `build-data-manifest`) → `npm run sync-r2-secure`. build-data-manifest cannot run in worktrees (symlink guard) — the "from main checkout" note stands. DoD checks: 3 backlog detail files + index lines ✓ present; credits ✓ Splash(3)/ATTRIBUTIONS(6)/README(1)/2× raw README. Earthshine DoD box: user has not objected → treat as accepted unless they say otherwise. Live: final review (opus), fix16 (sonnet).
- fix16 (allowDataFile meshes/ patterns + tests + DEPLOY.md note) LANDED (cherry-pick of 35be2bc19), pushed. NOTE for the final review's fix wave: fix16 landed AFTER the review package was cut (69eede86f..2feef94b9) — the scoped re-review must cover it (tools/deploy/r2/allowDataFile.ts, its tests, collectDataFiles.test.ts, docs/DEPLOY.md). Live: final review (opus).
- FINAL REVIEW DONE (final-review-report.md): verdict WITH FIXES — 1 critical (C1 = the deploy allow-list gap, already fixed by fix16 32896a3bc, reviewer re-checked it) / 6 important (I1 MIN_DISTANCE_MPC 1e-24 lowers the SURFACELESS pivot floor below the near plane; I2 dead SCENE_MESH_BODIES.length===0 branch; I3 cull docblocks vs cullMode none; I4 plan-voice comments ×5; I5 extractSelectionRow false invariant; I6 RENDERER.md) / 22 minor (all 18 deferred minors triaged; M10/M12/M14-meshBinaryFormat/M16/M20/M21/M22 ACCEPT). Perf gate FORMALLY OPEN: T18b NEUTRAL never framed a mesh body → cost unmeasured. FIX WAVE dispatched (opus) wt mesh-feat-t18fix17 off 32896a3bc, brief final-fix-wave-brief.md, report final-fix-wave-report.md: commit 1 = all findings (I1 ruling: prefer revert constant to 1e-17 + radius-derived floor with no second constant, else the report's named surfaceless floor), commit 2 = `mesh-body` perf scenario framing the whale. NEXT after landing: perf before/after on the mesh-body scenario (main server vs branch server, --url) → user land/park ruling → scoped re-review (must cover fix16 + wave) → deletion audit → /feature-done. PR body must note D10 (glint-scale mesh body not pickable). Live: fix wave only.
- USER RULING 2026-09-11: NO perf test — skip the mesh-body perf scenario and the A/B; perf gate closed by ruling (= land). Fix wave told to drop commit 2; baseline server :5624 + wt mesh-perf-base removed. Remaining: fix-wave commit 1 → cherry-pick → scoped re-review (fix16 + wave) → deletion audit → /feature-done.
- FIX WAVE LANDED 82fa5ab8a (cherry-pick of 66c96e852; 27 files +185/−171; suite 8309 green in the agent's wt), pushed. I1 took the report's variant (SURFACELESS_FLOOR_MPC = 1e-17 in pivotRadiusMpc.ts; MIN_DISTANCE_MPC stays 1e-24 because near0RingRadiusPx uses it as a divide guard — reverting would collapse a mesh body's ring). Agent concerns: M6 index-count guard untested (one-line revert if unwanted); perf-scenario framing hazard recorded in final-fix-wave-report.md (app boots live clock → J2000 target needs a clock pin). Perf commit skipped per user ruling. NEXT: scoped re-review 2feef94b9..82fa5ab8a (fix16 + wave) ∥ deletion audit (opus, legacy framing) → safe-now commit → /feature-done. mesh-feat worktrees: 19 removed; t18fix17 remains until the re-review clears.
- DISPATCHED in parallel (both opus, read-only): (a) scoped RE-REVIEW of 2feef94b9..82fa5ab8a (fix16 + fix wave; package review-2feef94b9..82fa5ab8a.diff) → report final-re-review-report.md; (b) DELETION AUDIT whole branch 69eede86f..82fa5ab8a (legacy framing; diff audit-69eede86f..82fa5ab8a.diff, fenced on all rulings + the 22 triaged minors) → report deletion-audit-69eede86f..82fa5ab8a.md. Handling: re-review open findings → one small fix (sonnet, wt mesh-feat-t18fix17 reused off 82fa5ab8a) else clear; audit SAFE-NOW bin → one deletion commit (per leanness.md), NEEDS-RULING → user; then /feature-done (tick plan checkboxes, archive ledger, move plan+spec, backlog sweep, memory update) → user squash-merges #678 → deploy from main checkout (build-data-manifest + sync-r2-secure). PR body already updated (deviations + D10 + deploy note). Live: re-review, deletion audit.
- RE-REVIEW CLEAN (final-re-review-report.md): all findings ADDRESSED, 0 open, no new breakage; I1 reasoning confirmed (surfaceless floor 309 km ≈ 5e4× MIN_NEAR_MPC; new test fails pre-fix). 4 minors → fold into the audit's safe-now commit: pivotRadiusMpc.ts ratio 17/22 (two docblocks say the same); near0RingRadiusPx.ts:60-63 comment now false for surfaceless pivots; packMeshBodyUniforms.ts header 19 lines; docs/RENDERER.md:16 loose list. Live: deletion audit only.
- DELETION AUDIT DONE (deletion-audit-69eede86f..82fa5ab8a.md): branch lean (~268 of +8053 removable, 3%). SAFE-NOW 5 items ≈123 LOC (#1 extractSelectionRow.test mock apparatus, #2 sceneBodyLabels.test resetModules apparatus, #3 cannot-fail "omits description" test, #4 restating headers on trivial files, #5 no-op whale alias row). NEEDS-RULING 6: #6 meshAssets.generated.ts unread fields (tier 1 key/path/triangleCount/normalMapSubstituted; tier 2 provenance strings — credits are hand-written, the "credit surface reads it" claim is false) → USER; #7 BodyStore.meshBodies parallel path (one consumer, 8 fixtures carry it) → USER; #8 sunVisibleFraction annular guard + test → USER; #9 overflowFade monotonic sweep re-tests fadeBand → CONTROLLER RULING delete (testing.md); #10 mm rung → KEEP (user asked for m/cm/mm explicitly); #11 buildMeshes test merge → KEEP (low confidence). Safe-now commit dispatched (sonnet) wt mesh-feat-t18fix17 reset to 82fa5ab8a: #1-#5 + #9 + the 4 re-review minors.
- SAFE-NOW DELETION COMMIT LANDED (cherry-pick of bd973d3fe; 16 files +45/−205, net −160: src −57 (comments −56), tests −104, docs −1; 3623 tests green in affected dirs), pushed. USER RULINGS OPEN (asked in chat, recommendations: #6 trim tier-1 fields only + re-bake; #7 delete store arm; #8 keep guard, delete its clamp test). NEXT: /feature-done audit (baseline suite+typecheck, tick 149 plan checkboxes, archive ledger, move plan+spec, backlog sweep, memory) — rulings can land as a follow-up commit on the PR before the squash.
- USER RULING 2026-09-11: SKIP the NEEDS-RULING deletions (#6 generated-row fields, #7 BodyStore.meshBodies, #8 annular guard) — all stay as-is. DoD baseline at 412ae99fe: tsc clean both projects, vitest 1236 files / 8307 tests green. /feature-done housekeeping now: tick plan checkboxes, plan+spec → completed/, ledger archived, backlog: nothing to sweep (the three 2026-09-10 items are follow-ons, stay).
- /feature-done DONE: completion commit 2d8e141ee pushed (plan 149/149 ticked → plans/completed, spec → specs/completed, ledger + deletion-audit archived beside). wt mesh-feat-t18fix17 removed (0 mesh-feat worktrees left). PR #678 body ticked, marked READY. Remaining = USER: squash-merge #678 from main; deploy from MAIN checkout (`npm run build-data-manifest` → `npm run sync-r2-secure`); prep R1-R4 rulings; remove execution worktree via skymap-wt-clean.sh.
- PR #678 CONFLICTING with main (4 PRs since base: #647 surface camera pose seam, #682 trails after composite, #683 star-cut, #676 splats); content conflicts engine.ts / frameContext.ts / slabs.ts. MERGE AGENT (opus) dispatched IN the execution worktree: merge origin/main, resolve, check semantic conflicts (orbitTrailsPass TRAIL_ELEMENTS vs #682 restructure, pass ordering), tsc + full vitest, commit + push; report merge-main-report.md. After: ledger, tell user what to eye-check at :5174, then user squash-merges.
- USER RULING 2026-09-11 (post /feature-done, pre-merge): DROP the `description` field from MeshBody/BodyInfo/SelectionRow/extractSelectionRow/buildFocusable + the card's third block — the blurbs go into data/seeds/planet_facts.seed.json (BODY_FACTS, wikiTitle = The_Hitchhiker's_Guide_to_the_Galaxy) so the card's existing `facts.description` path renders them. Brief: post-review-description-brief.md. Dispatch AFTER the merge agent releases the worktree (a fresh per-fix wt off the merge commit), cherry-pick, push; then update memory (decision list says "description? threaded" — now reversed).
- MERGE OF MAIN LANDED c4de2d39b, pushed (8958 tests green; 3 prose-vs-code conflicts; one semantic fix: two main fixtures needed meshBodies: [] because #682 routes trail occluders through sceneBodyPartition; suppression + pass placement intact). User eye-check on :5174 requested: whale/petunias still draw, no 400 km ring, Moon trail occluded behind Earth. Description-field removal dispatched (sonnet) wt mesh-feat-t18fix18 off c4de2d39b, brief post-review-description-brief.md.
- DESCRIPTION FIELD REMOVED: cherry-pick of 9c54e3504 (16 files +29/−67; blurbs in planet_facts.seed.json → BODY_FACTS; buildPlanetFacts quote() newline-escape bug fixed en route; 3262 tests green), pushed. USER FINDING (pre-existing): whale caption vanishes over Earth's disc although the whale is in front — cause: label/ring/marker-line occlusion = per-pixel alpha of foreground:0 (lib/sceneDepth.wesl sceneTransmittance), not depth. Proposed fix option 1 (per-caption analytic anchor-behind-occluder-sphere weight, mixed into the shader; ring picks its pipeline per draw); option 2 = replace per-pixel rule entirely. Awaiting user; default option 1.
- CAPTION OCCLUSION FIX dispatched (opus) wt mesh-feat-t18fix18 reset to af8d16cb3, brief caption-occlusion-brief.md (option 1: per-label occludeWeight in LabelData + marker-line instance, mix(1, sceneTransmittance, w); weight from a pure segment–sphere helper over the frame's body spheres; ring passes colorView only when its subject is occluded). Report caption-occlusion-report.md. After landing: cherry-pick, push, user eye-check whale caption over Earth's disc; then memory update (description decision reversed; occlusion rule) → user squash-merge → deploy from main.
- RATE-LIMIT: first caption-occlusion agent died before any edit (wt clean at af8d16cb3). Re-dispatched fresh (opus), same brief/worktree/report path. Live: caption-occlusion fix only.
- CAPTION OCCLUSION FIX LANDED (cherry-pick of 89dcd9bb4; 22 files +227/−90; 8967 tests green; LabelData stride 64→80, marker-line stride 48→52; helper skips a sphere CONTAINING the subject instead of id exclusion; NEAR0 ring now occludes when its subject is behind a body — it never had occlusion before), pushed. GPU-untested by the agent → USER EYE-CHECK on :5174: whale caption/leader/ring stay legible over Earth's disc; Moon caption still fades behind Earth; rings at a limb. Then memory update → user squash-merge #678 → deploy from main; wt mesh-feat-t18fix18 to remove.

## 2026-09-11 — post-merge user findings (two bugs), fix in flight
- User: whale + petunias vanish when Earth is behind the camera (petunias focus, 2.15 m). Root cause (read, not run): `visibleSlabBodies` frustum-culls Earth past ~126° off-axis; mesh bodies ride Earth's body-m row → no row → nothing drawn.
- User: orbit trails draw over the models. Root cause: `sceneOccluderBodies` (analytic trail occlusion from #682) lists flat/textured/stars/Earth, not the `meshes` branch.
- Dispatched ONE opus implementer in worktree mesh-feat-t18fix19 (detached 765bc9231): brief `host-cull-and-trail-occluder-brief.md`, report `host-cull-and-trail-occluder-report.md`. Two commits expected: `fix(mesh-bodies): keep a host's slab row while an attached mesh body is on screen`, `fix(orbit-trails): mesh bodies occlude orbit trails`.
- Handling: cherry-pick both onto the execution branch, push, user eye-checks on :5174, then remove worktree mesh-feat-t18fix19. Caveat to surface: mesh occlusion uses the bake's bounding sphere (whale 7.24 m), so a trail vanishes slightly outside an elongated silhouette.
- DONE: cherry-picked as beaea2c4b (host row kept while rider on screen) + f0ec7be0b (meshes in trail occluder set), pushed; worktree mesh-feat-t18fix19 removed. Report `host-cull-and-trail-occluder-report.md`. Occluder set peaks at 6 of 16. User eye-check on :5174 pending.
- 2026-09-11 user bug 3: petunias MESH drawn ~3.5× further from the whale (screen) than its caption/pick; swings with viewing side, grows on approach; whale itself centred. Hand probe (`probe-lift.ts`) shows caption anchor == mesh centre to 1e-4 px at the pose, so the divergence is in frame wiring or GPU-side. Dispatched opus investigator (read-only) with brief `frame-replica-brief.md` → report `frame-replica-report.md`. Handling: if a line is named, dispatch a fix agent in a fresh mesh-feat worktree; else run its proposed console instrument on :5174 via the user.

- 2026-09-11 bug 3 UPDATE: user confirms hover highlights on the DRAWN mesh → pick + mesh agree; the CAPTION + leader line are displaced (~215 px vs ~766 px left of centre for the petunias at ~150 m from the whale). Suspect = caption path (produceSceneBodyCaptions → label2DDirector lift → near0LabelProjection), mesh path = reference. Investigator afdc66572724e0f18 told via SendMessage; same handling: named line → fix agent in mesh-feat-t18fix20; else console instrument on :5174.
- 2026-09-11 bug 3: replica (frame-replica-report.md) reproduces NOTHING — mesh/pick/caption/leader coincide at the dumped pose and across a 3000→5 m sweep; geometric bound: at 150 m the petunias cannot exceed 450 px off centre, so 766 px (mesh) ⇒ eye ≈ 82 m while 215 px (caption) ⇒ ≈ 302 m. LIVE PROBE installed (UNCOMMITTED, tagged `PROBE(temp)`) in src/services/engine/frame/passes/meshBodiesPass.ts: throttled console.log per mesh body of meshPx / flushed caption px / leader tip px / live anchor px + canvas/fovY/roll/rowNear. User reads it on :5174 at the failing pose. REVERT with `git checkout -- src/services/engine/frame/passes/meshBodiesPass.ts` before any commit.
- 2026-09-11 bug 3 ROOT CAUSE (headless capture, scratchpad capture.mjs + cap82*.png): NEAR0 overlays (foreground labels, leader lines, near0 selection ring) project Mpc-unit eye-relative anchors; within ~309 m clip w < 1e-20 and the rasterizer floors it → uniform shrink about screen centre (ring 563 dev px CPU → 56 px drawn; whale caption lift 1127 → ~106 px; both give w_floor ≈ 1.0e-20 Mpc). Mesh/pick rows are in metres → unaffected. Skipping ring/label passes removed the visible ring/caption = confirmed same renderers. Fix: scale the NEAR0 overlay clip space (homogeneous scale by MPC_TO_M or metre units) — dispatch fix agent in mesh-feat-t18fix20. Probe patch saved at scratchpad/probes.patch; REVERT the 3 probe files (`git checkout -- src/services/engine/frame/passes/{meshBodiesPass,foregroundLabelsPass,near0SelectionRingPass}.ts`) before cherry-pick.
- 2026-09-11 dispatched fix agent (opus) in mesh-feat-t18fix20 with brief `near0-clip-scale-brief.md` → report `near0-clip-scale-report.md`. Handling: revert the 3 probe files in the execution worktree, cherry-pick the SHA, push, then verify headlessly with scratchpad `capture.mjs` against :5174 (expected at 31 m: ring radius ≈ 282 css px, whale caption above the top edge / petunias caption on its mesh), then ask the user to eye-check.

## 2026-09-11 bug 3 fixed — NEAR0 overlay clip-w floor
- Fix agent SHA f6b379f6b → cherry-picked as f0103542f (HEAD, pushed). Probes reverted in the 3 pass files; fix worktree mesh-feat-t18fix20 removed. Report: `near0-clip-scale-report.md`.
- Headless verification (scratchpad capture2.mjs, focus whale, card 31.3 m — setPose distance arg is NOT honoured under a focus pin, every capture lands at 31.3 m): ring radius ≈ 282 css px (unfixed: ~56), petunias caption on its marker, whale caption lifted correctly. Suite 8972 green, both tsc clean.
- User eye-check pending on :5174: (1) bug 3 across 309 m — caption placement AND caption size on both sides (em scale and matrix scale live in different files); (2) NEAR0 ring still draws very close; (3) COSMO labels/leaders/ring unchanged; (4) bugs 1–2: bodies stay drawn with Earth behind the camera; Moon trail passes behind the meshes.
- Then: user squash-merges #678 from main → deploy from MAIN checkout → remove execution worktree; prep R1–R4 rulings still open.

## 2026-09-11 two more user reports after the eye-check
- (A) "focused on petunias, search → whale, end up INSIDE the whale, can't get out": NOT reproduced headlessly (scratchpad repro-search.mjs / repro-search2.mjs: from 2.16 m and from the 1.24 m floor + drag + click result → lands at 31.3 m framing, wheel-out to 51 m works). Asked the user for the InfoCard distance when it happens + the preceding state. Mechanism reviewed: regimeArmFor releases a body arm on a differing focus; followApproach seeds bodyFocusDistance on a fresh focus row; no path found yet.
- (B) "cannot select the whale from its caption when it is in front of Earth": ROOT CAUSE confirmed in pickProgram.ts — per-slab near→far fold ranks NEAR0 (where body captions stamp) at star-sphere/far distance, so Earth's body row claims the texel under the caption. Fix agent (opus) dispatched in worktree mesh-feat-t18fix21 (detached f0103542f) with brief `caption-pick-over-body-brief.md` → report `caption-pick-over-body-report.md`. Handling: cherry-pick SHA onto the execution branch, push, remove the fix worktree, ask the user to click the caption over Earth's disc.
- (B) FIXED b1f1ff7ba (agent committed directly on the execution branch — harness blocks cross-worktree git from an isolated session; fix worktree t18fix21 unused, removed), pushed. Design: `ContentPass.pickTarget?: 'overlay'` + `pick:overlay` target folded ahead of the slab chain. Accepted residual: a caption whose subject is hidden behind a body takes the click over that disc. User check pending: click "Whale" caption over Earth's disc selects the whale; Earth off-caption still selects Earth; star behind a nearer planet still loses. (A) still awaiting user details.
- NOTE for future fix dispatches from THIS session: agents cannot git-commit in sibling worktrees while the session is worktree-isolated; either dispatch with isolation:"worktree" or let them commit on the execution branch.
- (B) user ATTESTED 2026-09-11: caption pick over Earth works.
- (C) user ruling: whale/petunias captions reveal only on close approach (easter egg); other mesh bodies keep default reach → per-seed optional `captionRevealM` (whale 300 m, petunias 60 m, band 2×→1×, controller's pick — user tunes). Agent (opus, isolation:worktree, base b1f1ff7ba) dispatched with brief `caption-reveal-brief.md` → report `caption-reveal-report.md`. Handling: cherry-pick SHA onto execution branch, push, remove the agent worktree (+ its worktree-agent-* branch), ask user to eye-check reveal distances and tune the two seed numbers.
- 0a0ecdf4a splash mesh credits dropped (ATTRIBUTIONS/README keep them), pushed.
- (D) mesh format v2 (4-byte aligned header, user ruling) — agent (opus, isolation wt) brief `mesh-format-v2-brief.md` → `mesh-format-v2-report.md`; after cherry-pick REBAKE in the execution worktree (`npx tsx tools/meshes/buildMeshes.ts`, NOT build-meshes) so :5174 reads v2 files; deploy later re-bakes from MAIN anyway.
- (E) host fill light generalised (no EARTHSHINE constant; solid-angle fraction; rename earthshine→host-neutral) + extract `drawableMeshBodies`/`composeMeshMvp` to utils — agent brief `host-shine-brief.md` → `host-shine-report.md` (extraction added by message).
- (F) pass-file purity: extract `pinInsideNearPlane`/`NEAR_PIN_MARGIN` from near0SelectionRingPass + ratchet test `passFilePurity.test.ts` + CLAUDE.md bullet — agent brief `pass-file-purity-brief.md` → `pass-file-purity-report.md`. USER RULING (binding, annoyed): pass files declare only the pass; every future brief carries that line. Memory feedback_pass_file_purity written.
- Cherry-pick order when they land: (C) caption reveal, (D) format v2 (+rebake), (E) host shine, (F) purity — (F)'s allow-list row for meshBodiesPass must be deleted once (E) lands (or lowered); run full suite after all four; remove each agent worktree + worktree-agent-* branch.
- c9e03f112 pick overlay group un-braided (user finding); da37ebdfb slabs near-face → `nearestSphereFaceM` + `HostFrameSphere` type (user finding); (C) caption reveal cherry-picked as 779503a2c (whale 600→300 m, petunias 120→60 m), pushed; agent worktree removed. Still in flight: (D) format v2, (E) host shine + helper extraction, (F) purity ratchet + ring/orbit-trail extractions.
- (D) format-v2 agent ALSO told (by message): extract meshBodyRenderer's VERTEX_SLOTS/TEXTURE_SLOTS → src/data/mesh/, MeshResources → @types/rendering, UNIFORM_BUFFER_SIZE (+MESH_BODY_UNIFORM_FLOATS if it lives in the utils pack file) → src/data/mesh/meshBodyUniformLayout.ts. Expect 1–2 SHAs from (D).
- (A) inside-the-whale: DROPPED by user ruling 2026-09-11 (no longer reproducible on their side either; not reproduced headlessly). Not backlogged.
- Pick residual BACKLOGGED 8be78e43f (docs/backlog/2026-09-11-caption-pick-hidden-subject-takes-click.md). PR #678 body being rewritten from scratchpad pr678-body.md (three unticked boxes = (D)(E)(F); tick them on landing via `gh pr edit 678 --body-file`).
- (E) host shine LANDED df91a20df (hostShineStrength 0.3158 vs old 0.4 → whale night side ~21% dimmer; old constant was two compounding slips; RULING NEEDED: keep physics or add artistic multiplier); composeMeshMvp went to utils/camera (beside composeBodyMvp — accepted). (F) purity LANDED a2efed7ac + 79c23dab5 (meshBodiesPass row removed); allow-list rows for misfiled helpers glintBandClass/labelPickQuads/sceneBodyPickId are FOLLOW-UP moves (not backlogged yet). Agent worktrees (E)(F) removed. NOTE: harness agent worktrees are cut from MAIN, not the branch — agents fast-forward themselves; verify base on every cherry-pick. Remaining in flight: (D) format v2 + renderer extraction.

## RESUME MAP 2026-09-11 (post fix-round, pre-compaction)
- Branch worktree-whale-petunias-mesh-bodies, HEAD 79c23dab5 pushed, tree clean, PR #678 body rewritten (scratchpad pr678-body.md; 3 unticked boxes: format v2 / host fill / purity — tick host fill + purity now (landed), format v2 on landing).
- IN FLIGHT: (D) format-v2 agent (opus, isolation wt cut from MAIN — it fast-forwards to branch HEAD itself; verify base). Brief `mesh-format-v2-brief.md`; report lands in ITS worktree `.superpowers/sdd/2026-09-10-mesh-bodies-02-feature/mesh-format-v2-report.md` (copy across). Expect 1–2 SHAs (format v2 + meshBodyRenderer extraction: VERTEX/TEXTURE_SLOTS → src/data/mesh/, MeshResources → @types/rendering, uniform constants → src/data/mesh/meshBodyUniformLayout.ts). Handling: cherry-pick, then REBAKE in the execution worktree `npx tsx tools/meshes/buildMeshes.ts` (NOT build-meshes), check meshAssets.generated.ts unchanged, :5174 loads both meshes; full `npm test` + `npm run typecheck`; push; remove agent worktree + branch; tick PR box.
- OPEN RULINGS (asked, unanswered): (1) host fill 0.316 vs old 0.4 — keep physics or artistic multiplier; (2) misfiled helpers glintBandClass/labelPickQuads/sceneBodyPickId — move on PR or backlog; (3) prep deletion-audit R1–R4.
- THEN (user-agreed sequence): merge main into branch (conflicts expected: orbit-trail occlusion #682, star-catalog #683) → scoped review + deletion audit over 765bc9231..HEAD → refresh archived ledger copy in completion commit → user eye-check (reveal distances, host fill, v2 load, ring close range) → user squash-merges from main → deploy from MAIN (`npm run build-meshes` → `npm run sync-r2-secure`, CDN check) → cleanup worktrees → memory update (landmines: clip-w floor, pick fold rank, agent worktrees cut from main, worktree-isolated git blocks sibling commits).
- DROPPED: inside-the-whale (user, unreproducible). BACKLOGGED: hidden-subject caption pick residual (8be78e43f).
- (D) format v2 LANDED aaabdb0cc + bfa77e980 (renderer extraction); local rebake done (raw GLBs symlinked from MAIN into this worktree's data/raw/meshes/*, gitignored); FULL SUITE 9053 green, both tsc clean, headless capture shows whale rendering on :5174. Agent worktree removed. PR #678 boxes all ticked. FIX ROUND COMPLETE at HEAD bfa77e980.
- NEXT per user (afk for hours, 2026-09-11 evening): land Voyager 1/2 + Mars rovers (each mesh+textures < 10 MB) as a SEPARATE feature branch/PR based on worktree-whale-petunias-mesh-bodies. Process: backlog detail files (hyperbolic-kepler-branch, surface-fixed-position-driver, rotation-table-tagged-union) → refactor-ground → spec → plan → SDD. Own ledger under .superpowers/sdd/<plan>/.

## 2026-09-11 landing run (session moved into this worktree from voyager-rovers; user: "do the last steps to land it")
- HEAD bfa77e980, tree clean, CI green, PR READY + MERGEABLE; origin/main is already an ancestor (no merge needed).
- DISPATCHED in parallel (opus, read-only): scoped REVIEW of c4de2d39b..bfa77e980 (package review-c4de2d39b..bfa77e980.diff, 16 commits, 98 files +1671/−647) → post-merge-review-report.md; DELETION AUDIT same range (legacy framing, earlier rulings fenced) → deletion-audit-c4de2d39b..bfa77e980.md.
- Handling: review findings → one fix dispatch (isolation:worktree, cherry-pick) + scoped re-review; audit safe-now → one deletion commit; refresh archived ledger copy (docs/superpowers/plans/completed/…ledger.md) in a docs commit; user rulings (host fill 0.316 vs 0.4, misfiled helpers glintBandClass/labelPickQuads/sceneBodyPickId, prep R1–R4) + eye-check attestation asked now; then squash-merge via API form (announced) → deploy from MAIN → worktree cleanup → memory.
- USER RULINGS 2026-09-12: host fill KEEP 0.316 physics (no multiplier); misfiled helpers MOVE NOW on #678; prep R1–R4 apply ALL FOUR; eye-check ATTESTED (reveal distances, host fill, v2 load, ring close range, captions over disc, bodies with Earth behind camera).
- DISPATCHED implementer (opus, isolation:worktree, base bfa77e980): brief helpers-and-prep-rulings-brief.md → 2 commits (helper re-home + allow-list rows deleted; R1–R4) → report helpers-and-prep-rulings-report.md in ITS tree. Handling: cherry-pick both, full gate here, push, remove agent worktree + worktree-agent-* branch. Live: review, deletion audit, this implementer.
- DELETION AUDIT DONE (deletion-audit-c4de2d39b..bfa77e980.md): range lean, no dead code. SAFE-NOW 3 (≈77 LOC): S1 delete apoapsisMpc.test.ts, S2 hostSkyFraction.test cases 1+3, S3 comment-budget prose (−40 src). NEEDS-RULING: R1 composeMeshMvp duplicates composeBodyMvp's Mat3→Mat4 embed → CONTROLLER RULING: DO IT (shared util, one symbol per file, ≈−24 net; the copy carries a landmine comment twice); R2 near0SelectionRingPass 69-line header → SKIP here (own comment-audit pass, not this PR); R3 occluder shape restated ×4 → SKIP (net +9); R4 sceneOccluderBodies 3×/frame un-memoised → SKIP (measure first; note only). Handling: S1–S3 + R1 ride ONE fix dispatch together with the review findings, sequenced AFTER the helpers/prep implementer lands (same tree, avoid overlap). Live: review, helpers/prep implementer.
- REVIEW DONE (post-merge-review-report.md): With fixes, 0 blocking / 2 medium / 6 low / 2 nit. RULINGS: F1 splash credit → SKIP (user-ruled drop 0a0ecdf4a; ATTRIBUTIONS/README carry CC BY — surface to user as a note); F2 occluder residency gate → APPLY; F3 comment budget → APPLY (with audit S3); F4 ratchet `<=`→equality → APPLY; F5 host-fill comment → APPLY; F6 eye inside occluder → SKIP (unreachable by construction, no code for it); F7 restatement tests → APPLY merged with audit S1 (keep far-vs-near apside case); F8 backlog twin → APPLY docs; F9/F10 → SKIP. Operational note (v2 rebake rewrote shared public/data meshes incl. #691's) is moot: #691 is on v2 and its meshes were baked v2.
- Fix brief WRITTEN post-merge-fix-brief.md (2 commits: review fixes; safe-now + R1 util). Dispatch AFTER the helpers/prep implementer lands (passFilePurity.test.ts overlap). Live: helpers/prep implementer only.
- HELPERS + PREP RULINGS LANDED: cherry-picked as 9b73e4c8a (5 helpers re-homed: glintBandClass/sceneBodyPickId/seedIndexOfBody/starPickId → utils/picking, labelPickQuads (+hasPickableLabel/isPickableLabel) → utils/labels, glintPickClasses → data/bodies; allow-list rows gone) + 818527585 (R1–R4; R2 collapsed on the scan side — filter skips both STAR rows, starPickId single route; 9062 tests green in agent tree), pushed. Agent worktree + branch removed. Task diff: 30 files +145/−196.
- DISPATCHED fix implementer (opus, isolation:worktree, base 818527585): brief post-merge-fix-brief.md → 2 commits → report post-merge-fix-report.md in ITS tree. Handling: cherry-pick, FULL gate here (npm test + npm run typecheck), push, remove agent worktree + branch, scoped re-review (sonnet) of 818527585..HEAD, refresh archived ledger copy, announce + squash-merge via API form, deploy from MAIN (agent cwd = main), cleanup, memory.
- FIXES LANDED: cherry-picked as d19595f70 (review F2/F4/F5/F8; F2 test in sceneOccluderSpheres.test.ts) + f31f79d5f (S1–S3 partial — 3 rows 1-line-over left, R1 mat4dFromMat3 util; net 22 files +102/−140), NOT yet pushed. Agent worktree + branch removed; report post-merge-fix-report.md copied here. Live: full gate (bg bash) + scoped re-review (sonnet) of bfa77e980..f31f79d5f → landing-re-review-report.md. Then: push, refresh archived ledger copy (docs commit), announce + squash-merge via API, deploy from MAIN, cleanup, memory.
- GATE GREEN at f31f79d5f (tsc both, 1305 files / 9061 tests), pushed. RE-REVIEW CLEAN (landing-re-review-report.md): 6/6 review findings + S1–S3/R1 addressed, R2 collapse behaviour-identical, 0 new breakage.
- Ledger archive refreshed (this file → docs/superpowers/plans/completed/2026-09-10-mesh-bodies-02-feature.ledger.md) in a docs commit; then squash-merge via API (announced to user), deploy from MAIN, cleanup, memory.
