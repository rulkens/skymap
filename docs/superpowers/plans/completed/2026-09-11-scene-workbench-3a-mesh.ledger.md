# SDD ledger — plan: docs/superpowers/plans/2026-09-11-scene-workbench-3a-mesh.md

Spec: docs/superpowers/specs/2026-09-11-scene-workbench-3-mesh-design.md (reachable). Execution mode: parallel implementers in per-task worktrees (.claude/worktrees/mesh-t<N>, branch mesh-t<N>), cherry-picked onto execution branches; reviewers pipelined. User rulings: max parallelism; prep as separate PRs; sidecar poses; gltf-transform both sides; placement deferred.

Branches: scene-workbench-mesh-prep (T1,T2; base origin/main 6eef3d89e) · scene-workbench-mesh (T3-6; PR #686; base = prep) · scene-workbench-bake-prep (T7,T8; base splat-crop-group 6b6320d47) · scene-workbench-mesh-bake (T9-11; base = bake-prep + T3 cherry-pick).

## Preflight scan
| pair | produces / consumes | finding |
| T1↔T5 | AssetLoader Promise<GpuAsset> / loadTexturedMesh async | consistent |
| T2↔T4 | SCENE_DRAW_ORDER 2 kinds + Record test / grows to 3, mesh before splats; T2's "splat last" test still holds | consistent |
| T3↔T4,T5,T6,T9 | TexturedMeshGeometry (packMeshGlb.ts) / upload, load, probe, bakeMesh | consistent |
| T4↔T5 | MeshGpuAsset / loadTexturedMesh return | consistent |
| T7↔T8 | both edit bakeSplats.ts | sequential (wave 1 then 2), same branch |
| T7↔T9 | geo3dLayout+publishAsset / bakeMesh | consistent |
| T3↔T9 | files cross branches | cherry-pick, dedup on rebase (plan says so) |
| T6 | packMeshGlb async makes syntheticProbeScene async; brief tells implementer to thread the promise at the call site | self-consistent, flagged |
Each task's tests-vs-code text agree; no task both creates and later moves a file. Clean — no rulings needed.

## Log
Task 1: implemented d926e4057 (mesh-t1), review dispatched 03:53
Task 1: complete (commits 6eef3d89e..d926e4057, review clean; 1 minor deferred: async-without-await loaders) — cherry-picked onto scene-workbench-mesh-prep
Task 2: implemented a1b6ee78e (mesh-t2), review dispatched; landmine noted: root vitest.config links wesl against repo-root wesl.toml, renderer imports in tests need vi.mock — carry to T4
Task 7: implemented a06e7bd5c (mesh-t7), review dispatched
Task 2: complete (a1b6ee78e, review clean; minors: order-constant lives with GPU factories (keep, brief-pinned; deletion audit may split), draw() untested → T4 adds draw-order behavioural test, attribution mismatch subsumed by squash) — cherry-picked 27ca5d74b onto scene-workbench-mesh-prep
Task 7: complete (a06e7bd5c, review clean; minors deferred to T11 deletion audit: GEO3D_DIR export unused, manifestPath hoisted in bakeSplats (T8 rewrites that area)) — cherry-picked bec182fc1 onto scene-workbench-bake-prep (worktree bake-prep)
Ruling: scene-workbench-mesh takes prep A by merge commit, not rebase — no force-push; user squash-merges so history shape is moot
Ruling: PR #686 base → scene-workbench-mesh-prep until prep A merges
Task 3: implemented 80aaa05ff (mesh-t3), review dispatched (agent a12d48cf9ca2c18e4); implementer deviations: WebIO not NodeIO (provisional accept, §7.1 browser use), REPEAT wrap per §5
Task 8: dispatched (agent ae030d1a2fdb082cf, worktree mesh-t8 off scene-workbench-bake-prep bec182fc1) — on DONE: review-package bec182fc1..HEAD, review, cherry-pick onto scene-workbench-bake-prep
Task 4: dispatched early (agent aa8ddfa6c63ffc85c, worktree mesh-t4 = scene-workbench-mesh c0b1b5598 + T3 cherry-pick 3814cb2a1); carries wesl vi.mock landmine + one draw-order behavioural test (T2 review minor). If T3 review forces fixes, T4 rebases onto the fixed T3
Prep A: scene-workbench-mesh-prep pushed (27ca5d74b); PR #686 base retargeted to it; draft PR for prep A opened below
Task 3: complete (80aaa05ff, review clean; rulings: WebIO accepted per §7.1, REPEAT wrap per §5; minors: 0-primitive message remedy, no TRIANGLES mode check, image.bytes retains GLB buffer → note for T5 review) — cherry-picked 273627706 onto scene-workbench-mesh
Toolchain: OpenMVS v2.4.0 built + installed 04:06 at ~/.local/opt/openmvs/bin/OpenMVS/ (T10 README recipe): src ~/.local/src/openMVS-2.4.0 (git tag v2.4.0), VCG clone cdcseacave/VCG at ~/.local/src/VCG, brew deps nanoflann eigen boost opencv@4 cgal jpeg-xl (brew link --overwrite jpeg-xl was needed); cmake flags -DOpenMVS_USE_CUDA=OFF -DOpenMVS_USE_PYTHON=OFF (else links miniconda libpython3.13 via @rpath and won't launch) -DOpenMVS_USE_OPENMP=ON -DOpenMVS_BUILD_VIEWER=OFF -DOpenCV_DIR=/opt/homebrew/opt/opencv@4/lib/cmake/opencv4 -DVCG_ROOT=$HOME/.local/src/VCG -DCMAKE_CXX_FLAGS="-Wno-missing-template-arg-list-after-template-kw -Wno-error=missing-template-arg-list-after-template-kw"; LIBRARY_PATH=/opt/homebrew/lib on cmake/build/install (bare -ljxl otherwise not found); build dir out/ (source's build/ holds CMake modules — never delete). COLMAP 4.2.0 brew, no GPU. Script: scratchpad/build-openmvs.sh
Task 4: implemented 9ced7b5b4 (mesh-t4), DONE_WITH_CONCERNS; fix round 1 resumed (agent aa8ddfa6c63ffc85c) — Ruling: atlas texture format rgba8unorm not rgba8unorm-srgb (spec §7.2 deviation) — swapchain is non-sRGB and lidar/splat layers write encoded colour straight through; -srgb would decode and render the mesh darker than its neighbours — cost if wrong: one-line format swap after visual attestation. Carry to T5: assetCount 'tris' = indexCount/3; image.bytes retains the GLB buffer (drop after createImageBitmap). Carry to T6/T10: no atlas-size guard vs maxTextureDimension2D (raw device error if a bake exceeds it).
Task 8: implemented d1ac62609 (mesh-t8), DONE_WITH_CONCERNS; fix round 1 resumed (agent ae030d1a2fdb082cf) — Ruling: error prefixes in the moved pose path become `scene-recon:` (brief said verbatim, but bakeSplats.test pins no prefix and bakeMesh will share the messages). Accepted: readStacItems extracted to tools/utils/skraafoto/readStacItems.ts (reusePly still needs items for sourceVintage); sourceVintage from first posed item.
Task 4: fix round 1 done c19b7b61d (mesh-t4); review dispatched (agent a2c99696e911eb6cb, package 3814cb2a1..c19b7b61d)
Task 8: fix round 1 done a5bb63ad6 (mesh-t8); review dispatched (agent a5834393984212be3, package bec182fc1..a5bb63ad6)
Task 5: dispatched early (agent adae6dfbbb2fdc13b, worktree mesh-t5 off mesh-t4 c19b7b61d)
Task 9: worktree mesh-t9 = bake-prep a5bb63ad6 + T3 cherry-pick 2174be166; WAITS for T5 (bakeMesh returns TexturedMeshAsset, a T5 type) — cherry-pick T5's commit onto mesh-t9 before dispatch. Ruling: bake branch carries T3 and T5 by cherry-pick (plan named only T3); the duplicates squash away when both PRs merge to main
Task 4: complete (9ced7b5b4 + c19b7b61d, review clean; parked for fix wave: renderResources.ts comment budget 25/43 (compress the rgba8unorm docblock to ~3 lines), buffer debug labels all say indexCount) — cherry-picked d65a3cc67 + b848cbfe8 onto scene-workbench-mesh
Task 8: complete (d1ac62609 + a5bb63ad6, review clean; parked for fix wave: bakeSplats reusePly arm loses the "no STAC items" hint on an emptied harvest (TypeError instead), manifestPath hoist, harvestDir unused by bakeSplats) — cherry-picked 7b39e7890 + 796e2a44f onto scene-workbench-bake-prep; draft PR #688 (base splat-crop-group)
Task 5: implemented 39c9c41d9 (mesh-t5), cherry-picked e315426c7 onto scene-workbench-mesh ahead of review; review dispatched (agent a7cfdd4f0762b4311, package c19b7b61d..39c9c41d9)
Ruling: mesh-t9 (T9) = bake-prep a5bb63ad6 + merge of scene-workbench-mesh e315426c7 (f77670214) — T9 needs T5's TexturedMeshAsset and the exhaustive kind tables, so a cherry-pick chain would drag T1–T5; stacked merge instead, PR diff shrinks as #685/#687/#686 merge
Task 9: dispatched (agent a0e41bca0d90abc25, worktree mesh-t9 f77670214)
Task 6: dispatched (agent ab9c3b350869494eb, worktree mesh-t6 off scene-workbench-mesh e315426c7; probe on a free port, :5600 untouched)
Fix wave (after T6): one dispatch for the parked minors above + T3's 0-primitive message remedy; then final whole-branch reviews per PR
Task 5: complete (39c9c41d9, review clean; parked for fix wave: SceneAsset.d.ts header says "plan 4 adds CameraPoseSetAsset" → plan 3b; ImageBitmap never .close()d in loadTexturedMesh; loadTexturedMesh 7/13 comment lines)
Task 6: implemented 8e72617b7 (mesh-t6), probe PASS on Metal (3 assets ready, 0 errors, ephemeral port — :5600 untouched); cherry-picked onto scene-workbench-mesh ahead of review; review dispatched (agent below, package e315426c7..8e72617b7). Visual attestation of the probe box (flipY) folds into T10's user session.
Fix wave A (feature branch) dispatched: worktree mesh-fix off scene-workbench-mesh — T3 0-primitive message, T4 renderResources comment budget + buffer labels, T5 header plan-3b + bitmap.close() + comment trim
Task 6: complete (8e72617b7, review clean, PNG bytes verified by CRC recomputation; nit MESH_CENTRE_M[k]! destructure — waived)
Fix wave A: 9f0ab27e4 (mesh-fix) cherry-picked onto scene-workbench-mesh; scoped re-review folded into the feature-branch final review (small cosmetic diff)
Task 9: implemented ff937ad9b (mesh-t9), DONE; review dispatched (package f77670214..ff937ad9b). Findings to carry: RefineMesh output stem is scene_dense_mesh_refine_texture.glb (brief assumed unchanged stem); DensifyPointCloud -h exits 1 (probe gates on banner); sparse-in/ cleared before writeColmapModel
Feature branch final whole-branch review dispatched (package scene-workbench-mesh-prep..HEAD)
Feature branch final review: CLEAN (agent ae578f27eae82f293). Minors parked for T11 audit: readMeshGlb 0-primitive message interpolates a constant 0; syntheticProbeScene artifactBlobUrl ignores byteOffset (pre-existing, new Blob([view]) would delete the cast); TRIANGLES-mode guard waived (OpenMVS SaveGLTF writes triangles; readMeshGlb is on the feature branch). PR #686 stays draft until the user's visual attestation (DoD) — code-complete.
Task 9: review NEEDS FIXES (High: sparse/ rm'd and never recreated, point_triangulator refuses; Low: colmapVersion '' on empty stdout; Low: reuse-glb items[0]! on empty harvest; nit: resize branch untested) — fix round 1 resumed (agent a0e41bca0d90abc25)
T10 preflight: crop group has lidar points.bin + splats.bin published, 61 harvested frames (jpg+json), colmap-soendermarken-crop/ dir from the splat bake; #685 still OPEN at 6b6320d47. Ruling: first bake runs from mesh-t9 after fix round 1 lands (reviewed code), in the background, log in scratchpad; publishAsset adds a 'mesh' row to the crop manifest that main's viewer can't load (ASSET_LOADERS has no row) — per-asset failure only, reversible by editing manifest.json.
Task 9: complete (ff937ad9b + 99e76f288, review + scoped re-review clean; nits parked for T11: "no STAC items" hint duplicated in bakeMesh + groupPhotoPoses; colmap probe wording for a missing binary) — branch scene-workbench-mesh-bake = mesh-t9 HEAD 99e76f288, pushed, draft PR below (base scene-workbench-bake-prep)
Task 10 (operator): first crop bake STARTED 04:45 from mesh-t9 (`OPENMVS_BIN=~/.local/opt/openmvs/bin/OpenMVS npm run bake-mesh -- --group soendermarken-crop`), log scratchpad/bake-mesh-crop.log; feature_extractor 61 frames in 0.42 min; workdir data/raw/skraafoto/skraafotos2025/soendermarken-crop/mvs-soendermarken-crop/
Task 10: FIRST BAKE FAILED at point_triangulator 04:47 — two COLMAP 4.2 issues found by hand in the workdir (mvs-soendermarken-crop/, extra dirs sparse-in-fixed/, sparse-test{,2,3}/, sfm-test/, database-test.db are my scratch — safe to delete):
  (1) FIXED-BY-EXPERIMENT: legacy text model's image/camera ids ≠ the threaded feature_extractor's db ids → "Check failed: existing_frame.RigId() == frame.RigId()" abort. Rewriting images.txt/cameras.txt ids from database.db `images` (by name) makes the triangulator run. Fix belongs in bakeMesh: after feature_extractor, read ids from database.db (sqlite3 CLI) and write the model with them (writeColmapModel needs an id map), or extract single-threaded with name-sorted poses.
  (2) OPEN: with ids synced (and even with the model intrinsics copied into the db cameras) point_triangulator triangulates 0 points from 820 verified pairs / 156k inliers, at any threshold → pose/intrinsics convention suspect. Audit agent aed0a441246d543aa (read-only) dispatched on writeColmapModel/photoPoseFromStacItem conventions; result pending. A from-scratch `colmap mapper` sanity run (sfm-test/) exited 1 — log scratchpad/sfm-test.log, not yet read.
  Also noted: harvest JPEGs are 4-component (CMYK-style) JPEGs — `sips` refuses them; COLMAP read them; OpenMVS/OpenCV untested.
USER: "pause" at 04:55 — no further dispatches; resume = read the audit agent's result + sfm-test.log, then decide the bakeMesh fix (id sync + whatever the audit finds), fix round on the bake branch, re-run the bake.
Task 10 diagnosis 06:20 (hand experiments, scratch under mvs-soendermarken-crop/: sparse-in-fixed, sparse-in-centred, sparse-test{,2,3,4,5}, sfm-test{,-orig}, sparse-seed, dense-seed, seed*.mvs/ply, database-test.db, scratchpad tri-check.py + lidar-seed.py):
  - Poses + intrinsics we write are CORRECT: numpy triangulation of COLMAP's verified matches under the written world→camera convention gives 0.15 px median reprojection, 100% in front (alternatives 20+ px). Spec §10 "GLB axis / convention" worry is closed for the model side.
  - ROOT CAUSE of 0 points: COLMAP's Camera::HasBogusParams rejects a principal point outside [0,w]×[0,h]; crops carry cx/cy thousands of px outside (e.g. -3000,-2938) → IncrementalTriangulator silently skips every image. Confirmed: centring the principal point (wrong values) triangulates 298 points. No Mapper option disables it.
  - id mismatch (rigs check) is a second, real bug but moot if COLMAP matching is dropped.
  - RULING (pending user): replace COLMAP feature_extractor/exhaustive_matcher/point_triangulator with a LiDAR-SEEDED sparse model: writeColmapModel projects the points.bin sample into each camera (z>0, inside crop) and writes POINTS2D + TRACKs; then colmap image_undistorter (format/layout only) → InterfaceCOLMAP → Densify → ReconstructMesh → TextureMesh. Fewer stages, no SIFT, no db. Prototype (lidar-seed.py): 17,001 pts, every point visible in every crop.
  - InterfaceCOLMAP `--image-folder` is joined onto `-i`: brief's `dense/images` → `dense/dense/images` (failed reload). Correct argv: `--image-folder images`.
  - JPEG "components 4" from `file` is a red herring: gdalinfo shows 3 RGB bands.
  - OpenMVS chain on the seed model running in background (log scratchpad/mvs-seed2.log) → if it yields seed_dense_mesh_texture.glb, that is the recipe for the bakeMesh rewrite.
Task 10 findings 06:41 — hand chain SUCCEEDS on the LiDAR-seeded model: undistorter → InterfaceCOLMAP(--image-folder images) → Densify L1 (~11 min) → ReconstructMesh (321,191 v / 642,304 f, .ply only) → TextureMesh --mesh-file (glb + sidecar _0.png, ~90 s). GLB positions are the group frame unrotated (Z up, bbox ≈ LiDAR) → §10 axis question CLOSED, no conversion. 19/61 crops are CMYK JPEGs (gdal_translate on 4-band nadir COGs) → OpenMVS refuses; sharp transcode of staged copies works.
Ruling: bakeMesh rewritten to the seeded pipeline (spec §6.2 deviation, docs updated same PR): no feature_extractor/matcher/triangulator; writeColmapModel gains opt-in `observations`; staged JPEGs transcoded to sRGB; re-pack via NodeIO (sidecar URI). Brief: task-9b-brief.md. Dispatched to a FRESH implementer (original T9 agent at ~170k tokens).
Adjacent finding for the user (ask, don't backlog): tools/fetch/fetchSkraafoto.ts should pass `-b 1 -b 2 -b 3` to gdal_translate so nadir crops are RGB JPEGs; existing harvest keeps 19 CMYK files (bake-side transcode covers them).
Task 9b: implemented 25c29ba13 (mesh-t9, fresh agent a107e8ae29e5e32f7), DONE; review dispatched (package 99e76f288..25c29ba13); real bake #2 STARTED 07:10 (log scratchpad/bake-mesh-crop-2.log). Note: 9b edits readMeshGlb.ts (feature-branch file) to share the accessor walk via pack/meshGlbGeometry.ts — stacked change, feature branch must not touch readMeshGlb again before merge order settles. Implementer concern: pointSampleTarget 200k → images.txt ~150 MB for the full group (crop is stride 1) — revisit if the full group is ever baked.
Task 9b: review APPROVED (Medium: plan still mentions sqlite sync; spec P5 paragraph contradicts §6.2; Low: "verified in first bake" overstated, writeColmapModel header 12 lines, O(points×poses) heap note, nit stale rotation clause). Bake #2 (07:00) ABORTED: DensifyPointCloud reused stale depth####.dmap caches in the workdir from my hand run (same dir, different image order) → SIGABRT at 44% of the geometric pass. Hypothesis test: dmaps moved to stale-dmaps/, densify rerun by hand (log scratchpad/densify-probe.log). Fix round 1 resumed on agent a107e8ae29e5e32f7: clear *.dmap before Densify (+ --remove-dmaps if available) + the doc fixes.
Task 9b: fix round 1 done fc9e59387 (clears .dmap + --remove-dmaps 1, doc fixes); stale-cache hypothesis confirmed (densify took real minutes once caches moved to stale-dmaps/; probe killed to free the workdir). Bake #3 STARTED 07:25 (log scratchpad/bake-mesh-crop-3.log); scoped re-review of fc9e59387 dispatched.
Task 9b: complete (25c29ba13 + fc9e59387, review + re-review clean; nits parked for T11: rm of .dmap lacks force:true; spec §5 "before any bake completed" temporal marker) — scene-workbench-mesh-bake → fc9e59387, pushed (PR #689)
Task 10: BAKE #3 COMPLETED 07:32 (20.0 min; 1,329,368 dense pts; 323,251 v / 646,423 f; mesh.glb 22 MB published into crop manifest as 'mesh'). Workbench from mesh-t9 on :5610 (`npx vite --config tools/scene-workbench/vite.config.ts --port 5610 --strictPort`, background) loads all three assets 'ready'; headless shots scratchpad/shot-{all,mesh-only,lidar-only}.png (script .superpowers/shot.mts in mesh-t9). Geometry silhouette matches the LiDAR; TEXTURE WRONG (saturated blobs). Diagnosis: the 4-component JPEGs are raw R,G,B,+1 bands mislabelled CMYK; sharp's CMYK→sRGB gives muddy frames; GDAL `--config GDAL_JPEG_TO_RGB NO -b 1 -b 2 -b 3` gives correct RGB (verified side by side). Neon atlas ≈ OpenMVS seam levelling overflow between muddy nadir and correct oblique frames (hypothesis; re-texture with fixed frames running by hand → probe-fixed.glb). Fix round 2 resumed on agent a107e8ae29e5e32f7: gdal_translate raw-band transcode in bakeMesh + fetchSkraafoto `-b 1 -b 2 -b 3` (adjacent fix, riding the bake PR — flag to user). UV range in served GLB is sane (v ≤ 0.33 = atlas top third, glTF top-left origin, no flip needed).
Task 9b: fix round 2 done 27d98d5ed (gdal_translate raw-band transcode via injected runGdal; fetchSkraafoto -b 1 -b 2 -b 3); scoped re-review dispatched (package fc9e59387..27d98d5ed).
Task 10 texture diagnosis 07:45: correct band decode alone did NOT fix the neon atlas. Native-res crop shows every patch interior clipped to an RGB-cube corner with intact photo margins = OpenMVS seam levelling blow-up. Hand re-texture with --global-seam-leveling 0 --local-seam-leveling 0 → clean photo atlas (probe-noseam_0.png). Probe with global 0 / local 1 running (probe-localseam) to decide the pinned flags. NEXT: fix round 3 = pin the TextureMesh seam flags (spec §6.2), then bake #4 (`--reuse-glb` cannot be used: the texture stage must rerun; full bake ~20 min), then shots for the user.
Task 9b: fix round 2 re-review CLEAN (nits folded into fix round 3: log wording, spec changelog sentence, plan test bullet). Fix round 3 resumed: pin `--global-seam-leveling 0 --local-seam-leveling 0` (local-only also clips — verified probe-localseam).
Task 10 08:00: hand-textured no-seam GLB re-packed via `--reuse-glb` → mesh.glb 19.7 MB; workbench shows photo-real mesh aligned with LiDAR+splats (shot-all.png / shot-mesh-only.png); small orange patches = OpenMVS empty-color for faces no view covers. Bake #4 (full, pinned flags) still required as proof after fix round 3 lands. scene_dense_texture.neon.glb.bak = bake #3's atlas kept for reference (scratch).
Task 9b: fix round 3 done e088aa07a (seam-levelling flags pinned); scoped re-review dispatched (package 27d98d5ed..e088aa07a). Bake #4 STARTED 08:05 (log scratchpad/bake-mesh-crop-4.log) = end-to-end proof of the pinned pipeline. After it: shots → branch push → T10 README (toolchain + bake record, delegate) → T11 audits.
Task 9b: fix round 3 re-review: one stale clause (bakeMesh.ts GDAL_RGB_ARGV header still blames seam levelling) — Ruling: parked into T11 comment audit (one-line trim), no round 4.
Task 10: BAKE #4 COMPLETED 08:20 with the final pipeline — 23.3 min, 1,321,360 dense pts, 323,985 v / 647,898 f, mesh.glb 19.9 MB published; workbench shot (shot-all.png) photo-real, aligned. README committed eb85015a5 (bake #3 numbers; bake #4 = 23.3 min / 647,898 tris — README says 20.0 / 646,423: acceptable, both real; T11 may swap). Bake branch → eb85015a5 pushed (PR #689). REMAINING for T10: user's own visual attestation (server: mesh-t9 on :5610 still running). Next: bake-branch final whole-branch review (package 796e2a44f..eb85015a5 = bake-prep..HEAD, includes the feature merge — reviewer to focus on tools/scene-recon + fetch + docs), then T11 (deletion audit, comment audit incl. parked nits, /feature-done).
T11 started 08:30: bake-branch final review dispatched (package f77670214..eb85015a5); deletion audit (GREENFIELD framing, all four diffs) dispatched → report deletion-audit-3a.md. /feature-done waits on the user's visual attestation (DoD) — everything else proceeds.
T11 08:45: bake final review = code sound, Important 1-6 all docs (README bake record is bake #3's; spec §6.1 → point at README, drop use_gpu clause; §9 test bullet; §7.1/§5 readMeshGlb credits; §10 #3/#4 settle; data/raw/skraafoto/README gdal line + four-band note) + Minor (stale seam clause, header restatement, spec signature lacks runGdal, test comments cite bake #2/#3 + "CMYK") + dead surface (meshWorkDir, runner types exported; atlas-resize branch unreachable). Deletion audit (deletion-audit-3a.md): net ~10 src/~23 tests high-confidence. Triage: APPLY — atlas resize branch delete; assetCount.test.ts delete (LayerList covers the row); unexport meshWorkDir + runner types; all doc items. PARK (user rulings): `Stage.clears` knob → one clear before loop; `harvestDir` return field unused until 3b; packMeshGlb sampler filters (spec §5 row); sceneRenderers "splats last" case (prep A PR already open, 3b rewrites); GEO3D_DIR export (prep B, zero-LOC). Fix dispatches: bake (mesh-t9) + feature (mesh-fix reset to 6c44fae88; must NOT touch readMeshGlb.ts — bake branch rewrote it; the 0-primitive message fix goes to meshGlbGeometry.ts on the bake side).
T11 09:05: fix wave B (bake) af26714ba + fix wave C (feature) d33f10a7e → 22b59f7b2 on scene-workbench-mesh (pushed); GPU probe PASS on the feature branch after wave C. Feature merged into mesh-t9 (eaea6f641; readMeshGlb conflict resolved to the bake-side wrapper — feature's fix-wave-A message edit is superseded by meshGlbGeometry.ts). Bake branch → eaea6f641 pushed (PR #689). Targeted tests 32 files/111 green, typecheck clean. Full suite running (scratchpad/full-test.log). Remaining DoD: full suite green; USER visual attestation (workbench on :5610 from mesh-t9, or `npm run scene-workbench` from any checkout of scene-workbench-mesh-bake); then /feature-done (relocate spec+plan, backlog sweep, ledger archive) — spec/plan live on the feature branch; bake branch carries their §6 rewrite. Parked for user: Stage.clears knob, harvestDir field, packMeshGlb sampler filters, sceneRenderers "splats last" case, GEO3D_DIR export; fetcher -b fix rode the bake PR (adjacent, flag).
DoD 09:12: full suite on merged bake worktree — see full-test.log summary (below in chat); typecheck clean; probe PASS. Only the user's visual attestation + /feature-done remain. Memory updated (project_scene_workbench.md + MEMORY.md index).
USER 09:20: wants a wireframe toggle to inspect mesh quality → ruled overlay on texture, lands on #686 (feature). Noticed orange tris on tree undersides = OpenMVS --empty-color default (faces no frame sees); proposed pinning dark grey in bakeMesh (awaiting answer). Task 12 (wireframe) dispatched to worktree mesh-fix (reset to scene-workbench-mesh 22b59f7b2).

## Task 12 (post-plan, user request): mesh wireframe overlay toggle
- Base 22b59f7b2 in worktree mesh-fix (branch mesh-fix). Implementer DONE: bc3367d3d (16 files, +213/−21); vitest scene-workbench 27 pass, typecheck clean, probe PASS 7/7 incl `mesh:wireframe`.
- Deviation to rule on: knob rides `SceneRenderers.draw(..., display: SceneDisplay)` (new `@types/SceneDisplay.d.ts`), not the splat-knob (camera uniform) route.
- Review dispatched (opus) → task-12-review.md. On APPROVED: cherry-pick bc3367d3d onto scene-workbench-mesh (#686), push, probe; merge feature into scene-workbench-mesh-bake (mesh-t9), push; screenshot on :5610 with Wireframe on; board v11.
- Skråfoto seasons STAC-verified: 2019-06-23 (265 frames) and Aug 2021 (92, 0.075 m) are LEAF-ON over the crop; prior "all leaf-off" claim was wrong. Awaiting user's pick for a leaf-on bake (recommend 2019).
- Orange empty-colour faces: proposal `--empty-color 4210752` awaiting user ruling (probe-grey.glb preview in scratch).
- Task 12 review APPROVED (task-12-review.md: 0 blocking, 3 minor, 3 nit; rulings: SceneDisplay seam stays, edge buffer as-is). Fix round 1 e1223b9f1. Feature branch scene-workbench-mesh ff'd to e1223b9f1 and PUSHED (#686).
- Bake side: `git merge scene-workbench-mesh` in mesh-t9 conflicted with the clip-box code (6 files, DisplayPanel/viewSlice/probe/README/test/css); resolver agent dispatched (keep both, tests+typecheck+probe, commit, no push). On its report: push mesh-t9 → scene-workbench-mesh-bake, screenshot on :5610 with Wireframe on, board v11.
- Task 12: complete. Bake merge a0b10b719 (clip box + wireframe both kept, probe PASS 8/8) PUSHED to scene-workbench-mesh-bake (#689). Wireframe attested in headless screenshot (scratchpad/shot-wire.png, cyan edges over crop mesh). Board v11 published. Open user rulings: #13 orange empty colour, #14 leaf-on collection (2019 vs 2021).
- User rulings applied: (#13) `--empty-color 4210752` pinned in bakeMesh + test + spec/plan argv, commit 7dc6451d4; crop mesh repacked from probe-grey via --reuse-glb (0 orange px; bake #4 snapshot at <workdir>/mesh.bake4.glb). (#14) new group `soendermarken-crop-2019` (skraafotos2019, 2019-06-23): bake-lidar + harvest 106 frames (3-band JPEGs) + bake-mesh 16.5 min → 2,186,705 dense pts, 924,720 tris, mesh.glb 29.6 MB; commit 2f864cc3d (group file, registration, README bake record, skraafoto README). Both pushed to scene-workbench-mesh-bake (#689). Harvest lives in MAIN checkout data/raw/skraafoto/skraafotos2019 (symlinked from mesh-t9); mesh-t9 data/raw/dhm has two tile symlinks.
- Open: lawn "overlapping islands" (textureless PatchMatch sheets) — proposed ReconstructMesh-only probe (smooth/remove-spurious/min-point-distance) on the existing dense cloud; awaiting user's go. 2019 obliques carry principal_point_offset [0,6.75] mm — first live exercise of that path, coherent.
- User "go" 2026-09-11 ~13:00: (a) camera-framing agent in mesh-t9 — manifest `boundsM?` written by bake-lidar, `frameCamera` reducer + saga put, re-run bake-lidar for all 3 groups, commit on mesh-t9 (then I push to #689). (b) lawn re-mesh probe agent — variants probe-remesh-smooth (ReconstructMesh flags) and probe-remesh-filtered (re-densify with filtering) in the crop mvs work dir, vertical-ray sheet metric, published as extra assets `mesh-probe-smooth` / `mesh-probe-filtered` in the crop group's manifest for visual A/B (remove later via manifest R-M-W). No code commits from (b).
- Camera framing landed: 7c5749973 pushed to #689 (park boundsM z noise-dominated → target ~200 m up; follow-up = percentile z, unasked). Lawn probe result: NOT stacked sheets (99.9% lawn rays 1 hit); crumpled single sheet (ground |dz| p90 6 cm). B (--number-views-fuse 4; note --filter-point-cloud only fires on negative values, exclusive mode) = no effect. A (--smooth 5 --remove-spurious 40 --min-point-distance 5) best lawn, −28% canopy. A2 (--smooth 5 --remove-spurious 40) halves roughness, −16% canopy → RECOMMENDED. Probe layers published in crop manifest: mesh-probe-smooth / -filtered / -smoothonly (+44 MB, delete via manifest R-M-W when done). Awaiting user: adopt A2 in bakeMesh + re-mesh both crops; keep/drop probe layers.
- Landed + pushed on #689: c353cef60 crop group renamed "Søndermarken (crop, 2025)" (lidar/mesh/splats republished with new labels); bde04cb92 merge of #686's 53f4a9430 (layer-row title tooltip). 2019 probe layers published: mesh-probe-smoothonly (24.4% >5 cm, was 33.4%) / mesh-probe-smooth (4.9%, decimated 173k tris). Probe rows in both crop manifests are hand-labelled; delete via manifest R-M-W when the user is done. Open rulings: adopt A2 (or A / point-distance 3) as bake default + re-mesh; drop probe layers; park boundsM z noise.
- User go (2026-09-11 ~17:30): (1) 2019 group re-harvest at 100 mm/px + default re-bake — agent running in mesh-t9 (old harvest mv'd to soendermarken-crop-2019.200mm, old mesh kept as asset `mesh-200mm`); (2) AFTER (1) lands: snapshot its mesh as an extra asset, then `bake-mesh --group soendermarken-crop-2019 --full-res --refine` (hours, background); (3) open/non-manifold edge colouring in the wireframe — agent running in scene-workbench-mesh (#686), then merge into mesh-t9 + push. Resolution finding: harvest 200 mm × half-res densify = ~40 cm depth, ~20 cm texels; that is the Google-gap explanation.
- Landed + pushed: fbd18fd3b open-edge wireframe colours (#686) → merge 2c03652e9 on bake; 2d11e3559 2019 group at 100 mm/px (harvest 106 frames 54 MB; bake 34.5 min: 3,412,071 dense, 2,118,416 tris, 8192 atlas = MAX_TEXTURE_PX ceiling, mesh.glb 66 MB). Old 200 mm harvest at soendermarken-crop-2019.200mm/; old mesh as asset `mesh-200mm`. Step 2 agent dispatched: snapshot half-res (`mesh-halfres` asset + .halfres files), then `--full-res --refine` (hours; if atlas splits: manual TextureMesh 16384 + --reuse-glb, user decides).
- Step 2 done + pushed (842a09b54 README record): --full-res --refine 142.9 min; densify 11.9 M pts (1h43), ReconstructMesh 4,098,829 tris, RefineMesh (level 1, hardcoded) DECIMATES to 660,051 tris, atlas 8192 (no split), mesh.glb 26.8 MB. 2019 manifest rows: mesh (refined 660k) / mesh-halfres (2.1 M) / mesh-200mm / two probes. Half-res workdir artefacts kept as .halfres siblings. Open: user's eye on refined vs half-res; lever = --full-res without --refine (~4.1 M tris) or RefineMesh level 0.
- USER RULING 2026-09-12: full-res + refine ("skråfoto MVS mesh", 660k tris) is definitively the best 2019 variant. Supersedes the A2/A smoothing question (refine wins on both lawn and canopy). Follow-through proposed, awaiting go: (a) make `--full-res --refine` the bake's only path (delete both flags); (b) re-bake 2025 crop the same way (~2.5 h); (c) drop mesh-probe-* / mesh-200mm / mesh-halfres manifest rows (files stay on disk, standing never-delete rule).
- 2026-09-12 01:05 WORKTREE CONSOLIDATION (user ask): removed mesh-t1..t8, mesh-prep, mesh-fix, bake-prep, scene-workbench-mesh (all content verified landed in scene-workbench-mesh-bake by ancestry/patch-id); local branches mesh-t1..t8 + mesh-fix deleted; PR branches scene-workbench-mesh-prep/-mesh/-bake-prep kept (no worktree). SDD workspace now lives HERE (mesh-t9/.superpowers/sdd/...). mesh-t9 (branch mesh-t9 → scene-workbench-mesh-bake #689, server :5610) is the single worktree going forward. The 17 agent-* worktrees belong to layer-composition / whale-petunias — untouched.
- LIDAR BLACK root cause: framing re-bake (7c5749973) ran bake-lidar from mesh-t9 which lacked data/raw/geodanmark/soendermarken-z19-jpg → VRT sources missing → GDAL zeros → all 3 points.bin have 0 coloured points. Ortho tree now symlinked into mesh-t9. OPEN (awaiting go): re-run bake-lidar ×3; add missing-ortho-tree guard in bakeLidar (failing test first).
- LIDAR REPAIR DONE 2026-09-12 01:45: bake-lidar re-run ×3 from mesh-t9 with the ortho symlink — crop 2.3 s, crop-2019 1.1 s, park 156.8 s; all points coloured (17,001 / 17,001 / 1,615,969). boundsM intact in all 3 manifests. Guard commit d1ea85870 (bakeLidar throws when the z19 tree is missing; no unit test — same untestable-without-disk shape as the DHM guard) pushed to #689.
- FOLLOW-UPS user go 2026-09-12 ~01:50: (3) manifests R-M-W done — crop-2025 now lidar/splats/mesh, crop-2019 lidar/mesh; probe/variant GLBs stay on disk under assets/. Old 2025 harvest mv'd to data/raw/skraafoto/skraafotos2025/soendermarken-crop.200mm/ (124 files incl. mvs-soendermarken-crop work dir with mesh.bake4.glb). (1)+(C) agent (opus) in mesh-t9: delete --full-res/--refine (always level 0 + RefineMesh) + crop groundMmPerPx 200→100, two commits, report task-13-report.md. ON REPORT: push, then `npm run fetch-skraafoto -- --group soendermarken-crop` (61 frames → main via symlink) then `npm run bake-mesh -- --group soendermarken-crop` in background (~2.5 h) → verify coloured/tris/atlas, README bake record, commit, push. Then /feature-done gate = user attestation.
- 12:40 agent commits 096d5124b (flags deleted, always level 0 + RefineMesh; frameWindow nadir test re-pinned: scale 0.959 not 1 — native = ground diag / projected diag ≈ 96 mm) + f16a3b30c (crop 100 mm) PUSHED to #689. Harvest 61/61 at 100 mm → main skraafotos2025/soendermarken-crop. data/raw/skraafoto/README.md table re-measured for 100 mm (outsize 1947×2647 / 2744×1807, ~96% of native) — UNCOMMITTED, rides the bake-record commit. Bake running (first attempt died: OPENMVS_BIN unset in bg shell; relaunched with it) → log .superpowers/bake-mesh-crop-2025-100mm.log, densify ETA ~30 min + refine + texture. ON DONE: verify tris/atlas/coloured, README bake record for crop 2025 (replaces the 200 mm record?), commit both docs, push; then user attestation → /feature-done.
- 13:36 2025 crop bake DONE on the new default path: 54.6 min — densify 48m0s 7,749,874 pts, ReconstructMesh 2,997,611 faces, RefineMesh → 482,384 faces, 4096 px atlas, mesh.glb 17.8 MB; manifest `mesh` row replaced (lidar/splats/mesh). README bake record rewritten to the two current bakes (variant rows gone), skraafoto README table at 100 mm; commit pushed to #689. ALL THREE FOLLOW-UPS DONE. Remaining DoD: user visual attestation on :5610 (both crops, refined meshes) → /feature-done (spec+plan relocate, backlog sweep, ledger archive, deletion audit already at deletion-audit-3a.md), parked leanness rulings.
- USER ATTESTATION 2026-09-12: both crops PASS on :5610. 2019 leaf-on judged "a lot better" — the one to carry into the main app later. DoD complete. Leanness rulings not explicitly given; applying the recommended set (Stage.clears → one clear; drop "splats last" case; keep harvestDir / sampler filters / GEO3D_DIR) then /feature-done.
- /feature-done 2026-09-12 in progress: baseline typecheck 0 / tests 1237 files 8274 pass (dod-baseline.log); leanness 6dfb4e683 (Stage.clears hoisted, draw-order case dropped); wave-2 deletion audit → deletion-audit-3a-wave2.md (−36 src/−3 tests/−1 doc, no rulings needed) — apply agent (sonnet) running on mesh-t9. #687 prep A SQUASH-MERGED 24fe12d8c (user: "merge what you can"); #686 conflicts with main (package.json/lock from #693, add/add on prep-A files) → merge agent (opus) merging origin/main into scene-workbench-mesh in temp worktree mesh-feature, retargets base to main; ON REPORT: squash-merge #686. Then completion commit on mesh-t9 (plan+spec → completed/, ledger archive, tick 42 boxes), push #689. #688/#689 stay behind #684/#685 (splat clip box effort).
