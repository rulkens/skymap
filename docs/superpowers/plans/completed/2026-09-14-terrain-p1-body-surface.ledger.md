# SDD ledger — plan: docs/superpowers/plans/2026-09-14-terrain-p1-body-surface.md

Execution branch: `terrain-p1-body-surface` on origin (created 2026-09-14 from main 35463c237). Controller session is pinned to worktree `per-planet-terrain` (spec branch); implementers run in `isolation: "worktree"` agents that reset to `origin/terrain-p1-body-surface`, commit, rebase, push. Reports live in the scratchpad `sdd/p1/` dir; briefs + review packages here. No harness task-list tool in this session — this file IS the task list.

## Task list (waves)

- Wave A: Task 1 + Task 2 (ONE dispatch)
- Wave B: Task 3 ∥ Task 4 ∥ Task 5a (parallel, disjoint files)
- Wave C: Task 5b
- Wave D: Task 6 (sweep, closes the red window)
- Wave E: Task 7 (visual — USER attests)
- Wave F: Task 8 (docs)

## Preflight scan

| pair / task | produces → consumes                                                                                     | finding                                                                                                                                                          |
| ----------- | ------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1 → T2     | `BodySurface`, `outerBoundRadiusM`, `innerBoundRadiusM` → arms/makers                                   | consistent; T1 has no consumer until T2                                                                                                                          |
| T3 ∥ T4     | both edit `meshBodiesPass.ts` (:102 outer vs :86 inner)                                                 | hunks 16 lines apart; parallel OK, rebase-safe                                                                                                                   |
| T3 ∥ T5a    | disjoint (T3: footprint hub, textureLoadRadius, atmosphereDrawList, meshBodiesPass, 2 clips)            | OK                                                                                                                                                               |
| T4 ∥ T5a    | disjoint (T4: atmosphereParams, sceneOccluderBodies, meshBodiesPass:86)                                 | OK                                                                                                                                                               |
| T5a → T5b   | 5a edits `poseFrameConversion.ts:163`, `replayInput.ts:133`; 5b threads standoff through the same sites | serial, 5b after 5a                                                                                                                                              |
| T2 → T6     | T2 opens red window; T6 closes it over test fixtures                                                    | T6 last before T7                                                                                                                                                |
| T5b self    | test `descent floor at Sgr A* matches pivotFraming's zoom floor`                                        | CHECK: `pivotFraming` may clamp with `MIN_DISTANCE_MPC`; if that clamp is above 2 r_s the equality is false by construction — implementer must report, not fudge |
| T1 self     | two one-line helpers each with one hand-computed asymmetric-tuple test                                  | rubric may call trivial; plan argues swapped-index bug; accept                                                                                                   |
| T5b self    | `bodyStandoffRadii falls back to the shared constant`                                                   | borderline constant restatement; plan argues the `in`+`typeof` widening; accept                                                                                  |

Ruling: T1+T2 batched into one dispatch — T1 is three tiny files whose only consumer is T2; a separate review seat buys nothing. Cost if wrong: one fix loop spans both.
Ruling: no harness task list exists in this session; ledger waves stand in. Cost if wrong: user has no live board.

## Dispatch log

- 2026-09-14 Wave A dispatched: Task 1+2 implementer (opus, isolated worktree, agent a067c9e6f34ef658b), BASE 35463c237, report → scratchpad/sdd/p1/task-1-2-report.md
- Task 1+2 implementer DONE: commits 43fd19d53 (T1), 7aadd3564 (T2); red-window count 177. Concerns carried: `tests/@types/scene/{EarthBody,PlanetBody}.test.ts` are runtime type tests → Ruling for T6: DELETE them (testing.md forbids runtime type tests). Cost if wrong: none observable. `RingSpec.d.ts:21` one-word comment fix out of list — accepted.
- Review T1+2 dispatched (sonnet, agent af0c9294ac954fe10) on review-35463c237..7aadd3564.diff. Draft PR #704 opened.
- Wave B dispatched off 7aadd3564 (all opus, isolated): T3 agent ac5309315f9e07d03 (report task-3-report.md), T4 agent ad0a0b1af762ecd60 (task-4-report.md), T5a agent af2802b833aaf2dc7 (task-5a-report.md).
- Task 1+2: complete (commits 35463c2..7aadd35, review clean; ⚠️ commit-body count verified by controller: 177 recorded in 7aadd3564's body).
- T3 DONE_WITH_CONCERNS (import parked at end of meshBodiesPass import block for rebase safety; cosmetic) — commit 6013a2cc2, errors 177→167. T4 pushed fb25de081 (agent died on usage limit after pushing, report file complete). T5a agent died on usage limit with the diff saved to scratchpad/sdd/p1/task5a.patch (uncommitted in its worktree) — resumed via SendMessage.
- Reviews T3 (7aadd3564..6013a2cc2) and T4 (6013a2cc2..fb25de081) dispatched, sonnet.
- Task 3: complete (commits 7aadd356..6013a2c, review clean). minor (deferred): `meshBodiesPass.ts:34` import parked at end of block — tidy in T6 sweep; `bodyFootprintRadiusM.ts` header 9 lines vs 6 code (pre-existing ratio).
- Task 4: complete (commits 6013a2c..fb25de0, review clean). minor (deferred): `atmosphereParams.ts` import order; `sceneOccluderBodies.ts` header 13 lines (pre-existing).
- T5a DONE (resumed agent): commit 519edd15d, errors 177→139 own delta, branch at 120 post-rebase. Review dispatched (sonnet) on review-fb25de081..519edd15d.diff. Concern carried: `earthPass.ts` already exports `PreparedBodySurfaceFrame` (pre-existing purity-allowlist debt; not ours).
- Task 5a: complete (commits fb25de0..519edd1, review clean). minor (deferred): commit scope `scene` loose; pre-existing purity debt in `earthPass.ts` (`PreparedBodySurfaceFrame`) and `partitionStarsByResolution.ts` (`STAR_RESOLVE_PX`) — allow-listed, not ours.
- T5b dispatched off 519edd15d (opus, isolated; report task-5b-report.md) with the MIN_DISTANCE_MPC check instruction.
- T5b DONE_WITH_CONCERNS: commit 90510662a, errors 120→117, MIN_DISTANCE_MPC does not bind (8.2e-13 Mpc ≫ 1e-24) so the equality is exact. Concerns: (1) 6 unlisted test files/helpers got the required standoff arg (~25 sites) — Ruling: accepted, the alternative leaves non-`radiusM` errors invisible to T6's worklist; cost if wrong: reviewer catches a wrong value. (2) `pivotRadiusMpc.ts:24,52` routed to datum — CENSUS GAP (spec §3.3 hub list had pivotRadiusMpc.ts:24; the plan's DATUM table omitted it); accepted. (3) T6 handoff: `tests/.../pivotRadiusMpc.test.ts:84` `SGR_A_STAR.radiusM` fixture now fails (was NaN-vs-NaN). (4) Sgr A\* floor doubles — eye-check at T7.
- Task 5b: complete (commits 519edd1..9051066, review clean — opus; behaviour change verified end-to-end; third test `returns the body's own override` is load-bearing because the agreement test is symmetric in the helper). minor (deferred): `surfaceFloorM.test.ts:20-24` toBe exactness rests on standoff 2.0 being a power of two (comment); `surfaceZoomStep`/`settledZoomPose` now carry two adjacent bare `number` params (transposition risk; don't extend); production wiring at `replayInput.ts:135`/`resolveWorldArm` correct by inspection only; `pivotRadiusMpc.ts:24,52` must be added to the plan's DATUM census (spec-branch edit, do at T8); `makeSurfaceDriver.ts:35` hardcodes the standoff.
- T6 dispatched off 90510662a (opus, isolated; report task-6-report.md) carrying: delete the two runtime type tests, pivotRadiusMpc.test.ts:84 fixture, meshBodiesPass import tidy, optional toBe comment.
- T6 DONE: commit dcd9ea0bb, errors 117→0, tsc exit 0, full suite 1337 files / 9243 tests green. One src straggler outside the census: `frameContext.ts:188` star-sphere partition → `starSphereRangeM` descriptor, implementer ruled OUTER (slab extent rubric). Runtime type tests deleted per ruling. Concern: new per-frame `.map` allocation in frameContext (F1 may fold). Review dispatched (opus, agent a80953b91012b0924) on review-90510662a..dcd9ea0bb.diff → scratchpad/sdd/p1/task-6-review.md.
- T6 review (opus): A Approved — frameContext.ts:189 OUTER confirmed right (NEAR0 Slab.distanceRangeM painter-sort interval; body rows bracket off the same outer footprint, slabs.ts:184); no assertion changed currency (numeric-literal diff per file); @types tests deleted cleanly; tsgo 0 at HEAD. B Changes required: Important frameContext.ts:190-191 comment states a false mechanism (distanceRangeM never clips; over-reach can trip chainOverlapViolations) — rewrite; Minor visibleSlabBodies.test.ts:37,100,131 "the datum" → outer bound. Per-frame `.map`: accept as-is (0–2 spheres of ≤119 stars; reshaping the descriptor braids currencies). Ruling: accepted the reviewer's recommendation, no code change. Cost if wrong: one tiny allocation per frame.
- T6 fix round 1/5 dispatched: fresh isolated sonnet agent a7578ed0e06f4eb0f (isolation agents are never resumed), comment-only, off dcd9ea0bb → report task-6-fix1-report.md.
- T7 prep: linked main's public/data into agent-a9b875965e3379e64 (P1 at dcd9ea0bb) and started its dev server on :5181 (controller background task bpb10ekr2, log scratchpad/sdd/p1/dev-server.log). Before-picture server: :5174 (main at 35463c237).
- Ruling: T8 split — RENDERER.md + docs/ grep land on the exec branch (sonnet isolated agent a7e67507878ba000e, dispatched off dcd9ea0bb in parallel with T7 attestation and the T6 comment fix; report task-8-report.md); the spec §3.3 joint 8/9 status, the plan's DATUM census row for pivotRadiusMpc.ts:24,52, and the P6 plan's false 80 B derivation (line 210) are one docs commit on the spec branch (per-planet-terrain-spec, PR #700) after both gates. Cost if wrong: PR #704 reads "P1 landed" before #700 carries the spec status — harmless ordering.
- T7 (USER attests) requested: :5181 (P1) vs :5174 (main at branch time), four poses + Sgr A\* floor doubling.
- T6: fix round 1/5 (2 addressed, 0 open — commits dcd9ea0bb..638cd7b98, comment-only, 2 files +13/−11). Ruling: scoped re-review by controller inspection — the frameContext comment is the reviewer's own justification verbatim and the three test-comment swaps are word-level; the review's "line 131" was a citation slip (content at 171–172). Cost if wrong: a wrong word in a test comment.
- Task 6: complete (commits 9051066..638cd7b, review clean after 1 fix round). minor (deferred): per-frame `.map` in frameContext (accepted); LANDMINE for the protocol: the sandbox denied `git reset --hard` in one agent worktree; `git checkout -B <own-branch> origin/<exec>` worked as the fallback.
- Task 8 (exec-branch half): complete (commits 638cd7b..522d9e2, docs-only 2 files +2/−2, reviewed by controller inspection: RENDERER.md:35 + docs/references/orbit-trail-fragment-math.md:98 → innerBoundRadiusM with the under-occlusion rationale; DATA.md clean). Spec-branch half (§3.3 joints 8/9, plan census row) pending with P6's docs batch.
- Remaining: T7 attestation (USER) → final whole-branch review (fable/opus) → ledger archive → finishing-a-development-branch (#704, squash-merge on user's word).
- Final whole-branch review dispatched (opus, agent af8ed92a12a91b9dc) on review-35463c237..522d9e286.diff (9 commits) → scratchpad/sdd/p1/final-review.md; runs in parallel with the T7 attestation (independent gates).
- Final review (opus): Changes required — 1 Important (I1: five comments still name the deleted radiusM: cloudShellPass.ts:15, starSpheresPass.ts:11, AtmosphereDrawEntry.d.ts:19, selectionHaloTable.ts:10,94, FamousStarRow.d.ts:12,19,34; T8's grep was docs/-only), 0 Blocking, 6 Minors (three-gate currency split → F1, not this PR; `as const` fixture outlier → skip; surplus withRadius() test helper; header budget on four one-liners; bodyStandoffRadii "one home" overclaim; unwrapped markdown line). Census complete, DoD inventory present, deferral boundary held, purity allow-list untouched, no trailers.
- Final fix dispatch (the ONE allowed): sonnet isolated agent ad5a8246cd4713532 off 522d9e286 — I1 + src/tests grep + the four cheap minors; report final-fix1-report.md. Then scoped re-review, then archive + finish.
- Final fix round 1/1 DONE: 64f5a2981 (12 files +40/−40): I1's five + 2 more stale mentions (regimeArmFor.test.ts:5, oneMpcSeam.test.ts:142); M4 withRadius() deleted (8 sites), M5 headers trimmed, M6 word swap, M7 rewrap; M2/M3 skipped per ruling. tsc + full suite 1337/9243 green. Scoped re-review dispatched (sonnet, agent a10fdddc50326a4b3) on review-522d9e286..64f5a2981.diff.
- Final review: clean after 1 fix round (re-review verified all I1 sites against code, withRadius inlining value-preserving, headers in budget). minor (deferred): bodyStandoffRadii.ts header lost its cross-file clause along with the overclaim — optional one-liner "one home for the per-body override". Branch HEAD 64f5a2981 = review-clean.
- Remaining: T7 USER attestation → spec-branch docs commit (dispatched now) → archive → finishing-a-development-branch (#704).
- Task 8 (spec-branch half): complete — cd9b91d3d on per-planet-terrain-spec (PR #700): §3.3 joints 8/9 status, DATUM census +pivotRadiusMpc.ts:24,52 (29→31, total 43→45), P6 plan line-210 layout note corrected. Task 8 fully complete.
- PR #704 body updated (rulings, behaviour, attestation PENDING). ALL TASKS COMPLETE except T7 attestation (USER). Next on pass: archive ledger → finishing-a-development-branch (squash-merge only on the user's word).
- Task 7: complete — four-pose attestation by user 2026-09-14 against main (:5181 vs :5174): PASS; Sgr A\* floor doubling confirmed as the one deliberate change. ALL TASKS COMPLETE. Ruling: LAND (PR #704 ready for user code review).
