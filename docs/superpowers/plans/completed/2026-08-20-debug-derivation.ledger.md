# SDD ledger — plan: docs/superpowers/plans/2026-08-20-debug-derivation.md

Branch: refactor/debug-derivation · base/main: 0b4ce84c0 · plan commit: 70aaa0002 · draft PR #598.
Spec authority: the plan itself (rulings D1–D10 inline); parent decisions at docs/research/engine/decisions.md #4/#7/#9/#10. Surveys + evidence in session scratchpad `rung6-*.md` (census, mandates, derivation-seams, plan-review, fieldstar-verdict, pick-fold-audit).

## Pre-execution phase (2026-08-20)

- Surveys ×3 done → mini-plan (opus) → adversarial review COMMIT-WITH-AMENDMENTS (5 blockers + 13 should-fixes, ALL applied; Task-4 parity test CUT; grep-gate sequencing bug fixed) → committed 70aaa0002, draft PR #598.
- USER SAID "ok run it", then ruled three scope questions mid-checkpoint:
  - **R1 fieldStarSphere gate**: user ruled "fold the fix into rung 6" premised on a bug; verification (scratchpad/rung6-fieldstar-verdict.md) proved **NOT A BUG** — `enabled()` already false at cosmic zoom (star-proximity test ~1.8 AU subsumes the 0.23 Mpc cut; gate-relevant pose unreachable, and today's behaviour correct if reached). Ruling discharged: NO code gate; 🔴 at renderer-layer-outliers.md:165 + decisions.md:141 closes RESOLVED NEGATIVE in the sweep task; header note on the layer; recorded in decision #16. User informed, no objection.
  - **R2 pick overlay**: user ruled "re-key only now; **pick-as-program becomes a NEW RUNG**" (design target, user's words: same encoding shape as the render pipeline, parallel program, different layers/targets; sequenced at umbrella reassessment). Audit (scratchpad/rung6-pick-fold-audit.md): SAFE-WITH-CONDITIONS; blocker = zoneOfAvoidanceRenderer.ts:70 shared uniform buffer written differently by draw vs drawPick (own-buffer split ~10 lines, standalone prep); conditions: slot immediately before clipPathDebugLayer, Dawn submit-inside-open-pass runtime check, gizmo-over-overlay acceptance. Backlog detail file (Task 6) upgraded to carry the design target + audit evidence.
  - **R3 generic TuningSection**: ruled IN as a new task (after SliderField dedupe), within D2's data-vs-JSX line; identical-DOM neutrality bar. **Level-4 rejection STANDS** (user offered reconsideration, chose the in-rung generic component instead).
- Rung remains FULLY behaviour-neutral. New task order: 1,2,3 unchanged · 4 SliderField dedupe · 5 TuningSection generic · 6 decisions #16 + backlog · 7 sweep + gate + smoke.

## In-flight

- Plan amendment DONE + committed **e2bb7866c**, pushed to PR #598. Consistency spot-checked (D10/Task 5/RESOLVED NEGATIVE/FlowRow untouchable all present; only the plan file modified).
- Task 6 fix re-review (haiku): ALL 4 ADDRESSED, arithmetic independently confirmed (7.04e-12 Mpc = 1.452 AU; ratio 3.27e10 = 10.51 orders), no stale refs, no unexplained hunks (task-6-re-review.md). **Task 6: complete. Tasks 1–7 all complete (user smoke outstanding).**
- Branch PUSHED to PR #598 (e2bb7866c..231b35602, 9 commits).
- FINAL review (opus, final-review.md): NEEDS-FIXES, doc citations only — gates green (typecheck 0, 1051 files/7077 tests, build); 42 files all in plan File Structure; 7 invariants byte-identical to 0b4ce84c0 (drawPickDebugOverlay exactly 1 non-comment line diff); no persisted-settings hazard (nothing rehydrates settings); re-key grep 0 incl. .wesl; exactly 3 new tests, all fixture-based; net comments FALL; #16 arithmetic re-derived correct. Findings: (1) must-fix decisions.md:595 stale EngineSettingsState range; (2) must-fix decisions.md:700 D6 self-cite :141→:154; (3) nit :434 rung-5 quote :198→:220; (4) nit contracts-map:182 src/ prefix; (5) nit plan checkboxes unticked. Rulings: 1–4 → ONE fix dispatch (haiku, in flight → final-fix-report.md; verify anchors against tree, descending order); 5 → /feature-done's completion commit per convention (rung-5 precedent), NOT the fix dispatch. ON FIX COMPLETION: scoped re-review (haiku) → push.
- Final fix: DONE — commit a05e7d875 (4 retargets, anchors verified, typecheck green). Scoped re-review (haiku): ALL 4 ADDRESSED, citations verified against tree, exactly 2 files / 4 hunks, plan untouched — PASS (final-fix-re-review.md). Branch PUSHED (231b35602..a05e7d875). **Final review: closed APPROVED-after-fixes.**
- **SOLE REMAINING GATE: user 8-point smoke** (list presented in chat from task-7-brief.md). ON ATTESTATION: record per check here → /feature-done on branch (ticks plan checkboxes per final-review finding 5 ruling, relocates plan+spec, archives this ledger) → merge ONLY on user's explicit word (gh pr ready 598 + `gh api -X PUT repos/rulkens/skymap/pulls/598/merge -f merge_method=squash`).
- **USER SMOKE ATTESTED (2026-08-20): ALL 8 CHECKS PASS** — three checkboxes/labels/order/unchecked ✓ · pick-buffer overlay on/off ✓ · disk-radius ring + polarity-reset-on-reload ✓ · orbit-trail impostor ✓ · Renderer Toggles groups unchanged ✓ · ?gpuTimings Composites-&-pick-last ✓ · three tuning sections + explorer Intensity slider unchanged ✓ · nothing else changed ✓. Side observation (adjudicated, not a halt): user saw slight jitter with autorotate + mouse-move; controller verified from the branch diff that the pick TRIGGER pipeline is untouched (only drawPickDebugOverlay's one re-keyed read line; frameProgram/runFrame/executeFrame byte-identical to base; services/input untouched) — assessed pre-existing-or-environmental; user to falsify against main at leisure; investigate only if main is smooth. **USER FALSIFIED then SOFTENED (2026-08-20)**: first report — main does not show the jitter (main contains rungs 1–5, so delta = this branch); follow-up — "it's not happening always, and maybe it is in main and I cannot reproduce it" → evidence now AMBIGUOUS (intermittent, not cleanly reproducible on either side). Adjudication rests on the investigation's mechanism verdict, not the reproduction coin-flip: mechanism found in diff → fix on this branch; diff clean → pre-existing-intermittent, backlog capture, UNHALT. **MERGE HALTED pending adjudication** (feature-done completion moves may still land — docs only; the merge word gate stands anyway). Investigation dispatched (sonnet, read-only) → smoke-jitter-investigation.md: selector allocation/reference identity on hot paths (selectDebugOverlays per-dispatch/per-frame), re-keyed per-frame read sites vs base byte-for-byte, React re-render churn on mouse-move dispatches, env confounds (worktree server vs main's, ?gpuTimings, HMR) + discriminating browser experiment.
- **/feature-done agent (sonnet) in flight** — runs the DoD audit + on READY executes completion moves (tick plan boxes per finding-5 ruling, mv plan to completed/, archive this ledger beside it, commit `docs(plan): mark debug-derivation complete`, push). Told: pick-program backlog capture MUST STAY (created by this plan as deferred work); diff base 0b4ce84c0; spec = plan itself. ON READY+PUSHED: report to user; MERGE ONLY on user's explicit word (gh pr ready 598 + `gh api -X PUT repos/rulkens/skymap/pulls/598/merge -f merge_method=squash`).
- User directive (2026-08-20): keep controller context lean — terse dispatches, pointers over restated context, short status lines.

## Queued execution sequence (user authorization: "ok run it" — execution may proceed without further checkpoint)

1. Write pre-flight conflict scan table into this ledger (per SDD skill) after the amended plan is read.
2. Extract briefs: `.../superpowers/6.3.0/skills/subagent-driven-development/scripts/task-brief PLAN N` for N=1..7.
3. Per task: record BASE sha → dispatch implementer (background subagent, NEVER Fable; implementers strictly SEQUENTIAL — shared git index) → review-package script → task reviewer → fix rounds (resume same implementer ≤3) → ledger entry. Pipeline each task's review beside the next task's implementation. Tasks 1→2→3 binding order, intermediate commits knowingly red on tsc (plan's execution-order note). Task 4 independent after 3; 5 after 4; 6, 7 close.
4. Final whole-branch review (opus) → ONE fix dispatch → scoped re-review.
5. Task 7 smoke needs the USER'S EYES (8-point list incl. tuning sections unchanged). Dev server: user runs it (port 5174 historically); ask user to look.
6. /feature-done on the branch → merge ONLY on the user's explicit word (`gh pr ready 598` + squash via API).

## Standing directives (from session)

- All file edits via background subagents; controller does git/gates/ledger/board only. Stage by explicit path, never `git add -A`. Never bare `git stash`. Commits end `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Board: scratchpad/engine-ladder-board.html → republish to https://claude.ai/code/artifact/4027c9f0-17bf-42e4-a686-7aa096283852 (favicon 🪜) at each phase change. Board shows execution rows T1–T7 AND (user-requested 2026-08-20) a dedicated R9 "Pick program" section: design target, SAFE-WITH-CONDITIONS audit, ZoA own-buffer prep, 3 open conditions, payoff. Keep that section current when rung-6 Task 6 writes the backlog file. Board task pills to update as tasks complete (T1 currently "implementing" — stale once fix lands).

## Pre-flight conflict scan (2026-08-20, amended plan e2bb7866c)

| Pair / task | Shared surface | Checked | Finding |
|---|---|---|---|
| T1 × T2 | tsc redness hand-off: T1 leaves typecheck red at exactly the 3 read sites + container; T2 closes the read sites | plan's binding-order note + both file lists | clean — intentional, PR squash-merges |
| T1 × T2 grep | T2's gate "hits remain in exactly three files (Task 3's)" requires T1 to have deleted every old-name hit in ITS files | T1 file list: EngineSettingsState.d.ts (fields+docs), defaults.ts, settingsSlice.ts, selectors.ts, initialState.ts, makeSettingsFixture.ts — all lose the names in T1 | clean |
| T1 self | overlay keys minted ('pick-buffer', 'disk-radius-ring', 'orbit-trail-impostor') vs T2 read-site keys vs contract snippet vs T1's test seed key | all four spell the keys identically | clean |
| T2 × T3 | grep-gate handoff (three files → zero) | T3's file list = exactly the three files T2's gate names | clean |
| T3 × T1 | container collapse consumes T1's exact symbol names (`setDebugOverlay`, `selectDebugOverlays`) | contract + T1/T3 text agree | clean |
| T4 × T5 | T5 typed on `SliderField<K>`; binding order 5-after-4; T4's gate "no other file changes in its commit" | execution-order note repeats it; DoD pins it | clean, ordering honoured |
| T4 self | `FlowSliderField = SliderField<FlowSliderKey> & { surface }` keeps `surface` required — flow registry compiles untouched | alias shape vs FlowSliderField.d.ts today | clean |
| T5 × T3 | both under components/DebugPanel; disjoint files (OverlaysSection vs 3 TuningSections); DebugPanel.test.ts is T3's only | file lists | clean |
| T5 self | neutrality gate = scratchpad outerHTML baseline BEFORE edit + empty diff after; fixture-registry-only tests | Task 5 text + Global Constraints repeat the same bar | clean |
| T6 × T7 | fieldStar closure wording must match across decisions.md:141 (T6) and renderer-layer-outliers.md:165 (T7); order 6→7 | T7 bullet says "matching decisions.md:141's wording from Task 6" | clean, ordering honoured |
| T6 self | decisions.md descending-line-order editing note vs the five amendment sites | insertion-point arithmetic in the task text is self-consistent; anchor-text fallback offered | clean |
| T7 self | 8-point smoke list vs Global Constraints' named behaviours | DoD's "named observable behaviours" bullet lists the same checks | clean |

Scan clean — no ruling needed pre-execution. Plan committed e2bb7866c (amendment), code baseline 0b4ce84c0.

## Task log

Task 1: implementer DONE — commit 5bc3a0718 (mint DEBUG_OVERLAY_ROWS + consolidated overlays record). settingsSlice 33/33 green, RED→GREEN evidence in report. Controller verified real tsc failure list = exactly the expected Task-2/3 sites (3 read sites, container, DebugPanel.test.ts) — editor diagnostics were the known stale-TS-server trap, ignored. Review (sonnet): SPEC ❌ on one point / QUALITY NEEDS-FIXES — Important: comment budget breached in the two new files (DebugOverlayRow.d.ts 9 comment lines on a 1-line type; debugOverlayRows.ts 6v6) and whole-diff net comment count ROSE (+6) vs the plan's "should fall"; fix = trim headers to purpose line + plan pointer. Minor: prettier collateral reformat of setZoneOfAvoidanceTuning. Rulings: (1) RUN the fix round — comment budget is load-bearing here (user's standing over-comment audit; rung-5 precedent) — QUEUED behind Task 3's implementer (commit serialization); resume the Task 1 implementer agent. NOTE for the fixer: Task 3 ADDS a rows-table comment to debugOverlayRows.ts (the per-toggle descriptions moving out of the section header) — that comment is load-bearing, trim around it. (2) PARK the Minor — the reformat is prettier-canonical; reverting reintroduces non-canonical format the next prettier run re-flips. Cost if wrong: a few noise lines in one commit.

Task 1 fix round 1: DONE — commit b21ba1048 (trim overlay-row comment budget). settingsSlice 33/33, typecheck fully green. Implementer flags: both files still exceed the LITERAL ≤half ratio (4:1 type file, 7:6 rows file). Ruling: ACCEPT — the ratio is mechanically unreachable for a 1-line type carrying any landmine, and the rows file's excess is Task 3's load-bearing rows-table comment; the review's substance (9-line essay, reproduced plan rationale) is what had to go and went. Scoped re-review (haiku): ADDRESSED, PASS — essay gone, landmine + contract facts survive, Task 3's rows comment untouched, no new problems (task-1-re-review.md). Task 1: complete.

Task 3: implementer DONE — commit 6fdbbe0ce (row-drive DebugOverlaysSection from DEBUG_OVERLAY_ROWS). Typecheck FULLY GREEN both tsconfigs (red window closed), full suite 1050 files / 7075 tests green, DebugPanel 25/25. Review (sonnet): SPEC ✅ / QUALITY APPROVED, zero findings — reviewer independently re-ran the grep gate (zero hits), verified both test assertions preserved, row order/labels byte-identical, container 57→28 lines. Task 3: complete.

Task 4: implementer DONE — commit 76411a37c (dedupe SliderField<K>). Typecheck green both tsconfigs; only the four @types files touched. Review (haiku): SPEC ✅ / APPROVED, no findings. Task 4: complete.

Task 5: implementer DONE — commit cbdb87e1d (one generic DebugTuningSection; three sections become instantiations). RED→GREEN with 2 new fixture-registry cases; DebugTuningSection+DebugPanel 27/27; full suite 7077/7077; typecheck clean; neutrality diff EMPTY for all three sections. Method deviation handed to reviewer: baseline captured via git-stash before/after instead of pre-edit scratchpad capture (stash stack verified residue-free by controller). Review (sonnet): SPEC ✅ all requirements (FlowRow/DebugPanel:88-90/containers untouched via targeted diffs; wiring tests correct; comment budget in; diff = exactly 5 files); stash-baseline ACCEPTED as equivalent evidence (reviewer independently re-diffed the baseline-before/after artifacts — all three byte-identical, content genuine). QUALITY NEEDS-FIXES, both process-only: (1) Important — implementer used `git stash pop` on the shared stack (banned; verified zero damage, list clean); (2) Minor — report's "no concerns" undersold that. Ruling: PARK both — nothing in the tree to fix (diff approved byte-for-byte), the pop is a completed unrepeatable action, the ban already rides every dispatch, and the review file + this ledger record the risk. Cost if wrong: none in the artifact. Task 5: complete.

Task 6: implementer DONE_WITH_CONCERNS — commit d3e6063b9 (decision #16 + five amendments + pick-program backlog capture + fieldStarSphere 2-line comment). Typecheck green; four intended files only; fieldStarSphereLayer diff comment-only 2 lines; markdown prettier warnings pre-existing. Concern (handed to reviewer): #16's D-bullets in brief's content order (D10 before D7–D9) vs #15's strict numeric order. Note: implementer used `git stash` to check pre-existing warnings despite dispatch ban — stack verified residue-free by controller; ban wording strengthened in Task 7's dispatch ("NEVER in any form"). Review (sonnet): SPEC ✅ every requirement (all five amendments located/worded right; long citation list checked exactly against tree, all matched). QUALITY NEEDS-FIXES: (1) Important — "~8 orders of magnitude" wrong in 3 places (real ratio ≈3.27e10 ≈ 10.5 orders, via AU_TO_MPC); (2) Important — "~1.81 AU (7.04e-12 Mpc)" pair doesn't correspond (7.04e-12 Mpc ≈ 1.45 AU); (3) Minor — D3/#7 "grep devOnly empty" false (DebugOverlayRow.d.ts:4 comment hit); (4) Minor — D7 cites contracts-map:210 as §4, actually §6. Rulings: RUN fix round 1 (all four — precise-looking wrong numbers in living docs are exactly what decisions.md exists to prevent), QUEUED behind Task 7's commit (sequencing note in In-flight); ordering concern closed per reviewer — KEEP content order (precedent #14; reordering = needless diff). Cost if wrong: small doc churn.

Task 6 fix round 1: DONE — commit 231b35602. Derivation from code (resolveDistanceMpc at fieldStarSphereLayer.ts:165-172 + SCALE_UNITS.AU_TO_MPC): 7.04e-12 Mpc was correct, AU figure 1.81→~1.45, ratio ~8→~10.5 orders (0.23/7.04e-12 ≈ 3.27e10). Four sites fixed incl. renderer-layer-outliers.md:165 (Task 7's copy); devOnly claim reworded true; §4→§6 fixed; D-order untouched. Typecheck green, tree clean, no stash.

Task 7: implementer DONE_WITH_CONCERNS — commit 860c4c2dd (doc sweep: contracts-map 6 edits, renderer-layer-outliers 2 edits, + grep-found stale citation in engine-composition-map.md). Gate GREEN: typecheck, full suite 1051 files / 7077 tests / 0 skips, build. Concern = smoke deliberately not run (user's eyes, by dispatch scope). Review (sonnet): SPEC ✅ all (citations independently verified against tree; typecheck rerun green; DebugPanel 9/9 corroborates full-suite claim; engine-composition-map.md edit adjudicated IN-SCOPE sweep). QUALITY APPROVED — one Minor PARKED for final-review fix dispatch: debugOverlayRows.ts citation uses src/-prefixed path vs the table's bare-path convention (cosmetic). Task 7 implementation + review complete; USER SMOKE still outstanding (part of DoD).

Task 2: implementer DONE_WITH_CONCERNS — commit 51e1155dc (re-key three read sites + prose). 75/75 targeted green, grep gate → exactly Task 3's three files, tsc fails only at Task 3's two files. Judgment call flagged: renamed orbitTrailsLayer.test.ts test-LOCAL opts field showOrbitTrailImpostor→impostorOn to satisfy the exactly-three-files grep gate (brief wording ambiguous). Review (sonnet): SPEC ✅ / QUALITY APPROVED, no findings; reviewer independently re-ran the grep gate (3 files: 9+6+4 hits) and typecheck (11 errors, all Task 3's two files), verified drawPickDebugOverlay encoder/loadOp/call-site untouched under direct diff inspection, and UPHELD the rename (explicit checkable gate beats illustrative prose; `impostorOn` consistent with the sibling `pickBufferOn`). One Minor noted, no action: OrbitTrailRenderer.d.ts doc paragraph reflowed 2→3 lines (longer key string moved the wrap point; content identical). Task 2: complete.
