# SDD ledger — plan: docs/superpowers/plans/2026-09-21-local-froxel-aerial-perspective.md

Spec: docs/superpowers/specs/2026-09-21-local-froxel-aerial-perspective-design.md
Worktree: .claude/worktrees/fix-atmosphere, branch worktree-fix-atmosphere, dev server :5173
Protocol: lean (docs/superpowers/conventions/sdd-execution.md). Plan committed e7f4eab28.

Asks answered 2026-09-21: parallelism = this plan alone; perf gate = YES (outside-shell A/B vs main,
neutral-or-negative halts; inside absolute numbers reported).

## Pre-flight scan

| pair / task | produces vs consumes | found |
| --- | --- | --- |
| T1 ↔ T3, T4 | `u.froxelSliceKm` (byte 108) read by bake + apply | agree |
| T2 ↔ T3 | `froxelSliceEndKm(i, sliceKm)` | agree |
| T2 ↔ T4 | `froxelCoord(distanceKm, sliceKm, sliceCount) -> { w, identityMix }` | agree |
| T3 ↔ T5 | bake bindings 0–7 vs bake BGL | agree |
| T4 ↔ T5 | apply bindings 5 params / 6 inScatter / 7 transmittance / 8 depth vs apply BGL + entry 8 | agree |
| T5 ↔ T6 | `bakeAerialPerspective(pass, id, uniforms)`, `drawAerialPerspective(pass, id, depthView)` | agree |
| T6 ↔ T7 | both edit frameOrder.ts (line 25 vs 259–265), sequential | no overlap |
| T7 ↔ T8 | frameOrder comments 255–258 vs 235–241 | no overlap |
| T7 self | trail uv = clip.xy / viewportPx (y down) matches `sceneRayUnproject`'s fullscreen-uv convention | agree |
| T6 self | `bodyRowSlabs(state: PassState)`: renderFrame.ts:73 passes EngineState, a superset | assignable |

Clean. Grouping: D1 = T1–T4 (Opus, review-tagged) · D2 = T5 (Opus) · D3 = T6 (Sonnet) · D4 = T7–T8 (Opus).
Mid-branch reviews: one per review-tagged group (T2–4, T5, T7), then one final whole-branch review.

## Dispatches
D1: T1–T4 · opus · BASE e7f4eab28 · dispatched 2026-09-21 · IN FLIGHT · brief task-{1..4}-brief.md, report d1-report.md. On return: review-package BASE..HEAD → one mid-branch review (T2–4 shaders, Opus) → D2 (T5).
D1: DONE 2026-09-21 · HEAD ad5108cd1 · T1 79fc27b22 · T2 e91a048f5 · T3 295209d1c · T4 ad5108cd1 · 80 tool uses · typecheck clean, tests pass. Implementer judgement flagged for review: straddling sub-step CLIPPED to the march end (not skipped); no identity write at slice 0 (identityMix handles it).
R1 (mid-branch, T2–4 shaders): opus · package review-e7f4eab28..ad5108cd1.diff · IN FLIGHT · report d1-review.md. One fix round max, by a fresh implementer on the shader files only; no re-review.
D2: T5 · opus · BASE ad5108cd1 · IN FLIGHT (parallel with R1; disjoint files, no .wesl edits) · brief task-5-brief.md, report d2-report.md. On return: mid-branch review of T5 (TS↔WGSL bind groups) → D3 (T6, Sonnet).
Note: D2 is editing passes/aerialPerspectivePass.ts (T6 file) because T5 changes drawAerialPerspective signature; accept, and tell D3 the pass draw call is already adapted. Live agents: R1 (shader review) + D2 (T5). If a fresh session resumes before their reports: wait for both notifications; R1 findings → one fix round on shader files by a fresh Opus implementer, no re-review; D2 DONE → review-package ad5108cd1..HEAD → mid-branch review of T5 → D3 (T6, Sonnet, brief via task-brief script).
R1: DONE · Spec ✅ · verdict land · 5 minors deferred to the final review: (1) froxelSlices.wesl comment budget 13/16 + no exemption line; (2) bake.wesl:65 `ground.x >= 0` should be `> 0` (tangent ray from the surface freezes identity); (3) bake clamp vs terrain below the sphere — Ruling: NOT a defect, `u.bottomRadius` is the relief FLOOR (innerBoundRadiusM, atmosphereShellUniforms.ts:36), so no drawn terrain lies below it; costs nothing if wrong beyond a Dead Sea eye-check; (4) `froxelSliceEndKm(i,s) − s` sole call wants the slice START, rename/reshape; (5) bake.wesl:39-40,56 history/restating comments. No fix round now (minors only).
D2: DONE 2026-09-21 · HEAD d4300f1f4 (T5) · DONE_WITH_CONCERNS: (a) also adapted passes/aerialPerspectivePass.ts (draw call 3 params, import dropped) — accepted, expected; aerial path is DARK on this commit until T6 packs the record on the bake side; (b) `rebind` rebuilds the bake group too, one extra createBindGroup per body per tier switch — accept, review may weigh in; (c) shell renderer test harness now records bind-group labels. Report d2-report.md.
R2 (mid-branch, T5 TS↔WGSL bind groups): opus · package review-ad5108cd1..d4300f1f4.diff · IN FLIGHT · report d2-review.md. One fix round max, no re-review; minors deferred to the final review.
D3: T6 · sonnet · BASE d4300f1f4 · IN FLIGHT (parallel with R2; R2 is read-only) · brief task-6-brief.md, report d3-report.md. Told: the aerial pass draw call is already adapted by D2. On return: no mid-branch review (T6 not review-tagged) → D4 (T7–T8, Opus, briefs 7+8) → mid-branch review of T7 → final whole-branch review. First user eye-check is after D3 lands (inside-shell fog is functional again from T6 on).
R2: DONE · Spec ✅ · Approved, no fix round · 4 minors deferred to the final review: (1) aerialPerspectiveRenderer.ts:186 `rebind` rebuilds the bake group though it holds no skyViewTex (plan-mandated, surplus; two tests pin it); (2) aerialPerspectiveRenderer.test.ts:167-178 rebind test asserts only a label — make it assert binding 2/3 views come from the replacement bundle, or drop it; (3) `FROXEL_FORMAT` lives in the renderer, the other froxel constants in data/atmosphere/froxelVolume.ts; (4) one shared volume pair is unenforced against two inside bodies in one frame (accepted: at most one inside body per frame by construction, bodyRowSlabs.insideAtmosphere[0]). Noted: `addressModeW` implicit on the shared LUT sampler = clamp-to-edge by default, fine. Report d2-review.md.
D3: DONE 2026-09-21 · HEAD 4279d6f9c (T6) · 687 frame tests pass, typecheck clean · also touched tests/services/engine/frame/expandFrameOrder.test.ts (stale step index after the FRAME_ORDER insert) — accepted. Report d3-report.md.
Headless Everest regression at 4279d6f9c: froxel build vs spike march (everest-after.png) mean 0.025/255, max 2 → the froxel path reproduces the spike within the gate (plan target ≲ 0.1). Captures scratchpad everest-t6.png, everest-t6-diff.png. Console shows a pre-existing `debug-line-pipeline` vertex-slot-4 validation warning (markerLines, not touched by this branch). Inside-shell fog is functional again from T6: user eye-check possible on :5173.
D4: T7–T8 · opus · BASE 4279d6f9c · IN FLIGHT · briefs task-7-brief.md + task-8-brief.md, report d4-report.md. On return: review-package 4279d6f9c..HEAD → mid-branch review of T7 (shader + TS↔WGSL + camera maths, Opus) → final whole-branch review (top tier, from merge-base 79615dfef; hand it R1's 5 + R2's 4 deferred minors) → ONE fix wave → scoped re-review → Verification.
D4: DONE 2026-09-21 · T7 fb85270e4 · T8 2077566ee · full suite green (1356 files / 13407 tests), typecheck clean · DONE_WITH_CONCERNS: (1) `viewportPx` for the trail uniform is taken from the instance record (float 18) because the pinned `OrbitTrailDepthFrame` + draw signature carry no viewport — pass↔renderer coupling, for the T7 review to judge; (2) smoothstep(-tol, tol) undefined at tol == 0, unreachable, kept per brief; (3) no GPU verification, Dead Sea eye-check is the gate; (4) three mock renderTargets fixtures gained farDepthView/depthViewOf because the executor resolves the new `{ sample }` line. Report d4-report.md.
R3 (mid-branch, T7 trails: shader + TS↔WGSL layout + camera maths): opus · package review-4279d6f9c..2077566ee.diff · IN FLIGHT · report d4-review.md. One fix round max, no re-review. On return: final whole-branch review (top tier, from merge-base 79615dfef) with R1's 5 + R2's 4 + R3's minors handed over → ONE fix wave → scoped re-review → Verification.
OPEN (user, 2026-09-21): labels through terrain — same root cause (caption occludeWeight = CPU sphere test at the relief floor, produceSceneBodyCaptions.ts:131). Recommended follow-up PR as third sampled-depth consumer; alternative task 9 here. Awaiting the user's ruling; the pipeline continues meanwhile.
R3: DONE · Spec ✅ · Needs fixes · Important 1: a null depth frame does not imply the far texture — with no body row in the chain the NEAR0 row is lastDepthClear (its star spheres write depth), so real depth binds while depthFrame is null and the fragment unprojects through identity → trails discarded under the Sun's disc; same for body-m with null bodyPose. Fix: pass `ctx.renderTargets.farDepthView()` when depthFrame is null (orbitTrailsPass.ts:161-173), and fix the pass test at :747 that blesses the world-mpc case. Minors 1,2,5,6,7,8 folded into the same fix round (all small, same files); Minor 3 ruled keep the instance-record viewport read; Minor 4 not reachable. Report d4-review.md.
F3 (fix round for R3): sonnet · BASE 2077566ee · IN FLIGHT · brief = d4-review.md Issues section · no re-review (lean); the final whole-branch review covers it. Report f3-report.md.
F3: DONE · 6a88d141b `fix(orbit-trails): a null depth frame binds the far placeholder` · 1126 frame+gpu tests pass, typecheck clean. Report f3-report.md.
RF (final whole-branch review): fable · package review-79615dfef..6a88d141b.diff (15 commits, 57 files, +2373/−1838) · IN FLIGHT · report final-review.md. Handed over: R1's 5 minors, R2's 4 minors, deletion-audit framing, plan DoD + spec. On return: ONE fix wave (Sonnet, or Opus if shader edits) → scoped re-review (Sonnet) → Verification (user eye-checks Everest/Dead Sea/descent/Mars/outside; perf A/B outside shell vs main, neutral-or-negative HALTS; inside absolute numbers; headless Everest diff already 0.025 at T6, re-run at fix-wave HEAD) → draft PR → CI watch → /feature-done.
RF: DONE · Ready to merge WITH FIXES · no Critical · Important: (1) frameOrder.ts:24-29 compute-order comment false, replace with one line; (2) shell/aerial mutual exclusion untested — add inside-camera shell test + aerialPerspectivePass.test.ts; (3) rebind rebuilds the bake group (surplus) — reset apply side only, bakeGroup readonly, test the apply-side rebind; (4) RENDERER.md:57 points at deleted insideRayDir → sceneRayDir. Minors confirmed: froxelSlices 13/16 trim; bake.wesl `>= 0`→`> 0`; froxelSliceStartKm; delete bake.wesl:26-28; unused NEAR0 imports (orbitTrailsPass.ts + test); stale 'depth: sample'/'inside-shell fragment'/'rewritten per draw' phrases; two headers > 5 lines (aerialPerspectivePass.ts, apply fragment.wesl); sceneDepthRay.wesl 21/18. Dropped: deferred 7, 8. Report final-review.md.
PERF GATE (outside shell, paired A/B vs merge base :5180, 30 frames, dpr2): earth-surface A 36.6/31.9 vs B 34.9/37.7 ms; solar-system A 30.6/28.3 vs B 29.6/29.6 ms — inside run-to-run swing, identical slot lists, no aerial bake row outside the shell → NEUTRAL (regression check passes). Inside numbers: `collectTimings` hangs at a body-arm pose (known landmine), read the DebugPanel row instead. User 2026-09-21: "performance doesn't seem a problem".
MERGED origin/main 907ecb66b → ea3ceec87 (conflict: docs/BACKLOG.md hoist item, both sides kept). Full suite green after the merge.
USER-REPORTED DEFECTS 2026-09-21 (eye-check on :5173): (a) inside the shell only a disc around the nadir is fogged, distant ground toward the terminator shows no atmosphere — root cause: the 256 km far bound truncates oblique rays before the dense layer; RULING: the last texel carries the march to the ray's end (spec amendment) → T9 brief task-9-brief.md; (b) star labels and other bodies' labels draw over terrain (trails are fine) → investigation dispatch I1, then T10. Capture tooling: share-link captures work (`scratchpad/shotLink.mts`, LINK=<share url> PORT=5173|5180); the old pose-commit script does not settle on Mars.
D5: T9 + final-review fix wave · opus · BASE ea3ceec87 · IN FLIGHT · report d5-report.md (two commits: T9 first, then `fix(atmosphere): final review fixes`). I1: labels-through-terrain investigation · opus · read-only · report i1-labels.md. On return: controller verifies T9 headless (60 km Everest vs main) → T10 brief from I1 → scoped re-review of D5 → user eye-check → PR.
CLEANUP OWED at feature end: scratch worktree `.claude/worktrees/perf-base-79615dfef` (detached merge base, node_modules + public/data symlinks, dev server :5180 started via `npm --prefix … run dev -- --port 5180`) → stop the server, `git worktree remove` it; agent worktree `agent-a448cfa9e402bacb5`; copy out `.superpowers/parked/2026-09-21-camera-commit-authoritative.md` + `.superpowers/spike-poses/earth-everest.json` before wt removal.
QUEUE after D5 + I1: controller headless check of T9 (`scratchpad/shotUrl.mts` with `earth-everest-60km.json` on :5173 vs `everest-60km-main.png`; Everest night diff vs `everest-after.png`) → T10 brief (labels + marker lines behind terrain, from i1-labels.md) → D6 (T10) → scoped re-review of D5 + D6 (Sonnet) → user eye-checks on :5173 (Everest, Dead Sea, Perseverance 146 km, descent, outside) → draft PR → CI watch → /feature-done (ledger archive, diff breakdown, deletion audit, cleanup above).
I1 DONE 2026-09-21 → i1-labels.md: ONE root cause (famous-star captions are NEAR0 scene-body captions; CPU sphere test at the relief floor sets occludeWeight 0 → per-pixel attenuation off). RULINGS → task-10-brief.md: per-pixel clip; both swap lines sample foreground:0; extract sampledDepthKmFrame + rename OrbitTrailDepthFrame→SampledDepthKmFrame; occludeNearKm second channel; daytime-washout gap left adjacent (report to user). Repro poses captured: scratchpad/perseverance-labels-before.png + perseverance-labels2-before.png (share links in the brief). D6 (T10) dispatches AFTER D5 lands (same tree; frameOrder overlap).
SIDE EFFORT (user 2026-09-21): perf harness pose hook fix as its OWN PR off origin/main — agent a965678903ffdb42d in an isolation worktree, brief scratchpad/perf-hook-brief.md, report scratchpad/perf-hook-report.md; on return: read report, watch CI on its draft PR, report to user. Not part of this ledger's /feature-done.
D5 DONE 2026-09-21: 5046df5ff (T9 remainder march; REMAINDER_STEP_SLICES = 0.5 instead of a km const — ruled fine, same value) + ca651d446 (final review fixes). Suite 13702 green, typecheck clean. Concern: bake cost for capped rays up to 1024 extra scatterSteps per column — check the aerial-perspective-compute timing row. sceneDepthRay.wesl comment ratio left at 19/18 under the landmine exemption (accepted).
D6: T10 labels · opus · BASE ca651d446 · brief task-10-brief.md · report task-10-report.md.
T9 VERIFIED headless 2026-09-21: everest-60km-t9.png matches main's far-ground haze (fog reaches the horizon); shotUrl.mts fixed (raw commas/colons in the hash — percent-encoding made the pose decode null; FOCUS env adds focus=body-<id>, needed for a non-Earth body arm or the Earth pivot-pin wins).
LIMB BAND (user hypothesis CONFIRMED 2026-09-21, captures mars-user-566km.png + earth-limb-{1,2}.png, present on main too): the shell classifies rays against the relief-FLOOR sphere; terrain pixels above it at the limb miss the floor (near wall discards them) and hide the far wall (depth test) → a band with NO extinction/in-scatter → bright line, strongest near the terminator. Fix shape (T11 candidate, after D6 since it reuses sampledDepthKmFrame): shell samples foreground:0 depth; ray is a ground ray iff depth finite (tFar = min(top exit, depth km)); floor test only picks the LUT half; duty split becomes front wall ⇔ depth finite; frame order: one marker { sampleDepth: [contact-shadows, atmosphere-shell] } after rings (bodyRowSteps allows ONE marker per roster). Awaiting user ruling: T11 here vs backlog.
RULED by user 2026-09-21 ("yes go for it"): limb band fix = T11 on this branch → task-11-brief.md; D7 (T11, opus) dispatches after D6 lands (reuses sampledDepthKmFrame). QUEUE now: D6 → D7 → scoped re-review of D5+D6+D7 (Sonnet) → user eye-checks → draft PR → CI → /feature-done.
PERF-HOOK SIDE PR DONE 2026-09-21: draft #794 (9793d1a68, branch worktree-agent-a965678903ffdb42d, CI green). PerfPose = { framed, rate?, clearFocus? }; collectTimings pumps the loop via engine.debug.requestRender; scenario mars-jezero-146km added. LANDMINE: a body-arm pose needs the focus inside its subtree (scenario: clearFocus: true; URL: focus=body-<id>) or bodyRung.releases drops the arm and Earth wins — same cause as the failed #pose=b,mars links without focus. Agent wt agent-a965678903ffdb42d to remove after #794 merges.
D6 DONE 2026-09-21: d04be8b92 (T10, 41 files). Decisions accepted: one optional OverlaySceneOcclusion joint {colorView, depthView, frame} instead of two positional args; overlaySceneOcclusion.ts resolves colour guard + km frame + far placeholder once for the five overlay passes; LabelData did not grow, MarkerLine 52→56 B. T10 VERIFIED headless: perseverance-labels-after.png (captions only over sky; Proxima/Mimosa hidden behind the ridge) + perseverance-labels2-after.png (Caph/Schedar/Gamma Cas gone behind the dune); Perseverance caption over its own rover intact. Open eye-checks for the user: rover-scale captions (metre error floor), Everest.
D7: T11 shell-by-depth · opus · BASE d04be8b92 · brief task-11-brief.md · report task-11-report.md. Perf baseline attempt (solar-system) useless: no atmosphere-shell row in that scenario and a probe capture ran mid-sample (FACE rows); T11 perf check = timings overlay at the Mars 566 km pose if needed.
COMPACT 2026-09-21: D7 (T11) IN FLIGHT — uncommitted changes in the tree belong to it; on its report: re-capture via shotLink.mts the Mars 566 km link (focus=body-mars&t=2026-09-21T03:41:33.106Z&pose=b,mars,0,0,0,101288.7540987499,3886780.5728784506,-726878.7714395784,-0.988942701760686,0.05173648317909166,0.13898082221132102,0.12423340901706281,0.800803619472927,0.5859006939090096,0.08098390406910762,-0.5966882765291432,0.7983762946971851) vs mars-user-566km.png and earth-limb-1 (link in the user-defects entry) → limb line gone; then ONE scoped review of 5046df5ff..HEAD (Sonnet, briefs 9/10/11 + reports) → fix round if needed → user eye-checks on :5173 → draft PR from worktree-fix-atmosphere → CI watch → /feature-done + CLEANUP OWED (add: agent wt agent-a965678903ffdb42d after #794 merges). Everything else in this ledger stands.

D7 DONE 2026-09-21: cf8dc3b5c (T11 shell classifies rays by sampled depth; report task-11-report.md, DONE_WITH_CONCERNS). VERIFIED headless: mars-566km-t11.png — limb line GONE (zoom pair mars-limb-{before,t11}-zoom.png); everest-60km-t11.png pixel-identical to t9 (inside path untouched). earth-limb-1-t11.png pixel-identical to before: that pose (130 km) is INSIDE the shell (top 100 km × 1.005 enter ratio ≈ 132 km) → froxel path, band still there (earth-limb-1-t11-zoom2.png: a lighter over-fogged band on terrain just under the silhouette).
RULING (T11 concern 1, probe roster): accepted for landing — `atmosphere-shell` leaves the probe capture roster (a probe of a subject outside the atmosphere loses the host's limb glow; e.g. a satellite mesh). Flag to the user at landing as a regression with the 4-pipeline alternative priced; their call to keep or restore.
RULING T12 (Earth inside-path limb band, same defect, second mechanism): bake.wesl marches a floor-missing ray's remainder to the TOP EXIT, so a terrain pixel above the floor at the limb reads the whole limb path (~2× its own). Fix: (a) bake — a floor-missing ray whose perigee lies beyond the slice bound ends its remainder at the perigee (the floor-grazing neighbour's answer; rays with a perigee inside the bound keep today's top-exit end so low-altitude views do not change); (b) apply — a geometry pixel whose depth is at/after the top exit (a body beyond the atmosphere) takes the shell's whole-column sky answer instead of the froxel, so the Moon at the limb is not half-hazed by (a) and matches the sky beside it. Cost if wrong: two shader lines to revert. Brief task-12-brief.md; D8 dispatch (Sonnet), BASE cf8dc3b5c.
QUEUE after D8: re-capture earth-limb-1 + earth-limb-2 (band gone), everest-60km (far plateau slightly less hazed is expected, eye-judge), mars-566km unchanged → ONE scoped review 5046df5ff..HEAD (Sonnet; briefs 9/10/11/12 + reports) → fix round if needed → user eye-checks → draft PR → CI watch → /feature-done + CLEANUP OWED (perf-base wt + :5180, agent-* wts, parked files).
D8 DISPATCHED 2026-09-21 (T12, Sonnet, BASE cf8dc3b5c). On its report: run the QUEUE line above (re-captures via scratchpad shotLink.mts / shotUrl.mts with earth-everest-60km.json; earth-limb links are in the user-defects entry and the compact summary).
D8 DONE 2026-09-21: e5c5736f6 (T12 perigee rule + bodies past the top take the sky answer; report task-12-report.md). VERIFIED headless: earth-limb-{1,2}-t12.png differ from before by ≤ 8 levels (blue) in the first ~15 terrain rows under the silhouette — the band is STILL THERE; everest-60km-t12 ≈ t9 (max 23, tiny); mars-566km unchanged.
EARTH BAND ROOT CAUSE (probes, all reverted, tree clean): (1) sky-view LUT step count 30 → 512: pixel-identical (LUT converged); (2) sky pixels routed through the froxel (cf8dc3b5c bake): the froxel's sky rim is a smooth 43→58 L ramp that continues into the terrain (68 L), while the LUT's sky rim peaks at 101 L two rows above the same terrain; (3) the same limb from 200 km (OUTSIDE path, T11 shell) is continuous: sky 34→92 flows into terrain 88→84 (earth-limb-200km-t12.png). So the inside-path band = the FROXEL cannot resolve the limb: FROXEL_DIMS is 32×32 in screen xy (~60 device rows per column at this frame) while the rim is ~10 rows tall; the froxel smears the rim into the terrain beneath, and the sharp LUT sky beside it shows the step. Not a bake-rule bug; no bake resolution can fix it (resolving 8 km of perigee altitude at the limb needs ~5 px columns → hundreds of thousands of columns).
PROPOSED T13 (needs the user's ruling — it reverses T9 and T12): the apply pass hands GEOMETRY BEYOND THE FROXEL FAR BOUND (tEnd·km > slices·sliceKm) to the shell's ground answer, `sampleShellSegment(dir, tDepth, true)` (T11's machinery: intersectsGround = floorHit || tDepth < top exit, horizon-row clamp) — the inside limb then renders exactly like the outside one; the near field (< 256 km) stays froxel. DELETE: T9's remainder march (REMAINDER_STEP_SLICES, MAX_REMAINDER_STEPS, the loop, the header admission) and T12's perigee rule; keep T12's "depth past the top takes the sky answer" (subsumed: it is the same hand-off with hasDepth false). Fog amount beyond the bound is the same as T9's (both integrate to the floor, not the terrain: high terrain beyond 256 km is over-fogged either way, as on main); what changes is angular resolution (per pixel, not 32×32) and continuity with the sky. Risk: an iso-distance seam at 256 km where the froxel integral meets the LUT integral (the 512-step probe says the LUT is converged; the froxel uses 1 km sub-steps, so they should agree to a few %); verify on everest-60km and the Dead Sea.
COMPACT 2026-09-21 (after D8): no agents in flight; tree clean at e5c5736f6. WAITING ON THE USER for two rulings: (a) T13 (apply hands geometry beyond the froxel far bound to the shell's ground answer; delete T9 remainder march + T12 perigee rule) — "T13 go" → write task-13-brief.md from the PROPOSED T13 entry above, dispatch Sonnet, verify earth-limb-{1,2} (band gone), everest-60km + Dead Sea (no seam at 256 km), mars-566km unchanged; "land as is" → skip. (b) atmosphere-shell dropped from the probe roster (T11 concern 1): keep, or restore via a second depth-tested pipeline pair. THEN the QUEUE: ONE scoped review 5046df5ff..HEAD (Sonnet; briefs 9–13 + reports) → fix round → user eye-checks on :5173 → draft PR → CI watch → /feature-done + CLEANUP OWED. Capture links: Mars 566 km + Earth limb 1/2 are in the COMPACT/D7/D8 entries and the compact summary; Earth 200 km outside pose = limb-1 eye scaled by (6371+200)/6501.2; scratchpad shotLink.mts (LINK, OUT, PORT) / shotUrl.mts (POSE json, FOCUS).

## SIDE 2026-09-21 — Bruneton reference cloned for reading
User asked the cost of switching to Bruneton (answered: ~one froxel-sized SDD; plumbing on this
branch survives; land froxel first, Bruneton = later PR swapping bake + lookup at the same seam).
Reference clone (BSD-3, ebruneton/precomputed_atmospheric_scattering, depth 1) at
`scratchpad/bruneton/atmosphere/`: `functions.glsl` (1896 lines; mapping at 773/837, GetTransmittance
493, GetSkyRadianceToPoint 1787), `model.cc` (bake driver), `constants.h` (R 32 · MU 128 · MU_S 32 ·
NU 8; transmittance 256×64), `demo/demo.glsl` (apply). No decision taken; rulings a/b still open.

## RULING 2026-09-21 — user: "ok try T13 for now" (Bruneton switch discussed; T13 first, Bruneton PR after landing if the froxel still bands)
D9 = T13 dispatched (Sonnet, background), BASE e5c5736f6, brief `task-13-brief.md`, report `task-13-report.md`.
On DONE: verify headless (earth-limb-1/2 band gone; everest-60km + Dead Sea no 256 km contour; mars-566km unchanged),
then the QUEUE (one scoped review 5046df5ff..HEAD → fix round → user eye-checks → draft PR → CI → /feature-done). Ruling (b) probe roster still open.
D9 DONE 2026-09-21: cf0e74cd5 (T13: geometry past the froxel slices → `sampleShellSegment(dir, tEnd, true)`; remainder march + perigee rule deleted; report task-13-report.md, DONE). VERIFIED headless: earth-limb-1/2-t13.png — the limb BAND IS GONE (zoom pair earth-limb-1-{t12-zoom2b,t13-zoom2}.png: stepped rim → smooth); mars-566km-t13 pixel-identical to t12 (mean 0.000, outside path); everest-60km-t13 vs t12: far plateau past 256 km changed (5% px, max 102) along an iso-distance curve, but NO visible step in either build at the crop (everest-60km-t12-t13-contour.png); user's Mars 73 km pose (INSIDE, horizon 709 km; link mars-73km-link.txt) captured mars-73km-t13.png — no hard line found in the crop, user to eye-check on :5173.
USER-REPORTED 2026-09-21 (mid-T13): hard iso-distance line at the froxel far bound on Mars 73 km (barely visible on Earth: step = in-scatter between terrain and the relief floor, large on Mars). PROPOSED T14 (awaiting ruling): adaptive slice km per frame = (camera horizon + relief-top horizon) / slices, floored at FROXEL_SLICE_KM, slice count 64 → 128; T13 handoff stays as the gas-giant fallback; re-run the slab gate at the Mars pose. Alternative offered: fade froxel→shell over the last quarter of the volume.
RULING 2026-09-21 — user: "can you do 1" = T14 is the ALTITUDE BLEND (per-frame weight, froxel↔shell), NOT the adaptive bound. D10 = T14 dispatched (Sonnet, background), BASE cf0e74cd5, brief task-14-brief.md, report task-14-report.md. Baseline for the 7 km check: scratchpad everest-7km-t13.png (spike-poses/earth-everest.json). On DONE: verify mars-73km / everest-7km / everest-60km / earth-limb-1 / mars-566km, then the QUEUE (scoped review 5046df5ff..HEAD with briefs 9–14 → fix round → user eye-checks → draft PR → CI → /feature-done). Probe roster ruling (b) still open.
D10 DONE 2026-09-21: 8065c6331 (T14 altitude blend: shellWeight = smoothstep(1, 2, horizonKm/boundKm); report task-14-report.md, DONE). VERIFIED headless vs t13: mars-73km — seam gone, scene uniformly a little hazier (23% px changed, max 21); mars-566km identical; everest-7km ≈ identical (max 8); earth-limb-1 small (max 17); everest-60km CHANGED 45% px (max 30): the near Himalaya, crisp at t13, is blue-hazed at t14 (everest-60km-t13-t14-contour.png, top t13 / bottom t14) — the shell's whole-ray over-fog now applies to ALL terrain above ratio 2 (Earth > ~20 km altitude, Mars > ~10 km). NEGATIVE-OR-NEUTRAL EVIDENCE → user rules: keep T14 (accept the over-fog), retune the ratios, or replace T14 by the ADAPTIVE BOUND (slice km follows the horizon; froxel correctness everywhere, no seam) — controller recommends the adaptive bound.
USER-REPORTED regression 2026-09-21: rover captions partially occluded from orbit (Curiosity from 7705 km, link scratchpad/curiosity-label-link.txt, capture curiosity-label-t13.png: only "ity" visible). CAUSE: T10's per-pixel depth test compares TEXT pixels; the caption is lifted screen-up from a seated subject, and from orbit "up" from Gale is toward the disc centre = terrain tens of km NEARER than the rover. PROPOSED T15 (awaiting "T15 go"): the label/marker-line VERTEX stage samples depth once at the anchor's screen position → flat verdict; fragment keeps the sphere channel, max as today; depth texture + frame gain VERTEX visibility in the two overlay BGLs.
RULING 2026-09-21 — user: "keep task 14 as it is now, it looks good enough" (over-fog above ratio 2 ACCEPTED; adaptive bound NOT pursued). T15 (label anchor verdict) still awaits "T15 go".
RULING 2026-09-21 — user: "T15 go". D11 = T15 dispatched (Sonnet, background), BASE 8065c6331, brief task-15-brief.md, report task-15-report.md. On DONE: verify curiosity-label (run-curiosity.sh t15), everest-7km, mars-73km; then the QUEUE: ONE scoped review 5046df5ff..HEAD (Sonnet; briefs 9–15 + reports) → fix round → user eye-checks → draft PR → CI → /feature-done + CLEANUP OWED. Probe roster ruling (b) still open.
D11 T15 first commit 17fea8d41 — VERIFIED headless: WORSE. curiosity-label-t15: the whole "Curiosity" caption hidden (t13 showed "ity"); Perseverance (upper-left limb) visible in both. CAUSE (code + evidence): `applyLift` in label2DDirector.ts REPLACES `worldPos` with the lifted placement (`liftedLabelPlacement`, screen-up by ≥ MIN_LABEL_CLEARANCE_PX, unprojected), so the "anchor" the vertex stage projects IS the lifted point over nearer terrain; Perseverance's screen-up is toward the limb (farther terrain) so it survives. Marker line `fromWorld` (tBottom lerp) is already near the subject. FIX ROUND 1 (resume implementer): the label carries its SUBJECT position — `Label2D.occludeSubjectPos?: Vec3` set by `applyLift` to the pre-lift anchor; `LabelData` gains `subjectPos: vec4` (xyz; 80 → 96 bytes, `LABEL_DATA_BYTES`, pack loop defaults to worldPos, parity test); `vsOcclude` projects `subjectPos` for the verdict.
D11 DONE 2026-09-21: 17fea8d41 + fix round 1 6b5dc8fc3 (T15: depth verdict at the SUBJECT position, in the vertex stage; `Label2D.occludeSubjectPos`, `LabelData.subjectPos`, LABEL_DATA_BYTES 96; report task-15-report.md). VERIFIED headless: curiosity-label-t15b.png — "Curiosity" fully visible (pair curiosity-label-t13-t15b-pair.png); everest-7km-t15 ≈ t14 (0.00% px>8); mars-73km-t15 identical to t14. Perseverance visible. Tree clean.
RF2 (scoped review of T9–T15, ea3ceec87..6b5dc8fc3, 9 commits, 69 files +1754/−491): Sonnet, IN FLIGHT; package review-ea3ceec87..6b5dc8fc3.diff, report review-t9-t15.md.
RF2 DONE 2026-09-21: verdict LAND, 0 critical / 2 important / 2 minor (report review-t9-t15.md). FIX WAVE (Sonnet, one commit, BASE 6b5dc8fc3): (1) labels/vertexOcclude.wesl history prose + 33-line header → ≤ 5 lines, no history; markerLines/vertexOcclude.wesl header trimmed; (2) dead varying `occludeNearKm` deleted from both VsOut (labels @location(6), markerLines @location(3)) and the plain `vs` writers; (3) minor: aerial fragment.wesl duplicated shell-answer branch folded into one condition. No re-review (comment/dead-code fixup; controller reads the diff). Then: user eye-checks → draft PR → CI watch → /feature-done.
FIX WAVE DONE 2026-09-21: e588ec0b8 (headers ≤ 5 lines, dead VsOut.occludeNearKm gone, aerial branch folded; curiosity t16 identical to t15b). MAIN MERGED 501cf3149 (origin/main 92ff2b01d, constellations Layer #795; typecheck clean; 381 files / 4700 tests green). PUSHED; DRAFT PR #798 OPEN; CI watch running in the background. NEXT: CI verdict → user eye-checks on :5173 (Everest, Dead Sea, rovers at rover scale, descent, outside, limb, body in front of another's atmosphere, Saturn ring vs shell) → probe roster ruling (b) → /feature-done (ledger archive, diff breakdown, deletion audit) → CLEANUP OWED (stop :5180 + remove wt perf-base-79615dfef; remove agent-a448cfa9e402bacb5; remove agent-a965678903ffdb42d after #794 merges; copy out .superpowers/parked/2026-09-21-camera-commit-authoritative.md + .superpowers/spike-poses/earth-everest.json). Adjacent gap to report at landing: weight-0 captions skip daytime atmosphere washout.
CI GREEN on #798 @ 501cf3149 (typecheck · test · format pass; Workers build pass). WAITING ON THE USER: eye-checks + probe roster ruling (b) + adjacent-gap pick-up; then /feature-done + CLEANUP OWED. Compact-safe.
USER 2026-09-21: "eye check done. can we merge?" → taken as: probe roster stays WITHOUT atmosphere-shell (b kept), weight-0 caption washout gap BACKLOGGED. /feature-done RUNNING: tests 1376 files / 13843 green, typecheck clean, no new TODOs, no commented-out code, no shrunken tests, 6 new test files. Deletion audit (opus, report deletion-audit-branch.md): −58 src/−1 test SAFE-NOW (S1–S7, comments + sampleShellRay passthrough) → Sonnet apply dispatch IN FLIGHT (one commit, report deletion-audit-apply-report.md); NEEDS-RULING N1–N4 (null-frame IDENTITY fills ~−22, vsOcclude twin helper ~−14, orbitTrailRenderer.draw record ~−22 tests, selectionRing dead depth-frame write −2) go to the user with the verdict. NEXT after the deletion commit: tick plan checkboxes, plan+spec → completed/, ledger archive, backlog sweep, completion commit, push, CI watch; then the user merges from main (never from a worktree); then CLEANUP OWED.
