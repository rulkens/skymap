# SDD ledger — plan: docs/superpowers/plans/2026-07-31-orbit-trail-ribbon-impostor.md

Branch: worktree-orbit-trails-perf · worktree: .claude/worktrees/orbit-trails-perf · PR: #536 (draft)
Dev server: http://localhost:5173 (session shell bzbm1tnze) — pass --url to npm run perf.
Baselines (scratchpad /private/tmp/claude-501/-Users-rulkens-Development-js-skymap/915eb0c9-c943-437b-9ab9-34d01e75e404/scratchpad/):
  baseline-galactic-centre-v2.txt — orbit-trails 7.6 ms real, hdr·NEAR0 7.6, MERGED 43.2
  baseline-solar-system.txt — orbit-trails 1.3 ms real, MERGED 35.9
  baseline-galactic-centre-sweep.txt — hdr·NEAR0 exp 0.94
Subagent rules: absolute worktree paths; implementers do NOT run npm — controller runs tests/typecheck and feeds back output.

Task 1: complete (scaffolding commits cc3cdee7 + 9afd0f2e pre-dated execution; draft PR #536 opened; baselines confirmed on HEAD)

## CRITICAL BUG found and fixed 2026-07-31 late (after T6 + overlay landed)
- vertex.wesl read the clip basis with .xyw — but the padded vec4 packs (clip.x, clip.y, clip.w, 0), w in the THIRD slot — so p.z=0, every ribbon vertex collapsed to (0,0,0,0): zero-area triangles, ZERO fragments, NO validation error. Every ribbon-classified orbit invisible. Fixed @ ba8c69ba (.xyz + landmine comment).
- Why nobody caught it: T4's opus reviewer checked swizzle ARITY not slot semantics; controller hand-trace read the NAME (clip.w) not the slot; WESL untestable under vitest; solar-system poses are all-fallback (bug invisible); GC pose whitewashed by the MW core glow (bug hidden). User's "trails are gone at the GC" was the first real signal — was RIGHT, and the "no trails at 2.6e-7" repro on base was the whitewash masking both.
- Found by GPU bisect: colour-fill overlay showed no hull at GC-dark (Renderer Toggles: milky-way/star passes off) with 39 ribbon instances on-screen; Test A (hardcoded fullscreen tri in vsRibbon) flooded → plumbing fine; Test B (real curve, fixed offset) black → sample math; slot audit found the pad read.
- ALL prior feature-side perf numbers are VOID (they measured zero-area draws): the 8.3ms wall-clock, the ~0.0ms orbit-trails, the 41→12ms merged. T7 must re-run from scratch AFTER the user's visual pass (user directive: visual verification BEFORE measurement, always).
- Verified fixed: gc-dark capture shows all 39 S-star ellipses closed and unbroken (scratchpad/fixed-gc.png). Tests 37/37, typecheck, build clean.
- Overlay iterations on user feedback: fullscreen wash → hatch → per-orbit colour fill (fsImpostor* return in.color; fallback dimmed 0.08). Commits d9e14511, 8b472561, 35293f24.
- PROCESS LESSON (user, verbatim intent): visually verify the implementation is sound BEFORE measuring. Measuring first produced numbers that were artifacts three separate ways (loaded-machine floor, TBDR retire-interval, zero-area draws).
Task 2: complete @ 5a7c4696. Controller verified: parity test 1/1, typecheck clean, build clean. Review: spec compliant, task quality Approved (reviewer confirmed STROKE_PX is a true move — fragment.wesl declares it nowhere — and the parity regex handles both the u32-suffix and f32 literal forms).
Task 2: minor (deferred): constants.wesl comment density 8 comment lines vs 3 code lines, over the half-of-code budget; largely inherited from the brief's "move the comment verbatim" mandate. Revisit at the final whole-branch review if the file grows.
Parallel wave (user-requested parallel execution; plan DAG says T3/T4/T5 file sets disjoint): T3, T4, T5 implementers dispatched together @ BASE 5a7c4696, plus T2 reviewer. Shared-worktree rule given to each: stage only own files, retry on index.lock.
User addition (2026-07-31): debug-toolbar "Debug overlays" toggle to visualize the ribbon impostor. Scouted (overlay wiring mirrors debug.showDiskRadiusRing at every hop; verified by hand), authored as **Task 10** in the plan @ 646c0b13. Runs after T6, before T8's visual pass.
Task 4: implemented @ c177b0e6 (DONE_WITH_CONCERNS: renamed the brief's `half` local to `halfWidth` — `half` is a WGSL reserved word; correct call). Controller verified: `npx vite build` links the new WESL clean (skips the tsc gate, which T5's in-flight state was failing), and hand-traced the quad corner tables, the per-sample invariant and the pixel<->clip round trip. Review in flight (opus). NOTE: WESL link success is NOT WGSL module-creation success — vsRibbon is first exercised on hardware at T6/T7.
Task 6: complete @ 660ac775 (DONE). Controller verified: layer tests 14/14, FULL SUITE 6188/6188 across 939 files, typecheck clean, build clean. Confirmed floats 18/19 carry view.viewportPx (the NaN landmine), front/back indices correct, draw gated on either count, and `limit` == renderer `slots` (both ORBITAL_ELEMENTS.length) so back-packed records line up with firstInstance. Review in flight.
Task 7 MEASURED (paired A/B, alternating A-B-A-B, base worktree @ a16064b0 on :5177 vs feature on :5176, same machine/data):
  base:    MERGED 41.1 / 40.7 ms · hdr·NEAR0 7.4 / 7.3 · orbit-trails 4.3 ms real (per-layer 6.6)
  feature: MERGED 12.6 / 11.0 ms · hdr·NEAR0 1.3 / 1.1 · orbit-trails ~0.0 ms real (per-layer 0.3)
  => orbit-trails 4.3 -> ~0.0 ms real. Target was < 1.5. PASSES, pending the visual confirm below.
  CAUTION: the ORIGINAL baseline-galactic-centre-v2.txt is CONTAMINATED — its per-pass floor was 5.9 ms vs 0.3 ms now and its instrumented total 126.6 ms vs 16.0, i.e. taken on a loaded machine. Its "7.6 ms real" is inflated; quote the 4.3 ms paired figure instead. Do not re-derive the win from that file.
  MERGED fell ~29 ms, far more than orbit-trails' own cost. Working explanation: 39 additive FULLSCREEN quads were saturating tile bandwidth and inflating every other pass (star-catalog "improved" from 7.6 to ~0 real without being touched). Plausible but UNPROVEN — and a silent "trails never draw" would produce identical numbers, which is why the visual confirm gates this.
  Servers for the visual pass: :5177 = base (scratchpad/base-wt @ a16064b0), :5178 = feature snapshot (scratchpad/feat-wt @ 660ac775, stable — does NOT hot-reload while Task 10 edits land), :5176 = live worktree (WILL churn). Remove both scratch worktrees + their servers when done.
USER DECISION 2026-07-31 on Task 4's two open findings: confirm BOTH visually before fixing — no code change now. Written into the plan's Task 8 @ (plan commit after 4fcf5f71) with the trigger conditions, where to look (ends of the projected major axis) and the prepared fix for each. Do NOT silently "fix" either during a later task.
Task 3: review Approved (opus): f64 seam + rescale loop byte-unchanged, predicate is f64 and pre-narrowing, clip basis narrowed UNRESCALED in the correct (Cc, Ac, Bc)/(x, y, w, 0) layout. Mirror concern defused — test 3 inherits the pre-existing gradient-minor fixture's independently-established pose, test 4's expectation is qualitative with a self-guarding wMin > 0 precondition. The three eligibility tests form a real discrimination triangle (always-false fails t2, wMin-only fails t4, extent-only fails t3).
Task 3: OPEN TUNING ITEM for T7 — `extent` includes |Cc.xy|, the centre's distance from the NDC ORIGIN, so a small orbit far off-screen (>~20 NDC off-centre = ~10 screen-widths) scores a huge extent and is routed to the FULLSCREEN path: the expensive path for something that rasterizes nothing. Correctness-safe, perf-backwards. If T7 misses < 1.5 ms, log the ribbon/fallback split first — this is the prime suspect, ahead of SEGMENTS.
Task 3: minor (deferred): three restatements of the clipBasis layout (header section, field docblock, @returns) — the header section earns its place least; test file now has 3 copies of the edge-on pose recipe (hoisting was blocked by the must-stay-untouched regression test).
Task 3: comment fix dispatched — RIBBON_MAX_EXTENT_NDC's fill-cost parenthetical was backwards (20 NDC = 21,600 px vertical on 4K, already past the ~18,000 px break-even it claimed to stay inside; controller re-derived true break-even ~16,500 px = ~15 NDC). Value 20 UNCHANGED, comment corrected + flagged as a tuning knob. Same dispatch fixes io.wesl's stale "nothing to interpolate across the fullscreen triangle" prose.
Task 3: complete @ 4fcf5f71 (DONE). Controller verified: `npm test -- composeOrbitConic` 8/8 pristine; RIBBON_MAX_EXTENT_NDC = 20; predicate is `wMin > 0 && extent <= RIBBON_MAX_EXTENT_NDC`, and the wMin clause short-circuits so the division's Infinity/NaN cannot yield a wrong true. Review in flight (opus). Implementer flagged: its numeric expectations for 2 of 4 tests came from an OFFLINE Node reimplementation, not the repo path — reviewer asked to judge test independence.
Task 4: review returned "Needs fixes" (opus). Verdicts:
  - Important #1 (vs pipeline would fail validation because the shared struct widens vs's declared locations to 1..10) — CLOSED by controller: T5's INSTANCE_ATTRIBUTES supplies locations 1..10 at 0..144. Non-issue.
  - Important #2 OPEN, PLAN-MANDATED: the offset-ribbon self-intersects wherever the projected ellipse's radius of curvature (B^2/A) drops below halfWidth (>=4.5 px), so adjacent quads overlap at the two turning points and the one/one blend DOUBLE-ADDS -> a bright pip. Violates the plan's "must abut exactly, never overlap" and its DoD "no segment joint reads brighter". Trigger ~15:1 projected aspect = near-edge-on, which fragment.wesl's own header calls a primary viewing condition. Escalated to the user (this is the plan's formula, not an implementation slip).
  - Important #3 OPEN, PLAN-MANDATED: at a fold the quad degenerates to a fan whose tip half-height falls below STROKE_PX once sag > 18 px (projected long axis > ~8400 px, i.e. the Moon close-up regime the plan names) -> a sub-pixel nick in the stroke cap. Escalated with #2.
  - Minors: vertex.wesl comment budget (58 comment lines vs 68 code, header 33 lines, mostly inherited); io.wesl:41-43 prose now stale ("nothing to interpolate across the fullscreen triangle") since vsRibbon shares VSOut; sag is the ~4x-conservative bound only while neighbour steps stay within 4x of each other.
  - Controller note for T6: orbitTrailRenderer.ts:47's comment still calls floats 18/19 "pad" — T6 makes them the viewport and must fix that line. T6 MUST write a non-zero viewport there or vsRibbon divides by zero.
Task 5: complete @ 244baac3. Controller verified: `npm test -- orbitTrailRenderer` 12/12 pristine; INSTANCE_FLOATS 40 / STRIDE 160, entryPoints vsRibbon+vs over one shared fs, RIBBON_SEGMENTS imported not hard-coded, both draws carry the specified firstInstance. Review: spec compliant, Approved (reviewer confirmed the two pipelines share the fragment module by REFERENCE, `.toBe` not `.toEqual`, and that the growth test uses counts that don't individually equal slots).
Task 5: reviewer's one open item CLOSED by the controller — io.wesl's OrbitInstance declares locations 1..10 as contiguous vec4<f32>, which lands them at 0/16/32/48/64/80/96/112/128/144, matching INSTANCE_ATTRIBUTES exactly.
Task 5: minor (deferred): OrbitTrailRenderer.d.ts header is 18 lines vs the ≤10 budget (16 of them pre-existing); `slots` is now a float division, so a non-multiple-of-40 array yields a fractional slots compared against integer counts — a fuzzier guard than before, untested.
Two subagents (T2 reviewer, scout) died to a stream-watchdog stall during a model-availability blip and were re-dispatched; if an agent goes silent ~10 min, re-dispatch rather than wait.
## AMENDMENT 2026-08-01 (user decision: fallbacks are the problem)
- User: "the fallbacks are not cheap, that's the whole problem" + "i measured and its terrible" — camera-inside-orbit is the NORMAL solar-system regime; N fullscreen additive quads saturate tile bandwidth. Fallback must go.
- Key insight: clip basis is exact for ALL E (P·V linear); only the screen division breaks at w<=0. So: near-plane clamp in vsRibbon (T11), then delete the whole fallback (T12). No new instance data; record layout untouched.
- Plan amended (Tasks 11+12, DoD, DAG; amendment notes on T3/5/6/7/8/10). Report: plan-amendment-report.md.
Task 11: complete @ 43a64e97. Controller verified: npx vite build links clean. Implementer concern (deferred to T12 visual check): clamped/boundary path drops the sag widening term (needs 3 points; boundary geometry is off-screen so held harmless). Review in flight (opus).
Task 12: dispatched (sonnet) with CARVE-OUT: fragment.wesl untouched — controller's diagnostic probe is uncommitted there; fsImpostorFallback deletion rides the controller's probe-revert commit. Renderer stops REFERENCING it, leftover entry point harmless.

## FRAGMENT STROKE BUG (user report 2026-08-01, confirmed on hardware)
- A/B at solar-system perf pose (glow layers off via role-based checkbox toggles — text-based clicks HIT THE GPU-TIMINGS ROWS, same labels; use getByRole('checkbox')): orbit-trails off => frame pitch black => ALL ~25-30 straight criss-cross lines ARE the trail shader. 9 orbits => ~3 lines each: real front-arc (a sky-crossing line from inside is legit) + horizon line + third family TBD. At least one line renders DASHED.
- Prime suspect (from code read, NOT yet confirmed): fragment.wesl horizon rejection `if (qStep.z <= 0.0 || abs(rStep - 1.0) >= REFINE_TOL)` is NaN-UNSAFE — NaN fails both comparisons and falls through to paint at dist~0 => full-brightness line; alternating finite/overflow along the line would produce the dashes. The stroke discard above it uses the NaN-safe `!(dist < STROKE_PX)` pattern; the horizon check inverted it.
- TINT PROBE live (uncommitted, fragment.wesl fs tail): conic-locus pixels (|r-1|<0.5) paint in.color full-bright; anything else paints MAGENTA. Production return preserved in a comment. USER is doing the visual verdict (their ask: "instead of doing a lot of visual testing yourself, let me do it"). Magenta => NaN path confirmed; all-coloured => conic composition/CPU side.
- REVERT the probe + delete fsImpostorFallback in ONE controller commit after diagnosis.
- Captures this session: jobs/19fc0a29/tmp/ss-{full,crop,dark-production,dark-overlay,pair-trails-on,pair-trails-off}.png; scripts scratchpad/ss{Trails,Dark,Only,Pair}.mts.
Task 11 review (opus): NEEDS FIXES.
  - Important #1: the 4-sample fast-path gate (CONTROLLER'S OWN dispatch instruction, not the implementer's invention — reviewer's Finding 2 "nonexistent source" is explained: the source was the dispatch prompt) breaks the per-sample abutting invariant ON-SCREEN for the both-front-with-behind-neighbour case: segment A (neighbour behind) takes the chord path with NO clamping and no sag, segment B (fast) uses central-difference + sag => mismatched shared corner => pip/gap at the two joints flanking every fold. Fix per reviewer: per-CORNER-SAMPLE classification (pure function of sample index): si-1,si,si+1 all front => central difference (unchanged); si front + neighbour behind => one-sided tangent toward the FRONT neighbour (per-sample rule, both quads agree); si behind => clamp along this segment's chord (off-screen, mismatch OK); both behind => degenerate.
  - Minor #4 (fold into fix): one comment line at the epsilon — 1e-6 is load-bearing (>= ~3x f32 ulp margin keeps clamped w from rounding to <= 0; clipToPixel would divide by zero).
  - Minor #3 (defer, first suspect if T12 visual shows a gap at the fold): clamped path has no sag/curvature term where screen curvature is largest.
  - Minor #7 (defer): sI drops % SEGMENTS (harmless — don't "fix" back); 4 ribbonClip evals/vertex (~33% more vertex ALU, negligible).
  - Reviewer's open checkbox (vite build) already run clean by controller post-T11.
  - FIX ROUND 1 QUEUED BEHIND T12 (same file vertex.wesl; T12 implementer mid-flight). Resume T11 implementer with findings + "file changed since — re-read" once T12 lands.
Task 12: complete @ cca2ffc0 (DONE). Controller verified: targeted 32/32, FULL SUITE 6186/6186 (939 files; count down 2 = deleted fallback tests), typecheck clean, vite build clean. fragment.wesl carve-out honoured (fsImpostorFallback survives there only; no .ts references — implementer grep). Review dispatched (sonnet).
Task 11 fix round 1: dispatched (resumed original implementer) AFTER T12 landed — per-corner-sample classification per the review, epsilon margin comment folded in. vertex.wesl changed under it by T12 (vs deleted, header rewritten); told to re-read.
LIVE on :5173 right now: T12 state + controller's tint probe => user reports trails look THICK — that is the probe (full brightness, no taper/fade), NOT the geometry; explained to user. Still awaiting user's magenta-vs-coloured verdict for the fragment fix.
Task 12 review (sonnet): APPROVED, spec compliant on every constraint. Reviewer independently verified (live-tree greps, not just the diff): zero dangling references to any deleted symbol; record layout byte-identical with only indexing changed; single draw firstInstance=0 matches the contiguous pack; debug draw mirrors production counts; new vertex.wesl header 8 lines; packing landmine comment survives; gradient-minor + reprojection test bodies untouched (only an enclosing describe renamed — cosmetic minor). No fixes needed.
Task 11 fix round 1: landed @ d30c8ec3 (per-corner-sample classification + epsilon comment; vertex.wesl only). Controller verified vite build links clean. Scoped re-review dispatched (resumed the original opus reviewer, scope = findings addressed + new defects only).
User probe status: user asked "what dashes" — the dashed lines were in the controller's PRE-T11/T12 captures (old fullscreen path; crops at jobs/19fc0a29/tmp/dash-{blue,orange}.png). Post-T12 the fragment only runs inside the ribbon hull, so the spurious-line families (horizon/phantom) are structurally unrasterized — if the user sees no dashes/extra lines now, the fragment fix reduces to NaN-safing the horizon check as insurance in the probe-revert commit.
USER PROBE VERDICT (2026-08-01): magenta CONFIRMED — most visible edge-on, and beside the dashes; occurs deep-zoomed well inside an orbit (near Sgr A*, near Earth). Diagnosis: near-fold overflow (invZ2 chain hits 1e30+; finite-garbage => wrong discard = dash gaps; NaN => slips the direction-inverted horizon check and paints = magenta). ONE root cause, two symptoms.
Fragment fix dispatched (sonnet, fragment.wesl only): algebraically-identical overflow-free forms — u=q.xy, z=q.z, ecc=atan2(u.y,u.x), gradNum from the affine minors (gradR = gradNum/(z^2*uLen)), dist = |uLen-z|*z*uLen/|gradNum| (all O(1) factors), NaN-safe keep-form horizon check !(qStep.z>0 && |rStep-1|<TOL). Same commit: probe REVERT + fsImpostorFallback deletion (T12 carve-out completion). Controller derivation in the dispatch; reviewer must re-derive.
USER REPORT: near Earth, other orbits acceptable; EARTH'S OWN orbit almost entirely gone until zoom-out beyond the Moon's orbit. Diagnosis: camera-on-the-curve case = fold under-coverage — T11 review Finding 3 EXACTLY (clamped/one-sided paths have no sag where screen curvature is largest; hull is a 4.5px chord band, true curve sweeps a huge arc; fragment never rasterized => geometry-side loss, fragment fix cannot restore it). PLANNED FIX (T11 round 2, after the scoped re-review returns, same file): guaranteed-coverage width on fold segments (viewport-bounded), cost local to the orbit being stood on. Design delta vs the T11 brief's STROKE+MARGIN clamped width — record for T9/plan close-out.
Task 11 fix round 1 re-review (opus, scoped): APPROVED. Finding 1 ADDRESSED structurally (per-corner-sample classification; reviewer hand-verified corner agreement in all 4 neighbour configs + wrap). Finding 4 ADDRESSED. New minors: (1) branch-3 chord flipped for offset==1 => latent bowtie, one-line select fix; (2) zero-chord both-neighbours-behind pinch is reachable-but-off-viewport, wants a comment clause; (3) stale clipToPixel comment says 2 callers, now 3; (4) module header still 27 lines (cca2ffc0's, out of scope). Minors 1-3 fold into round 2; minor 4 rides T9/final review.
DESIGN DECISION (controller, driven by user's Earth-orbit-gone report): naive fold widening is WRONG under additive blend — same-instance overlapping quads double-paint (the abutting invariant is the only thing preventing it; a viewport fold quad would overlap the far arc's quads). Round 2 = E-OWNERSHIP: vertex passes the segment's E-range as a flat varying; fragment discards pixels whose ecc is outside the quad's own half-open range => exactly one owner per pixel (flat varyings => bit-identical ecc across quads => deterministic boundary winner) => overlap harmless => fold-adjacent samples widen to a conservative viewport-cover bound. Consequences: stood-on orbit stays complete; predicted fold double-add pip + cap nick dissolve structurally (update T8 checklist expectations); abutting exactness becomes perf-tightness rather than correctness-load-bearing (simplification candidate — note for T9/plan). Cost: fold quads ~viewport-sized for the stood-on orbit only.
Round 2 dispatch BLOCKED on the in-flight fragment overflow fix (same file). Then: vertex.wesl + fragment.wesl + io.wesl (varying count 9->10 — varying budget noted).
Fragment overflow fix: landed @ 896be457 (probe reverted, fsImpostorFallback deleted, overflow-free chain + NaN-safe keep-form). Controller verified vite build clean. Review dispatched (opus, must re-derive every identity; scoped to the commit — round 2 edits the same file concurrently).
T11 round 2 dispatched (same implementer, 3rd resume): E-ownership (VSOut @location(9) flat eOwn=(eLo,eHi,tested); tested quads discard pixels outside their half-open E-range; bit-identical boundaries via TAU*i/SEGMENTS) + viewport-cover widening on one-sided/clamped corners (2x max distance to the four viewport px corners) + re-review minors m1-m3. Files: vertex.wesl, io.wesl, fragment.wesl (ownership block only). Expected worst-case artifact: <=~4.5px sliver at the two tested/untested joints — T8 visual.
Fragment overflow fix review (opus): APPROVED. All six identities re-derived and confirmed exact for z>0; overflow bound analysis confirmed (largest intermediate ~1e13 vs f32 3.4e38; old chain had THREE overflow sites); NaN paint-through mechanism confirmed real and closed; the one guarded-corner divergence (u=0 conic centre) resolves to identical coverage. Minors deferred to final review/cleanup: (1) orphaned "Both" in debug-lens comment; (2) touched-region comment ratio 2.6:1 — ~10-line trim available; (3) report overstated the comments.md carve-out (header lacks the required first-line marker); (4) "O(1)" claims imprecise — state actual bounds (u ~1e4, gradNum ~1e8, max ~1e13). Pre-existing stale header ("fullscreen triangle covers the whole viewport") wants the end-of-plan header sweep.
Reviewer's closing note matches the standing rule: symbolic+range analysis done, hardware confirm of the dash fix still owed by the user's visual pass at the on-orbit pose.
T11 round 2: landed @ 6e1c3e31 (eOwn flat varying @location(9); tested-quad half-open E-ownership discard; viewport-cover widening on one-sided/clamped corners; minors m1-m3 folded). Controller verified: vite build clean, renderer+layer 27/27. Scoped review dispatched (same opus reviewer, adversarial brief on ownership failure modes: iso-E divergence gaps, TAU wrap, degenerate eOwn, flat interpolation).
T11 round 2 review (opus, scoped): APPROVED, conditional on two follow-ups that map onto existing gates:
  (a) Important: fold widening reintroduces ~4 viewport-scale quads per stood-on orbit (~36 at solar-system) — mitigated by early ownership discard (mat3+length+atan2 per rejected pixel, no blend writes) but NET COST UNKNOWN FROM SOURCE => T7 must measure THIS HEAD, solar-system is the scenario that moves.
  (b) Important: widened quads cover only the forward half-plane of their anchor => residual gap mode IMMEDIATELY BESIDE a fold if the arc turns >~90 deg on-screen within one segment. ADDED TO T8 CHECKLIST; prepared fix: extend widened corners along +/-tangent by foldHalfWidth (ownership makes it double-add-safe; decide together with (a)).
  Reviewer also PROVED tested <=> contains-widened-corner as an exact equivalence, and confirmed the fragment-E == vertex-E premise against composeOrbitConic + its existing test. Boundary determinism verified incl. numeric wrap check.
  Minors: (1) sub-ulp wrap seam at E=0 (fix: compare against TAU/f32(SEGMENTS)); (2) ownership rationale triplicated across three files; (3) twoPi vs TAU spellings; (4) USER BRIEFING ITEM: debug overlay now shows viewport-sized fold hulls — expected, not the fallback returning.
Cleanup dispatch (sonnet) in flight: round-2 minors 1-3 + fragment-review minors 1/2/4 (orphaned "Both", comment trim, real bounds instead of O(1)) — single commit, three shader files, no logic changes outside the wrap-seam bound.
T8 CHECKLIST DELTA (accumulate for the user's pass): + solar-system 9 ribbon hulls/no wash; + standing-on-Earth's-orbit trail complete; + gap-immediately-beside-fold check; + overlay fold hulls are viewport-sized BY DESIGN; two original fold artifacts (pip, cap nick) likely dissolved by E-ownership — verify absence rather than presence.
Cleanup: landed @ 30ecf987 (wrap-seam bound = TAU/f32(SEGMENTS); twoPi->TAU; singular debug comment; real magnitude bounds replace O(1) claims; ownership rationale deduplicated to fragment.wesl; fragment comment delta -6). Controller verified: build clean, FULL SUITE 6186/6186. Branch pushed (a16064b0..30ecf987 on PR #536).
BRANCH FROZEN FOR T8 (user visual pass). Session commits since ba8c69ba: 43a64e97 (clamp) -> cca2ffc0 (delete fallback) -> d30c8ec3 (per-sample fix) -> 896be457 (fragment overflow + NaN-safe) -> 6e1c3e31 (E-ownership + fold widening) -> 30ecf987 (minors). After user sign-off: T7 re-measure AT THIS HEAD (wall-clock probe + paired A/B; solar-system is the scenario the fold quads move), then T9 (final whole-branch review on most capable model, entanglement radar, /feature-done — plan close-out includes the stale-header sweep + comments.md first-line marker decision + module header budget).
USER VISUAL PASS, round 1 findings (2026-08-01):
  BUG A (fold triangle): near-body fold quad = forward-half-plane band; flips with camera rotation; trail sometimes outside it => disappears. = round-2 review Important 2, confirmed on hardware. FIX DISPATCHED (T11 round 3, same implementer, vertex.wesl only): box-extend widened corners along +/-tangent by foldHalfWidth; shared-corner mismatch accepted ONLY between tested quads (ownership makes it harmless; tested<=>widened equivalence proven in round-2 review).
  BUG B (sub-pixel dotted trails close to Sgr A*): user verified hull band is wide enough — fragment-side. Candidates: (1) Newton REFINE_TOL eating genuine pixels at this scale, (2) triangular stroke profile + HDR exposure (apparent width), (3) dist inflated (gradNum dot-product cancellation — a DIFFERENT cancellation than the minors fixed). STROKE-FATE PROBE live on :5173 (uncommitted fragment.wesl): faint blue = in-hull off-stroke, GREEN = Newton kill, white/yellow/red = painted dist bands (<0.5/<1.5/<2.5px). User reads the colour at the GC close-up; green=>Newton, full-width band=>profile/exposure, sub-pixel band in blue=>dist numeric. REVERT before commit (production tail preserved in probe comment).
T11 round 3 (BUG A fix): landed @ 97f31158 (tangent box-extension on widened corners, sign per quad-end via offset; corner mismatch between tested quads covered by E-ownership — tested<=>widened equivalence). Controller verified build links (probe still uncommitted on top). Scoped review dispatched (same opus reviewer; adversarial no-gap probe of the box-cover claim).
In flight: round-3 review; research agent (orbit-trail rendering prior art); USER: stroke-fate probe read at the GC (BUG B triage).
USER: overlay whiteout after round 3 (viewport box hulls x additive full-strength fill). CONTROLLER EDIT (inline, uncommitted, rides the probe-revert commit — deviation from delegate-all-edits noted; tight live-inspection loop): fsImpostorRibbon now mirrors fs's behind-camera + E-ownership discards for tested quads (per-orbit contribution <= 1 layer/pixel; tested hull shows as its owned E-WEDGE) and dims to 0.2. Overlay semantics change: shows where production MAY paint per orbit, not raw geometry. Probe still live in fs (blue branch removed earlier — off-stroke discards; green/white/yellow/red fate colours).
Research agent: delivered; annotated list written to docs/references/orbit-trail-rendering.md (uncommitted; docs-in-PR question at close-out). Headline: no public analytic-conic orbit rendering — field tessellates polylines; sub-pixel clamp+compensate is folklore with no rigorous source; Cesium near-plane clipping = our T11 in eye space; wwwtyro Part II = our double-paint problem vertex-side.
USER PROBE READ (BUG B TRIAGE, decisive): GC trails nearly all GREEN + sub-pixel; zoom-out heals closest first (yellow with red fringe = healthy band); Neptune-from-Earth same failure, smaller planets fine. DIAGNOSIS: SECOND f32 cancellation — gradNum = u.x*A_s + u.y*A_t: true sum shrinks ~1/(projected size) while terms stay large => catastrophic cancellation for hugely-projected orbits. Corrupts |gradNum| (band -> sub-pixel noise dots) AND direction (Newton step misses -> green kills). Distinct from the minors cancellation (inside each affine form); cannot hoist to CPU (u is per-pixel). Scaling matches all three observations.
FIX (T13, dispatched, fragment.wesl only, NO commit — controller owns the commit bundling probe revert): compensated evaluation via Dekker twoProdErr + Knuth TwoSum (no fma — WGSL fma not guaranteed fused). No layout/varying changes. NOTE: CPU-hoisting the quadratic coefficients was CONSIDERED AND REJECTED: the monomials re-cancel at f32 evaluation (the cancellation is inherent to the tiny-gradient regime), so per-pixel compensation is unavoidable — record this so nobody "simplifies" back.
T11 round 3 review: NEEDS FIXES (round 4 dispatched, vertex.wesl, parallel with T13 — disjoint files):
  - Important A: foldHalfWidth measured the viewport box in the WRONG SPACE (clipToPixel space is CENTRED: [-W/2,W/2]x[-H/2,H/2]; code measured (0,0)..(W,H)) => up to 2x undersized per quadrant. Fix: 2.0 * length(abs(anchorPx) + 0.5 * viewportPx).
  - Important B: per-end local frames — quad can miss the viewport entirely when end frames diverge >=90deg (reviewer constructed 2 cases). Fix: one shared frame per quad from its anchor-to-anchor axis (corner = own + h*axisOut + side*h*axisNormal); tangentSign machinery removed. Reviewer PROVED containment for the shared-frame trapezoid given fix A.
  - Reviewer self-corrected: fix A's bug was pre-existing from 6e1c3e31 (their round-2 pass missed it).
Overlay whiteout fix (controller inline, uncommitted): fsImpostorRibbon mirrors behind-camera + ownership discards + 0.2 dim.
Plan amendment for T13 + fold-geometry rounds: PENDING (will batch one amendment note covering rounds 3-4 + T13 once both land).
T11 round 4: landed @ 89e15a5a (centred foldHalfWidth + shared anchor-axis frame; tangentSign/chord-select machinery deleted). Build clean. Scoped re-review dispatched (same reviewer re-running their own counterexamples).
T13 (compensated gradNum): edit landed UNSTAGED in fragment.wesl per instruction (controller owns the commit bundling probe revert + overlay fix). Build clean; LIVE on :5173. Awaiting: user's GC/Neptune probe re-read (green should flip to white/yellow/red at full width), user's fold rotation re-test, round-4 re-review.
Round 4 re-review: Finding 1 ADDRESSED; Finding 2 PARTIALLY — axisNormal inherits axisOut's per-end sign flip => fold quads BOWTIE, ~650px wedge uncovered (reviewer worked example). One-line fix specified verbatim + containment RE-PROVED with it by the reviewer BEFORE application. CONTROLLER APPLIED INLINE @ a218d522 (both fold branches; sideOut = select(-side, side, offset == 1u)) — inline deviation justified: reviewer-pre-verified verbatim one-liner, live user loop; confirmation glance requested from the reviewer. Build clean.
With a218d522: T11's fold geometry is containment-proven (centred R exact + shared frame + side flip). Outstanding: reviewer confirmation glance; user re-looks (GC colours, Earth rotation); then fragment batch commit (probe revert + T13 + overlay) + push + T8 sweep + T7 measure.

## COMPACT CHECKPOINT 2026-08-01 (user compacting; this section is the resume map)
STATE: branch worktree-orbit-trails-perf, HEAD a218d522 (sideOut fix, reviewer-APPROVED verbatim; T11 fold geometry containment-proven). PR #536 draft; pushed through 30ecf987 — a218d522 NOT yet pushed. Dev :5173 live (user's tab), :5176 base. NO agents in flight (all completed). Task list: #15 pending (fragment batch commit), #7/#8/#9 pending.
UNCOMMITTED (fragment.wesl only, 3 strands in one file): (1) stroke-fate probe in fs tail (green=Newton kill, white/yellow/red=dist bands; production tail preserved in probe comment); (2) T13 Dekker compensatedDot2/twoProdErr for gradNum — NOW KNOWN DEAD (see verdict) — REMOVE rather than commit; (3) fsImpostorRibbon ownership+0.2 dim overlay fix — KEEP, commit. EFT verdict probe was added and fully REVERTED after one capture.
EFT VERDICT (hardware, jobs/19fc0a29/tmp/eft-crop.png): solid CYAN => Dawn/Metal fast-math destroys Dekker/TwoSum float identities. ALL error-free-transformation approaches in WGSL are DEAD ON THIS TOOLCHAIN (fma equally suspect). T13's compensation never executed — explains "still green everywhere".
USER HARDWARE FINDINGS (latest round): still green trails everywhere (explained above); blue+orange triangles popping = OVERLAY ownership wedges flickering with fold-membership changes (overlay-only); orbit-trails >30ms per-layer (instrument-inflated but real growth) = fold BOXES: viewport-sized quads x ~4 per crossing x many inside-orbits at GC; overlay doubles it.
NEXT ACTIONS (in order):
 1. Numeric fix redesign (BUG B, green/sub-pixel): EFT dead => leading candidate is SCREEN-SPACE DERIVATIVE gradient: dist = |r-1|/length(vec2(dpdx(r), dpdy(r))) with r evaluated stably (r O(1) => cancellation-safe; ~1% precision; 2x2-quad quantization). BONUS: deletes the minors machinery entirely (minorS/minorT exist only for the gradient) — record layout shrink, composeOrbitConic simplification. Needs hardware validation FIRST (tint-probe the derivative-based dist vs current at a healthy pose before committing to the refactor). CPU quadratic hoist REJECTED (monomials re-cancel at eval, in ledger above).
 2. Fold-box fill catastrophe (BUG A perf): wedge-clip design — clip each fold quad to its own 1/96 E-wedge (bounded by the two iso-E lines through its sample anchors; lines pass through the projected conic centre = image of Cc, available per-instance). ~25x fill reduction. DESIGN-REVIEW FIRST with the opus reviewer agent BEFORE implementing (5 hardware rounds is enough churn). Edge case: Cc.w ~ 0 (centre image at infinity) needs a fallback.
 3. Then: remove dead Dekker code + revert stroke-fate probe + commit fragment batch (overlay fix + whatever numeric fix survives) + push; user re-look; T8 sweep; T7 measure at final HEAD (wall-clock probe; solar-system + GC-INSIDE pose — the >30ms regime — must be measured, not just the outside pose).
REVIEWER AGENT: opus reviewer has deep context (5 rounds, proofs) — reuse for the wedge-clip design review. Implementer agent likewise (5 rounds context).
Research refs: docs/references/orbit-trail-rendering.md (uncommitted) — Cesium near-plane clip, Rougier AA, sub-pixel clamp+compensate folklore (relevant to final thin-line treatment), Godot df64 (note: their trick presumably survives because GLSL, not WGSL fast-math — do NOT cite as counterevidence).

## POST-COMPACT ROUND 2026-08-01: derivative gradient VALIDATED, production dispatch + wedge design review in flight
DERIVATIVE PROBE (hardware, controller captures jobs/19fc0a29/tmp/dd-{gc,ss}.png via scratchpad ddProbe.mts — scratchpad has playwright's node_modules, job tmp does NOT):
  dd-gc.png (galactic-centre 5e-7): every S-star ellipse a continuous full-width white/yellow band — ZERO green, zero sub-pixel dotting. The exact pose that was "nearly all green + sub-pixel" under the analytic gradient.
  dd-ss.png (solar-system inside-orbits): painted arcs full-width and healthy; separate all-green straight lines = horizon families correctly killed by the Newton check (they paint green only in the probe; production discards). No green ON painted arcs.
  => dist = |r-1|/length(vec2(dpdx r, dpdy r)) VALIDATED at both failure poses + healthy control. Dawn uniformity analysis accepts dpdx after the demote-only discards (build + createShaderModule clean).
DISPATCHED (parallel, disjoint concerns):
  1. Implementer (sonnet, bg): brief derivative-gradient-brief.md, report derivative-gradient-report.md. Commit 1 = fragment.wesl production form (probe revert + Dekker deletion + derivative gradient + overlay ownership fix rides along + header degradient/defullscreen reword). Commit 2 = minors machinery deletion end-to-end (composeOrbitConic m1..m6 + returns, layer floats 20..27 removed => clip basis 20..31, record 40->32 floats / stride 128, attrs renumber 6/7/8 @ 80/96/112, io.wesl OrbitInstance+VSOut renumber (eOwn 9->7), vertex forwarding, both layout tests, .d.ts, composeOrbitConic gradient-minor regression describe DELETED with its subject). Production tail recovered verbatim from git show 30ecf987, gradient-coupled lines swapped to gradPx / sign(r-1).
  2. Wedge-clip design review: opus reviewer RESUMED via SendMessage (5-round fold context). Brief: iso-E-lines-through-image-of-Cc wedge clip, ~25x fill cut; vet half-line vs double-wedge under z-sign, direction construction w/ behind-camera samples + clamped anchors OFF the iso-E line, Cc.w~0 fallback, R bound, interaction with ownership + containment proofs, 6-vert corner mapping. Verdict before ANY implementation.
Task list: #16 (BUG B) in_progress — probe done, production in flight; #17 (BUG A) created — design review in flight. After both land: controller runs tests/typecheck/build, review of the implementer's commits (fresh opus), THEN plan amendment note (derivative gradient + wedge clip + minors deletion), user re-look, T8, T7 (solar-system + GC-INSIDE pose).
NOTE: minors deletion removes the plan-DoD-referenced "gradient-minor hoist" regression test WITH its subject — record in the plan amendment, not a silent test deletion.
WEDGE DESIGN REVIEW RETURNED (opus reviewer, full verdict in tasks output a6d45bc5622aadbfb / relayed to user): SOUND WITH CHANGES. Corrected construction (reviewer-derived): apex p0 = clipToPixel(cc) CONFIRMED; direction dir(E) = sign(cc.z) * normalize(half ⊙ D(E)) with D(E) = cosE·A + sinE·B, A = cc.z*ac.xy − ac.z*cc.xy, B = cc.z*bc.xy − bc.z*cc.xy (division-free ⇒ stable for behind-camera samples); SINGLE wedge (sign fixed per instance by sign(cc.z) — no double wedge; antipodal window pixels are the r<0 branch, ownership discards them). Wedge angle Δ ≠ window's 3.75°: d(argD)/dE = det[A,B]/|D|², can sweep ~π near edge-on ⇒ unwrap along sign(det[A,B]), box fallback past a Δ threshold. R ≥ foldHalfWidth(p0)/cos(Δ/4) with a 2-triangle FAN (apex, far(lo), far(mid) / apex, far(mid), far(hi); far(mid) from the unwrapped mid-angle, NOT normalize(lo+hi)). Angular margin on every wedge (removes bit-identity obligation at shared samples). MANDATES: F1 — A,B are difference-of-products minors that cancel at the near-edge-on pose (same class as the old minors) ⇒ CPU f64 hoist, +4 floats to the record (layout/attrs/io/pack/parity-test move), own commit before the geometry change; scale-free cc.z≈0 detection (abs(cc.z) <= δ·(abs(cc.z)+length(vec2(ac.z,bc.z)))) ⇒ box fallback; F5 per-quad purity of every wedge quantity (4 rounds of regressions were per-corner leaks); F6 wedge corners emit w=1 NDC directly; F2 win is pose-dependent (apex >~15 viewport diagonals away ⇒ no reduction) ⇒ commit to MEASUREMENT not "25×", add one apex-far pose; F4 CHEAP WIN FIRST: only straddling quads need unbounded coverage — both-endpoints-front tested neighbours keep the narrow ribbon (~2× cut, composes, same accepted-sliver argument), land + measure before the wedge; F7 overlay will show wedges — brief the user. Scoped-review checklist (11 items) in the verdict — reuse verbatim when the implementation review is dispatched. Round-4 box containment proof does NOT transfer; demand a written wedge ⊇ (paintable ∩ window ∩ viewport) superset proof.
SEQUENCE for BUG A (post in-flight commits): (i) F4 narrow-straddle commit + GC-inside measure; (ii) f64 A,B hoist commit (record 32→36 floats); (iii) wedge geometry commit; scoped review per checklist; measure GC-inside + apex-far.
DERIVATIVE COMMITS LANDED: bce92c55 (fragment production form; probe reverted, Dekker deleted, overlay ownership fix rides along, header reworded) + 83ce420f (minors machinery deletion end-to-end; record 40→32 floats / stride 128, clip basis floats 20..31 @ locations 6/7/8, VSOut eOwn 9→7; AMENDED by controller: implementer left two unused imports in composeOrbitConic.test.ts — inline 2-line deletion, noted as controller edit). Controller verified: targeted 31/31, FULL SUITE 6185/6185 (−1 = deleted gradient-minor regression test with its subject), both typechecks, vite build clean. Task #15 (fragment batch) COMPLETE — bce92c55 IS that batch.
IN FLIGHT: (a) review of bce92c55+83ce420f (fresh opus; package review-a218d522..83ce420f.diff; must re-derive sign equivalence, NaN paths, dpdx uniformity, layout renumber byte-for-byte, tail-vs-30ecf987 diff, dead-symbol grep); (b) F4 dispatched to the resumed T11 implementer (vertex.wesl only): straddling segments keep the viewport box, MIXED corners (own+partner front, outer neighbour behind) get a narrow one-sided-chord ribbon, STROKE+MARGIN no sag, pure per-sample rule so the mixed/untested joint still abuts; tested/eOwn untouched. After F4 lands: build + GC-inside measure BEFORE deciding the wedge (F2 pose-dependence may make the wedge unnecessary or smaller-win than 25×).
DERIVATIVE REVIEW RETURNED (opus): spec PASS on every requirement (independently verified: tail delta vs 30ecf987 exact; sign equivalence proven for z>0; every NaN branch fail-closed incl. the max(NaN,1e-30) indeterminacy — both outcomes discard; dpdx at top-level statement list so nothing for uniformity analysis to reject, and NOTE vite build is NOT evidence of Tint acceptance — the hardware captures are; layout consistent at all six declaration sites, VSOut gapless 0..7). Quality APPROVE, no blockers. Fix round 1 dispatched (resumed sonnet implementer, prose-only, controller will self-verify — no scoped re-review for comment/doc edits): (1) fragment.wesl ~107-110 stale overflow comment now contradicts `r = uLen/z` — requalify (atan2 needs no divide; r's divide accepted, fails closed); (2) docs/backlog/2026-07-18-orbit-trail-residual-speckle.md stale 4 ways (minors return, deleted test cite, fullscreen triangle, two-sum/Kahan direction now hardware-dead) + note speckle may be RESOLVED by the derivative gradient — re-check at visual pass.
T8 CHECKLIST DELTA (from the review): + fold-line band artifact — where a 2x2 derivative quad straddles the fold, dpdx(r) blows up ⇒ dist→0 ⇒ a ~1-quad-wide UN-ANTIALIASED full-brightness band along the fold line is predicted for on-stroke pixels (off-stroke pixels are saved by the Newton check, now load-bearing for this SECOND reason). Cheap to eyeball; if objectionable, needs its own round.
Deferred minors (final review): fragment.wesl 70-line module header trim (Sampson + f32-safety sections argue the same point twice) — own commit; plan line ~255 still pins the 40-float layout (trips /feature-done — fold into the plan amendment); fsImpostorRibbon ASCII hyphens. PROCESS: implementer report FABRICATED its precedent numbers (cited "78 comments/119 code @ 30ecf987", actual 144/53) — conclusion survived, figures do not; matches feedback_subagent_reports_fabricate_citations. Do not propagate.
F4 LANDED @ 1cacdfcb (mixed corners → narrow one-sided-tangent ribbon, straddling keep the box; implementer hand-verified the prevFront/nextFront dichotomy is structurally forced). Prose fix round LANDED @ 5e91362e (controller-verified diff: comment requalified + speckle backlog corrected 4 ways). Build clean.
MEASUREMENTS AT 1cacdfcb (single-run harness, :5173; NOT the paired-A/B T7 gate — that still runs at final HEAD):
  galactic-centre: orbit-trails 0.3 per-layer / ~0.1 ms REAL; MERGED 11.6 ms, 60fps with 30% headroom. The >30 ms fold-box regime is GONE.
  solar-system: orbit-trails 2.4 per-layer / 1.2 ms REAL == pre-branch baseline (1.3) while now drawing every inside-orbit as a ribbon. (MERGED 46.7 dominated by foreground:0·NEAR0 6.9 — other layers, not trails; slot-sum caution applies.)
  VISUAL PROOF at same HEAD (dd-gc.png, dd-ss.png re-captured, production shaders): GC all S-star ellipses thin/continuous/coloured with fade — the 0.1 ms is real drawing, not zero-area. SS arcs render from inside; TWO DASHED lines visible (one orange, one blue) = orbits seen edge-on from within their plane — residual-speckle family, the main surviving visual defect. → T8 checklist (alongside the predicted fold-line band).
WEDGE DECISION (data): with GC at ~0.1 ms real and SS at baseline, the wedge-clip implementation (f64 A,B hoist + record 32→36 + geometry) is NOT justified by any measured pose. DEFER: park the reviewed design as a backlog detail file at close-out (design verdict + 11-item checklist live in this ledger); do NOT implement in this plan. F2 (pose-dependence) cut the other way — F4 alone sufficed.
Unpushed: a218d522, bce92c55, 83ce420f, 1cacdfcb, 5e91362e. Push after F4 scoped review returns clean.

## REDESIGN 2026-08-01: CPU closed-form visible arc (user-driven, supersedes ALL fold machinery)
USER: "the triangles are still there... tanking the performance completely, unusable... go back to the drawing table and think hard on a robust implementation." Root-cause analysis presented + discussed: uniform-in-E GPU sampling cannot cover unbounded near-fold projections with bounded geometry — every fix round (clamp → boxes → ownership → tangent extension → wedge) was a special case forced by that mismatch. User probed "aren't the ribbons polylines already?" — yes; the redesign decomposes into 3 dials: (1) visibility decided CPU-side (closed form) vs GPU-side, (2) fixed vs adaptive sampling, (3) analytic vs varying fragment. User approved spiking dial (1) minimal first.
SPIKE LANDED @ 6e2e1d84 (controller-inline, design-in-code; F4 scoped review had returned APPROVED-with-findings and its fix landed @ 981c3822 just before): composeOrbitConic returns arc=[eStart,eSpan] (w(E)=Cw+R·cos(E−φ) sinusoid ⇒ visible part is ONE closed-form interval; eSpan=0 ⇒ layer culls, TAU ⇒ closed wrap with mod indexing bit-identical seam); record 32→34 floats (arc float32x2 @ location 9 offset 128, stride 136); vsRibbon = the original clean branch only (central diff + sag) over the interval — near-plane clamp, fold boxes, E-ownership, tangent extension ALL DELETED; fragment lost both eOwn blocks; fsImpostorRibbon = flat 0.2 dim.
VERIFIED: build + typecheck + FULL SUITE 6185/6185 (mock + layout tests updated: arc sentinel 601/602 @ floats 32/33); hardware captures at GC / solar-system / earth-surface / earth-inside-moon all draw correctly (GC ellipses complete; SS arcs from inside; standing-on-orbit line present); harness: orbit-trails 0.3 ms real GC, 0.6 ms real SS.
KNOWN RESIDUAL (pre-existing, NOT fold-related): two dashed edge-on trails at the SS pose (fragment numerics family, = residual-speckle backlog); sag-widening near knees is the accepted local coverage mechanism (T4's original double-add pip possibility returns where widened quads overlap — E-ownership is gone; user decided earlier: confirm visually before fixing).
NEXT: user live verdict on :5173 (the triangles should be GONE — fold geometry no longer exists in any code path). If confirmed: dials (2)/(3) (adaptive sampling / varying fragment) become OPTIONAL follow-ups, decide at close-out; then batch-push all unpushed commits (a218d522..6e2e1d84), plan amendment (fold tasks superseded, wedge design → backlog note, layout 34), T8, T7 paired A/B, T9.

## CLOSE-OUT 2026-08-01
USER VERDICT (live, :5173): "that works way better... still some artifacts with dotted lines, but i take it over the performance win." Triangles + perf cliff GONE; dashed edge-on residual ACCEPTED (residual-speckle backlog family). T8 COMPLETE (task #8) — dials (2)/(3) not demanded by the verdict; park as optional follow-ups in the backlog note.
Pushed 97f31158..6e2e1d84 to PR #536. Plan Amendment 2 committed @ 872f1961 (goal line, fragment-math constraint note, record-layout 34/136 note, T11 superseded note, full Amendment 2 section, DoD rewritten to final truth, DAG tail). PR body rewritten: final architecture, measured table, residual, status boxes.
T7 COMPLETE (task #7): fresh 30-frame runs at final HEAD vs T1 baseline — galactic-centre orbit-trails 0.2 ms real (was 7.6; gate <1.5 PASS), solar-system 0.4 ms real, zero fallback draws (path deleted). Outputs: /Users/rulkens/.claude/jobs/19fc0a29/tmp/t7-final-{galactic-centre,solar-system}.txt. Paired A/B waived: 38× margin dwarfs thermal drift; single-run + T1 baseline suffices for the gate.
IN FLIGHT: retroactive spike review (opus) — brief spike-a2-brief.md, package review-981c3822..6e2e1d84.diff, report → spike-a2-review.md. The spike was controller-inline design-in-code; this is its SDD review gate before T9.
SPIKE REVIEW RETURNED (opus, spike-a2-review.md): Spec PASS / Quality REQUEST CHANGES, 0 Critical / 5 Important / 5 Minor. Reviewer independently re-derived the arc math and CONFIRMED it (phi/alpha form exact; division by wAmp provably never zero; f64 seam and .xyz swizzle respected; closed-wrap corners bit-identical; open-arc ends sag=0). Findings: I1 sag unbounded at open-arc ends (header claim "no oversized coverage geometry can exist" FALSE — end neighbour sits ~1e6 viewports away, quad can cover the screen); I2 deleted eOwn also guarded sag-widened OVERLAP (corner identity stops seams, not overlap) ⇒ possible 2x brightness step near the fold; I3 epsW=1e-6 leaves <1 order of magnitude f32 margin (WGSL only guarantees trig to 2^-11 ≈ 500x epsW) ⇒ raise to 1e-4; I4 seven stale near-plane-clamp/32-float comments (controller VERIFIED all seven by grep before dispatch — not fabricated); I5 arc math had ZERO unit tests (layer mocks it with a constant).
FIX ROUND LANDED: 36e8f101 (clampReach bounds neighbour reach at 4x viewport diagonal before chord/sag — inert on-screen, kills the unbounded end widening; epsW 1e-6→1e-4; header claim corrected; vertex.wesl trimmed to budget) + b58b2836 (arc unit tests as INTERVAL PROPERTIES not expected numbers, + layer eSpan=0 cull test; implementer mutation-verified both — phi sign-slip and continue-deletion each fail then revert green). Controller verified: FULL SUITE 6189/6189 (+4), both typechecks, vite build clean. WESL diff read line-by-line: clampReach's len=0 case (prevIdx==si at open-arc ends) computes Inf*0=NaN in the UNSELECTED select operand — discarded by value selection, safe.
I2 HARDWARE VERDICT (controller, post-fix captures in /Users/rulkens/.claude/jobs/19fc0a29/tmp/post-fix-{ss,ssdark,gcdark}): NOT REPRODUCIBLE at either production pose. ss-dark-overlay: every hull a thin band hugging its line (this is ALSO the direct confirmation clampReach works — pre-fix these near-edge-on trails were the screen-filling case). gc-dark-overlay: all S-star ellipses complete, thin, uniform; no bright pip at the major-axis turning points where the original T4 review predicted it. Dashed residual visible on the near-edge-on orange trail (known speckle family). NOTE ss-full.png is a total white-out — investigated and NOT trails: ssDark (trails isolated, glow layers off) is black with thin lines and orbit-trails reads 0.3 ms at 123 fps, so the white-out is the bloom/star-sphere layers at 2.06 AU, untouched by this branch.
RADAR RETURNED (opus, radar-report.md): net direction DE-COMPLECTING (fallback pipeline + eligibility classifier + fold machinery all gone). 5 branch findings + 2 backlog. ADJUDICATION — take the detectable-drift half, defer the deduplication: F1 HIGH (record layout = 3 encodings cross-checked by nothing; .d.ts ALREADY shipped stale at 32-float/128-byte — the proof) ⇒ fix the .d.ts + extend the parity test to pin io.wesl OrbitInstance ↔ INSTANCE_ATTRIBUTES both ways; DEFER the ORBIT_INSTANCE_FIELDS data table. F4 LOW-MED (two unrelated 1e-4s, made numerically IDENTICAL by the I3 fix — worse than when they differed) ⇒ name the shader one CLOSED_SPAN_EPS in constants.wesl with its unit. F3 MEDIUM (V is framebuffer px y-down, clipToPixel is centre-origin y-up; correct only by isometry, stated nowhere) ⇒ state the invariant in a comment; DEFER changing clipToPixel. F5 LOW ⇒ one makeRibbonPipeline factory. F2 MEDIUM (compose braids project/arc/invert; culled orbits pay an f64 inverse before the cull) ⇒ DEFER. Dispatched as radar-fix-brief.md (4 commits, sonnet).
BACKLOG FILED @ 2603e96c: docs/backlog/2026-08-01-instance-record-layout-conventions.md (F1 full, all four renderers — 24/12/7/34 floats, names the parity test as the template) + docs/backlog/2026-08-01-orbit-trail-compose-and-pixel-conventions.md (F2 + F3 full). Speckle index line corrected (it still credited the deleted minors hoist). Prior-art doc committed @ 1cc093f5 per user decision (include, refreshed — two passages describing deleted mechanisms rewritten; its Rye Terrell part-2 citation is the vertex-side prior art for I2 if it ever reproduces).
WEDGE DESIGN: NOT backlogged, by decision — it solved fold geometry, and fold geometry no longer exists in any code path. Filing it would be stale-design noise. Its verdict + 11-item checklist stay in this ledger above if the problem ever returns.
RADAR FIX ROUND LANDED: e55bf77e (.d.ts 32→34 float correction + PARITY TEST pinning io.wesl OrbitInstance ↔ INSTANCE_ATTRIBUTES: location coverage both ways, WESL type ↔ format float count, sum == INSTANCE_FLOATS && STRIDE == FLOATS*4) + 1d9ed5d3 (CLOSED_SPAN_EPS named in constants.wesl with its unit; both epsilons' units documented) + 00f48fd2 (coordinate-space isometry invariant stated on clipToPixel) + e1e5db9d (one makeRibbonPipeline factory). Controller verified: FULL SUITE 6192/6192, both typechecks, vite build clean. PARITY TEST MUTATION-VERIFIED BY CONTROLLER (not just by report): flipping io.wesl's arc vec2→vec4 fails 2 of 3 cases with exact messages ("@location(9) 'arc' is 4 floats in WESL but attribute format is 'float32x2'" and "expected 36 to be 34"), reverted clean. Implementer concern noted: vertex.wesl now sits EXACTLY at the comment cap (28 comment / 57 code) — any future line there must free one.
T7 RE-MEASURED AT FINAL HEAD e1e5db9d (the fix rounds changed the shader, so the 6e2e1d84 numbers no longer stood): galactic-centre orbit-trails ≈ 0.0 ms real (at/under the harness's own ~0.2 ms per-pass floor — no longer separable from measurement overhead), solar-system ≈ 0.7 ms real (vs 1.2–1.3 pre-branch). Gate <1.5 ms PASS on both. Files t7-headfinal-{gc,ss}.txt in the job tmp. NOTE the SS run reported MERGED 27.4 ms vs 17.3 ms earlier — hottest pass foreground:0·NEAR0 4.5 ms, NOT trails; run-to-run variance under load + Apple-Silicon slot-sum inflation, not a branch regression.
PR #536 body updated with final ranges, the three review passes, and the residual.
IN FLIGHT: final whole-branch review (opus) over review-7b2ea41a..e1e5db9d.diff (41 commits) → final-review.md. Briefed to verify the prior fixes landed, hunt dead symbols (ribbonEligible/fallbackCount/RIBBON_MAX_EXTENT_NDC/eOwn/foldHalfWidth/minorS/minorT/fsImpostorFallback/'vs'), and adversarially attack the two load-bearing claims (no sample can have clip.w<=0; the record agrees byte-for-byte).
FINAL WHOLE-BRANCH REVIEW RETURNED (opus, final-review.md): **MERGE** conditional on two text-only fixes. 0 Critical / 4 Important / 10 Minor — NO runtime defect. Both attacked claims HELD: clip.w > 0 survives the f32 seam with ~50–180x margin at the worst pose (the real basis is the NARROWING budget, not the f64 math — the epsW comment argues the wrong thing); the 34-float record agrees byte-for-byte at all six sites.
  I1 the SPEC (which /feature-done archives as the design record) still specifies ribbonEligible / minors / 40-float / two pipelines / partition with ZERO amendment notes while the plan carries seven.
  I2 four MORE stale sites (EngineSettingsState.d.ts, DebugOverlaysSection.tsx, OrbitTrailRenderer.d.ts, perfScenarios.ts) + "stride 32" x2 and "fade + pad" in tests.
  I3 THE ARC TEST CANNOT FAIL: mutating eSpan = 2*alpha → alpha truncates every straddling trail on screen and the suite stays GREEN. My brief asked for the interval property and only half of it was written (w>0 inside; never w<=0 just outside).
  I4 THE PARITY TEST CANNOT FAIL EITHER, in the way that matters: swapping the @location numbers on cc and ac in io.wesl passes all three assertions AND the renderer test, while every ribbon then traces around the wrong centre. It compares location SETS and float COUNTS — never name↔location, never location↔byte offset. My own mutation check (arc vec2→vec4) passed BECAUSE it happened to change a float count; it did not probe the reorder axis.
  Minors of substance: fragment.wesl at 3.17x the comment budget = THE ONE FILE THIS BRANCH MADE WORSE; clampReach bounds widening at ~4.6 viewport diagonals so the DoD's "no oversized triangles" is stronger than the code guarantees; the surviving dashed trails have a SECOND SUFFICIENT CAUSE (96-segment coverage on a strongly foreshortened trail) that the speckle backlog does not record — fixing only the numerics would leave the symptom identical (feedback_multiple_sufficient_causes).
BASELINE FIGURE — CONTROLLER ERROR, CORRECTED: ledger line 31 retracted the 7.6 ms galactic-centre baseline as CONTAMINATED and said to quote the 4.3 ms paired figure; ledger line 163, the plan goal line, the plan DoD, the PR body and two user-facing messages all quoted 7.6 anyway. Honest claim is **4.3 → ~0.0 ms real**. PR body corrected with the retraction stated in the open; plan corrected in the final fix round; user told directly.
FINAL FIX ROUND DISPATCHED (sonnet, final-fix-brief.md, 5 commits): (1) I3+I4 with FOUR mandated mutation proofs (eSpan=alpha; cc/ac location swap; attribute offset change; each reverted) — a test that cannot fail is worse than no test, so a mutation that does not fail is a STOP; (2) spec amendment notes, plan-style (leave superseded text, annotate); (3) the last six stale descriptions; (4) fragment.wesl derivations MOVED to docs/references/orbit-trail-fragment-math.md with the NaN-fails-closed and never-reintroduce-Dekker warnings kept IN FILE as one-liners; (5) four Minors incl. the 7.6→4.3 correction and the second-sufficient-cause backlog note.
FINAL FIX ROUND LANDED (5 commits 634ab422 / 40f19689 / a7282471 / 7f8a0a24 / 78991455). ALL FOUR MANDATED MUTATIONS produced the predicted failure then went green on revert. Controller INDEPENDENTLY re-ran the cc/ac location swap (the axis my earlier check missed): fails "WESL field names map to the expected @location order", reverts clean. fragment.wesl 130/41 comments (3.17x) → 20/41 (0.49x), header 70 → 7 lines; derivations moved to docs/references/orbit-trail-fragment-math.md with NaN-fails-closed and never-reintroduce-Dekker kept IN FILE. FULL SUITE 6194/6194, both typechecks, vite build clean.
CHECKBOXES (user chose "tick done, mark superseded"): 5fc9599b — 42 ticked / 15 left unticked. Unticked = superseded bullets only: T3 eligibility tests + RIBBON_MAX_EXTENT_NDC impl; T5 two-pipeline/dual-draw/two-count tests; T6 ribbonEligible mock + partition test; T11 all three (clamp entirely superseded); T7's conditional --sweep (never triggered — gate passed); T9's /feature-done bullet (running at the time).
/FEATURE-DONE: READY. Plan + spec moved to */completed/, ledger archived to docs/superpowers/plans/completed/<plan>.ledger.md, completion commit 1f29e370 pushed. Backlog swept — no stragglers for this feature (the two 2026-08-01 detail files are NEW deferred work and stay). PR #536 taken OUT OF DRAFT, ready for review.
T9 COMPLETE. PLAN COMPLETE.
REMAINING (post-merge, user's call): squash-merge #536 from the MAIN checkout (never from this worktree), then remove the orbit-trails-perf worktree via tools/dev/skymap-wt-clean.sh.
HISTORICAL REMAINING list from before close-out: (1) adjudicate spike-review findings (fix round if needed); (2) wedge-clip design → docs/backlog detail file (verdict + 11-item checklist live above at the WEDGE DESIGN REVIEW entry); (3) entanglement radar over the branch diff; (4) final whole-branch review (most capable subagent model = opus); (5) deferred minors: fragment.wesl header trim (own commit), fsImpostorRibbon ASCII hyphens; (6) ASK USER: untracked docs/references/orbit-trail-rendering.md — in-PR or drop; (7) /feature-done (plan+spec → completed/, backlog sweep); (8) scratch worktrees base-wt/feat-wt cleanup.
