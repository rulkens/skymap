# SDD ledger — plan: docs/superpowers/plans/2026-09-11-layer-composition-03-contract.md

- Branch worktree-layer-contract off main 62e3ac11c; plan commit 63dec8312; draft PR #690. Parallelism 2 (user ruling). Spec: docs/superpowers/specs/2026-09-09-layer-composition-design.md (reachable).
- Ruling (controller): Task 14 perf gate reads "neutral-or-negative HALTS". This PR is behaviour-neutral by construction, so NEUTRAL is the expected pass; a REGRESSION halts and goes to the user. The leanness rule ("neutral halts") targets claimed wins, which this PR does not make. Cost if wrong: a hidden step-merge regression lands — mitigated by A-B-A-B paired runs at the gate.
- Dev server for this worktree: http://localhost:5173 (background shell). Perf baseline is taken at the gate, paired with a scratch main worktree, not now.
- Pre-flight scan: preflight-scan.md — 31 pairs, 11 conflicts, 7 rubric hits, 9 drifted ground claims. Rulings C1–C11 ACCEPTED as proposed (controller): C1 targetParity+timedSlotsGroupKeys re-point move into T9; C2 T11 narrows the 8 frame helpers too; C3 SagaFactory/PickResolverRow/LayerCoreDeps/LayerUiSection get real contracts at plan level (own @types files); C4 core reducer map extracted to a named const; C5 test-delta arithmetic replaced by report-with-reasons; C6 ContentPass.d.ts drawPick postcondition dies in T7; C7 galaxyPointSpritesPass ordering comment dies in T7; C8 T13 after 9 and 6; C9 renderTargetRows takes the swap format from phaseLocals / assembled rows; C10 T9 owns the startLoop fixture, boot check after the phaseLocals guard; C11 PassState keeps tier, drift row read-set = nine fields. Fix pass dispatched (opus) before Task 1.
- Plan fixes committed 82bdf6207 (pushed). Ruling: T3 "fragment reachable" test dropped, T5 composed-keys test is the one covering test. Ruling 9 (LayerCoreDeps = GpuHandleConstructDeps − fontAtlases + store + requestRender) accepted.
- WAVE 1 dispatched: Task 1 (settings chain head) + Task 7 (frame chain head, ZoA/structure-marker own group(0)), each in an isolated agent worktree reset to origin/worktree-layer-contract @ 82bdf6207; BASE for both = 82bdf6207; controller cherry-picks -x onto worktree-layer-contract in task order.
- Task 1 review (opus): Spec ✅, Quality findings — Important ×2: (1) `{reducer,prepare}` not rejected at type level (RTK SliceCaseReducers admits it) → ruling: add the `typeof reducer !== "function"` throw in the lift loop; (2) value-returning case reducers silently dropped → ruling: write-back (`const r = write(...); if (r !== undefined) state[key] = r`). Minor: comment ratio over in 8 files (2 are brief-mandated prose → accepted as brief conflict; other 6 trim to budget); assertUniqueFragmentReducerKeys unwired → Task 3 wires it at module scope per plan (no T1 change); core-shadow throw message names the core seed. Concern (b) explicit type args: acceptable, brief contract stands. Fix round 1 of 5 → resuming implementer. Freeze: no new implementer dispatch until T1 fix closes (T2 + T7 already in flight).
- Task 7 DONE_WITH_CONCERNS: 9944f618d cherry-picked as $(git rev-parse --short HEAD); concerns: no GPU validation (user pick smoke on :5173 requested), 5 NEAR0 comment-only fixes widened the file list, drawPick states no postcondition. Reviewer dispatched (opus).
- Task 2 DONE: 59e8dce4c cherry-picked as 3470e63db (type-only, 9 cluster types named). Reviewer dispatched (sonnet).
- Task 1 fix round 1: 7a6860345 cherry-picked as 33c90f8a2 (prepare-form throw, returned-cluster write-back, budgets trimmed, core-seed message). Full-suite timeouts reported by implementer vary per run and pass in isolation: ruled load-flake (several worktrees running suites concurrently), not a defect; re-check at the gate. Scoped re-review dispatched (sonnet).
- Ruling: the T1 freeze applies to consumers of T1's interface (T3+), not to the frame chain; Task 8 (new files only, consumes T7 nothing) dispatched now at BASE 33c90f8a2 (opus, isolated worktree).
- Task 1: complete (re-review CLEAN at 33c90f8a2). Task 3 dispatched at BASE 33c90f8a2 (opus, isolated worktree).
- Task 2 review (sonnet): Spec ✅ (nine shapes byte-identical, tsc clean). One Important = comment-ratio breach in all nine new cluster type files — BRIEF-MANDATED CONFLICT (docblocks moved verbatim per plan; the monolith never had the ratio applied). Ruling: accept now, no fix round; queued for the /feature-done deletion audit as "trim the nine cluster type docblocks to purpose + units + landmines". Task 2: complete at 3470e63db.
- Task 7 review (opus): Spec ✅, Quality approved; bind-group tables agree at every index; 192B pick camera buffer verified byte-exact against CameraUniforms head. Task 7: complete at 9b040341e. Controller note: no test pins pickRing self-binding → queued (deferred-minors) for the wave-end fix pass: assert pickRing writes caller bytes to a buffer ≠ draw-time uniforms and calls setBindGroup(0, …). User pick smoke on :5173 still requested (ZoA band click + structure ring click).
- Task 8 DONE: 4715577aa cherry-picked as 88dcacfbe (+877, 15 files). Concerns: FrameStep.d.ts gained optional passes/slot (outside file list, needed for non-circular equivalence); draw-order prose COPIED not moved → ruling: Task 9 deletes it from passes/index.ts + frameProgram.ts (it deletes frameProgram anyway); merge key adds face+depthLoad; two roster deviations from spec §5 derived from the registry. Reviewer dispatched (opus).
- Task 3 DONE: 7729309dc cherry-picked as 7241f122b. Deviations: CORE_SEED typed Omit<…> (no cast); local SettingsDraft alias. Concern: CORE_SEED is a module-level value (shared nested objects across buildInitialSettings() calls) — reviewer to check no in-place mutation of initial state in tests/sagas. Reviewer dispatched (opus). Task 4 waits for this review (same files).
- Task 8 review (opus): Spec ✅, no Critical/Important; equivalence non-circular, 37/37 rows on rosters in registry order, slot names byte-identical, EXPAND_STEP exhaustive record. Task 8: complete at 88dcacfbe. Riders assigned to Task 9 (ruling): delete passes/index.ts draw-order header prose (moves to frameOrder.ts, no duplication); drop redundant `spec as never` (expandFrameOrder.ts:157); trim frameOrder.ts header to ≤10 lines; trim BloomStepSpec/ComputeStepSpec headers to a line of purpose; assert authored slot values in the surviving expand test; the "Task 9 flips that round" comment dies with Task 9. Task 9 dispatched at BASE 7241f122b (opus, isolated worktree).
- Task 3 review (opus): Spec ✅, Quality approved (3 Minor). Verbatim move proven mechanically (21 moved + 55 core reducers identical; 76 keys set-equal); typed action creators verified by tsc probe. Task 3: complete at 7241f122b. Riders → Task 4 (ruling): `coreSeed()` becomes a factory (shared nested objects across buildInitialSettings() calls; Task 5 reads Object.keys(coreSeed())); `assertUniqueFragmentReducerKeys(fragments, reserved?)` gains an optional reserved-keys param and settingsSlice calls it with Object.keys(CORE_REDUCERS) so a fragment-vs-core reducer key collision throws at import like the cluster-key case; fix the stale "restating 0.05" comment in starCatalogsSettings.ts (DEFAULT_REFINE_THRESHOLD is 0.16). Task 4 dispatched at BASE 7241f122b (sonnet, isolated worktree).
- Task 9 DONE: e321bf55e cherry-picked as 03c66276f (+620/−1848, frameProgram function gone; file name kept). Concerns: startLoop fixture given real renderTargets.specs so the boot check stays unconditional (ruling: keep); frameProgram.ts filename now a misnomer (reviewer to say what it still holds → rename via move-files in Task 10 if warranted); matchesHdrPhase deleted leaving hdrPhase dead data until Task 10; ExecuteFrameArgs.passes removed. Reviewer dispatched (opus). Perf pairing stays at the gate.
- Task 6 dispatched at BASE 03c66276f (opus, isolated worktree): frame chain idle pending T9 review, T4 holds the settings slot; parallelism stays at 2 implementers.
- Task 4 DONE: 10 commits cherry-picked (9aa94d02a..e2f2e6593) → HEAD fd958dcf7; riders done (coreSeed factory, reserved-key guard, stale 0.05 comment). Reviewer dispatched (opus).
- Task 9 review (opus): Spec ✅, Quality approved (3 Minor); executor per-step behaviour character-identical except selection removal; 7 input combos measured old-vs-new: zero diff; TIMED_SLOTS (306) + groups byte-identical. Task 9: complete at 03c66276f. Riders → Task 10 (ruling): (a) passes/index.ts rows still carry ~100 lines of draw-order rationale duplicating frameOrder.ts + two now-false statements (:127 array-position-is-documentation, :184 "selects nothing by construction") → delete/correct; (b) startLoop.ts:96 `renderTargets!` safety comes from bootstrap phase order, not the phaseLocals guard → fix the comment or throw a named phase error; (c) matchesHdrPhase already deleted in T9 → strike from T10; (d) survivors of the four row fields: engine.ts:600 (target), pickProgram.ts:326,344 (slab), skyCapture rows ×4, hdrPhase rows ×2 + HdrPhase.d.ts, passes.test.ts mirrors; (e) rename frameProgram.ts → timedSlots.ts via `npm run move-files` (imports: initGpu, installPerfHook, GpuTimingsSection, RenderTogglesSection + prose refs). Task 10 dispatched at BASE fd958dcf7 (opus).
- Task 4 review (opus): Spec ✅, Quality approved (3 Minor). 11/11 seeds + 23/23 reducers verbatim; 76 keys set-equal; coreSeed() = nine core clusters; 32 core reducers incl. mergeSnapshot. Task 4: complete at fd958dcf7. Riders → Task 5 (ruling): sgrAStarLensingTuningSettings seed returns the shared DEFAULT constant → `{ ...DEFAULT_SGR_A_STAR_LENSING_TUNING }`; milkyWaySettings.ts:6 dangling "Same relationship:" → drop; flowSettings.ts:16 restore the lost why ("keeping the visibility intent off the generic merge path"). Task 5 dispatched at BASE fd958dcf7 (opus).
- Task 6 DONE: 4d52c7d5c cherry-picked as bd6939fa1 (Layer/defineLayer/EngineComposition/APP_COMPOSITION, createEngine(canvas, cb, composition), seven home stubs → STUB_COMPOSITION). Reviewer dispatched (opus).
- Queue: T12 (independent) next free slot; T13 after T10 lands (documents the frameProgram→timedSlots rename); T11 after T10 review; T14 gate last. Running: T5, T10 implementers; T6 review.
- Task 6 review (opus): Spec ✅, Quality approved (2 Minor: app.ts 5/6 ratio — keep, all non-derivable; single commit instead of two — no artifact effect). Contract byte-equivalent to the brief; four compile probes EXIT 0; LayerCoreDeps exact per Ruling 9. Task 6: complete at bd6939fa1.
- Task 5 DONE: b780f9e12 cherry-picked as 89e0564bc (EngineSettingsState derived, tsc clean first try, zero readers touched). Concern: CoreSettingsState.d.ts 120c/54code (inherited docblocks) → deletion-audit queue. Reviewer (sonnet) + Task 12 (sonnet) dispatched.
- Task 5 review (sonnet): Spec ✅, Quality approved (1 Minor: EngineSettingsState.d.ts 10c/7code — header is the "DERIVED, never edit" landmine; keep). Shape identity probe EXIT 0 (mutual assignability + key equality vs the deleted literal). Task 5: complete at 89e0564bc. Settings chain 1→5 COMPLETE.
- Task 12 DONE: 72ea03afb cherry-picked as cf6ab8624 (VISIBILITY_LAYER_ROWS, backlog line deleted). Reviewer dispatched (sonnet).
- Task 12 review (sonnet): Spec ✅, Quality approved, no findings (totality probed both directions; derived group byte-identical incl. order). Task 12: complete at cf6ab8624.
- Task 10 DONE: 4e7757323 (frameProgram.ts→timedSlots.ts via move-files) + 61bcbcafd (four fields leave the row, 63 files +304/−903) cherry-picked → HEAD eab7eb0eb. Concerns: toggle list derived names-from-CONTENT_PASSES minus FRAME_ORDER volume line (byte-identical today); 3 comment lines added to frameOrder.ts capture line; pickProgram.test.ts fakes use real names; HdrPhase mentions remain in docs prose (T13 sweeps). Reviewer (opus) + Task 13 docs (sonnet) dispatched.
- Task 13 DONE: 01e850739 cherry-picked as 8c5ba3aeb (CLAUDE.md tree, RENDERER.md FRAME_ORDER paragraph, spec §13 A1–A7; sweep clean). Controller reviews the docs diff directly (small). Stale 'FRAME program' wording in TimingSlotName.d.ts → deferred-minors.
- Task 13 reviewed by controller: CLAUDE.md tree (2 lines), RENDERER.md FRAME_ORDER paragraph + HdrPhase fix, spec §13 rows A1–A7 — approved. Task 13: complete at 8c5ba3aeb. Missing (queued, wave-end): a RENDERER.md landmine line "every pick draw binds its own @group(0); no pass may rely on an earlier pass's bind" (the Task 7 invariant now stated nowhere).
- Task 10 review (opus): Spec ✅; 1 Important = stale prose in Slab.d.ts:16-19 and Blend.d.ts:5-6 asserting the deleted `slab`/`target` row fields; 2 Minor (no test for the no-line⇒no-pick arm of passSlabOf; prettier drift in RenderTogglesSection.test.ts). Pick slabs identical for all 14 drawPick passes; toggle list identical, inversion ACCEPTED as the better (d) semantics; rename 100% similarity. Ruling: the Important is two sentences → rider on Task 11 (which owns the frame types) rather than a fix round; passSlabOf undefined-arm test also a Task 11 rider. Task 10: complete-with-rider at eab7eb0eb. Task 11 dispatched at BASE 8c5ba3aeb (opus).
- Data linked: public/data → main (15 files). Perf gate dispatched early (opus, isolated worktree at merge-base 62e3ac11c with its own server; B = :5173 at 8c5ba3aeb): Task 11 changes no per-frame code, so the pairing is valid against 8c5ba3aeb; re-run only if T11's review finds runtime change. Scenarios full-survey/milky-way/star-field/solar-system, A-B-A-B, frames 30; raw + report under scratchpad/perf-c/.
- Task 11 DONE: 30772ff68 cherry-picked as 53eb86057 (PassState; 9 helpers + 4 delegates narrowed, zero casts; riders: Slab/Blend prose, passSlabOf undefined-arm test). Reviewer dispatched (sonnet).
- Wave-end minors pass dispatched (sonnet): pickRing self-binding test, TimingSlotName docstring, RENDERER.md pick-bind landmine. Two remaining deferred-minors (cluster docblock trims, CoreSettingsState split) go to the /feature-done deletion audit.
- PERF GATE (paired A-B-A-B, A=62e3ac11c :5175, B=8c5ba3aeb :5173, frames 30, 3–5 pairs/scenario under load): full-survey −1.70 ms, milky-way −1.80, star-field +1.14 (median +0.4), solar-system −0.87 — all inside the loaded-machine spread (1.4–8.1 ms) → NEUTRAL, no regression; MERGED group names identical row-for-row, no group differs >0.5 ms. Per the controller ruling (neutral = pass for a behaviour-neutral PR) the gate PASSES; numbers go to the user for the record. Report: perf-gate-report.md.
- Task 11 review (sonnet): Spec ✅, Quality approved, no findings above Minor; cut = exactly nine kept / five excluded; UpsamplePassRow duplication pre-existing → backlog note for (d). Task 11: complete at 53eb86057. ALL 14 TASKS CODE-COMPLETE (T14 gate: perf PASSED neutral; smoke pending user). Waiting: wave-end minors pass → final whole-branch review (most capable model).
- Wave-end minors: 6a25feb34 cherry-picked as 93e9d8310. Final whole-branch review dispatched (opus) over 82bdf6207..HEAD.
- FULL GATE at 93e9d8310: tsc clean (both projects), 1283 files / 8851 tests green, build green. Final whole-branch review (fable) + deletion audit (opus, legacy framing, whole branch) dispatched in parallel. Pending user: visual smoke (5 poses + ZoA/ring pick) on :5173.
- FINAL REVIEW (fable): MERGEABLE, 0 Critical, 2 Important (I1 spec bodies still state pre-ruling contracts at §4.2/§4.3/§4.4/§5 — amend six spots; I2 Layer.settings erases literal keys so composition.layers can never source EngineSettingsState — record for (d)), 7 Minor (M1 duplicate assert site; M2 nine expand cases in the wrong test file; M3 cross-Layer UNIFORM_BYTES import for (d) manifest; M7 smoke unattested). Deletion audit: 7 SAFE-NOW (~65 src/90 tests), 9 NEEDS-RULING (~460 src mostly comments; ContentPass.blend has NO runtime reader). ONE fix dispatch (opus): I1 + I2 record (§13 A8 + backlog line) + M1 + M2 + BACKLOG.md:176 + all SAFE-NOW bins. User asked to rule on blend / startLoop header / timedSlots docblocks / cluster docblocks.
- Final fix pass: 1f13929c9 (SAFE-NOW S1–S6) + 23f0e6f21 (spec bodies amended, §13 A8, backlog seam item, M1, M2, N9) cherry-picked → HEAD aae22129b; tests 8851→8845 (S3/S4 deletions). USER RULINGS 2026-09-12: N1 delete ContentPass.blend + Blend.d.ts + test table; N2 startLoop header trim; N3 timedSlots MAX_* docblocks collapse; N4 CoreSettingsState split/trim; N5 nine cluster docblocks trim — ALL land in this PR. Rulings commit dispatched (opus).

## HANDOFF 2026-09-12 (compact-ready)
- Branch worktree-layer-contract @ aae22129b (pushed; draft PR #690). All 14 tasks complete + reviewed; final review MERGEABLE; perf gate NEUTRAL (perf-gate-report.md); full gate green (tsc, 8845 tests, build). Dev server :5173 (background shell bqkv9d6sw), public/data symlinked to main.
- IN FLIGHT: rulings agent (opus, isolated worktree, id a230026543747ba4a) making TWO commits: (A) delete ContentPass.blend + Blend.d.ts + test table + spec §13 A9; (B) comment trims N2 startLoop header, N3 timedSlots MAX_* docblocks, N4 CoreSettingsState split (DebugSettings.d.ts), N5 nine cluster docblocks. Report at <its worktree>/.superpowers/sdd/…/rulings-commit-report.md. ON RESULT: cherry-pick -x both SHAs onto the execution branch, push, review-package 93e9d8310..HEAD (covers fix pass 1f13929c9+23f0e6f21 = already packaged as review-93e9d8310..aae22129b.diff, plus the rulings commits) → ONE scoped re-review (sonnet) of the fix pass + rulings commits; then /feature-done.
- QUEUE after re-review clean: /feature-done (DoD audit, plan → plans/completed, spec STAYS (multi-PR), ledger archived via `cat` to docs/superpowers/plans/completed/2026-09-11-layer-composition-03-contract.ledger.md, backlog sweep — the LAYER_GROUPS row is already deleted; the new backlog item 2026-09-11-layer-settings-tuple-seam.md is a DEFERRED item for (d), leave it) → update PR #690 body (perf table, rulings, test delta 8195→8845 with reasons) → user rulings: smoke attestation (5 poses + ZoA/ring pick on :5173) is STILL PENDING → gh pr ready + squash-merge on user go → /wt-close (stop shell bqkv9d6sw; do not purge agent-* worktrees without attribution) → memory: project_layer_composition.md → (c) SHIPPED with squash SHA; NEXT (d) galaxyCatalog Layer plan; note the (d) seam backlog item + UpsamplePassRow duplication + structureMarker UNIFORM_BYTES cross-Layer import.
- User directives this session: tier stays store state (no tier/dataUrl on the composition; tier-immutable idea CANCELLED); blend deleted; all four comment trims land in this PR; parallelism 2; neutral perf = pass.
- Rulings commits 5c9cca3fe (blend deleted, §13 A9) + 160d8676c (comment trims; DebugSettings.d.ts split) cherry-picked → HEAD 0cd180164; tests 8844; net −443 vs aae22129b. Scoped re-review (sonnet) of 93e9d8310..HEAD dispatched.
- Scoped re-review of 93e9d8310..0cd180164 (sonnet): 9/9 addressed, CLEAN; one non-blocking note: StarCatalogSettings.d.ts lost the "loaded status is the asset slot's own readiness, NOT a store bit" overlay-convention sentence → controller restores it (it IS the state-landmines overlay convention).
- /feature-done 2026-09-12: DoD audit READY pending smoke attestation (6 smoke boxes unticked at plan lines 1337/1399-1407); 89 boxes ticked, 3 superseded-by-ruling; TODO scan clean; backlog swept (layer-blend-declared-twice DELETED as resolved by the blend deletion; frame-assembly item narrowed to target-format parity); plan → plans/completed; spec STAYS (multi-PR); ledger archived.
