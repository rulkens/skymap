# SDD ledger — plan: docs/superpowers/plans/2026-09-01-galaxy-field-state-unbraid.md

Spec: docs/superpowers/specs/2026-09-01-galaxy-field-state-unbraid.md (binding authority)
Branch: galaxy-field-state-unbraid (worktree galaxy-field-extraction) · base 95e88d420 · draft PR #650
Plan+spec commit: a141ae5da
User rulings: ALL radar findings in ONE PR; graph-as-data is the organizing principle (user, 2026-09-01, mid-authoring steer — spec §1 carries it).

## Pre-flight conflict scan (2026-09-01)

Pairs sharing a file/interface:

| Pair | Produces vs consumes | Verdict |
| ---- | -------------------- | ------- |
| T1×T2 | both edit orchestrator's early let block; disjoint symbols, sequential | clean |
| T2×T12 | T2 adds `central`/`centralHii` + EMPTY sentinels; T12 deletes them (null geometry moves into `compute`) | clean, but see Ruling 1 (plan typo says "Task 11") |
| T4×T5 | T4 creates two grow-buffer instances reading `.buffer`; T5 renames to `getBuffer()` and lists both instances as call sites | clean (ordered) |
| T5×T8 | both edit createGrowOnlyRecordBuffer + its test; T8 deletes `onRegrow` after T5's accessor rename | clean (ordered) |
| T7×T8 | T7 adds internal table keeping 4 public rebuild* wrappers byte-compatible; T8 swaps public surface to `sync` | clean (explicitly two-part design) |
| T8×T12/T13/T14 | T8 leaves encode-local `dustMap` slot + sync-at-encode; graph tasks don't touch those (spec §4: "two encode-local slots" survive) | clean |
| T12×T13 | T12 keeps 12 booleans feeding effect calls; T13 deletes them + rebuild fns, adds transitional .invalidate() shim on the six keyed-rebuild nodes | clean (shim explicit) |
| T13×T14 | T13 sync rows 1–5 with shim; T14 step rows 6–11, deletes six nodes + shim + ensureFresh order | clean |
| T9/T10×T12/T13/T14 | primitives land with tests, no consumer, before consumers | clean |

Per-task self-consistency: T6 carries the failing-parity escape hatch (stop, report — live bug); T13 carries the placeDust.wesl every-slot verification with the conservative fallback (add generator to fieldPack key); T8 carries the onRegrow stop-and-report hatch. Plan §File Structure lists every file the tasks name. No task contradicts Global Constraints; no mandated test asserts nothing.

Ruling 1: Plan Task 2 says "(Task 11 deletes both …)" — off-by-one; Task 12 is the deleting task (its own checklist says so). Cost if wrong: none, it's a pointer. Carry the correction in T2's and T12's dispatches.

## Model plan

Implementers: sonnet default; opus for T8, T12, T13, T14 (multi-file integration / orchestrator rewiring), T15 (leanness judgment). Reviewers: sonnet for small mechanical diffs, opus for T8/T12/T13/T14 and the final whole-branch review. Never Fable.

Board artifact: https://claude.ai/code/artifact/4826e03a-e634-4761-a080-bc9667e7dafd
(file scratchpad/state-unbraid-board.html — republish same path to update; flip task chips as tasks land).

## Task log

(BASE recorded before each dispatch; review package per task; fix rounds R≤3 resume implementer.
Loop per task: implementer → review-package script → task reviewer (spec+quality) → fix rounds → ledger line.
After T15: final whole-branch review on opus → perf A/B vs main (--url http://localhost:5400) → STOP for USER visual pass (7 smoke behaviours in plan DoD) → user merge call on PR #650.)

Task 1: complete — 2cb252b58, spec ✅ quality APPROVED (review clean; `?? 0` fallback verified gated). Suite 7873 green.
Task 2: complete — 6656f724a, spec ✅ quality APPROVED (co-temporality via param verified at both call sites; atomic assignments; hiiRegions diff = export-only).
Task 3: complete — 734de1d39, spec ✅ quality APPROVED (18/18 entries reconciled, none dup/dropped; destroy|dispose union unambiguous per entry).
Task 4: complete — 56c1face5, spec ✅ quality APPROVED (growth semantics exact match, per-dispatch bind groups, single-ownership preserved).
Task 5: complete — d131188a5, spec ✅ quality APPROVED (no missed reads, no hoisted caches, test unchanged/behavioural, comments −6).
Task 6: implementer DONE — 26c3ccc6b (suite 7889 green, +26 assertions). Ruling 2: implementer's deviation ACCEPTED — 3 packers already had parity coverage via tools/utils/wgsl/ suite; goal is coverage of all 8, not eight new files; duplication would violate testing.md. Cost if wrong: a coverage hole the reviewer is told to check assertion-by-assertion. Also noted: PLACE_DUST_PARAMS_FLOATS over-allocates 16B (benign, GTE precedent). Review DONE: spec ✅ quality APPROVED (3 skipped packers verified genuinely covered, offsets exact .toBe, live .wesl parsing, no production code). Task 6 complete.
Task 7: complete — f55996cf4, spec ✅ quality APPROVED (table matches §3.3, walker reproduces old rebuild sets role-for-role at all 4 call sites, tierBGMap indivisible; getDustMapTex nullable-lie wart correctly deferred to T8).
Task 8: implementer DONE — f9990e97f. OPUS review: spec ✅, 5 findings — 1 MEDIUM (probe.fieldSplatBG stale/destroyed-buffer window between setMixture regrow and next encode; dev-only, harness settleFrames dodges) + 3 LOW (dangling comment pointers to deleted symbols) + 1 NIT (sync early-return, skipped per prior per-frame-alloc ruling). Ruling 3: MEDIUM accepted as documented dev-only contract, doc fix not mechanism — probe surface is already ledger-parked from #643, and §6 delta-2's "strictly no later" caveat for this one reader is recorded here. Fix round 1 DONE — 424bdb8ef (+10/−6 comment/doc only; MEDIUM doc placed on the GalaxyFieldProbe type decl, placement deviation accepted — that's the TS-surfaced doc). Scoped re-review DONE: 4/4 ADDRESSED, comment-only confirmed, no new problems. Task 8 complete (f9990e97f + 424bdb8ef).
Task 9: complete — 13297c019, spec ✅ quality APPROVED (lastKey-undefined sentinel avoids undefined-T collision; NaN/-0 tests would catch === bug; contract verbatim per §3.1).
Task 10: complete — 2b6458a91, spec ✅ quality APPROVED (retention structural, tokens fresh objects per run, whole-table validation, all 7 tests discriminating).
Task 11: complete — 4a4c5fc49, spec ✅ quality APPROVED (all ~60 shard reads live inside closure bodies, no creation-scope hoists; narrowing aliases all in synchronous bodies; EMPTY_INPUT member-for-member match; setMixture prev/current atomic; comment net 0).
Task 12: implementer DONE — 59858e219 (suite 7909 green, probe PASS 27/27, code net +18/comment net −28; scaffolding cost recouped in T13/T14). Implementer concerns for reviewer: (1) behaviour delta spec-sanctioned §3.1 — null-geometry now returns empty central/centralHii instead of retaining previous galaxy's; (2) deviation: EMPTY_SHELLS_AND_YOUNG import KEPT (centralHii null branch needs it, re-declaring would duplicate hiiRegions.ts's own constant); (3) deviation: rebuildDustMixture/rebuildDigVeilBudget DELETED not thinned (reduced to bare CDF-scan passthroughs T13 would delete anyway — call sites now call dispatch*CdfScan directly); (4) placeDust every-slot verification correctly deferred to T13, generatorMoved && !dustMoved epilogue unchanged. OPUS review DONE: spec ✅ (all nine key tuples verbatim §3.1; invalidation parity input-for-input; getter timing safe incl. fieldCounts-vs-GPU-buffer re-derivation; comment net −28 verified). QUALITY APPROVED, findings: 3 LOW dangling comment pointers to deleted rebuildDustMixture/rebuildDigVeilBudget (io.wesl:88+:102, GalaxyEngineOptions.d.ts:18, probeGpuErrors.ts:2048) + 2 NIT skipped (sentinel asymmetry, per-frame key alloc — sanctioned by brief, perf A/B is the net). All 4 implementer concerns ACCEPTED by reviewer with verification. Finding 4 LEDGERED, no action this branch: centralHii/extraHiiMixtures keys don't observe tuning.ismMap/ismMapFluid though buildHiiShellsAndYoungWithSegments reads them — EXACTLY old hiiMoved behaviour, pixel-neutral, spec-pinned; T13/T14 or backlog decides deliberately. Finding 5 for T13 dispatch: rebuildDustMixture/rebuildDigVeilBudget already deleted in T12 — T13's checklist line for them is done. Fix round 1 DONE — 6078ebf5b (+7/−7 comment/doc-only; io.wesl ×2 → deriveDustHeaderLanes, GalaxyEngineOptions.d.ts → noteOrientationRebuilt (doubly stale), probeGpuErrors → digBudget node; typecheck+tool tsc+build pass). Scoped re-review DONE: 3/3 ADDRESSED, comment-only CONFIRMED, all pointer targets verified to exist, old symbols confirmed dead repo-wide, no new problems. Task 12 complete (59858e219 + 6078ebf5b).
Task 13: IN FLIGHT — implementer dispatched (OPUS), BASE 6078ebf5b, brief task-13-brief.md (stage table sync phase rows 1–5 via createStageGraph; deletes the 12 booleans + rebuildForGeometry/rebuildForTuning; transitional .invalidate() shim on six keyed-rebuild nodes; carries placeDust.wesl every-slot verification, conservative fallback = add ismMap.generator to fieldPack key). Carried: T12 already deleted rebuildDustMixture/rebuildDigVeilBudget (checklist line done); finding-4 ismMap/HII key gap is ledgered pixel-neutral — don't widen HII keys silently. Implementer DONE_WITH_CONCERNS — d24cf842f (suite 7909 green, probe PASS, code +11/comment −66 = net −64). Ruling 4: scan:dig key widening ACCEPTED — spec §3.2 row 3 pinned key omits current.fieldTuning.arms.widthScale, but buildDigArmEnvelopeTable→armCrossSigma reads exactly that lane and old hiiMoved covered it; the pinned row would introduce a stale-envelope bug on arm-width drag. Spec's own principle (key = old trigger set) mandates the element; treating as spec-table omission. Cost if wrong: one spurious dig re-scan per width drag. Reviewer told to verify. Ruling 5: COPY_DST fix on createIsmMapOutput's three targets ACCEPTED — clear()'s writeTexture was ALWAYS a silently-dropped write (implementer verified probe fails without the fix, 3 validation errors); behaviour delta: generator→none now blanks the map (stale before), matching orientationTexRebuild's asserted gasFloor invariant. ADDED TO USER VISUAL PASS: 8th behaviour — toggle ISM generator to none, confirm map blanks. placeDust verification CLOSED: shader writes every reserved slot (culled → amplitude 0, record stored; placeDust.wesl:93-208) → fieldPack key stays pinned, deleted epilogue was genuinely redundant, NO generator element needed. OPUS review DONE: spec ✅ (rows 1–5 byte-exact apart from ruled widening; all three old call paths walked and reproduced; under-observation sweep of all five rows = no gaps; 11 .invalidate() shim sites one-for-one with BASE; setMixture contract unchanged). QUALITY APPROVED, rulings 4+5+placeDust all evidence HOLDS. Findings: LOW-1 createGalaxyFieldRenderer.ts:858-864 forceGeneratorIsFluid doc still describes the clear-path bug this commit FIXED (misleading; fix now — T14 rewires this file); forceGeneratorIsFluid override itself now justification-free → BRANCH DELETION AUDIT item. LOW-2 two src/ shader headers still name deleted rebuildIsmMap (dustDetail.wesl:21, ismMapPresent.wesl:35) → T15 SWEEP LIST. LOW-3 boot delta: row 1 now fires onIsmMapRebuilt(defaultGrid) at boot (one extra blank-map CPU readback, pixel-neutral) → VISUAL PASS NOTE alongside generator→none blanking. Ledger notes: row 1 key gains seed (spec-pinned, more correct); row 2 tuning.ismMap element redundant-but-harmless (spec-pinned, keep). Fix round 1 DONE — ffd66886c (+4/−3 comment-only, forceGeneratorIsFluid doc → determinism rationale). Scoped re-review DONE: ADDRESSED, comment-only CONFIRMED, determinism rationale verified against stage-table rows. Branch PUSHED to origin through ffd66886c (user asked; PR #650 now carries all task commits). Task 13 complete (d24cf842f + ffd66886c).
Task 14: IN FLIGHT — implementer dispatched (OPUS), BASE ffd66886c, brief task-14-brief.md (step phase rows 6–11; six keyed-rebuild nodes + .invalidate() shim + sigmasMoved + ensureFresh ordering die). Carried: LOW-2 wesl pointers belong to T15 not this task; boot onIsmMapRebuilt delta already ledgered. Implementer DONE — e3083c97d (one file +178/−182, comment −39; suite 7909 green, probe PASS; rows 6–11 byte-exact vs §3.2, zero deviations; setMixture = two statements, stepIsmMap = graph.run('step')). Concerns for reviewer: (1) row 8 keys on T(orientation:tex) → sigma drag now also re-places dust — spec-pinned, conservative (placeDust samples orientationTex; old staleness was latent); provisional ruling ACCEPT + VISUAL PASS NOTE (9th: sigma drag, confirm dust re-place looks right, one extra dispatch per drag); (2) two comments relocated not deleted (arm-cloud dead orientationTexture bind; row 6 zero-init safety) — facts the table can't state; (3) +2-line comment on row 11's D(hiiPack) key element. Implementer verified: place:spur/arm key on tuning.arms suffices (all six tuning reads are arms.*); token bumps even when run early-returns keep ismMap→placement edges transitive; first-run falls out of retention. OPUS review DONE: spec ✅ (rows byte-exact; invalidation parity trigger-for-trigger incl. transitive token edges verified against createStageGraph internals; ordering identical, no reorder; first-run + zero-work-idle confirmed; deletions complete; live reads hold). createKeyedRebuild.ts LIVES — other consumer (createGalaxyModel.ts:305 bubblePlacements) + spec §9 item 7 pins "kept". All 3 implementer concerns ACCEPTED (concern 1 verified in-shader: placeDust samples orientationTex when fluid → BASE was stale). SECOND widening found, no-action: seed on rows 9/10 (BASE never re-placed clouds on bare seed move despite passing seed — latent fix, same direction). VISUAL PASS NOTES now: (8) generator→none blanks map; (9) sigma drag re-places dust (with generator none + orientation view on: wasted-but-identical work). Findings: LOW-1 :1186-1188 D(hiiPack) comment justifies unreachable case — reword per reviewer's suggested framing; NIT :929 "each submit an encoder" wrong for orientation:data; LOW-2 probeGpuErrors.ts has 9 dangling deleted-node names + 10 stale repackFieldComponents → T15 SCOPE WIDENED to include probeGpuErrors.ts. Fix round 1 DONE — 09badba22 (+9/−5 comment-only; D(hiiPack) → spec-pinned insurance framing; graph doc names the five encoder-submitting rows, excepts orientation:data). Scoped re-review DONE: 2/2 ADDRESSED, comment-only CONFIRMED, both wordings verified against table code (incl. orientation:tex's encoder inside createIsmMapOrientation). Task 14 complete (e3083c97d + 09badba22).
Task 15: IN FLIGHT — implementer dispatched (OPUS, leanness judgment), BASE 09badba22, brief task-15-brief.md. Scope ADDITIONS carried: probeGpuErrors.ts (9 dangling deleted-node names + 10 stale repackFieldComponents refs incl. probe failure messages), dustDetail.wesl:21 + ismMapPresent.wesl:35 (deleted rebuildIsmMap pointers). Constraints carried: createKeyedRebuild.ts LIVES (tool-side consumer, spec §9 item 7); forceGeneratorIsFluid override deletion question PARKED for branch deletion audit, not T15's call; net must be negative on code+comment or the report must say why not. On DONE: review package from BASE, OPUS reviewer (final task). Implementer DONE — e30805016 (single commit, 19 files, comment/doc-only, task net −24 comment; suite 7909 green, probe PASS, closing grep clean for all deleted symbols across src tools tests). Concerns raised: (1) BRANCH code+comment net +30, entirely the two primitives + 4 types (+122); excluding them −92; module itself −69 — land/park = USER ruling at branch gate; (2) createFieldPipelines.sync `| null` return is dead code ~11 lines (caller passes non-nullable; guard also misses the real undefined pre-rebuildAll case) — raised not applied, spec pins signature → BRANCH DELETION AUDIT item alongside forceGeneratorIsFluid; (3) six files over comment ratio, all pre-existing, all improved by branch. Review package review-09badba22..e30805016.diff written; OPUS reviewer dispatched. OPUS review DONE: SPEC ✅ (comment/doc-only verified line-by-line — only non-comment lines are 4 error-message template literals, text only; scope covered; createKeyedRebuild + forceGeneratorIsFluid untouched; reviewer's own 22-symbol grep zero matches; must-stay landmines present). QUALITY APPROVED, 3 non-blocking: LOW-1 probeGpuErrors.ts:2273 "token alone" overstates (hiiPack.get() also moves) → FIX; NIT extrasFragment.wesl:4 + timingSlots.ts:42 "see hiiPack for why" pointer thin (reason lives only in .wesl header) → FIX; LOW-2 task-number stripping uneven (prior-plan "Task 15" refs survive in probe/packers) → Ruling 6: PARKED — prior-plan task numbers are not branch-stale symbols; renaming them is out of scope, ambiguity pre-existing. Cost if wrong: a reader confuses plan numberings, zero runtime. Reviewer confirmed concerns 1–3 (branch +30 numbers hold; sync `| null` dead-code claim verified, defer to branch audit correct; over-ratio files pre-existing — note deriveDustHeaderLanes wording slip in report, ratio nominally worse not reduced, not a defect). Fix round 1 DONE — 7d0166d04 (3 files, +9/−9 comment-only; "alone" → "off the upstream movers"; timingSlots repointed at extrasFragment.wesl header where the reason lives, extrasFragment.wesl pointer role made explicit "where the span is built"; typecheck both projects + tool tsc clean). Scoped re-review DONE: 2/2 ADDRESSED (new claims verified against stage-table keys :1193-1200 and extrasFragment.wesl header lines 1-9), comment-only CONFIRMED, no new problems. Task 15 complete (e30805016 + 7d0166d04).

## Branch gate sequence (after T15)

1. Final whole-branch review — OPUS, package review-95e88d420..7d0166d04.diff; ONE fix dispatch + scoped re-review + adjudicate residuals. DISPATCHED (branch pushed through 7d0166d04; board at 15/15). DONE: APPROVED, no blocking findings; verified clean = §3.1 key walk verbatim (incl. real-read under-observation sweep), §3.2 stage walk exact, retention composed-unreachable, live-read coverage, bind-group table vs layouts, teardown ledger, grow-buffer floor, probe claims, parity, zero leftover symbols. Findings adjudicated (Ruling 7): F1 MEDIUM createStageGraph:26 phase-blind `after` validation (sync-after-step constructs fine, silently violated) → FIX + test; F2 LOW both primitives record key/token BEFORE work, throw remembered as done + token bumped on failed effect → FIX (record after success; spec's token-before-run pin was about early-returns/transitive edges, not throws — preserve those, one discriminating test each; implementer told to STOP if reorder breaks documented behaviour); F3 LOW :1301-1305 dead bindGroups===null guard = src twin of sync `| null` → MERGED into deletion-audit item 2(b), one contract ruling; F4 NIT live backlog file names deleted symbols → FIX (1 doc line); F5 NIT dropped ordering note → confirmed-deliberate, no action. ONE fix dispatch out (opus, items F1+F2+F4, full gates incl. probe). Fix DONE — c80939d60 (5 files +82/−8; phase check {index,phase}; record-after-success both primitives with all three preserved semantics verified, new tests confirmed red pre-fix; backlog line retargeted). Suite 7912 (baseline 7909 + 3), probe PASS. Scoped re-review DONE: 3/3 ADDRESSED, semantics a–d all CONFIRMED (phase check discriminating; record-after-success verified no double key-eval/token-bump, no stale value; backlog targets exist). Final whole-branch review CLOSED. Branch pushed through c80939d60.

Perf A/B DONE (2026-09-01): paired-baseline per perf skill — scratch worktree at 95e88d420, symlinked node_modules+data, server 5601 vs branch server 5402 (NOTE: 5400 is the galaxy-renderer TOOL server, not the app; ledger's earlier --url 5400 pin was WRONG; app server for this worktree now runs on 5402; 5400 tool server restarted after accidental kill). A-B-A-B ×4 scenarios @30f: milky-way +1.02, outside −0.21, close +0.67, galactic-centre −1.49 (B-side self-noise 1.8). milky-way re-run @90f A-B-A-B: branch −4.95 ms (sign FLIPPED), top movers hdr/star-aggregates/foreground — passes the branch never touched. VERDICT: NEUTRAL — deltas both directions, attribute to untouched passes, Apple Silicon retire-interval noise. Identical benign SDSS-404 page errors both sides. Scratch rig removed. Raw JSONs in scratchpad perf-A*/B*.json. USER CORRECTION (2026-09-01): main app still renders the OLD v1 Milky Way (v2 analytic field not wired in until task C), so the A/B measured paths that exclude the branch's code — NEUTRAL stands only as a no-app-regression check; meaningful gates = probe PASS + visual pass IN THE TOOL. Visual pass venue is the galaxy-renderer tool at localhost:5400, NOT the app at 5402.

USER GATE (2026-09-01): tool visual pass ATTESTED by user ("tool visual gate done", all nine behaviours). Ruling (USER): branch net +30 — LAND AS-IS. Ruling (USER): deletion-audit items — DELETE BOTH (a: forceGeneratorIsFluid override + type, generator follows live setting; b: sync `| null` arm + caller's unreachable twin guard, spec signature lines amended). Deletion implementer dispatched (opus); on DONE: scoped review, push, /feature-done, then user merge call on #650. Item (b) DONE — 900ea7ff4 (net −10; premises verified: dustMapTex non-nullable, pre-rebuildAll case was undefined never caught by the null guards, first encode always follows construction-time rebuildAll; spec signature lines amended; suite 7912 + probe PASS; prettier's whole-spec reformat reverted, kept only signature edits — NOTE repo-wide `npm run format` will churn the spec ~116 lines). Item (a) STOPPED correctly — RULING PREMISE FALSE: forceGeneratorIsFluid never pinned production (optional param defaulting to live setting, production passes nothing); sole caller is the probe passing FALSE to exercise placeDust mode-1/smoothDisc — the repo's only execution of that branch; deletion would take ~90 probe lines of coverage with it. Ruling (USER, revised): KEEP override + coverage, retarget stale comment only (task-sequencing narration → actual purpose). Comment fix dispatched (resumed implementer).
2. Branch deletion audit items to adjudicate with/after final review: (a) forceGeneratorIsFluid override + type (justification-free since T13); (b) createFieldPipelines.sync dead `| null` return ~11 lines (spec-pinned signature — contract change, needs ruling).
3. User ruling queued: branch net +30 (primitives +122, rest −92) — land/park per code-is-liability.
4. Perf A/B vs main — read .claude/skills/perf/SKILL.md FIRST; `npm run perf -- --url http://localhost:5400`; negative HALTS landing (user's ruling).
5. STOP for USER visual pass — NINE behaviours: 7 plan-DoD smoke + (8) generator→none blanks ISM map + (9) sigma drag re-places dust; boot fires one extra blank-map readback (pixel-neutral).
6. /feature-done → user merge call on PR #650.
