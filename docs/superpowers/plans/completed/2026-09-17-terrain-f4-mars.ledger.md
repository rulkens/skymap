# SDD ledger — Terrain F4 Mars

Plan: `docs/superpowers/plans/2026-09-17-terrain-f4-mars.md` · wt `mars-terrain-feature` · branch `worktree-mars-terrain-feature` · draft PR #743

## Start questions
- Parallelism: this plan alone in this worktree; F3a runs in another session (user OK 2026-09-17). Merge after F3a.
- Perf gate: NOT run by default (runtime diff = base-globe radius + ambient source). User was AFK when asked — confirm on return.

## Rulings
- R1 Opportunity moves to final position (Endeavour box); coordinates: −2.336, 354.619 E — centre of HiRISE ESP_087985_1780 "Opportunity Rover Position" (uahirise.org, 2025-05-04); a PROXY, not an RMC fix (no official lat/lon found; PDS traverse table is site-frame only). Inside Endeavour box, min margin 3.8 km. Flag at eye-check.
- R2 site window 3×3 km around each rover.
- R3 Jezero extracted (done 2026-09-17 01:41).
- R4 (user 2026-09-17): Perseverance moves to its CURRENT position, if that is inside the Jezero mosaic (77.058–77.381 E, 18.136–18.588 N). RESOLVED: sol 1980 (~2026-09-14), RMC 91_970 end-of-drive, 18.43687 N, 77.23205 E, source mars.nasa.gov/mmgis-maps/M20/Layers/json/M20_waypoints_current.json (landing sol 13 in same file = 18.44463, 77.45089 — frame OK). Inside Jezero box, min margin 8.4 km.
- O4 base-globe colour: at eye-check.

## Cherry-picks
- f1903fa86 (F3a branch, SHGT v3 CPU post grid) → fc7ff18f6, 2026-09-17, user request "before baking". Mars bake emits v3 height tiles. Consequences: main's runtime (v2) can't read Mars heights until F3a lands; THIS worktree's runtime rejects main's Earth v2 height tiles until Earth is rebaked v3 (F3a's job) — so the "Earth unchanged" eye-check pose can't pass here; do it after merging F3a. User 2026-09-17: an Earth v3 re-bake is running in MAIN; user will say when to verify v3 height tiles. Don't start the full Mars bake (T6) while it runs (shared machine/disk); a small `--body mars --dev` is OK. On merge of F3a, the squash contains this change → expect a trivial conflict/no-op.

## Dispatches
- D1 (Sonnet): T2 8e1218f86, T3 ff86f3cf5 DONE (fixed sharp grey16 rescale + 0·NaN bilinear). T1 stopped: rawDataPath is cwd-relative → ruling C1 leaf symlinks. Probe: Viking strip 14 s/box vs COG 9 ms → C2 COG conversion (bg job b0vvz56r7: MOLA, Gale DTM, Viking → *_cog.tif in main data/raw).
- COG conversion DONE (02:19): MOLA 662 MB, Gale DTM 1.2 GB, Viking 1.3 GB JPEG-YCbCr, all LAYOUT=COG 512². D2 told.
- D2 (Opus, IN FLIGHT, bg agent): T1 rest (C1 leaf symlinks, READMEs, DATA.md) + T4 (mars row, marsSurfaceBake, reliefM, rover moves R1/R4, --dev bake) + T5 (textured globe at innerBound, ambient, body-switch test). ON RETURN: check the tree mid-flight showed generated files modified (bodyFacts.generated.ts, famousStars.generated.ts, data/seeds/planet_facts.seed.json) — confirm intended (likely reliefM/seed regen) or revert; new files bodyAmbientLight.ts, clippedHeightSource.ts; verify earth-tiles manifest mtime unchanged after its dev bake; then dispatch the review.
- D2 DONE (Opus, 185 tool uses): T1 68aadbd52, T4 d94406a49, T5 20383d5bc; 44c9dd521..20383d5bc. Generated-file diffs intended (rover fact texts; famousStars reverted). Earth manifest mtime unchanged. Dev bake OK. Deviations: MOLA Float32 COG `…463m_f32_cog.tif` (amends C2; sharp clamps neg Int16) → Int16 `…_cog.tif` deletable (ASK USER); clipped sources; pole clamp; registry keyed by BodyTextureId. LANDMINE: Jezero README now tracked → main's untracked copy blocks pull on main (tell user).
- Review DONE (Opus, 32 tool uses): 9 findings, all 6 declared deviations OK. Top: Gale ortho internal mask ignored → black fringe (BUG); §4.3 reliefM assertion missing (FIDELITY); partial-overlap stretch (latent). Finding 4 (fold etopoHeightSource into geoTiffHeightSource, −~100 lines) = adjacent → ASK USER.
- Fix round DONE (Sonnet, 108 tool uses) → 4356fff7c: all findings except Gale mask. Gale mask UNFIXED: libvips throws on 1-bit mask IFD (dir 1); gdal reads it fine. PROPOSED to user: extract the mask to its own 8-bit COG (`-b mask`) and read it beside the RGB — needs a data/raw write → awaiting ruling.
- User 2026-09-17: ETOPO fold → YES on this PR (D3 Sonnet bg IN FLIGHT, must prove byte-identical ETOPO samples). Delete Int16 MOLA COG → YES, but auto-mode denied my rm → user runs it. Jezero README → user said "commit" (on main; I can't — isolated; main's copy differs from branch's, is shorter).
- T6 full bake: after D3 lands AND the Gale mask ruling (avoid baking the fringe twice).
- QUEUED after review + fix round: T6 full `--body mars` bake — ONLY after the user says main's Earth v3 re-bake is done AND tells me to verify v3 height tiles (verify: height tiles in public/data/images/earth-tiles carry SHGT v3 header). Then start dev server on this wt, eye-check poses in plan T6 (Earth pose deferred to after F3a merge).
- Earth v3 re-bake DONE (user, 2026-09-17). VERIFIED: all 20,684 earth-tiles/v9/height/*.webp carry SHGT v3, 883 B (oldest 02:05). earth-tiles manifest.json mtime baseline = 17 Sep 02:20 (main's re-bake) — D2's check compares against this. T6 gate is now only review + fix round.
- T7 gated on F3a. T8 docs at end.
- D3 DONE (Sonnet, 46 tool uses) → fa77c44f3: etopoHeightSource is a thin wrapper over geoTiffHeightSource (−88 lines), ETOPO samples byte-identical on 10 probe boxes (antimeridian + poles).
- Jezero README: main's untracked copy now IDENTICAL to branch's; user NEVER commits on main → before pulling main after this PR merges, delete main's untracked `data/raw/hirise/jezero/README.md` (git refuses to overwrite untracked files). Say this at landing.
- NEXT: Gale mask ruling (pending) → T6 full bake.
- Jezero README: user REMOVED main's untracked copy (2026-09-17) — landing note resolved.
- Ruling C3 (user 2026-09-17): Gale mask → own 8-bit COG `data/raw/hirise/gale/MSL_Gale_HiRISE-LRGB_78quads_sharp_mask_cog.tif` (gdal `-b mask`, DEFLATE, 512²) — bg job b1hsn7qma running. ON DONE: dispatch D4 (Sonnet): leaf symlink + registry row + README line, reader applies mask as alpha, re-run the reviewer probe scratchpad/gale.cjs; then T6 full bake.
- Mask COG DONE (03:14, 4.7 MB). D4 (Sonnet bg) IN FLIGHT: symlink + registry + README + C3 in plan, optional mask on geoTiffImagerySource (Gale only), test, real-data probe. ON RETURN: check probe before/after → T6 full `--body mars` bake.
- D4 (Sonnet) → 01a96ae41 then REVERTED 325c15e84: Gale mask is 255 everywhere (gdalinfo -stats) — review finding #1 was real dark ground (dunes). Mask file + symlink deleted. C3 void.
- Bake level overview given to user (global z3–z7 10,912/product; sites z10–z17 ~542–594/site; z8–z9 gap by spec, ancestor fallback).
- Controller finding 2026-09-17: Curiosity + Spirit rows were still LANDING sites (inconsistent with R1/R4; user wants "rovers correctly placed", AFK → my call, flag at return). Also Gusev/Endeavour grey stretches clip bright ground (Home Plate max 191 > 174).
- D5 (Opus bg) IN FLIGHT: Curiosity → sol 5016 RMC 125_198 (−4.8246039, 137.3884767, MSL_waypoints_current.json); Spirit → Troy (proxy search/visual); stretches from 0.1/99.9 pct over each window; R5 in plan. ON RETURN: check margins → T6 full bake (bg, log to this dir) → headless screenshots (playwright, window.__skymapPerf.dispatch(flyToLonLat)).
- Dev server: this wt on :5174 (bg shell b8hnr7246). Eye-check tool WORKS: `WAIT_MS=… SUN_ELEV_DEG=… node .superpowers/sdd/2026-09-17-terrain-f4-mars/shots.mjs http://localhost:5174 <outDir> name:lonE:lat:altKm[:headingRad] …` (sets clock so sun is at SUN_ELEV_DEG afternoon over the pose, nadir flyToLonLat, screenshot). Shots in ./shots/.
- D5 DONE (Opus, 64 tool uses) → 4d6c23b05: Curiosity sol 5016; Spirit −14.60036, 175.52576 (PROXY, eye-matched to PIA12205, ~8 m W of Home Plate; controller verified crop); stretches Gusev [10,206], Endeavour [58,247]; R5 in plan. Spirit window: E margin 87 m and ortho/DTM data void in easternmost ~300–400 m (12 %) → Viking/MOLA fill; flag at eye-check. Plan's old "Rover rows" line still lists old coords → fix in T8.
- T6 STARTED: dev-bake dir moved aside → public/data/images/mars-tiles.dev-bake-0917 (delete after full bake OK). Full bake bg b3l96lb7r, log bake-full.log here. Earth manifest baseline 02:20:46.
- Run 1 + dev server KILLED by the system (low memory, other apps) mid Endeavour z17; 3,915 files on disk; datum checks gale +47.4, jezero −12.3, gusev −1.3, endeavour −8.2 m (log bake-full-run1.log). Run 2 (resume) bg byhm615v7. Dev server NOT restarted until the bake ends (memory).
- T6 BAKE DONE (run 2 exit 0): 26,987 tiles indexed (albedo 410 MB, height 254 MB); manifest 5 bands with builtFrom both products; datum checks within ±200 m; Earth manifest mtime unchanged 02:20:46. Next: dev server + screenshots.
- EYE-CHECK round 1 (shots/): orbit Olympus/Valles + Hellas GOOD. PROBLEMS: (P1) low-alt site views blurry (~z12 look) though z17 tiles exist on disk — runtime; (P2) site window = hard square, height wall at edge (Opportunity NE), DTM vs MOLA step → needs feathering; (P3) grey sites look grey/dark vs Viking surround (colour match weak); Gale has grey strips (RED-only parts of the 78-quad mosaic); (P4) blocky Viking underfill in z10–z12 site tiles. NOTE flyToLonLat durationMs 0 does NOT move the camera — shots.mjs uses 1000.
- D6 (Opus bg) IN FLIGHT: P1 debug (runtime).
- D7 (Opus bg) IN FLIGHT in parallel (tools/ only): P2 feather height+albedo at window edge, P3 colour match (+Gale), P4 bilinear underfill. ON RETURN: delete the site tiles it names, re-bake (bake skips existing tiles), re-shoot.
- D6 DONE (Opus, 42 tool uses) → 612acc02f: P1 root cause = cutSurfaceTiles probe samples on the DATUM sphere (Mars ground +2.7 km above it → 10× too far); now samples at deepest resident height ancestor's subtreeMinM. 300 m over Gale zWin 13→16, sharp (scratchpad/shots/gale-300m-after.png). RISKS: touches the shared Earth walk (Earth re-check after F3a merge; F3a also edits cutSurfaceTiles → conflict likely); test has an unused destructure (final review). ADJACENT (ask user): scale bar measures from datum, not ground.
- D7 DONE (Opus, 67 tool uses): P2 a298962d1 (500 m feather ring, height MOLA→DTM + ortho alpha; featherWeight.ts), P4 cb6ee751c (bilinear upsample when box finer than raster), P3 no change (Viking itself grey-mauve at those sites; Gale's 250 m RED-only strip left as is — colour match would dull Gale). TILE_PREFIX stays v1 (never deployed).
- Site re-bake: z10–z17 moved aside → public/data/images/mars-tiles.sites-run1/ (delete after OK); bake bg, log bake-sites2.log. Then re-shoot all poses (shots.mjs, durationMs 1000).
- Site re-bake DONE (exit 0, 27,096 indexed, Earth manifest unchanged). EYE-CHECK round 2 (shots2/): edges feathered (gale/opp 15 km), Gale low sharp colour, Spirit shows Home Plate with the marker at its W edge, Jezero 6 km sharp. (Low shots below ground-level altitude look blurry — pose artefact, not a bug.) Possible: small dark dot ~130 px below Perseverance marker at 6 km — T7 check.
- LEFTOVERS for user: rm denied for public/data/images/mars-tiles.dev-bake-0917 and mars-tiles.sites-run1 (user to delete). F3a #744 still DRAFT → T7 + Earth pose + final review + merge wait.
- D8 (Sonnet bg) IN FLIGHT: T8 docs (+ plan rover line, backlog: scale-bar datum, MOLA z8–z9).
- D8 DONE (Sonnet) → 9944facab: spec §9/§10 as built, plan rover line + T6 bake boxes, DEPLOY mars line, BACKLOG (scale bar datum; MOLA z8–z9). DATA.md already covered.
- O4: no colour step seen at tile engagement in orbit shots (shots/orbit-*.png, shots2) — user to confirm by eye.
- STATE: all non-gated work DONE. Remaining: F3a merge → merge main (conflict likely in cutSurfaceTiles.ts) → T7 rovers-on-terrain + Earth poses + perf ask → final whole-branch review + one fix round → /feature-done → merge on user's word.
- Dev server :5174 killed again by low memory (06:48); NOT restarted — restart for T7.

## 2026-09-17 — F3a merged in
- User: rovers + zoom target sit km under the terrain. Cause: site placement + camera pivot use the bare 3,390 km datum (F3a routes placement only; camera = F3b item 1, docs/backlog/2026-09-17-terrain-f3b-remaining-routing.md).
- #744 merged → main eef0d8420; merged into branch as 1d5fe2861 (conflicts: cutSurfaceTiles → my range fn on main's deepestResidentAncestor; HeightTileHeader doc → main's; surfaceFixedSites header → branch's, altitudeM = lift above terrain). Typecheck + 201 files green; pushed. Dev server :5174 restarted.
- Shots (scratchpad shots3): labels now read on the ground at Gale/Jezero (top-down; placement fixed by F3a).
- ASKED: F3b item 1 (camera anchor/pivot/pixel-lock at terrain radius) (a) on this PR [recommended] or (b) own PR. Not started.
- User ruled: camera fix on THIS PR, first. DONE cdbcdb46b: sitePointBodyFixed/sitePose*/hostedFocusPivotM take GroundRadiusLookup (host.groundRadiusAtM); siteRung + bodyRung + deriveBodyStates route it; regression test in siteRung.test (mutation-verified). Gesture picks without a rover focus (pickOnBody, surfaceZoomStep) stay datum → backlog item 1 remainder.
- NEXT (user asked, not yet ruled): base globe from Viking MDIM instead of SSS 8k_mars.jpg — colour step between globe and tiles.
- Colour bench artifact published: https://claude.ai/code/artifact/74971767-2277-4384-b6f0-d11344dd8196 (source scratchpad/colour/: template.html + lightjs.txt + patch.py + build.py; crops from viking/sss/MOLA at 60–150E ±30 and globe). User tunes per-texture delight (MOLA relief-shade divide, flatten, rim knee, ice keep) + grade, and a shading preview; hands back JSON. NEXT: apply the returned settings in the bake (Viking tiles + a Viking-based base globe) — design the bake-side delight once numbers arrive.
- RULED: albedo bench = committed tool (tools/albedo-bench + tools/utils/textures fns), SEPARATE PR off main; base globe probably Viking-based, depends on cleanup quality. Handoff brief: albedo-bench-brief.md (+ albedo-bench-prototype/). F4 then wires marsAlbedoRecipe.json into marsSurfaceBake + re-bakes (after that PR merges).
- User handed back recipe (JSON in brief-albedo-recipe.md) + "land this now, refine later" (the bench PR replaces it later). D-albedo (Sonnet, bg) dispatched on brief-albedo-recipe.md: delightedImagerySource (coarse z5 S/B fields + per-pixel knee/ice/grade), gradeRgbaInPlace, ColourTreatment 'grade' for the SSS globe, Gale colour-matched too, TILE_PREFIX v2. ON RETURN: review diff → typecheck/tests → dev bake spot-check (--dev? then look) → full `buildSurfaceTiles --body mars` + mars globe texture build + build-data-manifest (writes main public/data via symlink) → push → user eye-check. Then prune mars-tiles/v1 (ask).
- D-albedo landed 0a6c33407 (reviewed; coarse field at z5 lattice = 0.0879°/px, not 0.0703 — my arithmetic slip). Preview (previewAlbedo.ts) showed red/cyan rim fringes = dark shadows' colour noise multiplied 3–4x; fixed 07f3f799f (brightening adds neutral grey; graded overflow scales all channels). Main merged 4412360f1 (Earth v10 #747, settings #745); pushed.
- F3a fully closed (user). Full v2 bake RUNNING bg b3965ik4i: buildSurfaceTiles --body mars && buildTextures && buildDataManifest, log bake-v2.log. ON DONE: restart :5174 if killed, eye-check shots (shots.mjs) + ask user to look; then ask to prune mars-tiles/v1 + the two leftover dirs; then final review → /feature-done.
- v2 BAKE DONE (exit 0). buildTextures had skipped mars:surface (worktree has no raw 8k_mars.jpg) → leaf symlink data/raw/textures/8k_mars.jpg → main's, re-ran: mars-2048/4096/8192 graded + body-atlas. Manifest skip harmless (manifest has no image entries). Shots: scratchpad shots-v2 (gale 3 km colour-matched sharp; orbit good; my jezero/opp poses were guesses, off-target). ASKED user to look live + prune ruling (mars-tiles/v1, mars-tiles.dev-bake-0917, mars-tiles.sites-run1).
- Albedo-bench prep PR #749 (draft, mars-geotiff-readers 3b5670b9f, CI green) — agent worktree agent-a880e5ca821738288 to remove at cleanup. After #749 merges: merge main here (readers now on main → conflicts likely identical).
- Terrain-aware camera gestures (F3b item 1 remainder): user hands to ANOTHER agent. Contact-shadow decal: backlog stage 1 (c841e818b), own PR after F4 on user's go.
- User eye-check v2: "looks good". Prune ruling still unasked-answered (don't delete).
- #749 MERGED → 38854045c (user go); agent worktree + local/remote branches removed. TODO: merge origin/main into this branch AFTER the final review returns (reviewer reads origin/main...HEAD).
- FINAL REVIEW (Opus bg) IN FLIGHT. ON RETURN: one fix round (Sonnet) → merge origin/main (#749 overlap) → typecheck/tests → push → perf-gate ask + Earth eye-check → /feature-done.
- User ruling: NO perf gate for F4.
- mars-tiles/v1 DELETED (641 MB, user go) + its 27,096 index.txt lines pruned (27,096 v2 left). The two leftover dirs (dev-bake-0917, sites-run1) NOT ruled.
- Leftover dirs deleted (user go). No perf gate (user).
- FINAL REVIEW returned: no correctness blocker. Findings: (1) Mars atmosphere shell moved down ~2 km via innerBoundRadiusM (reliefM) → density at sites −17% — needs user eye-check/ruling; (2) tautological assertInsideReliefM; (3) delight test gap; (4) cut lift at body min until deep ranges load (by design, Tharsis eye-check); (5) NaN DTM post in feather ring → MOLA step (plausible); (6) dead rowBottom<rowTop; (7) delightedImagerySource hardcodes Mars radius (accept until bench replaces); (8) MOLA nodata NaN (no voids today); (9) nits.
- ROVER BUG root-caused: deriveBodyStates memo keyed on simDays → paused clock freezes site height at 0 (pre-tile-load). User confirmed (unpause snaps). RULED: bake site ground heights at build time into placement data (generated file), drop runtime lookup from site placement.
- D-siteheights (Sonnet bg) IN FLIGHT: commit 1 = generated siteGroundHeights + un-route site lookup (cdbcdb46b parts); commit 2 = review fixes 2,3,6,9. ON RETURN: review diff, check user sees rovers seated with clock paused; ASK user on findings 1 (atmosphere), 5, 4; then merge origin/main (#749 overlap) → /feature-done.
- NEW BUG (user): near Perseverance at grazing angle (siteArm heading −0.546, elev 0.0325, range 12.23 m, eyeHeight 0.398 m, simDays 2461307.8555) the tile under the rover doesn't draw. D-debug (Opus bg, read-only) IN FLIGHT: root cause + proposed fix + failing test. ON RETURN: fix after D-siteheights lands (same files region).
- D-debug DONE: hole = cutSurfaceTiles.ts:197-203 frustum sphere centred on DATUM (relief only in radius, capped at corner chord) → Mars nodes ~4 km underground culled by bottom plane; 612acc02f lifted step 3 but not this step 2. Fix (tested in copy cutFixed.ts): centre at subtree mid-height, pad min(half-range, chord); pose2 26/72 holes → 0. Failing test spec in agent report (synthetic residency [4246,4277], heading 0.785 fails). SEPARATE bug: site-arm eye has no terrain floor (clampedSitePose only) → uphill eye −0.88 m → back-face cull (surfaceTileRenderer.ts:160). ASKED user: both on this PR?
- RULED: both fixes (cull sphere mid-height + site-arm eye terrain floor) on THIS PR. Dispatch AFTER D-siteheights returns (overlapping camera site files).
- D-siteheights DONE: 488fd32b4 (baked SITE_GROUND_HEIGHTS generated file + generator; siteGroundRadiusM; sitePointBodyFixed/sitePose*/hostedFocusPivotM back to numbers; live lookup kept for camera floor/bodyRung/hostedFocusOverHorizon floor; real bug fixed in hostedFocusOverHorizon lift) + 949c3eaf2 (review fixes 2,3,6,9). Full suite green, pushed. NIT: unused 'i0' in delightedImagerySource.test.ts:57. D-fixes (Sonnet bg) IN FLIGHT: commit cull-sphere mid-height + commit site-eye terrain floor. ON RETURN: user eye-check paused-clock rovers + Perseverance tile; findings 1/4/5 asks; merge main; /feature-done.
- D-fixes (Sonnet) STOPPED uncommitted: mid-centred cull breaks 3 pinned Earth tests (node 8/143/53, unit-length, Everest 13/6076/1412); its 'old was loose' evidence corners-only. Controller: clamp min(halfRelief,chord) is NON-conservative once mid-centred (fallback ranges float sphere 4–13 km up). Dropped its leftover stash (identical to tree). D-fixes2 (Opus bg) IN FLIGHT: provably conservative formula (unclamped halfRelief, bound range not pad), exact-geometry verdict per Earth test, then fix 2 site-eye floor; no stash.
- D-fixes2 DONE fix 1 → 775a17a66 (range clipped to body reliefM; sphere at mid, radius 1.5chord+halfRange unclamped; Earth tests re-pinned on exact-geometry evidence; +deep-patch hole test; worst-case 0–42% more survivors pre-deep-ranges). Fix 2 STOPPED: ASKED user A (site 0.2 bounding-radius clearance vs LIVE ground under eye, in clampedSitePose; recommended) / B (host floor ~8 m, kills close-ups) / C (world-arm floor, no). ADJACENT asked: step-3 projected-bbox cull samples only lowest height (peaks at screen edge can drop).
- User RULED fix 2 = option A (keep heading+range, raise elevation to clear live ground under eye + 0.2 bounding radius). D-eyefloor (Sonnet bg) IN FLIGHT. Step-3 bbox adjacent finding still unanswered.
- User: STOP fixing on this branch, land it; other agent takes camera fixes. Stopped D-eyefloor (no changes). Handoffs: docs/backlog/2026-09-17-site-arm-eye-terrain-floor.md (option A ruled) + 2026-09-17-mars-terrain-followups.md (step-3 cull, atmosphere shift, NaN ring, Tharsis, delight radius). Merged main (#746, #749, #751 camera relief shells) → c61bcc32c; conflicts DATA.md/geoTiffHeightSource (ours), BACKLOG (zoom-anchor line dropped, main picked it up). npm install (earcut). typecheck clean, 8731 tests green, pushed. NEXT: CI → /feature-done → merge on user word.
- Deletion audit: ~−63 safe-now → D-cleanup (Sonnet bg) IN FLIGHT. Rulings: (1) keep reliefM clip; (2) don't simplify ambient switch; (3) site-height step must be GENERIC (Earth soon) — today it's 'bodyId === mars' in buildSurfaceTiles.ts:1204 → make generic after D-cleanup. Plan boxes ticked (uncommitted); spec stays (F3b/F3c open).
- Rulings applied: site-height step generic (e22d63997). /feature-done READY → completion commit.
