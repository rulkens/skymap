# SDD ledger — plan: docs/superpowers/plans/2026-09-10-scene-workbench-2-splats.md

Spec: docs/superpowers/specs/2026-09-10-scene-workbench-2-splats-design.md (merged #673).
Execution branch: `scene-workbench-2-splats` (worktree `.claude/worktrees/scene-workbench-splats`), draft PR #676.
Session: 2026-09-10. No harness task-list tool in this session — this ledger is the task board.

User rulings (2026-09-10, before dispatch): parallelism = 3; task 8 (real fetch + bake)
AUTO-STARTS in the background the moment task 7's review is clean.

## Plan review (main thread, before execution)

11 fixes applied by one sonnet round (commit: see below). Substantive ones: CctRunner carries
the pipeline string; T4 fixture ground truth read off the JPEG (was a mirror test); provenance
needs `source` + `sourceVintage` (+ `datetime` on SkraafotoStacItem); T3 destDir pinned; T11
two-variant mechanism pinned (two vertex entry points, statically-used-binding validation);
T12 bucket sort not comparator sort; T8 brush already installed; T5 parsePoints returns raw
records; commit-template boilerplate collapsed.

## Pre-flight scan

| Pair / task | Produces → consumes | Finding |
| --- | --- | --- |
| T1 → T2 | `GaussianSplatRecord` (packSplats.ts) → readGaussianPly return | consistent; T2 must follow T1 (file must exist) |
| T1 → T6 | `parseSplats`/`ParsedGaussianSplats` → uploadGaussianSplat/loadGaussianSplat | consistent |
| T1 → T13 | `packSplats` in the browser probe scene | pure; f16 encoder must be browser-safe (`src/utils/math/f32ToF16Bits.ts` is) |
| T3 → T4 | `SkraafotoStacItem` → photoPoseFromStacItem | consistent after fix 5 (`datetime` added) |
| T3 → T7 | destDir `rawDataPath('skraafoto.dir')/<collection>` → bakeSplats glob; both edit package.json | consistent; serial |
| T4 → T5 | `PhotoPose` (cam→world rotation) → writeColmapModel conjugate | consistent |
| T4 → T7 | `CctRunner(pipeline, lines)` → main() wires spawn | consistent after fix 1 |
| T5 → T7 | writeColmapModel spec, pointSampleTarget 200_000 | consistent |
| T6 → T7 | `GaussianSplatAsset` + unchanged nextManifest/upsertAsset; provenance needs sourceVintage | consistent after fix 5 |
| T6 → T12 | `SplatGpuAsset.positionsM`/`order` → saga | consistent |
| T9 → T11 | `lib/sceneCamera.wesl` import; `1/cam.metresPerPx` as focal px | verified: writeSceneCamera doc says metres per pixel PER METRE of depth → focal = 1/metresPerPx holds |
| T9 → T10 | `write(view, pointSizePx, splatScale, opacityScale)`; T9 hardcodes 1,1, T10 swaps in store reads | consistent |
| Viewport.tsx chain | T9 (call site) → T10 (store reads) → T6 (visibleAssets narrowing) → T11 (splat draw) | distinct hunks but same file → serialized in that order |
| T10 → T12 | `commitCameraPose` exists (viewSlice.ts:37) | consistent |
| T12 | `assetStatusChanged({assetId,status})` exists (groupSlice.ts:35) | consistent |
| T13 | `expect` needed for `toHaveCount` — probe already imports from `@playwright/test` | consistent |
| Global constraint "one function per file under tools/scene-recon/**" vs packSplats.ts/topocentricPositionsM.ts/bakeSplats.ts/assetCount.ts each exporting a companion type | Ruling: a companion type beside the one function is the standing precedent (`packPoints.ts` exports `ScenePoint`, `bakeLidar.ts` exports `PdalRunner`) — not a violation. Cost if wrong: a rename sweep later. |
| T3 README licence wording | requires live lookup at dataforsyningen.dk/Vilkaar | implementer must WebFetch; if unreachable, report DONE_WITH_CONCERNS with a placeholder marked, controller resolves |

Per-task self-consistency: T1 tests decode via DataView + `f16ToFloat` (distinct from the encoder) ✓; T2 fixture hand-built ✓; T4 fixture procedure independent after fix 2 ✓; T5 golden exact text ✓; T6 LayerList test ✓; T9 parity asserts 192 ✓; T10 slider tests via store ✓; T12 two pure tests ✓.

## Dependency waves (3 parallel, own agent worktree per parallel task, cherry-pick -x onto the execution branch in task order)

- Wave 1: T1, T3, T9
- Wave 2: T2 (after T1), T4 (after T3), T10 (after T9)
- Wave 3: T5 (after T4), T6 (after T1 + T10)
- Wave 4: T7 (after T2, T4, T5, T6), T11 (after T6, T9, T10), T12 (after T6)
- T8 (operator, auto-start after T7 clean) runs in the background on this worktree
- Wave 5: T13 (after T11, T12)
- T14 (operator) after T8 + T13

Worktree recipe for parallel implementers: `isolation: "worktree"`, then `git fetch origin && git switch -C task-<N> origin/scene-workbench-2-splats`, symlink `node_modules` from the main checkout, commit on `task-<N>`, never push. Serial single tasks may run directly in the execution worktree.

## Models

Implementers: opus default; T10 sonnet (mechanical UI); T11 fable (keystone shader/renderer). Reviewers: sonnet for small/mechanical diffs, opus for T1/T4/T5/T6/T9/T11; final whole-branch review opus.

## Task log

Plan review fixes: commit 9bed62ea5 (pushed). Execution BASE for wave 1: 9bed62ea5.
Wave 1 dispatched (opus ×3, isolation worktrees, branches task-1/task-3/task-9): T1 agent acbe6f00ad3ed0b78, T3 agent ae24033db66539d13, T9 agent ae220e5e1784360db.
Task 1: implementer DONE_WITH_CONCERNS (Vec4 mutability; fRest not validated at deg 1; brief cited src/utils/math/f16BitsToFloat.ts but it lives in tools/utils/math/) — cherry-picked 811089352 → 1f0061d9a on execution branch; typecheck + focused tests green; pushed. Review package review-9bed62ea5..1f0061d9a.diff; reviewer (opus) dispatched. Wave 2: T2 implementer (opus, worktree task-2) dispatched off 1f0061d9a.
Task 9: implementer DONE_WITH_CONCERNS (camera bind-group visibility VERTEX-only — T11 must add FRAGMENT if fs reads cam; node_modules symlink shows untracked in agent worktrees, stage by path; no on-device frame) — cherry-picked 45fd139ae → c92531695; typecheck + scene-workbench tests green; pushed. Review package review-1f0061d9a..c92531695.diff; reviewer (opus) dispatched. T10 implementer (sonnet, worktree task-10) dispatched off c92531695. Note: isolation-worktree agents may be unable to write to the controller's .superpowers dir — copy reports from `<agent-worktree>/.superpowers/sdd/<plan>/` when they say so.
Task 1: review clean (opus). minor (deferred): `rotation: Vec4`/`logScale: Vec3` could be `Readonly<…>` at the non-mutating boundary (plan pins `Vec4` verbatim — Ruling: keep plan shape, revisit at final review; cost if wrong: a two-line type tweak) · fRest ordering comment ambiguous (packer needs channel-major R0R1R2 G0G1G2 B0B1B2; T2 dispatch already carries the slicing rule) · packer silently zero-fills null/short fRest at deg 1 · tolerance-rationale comment dropped vs packPoints test · pad bytes never asserted zero.
Task 1: complete (commits 9bed62ea5..1f0061d9a, review clean)
Task 3: implementer DONE_WITH_CONCERNS — live data: `pixel_spacing` is `[x, y]` mm (Ruling: live data wins, type ships as `readonly [number, number]`; T4 told; cost if wrong: none — it IS the data), `proj:shape` = [rows, cols]; licence CC BY 4.0 (spec §10 Q4 answered); touched nextManifest.test.ts fixture (required cascade); GDAL_PAM_ENABLED=NO + limit warning unrequested (reviewer judges). Live check: 306 items, one-item gdal_translate 1433×1920 OK. Cherry-picked 25266a21c → e76e9e510; typecheck + scene-recon/utils suites green; pushed. Reviewer (opus) dispatched on review-c92531695..e76e9e510.diff. T4 implementer (opus, worktree task-4) dispatched off e76e9e510.
Task 9: review clean (opus). minor (deferred): lib/sceneCamera.wesl 7/12 comment lines (one over budget; camera.wesl precedent says so in-header) · writeSceneCamera.ts:24 "≥ 46 floats" should read `≥ SCENE_CAMERA_BYTES / 4` · view assertion would pass if proj were written at 28 (eyeM→origin property check would close it) · no wesl link() test in repo — runtime-only failure mode, remember for T11.
Task 9: complete (commits 1f0061d9a..c92531695, review clean)
Task 2: implementer DONE — cherry-picked 566bfc4e0 → ef69c3f33; scene-recon suite 36/36 + typecheck green; pushed. Reviewer (opus) dispatched on review-e76e9e510..ef69c3f33.diff. Note for T7: reader returns unrounded float dcColor / unquantized fRest — pass straight into packSplats.
Task 10: implementer DONE — cherry-picked ca5fb5ef5 → 059bc5ae4; scene-workbench 19/19 + typecheck green; pushed. Reviewer (sonnet) dispatched on review-ef69c3f33..059bc5ae4.diff.
Wave 3: T6 implementer (opus, worktree task-6) dispatched off 059bc5ae4. T5 waits on T4 (in flight).
Task 3: review (opus) — Important: failed frame orphans `<itemId>.json` while README claims no leftovers (T5/T7 enumerate `*.json`). minor (deferred): `-outsize <pct>%` would retire the [rows,cols] landmine; empty/absent `features` exits 0 silently; SkraafotoStacItem.d.ts over comment budget; `exit null` on signal; README blockquote near-quote. Fix round 1 dispatched: resumed implementer ae24033db66539d13 (Important + two cheap minors optional). Freeze: no new implementers until this closes (T6 already in flight runs to completion).
Task 3: fix round 1/5 (1 Important + 2 minors addressed by implementer — Ruling: implementer's write-reorder (JSON written last) accepted over the prescribed rmSync: it also covers Ctrl-C mid-download; cost if wrong: a JPEG-without-JSON leftover, invisible to *.json enumeration and re-fetched on resume; commit 3682116b0 → e9edd71f1). Re-review (sonnet) dispatched on review-059bc5ae4..e9edd71f1.diff.
Task 10: review clean (sonnet). minor (deferred): two new Slider blocks repeat the point-size slider's shape (3 call sites, extraction premature); growing viewSlice import line.
Task 10: complete (commits ef69c3f33..059bc5ae4, review clean)
Task 2: review (opus) — Important: quaternions un-normalized while packer clamps ±1 (spec-silent gap). Ruling: reader normalizes (idempotent; zero-length → identity); cost if wrong: none (unit input unchanged). minor (deferred unless taken in the fix): CRLF end_header; missing `format` line accepted; bodyOffset assumes vertex element first; tuple casts; 3 rejections in one `it`; shDegree via count/3≥3 rather than f_rest_0 presence (benign). Fix round 1 dispatched: resumed implementer a73333d93f8125579.
Task 3: re-review (sonnet) — all addressed, no new breakage. minor (deferred): SkraafotoStacItem.d.ts 9/17 comment lines (marginally over); missing-gdal ENOENT would print 306× (no install-hint short-circuit).
Task 3: complete (commits c92531695..e76e9e510 + fix 3682116b0 → e9edd71f1, review clean after 1 fix round)
Task 6: implementer DONE — cherry-picked dec929baa → 3e228442b; scene-workbench 21 + scene-recon 57 tests + typecheck green; pushed. Reviewer (opus) dispatched on review-e9edd71f1..3e228442b.diff. Note for T12: `order` is VERTEX|COPY_DST (a GPU-side sort would need STORAGE — not this plan).
Ruling: freeze-on-Important (T2 fix loop open) lifted for T11/T12 dispatch — the rule's rationale (fixes edit the shared tree) doesn't hold with isolated agent worktrees; T2's fix touches only readGaussianPly.ts. Cost if wrong: none to T11/T12 (disjoint files).
Wave 4 (partial): T11 implementer (fable, worktree task-11) + T12 implementer (opus, worktree task-12) dispatched off 3e228442b. T7 waits on T2 fix + T4 + T5.
Task 6: review clean (opus). minor (deferred): `assetCount(asset)` called twice per LayerList row (plan-mandated JSX); `as never` would be the narrower cast in assetCount.ts:20; single-use `uploadSh1` closure breaks the flat create→write rhythm; LayerList fixture split proportional-not-minimal.
Task 6: complete (commits e9edd71f1..3e228442b, review clean)
Task 2: fix round 1/5 (1 Important + 4 minors addressed by implementer; fixture VERTEX_A quaternion made unit; commit fec128cff → 2aba068d0, scene-recon 40/40 + typecheck green, pushed). Re-review (sonnet) dispatched on review-3e228442b..2aba068d0.diff.
Task 2: re-review (sonnet) — all 5 addressed, fixture change sound, no new breakage.
Task 2: complete (commits e76e9e510..ef69c3f33 + fix fec128cff → 2aba068d0, review clean after 1 fix round)
Task 11: implementer DONE_WITH_CONCERNS (fable) — splatScale applied to Σ2D (Gaussian grows with quad, not quad-only); fs never reads cam so uniform visibility untouched; naga-validated + finite-difference Jacobian check 3e-4; no GPU frame (no data yet). Cherry-picked 0c417b30e → 206f3e807; typecheck + scene-workbench/conventions 1243 tests green; pushed. Reviewer (opus) dispatched on review-2aba068d0..206f3e807.diff.
Status board: user asked for a task-list artifact (2026-09-10 mid-run) — building from this ledger; republish on each state change.
Status board artifact: https://claude.ai/code/artifact/86210d40-3b05-4ad9-8fe6-44a7764977ba (source scratchpad/splats-board.html — edit TASKS/UPDATED, republish same path).
Task 12: implementer DONE — cherry-picked d9d38308f → 92b57670c; scene-workbench 23/23 + typecheck green; pushed. 1 M sort ~8 ms steady, 41–51 ms GC outliers (per-call scratch). Reviewer (opus) dispatched on review-206f3e807..92b57670c.diff.
Wave 5: T13 implementer (opus, worktree task-13) dispatched off 92b57670c — the GPU probe gate; may make a narrow renderer fix in a separate commit.
Task 11: review clean (opus; depth sign, focal identity, rightM/upM≡lookAt axes re-derived independently). Ruling: splatScale multiplies Σ2D by s² (canonical 3DGS scaling modifier) — spec §7.4's "multiplies the 3σ half-extent" sentence to be AMENDED in the docs pass, not the code; cost if wrong: one-line shader change. minor (deferred): `min(alpha, 0.99)` transplant unexplained; clippedVertex relies on WGSL zero-init; draw doesn't skip zero-splat assets (lidar same); `as Extract<GpuAsset,…>` cast in Viewport; numeric-keyed pipeline tables.
Task 11: complete (commits 2aba068d0..206f3e807, review clean)
Docs TODO at finish: amend spec §7.4 splatScale sentence; README (T14) carries the two credentials.
Task 12: review (opus) — Important (plan-mandated): wheel zoom commits a pose per frame → 8 ms sort per frame for an unchanged order. Ruling: saga skips the sort when forwardM is unchanged (translation preserves depth order); assetStatusChanged ready always sorts; reset on empty. Cost if wrong: a missed re-sort only if a rotation somehow arrives with identical forward (impossible by construction). minor taken: Uint16 key buffer (8 MB→2 MB scratch), commands.ts comment trim. minor (deferred): `put` per asset; import order; Readonly<Vec3> params; brief's Viewport line drift. Fix round 1 dispatched: resumed implementer a019b83929f3a6a37.
Task 13: implementer DONE — probe PASS on first browser contact (apple/metal-3, 6/6 steps, no GPU/page/console errors); 320 synthetic blobs render depth-tested against the lidar plane. No renderer fix needed. Cherry-picked e7924a86c → 0707dde78. Reviewer (sonnet) dispatched on review-92b57670c..0707dde78.diff. Concerns: splat cloud covers the box from boot pose (deliberate); provenance.source 'userPhotoCapture' author's call; splat-layer toggle not probed.
Task 12: fix round 1/5 (1 Important + 2 minors addressed — per-watcher SortMemo via takeLatest extra arg, skip on unchanged forward, always-sort on ready, reset on empty; Uint16 keys: 1 M steady ~7.9 ms, worst 11.7 ms, GC outliers gone; commit e25d13567 → 771acbc39). Re-review (sonnet) dispatched on review-0707dde78..771acbc39.diff.
Both pushed; typecheck + scene-workbench 23/23 green at 771acbc39.
Task 13: review clean (sonnet). minor (deferred): provenance.source 'userPhotoCapture' for a synthetic asset (union has no synthetic value); splat cloud hides the box at boot pose; splat-layer toggle unprobed.
Task 13: complete (commits 92b57670c..0707dde78, review clean)
Task 12: re-review (sonnet) — all 3 addressed, no new breakage. Out-of-scope: guard early-returns leave memo untouched (intended); memo behaviours untested by ruling.
Task 12: complete (commits 206f3e807..92b57670c + fix e25d13567 → 771acbc39, review clean after 1 fix round)
Dev server: `npm run scene-workbench` running on :5600 in this worktree (log scratchpad/scene-workbench-dev.log); ?probe shows the synthetic splats.
Remaining: T4 (in flight, agent ~280k tokens — do NOT resume for fixes past 350k; fresh dispatch instead), T5, T7, T8 (auto bake), T14; then final whole-branch review.

Task 4: implementer DONE_WITH_CONCERNS (agent a54ba754fdd36f47d, 389k tokens, 95 min — drifted into NCC image registration, RETIRED; do not resume). Commit b6879db47 → cherry-picked ab99685fe; typecheck + scene-recon (12 files/43 tests) green; pushed. Fixture anchor (665,644)±3 px by overlay sweep against 2025 ortho; pose projects 0.42 px off. Concerns: 15 px tolerance barely discriminates Rz(γ) (15.04 px); ppy sign unverified (offset [0,0] on whole flight); implementer settled collinearity = TRANSPOSE of Rx(ω)Ry(φ)Rz(κ) + Rz(+γ) on the right — reviewer told to verify against oblique `direction`. Reviewer (opus, a7942a04fd64c46f9) dispatched on review-771acbc39..ab99685fe.diff → task-4-review.md. T5 implementer (opus, ac77c2bff8b4cd545, worktree task-5) dispatched off ab99685fe; BASE for T5 review = ab99685fe.

Task 5: implementer DONE_WITH_CONCERNS (agent ac77c2bff8b4cd545, 90k tokens, 7 min). Commit d4eea52d9 → cherry-picked f9ff78bc7; typecheck + scene-recon (13 files/48 tests) green; pushed. Deviations: pose B = (0,0,0.6,0.8) ≈73.74° about Z instead of 90° (float-exact golden; Ruling: accepted, a 90° golden would encode summation order — cost if wrong: none, the fixture still discriminates transpose/conjugate); async copyFile not copyFileSync (contract is Promise<void>, ruling: fine). Flagged for T7: writeColmapModel passes pose.imageUrl straight to copyFile while photoPoseFromStacItem emits bare `${id}.jpg` — T7 brief must resolve imageUrl against the collection dir (carry into T7 dispatch). Reviewer (opus, a470ad16fdb662e7c) dispatched on review-ab99685fe..f9ff78bc7.diff → task-5-review.md. T7 waits on T4 + T5 reviews clean.

Task 5: review (opus) — spec ✅, quality changes-needed: I1 frame-arrow comments backwards (3 sites); I2 both goldens pure-Z, off-axis terms unexercised (add pose [0.5,0.5,0.5,0.5] @ [10,20,30] → `3 0.5 -0.5 -0.5 -0.5 -20 -30 -10 3 photo_c.jpg`, reviewer-verified); I3 producer emits bare `${id}.jpg`, copyFile would ENOENT at bake (Ruling: Task 7's bakeSplats resolves imageUrl to absolute before staging, with a test; T5 untouched — cost if wrong: an ENOENT at the operator bake, caught by T7's test). Minors deferred: M1 (dup `${id}.jpg`, folded into the fix round), M2 buffer.slice doubles peak memory (~26 MB, ignore), M3 raw ENOENT after txt written, M4 degenerate inputs single newline, M5 format asserted vs COLMAP reader not Brush (T8 exercises Brush), M6 f64 print of f32 coords. Fix round 1 dispatched: resumed implementer ac77c2bff8b4cd545 (I1+I2+M1).

Task 5: fix round 1/5 — I1+I2+M1 addressed (commit 06c879569 → 49f937064, pushed). Re-review (sonnet) clean: photo_c golden hand-verified. Task 5: complete.
Task 4: review (opus) — spec ✅ (transpose convention independently verified 3 ways); quality changes-needed: I1 tolerance 15 px leaves 0.08 px margin vs γ-dropped mutation (→ 8 px); I2 transpose ruling has no committed fixture/test (nadir-only fixture; 1.8 px discrimination) → oblique fixture + direction assertion. Minors deferred (6, see task-4-review.md). Fix round 1 dispatched: FRESH implementer a7bb0ed58c3b95594 (opus, isolation worktree, branch task-4-fix off 49f937064; oblique west item supplied from scratchpad item0.json; adds `direction` to SkraafotoStacItem). Ruling: fix files (poses/, fixtures/, SkraafotoStacItem.d.ts) disjoint from T7's, so T7 dispatched in parallel — cost if wrong: a cherry-pick conflict on SkraafotoStacItem.d.ts, trivially resolved.
Task 7: implementer a33970fc8f42a0041 (opus, isolation worktree, branch task-7 off 49f937064) dispatched. Carried: resolve imageUrl to absolute (T5 I3) with a minimal test; recompute downsample scale 1920/max(proj:shape); do not edit SkraafotoStacItem.d.ts. BASE for T7 review = 49f937064 (plus whatever T4-fix lands first — record at package time).

Task 4: fix round 1/5 — fresh implementer a7bb0ed58c3b95594: tolerance 8 px, oblique west fixture 2025_84_40_5_0052_00001969_100mm + optical-axis-points-west test (fails under transpose: E −0.033), `direction` union added to SkraafotoStacItem. Commit fc434b459 → 176507865; scene-recon 13 files/49 tests + typecheck green; pushed. Note: ppy sign still unverified (offset [0,6.68] on the oblique but no ground-truth pixel) — carry to final review as a known residual. Re-review (sonnet, acd119ff7f0b6d388) dispatched on review-49f937064..176507865.diff → task-4-rereview.md. T7 BASE for its review package: T7 branched off 49f937064; cherry-pick onto 176507865, so package from 176507865.

Task 4: re-review (sonnet) clean — I1 + I2 resolved; note: test comment cites pers:rotation_matrix which the trimmed fixture omits (docs-only, carry to final review). Task 4: complete.
Task 7: implementer COMPLETE (agent a33970fc8f42a0041, 119k tokens, 7 min). Commit ae95d91d6 → cherry-picked c5c0e15d9; scene-recon 14 files/50 tests + typecheck green; pushed. Controller verified real `brush-cli 1.0.0` accepts --sh-degree/--total-train-iters/--max-resolution/--export-path/--export-name; export-path is documented relative to the dataset's parent but bakeSplats passes an absolute colmapDir (rawDataPath = resolve()). Deviations: runCct joined deps; brushVersion probed before staging. Test uses process.chdir (vitest forks pool). Reviewer (opus, a7c6f9de76d722923) dispatched on review-176507865..c5c0e15d9.diff → task-7-review.md. On clean → T8 AUTO-START.

Task 7: review (opus) — spec ✅ (deviations accepted; shDegree from the PLY is right); quality changes-needed: I1 runBrush→readGaussianPly unguarded (stale final.ply reused, zero-splat asset shipped, bare ENOENT); I2 LONG_EDGE_PX=1920 + scale derivation duplicated across fetchSkraafoto/bakeSplats (Ruling: extract `tools/utils/skraafoto/skraafotoDownsampleScale.ts`, one function + module constant, both callers use it — cost if wrong: one small util file). Minors: stdin error handler (folded into fix), pipeline.split(' ') coupling, process.chdir/pool note, flags unverified vs real binary (controller verified --help; export-name/--version exercised by T8). Fix round 1 dispatched: resumed implementer a33970fc8f42a0041 (I1+I2+M1).

Task 7: fix round 1/5 — I1+I2+M1 (commit b8b331fa4 → f47488742; 21 files/137 tests + typecheck green; pushed). Ruling: `LONG_EDGE_PX` exported beside `skraafotoDownsampleScale` (constant, not a second function) accepted for `--max-resolution` — cost if wrong: a one-line re-home at final review. Re-review (sonnet, a3a71fc3fcc59601d) dispatched on review-c5c0e15d9..f47488742.diff → task-7-rereview.md.

Task 7: re-review (sonnet) clean (I1/I2/M1 resolved; LONG_EDGE_PX export judged the cleanest route). Task 7: complete.
Task 8: AUTO-STARTED 2026-09-10 12:42 UTC (user ruling) — background shell task b1r1xoco2 runs scratchpad/bake-chain.sh: `npm run fetch-skraafoto` (log scratchpad/fetch-skraafoto.log, ~4 s/frame → ~20 min for 306) then `npm run bake-splats` (log scratchpad/bake-splats.log; brush 30000 iters, hours); step markers in scratchpad/bake-chain.log. Preconditions verified: lidar points.bin present, keychain entry present, cct/gdal_translate/brush-cli on PATH. On completion: record splatCount/shDegree/wall time here; check manifest.json + scenes.json; T14 visual check on :5600.

Task 14 (README half): sonnet a95b1c0d9aefb4d78 dispatched in isolation worktree, branch task-14-readme off f47488742; leaves a placeholder for bake numbers. On DONE: cherry-pick -x, prettier check, push; quick controller read (docs, no reviewer needed beyond a skim for LLM tells). Bake numbers filled in by the controller after T8 exits.

Task 14 (README half): sonnet commit 0d19f32d5 → d7d806b9d; controller follow-up commit (splatScale sentence → covariance s², unwrapped code span) pushed. Placeholder for bake numbers at README "bake-splats" step. Remaining T14: fill numbers after T8, operator visual check.

Final whole-branch review: dispatched EARLY (Ruling: all code landed; only README numbers follow — cost if wrong: a trivial re-package for the docs commit). Reviewer opus aebad2997e0ea8d50 on review-89d716438..0aac20527.diff → final-review.md. On findings: ONE fix dispatch (fresh opus implementer, isolation worktree off HEAD) + ONE scoped re-review; then docs pass (spec §7.4 splatScale sentence), ledger archive, /feature-done.

Final review (opus, 217k tokens): mergeable after ONE fix round — 0 Critical, 3 Important (I1 ppy sign untestable on this flight; I2 probe ships deg-0 so the SH1 buffer/two-binding bind group never ran; I3 parity test can't tell view from proj), 10 Minor (fix-now: M1 M4 M5 M6 + T9b T11a; parked with reasons: M2 M3 M7–M10), all cross-file contracts + secrets sweep verified clean; docs-pass list of 8 in final-review.md. Ruling on I1: option (b) cited authority, bounded search; if not found, landmine comment + ledger residual, no overlay — cost if wrong: blurred obliques from the bake, which T14's visual check would show. Fix dispatched: fresh implementer a16208c722ffe4d0a (opus, isolation worktree, branch final-fix off 0aac20527); must re-run the GPU probe. Then ONE scoped re-review (sonnet) → docs pass → archive.

Task 8: fetch DONE 12:42→13:05 UTC, 306/306 OK (24 min, ~4.6 s/frame). bake FAILED at 13:05: brush-cli panicked in `Splats::from_raw` via `splat_init::to_init_splats` ("Tensor::require_grad requires autodiff") 1 s in and exited 0; bakeSplats' missing-PLY guard (T7 fix I1) caught it. COLMAP staging verified sane (306 cameras/images, 201,997 points3D, JPEG 1920×1310 matches cameras.txt; this flight's proj:shape is [14016, 20544]). Root cause: `cargo install --git` without `--locked` resolved burn to main 4a0d726 while brush's Cargo.lock pins 05ac3f79. Fix in progress: background task bob8bqhwc = `cargo install --locked --git … --rev 063945e7 brush-cli` (log scratchpad/brush-reinstall.log). On exit 0: re-run `brush-cli <colmapDir> --sh-degree 1 --total-train-iters 20 …` smoke, then `npm run bake-splats` (fetch is complete, no need to re-fetch) in background with bake-splats.log. Delete `<colmapDir>/probe.ply` if the smoke wrote one. Landmine for the README/memory: install brush with `--locked`.

Final fix round: implementer a16208c722ffe4d0a commit e7e82f09a → 4f24561a4 (after controller commit a79e74659 `--locked` hint); typecheck + tests/tools 328 files/1577 tests green; GPU probe PASS with the SH1 asset (6/6, metal-3, zero errors, own ephemeral port). I1 RESOLVED by authority: SDFI dokumentation.md defines ppo with origin lower-left, y up → H/2 − ppo_y/spacing is correct; ppy residual STRUCK. Re-review (sonnet, a5a983087588fa6bd) dispatched on review-0aac20527..4f24561a4.diff → final-rereview.md. Concern carried to docs pass: photoPoseFromStacItem.ts:44-45 still cites pers:rotation_matrix (docs-pass item 4).
Task 8: brush reinstalled `--locked` (task bob8bqhwc exit 0, ~9 min); 20-iter smoke exported 18.6 MB probe.ply (deleted). REAL BAKE RESTARTED 13:12 UTC as background task bl7b0ek60 (scratchpad/bake-only.sh → bake-splats.log; markers "bake2" in bake-chain.log).

Docs pass: items 1 (spec §7.4 splatScale → Σ2D·s²), 3 (skraafoto README harvest row), 4 (photoPoseFromStacItem rotation-matrix citation) done in 678fe3342 (pushed); 5 + 6 were in the fix round (M5, T9b); 2 (README bake numbers) + 8 (ledger residual strike + T8 numbers) wait for T8; 7 no change.

Final re-review (sonnet) clean: I1–I3, M1, M5, M6, T9b, T11a addressed; M4 test header over comment budget (5/8) → trimmed by controller (commit after 678fe3342, pushed). FINAL REVIEW CLOSED. Branch is code-complete; remaining = T8 bake result + README numbers + operator visual check + ledger archive + /feature-done.

## RESUME MAP (updated 2026-09-10 12:45 UTC)
State: execution branch HEAD 678fe3342 (pushed, tree clean). Docs pass done except README bake numbers. IN FLIGHT: (1) T8 real bake task bl7b0ek60 (started 13:12 UTC; brush 30000 iters; check `pgrep -fl brush-cli` + scratchpad/bake-splats.log before ever re-running); (2) nothing else — final review CLOSED, docs pass done. Remaining (final-review.md "Docs pass" list, incl. spec §7.4) → after T8: README numbers + visual check → archive ledger → /feature-done. ALL 12 code tasks complete + review-clean (T1–T7, T9–T13). IN FLIGHT: T8 bake — background shell task b1r1xoco2 (scratchpad/bake-chain.sh → bake-chain.log step markers, fetch-skraafoto.log, bake-splats.log). If the session restarts: check `pgrep -fl 'brush-cli|fetchSkraafoto|bakeSplats'` and the logs before re-running; both scripts resume (fetch skips existing json+jpg pairs; bake deletes a stale final.ply). On bake exit 0: ledger splatCount/shDegree/wall time, verify public/data/geo3d/groups/soendermarken/assets/splats/splats.bin + manifest.json + scenes.json, then T14. T14 README part delegated to sonnet in parallel (see Task 14 entries).
Then T7 (opus, worktree task-7; needs T2 T4 T5 T6 — all landed once T5 is): brief 7; context: readGaussianPly returns unrounded floats (pass straight to packSplats); provenance {source:'nationalGeodataApi', sourceVintage: first item's datetime date, pipeline[…]}; CctRunner(pipeline, lines); fetchSkraafoto's downsampledSize() for scale; brush-cli at ~/.cargo/bin.
Then T8 AUTO-START (user ruling) the moment T7's review is clean: in THIS worktree, background `npm run fetch-skraafoto` (306 frames, token in keychain skymap-dataforsyningen-apikey) then `npm run bake-splats` (hours; brush-cli 30000 iters). Record splatCount/shDegree/wall time in ledger. Then T13-style probe still green; T14 = operator visual check on :5600 (dev server already running in this worktree, `npm run scene-workbench`, log scratchpad/scene-workbench-dev.log) + README update (tools/scene-workbench/README.md: fetch-skraafoto → bake-splats chain, two credentials, Display panel section) — delegate README to sonnet.
Then: final whole-branch review (opus, review-package <plan> 89d716438 HEAD, point at all "minor (deferred)" + Ruling lines above) → ONE fix dispatch + ONE scoped re-review → docs pass (amend spec §7.4 splatScale sentence to "scales the covariance (s²), the canonical 3DGS scaling modifier") → archive this ledger to docs/superpowers/plans/completed/2026-09-10-scene-workbench-2-splats.ledger.md BEFORE deleting the workspace → /feature-done → mark PR #676 ready (user merges).
Status board artifact: https://claude.ai/code/artifact/86210d40-3b05-4ad9-8fe6-44a7764977ba — source scratchpad/splats-board.html (session-local; if lost, rebuild from this ledger). Republish on state changes.

- 13:36Z PREVIEW: Brush 5000-iter checkpoint (exports every 5000) packed via scratchpad/packPreview.mts → public/…/splats/splats.bin 183 MB, 4,580,122 splats, SH1, manifest label "(PREVIEW iter 5000)". Final bake (~14:50Z ETA, ~16 min/5k iters) overwrites both. Bake pid 2532 alive.

- 23:10Z BAKE STOPPED BY USER ("took too long"): brush ran 13:12Z→~16:34Z (last 5000-step export final.ply 16:34Z, iteration count unknown, ≥5000 <30000). Other session (f6fbac4b) landed 619e73e8e shader hardening + f835035a2 floor prune/--reuse-ply (pushed, UNREVIEWED) and repacked 17:20Z → 5,998,053 splats/240 MB (min-floor prune, ineffective: DHM blunders to −413 m). Uncommitted in tree: lidarFloorZM 0.1% quantile + test (other agent). HOLD: controller waits for other agent to finish; then review both commits + quantile as one package, repack, README numbers (must say run was stopped early + wall time), archive, /feature-done.

- 23:15Z TAKEOVER (user ruling "finish the other agent work"): committed quantile floor a027e0bcc; repack --reuse-ply 20 s → pruned 1,773,702 below −22.1 m of 7,771,755 → 5,998,053 splats (identical to 17:20Z file, so that repack already used the quantile); README numbers 9299c1975; pushed. Review of 6f60d0f26..9299c1975 dispatched (opus) → report post-review-hardening.md. Then: fix round if needed → user visual check (:5600, zoom in on the drifted target; also known: orbit target has no ground pivot, backlog candidate) → archive ledger → /feature-done → PR ready.

- 23:25Z USER REPORT: CreateBindGroup failed, 167,945,484 B > default maxStorageBufferBindingSize 128 MiB. Fix: Viewport.tsx initGpu requiredLimits maxStorageBufferBindingSize MAX_SAFE_INTEGER (mcpm precedent, initGpu clamps) → commit pushed. Reviewer will see it in a scoped follow-up.

- 23:45Z REVIEW post-review-hardening.md: FIX REQUIRED, I1 (reuse test order-dependent) I2 (repack stamps installed brush version; needs brush on PATH) I3 (radius cull: unattested, deletes 1 m-σ ground splats within 3.6 m, splatScale slider changes which splats exist) + M1-M11. Fix round dispatched (opus, isolation worktree fix-hardening) for I1 I2 M1-M8 M10 M11 with rulings (I2: carry prior manifest stamp, probe lazily). I3+M9 = USER RULING PENDING; controller recommends DELETE the radius cull (net deletion incl. viewportW + writer line + parity assertion), keep tangent clamp. Then scoped re-review (sonnet) incl. 723b485e4.

- 23:55Z FIX ROUND landed f55a960fd (cherry-pick of 27315caff; I1 I2 M1 M2 M3 M4 M7 M8 M11 applied; M5 M6 M10 skipped w/ reasons in post-review-hardening-fix-report.md), gates green, pushed; agent worktree removed. Scoped re-review (sonnet) dispatched over 9299c1975..f55a960fd → post-review-hardening-rereview.md. I3/M9 STILL AWAITING USER RULING (delete recommended).

- 00:05Z USER RULING: try removing radius cull, user will report regression. Removed MAX_RADIUS_SCREENS + cull branch only (viewportW stays: the tangent clamp uses it) → commit pushed. I3/M9 CLOSED pending user visual.

- 00:15Z USER VISUAL: works without radius cull (I3 resolved by deletion, attested). Remaining: re-review result → archive ledger → /feature-done → PR ready. Visual check of the splat layer itself (T14) partially attested: user has been driving the :5600 view with splats + LiDAR, zoom to ground OK.

- 00:20Z RE-REVIEW post-review-hardening-rereview.md: all 9 resolved, skips sound, 723b485e4 OK; N1 test header 12 lines → trimmed by controller (commit above, pushed); N2 informational (unreachable JSON.parse path) — no change. HARDENING REVIEW CLOSED. Next: archive ledger → /feature-done → PR ready.
