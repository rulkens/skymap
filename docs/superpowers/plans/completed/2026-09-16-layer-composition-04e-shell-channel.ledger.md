# SDD ledger — plan: docs/superpowers/plans/2026-09-16-layer-composition-04e-shell-channel.md

Spec: docs/superpowers/specs/2026-09-09-layer-composition-design.md §4.2/§4.5/§4.6/§9(d) D6+D13/§15 — PR-D second half (04d/04e split, user ruling 2026-09-15).
Worktree: .claude/worktrees/layer-composition-04e · branch worktree-layer-composition-04e · base ce1675fba · draft PR #731.
Protocol: lean (sdd-execution.md). User rulings at plan start (2026-09-16): 3 serial dispatches in this worktree; **Sonnet** implementers; one final whole-branch review; **perf run SKIPPED** (recipe only — no renderer/pass change; both reconciles are key compares); **galaxiesOnly reference engine DEFERRED to §10(e)** (36 non-galaxy passes still core → a galaxies-only composition is identical to APP_COMPOSITION today).

Plan commit: df1860583.

## Dispatches

- D1 (tasks 1–2, the two new facts): sonnet · df1860583→7400c3c42 (1ac3a22e4 T1, 7400c3c42 T2) · DONE · gate re-run by controller: tsc:fast clean, 1258 files / 8497 tests green · deviations: (1) **`GalaxyCatalogRuntime` gained `publish: (patch: Partial<GalaxyCatalogFacts>) => void`, captured from `deps.publish` in `create.ts`** — the plan's Task 1 contract block missed it: `Layer.frame(runtime)` hands `frame` only the runtime, so a per-frame reconcile has no other channel to the facts bus (same construction-time-closure idiom as `famousMeta`); (2) type ripple outside Task 1's file list, forced by the per-commit typecheck gate — `useAliasIndex.ts` patched to build a `catalogs` map from `handle.sources.getCloud` (dies wholesale in T3), `focusIdForRow.ts` gained `BigInt(row.entry.pgc)` for `encodeGalaxyId`'s bigint param, three test fixtures switched from `42038n` to plain numbers, and `frame.biasReconcile.test.ts` + `wireGalaxyCatalogSourceSlot.test.ts` mocks gained the new runtime fields (both reconciles run unconditionally each tick) plus one new assertion that the commit bumps `catalogsVersion`. Plan otherwise matched the tree exactly (SelectionRow/PassState/AssetSlot shapes, `LayerCoreDeps<Facts>.publish`).
- D2 (tasks 3–4, the deletions — ONE commit, plan Ruling 4): sonnet · 7400c3c42→c6753dac9 · DONE · gate re-run by controller: 1258 files / 8498 tests green, `EngineHandle` = `{ debug, destroy }`, no `RequestKey`/`galaxyBridge`/`useAliasIndex`/`awaitSlotReady` left in `src` or `tests` · `EngineState.requests` → `EngineState.ui` (store getter, mirrors `selection`); `DemandCtx.request` → `DemandCtx.ui: Readonly<UiState>`; `watchPaletteWakeSaga` forked in `rootSaga.ts` beside `watchSelectionWakeSaga`, wakes only on `setPaletteOpen(true)` · deviation: four existing test files build `DemandCtx`/`EngineState` literals by hand (`assetWiring.test.ts`, `demandTable.test.ts`, `buildDemandCtx.test.ts`, `galaxyCatalogAssetRows.test.ts`) and were updated `requests: Set` → `ui: { paletteOpen }` — mechanical fallout of the type change, not named in the plan's file list · `awaitSlotReady`'s two predicted readers (Finding 3) were its only two · the four `04e deletes this` markers are already gone (all sat in files this dispatch touched), so Task 6's marker hunt is a verify-only step.
- D3 (tasks 5–6, ui section + docs + audits): sonnet · c6753dac9→33d864d41 (90d73386b T5, 9eed342c2 T6a audits, 33d864d41 T6b docs) · DONE · gate: 1258 files / 8505 tests, build green · **Task 5 landed the spec shape — `layer.ts` declares `ui: GalaxiesSectionContainer` directly, no `appUi.ts` fallback; plan Ruling 5's cycle fear was stale** · both section files moved into `src/layers/galaxyCatalog/ui/` (move-tool misses fixed: a stale CSS specifier + the SettingsPanel test's mock path) · audit applied: `galaxyCatalogConstants` module (dead), `countOf` (dead), `GalaxyCatalogRuntime.provenanceCounts` (no reader), one non-failing test case, six inline types RELOCATED to their own files after the controller's mid-flight correction (`CatalogDrawEntry`/`CatalogStore` → Layer `types/`, four `*Deps` → `@types/engine/subsystems/`) · comment audit: `diskRadiusRing` 68→35, `EngineHandle`, `createLayers`, `watchPaletteWakeSaga`, `CommandPaletteContainer` 25→5 · **flagged: `layerImportBoundary` ratchet needed a carve-out for the Layer's `ui/` container** (settings containers dispatch by design) · plan's `built: 'external'` citation was wrong (lives in `assetWiring`/`installSlots`/`wireSlots`, none touched here).

- D4 (fix round on the audit rulings): sonnet · 33d864d41→ee13aa461 · DONE · gate re-run by controller: full `tsc` clean, 1260 files / 8505 tests · (1) `Layer.targets` RESTORED (user ruling — not dead, it is a Layer's own `RenderTargetSpec` rows; `zoa`/`mw-aggregate`/`star-aggregates`/`volume` move onto their Layers in (e)); (2) **the Layer-saga apparatus now exists** (user ruling): `createAppStore` returns a `runSaga` sibling, reaching the engine through a new `RunSagaProvider` context (mirrors `SagaContextProvider`) and `EngineCallbacks.runSaga`; `createLayers` forks `layer.sagas` in tuple order; `watchPaletteWakeSaga` moved into `src/layers/galaxyCatalog/sagas/` and declared on the Layer; proof test `createLayers.sagas.test.ts` composes a stub Layer over a real store and asserts its saga's effect fires; (3) `ignoredPaths` size exemption for `aliasIndex` on both `serializableCheck` and `immutableCheck`; (4) `CatalogStore.unload` + the `GalaxyPointRenderer` passthrough + 3 tests deleted; (5) ratchet carve-out unified as `LAYER_STORE_REACH_DIRS = ['/ui/', '/sagas/']` across both sweeps — `/sagas/` was a necessary consequence of the sanctioned saga move.

- D5 (delete the disk-radius-ring overlay, user ruling): sonnet · `ee13aa461`→`235d4ab49` · DONE · 24 files, +18/−515 · gate re-run by controller: full `tsc` clean, 1259 files / 8495 tests, build green · the `@types/rendering/DiskRadiusRing.d.ts` + `shaders/diskRadiusRing/` folder + `FRAME_ORDER`/`debugOverlayRows` rows all gone; `effectiveTilt`/`paddedRadiusMpc` KEPT (shared with `texturedDiskSubsystem`/`resolveDiskPlacement`/`buildPointInterleavedBuffer`) · also deleted the now-dead key-collision "trap" comments in `DebugOverlayRow.d.ts` + `DebugSettings.d.ts` (verified no other pass name collides with an overlay key) · surprise: `buildSwapRenderers.test.ts` + `initGpu.hdrCapabilityWiring.test.ts` stubbed a `state.gpu.diskRadiusRing` field that has not existed since 04d's Layer move — stale inert fixtures (`as unknown as EngineState` hid it from tsc), removed as dead scaffolding · **LANDMINE it hit: `npm run format` reformats ~626 unrelated files repo-wide right now (prettier version drift vs. what's checked in) — scope prettier to your own files, never run it repo-wide in this worktree.**

- MERGE of `origin/main` (3 commits: #732 Adreno patch-angle fix, #733 perf hook, #734 terrain bisect toggles) → `43f90d371` · ONE conflict, in `src/@types/data/debug/DebugOverlayRow.d.ts`: main added a `section?: 'earth-tiles'` field, we deleted the disk-radius-ring trap comment — resolved by keeping BOTH (main's field, our shorter comment) · `debugOverlayRows.ts` auto-merged correctly (ring row gone, main's two terrain rows in) · gate re-run after the merge: full `tsc` clean, 1259 files / 8495 tests, build green.

## Landing sequence (queued, in order)

1. ~~fetch + merge `origin/main`~~ DONE (`43f90d371`, see above).
2. Whole-branch review DISPATCHED (fable, diff `origin/main...HEAD`, report → scratchpad `04e-review.md`, verdict LAND / FIX-THEN-LAND / STOP). Brief carries the three drift rules (types never in implementation files; comment budget; pass/saga-file purity) plus reconcile-key correctness, saga lifecycle/leak, the targeted `ignoredPaths`, the ratchet carve-out, and leanness-of-added-surface.
3. Fix round: review came back **FIX-THEN-LAND** (report: scratchpad `04e-review.md`). D6 IN FLIGHT (sonnet, off `43f90d371`, ONE commit, no push). Its nine adjudicated fixes: (1) Layer sagas leak — `RunSaga` returns `void`, `createLayers.ts:100-102` drops the `Task`, `destroy()` never cancels → return + keep + cancel BEFORE `instance.destroy()`, proof test extended; (2) `BuildRunner`/`LoadedSource` still inline in `catalogStore.ts` → relocate to Layer `types/`; (3–4) stale citations of deleted things (`BACKLOG.md:49` cites `RequestKey`, `useEngine.ts:12` cites `useAliasIndex`, the wesl-constants backlog cites the deleted ring shader, RENDERER.md + CLAUDE.md Layer folder lists omit `sagas/`); (5) the **premise correction** below, in four places; (6) two missing tests (second `catalogsVersion` bump with the sidecar ALREADY committed — the headline path — and the memberCount version arm, held at 0 throughout today); (7) `ignoredActionPaths` for `factsReported`'s payload (the state walk is exempt, the ACTION walk still deep-walks 48k rows per publish in dev); (8) comment trims — the "mainSaga can't import the composition" fact is in SIX files and the pgcAlias wake rationale in FOUR (one home each), plus `watchPaletteWakeSaga` 7/10 and the two moved `ui/` headers at 21 and 25 lines; (9) `HiResFamousDeps.now?` unread + self-admittedly speculative → delete.
   **HANDLING when it reports:** verify the commit + clean tree → re-run the gate myself (full `tsc`, `npm test`, `npm run build`) → push → step 4.
   **PREMISE CORRECTION (reviewer, verified by controller):** an alias palette row resolves via `encodeGalaxyId({ objId: BigInt(entry.pgc) })` (`focusIdForRow.ts:41-50`) and is resolved by objID; `AliasIndexEntry.localIdx` is read ONLY as a React key/testid (`paletteRows.tsx:85-86`). So a stale alias index after a tier swap yields a stale ROW SET (aliases for galaxies the new tier dropped, none for the ones it added) — **not** "selects the wrong galaxy", as the plan's DoD and three comments claimed. The `catalogsVersion` key is still the right fix; only the wording was wrong.
   **D6 DONE** · `43f90d371`→`0e2ca78a1` · all nine fixes applied, none skipped · gate re-run by controller: full `tsc` clean, 1259 files / **8506** tests, build green · pushed · verified by controller: `state.layerSagaTasks` cancelled at `engine.ts:480`, i.e. BEFORE the Layer-destroy loop at :482 · `ignoredActionPaths` derives from the same `ALIAS_INDEX_FACT_KEY` as the state path · landmine the agent hit: **this worktree's `node_modules` is EMPTY** and `npm install` is denied by the permission classifier — every tool resolves up to the main checkout's `node_modules` via Node's directory walk, so the gates run fine; don't "fix" it.
4. CI on PR #731 at `0e2ca78a1`: **GREEN** — `Workers Builds` pass, `typecheck · test · format` pass (4m25s). Note the `format` job passed, so CI's prettier agrees with the checked-in tree; the drift is in the LOCAL `npm run format` only. PR is still a DRAFT — mark ready at the user's go, not before.
   Branch diff: 112 files, +1653/−1859 (src +503/−1453 · tests +588/−367 · docs +562/−39) — net −206 lines with the ring deletion and the scaffolding removal.
5. Dev server for the visual pass: **http://localhost:5180** (`npm run dev -- --port 5180 --strictPort`, background shell in this session; `public/data` is symlinked). Leave it running.
5. Start the dev server here (`/dev`) and hand the USER the plan's DoD "Named observable behaviours" list for the visual pass (f.lux off). Note for that hand-off: the palette alias checks are the load-bearing ones (open the palette with the scene AT REST — that is what proves the wake saga), and the tier-swap alias check is a bug FIX vs `main`, not a parity check.
6. Perf: SKIPPED by user ruling; recipe only.
7. On the user's go: squash-merge #731 → move the plan + copy this ledger into `docs/superpowers/plans/completed/` (the SPEC STAYS in `specs/` — (e)–(g) still need it) → `/wt-close` → memory update.

## Rulings made this run (all user's unless marked controller)

- **Reference engine `galaxiesOnly` DEFERRED to §10(e)** — 36 non-galaxy passes are still core, so a galaxies-only composition is behaviourally identical to `APP_COMPOSITION` today.
- **Delete the disk-radius-ring overlay** in this PR (D5).
- **`aliasIndex` gets a targeted `ignoredPaths`** on both store checks rather than a measurement round or nothing.
- **Keep BOTH `Layer.targets` and `Layer.sagas`, and build the apparatus that actually forks Layer sagas** — landed in D4.
- **Types never live in implementation files; relocate, never un-export** (stated while watching D3; also written to the user's conventions memory).
- Controller: `CatalogStore.unload` deleted with its three tests; `LayerUiSection.d.ts` stays its own file; the `frame.memberCountReconcile` "not a structure" case stays (a stale count surviving a selection change is a real bug); the ratchet carve-out is `/ui/`-and-`/sagas/`-shaped in both sweeps, not a per-file allow-list.

## Carried from 04d (this PR owes them)

- Deletion audit + comment audit over the whole galaxy Layer surface (04d deferred both here) — Task 6.
- `diskRadiusRing.ts` comment budget 68/106; `createLayers.ts` `built: 'external'` marker; `diskWalkHarness.ts` co-location — Task 6.
- Four `04e deletes this` markers on temp scaffolding must all be gone (`rg -n "04e" src`).

## Open

- Plan Ruling 5 is empirical: Task 5 declares `ui` on `layer.ts` and falls back to `compositions/appUi.ts` only if the suite or the build fails. The report must say which landed.
