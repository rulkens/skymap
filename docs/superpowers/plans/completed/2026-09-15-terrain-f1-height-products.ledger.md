# SDD ledger — plan: docs/superpowers/plans/2026-09-15-terrain-f1-height-products.md

Branch `terrain-f1-height-products`, worktree `.claude/worktrees/terrain-f1-height-products`, draft PR #713. Lean protocol (sdd-execution.md). Controller in the worktree; implementers are plain agents in the same tree, serial.

## Two questions (defaults applied 2026-09-15, user AFK-run; override any time)

- Parallelism: **single lane, serial dispatches** (one PR, one worktree).
- Perf gate: **none for F1** — no draw, record or shader change; perf measured at F2.

## Dispatches

| D   | tasks | model             | notes                                           |
| --- | ----- | ----------------- | ----------------------------------------------- |
| D1  | 1–3   | sonnet            | rename stack, registry, product axis            |
| D2  | 4–5   | sonnet            | generic stream/atlas, idempotent bake           |
| D3  | 6–9   | opus              | format + bake — review: yes                     |
| D4  | 10–12 | opus              | height stream, gated walk, balance — review: yes |
| T13 | —     | controller + user | acquisition, bake, eye-check                    |

## Data acquisition (main checkout, started 2026-09-15 01:52)

- ETOPO 2022 30s surface GeoTIFF → data/raw/etopo/ (1,585,813,987 B), curl -C - resumable, log download.log
- skadi 47 cells → data/raw/skadi/<N55>/<N55E012>.hgt via fetch.sh (cells.txt), log download.log
- Mars (F4, pulled early). USER RULINGS 2026-09-15, latest wins: (1) global = z7 only, MOLA 463 m DEM (`https://planetarymaps.usgs.gov/mosaic/Mars_MGS_MOLA_DEM_mosaic_global_463m.tif`, 2.1 GB, data/raw/mola/) REPLACES the spec's 200 m HRSC blend — §4.2 line to be amended at /feature-done; (2) REVISED 02:25: **rover-site bands ARE wanted, only at the sites ("a nice 3d map + textures for the rover surroundings")** → spec §9/§10 site rows STAND (HiRISE ortho over HiRISE DTM, z17). Viking MDIM21 232 m colour → data/raw/viking/ (12.7 GB). Site pulls restarted (scratchpad `fetchRoverSites.sh`, log `fetchRoverSites.log`, resumable): Gale `data/raw/hirise/gale/` MSL_Gale_DEM_Mosaic_1m_v3.tif 3.87 GB + MSL_Gale_HiRISE-LRGB_78quads_sharp_cog.tif 1.03 GB (+ .txt); Jezero `data/raw/hirise/jezero/` MSR_TRN_HiRISE_soc_003_USGS_release_aug2024_mosaics.zip 5.47 GB (+ README.md). Gusev/Meridiani have NO USGS mosaic; using controlled HiRISE stereo-pair COGs from the public bucket `https://astrogeo-ard.s3.us-west-2.amazonaws.com/mars/mro/hirise/controlled/dtm/<pair>/` (scratchpad `fetchMerSites.sh <site> <pair> <files…>`, logs `fetchGusev.log`, `fetchMeridianiLanding.log`, `fetchMeridianiEndeavour.log`): `data/raw/hirise/gusev/` pair PSP_001513_1655_PSP_001777_1650 (DTEEC 136 MB + two RED_A 25 cm orthos 696+777 MB; covers Spirit site −14.5684, 175.4726); `data/raw/hirise/meridiani-landing/` pair PSP_001414_1780_PSP_001612_1780 (~50 MB total; Opportunity site −1.9462, 354.4734); `data/raw/hirise/meridiani-endeavour/` pair ESP_018701_1775_ESP_018846_1775 (DTEEC 248 MB + orthos 1.59+1.15 GB; where the rover ended, ~45 km SE). MER orthos are RED single-band (greyscale) — colour for those two sites must come from tinting with the Viking/HRSC base; note for F4. Baked Mars z7 ≈ 360 MB albedo + 727 MB height, + ~250 MB albedo per spec §10 for sites.
- DHM/Terræn: 24/24 tiles DONE in data/raw/dhmterraen/ (scratchpad fetchDhmTerraen.sh). ETOPO connection reset at 1.04 GB, resumed with --retry-all-errors.
- DHM/Terræn: UNBLOCKED — Datafordeler `GetRasterFile?FileName=DTM_1km_<N>_<E>.tif` works with the existing keychain key (probed 2026-09-15); fetch runs in Task 8/13

## Dispatch log

- 2026-09-15 D1 (T1–T3) dispatched, sonnet, plain agent in this worktree, BASE = plan commit (HEAD of `terrain-f1-height-products` at dispatch). Handling on completion: read its ≤8-line reply, `git log --oneline main..` to confirm 3 commits + push, spot-check `git grep -n "EarthTileKind\|bodyId === 'earth'" src tools` is empty, then log BASE→HEAD + turns here and dispatch D2.

## Queued sequence (verbatim, after D1 lands)

1. Commit the uncommitted plan amendment (Task 8 DHM/Terræn = Datafordeler `GetRasterFile`, `lonLatToUtm32` util; Task 13 DHM bullet) — `git add docs/superpowers/plans/2026-09-15-terrain-f1-height-products.md` only; held back because the index is shared with the running agent.
2. Dispatch D2 (T4–T5, sonnet) → D3 (T6–T9, opus, mid-branch review of T6 + T9 against spec §5.3/§5.4 and rulings R1–R3) → D4 (T10–T12, opus, mid-branch review of T11–T12 against spec §5.2/§6).
3. One whole-branch final review (opus): spec fidelity + slop; one fix round by a Sonnet implementer; no re-review.
4. T13: verify downloads (ETOPO 1,585,813,987 B; 47 skadi; 24 DHM tiles `data/raw/dhmterraen/DTM_1km_{6173..6176}_{718..723}.tif`), write `.sha256` sidecars, relayout `cp -R public/data/images/earth-tiles/v7/surface …/v8/albedo` in MAIN, run `npm run build-surface-tiles` from main (or symlink `data/raw` here), user eye-check at four poses, R3 water diagnostics → user ruling.
5. `/feature-done` (fix spec §3.4c wording per R1, §3.6 stale conflict paragraph, §4.2 ETOPO URL + DHM source), archive this ledger to `plans/completed/`, `gh pr checks`, then squash-merge ONLY on the user's word, then wt-close.

User directives this session: "do the cleanup, then start the work" · "start downloading height data so we have something to work with" (done, main `data/raw/`) · asked whether Mars is in scope → answered: code is body-generic, Earth-only bake in F1, Mars = F4, MOLA pulled early · no answer yet on parallelism/perf-gate → defaults above stand.
- 2026-09-15 D1 LANDED: 3738473a0 → 8de3d9eac (a498219f6 P2a, 87e2cc90f P2b, 8de3d9eac P3), 404 tool turns, ~517k tokens. typecheck:fast clean. Deviations accepted: EARTH_EQUATORIAL_CIRCUMFERENCE_M left (no prod reader); P2a straggler doc-labels bundled into P3; a few tests/ describe() names still say earthTile* — sweep at final review. Plan amendment committed 7a08e2a4e.
- 2026-09-15 D2 (T4–T5) dispatched, sonnet, plain agent in this worktree, BASE = 7a08e2a4e. Handling: read its ≤8-line reply, `git log --oneline 7a08e2a4e..` = 2 commits + pushed, `git grep -n "BitmapStream\|ImageBitmap" src/services/engine/subsystems` empty, `buildEarthTiles` gone from tools/ + package.json, typecheck:fast clean; log here, dispatch D3 (T6–T9, opus).
- 2026-09-15 USER (going to sleep): run F1 to completion unattended — D2→D3→D4→final review→fix round→T13 bake (symlink data/raw → main's, relayout v7→v8/albedo, build-surface-tiles from this worktree, sha256 sidecars, save R3 water diagnostics to the ledger)→spec amendments staged. STOP before: eye-check, R3 ruling, squash-merge. Then DRAFT the F2 plan (writing-plans), do not execute it.
- 2026-09-15 USER: overnight queue APPROVED, extended: after F1 is PR-ready → write the F2 plan (writing-plans, spec F2 row: renderer + shader displacement only, walk already reworked in F1 per R4) → execute F2 under lean SDD in a NEW worktree branched off terrain-f1-height-products (stacked draft PR, --base terrain-f1-height-products) → stop at F2's visual gate. NO perf runs anywhere (user: "dont run the perf test, thats for sure"). Morning gates for the user: F1 eye-check + R3 water ruling + merge #713; F2 eye-check.
- 2026-09-15 D2 LANDED: 7a08e2a4e → 7d29983d2 (f979ce360 P4, 7d29983d2 P5), 221 turns, ~382k tokens. typecheck:fast + eslint clean. Deviations accepted, flag to final review: (1) bitmap close folded into uploadBitmapToAtlas, deps.release only on recycled/post-destroy; (2) --product/--body CLI flags validated-but-inert until T9 wires dispatch. Straggler: unused writeFileSync import in tests/tools/textures/buildSurfaceTiles.test.ts (tests not linted) → D3 sweeps. T13 groundwork DONE: v7/surface → v8/albedo relayout (423 MB) in main's public/data; 72 height source files (etopo 1, skadi 47, dhmterraen 24) symlinked per-file into this worktree's data/raw via scratchpad linkRaw.sh (real dirs, so .sha256 sidecars commit here).
- 2026-09-15 D3 (T6–T9) dispatched, opus, plain agent in this worktree, BASE = 7d29983d2. Handling: read reply, `git log --oneline 7d29983d2..` = 4 commits + pushed, typecheck:fast, then dispatch the mid-branch REVIEW (opus, read-only) of T6 + T9 against spec §5.3/§5.4 + R1–R3 + the 6 bake rules; fix round by sonnet if findings; then D4.
- 2026-09-15 D3 LANDED: 7d29983d2 → e4331175e (fd6d57e6e T6, c530c0a9f T7, 005f7951e T8, e4331175e T9), 119 turns, ~323k tokens. tsc+tsgo+eslint+prettier clean per agent; typecheck:fast re-verified. Deviations (flag to reviews): (1) lattice lat step = 360/(2^z·128) on both axes, one place tools/utils/textures/heightLatticeStepDeg.ts (plan's Task 8 comment was wrong); (2) geometricResidualM at coarser levels = upper bound from 4 children, deepest = 0; (3) rules 1+2 one code path (children on disk else resample), forced by R2; (4) sourceBounds=null when flattenWater, water pass BEFORE children laid over grid; (5) band.height optional (--dev), --product now dispatches; (6) extra files heightLatticeStepDeg/heightLatticeBounds/boundsOverlap, band flattenWater flag, docs/DATA.md height paragraph. R3 WATER DIAGNOSTICS (z6 global smoke, real ETOPO, scratch dir, 34 s, 2048 tiles): ocean = 0 on 65.5% posts; Dead Sea −395.7; Superior +35.2; Caspian −130.8 (expected ≈ −28); Black Sea −401.4 (expected 0); global −4189.1..8073.7. Misses = mask/ETOPO disagreement (mask-land post under deep water donates depth as 'lowest shore'); candidate fixes: ignore land-adjacent posts below a threshold, or seed the two basins → USER RULING. T13 facts: skadiCellsForBounds over EOX_REGIONS = 49 cells, 47 on disk, missing N49E002 + N35E140 (~4 MB); Søndermarken z14 needs 20 DHM tiles N6173–6176 × E720–724, on disk E718–723 → four E724 tiles missing, run fetch-height --dhm-terraen. Worktree-local: data/raw/textures/world.watermask.21600x10800.png symlinked to main.
- 2026-09-15 D3 REVIEW dispatched (opus, read-only): T6+T9 vs spec §5.3/§5.4, R1–R3, six bake rules, + the 6 deviations above. Handling: findings → sonnet fix round (one), then D4 (T10–T12, opus). No findings → D4 directly.
- 2026-09-15 MARS DOWNLOADS ALL DONE in main data/raw: viking MDIM21 12,742,411,029 B; mola 463m 2,125,771,142 B; gale DEM 3,868,256,546 + LRGB cog 1,030,625,379; jezero zip 5,468,062,403; gusev DTEEC 136 MB + 2 orthos; meridiani-landing 48 MB; meridiani-endeavour DTEEC 248 MB + 2 orthos. Logs say ok/DONE everywhere. 44 GB free.
- 2026-09-15 T13 sources COMPLETE: fetch-height --skadi --dhm-terraen from this worktree pulled N35E140, N49E002 (49/49 skadi) and DTM_1km_617{3,4,5,6}_724 (20/20 DHM for the Søndermarken z14 rect); moved to main data/raw and re-symlinked (scratchpad moveNewRaw.sh; 78 links, only READMEs real). Key never printed.
- 2026-09-15 D3 REVIEW back: FIX ROUND. Byte layout exact, rules a–f verified in code, deviations 1–6 all OK. SHOULD: (1) bakeHeightLevel regionsOf hardcodes 2^z cols/2^(z-1) rows → derive from surfaceTileColumns; (2) global z3–z6 still resamples ETOPO + decodes mask + labels though every post comes from children → skip when all children exist; (3) dhmTerraenHeightSource TILE_CACHE_LIMIT 4 FIFO re-decodes 25 MB tiffs per lattice row (~33k decodes) + boundsInBox samples 37k/tile → strip-sized cache, bounds from read posts; (4) no test asserts north-row-first in a BAKED tile; (5) module headers 10–26 lines vs ≤5 budget in ~8 files, decimateHeightGrid/heightLatticeStepDeg comment>code. NIT: (6) antimeridian shore gather uses duplicated column, should be nx−2/1; (7) water test lacks lon-wrap case; (8) '~540 MB' note understates z7 peak ≈1.4 GB; (9) printWaterDiagnostics scans all posts (545M iters); (10) global band underfill null → one NODATA aborts bake.
- 2026-09-15 D3 FIX ROUND dispatched (sonnet, findings 1–10). Handling: read reply, `git log --oneline e4331175e..` ≥1 commit + pushed, typecheck:fast, `npx vitest run tests/tools/textures tests/utils/scene` green, then dispatch D4 (T10–T12, opus).
- 2026-09-15 D3 FIX ROUND LANDED: 003ceaef1 (all 10 findings; constantHeightSource(0) added as global underfill since ETOPO maps NODATA→NaN; DHM cache strip-sized, boundsInBox reads native pixels; no dhmTerraen unit test — flag to final review). typecheck:fast + vitest tests/tools/textures tests/utils/scene 235 green.
- 2026-09-15 D4 (T10–T12) dispatched, opus, plain agent in this worktree, BASE = 003ceaef1. Handling: read reply, `git log --oneline 003ceaef1..` = 3 commits + pushed, typecheck:fast, `npx vitest run tests/utils/scene tests/services/engine` green, then dispatch the mid-branch REVIEW (opus, read-only) of T11+T12 vs spec §5.2/§6 + R4/R9; sonnet fix round if findings; then FINAL whole-branch review.
- 2026-09-15 T13 BAKE DONE (from this worktree, ~10 min, log scratchpad/bake.log): 19,701 height tiles, 1.3 GB in main public/data/images/earth-tiles/v8/height; albedo skipped as existing; 59,103 tiles indexed; manifest written (prefix earth-tiles/v8, bands: soendermarken 14–19, eox 8–13, global 3–7). Required extra worktree links: eox + geodanmark per-file (15,298 links, scratchpad linkRaw2.sh — dir symlinks fail the sources' entry.isDirectory()), textures BMNG quadrants. Sidecars written (etopo 1, skadi 49, dhmterraen 28 — 8 extra E718/E719 tiles from the earlier fetch are harmless), UNCOMMITTED until D4 lands (shared index). R3 DIAGNOSTICS at z7 (post-nesting, real): global −4945.0..8748.0; Dead Sea −426.0..831.8 ✓; Caspian −131.4 (expected ≈ −28 ✗); Superior 24.6..650.6; Black Sea −540.7 (expected 0 ✗). PITS: scratchpad deepPits.mjs finds 144 z7 tiles with subtreeMin < −1000 m — open-ocean atolls/Antarctic coast (x117 y44 Tasman −4945; x10 y37 Pacific −4714; x6 y29 −4348; y55–57 Antarctic coast −3858…) = posts the NASA mask calls LAND but ETOPO 30″ puts under deep water; they escape flattening and stay as single-post km-deep needles. Same root cause as the Caspian/Black Sea 'lowest shore' misses. USER RULING NEEDED (R3 amendment candidates): (a) land posts below −X m (e.g. −50) adjacent to water adopt the water component's level; (b) the shore level ignores land posts below that threshold; (c) seed Caspian/Black Sea explicitly. No code change made.
- 2026-09-15 D4 LANDED: 003ceaef1 → dca2ac52f (14d45599d T10, 9f7388b48 T11, dca2ac52f T12), 117 turns, ~306k tokens; tsc+tsgo+eslint+prettier clean, 3171 tests green. Sidecars committed 9793e51ef. D4 deviations: (1) refine gate narrowed to children some band bakes at z+1 (literal 'every visible child' froze Søndermarken at z13: its z14 rect spans ~2 z13 tiles); (2) NEEDS RULING: leaves at levels no band bakes are dropped → hole ring one z7-tile wide around every EOX region / z13 around Søndermarken; (3) blocked nodes request ALL visible bakeable children (request = LRU touch); (4) zWin reports z+1 when blocked (engage gate, else cold-start deadlock); (5) walk restructured with probe() result on the stack; (6) balanceSurfaceCut(cut, tilePx, resolveParent); (7) two residency maps; (8) cut fixtures default height-everywhere; (9) io.wesl:38 still says .resident (F2 fixes), stand-down test uses bodyId 'planet'.
- 2026-09-15 CONTROLLER RULING R11 (user to confirm in the morning): the height pyramid must be sibling-closed so the plan's literal refine rule is satisfiable everywhere — a band's tile set at level L = every tile whose PARENT (L−1) box overlaps the band bounds and min ≤ L ≤ max (so any baked tile's three siblings exist; halo comes from the band's underfill: etopo for EOX, skadi for Søndermarken). Same rule for BOTH products (one shared helper used by bake and walk; geodanmark band gets underfill: eox for its albedo halo). Walk reverts D4 deviation (1) to the literal rule: refine only when every visible child's own height tile is resident, else emit and request all visible children in both products; leaf emit/request gates use the same shared helper. Halo tiles never refine further (their own ceiling is the coarser band). Then delete v8/height and rebake (~10 min) so R2 nesting stays bit-identical.
- 2026-09-15 D4 REVIEW dispatched (opus, read-only): T11+T12 vs spec §5.2/§6 + R4/R9 + D4 deviations 1–9 with R11 as context. Handling: findings + R11 → ONE fix round (opus, bake + walk + tests) → delete public/data/images/earth-tiles/v8/height + rebake (background, log scratchpad/bake.log) → FINAL whole-branch review.
- 2026-09-15 D4 REVIEW back: FIX ROUND. §5.2 holds on every path; T10 correct; balance terminates/wraps/R9-correct. BLOCKER (1): balanceSurfaceCut coarsens the fine side of a BAND-BOUNDARY step (z13 EOX leaves ringed by band-capped z7 leaves → four z8 leaves; Søndermarken 3×1 z14 rect collapses entirely) — R4 assumed steps ≤ 1. SHOULD: (2) requests built from the unbalanced walk pin never-drawn slots; (3) resolveParent can be null (refinement checks children's heights, not the node's own) and the code then still sets edgeCoarser=1 on a ≥2-level edge; (4) balance tests never hit resolveParent→null nor a band-capped neighbour; (5) R11 revert = delete childBakeable/bakeable&&/:310 gate, use the shared sibling-closed helper at :279-286/:298/:326; (6) test :604-646 encodes deviation 1 — rewrite to 'parent does NOT refine'; (7) HEIGHT_ONLY/withHeight fixtures hand height for every tile → hole ring invisible before and after R11 → one fixture whose height residency IS the existence helper. NIT: (8) debug miss/levels albedo-only; (9) three stale .resident sites (SurfaceTileSubsystem.d.ts:53, io.wesl:38,45); (10) 12/14-line module headers; (11) NO_COARSER_EDGES always discarded. Deviations: D1/D2 NOT OK (R11), D3–D7 OK (D6: amend plan contract line with tilePx), D8/D9 NOT OK. Behaviour note for the eye-check: refinement is serialized one level per height round trip (~15 sequential fetches from z4 to sharp) — per §6, user judges.
- 2026-09-15 CONTROLLER RULING R12 (user to confirm): balanceSurfaceCut never coarsens a leaf against a neighbour that cannot refine (no tile exists under the neighbour at neighbour.z+1 per the shared sibling-closed existence helper) — balance acts within a band's reach only; a band-boundary seam keeps its multi-level step with edgeCoarser=0 on that edge and F2 hides it with a skirt (spec §6/F2 row to be amended). edgeCoarser stays 'exactly one level'; when a collapse is refused (resolveParent null) the bit stays 0. R4 stands inside bands.
- 2026-09-15 D4 FIX ROUND dispatched (opus; R11 bake+walk, R12 balance, findings 2–11). Handling: read reply, `git log --oneline 9793e51ef..` ≥1 commit + pushed, typecheck:fast, `npx vitest run tests/utils/scene tests/services/engine tests/tools/textures` green; then rm -rf public/data/images/earth-tiles/v8/height + rebake (background) ; then FINAL whole-branch review (opus).
- 2026-09-15 D4 FIX ROUND LANDED: 3a55f90e6 (R11 bake: surfaceTileInBand(bands,tilePx,z,x,y) single authority; underfillImagerySource now serves the filler alone OUTSIDE primary coverage so halo albedo bakes; Søndermarken underfill: eox), ea80ca6d3 (R11 walk literal rule; R12 band-capped balance; requests capped at balanced leaf +1; surfaceTileBandRequestAllowed deleted, surfaceTileBandRefineAllowed kept for band-gap descent), 3aad16db3 (miss tally both products; resident rename done). Extractions: surfaceTileBandFromBounds, packSurfaceTileKey. typecheck:fast + 2881 tests green. v8/height DELETED; REBAKE running (background, scratchpad/bake.log). Next: FINAL whole-branch review (opus) dispatched in parallel with the bake.
- 2026-09-15 Spec amendments committed 979ee5f9e (status line; §3.4c R1; §3.6 v7/v8; §4.2 ETOPO URL/DHM Datafordeler/MOLA 463 m/MER stereo pairs; §4.4 R3 as implemented + open mask/ETOPO ruling; §5.4.4 R11; §6.2 R12 + drawability wording; §7.3 skirts scoped to band seams; §9 MOLA; §10 halo note).
- 2026-09-15 REBAKE finding (for the post-final-review fix round): albedo halo tiles at coarser levels are NOT baked — coarser albedo levels are '2x2 average of children' only and a halo tile has no children (z14: albedo 3 files vs height 8). Runtime-harmless (404 memoised as failed, albedo climbs) but breaks R11's 'one tile set for both products'. Fix: in the albedo coarser-level path, a sibling-closed tile with no children on disk bakes from the band source/underfill at that level (as the height path does); then re-run the bake (skip-existing → only halo albedo tiles).
- 2026-09-15 REBAKE DONE (R11 pyramid, ~12 min): height 20,684 tiles (19,701 + 983 halo) 1.3 GB; albedo 20,120 (halo only where baked from source at a band's deepest level; coarser-level albedo halos = the gap above, 564 tiles); 60,505 indexed; manifest written. z7 diagnostics unchanged in kind: global −5292.9..8748.0 (146 pit tiles, worst Tasman x117 y44 −5293), Dead Sea −426.0, Caspian −131.4, Superior 24.6, Black Sea −540.7.
- 2026-09-15 DEV SERVER for the eye-check: this worktree, http://localhost:5175/ (background task, log scratchpad/dev.log). Other servers: 5173-ish site-rung, atmosphere-froxel, add-probe-skill — not ours.
- 2026-09-15 FINAL REVIEW back: READY FOR FIX ROUND. SHOULD: (1) DEPLOY.md still v7/fetchEarthTileManifest/levels.surface, no R2 order note; (2) RENDERER.md 3 refs to deleted earthTileSubsystem.ts, no height/atlas/gate/balance bullet; (3) DATA.md:196 claims basin outcome the bake doesn't deliver → qualify pending R3; (4) SurfaceTileSpec.circumferenceM has zero readers → delete; (5) --body value discarded → delete bodyFlag; (6) builtFrom.height written on --product albedo runs → && products.has('height'); (7) derivePlannerParams admits albedo-only bands → skip bands lacking builtFrom.height; (8) bodySurfaceTier.ts 15-line header → trim; (9) no test for dhmTerraen global-pixel→tile-name derivation → 3-post fixture test. NIT: (10) underfillImagerySource re-implements boundsOverlap; (11) surfaceTileBandRefineAllowed has no exercised case → one naming comment; (12) earthTilesAnimating half-rename in runFrame/shouldKeepTicking; (13) P5 completeness gate narrower than the task text → amend plan line. DoD: all DONE except docs PARTIAL and the observable behaviours (user eye-check). Comment budget did not regress; R6/R7/R11/R12 match code.
- 2026-09-15 FINAL FIX ROUND dispatched (sonnet; findings 1–13 + albedo coarser-level halo gap). No re-review. Handling: read reply, `git log --oneline 979ee5f9e..` ≥1 commit + pushed, typecheck:fast, `npx vitest run tests/tools/textures tests/utils/scene tests/services/engine` green; then re-run build-surface-tiles (skip-existing → only the coarser-level albedo halos), verify albedo count == height count (20,684); then /feature-done PREP (PR description, gh pr checks; NO merge, ledger stays until the user's gates); then F2 plan.
- 2026-09-15 F2 PLAN WRITER dispatched (opus, read-only + one new file docs/superpowers/plans/2026-09-15-terrain-f2-displacement.md, UNCOMMITTED in this worktree). Handling: read reply; after the F1 fix round lands → create worktree .claude/worktrees/terrain-f2-displacement on branch terrain-f2-displacement off terrain-f1-height-products HEAD; copy the plan file there; commit; link public/data; draft PR --base terrain-f1-height-products; new ledger .superpowers/sdd/2026-09-15-terrain-f2-displacement/progress.md; execute under lean SDD; STOP at the F2 eye-check. NO perf runs.
- 2026-09-15 F2 PLAN WRITTEN (untracked here): docs/superpowers/plans/2026-09-15-terrain-f2-displacement.md, 365 lines, 9 tasks (D1 T1–2 opus: 3-valued edge code 0/1/2 + PatchInstance 80 B/height atlas binding; D2 T3–6 opus: displacement n=64, edge collapse, skirt ring on band seams, fragment normals; D3 T7–8 sonnet: base globe at innerBound with Earth reliefM=[-430,8849], RENDERER.md; T9 user eye-check + USER-RUN perf). Plan-time rulings flagged for the user: F2-R1 single 2-bit edge code instead of a separate edgeSeam field; F2-R3 skirt depth 0.05×patch N–S extent; F2-R4 literal reliefM. Perf cliff risk noted (33k → ~2.2M tris/frame at n=64) — Task 9 measurement (user) answers it.
- 2026-09-15 FINAL FIX ROUND LANDED: e8ce9c589 (childless halo albedo tiles baked from source; circumferenceM + --body deleted; builtFrom.height gated; derivePlannerParams needs height provenance; dhmTerraen tile-name test; rename sweep; boundsOverlap NOT reused — halo check needs exclusive edges), e3732488e (DEPLOY v8 order, RENDERER bullet, DATA water caveat, plan Task 5 wording). Halo rebake: albedo 20,684 == height 20,684, 61,069 indexed. MERGED origin/main (dc222eaef) → 937182c68: 3 conflicts (earthTiles→surfaceTiles rename vs probeDue + #715's test deletions), resolved keeping main's shape + our name; full suite 8033 green; pushed. PR #713 body rewritten (scratchpad pr713.md). CI pending at push time.
- 2026-09-15 F1 STATE = PR-READY, STOPPED AT USER GATES: (1) eye-check on http://localhost:5175/ (four poses), (2) R3 water ruling (spec §4.4 candidate), (3) confirm R11/R12, (4) squash-merge #713 on your word, then /feature-done proper (archive plan+spec to completed/, this ledger to plans/completed/). Deploy afterwards per DEPLOY.md v8 order.
- 2026-09-15 F2 WORKTREE CREATED: .claude/worktrees/terrain-f2-displacement, branch terrain-f2-displacement off 937182c68; plan file moved there (uncommitted); node_modules + public/data link still needed; controller moves there via EnterWorktree; F2 ledger .superpowers/sdd/2026-09-15-terrain-f2-displacement/progress.md.
- 2026-09-15 DEV SERVER :5175 DIED (background task reaped, exit 137) — USER starts `npm run dev` in this worktree for the eye-check.
- 2026-09-15 EYE-CHECK BUG (user): flicker when deeper levels load. ROOT CAUSE: refine rule required EVERY visible child's height; a culled sibling scrolling into view un-refined the parent, discarding settled subtrees (a base-level parent is never resident → whole root quad to base globe; repro: 1000 km, 0.5° pan, 12 z7 leaves vanish for 3 sequential round trips). RULING R13 (user ok): refine onto READY children only, missing child = hole the base globe fills; parent holds only when no child is ready. Failing tests first (rewrote 'holds the parent…' + new pan-survival test), fix in cutSurfaceTiles.ts, spec §5.4.4/§6.1/Drawability + plan walk rule + RENDERER.md amended. Remaining cosmetic: entering-edge tile still coarsens its z7 neighbours to z6 via 2:1 balance for one round trip; zoom-in cascade still one level per round trip (D4 note). NEXT: push, CI, rebase terrain-f2-displacement onto this head.
- 2026-09-15 R14 RULED (user 'go'): holes = §5.2 drop + faded base globe <150 km; coarser-in-a-zoom-range = 256-slot atlas vs 400–1850-slot working set in tilted views (allocator refuses, never fetched); horizon slivers over-refined by max-extent screenPx. Bake verified clean (20,684/product, paired, sibling-closed, all decode). Plan amendment appended (A1 walk/balance/height fallback, A2 sqrt-area screenPx + 32/row height atlas, A3 docs). NEXT: dispatch A1–A3 (opus, this worktree) → review → fix → rebase F2 → F2 tasks (bilinear stride sampler + heightCells @76 + drop base-globe fade).
- 2026-09-15 R14 LANDED (opus, 835e6838b A1, 4451bad67 A2, 464cb4535 A3; 4214 tests green, build clean). Agent's A1(d): 300 km/60° still 1819 height requests — straddlers skip the frustum cull. Deviations: resolveHeightLattice arg is minLevelDelta (climb floor); balance enumerates pairs from the finer-id side, acts symmetrically.
- 2026-09-15 EYE-CHECK BUG 2 (user): wrong tiles around Søndermarken. NOT the walk (my first guess, wrong — user pushed back). ROOT CAUSE in the bake: eoxTileSource.readBox ignored the box SIZE and always returned the 2×2 z13 EOX block at the box's NW corner; the five GeoDanmark z14 halo tiles (v8-only, filled from EOX) each got a z13-sized region (14/8760/1562 = its whole parent). Every tile shared with v7 is byte-identical (scratchpad diffTiles.mjs); height halos match parents exactly (heightHalo.mjs). FIX: readBox now finds the ladder cell containing the box, composites, extracts the sub-rect and upscales; declines only when nothing under the box exists; eoxTileAt deleted. Test 'serves a box finer than the z13 ladder cell…' (fails on old code). Five tiles deleted + `build-surface-tiles --product albedo` regenerated them (montage m14b.png matches parents). R2: those five must be re-uploaded with the v8 deploy.
- 2026-09-15 STRADDLER CULL: probe now tests a bounding sphere (centre, 1.5× corner chord) against the four frustum side planes (Gribb–Hartmann rows of vp) BEFORE the NDC bbox; that is the only cull a straddler gets. 300 km/60°: 1819 → 39 height requests; 50 m fixture: ~1100 → ~20 leaves (the ~1100 were eye-plane straddlers behind the camera). 50 m test's `>200` floor + knownEdgeAncestors (straddlers 7 km away, NDC ±hundreds) replaced by an on-screen coverage oracle. RENDERER.md + plan A2 paragraph updated. Commits: fix(bake) + feat(terrain), NO Co-Authored-By (memory rule; the four earlier R14 commits carry one — squash-merge drops them).
- NEXT: push, CI on #713 → user re-checks :5176 (Søndermarken tiles, holes, flicker, alloc dropped) → R14 review (opus) → F2 rebase + r14-f2-plan.md → B1–B3.
- 2026-09-15 R14 REVIEW back (opus): correct on planes/sphere/originPosts/balance termination/touch pass/atlas rows/docs. SHOULD: (1) R12 exemption skips the edge-bit pass → one-level band-ceiling step gets edgeCoarser=0; (2) exemption keyed on leaf z not height level → stale-height neighbour treated as permanent ceiling; (3) sphere headroom ≈1.1× half-diagonal — fine at the datum (F1), landmine under F2 displacement → F2 plan Task B4 (scratchpad r14-f2-plan.md, per-tile subtree bounds from the resident ancestor); (4) isotropic screen error is only 2% of the win and softens horizon ground ~3 levels → USER keep/revert ruling at the eye-check (add item: horizon-band sharpness at 60° tilt); (5) stale own-tile comments in ResolvedTileResidency.d.ts + surfaceTileSubsystem.ts. NIT: cells=128>>d hits 0 at d≥8 (REACHABLE: z19 leaf with only z≤11 resident) → cap climb at 7; balance allocates 900 objects/frame; test name pins the sphere number; comment budget 0.495. FIX ROUND dispatched (sonnet: 1,2,5, cap, alloc, test name, comment trim). Handling: read reply, `git log --oneline e2d8a9e61..` ≥1 + pushed, typecheck:fast, vitest scene+engine green, CI on #713; then EnterWorktree F2 → rebase → append r14-f2-plan.md (B1–B4) → dispatch B1–B4 (opus).
- 2026-09-15 FIX ROUND LANDED (sonnet): b7e24c857 (ceiling exemption on height level, edge bits unconditional, no realloc), 9a7f24c06 (climb cap 7 + plan/spec), 12e671560 (stale comments), 1889f6e95 (test pins ceiling <100), 49f5ece08 (comment trim); 3230 tests green, typecheck clean. Merged origin/main (c43f43615, #717 + #721) → bf10e4657, pushed; `npm run build` running. F1 = PR-READY again at bf10e4657. USER GATES: eye-check :5176 (Søndermarken tiles after hard reload, holes nadir+tilt, flicker on pan, alloc dropped = 0, horizon sharpness at 60° tilt → isotropic-error keep/revert), R3 water, merge on your word. Controller moves to the F2 worktree: rebase onto bf10e4657, append r14-f2-plan.md (B1–B4), dispatch.
