# SDD ledger — plan: docs/superpowers/plans/2026-08-17-handle-registry.md

Execution worktree = THIS worktree: /Users/rulkens/Development/js/skymap/.claude/worktrees/land-milky-way-refactor, now checked out on refactor/handle-registry (the satellite handle-registry worktree was removed 2026-08-18 — session + subagent Write/Edit are both isolated to this worktree, so cross-worktree execution was unworkable; T1+2's files were captured, verified, and re-committed here). Branch base: origin/main 05cce3e30. Draft PR #571.
Binding authority: docs/research/engine/decisions.md (#9/#10/#11) + docs/adrs/0011-engine-composition-bundle-rows.md. No separate spec file — rulings resolve against decisions.md.
Deviation note: harness session has no todo-list tool; this ledger IS the task list (addendum Rule 1), mirrored in user-visible narration.

Per-task flow (gate precedes commit, per feedback_bg_subagents_no_npm):
1. Implementer (bg subagent) edits in this worktree via absolute paths, self-checks via `node_modules/.bin/tsc --noEmit -p <wt>/tsconfig.json`, attempts targeted vitest (`node_modules/.bin/vitest run --root <wt> <test>`, reports if denied), writes report file, does NOT commit (briefs say so; a later commit request contradicts them — controller commits instead).
2. Controller runs the gate natively: `npm run typecheck` + targeted `npm test -- <pattern>`.
3. On green, controller commits exactly the task's files (Co-Authored-By trailer), records BASE..HEAD, builds the review package natively (git log --oneline + diff --stat + diff -U10 into one workspace file).
4. Controller dispatches reviewer (+ next implementer when files are disjoint — sdd-execution.md Rule 2). Reviewer is read-only.

## Task list

- [x] Task 1 — Disposable type            (batched with 2)
- [x] Task 2 — GpuHandleKey / GpuHandleConstructDeps / GpuHandleRow types
- [x] Task 3 — constructGpuHandles walker (TDD)
- [x] Task 4 — destroyGpuHandles walker (TDD)
- [x] Task 5 — Author GPU_HANDLE_ROWS
- [x] Task 6 — Wire initGpu.ts to the construction walker
- [x] Task 7 — Wire wireInput.ts to the construction walker
- [x] Task 8 — Swap-format rebuild via the registry
- [x] Task 9 — Wire engine.ts destroy() to the teardown walker
- [x] Task 10 — Integration regression test + retire destroyReachability
- [x] Task 11 — EngineGpuHandles.d.ts docblock
- [x] Task 12 — Full-suite gate + visual smoke (user looks)

Dispatch batching: Tasks 1+2 batched (four tiny type files, same shape, one review surface). All others individual.

## Pre-flight conflict scan (2026-08-18)

| Pair / task | Produces vs consumes | Finding |
| --- | --- | --- |
| T1 ↔ T2/T4 | `Disposable` type vs its consumer | No task names Disposable's consumer; GpuHandleRow doesn't reference it. Ruling below. |
| T2 ↔ T3/T4 | GpuHandleRow typing vs fake test rows | Test rows must use real GpuHandleKey keys + casts; fixture concern, not a conflict. |
| T5 ↔ T6 | Row order (focusUniform row 0) vs walker call position | REAL CONFLICT: swap rows need uiCtx/fontAtlases in the deps bag (built at initGpu.ts:198-200 today), but T6 says the call sits "where focusUniform's construction used to be" (:116). Also `biasCorrection.attachRenderer(state.gpu.renderer!)` (:181) sits mid-span and would read null. Ruling below. |
| T6 ↔ T8 | Boot-time labelDirector.attachRenderers | Today runs inside buildSwapRenderers (:78-81), which T6 stops calling at boot. Plan anticipates: T6 keeps it as a named post-construction wiring step in initGpu; T8 keeps the rebuild-path call. Two sites, both reading state.gpu.* — consistent. |
| T6 ↔ T8 (mid-sequence) | buildSwapRenderers unchanged until T8 but no longer boot-called after T6 | applySwapFormat path still uses old-style destroy+create against the same fields — consistent tree at every commit. No conflict. |
| T5 ↔ T9 | Declared order vs teardown expectations | Consistent: row 0 focusUniform → destroyed last; pick rows last → destroyed first. |
| T8 | Plan mandates a second inline teardown site (per-row destroy+construct in buildSwapRenderers) | Plan-mandated by design (drift mitigated by shared rows). If a reviewer flags it, it gets a ruling then — not pre-judged in the dispatch. |
| T10 | (a) delete vs (b) strip destroyReachability | Explicit either/or with re-verification duty; implementer decides and reports which. Not a conflict. |
| Per-task self-consistency | T3/T4 tests vs signatures; T5 checklist vs contract; T12 compositor check vs "not a rebuild row" | All agree with their own text. |

Ruling: Disposable consumer — destroyGpuHandles (T4) is the natural consumer (narrowing `state.gpu[key]` before `.destroy()`); T4's implementer uses it there, and if TS resolves the union's `.destroy()` without it, the implementer flags that in its report and we re-rule (an unused one-symbol file violates conventions). Cost if wrong: a dead type file, caught at review.

Ruling: T6 walker call position — the single `constructGpuHandles` call goes where `buildSwapRenderers(state, format)` sits today (after uiCtx/fontAtlases at :198-200), NOT at :116. Every intervening statement in the old :116-:200 span that reads an in-scope handle (at minimum `biasCorrection.attachRenderer(state.gpu.renderer!)` at :181 — implementer re-verifies the whole span fresh) moves to after the walker call with relative order preserved. Rationale: moving pure internal wiring later is behaviour-neutral; moving the fontAtlases load earlier could reorder async boot work. Cost if wrong: boot-order regression, caught by initGpu tests + Task 12 smoke.

## Progress
Ruling: PR #565 (renderer→pointRenderer rename + slot minting → wireSlots) overlaps Tasks 5/6/9/10/11's ground. HOLD Task 5 until #565 + current main are merged into refactor/handle-registry; Tasks 3–4 are name-agnostic and proceed. After the merge, correct citations in the task briefs + ledger (NOT the plan file on main — the plan mandates fresh re-verification at execution time; briefs are the operative requirements). Key rename means the registry row is 'pointRenderer'; the GALAXY_CATALOG_SOURCE_REGISTRY loop leaves initGpu, shrinking Task 6's kept-wiring list. Cost if wrong: merge conflicts on every central file.
Task 1+2: implemented (bg agent, tsc clean), files verified by controller against brief, committed 894515ea4 (05cce3e30..894515ea4), full typecheck green. Draft PR #571 opened. Review dispatched; Task 3 implementer dispatched in parallel (disjoint files).
Task 1: complete — review approved 2026-08-18, zero findings (spec byte-for-byte, imports resolved, independent tsc pass).
Task 2: complete — same review, approved, zero findings.
Ruling: T3 — the plan's verbatim contract line `state.gpu[row.key] = row.construct(state, deps)` fails tsc (TS2322: correlated-union limitation, TS can't track key↔return correlation through a union-typed row variable). Fix = narrow cast on the assignment with a one-line comment citing the distributive GpuHandleRow guarantee; walker semantics unchanged. Cost if wrong: a cast masks a real row-type mismatch — mitigated because each row's `construct` return is still pinned per-key at the row literal, where authoring happens.
Task 3: gate FAILED round 0 (controller tsc reproduced the implementer's named risk; implementer's self-checks never ran — worktree node_modules unpopulated, gates run natively by controller only from here on). Fix round 1: resumed implementer.
Task 3: fix round 1 landed (LHS cast via Record<GpuHandleKey, unknown> + 3-line soundness comment). Controller gate GREEN (full typecheck + 2/2 walker tests). Committed 81f58fcc5 (894515ea4..81f58fcc5). Review package built; T3 reviewer + T4 implementer dispatched in parallel (disjoint files).
Task 3: review PASS / Approved, one Minor — comment budget overrun (10 comment vs 13 code lines; cap is half). Reviewer independently re-ran tsc+vitest green and verified the cast ruling (narrowest reasonable).
Ruling: T3 Minor — trim, don't waive: the comment-budget convention is hard (user feedback: over-commenting is a known failure mode). Both walker files get their inline cast comment compressed (destroyGpuHandles references constructGpuHandles instead of repeating the derivation) and headers tightened to bring each file ≤ half. Folded into T4 fix round 1 as batched mechanical work; T3 trim commits separately from T4 files. Cost if wrong: a lost explanatory line, recoverable from git.
Task 4: implementer DONE_WITH_CONCERNS (no self-check possible, expected). Gate FAILED round 0: (a) destroyGpuHandles.ts:15 TS2532 rows[i] possibly undefined (noUncheckedIndexedAccess); (b) test rows cast construct to the union-wide GpuHandleRow['construct'], which satisfies no single union member (3× TS2322). Fix round 1: resumed implementer.
Task 3: complete — Minor discharged by trim commit c763357c3 (both walker files now ≤ half comment/code).
Task 4: fix round 1 landed (rows[i]! + array-level test cast + comment trims). Gate GREEN (full typecheck, 5/5 gpuHandles tests). Committed 585396191 (T4 files) + c763357c3 (T3 trim), range 81f58fcc5..c763357c3. T4 reviewer dispatched. NO next implementer: Task 5 HELD on PR #565.
Task 4: complete — review PASS / Approved, zero findings; reviewer independently re-ran typecheck + 5/5 tests and spot-checked six more handle classes for destroy(): void (Disposable ruling confirmed live). Branch pushed 894515ea4..c763357c3.
HOLD RELEASED 2026-08-18: #565 merged to main; origin/main merged in (2252c9705, also brings #572 atmosphere constituents), walker test fixtures adapted to galaxyPointRenderer/galaxyPickRenderer keys (f2489a512), gate green, pushed. T5 implementer dispatched with corrections carried in the dispatch text (brief extracted unmodified; implementer re-derives row count + citations fresh). USER DIRECTIVE: controller makes NO file edits in main context — all edits via bg subagents; ledger appends stay terse.
(superseded) PAUSED at the #565 hold: next action when PR #565 (renderer→pointRenderer) merges = `git fetch` + `git merge origin/main` into refactor/handle-registry, re-verify walker tests, update task-5/6 briefs' citations (registry key 'pointRenderer'; GALAXY_CATALOG_SOURCE_REGISTRY loop left initGpu), then dispatch T5 implementer.
Ruling: T5 totality-check deviation ACCEPTED — plan's `as never` snippet is a silent no-op (`as` accepts either assignability direction); implementer's strict `const _totalityCheck: _AssertEveryKeyCovered = true` enforces totality. Controller mutation-verified: deleting the pickProgram row → TS2322 naming the missing key; file restored from commit. Cost if wrong: none — mechanism compiler-proven.
Task 5: implementer DONE_WITH_CONCERNS (totality-check deviation, ruled above). Gate GREEN (full typecheck + 5/5 gpuHandles tests + mutation-verify). Committed 05b79f528 (f2489a512..05b79f528), 44 rows / 8 swap rows. Review package built; T5 reviewer + T6 implementer dispatched in parallel (T6 edits initGpu.ts only — disjoint from registry file; T6 BASE = 05b79f528).
Task 5: complete — review PASS / Approved, zero findings (reviewer independently re-derived the 44-row count, spot-checked rows argument-by-argument against initGpu/wireInput, verified order + swap set + comment budget, re-ran gates green).
Task 6: implementer DONE. Gate GREEN (full typecheck + 11/11 initGpu/gpuHandles tests). Committed 66ecd044a (05b79f528..66ecd044a, initGpu.ts -525/+45): walker call sited per the position ruling, labelDirector.attachRenderers moved out of buildSwapRenderers' boot call, timingService stays hand-constructed. Review package built; T6 reviewer + T7 implementer dispatched in parallel (T7 edits wireInput.ts — disjoint; T7 BASE = 66ecd044a).
Task 6: complete — review PASS / Approved, zero findings (every deleted statement traced to a registry row, a relocated wiring step with order preserved, or a ruled exclusion; reviewer re-ran gates green 11/11). Reviewer noted the in-progress wireInput.ts working-tree edit = T7, out of scope.
Task 7: gate FAILED round 0 — typecheck clean but 5/5 wireInput tests fail: handleDeps bag adds EAGER reads (state.gpu.uiCtx!.context, renderTargets swap-spec format) the old inline code never performed; test stubs correctly reject the widened state requirement. Ruling: reads the old inline construction performed stay eager as-is; any NEW read that exists only to fill the deps bag becomes a lazy getter over the same source, so unread fields cost nothing and fixtures stay untouched (fixtures are the oracle — widening wireInput's boot-state requirement is a behaviour change, not a fixture bug). Fix round 1: resumed implementer. Cost if wrong: a getter hides a field a future row reads before its source exists — surfaced by the same tests.
Task 7: fix round 1 landed (5 unread deps fields → lazy getters; eager set matches old inline read set). Gate GREEN (full typecheck + 19/19 wireInput/initGpu/gpuHandles tests). Committed 25b1ce026 (66ecd044a..25b1ce026, wireInput.ts only). Review package built; T7 reviewer + T8 implementer dispatched in parallel (T8 edits buildSwapRenderers.ts — disjoint; T8 BASE = 25b1ce026).
Task 7: complete — review PASS / Approved, zero findings (eager set = exact read set of the two rows, sources identical to deleted inline code; 5 lazy getters never evaluated, format getter mirrors applySwapFormat.ts:20; fixtures untouched; reviewer re-ran gates green 19/19).
Task 8: gate FAILED round 0 — TS2339 at buildSwapRenderers.ts:45: GPU_HANDLE_ROWS is an as-const literal tuple (required by T5's totality check), so unflagged row literals lack the rebuildOnSwapFormat property entirely and union property access is illegal; 22/22 tests pass. Ruling: filter with an `'rebuildOnSwapFormat' in row` narrowing guard (no cast) — keeps the registry declaration untouched (totality check depends on literal key types) and the consumer honest. Fix round 1: resumed implementer. Cost if wrong: none — compiler-checked either way.
Task 8: fix round 1 landed ('rebuildOnSwapFormat' in row guard). Gate GREEN (full typecheck + 22/22 tests incl. new non-flagged-row test). Committed 457ddd635 (25b1ce026..457ddd635, buildSwapRenderers.ts + its test). Review package built; T8 reviewer + T9 implementer dispatched in parallel (T9 edits engine.ts — disjoint; T9 BASE = 457ddd635).
Task 8: review PASS / Approved, 1 Minor — test-file module header 13 lines (cap 10). Ruling: trim per T3 precedent (comment budget is hard); resumed implementer for the trim, separate commit.
Task 8: complete — Minor discharged by trim commit 2c4d7fe5a (test header 13→10).
Task 9: implementer DONE (free-order sequence diff = plan's pre-accepted trade-off; excluded keys keep hand teardown). Gate GREEN (full typecheck + 22/22). Committed 286fd25f0 (engine.ts -95/+12; trim commit 2c4d7fe5a precedes it, package range 457ddd635..286fd25f0 covers both). T9 reviewer + T10 implementer dispatched in parallel (T10 = integration test + retire destroyReachability — new test file + test deletions, disjoint from engine.ts under review; T10 BASE = 286fd25f0).
Task 9: complete — review PASS / Approved, zero findings (1:1 set match of 44 walker-covered pairs, excluded-key lines verbatim in position, both pinned orderings hold, trim commit comments-only). Reviewer ran the FULL suite by accident: 7004/7004 green at 286fd25f0 — early T12 signal.
Task 10: implementer DONE (option b: destroyReachability stripped to HDR-capability block, renamed initGpu.hdrCapabilityWiring.test.ts; 2 low-value checks deliberately dropped with rationale — coverage table in report). Gate GREEN (full typecheck + 22/22 across 7 files). Committed d19d0f3ec (286fd25f0..d19d0f3ec). T10 reviewer + T11 implementer dispatched in parallel (T11 edits EngineGpuHandles.d.ts docblock only — disjoint; T11 BASE = d19d0f3ec).
Task 11: implementer DONE, typecheck GREEN (comments-only diff, 9/8 lines in EngineGpuHandles.d.ts). Committed 1efee7511. Cheap-tier reviewer dispatched (tiny doc diff).
Task 11: complete — review PASS / Approved, zero findings (every docblock claim verified against source; 10-line header, timeless).
Task 10: review spec PASS / quality NEEDS CHANGES — 1 Major (round-trip test's duplicate-key/createPlanetRenderer-once coverage claim false: dup-key row collapses to 1 destroy, class uncovered anywhere), 1 Minor (redundant second assertion in test 2). Ruling: Major → add a key-uniqueness assertion over GPU_HANDLE_ROWS (dup row = silent overwrite leak; not compiler-enforced, real copy-paste class) + correct the false claims in test comment and report. Minor → KEEP the redundant assertion (documents the pinned risk-register constraint; reviewer concurs). Fix round 1: resumed implementer.
Task 10: fix round 1 landed (uniqueness test 'declares every key exactly once' + claim corrections in test comments and report). Gate GREEN (typecheck + 23/23). Committed dc83de2ce. Scoped re-review dispatched to original reviewer.
Task 10: complete — scoped re-review ALL FINDINGS ADDRESSED (uniqueness test genuinely closes the gap, claims clean).
Task 12: full gate GREEN — typecheck clean, npm test 7005/7005 (1043 files). DoD structural verifier dispatched. REMAINING: user visual smoke at http://localhost:5174 (galaxy points, MW disk, labels, pick/hover; HDR toggle → applySwapFormat; compositor staleness verdict either way — compositor deliberately NOT a rebuild row, decisions.md #11 bug suspect; if stale → follow-up, separate PR).
Task 12: DoD structural verification — all 6 bullets PASS (verifier report in transcript; notable: BGLs correctly have no teardown — no destroy() in WebGPU spec; buildSwapRenderers' inline per-row loop confirmed as the plan's deliberate second teardown site). Whole diff vs main = 12 src files + 5 test files, boundary clean. BLOCKED ON USER: visual smoke pass (last open item of rung 1).
Task 12: complete — USER visual smoke attestation 2026-08-18: 'yes all works' (scene renders, pick/hover fine, HDR toggle clean, compositor NON-STALE during swap toggle — decisions.md #11 bug-suspect check discharged as verification-only, no follow-up needed). ALL 12 TASKS COMPLETE. Finishing: final whole-branch review (opus) + plan checkbox tick dispatched; then /feature-done housekeeping, PR #571 out of draft.
FINAL REVIEW (opus): Approved — 0 Critical / 3 Major / 8 Minor / 7 Obs (full: final-review.md). Rulings: FIX ON BRANCH in one dispatch = 1.1 phase flag on the two wireInput rows (row-divergence rule, mirrors rebuildOnSwapFormat; filters derive complementary by construction) + 1.2 structural assertions per review + 3.3 re-home foreground:0 + MW starCount rationale into registry + all 8 Minors incl. 5.1 ctx:GpuContext consolidation and 5.2 in-guard+value-test + strike decisions.md #11 compositor suspect (resolved NEGATIVE, matches user smoke). PARKED as follow-ups: 4.6 dead compositor swapFormat/hdrFormat args (adjacent cleanup → backlog rule), 4.5 key-semantics join = rung-2 decision, 4.7 import-surface observation. Cost if wrong: rework visible in one commit.
IN FLIGHT: final-fix implementer (bg, sonnet) — fixes Majors 1.1/1.2/3.3 + 8 Minors + decisions.md #11 resolved-negative reword; report → final-fix-report.md. ON ITS DONE: controller gates (npm run typecheck + npm test full), commits ONE fix commit, scoped re-review by the final reviewer (resume, verdict appended to final-review.md), then completion: copy this ledger to docs/superpowers/plans/completed/2026-08-17-handle-registry.ledger.md, git mv plan to plans/completed/ (checkboxes already all ticked; no spec file — plan resolves against decisions.md; no backlog item to sweep — verified), completion commit 'docs(plan): mark handle-registry complete', push, then gh pr ready 571 (announce). Follow-ups parked above (compositor dead args → backlog file if promoted; 4.5 rung-2).

## Final fix round — 2026-08-18
- Fix implementer (aada49effa9c8edd0) DONE: all in-scope findings (Majors 1.1/1.2/3.3, Minors 1.3/3.2/4.1-4.4/5.1/5.2, decisions.md #11 reword). Report: final-fix-report.md.
- Controller gates: typecheck CLEAN, npm test 7008/7008 (1043 files, +3 tests). Prettier: all touched files unchanged.
- Fix commit 01e386d90 (12 files, +262/-160). Plan-file checkbox ticks held back for the completion commit.
- Scoped re-review dispatched: resumed final reviewer af78980936e00522e with final-fix-diff.md (dc83de2ce..HEAD) + final-fix-report.md; 4.5/4.6/4.7 marked out of scope. Awaiting verdict.
- Re-review verdict (af78980936e00522e): ALL FINDINGS ADDRESSED. Two non-blocking notes, adjudicated:
  - Ruling: PARK note 1 (bare `'constructPhase' in row` filters vs the value-tested shape 5.2 fixed in buildSwapRenderers) — filters stay complements by construction so no double-construct/drop is possible, and a `constructPhase: undefined` row fails loudly via the ctx.format throw; fold the `=== 'wireInput'` uniformity tweak into rung 2's first touch of these files — cost if wrong: a confusing (but loud) error message, never silent corruption.
  - Ruling: KEEP note 2 (throwing ctx.format getter in wireInput makes deps.ctx non-spreadable) — the throw is the point: format is unresolvable at that phase, and a loud throw beats a silently-wrong format; the message names ctx.format which is greppable — cost if wrong: a future wireInput row author spends minutes tracing a spread.
- COMPLETE: ledger archived to docs/superpowers/plans/completed/2026-08-17-handle-registry.ledger.md; plan moved to plans/completed/; PR #571 marked ready.
