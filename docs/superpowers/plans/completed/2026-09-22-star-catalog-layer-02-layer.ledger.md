# SDD ledger — plan: docs/superpowers/plans/2026-09-22-star-catalog-layer-02-layer.md

Spec: docs/superpowers/specs/2026-09-21-star-catalog-layer-design.md. Protocol: docs/superpowers/conventions/sdd-execution.md (lean).
Worktree: .claude/worktrees/star-catalog-layer, branch worktree-star-catalog-layer, dev server :5174. BASE (plan commit) bba53a8bf on main 29470a2d9.
User rulings at start (2026-09-22): perf gate NONE (spec §9); parallelism: a separate volume-Layer agent may run in another wt — land order first-ready, the other merges main; neither changes the Layer contract.
Grouping: D1 = T1+T2 (Opus, T2 review: yes) · D2 = T3+T4 (Sonnet) · D3 = T5+T6 (Opus, T5 review: yes) · D4 = T7+T8 (Sonnet). One final review (Opus). Mid-branch reviews: T2, T5.
Pre-flight scan: clean — the T2→T5 transitional pieces (body row seeded branch over SEEDED_STAR_CATALOGS, starPickId walk) are deleted by T5 by design; T3 moves the selection row with the `star` arm and T5 reshapes it; T2 edits the star caption rows T7 removes.
Draft PR #802 opened at bba53a8bf (plan commit only).
D1 (T1+T2) dispatched: Opus, BASE bba53a8bf — RUNNING.
Task 1: complete (commit 69032619b, no review — behaviour-neutral, compiler-checked).
D1 handed back 2026-09-22: T2 committed f25d1a058 (54 files, +568/−560), tree clean, typecheck:fast clean on src, full suite green per report (task-1-2-report.md: 7 deviations incl. `...SCENE_SUN` in sceneBodies.ts, Sun caption now follows the cluster master, pass-test fixtures drive real seeded tables; findings for /feature-done deletion audit: `bodies.items[id].enabled` has zero readers, `SCENE_STAR_LABEL_IDS` has no src reader; build-famous-stars rewrote MAIN's gitignored public/data/famous_stars_meta.json via the symlink).
Task 2: complete (commit f25d1a058; Opus review task-2-review.md: spec ✅, Approved, 0C/0I/7M).
  Ruling: the 7 Minors are PARKED for the final fix wave — T5 deletes two of the four duplicated seeded-derivation sites (starPickId walk, bodySelectionRow seeded branch), so the residual dup is judged then; the rest (starPickId.test.ts stale "TWO tables" header, two fixtures passing the deleted `data.bodies.stars`, sceneSun.ts + seededStarCatalogs.ts over comment budget) are one-line fixes. Cost if wrong: one extra commit in the fix wave.
  USER EYE-CHECK at smoke: Stars cluster master off now hides the Sun's dot/sphere/caption/occluder too (§8 "each source gates itself"; previously the Sun survived master-off).
COORDINATION 2026-09-22 (user note + dome session msg): the dome-planning-prep PR (wt dome-planning-prep, NOT yet on main, plan docs/superpowers/plans/2026-09-22-per-view-planning-prep.md) DELETES Layer.frame/LayerInstance.frame/LayerFrameVote/ReadyFrameContext.layersSettling and replaces them with `Layer.planners?(runtime): readonly ContentPlanner<unknown>[]` rows ({ name, scope: 'once', plan(snapshot, views, state) } | { name, scope: 'perView', plan(view, state) } → PlanResult<T> { value, awake, settling }), results in snapshot.plans. Ruling: #802 continues on the T1 shape (already landed); whichever PR merges second rebases the star hook onto a scope 'once' planner row (mechanical; the prep PR's Task 2 shows galaxyCatalog/flow rows as the pattern). If the prep PR lands first, merge main into this branch BEFORE the final review and convert then. Replied to the dome session.
D2 = T3+T4 (Sonnet) DISPATCHED at BASE f25d1a058 → report task-3-4-report.md. On handback: verify commits + typecheck:fast, no review (T3/T4 are `review: no`), ledger, then D3.
  (carried to D2: briefs task-3-brief.md + task-4-brief.md; carry: T1's hook shape `(views, state)`; T2's names (`SEEDED_STAR_CATALOGS` in src/data/bodies, `SeededStarCatalogId`, `StarCatalogSourceType`, `PositionedStar.source/seedIndex`, `visibleStars(settings)`); the T4 verification landmine (count exports per @types file; grep value-imports of pure types).
  4. Then D3 = T5+T6 (Opus, T5 review: yes), D4 = T7+T8 (Sonnet), final review (Opus) over merge-base..HEAD, one fix wave, push, `gh pr checks 802 --watch`, DoD audit, diff breakdown, ledger archive to docs/superpowers/plans/completed/, plan+spec → completed/ at /feature-done, deletion audit at /feature-done, squash-merge ONLY on the user's word via `gh api -X PUT repos/rulkens/skymap/pulls/802/merge -f merge_method=squash` (never `gh pr merge` from the wt), delete remote branch via API.
D2 RETIRED mid-T3 2026-09-22 (context cliff >400k, user flagged). Ordered: write task-3-4-report.md as a HANDOFF (moves done/not-done, hand-edited files, remaining T3 steps + settled names, typecheck state), NO commit, NO test run. Tree is DIRTY and uncommitted (moves under src/layers/starCatalog/{@types,load,passes,present,render,sources,state,ui} + tests mirror; imports still broken). On its reply: dispatch a FRESH Sonnet implementer D2b for the REST of T3 (+T4) reading task-3-brief.md + task-4-brief.md + the handoff report; it continues in the dirty tree, commits T3 then T4 separately. Do not re-run the moves.
D2 handoff received (task-3-4-report.md: 35/35 moves done, 22 files hand-edited, T3 §3 checklist 1–8 remaining, T4 not started). D2b = fresh Sonnet DISPATCHED 2026-09-22 to finish T3 (§3) then T4 from the dirty tree → report task-3-4-final-report.md. Rulings carried: R1 famousStarsMeta facts-only ACCEPTED; R2 restore `if (!settings.enabled) return false` in advanceStarFades; R3 add starCatalogLayer to compositions/app.ts (deviation, not in brief); R4 pass order from frameOrder.ts; R5 inspect Stars UI files before wiring; R6 fix starCatalogFadeRows id-union type properly, no cast. On handback: verify two commits + typecheck:fast, no review (T3/T4 review: no), ledger, then D3.
Task 3: complete (commit e5a946ed5, no review — review: no; final report task-3-4-final-report.md, 24 listed deviations incl. R1–R6 applied, whole `starCatalogVisible agrees` describe deleted, app.ts composition wired, pass order from frameSections.ts).
Task 4: complete (commit d579d8636; 5 types KEPT core with real non-Layer importers — StarCatalogSettings/ItemSettings (visibleStars, LabelHomes), the 3 StarCatalog*SourceEntry (SourceEntry union). Ruling: ACCEPTED, the plan's move list was wrong. StarNodeDraw.d.ts deleted).
  Verified: tree clean, typecheck:fast clean, full suite green per report (1390 files / 14301 tests), npm run build green.
  PARKED for the final fix wave: (a) coverage gap — boot-seed test for `src/layers/starCatalog/create.ts` (model on tests/layers/flow/create.test.ts); (b) 5 test files still at tests/services/gpu/renderers/starCatalog/ (mirror gap: nodeParamsLayout, recordRepackUnpackParity, starCatalogPickRenderer.frustumCull, starCatalogRenderer.frustumCull, starCatalogRenderer.viewSlot) → move to tests/layers/starCatalog/render/.
  LANDMINE (memory-worthy): move-files does not rewrite `.wesl?static` specifiers, `.mts` files, or `vi.mock()` string literals; only `npm run build` catches the wesl ones.
D3 = T5+T6 (Opus, T5 review: yes) DISPATCHED at BASE d579d8636 → report task-5-6-report.md. On handback: review-package d579d8636..HEAD, Opus task review over T5 (+T6 in the same package), ledger, then D4.
D3 agent STALLED (stream watchdog, 352k context) after committing T5 = b27aac0e3 and mid-T6 ("BodyDetailCard trimming"); no task-5-6-report.md written. NOT resumed (retire rule). Tree DIRTY with T6 work: StarInfo/StarInfoDetail types, StarDetailCard/CompactStarCard created, FieldStar* deleted, buildFocusable/refOf/selectors/detailCardTable/BodyDetailCard/container/sStarOrbitInfo edited, BodyOrbitInfo → src/@types/engine/StarOrbitInfo.d.ts (kept core), StarDetailCard.test.tsx created; BodyDetailCard tests + container test + buildFocusable.test not yet done. D3b = fresh Opus DISPATCHED to finish T6 in the dirty tree, commit, and write task-5-6-report.md (T5 part reconstructed from `git show b27aac0e3`). On handback: review-package d579d8636..HEAD, Opus task review (T5 review: yes; T6 rides in the same package), ledger, then D4.
Task 5: committed b27aac0e3 (D3, stalled agent). Task 6: committed c314916d0 (D3b; DONE_WITH_CONCERNS: StarInfo/StarInfoDetail placed in src/@types/engine/ not the Layer's @types — reason: core buildFocusable/refOf would import into a Layer and break layerImportBoundary; StarOrbitInfo kept core; buildFocusable's local StarCatalogRow alias dropped). Verified: tree clean, typecheck:fast clean, full suite 1391/14317 green, build green per report.
  Findings from D3b for review/audit: StarInfo.radiusM writer-only; Task 5 did not write the brief's starCatalogSelectionRow.test.ts; Sun's dot reportedly unclickable in the solarSystem exhibit (UNRULED — needs trace + user eye-check); fieldStarSpherePass is the last `fieldStar` half-rename.
  Review package review-d579d8636..c314916d0.diff written. T5+T6 Opus REVIEW DISPATCHED → task-5-6-review.md. On handback: rule on findings; Critical/Important → one fix dispatch (Sonnet) + scoped re-review; Minors → park to the final wave; ledger `Task 5/6: complete`; then D4 = T7+T8 (Sonnet) at the resulting HEAD.
T5+T6 REVIEW (task-5-6-review.md): spec T5 ❌ (§8: solarSystem exhibit lost every star's click; §6 doc nit `star-S2` vs seed id `s2`), T6 ❌ (§2.3 StarInfo location only — reviewer + controller RATIFY the code: core buildFocusable/refOf/FocusableTarget name StarInfo, so it is a shared type under src/@types/engine/; spec §2.3 gets a one-line correction in T8). Quality: Changes requested — 1C/3I/10M.
  RULINGS:
  C1 solarSystem exhibit `starCatalog: false` → option (a): flip to `true`, comment rewritten. Why: the pre-branch comment's stated intent was that the Sun's own dot stays clickable in the Sun's exhibit; the kind gate can no longer separate seeded from survey stars, and losing the subject's click is the worse asymmetry. Cost if wrong: a Gaia star click becomes possible at 42 AU (faint, needs aiming); one boolean to revert. §8 gains this line in T8. USER EYE-CHECK at smoke.
  I2 delete dead FAMOUS_STAR_IDS + its two lying comments. I3 rewrite watchFocusTweenSaga.ts:111-118 comment around the S-star case. I4 add caption pick-id test (famous star + Sun) in sceneBodyLabels.test.ts. M2 delete writer-only StarInfo.radiusM (spec §2.3 correction rides T8). M5 fold paletteRows duplicated arm into one helper. → ONE fix dispatch (Sonnet), commit `fix(stars): Tasks 5+6 review fixes`, then scoped re-review (Sonnet).
  PARKED: M1 accepted (transitive cover; no new test); M3 distance-row format = USER EYE-CHECK at smoke; M4 comment budgets on the 4 new headers → final wave; M6 tie-break note only; M7/M9 no-op; M8 spec `star-S2`→`star-s2` in T8; M10 `fieldStarSpherePass` half-rename → final wave (name decided then). Could-not-verify: stale `#focus=body-sirius` parks in the deferral loop like any unresolvable id — pre-existing, accepted.
FIX DISPATCH (T5/6, round 1, Sonnet) at BASE c314916d0 → task-5-6-fix-report.md.
Fix round 1 landed a959384c1 (all 6 items; full suite 1391/14318 green). Scoped re-review (Sonnet) DISPATCHED over review-c314916d0..a959384c1.diff → task-5-6-rereview.md; any open item folds into the final wave.
Task 5: complete (b27aac0e3 + fixes a959384c1; review task-5-6-review.md, rulings above). Task 6: complete (c314916d0 + fixes a959384c1).
D4 = T7+T8 (Sonnet) DISPATCHED at BASE a959384c1 → report task-7-8-report.md. T8 additionally carries the spec corrections ruled above: §2.3 StarInfo/StarInfoDetail live in src/@types/engine/ (shared with core) and StarInfo has no radiusM; §6/§8 `star-S2` → `star-s2`; §8 gains "the Solar System exhibit keeps every star pickable (the Sun's dot is a star-catalog row)". On handback: verify commits + typecheck:fast, no review (T7/T8 review: no), ledger, then check dome-planning-prep #805 merge state (if merged: merge main + convert frame hook → planners row BEFORE the final review), then final review (Opus) over merge-base..HEAD.
Re-review (task-5-6-rereview.md): All addressed, no open items, no new findings. T5/T6 review loop CLOSED.
Task 7: complete (967d6bdd3, no review). Task 8: complete (17eee6929, no review; report task-7-8-report.md: captions walk SEEDED_STAR_CATALOGS unconditionally so a toggled-off star eases out; bodyCaption extracted to src/utils/labels/; CaptionSourceRow type added; meta-getters backlog deleted whole; ONE new oneMpcSeam.test.ts allow-list row (flag for final review); concern: produceStarCaptions.ts and produceSceneBodyCaptions.ts duplicate ~50 lines of fade/occlusion/prominence math; DoD inventory line about StarInfo location is stale).
#805 (dome prep) still OPEN → no planners conversion; the star hook stays on Layer.frame. Merged origin/main (db3a14341) → merge commit 7880f51b9, clean; typecheck:fast clean; full suite 1393/14348 green.
FINAL REVIEW (Opus) DISPATCHED over merge-base db3a14341..7880f51b9 (314 files, +6091/−4991) → final-review.md. Branch pushed; `gh pr checks 802 --watch` running in the background. On handback: rule; ONE fix wave folding in the parked items (T2's 7 Minors, create.ts boot-seed test, 5 test files to tests/layers/starCatalog/render/, T5/6 M4 comment budgets, M10 fieldStarSpherePass half-rename, produceStarCaptions duplication + unused `runtime` param if confirmed) + scoped re-review; push; CI; DoD audit; diff breakdown; ledger archive; /feature-done on the user's word.
CI GREEN at 7880f51b9 (typecheck · test · format pass; Workers Build pass). Final review still running.
FINAL REVIEW (final-review.md): spec ✅ except §9 (two plan-mandated tests missing); DoD holds (StarInfo DoD line stale; S_STAR_ORBITAL_ELEMENTS also read by coreTrailElements — legitimate); Changes requested 0C/4I/10M.
  RULINGS for the ONE final fix wave (Sonnet):
  I1 write tests/layers/starCatalog/frame.test.ts + render/cut/starSourcesInBand.test.ts per the plan's Task 3 text; delete the three self-comparing "shares the gate" tests (starAggregateUpsamplePass ×2, starAggregatesPass ×1). I2 restore the identity memo in produceStarCaptions. I3 SEEDED_STAR_CATALOGS_BY_SOURCE rows carry `id`; visibleStars/produceStarCaptions/create.ts read it; casts deleted (closes T2 M2). I4 clean initGpu.hdrCapabilityWiring.test.ts. M1–M5, M7, M10 fix; M6 trim the nine over-budget files (folds T2 M5/T5 M4); M8/M9 one-line trap notes.
  Deletion candidates taken NOW (trivial): produceStarCaptions' unread `runtime` param; the ~50-line caption compose duplication → extract ONE shared helper in src/utils/labels/ used by both producers (pixel-identical; parameterised only by the clip key), which also deletes the new oneMpcSeam allow-list row and hosts I2's memo. DEFERRED to /feature-done (user rules): bodies.items[id].enabled, SCENE_ORBIT_CONICS, the *Props exports.
  Parked items folded in: 5 orphaned test files → tests/layers/starCatalog/render/ + empty cut/ dir removed; create.ts boot-seed case added to the existing create.test.ts if a stub device is feasible (else report). Two T2 fixtures still passing deleted `data.bodies.stars` → check + fix. M10 `fieldStarSpherePass` RULED NOT a half-rename: "Field star" is the live UI term for a Gaia star (StarInfo.displayName) — keep the name.
FINAL FIX WAVE DISPATCHED at BASE 7880f51b9 → final-fix-report.md; then scoped re-review (Sonnet), push, CI, DoD audit, diff breakdown, ledger archive, /feature-done on the user's word.
Final fix wave landed b5b042d89 (40 files +514/−518; full suite 1395/14357 green; build green). Extras: 4 more dead vi.mock blocks removed in initGpu.hdrCapabilityWiring.test.ts (same defect class); frameFilePurity row `frame/visibleStars` 1→0; create.test.ts boot-seed case DECLINED (needs a 7-mock harness) → backlog candidate at /feature-done. Pushed; scoped re-review (Sonnet) DISPATCHED → final-rereview.md; `gh pr checks 802 --watch` in background. Next: DoD audit + diff breakdown (code/comment/test/doc) over db3a14341..HEAD, ledger archive, then WAIT for the user's smoke + word before /feature-done and the API squash-merge.
DIFF BREAKDOWN db3a14341..b5b042d89 (320 files, +6124/−5028 excl. lockfile): code +1910/−1582 (net +328) · comment +1079/−1266 (net −187) · test +2395/−1909 (net +486) · doc +566/−138 (net +428) · other +174/−133. src only +3164/−2962. Script: scratchpad/diffBreakdown.mjs.
DoD AUDIT = final-review.md §2 table: every line holds except two stale plan lines to correct at /feature-done — (1) StarInfo/StarInfoDetail live in src/@types/engine/ (core-shared); (2) S_STAR_ORBITAL_ELEMENTS is also read by coreTrailElements.ts (by-reference exclusion). §8 observables need the user's smoke.
Final re-review (final-rereview.md): All addressed, 0 open, no new findings. REVIEW LOOP CLOSED. Awaiting CI at b5b042d89, then the user smoke + word.
CI GREEN at b5b042d89 (typecheck · test · format pass). EXECUTION COMPLETE. Waiting on the user: smoke (§8 observables + queued eye-checks) → word → /feature-done (plan+spec → completed/, DoD lines corrected, ledger archived, deletion audit incl. bodies.items[id].enabled / SCENE_ORBIT_CONICS / *Props exports / create.test.ts boot-seed backlog) → API squash-merge → API branch delete → wt cleanup. If #805 merges first: merge main + convert the star frame hook to a scope once planner row, re-run CI.

## Smoke 2026-09-22 (user, on :5174) — findings and proposed sequence
- FAMOUS-STAR CARDS EMPTY = STALE DEV SERVER, not the branch: T2's `build-famous-stars` rewrote `public/data/famous_stars_meta.<hash>.json` under a new hash through the symlink at 02:11; the :5174 server (started 21 Sep 22:47) serves the new name as `text/html` (Vite public-dir cache), sidecar fetch throws on JSON.parse → slot publishes `[]` → `detail: none`. Fix = user restarts :5174 (never kill it ourselves). Memory updated (reference_vite_serves_html_for_new_public_dirs).
- SUN CARD LOST ITS CURATED INFO = REAL REGRESSION: T2 deleted the Sun's row (G2V, 5772 K, 4.6 Gyr, description …) from `data/seeds/famous_stars.seed.json` to get it out of the drawn famous table; the sidecar is generated from the same seed, so the Sun's InfoCard meta went with it. Spec §2.3 "Sun uses detail none until it has something of its own" was written blind to that entry. Options for the user: (A, recommended, smallest) compiled-in `src/data/bodies/sunMeta.ts` (`SUN_META: FamousStarMetaEntry` = the deleted row) seeded into the Layer's `famousStarsMeta` fact (`facts` initial + slot publish `[SUN_META, ...meta]`), no lookup change, works before the sidecar lands; (B, spec's "seed-row field" shape) restore the seed row + a `catalog` field the generator uses to keep the Sun out of the drawn table and the Rust dedup — parser type, generator, regenerate, tests.
- MAIN MOVED: #807 `df8e1fce2` (card-shot thumbnails; rewrites BodyDetailCard 225 lines, keys famous-star shots through FAMOUS_STAR_IDS which this branch deleted). Needs merge main + conflict resolution + moving the famous-star shot into StarDetailCard (shot ids were `body-<id>`, focus ids are now `star-<id>` — check SHOT_CARD_IDS/cardShotUrl keying). #805 planners NOT merged yet.
- SEQUENCE on the user's ruling: one fix dispatch (Sun meta per A/B + merge main + famous-star shot in StarDetailCard) → typecheck/test → push → CI → user re-smoke (server restarted) → /feature-done.
- USER RULED 2026-09-22: "the correct shape" — the Sun is a seed-backed catalog like the famous stars: `data/seeds/sun.seed.json`, the generator iterates a seed registry (one generated table per seed, one sidecar + Rust dedup over all), `sceneSun.ts` = `SUN_GENERATED.map(star)`, anchor via `starAnchor`, search names from the row; no `id === 'sun'`, no catalog field. Sidecar/runtime "famous" names stay this PR (backlog line). Card shots: card id == focus id again (six card ids + webp files renamed `star-*`, `cardShotByFocusId` bridge deleted, StarDetailCard hosts the shot per main's #807 body layout).
- SMOKE-FIX DISPATCH (Opus, background) at BASE b5b042d89: brief `smoke-fix-brief.md` (Unit 1 merge main df8e1fce2 with 3 conflicts; Unit 2 Sun seed; Unit 3 shots) → report `smoke-fix-report.md`. On handback: verify commits + typecheck:fast + suite, review package BASE..HEAD, scoped re-review (Sonnet), CI watch, then user re-smoke on a RESTARTED :5174 → /feature-done.
- SMOKE-FIX handed back 2026-09-22: 3 commits pushed — 4bf5e9299 merge main df8e1fce2 (3 conflicts resolved; shotCardIds.ts restored from main) · eac20a168 Sun's own seed file (registry-driven generator, famousStars.generated + Rust byte-identical, sidecar 119 entries incl. sun) · fc0427bcc star card shots (TEN card ids + webp → star-*, not six: alpha/proxima-centauri, rigel, antares also flipped; shot gate = detail.kind curated, image only withheld so S2 keeps its orbit rows). HEAD fc0427bcc; full tsc + suite 14,371 green + build green per report; typecheck:fast clean here; tree clean. Report smoke-fix-report.md.
  MANIFEST: buildDataManifest skips the symlinked public/data BY DESIGN ("a linked tree belongs to the checkout that built it"); rebuilt main's manifest from the wt via scratchpad `rebuildMainManifest.mts` (imports the exported buildDataManifest with main's absolute path) → famous_stars_meta → afefc923, old fc3cceb2 removed. :5174 MUST be restarted before smoke (new hashed file).
  Scoped re-review (Sonnet) DISPATCHED → smoke-fix-review.md; CI watch running in background. Then: fix findings if any → user re-smoke → /feature-done.
- Scoped re-review APPROVED (smoke-fix-review.md): 0C/0I/3M — M1 StarDetailCard.tsx:258-260 comment names "the Sun" for a branch it can no longer reach (fold into /feature-done's final tidy); M2/M3 module headers over 5 lines in StarDetailCard.tsx and buildFamousStars.ts (pre-existing debt, park). All 4 deviations verified. Waiting: CI + user re-smoke (RESTART :5174 first).
- CI GREEN at fc0427bcc (typecheck · test · format 6m18s; Workers build pass). Back in the wait state: user re-smoke on a RESTARTED :5174 → word → /feature-done (M1 comment fix rides the final tidy) → API squash-merge → branch delete → wt cleanup. #805 planners still open: if it merges first, merge main + convert the star frame hook to a scope once planner row, re-run CI.
- 2026-09-22 user smoke finding: Sun description had LLM tells (carried verbatim from main's old seed). Rewritten plainly in data/seeds/sun.seed.json → 5c6a09443 pushed; sidecar rebuilt (hash 6f5bb0e4), main's manifest rebuilt via the scratchpad script, :5174 restarted (user asked; detached `npm run dev -- --port 5174 --strictPort`, log in scratchpad dev-5174.log). CI watch running. Still waiting on the user's re-smoke + word.
- CI GREEN at 5c6a09443. /feature-done RUNNING on the user's "we need to land this": checkboxes ticked (35/35) + four DoD lines corrected in the plan (uncommitted); backlog clean (T8's two new items present); zero TODO markers; deletion audit (Opus, deletion-audit-whole-branch.md): SAFE-NOW ~8 src/~40 test LOC (6 items) being applied by a Sonnet agent as `chore(stars): deletion audit …` (not pushed until verified); NEEDS-RULING for the user: N1 `SCENE_ORBIT_CONICS`/`deriveOrbitConics` (66 src + 132 test LOC, zero src readers, pre-existing) and N2 `settings.bodies.items[id].enabled` (zero production readers, ~18 LOC / 8 files, settings-shape change) — both pre-existing, outside the feature remit; landing without them, offered as a follow-up.
  NEXT: verify deletion commit → completion commit (plan+spec → completed/, ledger archived) → push → CI → API squash-merge → API branch delete → wt cleanup incl. agent-* worktrees.
- Deletion commit f960d4a92 (19 files, +51/−87; src −2, tests −34; S1–S6 + the frustumCull test mirror move). USER RULED: N1 `SCENE_ORBIT_CONICS` → delete in a follow-up, backlog line added to docs/BACKLOG.md. N2 `settings.bodies.items[id].enabled` UNRULED at landing (asked; left in place).
- /feature-done VERDICT: READY (tests + typecheck green via CI at 5c6a09443 and locally on f960d4a92; 35/35 checkboxes; DoD present, four lines corrected to what shipped; spec has "Ground preparation" §3; no TODO markers; smoke attested by the user on :5174 2026-09-22 incl. the Sun/famous card fixes; deletion audit applied). Completion commit next, then push → CI → API squash-merge on the user's "we need to land this".
