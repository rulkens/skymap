# SDD ledger — plan: docs/superpowers/plans/2026-09-22-per-view-planning-prep.md

Worktree: .claude/worktrees/dome-planning-prep, branch worktree-dome-planning-prep, draft PR #805.
Spec: docs/superpowers/specs/2026-09-19-view-rigs-dome-fisheye-design.md § Ground preparation 2.
Fact sheet: /Users/rulkens/Development/js/skymap/.claude/worktrees/dome-fisheye/.superpowers/sdd/2026-09-19-dome-fisheye/prep-callgraph.md

Plan-start answers (user "ok" 2026-09-22): parallelism = this PR only, serial grouped dispatches; grouping A=T1+T2, B=T3+T4, C=T5+T6 (review: yes, Opus), D=T7.
Ruling: no perf gate — prep PR, T1–T4 bit-identical mono, T5/T6 retune constants only — cost if wrong: an unmeasured CPU delta in the planner path; the dome feature's own gate would catch it.
Pre-flight scan: no conflicts found (T1 signature change on checkFrameOrder is reconciled against #800 at merge, per Global Constraints).

Docs commit c557a97ff (spec section + plan) — controller.
Group A (T1+T2) Sonnet: c557a97ff → ad0e35dfd (f86f51c66 T1, af4ebc896 T2, ad0e35dfd PlanTarget type extraction). Status DONE. diskPlannerWalk: no frustum cull (distance gate only). Deviations: boot-test fixtures gain inert galaxy-catalog/flow planner stubs; structureMarkersPlanner casts PassState→EngineState (runMarkerProducers kept); purity sweep extended to frame/planners with index.ts exempt as registry; runFrame hook-loop describe retired for a plans.awake fold test. Turn count: not reported.
Task 1: complete (commits c557a97..ad0e35d)
Task 2: complete (commits c557a97..ad0e35d)
Group B (T3+T4) Sonnet: ad0e35dfd → 50acb72be (5f420d3e3 T3, 50acb72be T4). Status DONE. Deviations: faceViewSpec takes `key`; static slot-name sites (timedSlotRowsOf, plainPassGroupKeys, engine.ts toggles) reconstruct 'canvas' / key:face; expandFrameOrder.test asserts aerial position relative to capture count.
Task 3: complete (commits ad0e35d..50acb72)
Task 4: complete (commits ad0e35d..50acb72)
Group C (T5+T6, review: yes) Opus dispatched from 50acb72be.
Group C (T5+T6) Opus: 50acb72be → 68ddd8249 (66d7440df T5, 68ddd8249 T6). Status DONE. Deviations: (a) worldEmMpc NOT scaled (load-bearing physical length) → LABEL_EM_PX_RETUNE in labelSizingDefaults.ts mirrored as EM_PX_RETUNE in labels/vertex.wesl, no parity test; (b) RING_PX_WIDTH/AA invariant → marker RADIUS retuned via MARKER_SIZE_PX_RETUNE, expandBillboardWorld deleted; (c) STAR_SPHERE_FADE_START_PX carries the fold; (d) InstancedQuadRenderer.draw pxPerRad required; (e) off-canvas passes scale drawPxPerRad by viewportHeight/canvasHeight; CPU label twins (labelScreenRect, liftedLabelPlacement) take pxPerRad. Deletion candidate: cameraBillboardBasis.ts (no src callers).
Task 5: complete pending review (commits 50acb72..68ddd82)
Task 6: complete pending review (commits 50acb72..68ddd82)
Group D (T7) Sonnet: 68ddd8249 → dd7c97625. Status DONE, no deviations.
Task 7: complete (commits 68ddd82..dd7c976)
Mid-branch Opus review of group C dispatched (review-C.diff).
Group C review (Opus): math PASS, byte layouts PASS, deviations (a)–(f) ACCEPTED (a, f conditional on parity test). Findings 5 Important + 6 Minor → ONE fix round resumed on the group C Opus implementer (findings 1–9). Ruling: finding 3 delete cameraBillboardBasis.ts + test in this PR (T6 removed the last caller; deletion beats a deferred audit line) — cost if wrong: a trivial revert. Ruling: finding 8 (texturedDisks `_pad2` carries live pxPerRad) deferred — the plan's Deferral boundary keeps the per-renderer pxPerRad copies for the dome deletion audit; eye-check "MW blobs render at all" joins the PR body. Finding 11 (off-canvas scaling unobservable today) noted, no fix.
Group C fix round: dd7c97625 → 2e7820798 (findings 1–3, 5–9 fixed). Task 5: complete. Task 6: complete.
Parked — review finding 4 (drop `view` from deriveFrameView's returned type) — Ruling: reviewer wrong, `FieldHeaderFrameLanes` reads `frame.view` via buildFieldHeaderInputs.ts:75 (typecheck-verified by the implementer); the code stands.
Final whole-branch review (Opus, 890984682..2e7820798): 1 Critical (structure markers never render — enabled gates on markerCount() which is 0 until draw uploads; controller VERIFIED in structureMarkersPass.ts), 4 Important (settling⇒awake fold lost; PassState→EngineState cast — narrow runMarkerProducers instead; capture-face view id minted twice; MW io.wesl f32-index line stale), 7 Minor. DoD: all deliverables MET except "mono unchanged" (Critical 1).
Ruling: ONE fix wave on a fresh Opus (group A's Sonnet is past the 350k context cliff) for findings 1–12; finding 9 ACCEPT with a comment (boot stub names mirror frameSections) — cost if wrong: a Layer planner rename is caught at real boot, not by the test; finding 12 fixer's discretion (registry mutation stays if renderFrame cannot take the rig otherwise).
Fix wave (Opus, fresh): 2e7820798 → 923713125. Findings 1–11 fixed; 12 left (renderFrame reads VIEW_RIGS[state.viewRig]; an injection knob would be speculative). Deviations: surveyFade helper rejected by the purity ratchet → expression stays inlined; tests/visual/renderFrameSplitBaseline.test.ts was RED on CI at 2e7820798 (snapshot not re-blessed after pxPerRad joined renderer signatures) → re-blessed, order unchanged.
Ruling: finding 12 parked — the registry-mutation test stands; cost if wrong: the test breaks the day VIEW_RIGS is frozen, a local fix then.
CI GREEN on 923713125 (2026-09-22). PR body updated with eye-checks + reviews. Diff breakdown: code +1205/-741, tests +1042/-464, docs +418/-1, comment lines +514/-419. NEXT: user eye-check on a dev server → /feature-done → squash on the user's word.
User eye-check 2026-09-22: MW sprite streaks (far) + non-circular blobs (near). Controller diagnosed with headless screenshots (shot.mts in this workspace): (1) corners projected independently on a tilted quad; (2) star LOD cull per vertex on the corner's own apparent size → threshold sprites tore into a triangle parked at (2,2,2,1). Fixed inline (2 shaders), verified by screenshot at both poses. Main merged fdcedb75b. Fix commit pushed; CI watching.
User rulings 2026-09-22: renames Plans→FramePlannerResultStore, PlanStepSpec→PlannerStepSpec, PlanTarget→PlannerScopeTarget, PlanResult→PlannerResult, ContentPlanner→FrameContentPlanner (2d27e5dae); headers rewritten to say what/for (next commit). Ruling: FrameContentPlanner + store headers exceed the 5-line budget (10 and 8) because the user asked for self-explaining type docs — cost if wrong: two headers to trim.
Budget loosened to 10-line headers (cf85f47f3, user ruling). Factory renamed createPlans→createFramePlannerResultStore (f849ab059). CI watching.
RESUME (2026-09-22, HEAD f849ab059, pushed, main merged at fdcedb75b): all 7 tasks + reviews + MW sprite fix + planner renames + budget loosening landed on #805. WAITING on the user's 8-item eye-check (list in the PR body + memory). Then: /feature-done (plan → completed/, archive this ledger to docs/superpowers/plans/completed/2026-09-22-per-view-planning-prep.ledger.md, spec STAYS live for #800, skip deletion audit — prep PR), gh pr ready 805, squash via `gh api -X PUT repos/rulkens/skymap/pulls/805/merge -f merge_method=squash` ONLY on the user's word, then tell the dome session (memory project_wisdome_dome_fisheye) to merge main into #800. Dev server :5173 in this wt (bg shell of this session). shot.mts here = headless screenshot of a share URL (npx tsx shot.mts <url> <out.png>).
