# SDD ledger — plan: docs/superpowers/plans/2026-08-26-body-render-slabs.md

Spec: docs/superpowers/specs/2026-08-25-body-render-slabs.md (reachable, read).
Branch: globe-camera (on post-prep main). Perf baseline: perf-baseline-main.md beside this file.

## Pre-plan rulings (made at plan review, user notified 2026-08-26, no overrule received)

- Ruling: BodyState.radiusM DECLINED (spec §3 delta not built) — SceneBody arms already carry radiusM post-P4; adding it is mirror state — cost if wrong: one small additive field later.
- Ruling: §10 grep criterion narrowed to §5's body-slab-path file list (Task 13) — literal form unsatisfiable (10 world-mpc/CPU-LOD importers survive by design) — cost if wrong: widen the test's file list later.
- Ruling: camPosLocal fate = conditional deletion-audit in Task 13 — cost: none (reversible).
- Ruling: lonLatFocusPose stop-and-defer boundary confirmed (defer if it reaches spec-2 camera state) — cost: one deferred repoint.
- Ruling: perf baseline captured 2026-08-31 on main-identical tree (see perf-baseline-main.md) — satisfies "before Task 1".

## Pre-flight conflict scan (2026-08-31)

| pair / task | produces vs consumes | finding |
| --- | --- | --- |
| T1→T4 | BodyPoseProvider / `pose` input | match |
| T1→T8/9 | BodyRelativePose.eyeRelBodyM | match |
| T2→T4 | bodyDrawRadiusM(SceneBody) / rMaxM | match |
| T3→T4 | Slab near/far/distanceRangeM | match; T4 replaces NEAR0 range with §7.1 derivation as T3 says |
| T4→T5 | slabs.ts touched by both | sequential, no conflict; ChainRow.centrePx needs a projected centre (T5 wiring detail, vp in scope at deriveSlabs) |
| T4→T7 | foregroundChainOrder / foregroundChain param; body rows index from 2 | match; renderFrame.ts:98 call site consistent |
| T6→T7 | 'body' sentinel; timedSlotRowsOf matches slab≥2 without ctx | consistent — body rows always start at index 2 per T4 |
| T6→T9/10/11 | enabled(state,ctx,view) 3-arg | match across all six layers |
| T8→T9/10/11 | composeBodySlabMvp / bodySlabCamLocal signatures | match at every call site named |
| T7→T12 | per-row depthLoad:'clear' / per-row depth premise | match |
| T9/10/11→T13 | six composeBodyMvp body callers removed / T13 verifies gone | match |
| T12 | overlays sample foreground:0 colour while rendering | no read-write hazard: overlay layers render to swap (perf table confirms swap·NEAR0 grouping) |
| T7 self | test "emits today's exact list when chain=[NEAR0]" vs expansion emitting depthLoad:'clear' | CONFLICT — see R1 |
| all other tasks self-check | tests vs code vs files | agree |

- Ruling R1: Task 7's [NEAR0]-chain no-behaviour-change test may differ from today's literal list by exactly one thing: an explicit `depthLoad: 'clear'` on the foreground:0 chain step (semantically identical to the first-touch rule). Update the expected list accordingly; everything else byte-identical. — Why: the expansion uniformly stamps 'clear' per spec §7.3; carving an exception for a 1-length chain would be a special case. — Cost if wrong: one test literal reverted.

## Task log
- Task 1: implemented 29a0f4725 (DONE, 3/3 focused + full suite 7568 green, typecheck clean); reviewer dispatched (sonnet) over review-b381cdc6b..29a0f4725.diff
- Task 1: minor (deferred): test-file module header 14 lines (>10) in bodyRelativePose.test.ts
- Task 1: minor (deferred): local IDENTITY literal duplicates IDENTITY_MAT3 (spread would do) in bodyRelativePose.test.ts:235
- Task 1: minor (deferred): round-trip test = one it() looping 3 magnitudes on 'earth' vs brief's 'three cases, one test body each' — accepted interpretation
- Task 1: complete (commits b381cdc..29a0f47, review clean — spec ✅, approved, 3 minors deferred)
- Task 2: implementer IN FLIGHT (sonnet, BASE 29a0f4725, brief task-2-brief.md). On DONE: review-package 29a0f4725..HEAD → task reviewer (sonnet) → loop per skill. Task 3 next (brief not yet extracted).
- Board artifact https://claude.ai/code/artifact/2703eadb-3d05-4120-9dce-0c14390c0fd2 mirrors this ledger; update on each completion (source HTML in session scratchpad, purgeable — recreate via url= param if gone).
- Dev server: orphan on 5199 KILLED at user request 2026-08-31; fresh vite for THIS worktree now on port 5173 (shell bl10ufadr — main's server was down, so the worktree took the default port). Task 9-12 visual checks + Task 14 perf runs use --url http://localhost:5173 (re-verify the Local: line before any perf run — port may shuffle if main's server returns).
- Task 9 visual check HANDED TO USER 2026-08-31 (URL 5173): whole globe + continent longitudes (confirmatory), zoom-floor descent (no black nadir, tile detail, ocean glint, no UV shimmer), Earth pick still works; atmosphere/clouds EXPECTED WRONG until Task 10. Attestation NOT yet received — record it here when it arrives; collect for Task 14 gate.
- Task 2: implemented 56e8afb11 (DONE, 3/3 new, suite 7574 green, typecheck clean; implementer note: cloud shell gated body.id==='earth', no registry row exists); reviewer dispatched (sonnet) over review-29a0f4725..56e8afb11.diff
- Task 2: minor (deferred): cloud-shell radiusRatio multiply now has second call site (bodyDrawRadiusM + cloudShellLayer:153) — watch if a later task touches the ratio again
- Task 2: complete (commits 29a0f47..56e8afb, review clean — spec ✅, approved, 1 minor deferred)
- Task 3: implemented 2fcecacb2 (DONE, suite 7575 green, typecheck clean, net -110 lines; new shared fixtures tests/fixtures/makeSlab.ts + makeCosmoSlab.ts; implementer fixed brief's slabs.ts:153 alias trap + an `as unknown as SlabView` fixture hole in near0SelectionRingLayer.test.ts; refactor CLI can't rename type-literal props — one-off ts-morph script used, CLI gap noted); reviewer dispatched (sonnet) over review-56e8afb11..2fcecacb2.diff.
- Task 3: minor (deferred): near0SelectionRingLayer.test.ts:107-121 `farClippingView` hand-built Slab lacks distanceRangeM behind `as unknown as SlabView` cast — inert until something reads distanceRangeM; switch to makeSlab() when Task 9 (or any task) touches that file
- Task 3: complete (commits 56e8afb..2fcecac, review clean — spec ✅, approved, 1 minor deferred)
- Task 4: implementer IN FLIGHT (sonnet, BASE 2fcecacb2, brief task-4-brief.md; dispatch notes: brief line numbers may have drifted post-Task-3 — code reality wins for mechanics; starSphereRangeM's Mpc→m distance scalar = spec §7.1-sanctioned exception to the one-seam rule, done once inside that util). On DONE: review-package 2fcecacb2..HEAD → reviewer. Board republish still pending (will show 3/14).
- Board republished 3/14, Task 4 in flight.
- Task 4: implemented 10698b218 (DONE_WITH_CONCERNS, suite 7590 green, typecheck clean). Implementer concerns: (a) bodyStates+viewportPx unused inside deriveSlabs (aspect from cam.aspect); (b) field-star sphere excluded from NEAR0 interval per brief permission → Task 14 finding recorded; (c) MIN_NEAR_M imported from foregroundFrustum.ts, body-row far=Infinity; (d) 3 test fixtures gained bodies/settings fields. Reviewer dispatched (sonnet) over review-2fcecacb2..10698b218.diff with concerns (a)-(d) for explicit adjudication. On clean review: ledger complete → task-brief 5 → dispatch Task 5 implementer (sonnet, screen-overlap⇒disjoint invariant).
- Task 4: minor (deferred): bodyStates+viewportPx unused in deriveSlabs input (spec's verbatim signature; reviewer confirmed unused through Task 7) — viewportPx may gain a consumer in Task 5's centrePx wiring; if still dead at Task 13, trim there
- Task 4: minor (deferred): starSphereRangeM.ts docblock 13 lines / ratio ~0.59 (over budget, self-justifying) — trim on next touch
- Task 4: minor (deferred): timedSlotsGroupKeys.test.ts:34 comment references retired SLAB_NAME — fix when Task 6/7 touches that area
- Task 4: complete (commits 2fcecac..10698b2, review clean — spec ✅, approved, concerns (a)-(d) all adjudicated OK, 3 minors deferred; field-star-sphere gap = standing Task 14 finding)
- Task 5: implementer IN FLIGHT (sonnet, BASE 10698b218, brief task-5-brief.md; dispatch notes: viewportPx may be centrePx's natural consumer; ChainRow type placement per one-type-per-file). On DONE: review-package 10698b218..HEAD → reviewer. Then task-brief 6 (slab:'body' sentinel + executor).
- Task 5: implemented 92e651cce (DONE, suite 7596 green, typecheck clean; viewportPx now consumed for centrePx/radiusPx — resolves Task 4 unused-input minor for that field). Implementer judgment call: radiusPx feeds bodyApparentDiameterPx a synthetic radial Mpc position (helper reads only hypot distance) — reviewer dispatched (sonnet) over review-10698b218..92e651cce.diff to adjudicate incl. possible M_TO_MPC one-seam violation in slabs.ts. On clean review: ledger complete → task-brief 6 → dispatch Task 6 implementer (sonnet).
- Task 5: minor (deferred): no test pins the screen-circle overlap boundary (inclusive <=) — fixtures sit well clear of it
- Task 5: complete (commits 10698b2..92e651c, review clean — spec ✅, approved; synthetic-position call ruled SOUND: bodyApparentDiameterPx reads only hypot distance, slabs.ts never inside the one-seam perimeter (pre-existing MPC_TO_M at COSMO row), trick documented in bodySlabRow docblock)
- Task 6: implementer IN FLIGHT (sonnet, BASE 92e651cce, brief task-6-brief.md; dispatch notes: use makeSlab fixture for body-m rows; mass enabled-widening is mechanical, no gating-logic changes). On DONE: review-package 92e651cce..HEAD → reviewer. Then task-brief 7 (frameProgram foreground chain, Ruling R1 applies).
- Task 6: implemented 63fa64d6f (DONE, suite 7600 green, typecheck clean). Scope expanded of type necessity beyond brief file list: pickProgram.ts (pickablesBySlab resolves SlabView pre-gate, EXCLUDES 'body'-slab candidates — no body drawPick yet, Tasks 9-11 revisit) + runFrame.ts (earth planner enabled call gets hoisted slabViewOf) + 26 test files. Implementer minor: duplicate slabViewOf resolve in pick path (hover cadence, not hot). Reviewer dispatched (sonnet) over review-92e651cce..63fa64d6f.diff — hardest scrutiny on the two production expansions (pick exclusion behaviour-safety, runFrame slab-index fidelity). On clean review: ledger complete → task-brief 7 → dispatch Task 7 implementer (sonnet, carry Ruling R1 in dispatch).
- Task 6: minor (deferred): duplicate slabViewOf resolve on pick path (pickablesBySlab + recordSlabPass) — Task 7 implementer told to fold it in if natural, else stays deferred
- Task 6: complete (commits 92e651c..63fa64d, review clean — spec ✅, approved; expansions (a) pickProgram body-exclusion and (b) runFrame NEAR0-view hoist both verified behaviour-preserving)
- Task 7: implementer IN FLIGHT (sonnet, BASE 63fa64d6f, brief task-7-brief.md; dispatch carries Ruling R1 verbatim + Task 6's pick restructure context + SLAB_NAME stale-comment fix + duplicate-resolve fold-if-natural). On DONE: review-package 63fa64d6f..HEAD → reviewer. Then task-brief 8 (transform primitives).
- Board republished 6/14, Task 7 in flight.
- Task 7: implemented f0bc9a777 (DONE, suite 7605 green, typecheck + lint clean, no implementer concerns). Reviewer dispatched (sonnet) over review-63fa64d6f..f0bc9a777.diff — R1 conformance check + empty-chain composite stale-read question flagged as potential blocker. On clean review: ledger complete → task-brief 8 → dispatch Task 8 implementer (sonnet, composeBodySlabMvp/bodySlabCamLocal).
- Task 7: complete (commits 63fa64d..f0bc9a7, review clean — spec ✅ incl. R1 exact conformance, approved, 0 minors; empty-chain stale-read ruled NOT REAL — renderedTargets fresh per frame + composite touched-guard; Task 6's duplicate slabViewOf resolve FIXED in this diff, deferred-minor closed)
- Task 8: implementer IN FLIGHT (sonnet, BASE f0bc9a777, brief task-8-brief.md; dispatch notes: mirror composeBodyMvp's wgpu-matrix idioms, non-symmetric slabVp in tests, camPosLocal oblateness convention verbatim). On DONE: review-package f0bc9a777..HEAD → reviewer. Then task-brief 9 (Earth layer migration — FIRST visual-check task: present dev-server check to USER, never self-attest; server port 5199).
- Task 8: implemented f1af82ee5 (DONE_WITH_CONCERNS, suite 7616 green, typecheck clean, mutation-checked). Implementer found SPEC NUMBER ERROR: §10 denormal criterion says Earth radius² ≈4.3e-42 Mpc² (denormal) — true value ≈4.26e-32 (NOT denormal); actual denormal-crosser = ~3.3km camera-delta square ≈1.14e-38 (matches shipped black-nadir landmine). Controller verified arithmetic independently: implementer correct. Test rewritten with correct numbers preserving regression intent. Reviewer dispatched (sonnet) over review-f0bc9a777..f1af82ee5.diff to re-verify arithmetic + rule whether rewritten test can still fail on real bugs (constant-restatement ban). Ruling pending review: spec §10 figure to be corrected in Task 13's docs sweep (note here so it isn't lost). On clean review: ledger complete → task-brief 9.
- Task 8: review Needs-fixes (1 blocking, 2 minor). Blocking: denormal test never calls composeBodySlabMvp — constant restatement, can't fail on any mutation (defect inherited from the plan's own test description). Ruling: testing convention wins over plan text — DELETE the test; the unit-scale test already pins the Mpc-leak path; spec §10 figure correction stays queued for Task 13 docs sweep. Minor accepted as-is: bodySlabCamLocal.ts ratio (self-justified). Fix round 1 dispatched (resumed implementer): delete test + trim 16-line test header to ≤10. On fix DONE: scoped re-review not needed if diff is exactly the deletion+trim (controller verifies directly — deletion diffs are self-evident); then ledger complete → task-brief 9.
- Task 8: fix round 1 landed 491e1124e (denormal test deleted, header 16→9; controller verified diff = exactly deletion+trim, 4/4 targeted tests green)
- Task 8: complete (commits f0bc9a7..491e112, spec ✅, approved after 1 fix round; spec §10 figure correction queued for Task 13 docs sweep)
- Task 9: implementer IN FLIGHT (sonnet, BASE 491e1124e, brief task-9-brief.md; dispatch carries WESL skill + RENDERER.md mandatory pre-read, Task 6/7 pick + runFrame context, 176-byte uniform parity, visual check reserved for USER). On DONE: review-package 491e1124e..HEAD → reviewer (this diff touches WGSL — reviewer must check byte parity + landmines). After review: present Task 9 dev-server visual check to user (whole globe + zoom floor; atmosphere/clouds expected wrong until Task 10); do NOT gate Task 10 dispatch on user attestation — spec says atmosphere lands next; visual attestations collect at Task 14 gate.
- Task 9: implemented ad1c6cc8e (DONE_WITH_CONCERNS, suite 7622 green, typecheck+lint clean; controller re-verified typecheck:fast clean — editor diagnostic was stale). Two necessary scope expansions: (A) drawPick rewritten onto body primitives (old drawFlooredSpherePick composed Mpc — would misplace pick under Task 7's body routing); (B) tile origins now written with IDENTITY orientation instead of earthState.orientation (implementer's frame-consistency derivation; HIGHEST RISK, no dedicated test — continents-at-wrong-longitude is the failure mode). Reviewer dispatched on OPUS (risk-scaled) over review-491e1124e..ad1c6cc8e.diff — must rule (B) code-conclusive or defer-to-visual. On review: if (B) unresolved, visual check instructions to user MUST name continent alignment explicitly.
- Task 9: review Needs-fixes (2 blocking, 10 minor — minors deferred, listed in task-9-review.md). Expansion rulings: (A) drawPick JUSTIFIED (real 3.09e22× mismatch); (B) identity tile origin CORRECT + CODE-CONCLUSIVE (basisM body-fixed; orientation would double-apply spin; visual check confirmatory only). B1 = both cutSurfaceTiles metre tests vacuous (mutation-verified; fix = requests-equality + horizonFixture pattern); B2 = camBasisWorldOf re-derives frameContext camera basis (fix = ctx.bodyPose field reusing deriveSlabs' provider closure, delete camBasisWorldOf — must land BEFORE Task 10 copies the pattern). Fix round 1 dispatched (resumed implementer, both findings + mutation re-verification required). On fix DONE: scoped re-review (sonnet) over fix diff — B1 needs mutation claim checked, B2 needs closure-reuse (not second closure) checked.
- Task 9: fix round 1 landed c396d0af0 (16 files +154/-63; B1 = requests-equality + horizon binary-search tests, implementer hand-mutated radiusM param out and saw both fail [87 vs 89 tiles; false→true]; B2 = ctx.bodyPose forwards deriveSlabs' closure, camBasisWorldOf deleted, 12 fixture updates; suite 7623 green, typecheck+lint+prettier clean). Scoped re-review IN FLIGHT (sonnet, package review-ad1c6cc8e..c396d0af0.diff → task-9-re-review.md; verifies B1 mutation claim + B2 same-closure-object, checks no unrelated riders). On clean re-review: ledger Task 9 complete → republish board 9/14 → task-brief 10 → dispatch Task 10 implementer (sonnet; atmosphere+cloud shells onto body rows; carry ctx.bodyPose pattern + B2 ruling: layers consume ctx.bodyPose, never re-derive). Worktree public/data now symlinked to main's (user visual check at 5173 needs hard refresh).
- Task 9: complete (commits 491e112..c396d0a, spec ✅, approved after 1 fix round; re-review INDEPENDENTLY mutation-verified B1 [reverted radiusM param, both tests failed, 22 others green, file restored byte-identical] and confirmed B2 single-closure reuse [frameContext.ts:211-222 built once, used twice]; 10 minors deferred in task-9-review.md; user visual attestation still pending — collect at Task 14 gate)
- Task 10: implementer IN FLIGHT (sonnet, BASE c396d0af0, brief task-10-brief.md; dispatch carries Task 8 primitive signatures + ctx.bodyPose seam ruling [identity orientation, never re-derive] + earthLayer.ts as worked example + depth-test-not-write contract for Task 12). On DONE: review-package c396d0af0..HEAD → reviewer (sonnet unless WESL structs churned — then risk-scale). After review: present Task 10 visual check to user (whole-globe limb/terminator/atmosphere gradient/cloud shell vs main side-by-side); do not gate Task 11 dispatch on attestation. Then task-brief 11 (planets/rings).
- Task 10: implemented 49ff0d15d (DONE, suite 7634 green, typecheck+lint clean). Two judgment calls in report: atmosphereDrawList/encodeAtmosphereSkyView left untouched (equivalent-not-shared altitude math); per-row drawList recompute mirrors cloudShellDraw pattern. Reviewer dispatched (sonnet) over review-c396d0af0..49ff0d15d.diff — lens: bodyPose seam (no local re-derivation, Task 9 B2 shape), depth-test-not-write intact, vacuous-test suspicion, the two judgment calls. USER DIRECTIVE (mid-session): continue with Tasks 10 and 11 — on clean Task 10 review dispatch Task 11 implementer immediately (brief task-11-brief.md extracted).
- SIDE INVESTIGATION (user bug report "atlas doesn't update"): NOT a code bug. Root cause = dev server on 5173 predates the public/data symlink; Vite's public index never picked up nested symlink dirs (vdemo/surface/* → v3 and → data/raw), tile URLs fell through to index.html (200 text/html). Verified: fresh server on 5175 serves vdemo z3 + z14 tiles as image/webp. User told: test at http://localhost:5175 (or ask me to restart 5173). Per user ruling both old servers killed; ONE fresh server on 5173 (shell beqqjzof4), z3+z14 tiles verified image/webp. USER CONFIRMED 2026-08-31: "tiles serve neatly now" — atlas refines on the Task 9 path (partial visual evidence; full Task 9 checklist items — black nadir, ocean glint, UV shimmer, pick — still uncollected, roll into Task 14 gate). Manifest prefix is earth-tiles/vdemo (GD demo tree, dev-local; never rides R2).
- Task 10: minor (deferred) ×4, listed in task-10-review.md: stale composeBodyMvp docstrings in packAtmosphereUniforms/packCloudShellUniforms; cloudShellLayer header 81 lines (over budget); atmosphereShellLayer header ~60 over (pre-existing); atmosphereDrawList recomputed in enabled+draw per row (named, cheap)
- Task 10: complete (commits c396d0a..49ff0d1, review clean — spec ✅, approved, 0 blocking; reviewer independently re-derived the sky-view bake↔draw MPC_TO_M cancellation, and ruled surviving M_TO_MPC in cull/fade math conformant with the Task 9 precedent [finding j]; user visual check pending, collect at Task 14 gate)
- Task 11: implementer IN FLIGHT (sonnet, BASE 49ff0d15d, brief task-11-brief.md; dispatch carries bodyPose seam + identity-orientation ruling + partition-adapter keep + bodyGlintsLayer untouchable [spec §11-O2] + earthLayer/atmosphereShellLayer as worked examples). On DONE: review-package 49ff0d15d..HEAD → reviewer (sonnet; lens = double-draw partition tests real, ring km-native ratios untouched, glints layer untouched). Then task-brief 12 (caption occlusion → alpha coverage).
- VISUAL FINDING (user, 2026-08-31): background/cosmo labels visible in a small window ~78–99 km Earth altitude (distanceMpc 2.090e-16..2.097e-16), disappearing one by one on descent. Investigator IN FLIGHT (sonnet, read-only) → label-window-investigation.md; must rule regression-vs-preexisting (suspect: caption occlusion path vs this branch's depth/slab reshape — note Task 12 rewrites caption occlusion to alpha coverage, may be the fix or may mask it). On result: report to user; if regression, fold fix into Task 12 or a fix round per severity; if pre-existing, log for Task 14 gate context + backlog per user ruling.
- Label-window RESOLVED as REGRESSION (investigation in label-window-investigation.md): bodySlabRow near = max(dM − bodyDrawRadiusM, MIN_NEAR_M) snaps to 1e-6 m below the 100 km atmosphere top → reversed-Z collapse in body slab → coveredByScene reads ≤0 as uncovered → cosmo labels leak 78–99 km; one-by-one = per-pixel precision recovery. main unaffected (NEAR0 altitude-ratio near). User acked plan. Ruling: near-plane fix RIDES the Task 12 dispatch as its own FIRST commit (near keys on altitude above surface once inside the outermost shell), then Task 12 proper (alpha-coverage rewrite also de-fangs the label mechanism); reviewer verifies both; user re-checks the 78–99 km window at Task 12 visual check.
- Task 11: implemented ff55c06a8 (DONE_WITH_CONCERNS, suite 7643 green, typecheck+lint clean). Two informational concerns: planetRenderer stale N-body-batching header (count=1 per row now; out of file scope) + ringsLayer sub-pixel cull not redundant (visibleSlabBodies gates on body radius, not ring footprint — narrow zoom band could drop a resolvable ring; pre-existing, documented in header). Reviewer dispatched (sonnet) over review-49ff0d15d..ff55c06a8.diff — hardest lens on RING ORIENTATION (what fixed-axes frame carries vs ring-plane tilt), bodyGlintsLayer untouched, three mandated tests non-vacuous; reviewer adjudicates both concerns. On clean review: ledger complete → republish board 11/14 → task-brief 12 → dispatch Task 12 implementer (sonnet) WITH the near-plane fix as first commit (see label-window ruling above).
- Task 11: minor (deferred) ×2: f64-seam/bodyPose explanation echoed in three module headers (point at composeBodySlabMvp instead); sceneBodyPartition O(bodies) scan per row per call-site (cheap at 8 bodies; adopt earthLayer's per-ctx memo if roster grows)
- Task 11: complete (commits 49ff0d1..ff55c06, review clean — spec ✅, approved, 0 blocking; reviewer independently re-derived ring orientation equivalence, no double-applied spin; both implementer concerns adjudicated out-of-scope BUT ring sub-pixel/visibleSlabBodies gap is a NEW EXPOSURE (pre-Task-11 ringsLayer wasn't coupled to visibleSlabBodies) — add Saturn narrow-zoom-band ring check to Task 14 gate context; user visual check pending, collect at Task 14 gate)
- Task 12: implementer IN FLIGHT (sonnet, BASE ff55c06a8, brief task-12-brief.md; TWO commits mandated: (1) near-plane fix — near keys on surface altitude inside outermost shell, foregroundFrustum ratio convention, regression test at 78 km must fail on formula revert; (2) Task 12 proper — alpha-coverage occlusion, move-files for occlusionDepthGroup→occlusionCoverageGroup, alpha-write audit per layer). On DONE: review-package ff55c06a8..HEAD → reviewer (sonnet; lens = near-plane fix correctness + invariants, coverage threshold 0.5 reasoning, bind-group parity, alpha-write audit verification). After review: present combined visual check to user (78–99 km label window gone + limb caption clipped by globe not atmosphere + cosmo label behind planet covered). Then task-brief 13 (delete incumbent path + docs sweep incl. spec §10 figure correction).
- Task 12: implemented — commit 1 15d967998 (near-plane surface-altitude fix, mutation check confirmed: test fails on formula revert) + commit 2 4d8ffa43e (alpha-coverage occlusion). DONE, suite 7653 green (1159 files), typecheck+lint+build clean. Implementer concerns: (1) fs/fsCoverage WESL entry points + 'compare'|'coverage' TS mode kept as naming-only distinction (outside brief scope, follow-up candidate); (2) alpha-write audit found pre-existing Task 9 interaction — earthRenderer base-globe descent-fade alpha can dip <0.5 during tile transition for patches with no resident tile → caption could flicker through for a frame; narrow, self-healing, left for controller adjudication. Reviewer dispatched (sonnet) over review-ff55c06a8..4d8ffa43e.diff → task-12-review.md; lens = near-plane formula continuity/boundary + regression-test genuineness, 0.5-threshold shell reasoning, bind-group parity, alpha audit spot-checks, both concerns adjudicated. Editor flagged foregroundLabelsLayer.ts:25 unused 'view' — reviewer verifies vs tsc (stale-diagnostic landmine suspected). On clean review: ledger complete → republish board 12/14 → present COMBINED visual check to user (78–99 km label window + limb caption vs atmosphere + cosmo label behind planet + deferred Task 10/11 checks) → task-brief 13.
- Task 12: minor (deferred) ×3, listed in task-12-review.md: (1) near-plane formula can exceed pre-fix value ~10× in a ~10 m band at the shell boundary (single-digit metres on a 6371 km body — negligible; report's "unchanged outside shell" claim slightly overstated); (2) cloud alpha (c.a × cloudOpacity) architecturally unbounded to ~1.0 and rows composite colour with load-not-clear, so cross-row accumulation could pass the 0.5 coverage threshold at a dense-cloud grazing limb — NO test covers reachable range; VISUAL CHECK MUST include a dense-cloud/grazing-limb view; (3) the two adjudicated-deferred judgment calls (compare/coverage naming split; earthRenderer descent-fade alpha dip) live only in prose — capture in backlog at Task 13/14 if still open.
- Task 12: complete (commits ff55c06a8..4d8ffa43e, review clean — both commits spec ✅ + quality approved, 0 blocking; near-plane regression test genuine, bind-group parity verified, alpha audit spot-checked; foregroundLabelsLayer unused-'view' editor diagnostic not substantiated by reviewer tsc runs — stale). USER VISUAL CHECK NOW DUE (combined): (a) 78–99 km label window gone (descend over Earth ~99→78 km, no cosmo labels popping); (b) caption over Earth's limb clipped by opaque globe, NOT by atmosphere glow; (c) cosmo label behind a planet still covered; (d) dense-cloud grazing-limb view — captions must NOT clip against thick cloud (minor 2); (e) deferred Task 10 check: whole-globe limb/terminator/atmosphere/clouds vs main; (f) deferred Task 11 check: Mars/Moon occultation, Saturn rings vs own globe, 3 px glint↔sphere boundary. Server http://localhost:5173 (shell beqqjzof4). After attestation (or user defers to Task 14): task-brief 13 (delete incumbent path, one-seam import-graph test, docs sweep incl. spec §10 denormal-figure correction).
- Board republished 12/14 (label task-12-done). Ruling: Task 13 dispatched WITHOUT gating on the visual attestation — plan precedent (Tasks 9-11 never gated dispatch on user eyes), deletion is git-reversible, attestations collect at Task 14 gate; visual check presented to user in parallel. Cost if wrong: a regression found visually needs the old path checked out from history rather than live toggle.
- Task 13: implementer IN FLIGHT (sonnet, BASE 4d8ffa43e, brief task-13-brief.md; dispatch carries: encodeAtmosphereSkyView left-untouched ruling [don't force onto bodySlabCamLocal], camPosLocal delete-only-if-smaller, lonLatFocusPose stop-if-spec-2-territory, Task 12 alpha-coverage occlusion for RENDERER.md bullet, QUEUED spec §10 denormal-figure correction [4.3e-42 → 4.26e-32, real crosser 1.14e-38], M_TO_MPC outside-path precedent). On DONE: review-package 4d8ffa43e..HEAD → reviewer (sonnet; lens = import-graph test non-vacuous + file list fidelity, deletion-audit honesty, spec §10 arithmetic re-verified, backlog sweep correctness, lonLatFocusPose spec-2 boundary). Then task-brief 14 (verification gate: perf vs perf-baseline-main.md at THIS worktree's server URL, halt-on-neutral, land/park = USER).
- VISUAL ATTESTATION (user, 2026-08-31): label fix CONFIRMED — 78–99 km window checked, item (a) of the Task 12 combined check done. Items (b)-(f) still open.
- TWO NEW USER BUG REPORTS (2026-08-31, both suspected branch regressions; report + camera states in saturn-vanish-bug-report.md): (1) SATURN VANISH — focused on Titan, Saturn globe appears/disappears with yaw/pitch at constant distance; its atmosphere flips at slightly different poses (globe and atmosphere culled independently). Investigator IN FLIGHT (sonnet, read-only) → saturn-vanish-investigation.md; must reproduce numerically with the real gate code, rule regression-vs-preexisting, name deciding file:line + fix shape. (2) PICK DEBUG — stars render over planets in picking debug layer, bodies hard to pick; suspect per-row depth clears broke star-vs-body pick depth sharing (Task 6 pick exclusion / Task 9 drawPick rewrite). Investigator IN FLIGHT (sonnet, read-only) → pick-stars-investigation.md; must rule regression-vs-preexisting-vs-debug-only. HANDLING PLAN on both results: report to user; regressions get fix commits — fold into a Task 13 fix round or dedicated fix dispatch per severity/timing (Task 13 implementer currently in flight; do NOT let fixes collide with its tree — wait for its DONE first). Both go on the Task 14 gate list regardless.
- PICK BUG root-caused (pick-stars-investigation.md): REGRESSION from Tasks 9-11, real pick-resolution bug not debug-only. pickablesBySlab (pickProgram.ts:242-244) sorts slabs by raw index ascending and frontmostPick takes first non-zero hit — NEAR0 (index 0, stars) always beats body rows (2+) regardless of depth. main was safe because bodies lived IN NEAR0 sharing one depth buffer. Fix shape: order {NEAR0 ∪ body rows} by distanceRangeM[0] near→far (same key foregroundChainOrder at slabs.ts:358-372 already uses, reversed), COSMO fixed tail. One file, self-contained. Ruling: dedicated fix commit AFTER Task 13 lands, before Task 14 gate; regression test = pickablesBySlab ordering fixture with a body row nearer than a star hit.
- Task 13: implemented 486b29669 (DONE, suite 7707 green across 1160 files, typecheck clean). Key outcomes: composeBodyMvp + camPosLocal narrowed not deleted (both have legit survivors; deletion-audit in report); lonLatFocusPose STOPPED per standing ruling (reaches CameraPose/OrbitCamera = spec-2 territory) → recorded as Task 14 finding; oneMpcSeam.test.ts = ts-morph import-graph test, 54 assertions, with implementer DEVIATION: 3-file allow-list (earthLayer/cloudShellLayer/ringsLayer, cull/fade bridging) vs brief's "bodyRelativePose is the one permitted importer"; spec §10 arithmetic corrected; RENDERER.md 2 bullets; BACKLOG sweep = nothing consumed. Implementer concerns: allow-list is per-file (new Mpc uses in those 3 files uncaught); one-transform-not-three only 2/3 achieved pending spec 2. Reviewer dispatched (sonnet) over review-4d8ffa43e..486b29669.diff → task-13-review.md; lens = test non-vacuous + file-list existence, allow-list adjudication vs M_TO_MPC precedent, spec §10 numbers re-verified, lonLatFocusPose stop justification. On clean review: ledger complete → PICK-BUG FIX dispatch (dedicated commit per ruling above, pickablesBySlab distance ordering + regression test) → then Saturn fix per investigation verdict → then task-brief 14.
- MAIN MERGED IN per user directive (2026-08-31): origin/main (5 commits, incl. #639 label pick hit-targets + v0.5.0 + README #633) → merge commit 2d7359039, pushed to PR #634. One conflict in labelRenderer.ts imports (our Task 12 occlusionCoverageGroup rename vs main's new labelSizingDefaults import) — resolved keep-both. Full suite 7734 green post-merge, typecheck:fast clean. NOTE for pick-bug fix: #639 touched pickProgram.ts (label quad hit-targets) — the pickablesBySlab ordering fix must be written against the MERGED tree; new BASE for that dispatch = 2d7359039. Task 13 reviewer was dispatched over pre-merge package 4d8ffa43e..486b29669 — still valid (merge adds nothing to Task 13's diff).
- Task 13: minor (deferred) ×3, listed in task-13-review.md: (1) oneMpcSeam.test localName computed but filter hardcodes 'SCALE_UNITS' — aliased import would escape (inert today, sole export); (2) report's backlog-sweep undercount — 2 docs/backlog files (star-renderer-analytic 2026-07-29, sun-bloom-eclipse 2026-08-21) now carry stale camPosLocal call-site claims, freshen owed; (3) oneMpcSeam.test header ~20 lines vs ≤10 budget.
- Task 13: complete (commits 4d8ffa43e..486b29669, review clean — spec ✅ APPROVED, quality approved 0 blocking; ALLOW-LIST ADJUDICATED: 3-file cull/fade allow-list conformant with Tasks 9/10 precedent, per-file granularity acceptable, flagged for USER RULING in PR body per brief; lonLatFocusPose stop verified justified → Task 14 finding). NEXT: pick-bug fix dispatch (BASE 2d7359039, merged tree).
- PICK FIX: implementer IN FLIGHT (sonnet, BASE 2d7359039 merged tree; TDD mandated — failing ordering-fixture test first; fix = pickablesBySlab orders NEAR0∪body rows by distanceRangeM[0] near→far, COSMO tail, reuse foregroundChainOrder key; must not disturb #639 label hit-targets; full suite gate). Report → pick-fix-report.md. On DONE: review-package 2d7359039..HEAD → reviewer (sonnet; lens = red→green evidence genuine, comparator shared-not-duplicated judgment, #639 label-pick interaction, COSMO tail). Saturn investigation still IN FLIGHT — its fix (if regression) dispatches after pick fix lands to avoid tree collision. Then task-brief 14.
- SATURN VANISH phase 1 result: REGRESSION CONFIRMED (all deciding code new on this branch: 2fcecacb2/10698b218/92e651cce/63fa64d6f/ff55c06a8) but mechanism NOT pinned. Phase 1 proved: all layer gates are apparent-diameter (view-independent) tests — can't explain yaw/pitch flip; Saturn's projected centre moves inside-frame → ≤4% past edge between states while disc is 92–223 px, so plain rasterization can't fully vanish it; centrePx is DEV-diagnostic only. Phase 2 IN FLIGHT (OPUS, read-only, avoids pickProgram.ts): hypotheses H1 = impostor/quad vertex path anchored at projected centre (must also explain atmosphere flipping at different pose via different radius), H2 = row dropped after admission (chain assembly/renderedTargets/Task-5 disjoint invariant), H3 = near/far or f32 narrowing on Saturn's row (rings inflate rMaxM). Must produce decisive numeric flip between states A/B or name the ONE dev-server instrumentation that settles it. Appends '## Phase 2' to saturn-vanish-investigation.md.
- PICK FIX: implemented bc6dd587b (DONE, red→green on 2 regression tests [body row beats NEAR0 and COSMO], suite 7736 green, typecheck clean, no concerns). Reviewer dispatched (sonnet) over review-2d7359039..bc6dd587b.diff → pick-fix-review.md; sharpest lens = can the fix INVERT the bug (planets always beating genuinely nearer stars — what is NEAR0's distanceRangeM[0]?); also #639 label hit-target interaction, comparator duplication, COSMO tail. On clean review: ledger complete → push → then Saturn fix (pending phase-2 verdict) → task-brief 14.
- SATURN: USER EVIDENCE (mid-turn): back parts of the globe disappear FIRST when rotating — progressive far-side slicing, then full vanish. Weakens H1 (quad = all-or-nothing), supports H3 plane-conditioning. Forwarded to phase-2 agent with the radial-vs-view-axis analysis: row near/far built from radial dM±rMaxM but clip depth is along the view axis; off-axis θ slides Saturn's view-z span to dM·cosθ±r, so a radially-derived plane can sweep through the globe as yaw/pitch grows θ. Phase-2 asked for the numeric flip boundary at both camera states + the atmosphere's different flip pose, and to check reversed-Z plane mapping / wrong-row near-far / composeBodySlabMvp off-axis behaviour before concluding.
- PICK FIX: review PASS/PASS, 0 blocking, 4 minor (pick-fix-review.md). Ruling: minor 1 (NEAR0 distanceRangeM degrades to [0,0] with no resolved star sphere → sorts nearest again, reproduces the bug when Sun toggled off) is the same bug class — FIX ROUND 1 dispatched (resumed implementer): degenerate NEAR0 range sorts unknown-far (+Infinity key), one regression test, second commit. Minors 2-4 deferred (report test-count 12→8 typo; comment cites wrong doc for COSMO detail; pickProgram.ts pre-existing 69% comment ratio). Reviewer independently re-verified red→green through the pre-fix sort. On fix DONE: controller verifies diff directly (small, self-evident) — no scoped re-review needed unless it exceeds the mandate; then push both commits → Saturn fix per phase-2 → task-brief 14.
- SATURN ROOT CAUSE CONFIRMED (phase 2, decisive — '## Phase 2' in saturn-vanish-investigation.md): H1 killed (real uvSphereMesh × PROXY_SCALE, no centre-anchored quad), H2 killed (chain uncapped, gates view-independent), H3 CONFIRMED via the NEAR plane only (far is Infinity/reversed-Z, f32-insensitive). Mechanism: bodySlabRow builds near from RADIAL dM (slabs.ts:165 hypot → :175 near = max(dM − rMaxM, …)) but clip depth is along the view axis; off-axis θ spends dM(1−cosθ) out of the zero-margin budget rMaxM − r_proxy, then the near plane sweeps through the body far-limb-first. Numeric flip at the user's states: A θ=20.55° → 1225/1225 verts pass (z/w max 0.999); B θ=33.80° → 0/1225 (min 1.004). Predicted onsets: globe 25.00°, atmosphere 21.08° (cullMode none, no proxy scale) — one mechanism explains BOTH symptoms incl. the atmosphere's earlier flip. NOT Saturn-specific: bodies without rings have NEGATIVE margin (rMaxM = radiusM < 1.05·radiusM proxy). FIX SHAPE (per phase 2): near from view-axis depth (project eyeRelBodyM onto basisM[6..8], already at slabs.ts:178) minus margin covering PROXY_SCALE×radiusM + epsilon; distanceRangeM STAYS radial (painter sort is radial — reusing one dM for both is what braided them). Orientation variable settled: default 'ecliptic', θ basis-invariant. DISPATCH PLAN: Saturn fix implementer (sonnet) AFTER pick fix-round lands (implementers never parallel); TDD — regression test at the user's state B (body row near must not exceed view-axis front of the sphere; assert θ=33.8° pose passes), plus interplay check with the Task 12 near-plane fix (surface-altitude term must survive). NOTE: near-plane fix commit 1 of Task 12 also reads these lines — fix must compose with (dM − radiusM)·NEAR_RATIO term; PROXY_SCALE source of truth in shaders (bodies/planet/vertex.wesl:45, texturedBody:50) — mirror constant carefully.
- PICK FIX: fix round 1 landed 42cc9af09 (degenerate NEAR0 [0,0] → [Inf,Inf] remap before foregroundChainOrder, 1 red→green regression test, suite 7737 green; controller verified diff = exactly the mandate). Both pick commits PUSHED (bc6dd587b + 42cc9af09 → PR #634). PICK BUG CLOSED — user should re-check picking at Task 14 gate.
- SATURN FIX: implementer IN FLIGHT (sonnet, BASE 42cc9af09; TDD, near from view-axis depth [project eyeRelBodyM on forward] with margin max(PROXY_SCALE·radiusM, rMaxM)+eps; distanceRangeM stays RADIAL; Task 12 altitude term survives + 78 km test stays green; PROXY_SCALE mirroring decision recorded; no push). Report → saturn-fix-report.md. On DONE: review-package 42cc9af09..HEAD → reviewer (sonnet; lens = margin derivation correct at pose B numbers, ringless negative-margin case, radial/view-axis split respected, WGSL parity constant, Task 12 composition). On clean review: push → republish board → task-brief 14 (gate: perf halt-on-neutral + visual attestations incl. Saturn re-check, pick re-check, label window already attested).
- SATURN FIX: implemented 7aa71b1f2 (DONE after mid-run machine-sleep kill + resume; suite 7743 green across 1163 files, tsc clean; red→green on pose-B + ringless regression tests; one pre-existing 90°-off-axis synthetic-pose test updated to on-axis; judgment call: PROXY_SCALE mirrored as TS constant + parity test via readWeslConst against analyticSphere.wesl). NOT pushed. Reviewer dispatched (sonnet) over review-42cc9af09..7aa71b1f2.diff → saturn-fix-review.md; hardest lens = forward-axis sign convention (systematic-error risk shared by tests), pose-B numeric re-derivation, Task 12 altitude-term composition + 78 km test still meaningful, distanceRangeM untouched (pick fix depends on it), parity test reads the file the shaders actually use. On clean review: push → republish board → task-brief 14.
- SATURN FIX: complete (commit 7aa71b1f2, review CORRECT/ACCEPTABLE, 0 blocking, 1 minor deferred [slabs.ts comment ratio overshoot 1.08→1.13, content ruled load-bearing]; reviewer independently re-derived sign conventions, hand-computed pose-B margins [ring face tight-controls by exactly NEAR_MARGIN_EPS], empirically reproduced red→green by reverting slabs.ts only, confirmed distanceRangeM + 3 consumers untouched and parity test reads the real shader source). PUSHED to PR #634. SATURN BUG CLOSED — user re-checks from Titan at Task 14 gate. NEXT: task-brief 14.
- Task 14 GATE OPENED: perf half dispatched (sonnet; both scenarios @30 frames vs perf-baseline-main.md, --url http://localhost:5173 [server verified alive], outputs → task-14-perf-after-*.txt, analysis → task-14-perf-report.md; halt rule stated in dispatch, controller applies it). Visual half = USER: four spec §10 views vs main + failure-path (no manifest/atlas/tiles → base picture) + pick re-check + Saturn-from-Titan re-check + chainOverlapViolations silence. Outstanding gate-context items to fold into the final report: field-star-sphere gap (Task 4), Saturn narrow-zoom-band ring exposure (Task 11), lonLatFocusPose deferred to spec 2 (Task 13), cloud-alpha >0.5 accumulation minor (Task 12), Sun/Moon standoff (EOX memory, if relevant), deferred minors lists Tasks 1-13. On perf result: if neutral-or-negative → HALT, hand land/park to user with full report; if improved → still hand gate report to user (their ruling either way).
- Task 14 PERF: first comparison INVALID — after-run REGRESSED verdict (+16.4/+21.0 ms) is unsound because perf-baseline-main.md was captured BEFORE the public/data symlink existed: baseline scene had NO catalog data (no star-catalog/star-aggregates/star-upsample rows, star-points 0.1 ms, per-layer total 6.2 ms) while the after-run has the full dataset. Controller verified by diffing layer rows in the two txt files. HALT stands until a valid before exists. RE-BASELINE dispatched (sonnet, isolation:worktree, detached at b381cdc6b = main-equivalent, symlinks MAIN's public/data, own dev server on its own port, both scenarios @30 + earth-surface rerun, must CONFIRM star-catalog rows present, outputs → scratchpad/rebaseline-main-*.txt). On result: recompute deltas vs task-14-perf-after-*.txt; if still neutral-or-negative → HALT + hand land/park to user with honest numbers; the after-run's N-pass growth (foreground chain 2→4 passes, 1.8→7.2 / 1.9→8.6 ms MERGED) is real regardless of baseline and goes in the gate report either way.
- Task 14 PERF FINAL (valid baseline, scratchpad/rebaseline-main-*.txt; star rows confirmed present): earth-surface 20.2→22.0 ms med (+0.7…+1.8 incl. reruns, ≈noise) = NEUTRAL-to-slight; solar-system 21.8→27.6 med / 30.0→36.1 p90 (+5.8/+6.1) = REGRESSED, attributed to chain 2→4 passes (5.2→8.6 ms). HALT RULE FIRED — pipeline halted, land/park handed to USER with task-14-gate-report.md (perf table, outstanding visual checklist, all deferred findings, mitigation candidates, housekeeping incl. throwaway worktree agent-a90b98ef278960323 cleanup). NO further dispatches until user rules. Original perf-baseline-main.md VOID (no-data scene) — do not reuse.
- GATE DISCUSSION (user probing perf): two findings. (1) User's debug-overlay paste shows ~24 repeated 4-layer blocks (earth/cloud-shell/textured-bodies/atmosphere-shell) with identical timings — matches the FIXED POOL: scenePlanets.ts has 23 bodies + Earth = 24 candidates; frameProgram sizes a fixed row pool (spec §6, per visibleSlabBodies header) and the overlay lists every registered instance; per-layer timers are keyed by NAME so all instances show the same aggregate. Display artifact, NOT 24 executed passes (harness confirms only BODY[0]/[1] execute at perf poses). (2) The REAL waste: visibleSlabBodies culls on apparent diameter ONLY (SUB_PIXEL_BODY_CULL_PX=1, no frustum test — phase-1 Saturn investigation documented all gates view-independent) → a body >1px but off-screen/behind camera still gets a full pass (clear+draw+composite ~1.4-1.7 ms each). MITIGATION CANDIDATE (not dispatched, awaiting user ruling): sphere-vs-frustum angular cull in visibleSlabBodies (offAxisAngle − angularRadius > frustumHalfDiagonal + proxy margin ⇒ no row) — Saturn-lesson-safe (angular, not centre-projection), cannot affect caption coverage (off-screen body contributes no on-screen alpha) or pick (cursor is on-screen). Overlay dedupe = separate cosmetic item.
- USER RULING: mitigate before land/park decision — frustum cull approved ("yes you can do that"). ANSWERED user's earth-×24 confusion: frameProgram.ts:316 stamps every slab:'body' layer onto every pool row (matchesStep || layer.slab==='body' && step.slab>=2); instances self-gate via enabled() bodyId check (earthLayer.ts:164); panel timers keyed by NAME echo the one real draw onto all listed instances. MITIGATION implementer IN FLIGHT (sonnet, BASE 7aa71b1f2, two commits: (1) angular sphere-vs-frustum cull in visibleSlabBodies [off-axis − angularRadius > halfDiag + eps; r_eff = bodyDrawRadiusM × PROXY_SCALE mirror const; forward passed from frameContext camForward; oneMpcSeam allow-list respected via bodyApparentDiameterPx seam; TDD: behind-camera/90°-off/partial-edge-kept/pose-A-kept]; (2) debug-panel dedupe by layer name, STOP if >2 files). Report → frustum-cull-report.md. On DONE: review (sonnet; lens = partial-visibility safety [Saturn lesson], seam compliance, dedupe scope) → then perf RE-MEASURE both scenarios vs rebaseline-main-*.txt → gate report update → USER land/park ruling.
- MITIGATION landed: 034df6436 (frustum cull: offAxis − angularRadius > halfDiag×1.15 ⇒ no row; inside-shell always-keep guard; rEff = max(PROXY_SCALE·radiusM, bodyDrawRadiusM); NOTE implementer imported SCALE_UNITS.M_TO_MPC directly — legal vs oneMpcSeam list, reviewer adjudicates rightness) + 964a5156c (panel dedupe by layer name; implementer flagged PRE-EXISTING gpuTimingService name-collision: shared layer names capture only LAST body row's cost — matters if per-layer numbers ever drive a perf ruling). Suite 7748 green. TWO agents IN FLIGHT in parallel (both read-only): reviewer (sonnet → frustum-cull-review.md; lenses = multiplicative-vs-additive margin at FOV extremes, false-cull worst case, seam ruling, dedupe not worsening slot collision) + perf re-measure (sonnet; both scenarios vs valid main baseline AND pre-mitigation runs → task-14-perf-after2-*.txt + '## Post-mitigation' section in task-14-perf-report.md). On both back: if review clean + perf verdict in → push both commits, update gate report + board, hand land/park to USER. If review forces changes → fix round, re-measure again.
- POST-MITIGATION PERF (task-14-perf-report.md '## Post-mitigation', after2 txts; star rows present, valid): solar-system 21.5/29.7 vs main 21.8/30.0 = IMPROVED (regression RESOLVED, was +5.8); earth-surface 22.0-22.2 vs main 20.2-21.3 = NEUTRAL-BORDERLINE (+0.7…+1.8, straddles ~1 ms noise; BODY passes 2.3→1.2-1.4 ms but hdr·NEAR0/star-aggregates·NEAR0 ran ~0.5-0.8 higher — Apple-Silicon slot-sum caveat flagged). BODY groups 2→1 in both scenarios. Awaiting frustum-cull review; on clean: push 034df6436+964a5156c → final gate handover to USER (perf verdict now: one improved, one borderline-neutral — halt rule technically still touches earth-surface's borderline; user rules).
- DECOMPLECTION REVIEW (user request): entanglement-radar fanned out 3 read-only sonnet reviewers over the branch diff → radar-frame-derivation.md, radar-layers-renderers.md, radar-seams-tests.md (slices: frame derivation / layers+renderers+occlusion / seams+tests+docs; each reads the skill + simplicity.md, reports in skill format). On all 3 back: merge, dedupe cross-slice, triage vs known-entanglements, report to user (findings are review output — /simplify or explicit ask applies fixes; fold accepted ones into the pre-land fix wave). Frustum-cull REVIEW still in flight in parallel.
- LOC STATS (comment/blank-stripped, origin/main...HEAD, delivered to user): code src+tools +923/−495, tests +2641/−1034, shaders +12/−18, docs +1981/−0; comments +1899/−1279.
- MITIGATION review clean (frustum-cull-review.md): both commits APPROVED, 0 blocking, 1 minor deferred (visibleSlabBodies header 11 lines vs ≤10). Margin ruling: ×1.15 provably redundant — un-margined test already geometrically exact against false culls at any FOV/aspect (corner maximizes off-axis angle). Seam ruling: direct M_TO_MPC use correct outside the gated MVP-compose list, matches sanctioned precedent, no gate change. Reviewer independently traced buildTimingSlotMap: dedupe touches display only, query allocation untouched, last-row-wins collision accurately flagged + pre-existing. PUSHED 7aa71b1f2..964a5156c → PR #634. GATE STATE: perf improved (solar-system) + borderline-neutral (earth-surface); awaiting 3 radar agents → merged decomplection report to user → then land/park handover (user also owes visual checks).
- RADAR COMPLETE (3 slices → radar-frame-derivation.md, radar-layers-renderers.md, radar-seams-tests.md; 12 raw findings, merged/deduped to 9 by controller): TOP = (1) alpha composite-weight × occlusion-signal braid (0.5 threshold; already produced 2 review minors); (2) hand-maintained unit-seam rosters — oneMpcSeam TS_FILES missed visibleSlabBodies on day one + 3 uncoordinated allow-list homes; (3) distanceRangeM [0,0] = interval × nothing-drawn sentinel (pick remap is a symptom patch); (4) index≥2 body-row test restated ×3 vs frame.kind; (5) partition/drawList recomputed per row×layer (value×time); (6) cloud-shell-is-Earth in 3 homes; (7) drawPick floored-sphere block duplicated earth/planets layers; (8) pixel-floor uses radiusM while frustum cull uses bodyDrawRadiusM in same fn — ringed body droppable while its ring resolvable = SAME exposure as Task 11 Saturn-ring narrow-band gate item; (9) earthSurfaceTile rotCol0/1/2 always-identity kept for layout parity. Refuted as clean: stamp-every-layer shape, 24-pool sizing, 3-term near max(). Radar = review only; fixes await user ruling (candidates for pre-land fix wave: 2,4,8 small; 1,3 medium; rest backlog).
- USER RULING (radar): fold findings 2, 4, 8 now (wave 1); THEN findings 1 (alpha dual-meaning) and 3 ([0,0] sentinel) as wave 2. Wave 1 implementer IN FLIGHT (sonnet, BASE 964a5156c, three commits: (1) oneMpcSeam gate roster derived by glob + ONE consolidated allow-list home w/ justifications [visibleSlabBodies gets an entry, exclusion-ruling preserved] + mutation check; (2) single is-body-row predicate repointing frameProgram:254/executeFrame:209/pickProgram:410, prefer frame.kind; (3) visibleSlabBodies pixel floor keys on same r_eff as frustum cull [ring-resolvable roster admission; TDD Saturn-ring-clears-floor case] — closes the Task 11 Saturn narrow-band gate item if clean). Report → radar-wave1-report.md. On DONE: review (sonnet; lens = glob sweep can't pass vacuously, predicate choice leaves fewest index-layout knowers, widened roster draw-regime reasoning) → then WAVE 2 dispatch (findings 1+3: design the un-braiding — alpha/occlusion separation + distanceRangeM sentinel → explicit empty representation; risk-scale review). Land/park handover after waves + user visual checks.
- USER DIRECTIVE (explicit): radar finding 1 (alpha composite-weight × occlusion-signal un-braiding) RIDES THIS BRANCH — wave 2 is on-branch work before merge, not backlog. Wave 2 = findings 1 + 3, dispatches after wave 1 review.
- RADAR WAVE 1: implemented f1c9a7b84 (seam roster directory-swept + ONE allow-list home) + 2636fb886 (isBodySlabIndex single predicate) + 865f90e1a (pixel floor keys on frustum-cull footprint, TDD Saturn-ring case). Suite 7823 green, typecheck clean, NOT pushed. Implementer concerns: utils/scene+utils/camera half of roster still hand-named; nothing structurally prevents future inline >=2. Reviewer dispatched (sonnet) over review-964a5156c..865f90e1a.diff → radar-wave1-review.md; lens = sweep non-vacuity + no-file-lost-gating, predicate repoint completeness, widened-roster draw regime (glint + ring, no double-draw), rEff single expression; rules both concerns. On clean review: push wave 1 → dispatch WAVE 2 (findings 1 [alpha/occlusion separation — user directive: rides branch] + 3 [[0,0] sentinel → explicit empty]; wave 2 is a DESIGN-bearing change, brief the implementer with the radar write-ups + Task 12 threshold reasoning; risk-scale review, consider opus). Then deletion-audit (offered to user, fits /feature-done sequence anyway) + land/park handover.
- RADAR WAVE 1 review: 0 blocking, 1 minor — foregroundChainOrder slabs.ts:407 still inline `index >= 2` (4th site of the commit-2 braid; reviewer ruled close directly, no lint gate). Concern rulings: utils hand-named residual ACCEPTABLE (sweeping 67 unrelated files dilutes the gate); no >=2 gate needed beyond the repoint. Reviewer independently: injected-offender mutation test (sweep non-vacuous, temp file cleaned), hand-recomputed Saturn fixture (≈0.485px globe / ≈1.169px ring, RED-before-fix genuine), glint partition unaffected. Fix round dispatched (resumed implementer): one-line repoint to frame.kind at :407. On DONE: controller verifies diff directly → push all wave-1 commits → dispatch wave 2.
- RADAR WAVE 1 complete + PUSHED (964a5156c..946428f83: sweep+allow-list f1c9a7b84, predicate 2636fb886, footprint floor 865f90e1a, fix-round repoint 946428f83 [controller verified 1-line diff]). Saturn narrow-band ring gate item CLOSED by 865f90e1a. WAVE 2 dispatched (OPUS implementer, BASE 946428f83, two commits: (1) alpha un-braid — candidate shape = per-pipeline alpha-channel blend separation [shells preserve dst alpha, opaque bodies write 1, threshold 0.5 → >0]; must verify HDR composite (shells rely on alpha as weight — premultiplied-additive reasoning or dedicated channel as fallback); design decision recorded in report BEFORE coding; may fold fs/fsCoverage collapse if natural; (2) [0,0] sentinel → explicit type-level emptiness, compiler-driven consumer sweep, pick regression tests preserved). Report → radar-wave2-report.md incl. REQUIRED user visual checklist (visible compositing change). On DONE: review (risk-scaled, likely OPUS — blend/composite subtlety) → push → deletion-audit question to user → land/park handover with full visual checklist (four §10 views + Saturn/pick re-checks + wave-2 compositing checks).
- RADAR WAVE 2: implemented 86f4b1269 (alpha un-braid — DESIGN PIVOT: briefed blend-separation rejected as impossible under straight-alpha OVER composite; shipped shape = coveredByScene → sceneTransmittance(xy)=saturate(1−a), overlays scale premultiplied output continuously, NO threshold; 20 files +136/−242) + c9283f249 (sentinel → type-level emptiness, 5 files). Suite 7824 green, tsc+lint+build clean (WESL link verified in bundle). NOT pushed. Implementer concerns: (1) BEHAVIOUR DELTA — captions dim up to 50% under faint glow (previously untouched), never clipped harder; 6-item visual checklist in radar-wave2-report.md for USER; (2) self-corrected radar finding-3 analysis (fieldStarSphereLayer also on NEAR0/foreground:0, order genuinely changes in narrow regime, new order correct); (3) no unit test for commit 1 (WGSL-only, mirror-test ban) — verification = build link + visual; (4) rejected alternative = dedicated coverage attachment (fallback if user dislikes dimming; separate larger PR). Reviewer dispatched on OPUS over review-946428f83..c9283f249.diff → radar-wave2-review.md; lens = verify straight-vs-premultiplied composite claim IN CODE (pivot stands or falls on it), dissolved-or-renamed adjudication, monotonic behaviour-delta math, opaque a=1 everywhere + descent-fade interaction, blend-state-assertion test possibility. On clean review: push → final handover (visual checks incl. wave-2 checklist + deletion-audit question + land/park).
- STALE-DIAGNOSTICS note: editor flagged labelRenderer packedLabels / foregroundLabelsLayer unused view AGAIN post-wave-2 — implementer's full tsc+build green; landmine pattern, ignore unless tsc agrees.
- RADAR WAVE 2 review (OPUS): APPROVE both commits, 0 blocking, 7 minor. PIVOT VERIFIED in code (BLEND_TABLE.over straight-alpha at compositor.ts:159-165, used by frameProgram.ts:178 — briefed separation genuinely unavailable). Braid ruled DISSOLVED (all 3 costs gone; disclosed price = "media never occludes" distinction dropped + documented-unenforced premultiplied dependence). Reviewer independently verified bundle link (3× sceneTransmittance, 0 coveredByScene), opaque a=1 through atmosphere composition, discard→zero-write equivalence. FIX ROUND dispatched (resumed implementer): M1 stale shader comments ×3, M2 blend-state assertion test (closes the unenforced coupling), M5 header trim, M7 wrong star-distance premise → mutual-exclusivity wording. Deferred: M3 (commit-msg wording), M4 (= user visual gate, wave-2 checklist unattested), M6 (perf pair — controller folds an earth-surface re-run into final gate). On fix DONE: controller verifies diff → push wave 2 + fix → FINAL HANDOVER (perf incl. M6 pair, all visual checklists, deletion-audit question, land/park).
- WAVE 2 + fix round PUSHED (..8994423c5). M6 perf pair DONE, both radar waves in: earth-surface 21.2/29.5 (inside main's 20.2-21.3 band — the residual borderline delta GONE); solar-system three runs 21.5 / 26.2 / 22.5 with ONE BODY group each (no roster creep from the ring-aware floor) — median-of-runs ≈22.5, vs main 21.8 = NEUTRAL; run variance ±2 ms while background agents load the machine, noted honestly (quiet-machine re-run available if user wants a cleaner number). PERF GATE VERDICT: both scenarios neutral-or-better vs main. BRANCH CODE COMPLETE. FINAL HANDOVER delivered to user: consolidated visual checklist (4× §10 parity views, Saturn-from-Titan re-check, pick re-check, wave-2 transmittance 6-item list in radar-wave2-report.md, failure path, chainOverlapViolations) + deletion-audit offer + land/park ruling. Remaining after ruling: [if land] deletion-audit → final whole-branch review (opus) → one fix wave → /feature-done → finishing-a-development-branch; spec 2 next.

## User bug 4: body sphere picks clobbered (2026-09-01)

User report post-code-complete: planet labels win over stars, but planet
BODIES have no pick representation. Root-caused WITHOUT dispatch (code
reading): `bodyPickRenderer.drawSphere`'s dynamic-offset cursor resets per
PASS (`beginPassIfNew`); pick program now records one pass per body row on
one encoder + one submit, so every body row writes slot 0 and all sphere
picks collapse onto the last-recorded (farthest) row's bytes. Same-class
latent visual bug: `planetRenderer` single instance buffer + one write per
`planetsLayer.draw` call → ≥2 flat bodies in view collapse onto the last.
Audit of the other per-body renderers (textured/atmosphere OK per-body;
earth/rings/cloud-shell single-body today; starRenderer race PRE-EXISTING
on main, out of scope) + fix shapes recorded in
`body-pick-clobber-investigation.md`.

BASE 8994423c5. Fix implementer (sonnet, background) dispatched with the
investigation doc as brief; report → `pick-clobber-fix-report.md`. Handling:
on DONE → review-package BASE..HEAD → reviewer (sonnet, small mechanical
diff unless it grew) → fix rounds per SDD loop → push → re-offer user pick
re-check (this reopens the pick item of the visual gate).

Fix landed 9cf59f465 (implementer DONE, full suite 7835/7835, both
typechecks clean; stale editor diagnostics fired again — ignored per
landmine). Review package review-8994423c5..9cf59f465.diff (70KB).
Reviewer dispatched on OPUS (risk-scaled: GPU race correctness), report →
pick-clobber-fix-review.md. On APPROVED → push → tell user pick re-check
reopens. NOT pushed yet.

Fix round 1 landed 9ebd1ec02 (B1 doc sweep net -47 src lines, M2 BodyId,
M3 test dedupe, M7 'less'→'greater'; full suite 7834/7834, typechecks
clean; stale editor diagnostics fired a 3rd time — ignored). Deferred to
deletion-audit: planetRenderer count>1 instancing path now caller-less.
Scoped re-review (sonnet) dispatched on review-9cf59f465..9ebd1ec02.diff →
pick-clobber-fix-rereview.md. On APPROVED → push both commits → user pick
re-check reopens.

Re-review APPROVED (all findings verified addressed, landmine comments
survive, no hidden behavior change). PUSHED: 9cf59f465 + 9ebd1ec02 →
PR #634, new HEAD 9ebd1ec02. Bug-4 loop CLOSED. Visual-gate pick item
REOPENED for the user: verify planet/moon bodies show pick discs again
(debug overlay) + hover/click resolves nearest body; flat-moon visual
check (≥2 untextured moons in view simultaneously — previously only the
last one drew).

USER ATTESTED 2026-09-01: picking works again (bug-4 fix verified
visually). Gate items still open: flat-moons multi-body check, 4× §10
parity views, Saturn-from-Titan both bug poses, wave-2 caption dimming
acceptability, failure path; then land/park ruling.

Flat-moons visual check: user cannot stage it (two untextured moons
multi-pixel simultaneously is rare geometry — flat roster = small
Saturnian moons + Phobos/Deimos/Titan). Ruling: CLOSED-BY-TEST — the
fix's regression test pins the exact mechanism (distinct write targets
per same-submit draw); no extra code path in a two-flat-body pose.
Remaining gate: 4× §10 parity views, Saturn-from-Titan poses, caption
dimming, failure path → land/park.

USER ATTESTED: Saturn-from-Titan works (both bug poses — near-plane fix
verified visually). Remaining gate: 4× §10 parity views, caption dimming
acceptability, failure path → land/park.

USER: "all good" — remaining gate items (parity views, caption dimming,
failure path) ATTESTED. VISUAL GATE PASSED in full; reading it with the
neutral-ruled perf as LAND. Post-gate queue begins: deletion-audit →
whole-branch opus review → one fix wave → /feature-done →
finishing-a-development-branch. Merge itself stays a user action.

Deletion audit dispatched (opus, GREENFIELD --justify framing, full
branch diff audit-main..9ebd1ec02.diff, report →
deletion-audit-branch.md). Invited candidate: planetRenderer count>1
machinery. Handling: triage per leanness.md (behavior changes + debug
surface = user rulings), apply accepted findings as one fix wave, THEN
whole-branch opus review → /feature-done. Sequential, not parallel — the
branch review must see post-audit code.

Deletion audit RETURNED: 9 candidates, ~535 LOC (src ~340 / tests ~195),
20 traps recorded, verdict "nothing halts a landing"; report =
deletion-audit-branch.md. Triage: #1,2,4,5,6,7,8,9 APPLY as written;
#3 (chainRow §7.2 check) MODIFIED → gate data derivation behind the DEV
flag instead of deleting (spec-mandated debug surface; full deletion =
open user ruling). Apply wave dispatched (sonnet, BASE 9ebd1ec02),
report → deletion-wave-report.md. Handling: on DONE → review-package →
scoped review (sonnet) → push → whole-branch opus review → /feature-done.

Deletion wave landed 185dba027 (all 9 candidates, C3 per modified ruling;
net -288 LOC: src -86, tests -202; suite 7821 = baseline minus exactly
the 13 deleted tests; tsc clean both projects; new editor diagnostics
were scratchpad saturn-*.ts probes referencing the removed bodyId param
— outside tsconfig, ignored). C2 comment sweep PARTIAL by design;
follow-up /comment-audit candidate flagged in wave report (slabs.ts,
bodyPickRenderer.ts, occlusionCoverageGroup.ts left dense). Scoped
review dispatched (sonnet, trap-list walk) → deletion-wave-review.md.
On APPROVED → push → whole-branch opus review → /feature-done.

Deletion-wave review APPROVED (trap walk clean). PUSHED 185dba027 →
PR #634 = new HEAD. Final whole-branch review dispatched (OPUS, full
36-commit diff, 8 cross-cutting focus areas, prior reviews fenced) →
final-branch-review.md. On verdict: findings → ONE fix dispatch + scoped
re-review per SDD, then /feature-done (gates DoD, relocates plan+spec,
archives ledger, sweeps backlog, commits+pushes). Merge = user action.

FINAL BRANCH REVIEW (opus): REVISE — 1 blocking (B1 pick-target memory:
per-body-row full-viewport r32uint+depth32float retained, ~500MB at 6
rows), 3 major (M1 skyView bypasses bodyPose seam; M2 TIMED_SLOTS dup
names break per-layer body timings; M3 body-row reversedZ hard-coded,
"partial flip impossible" claim false), 10 minor. Report =
final-branch-review.md. ONE fix wave dispatched (sonnet, BASE 185dba027):
B1+M1+M2+M3 + folded minors (bodyPose identity test, dead oblateness
params, 6 stale composeBodyMvp docblocks, 2 stale spec passages,
cullMode-dependency comment). PARKED (ruled/deferred): allow-list
(ruled), Earth pick-gate handle keying, proxyScale comments (ledgered
comment-audit), cutSurfaceTiles signature, ringRenderer shared uniform
(single ringed body; precondition documented). Report →
final-fix-wave-report.md; then scoped re-review (opus, B1 is a design
change) → push → /feature-done.

Final fix wave: agent finished ALL findings before the checkpoint request
landed. 5 commits 195c6bfe6 (B1) / c65042a55 (M2+M3) / 19077c327 (M1) /
7eb298acf (m4/m5/m10) / db1b5a4d5 (m9 spec amendments), HEAD db1b5a4d5
NOT pushed. Suite 7830 (+9 new tests), tsc+tsgo clean. One pre-existing
test updated (GpuTimingsSection pinned the M2 bug shape; now
'earth·BODY[0]'). Scoped re-review dispatched (OPUS — B1 design change)
→ final-fix-wave-review.md. On APPROVED → push → /feature-done.

Re-review: REVISE with 2 NEW findings only (all 9 originals verified
fixed): N1 renderForDebug per-frame texture churn (~370MB/frame while
overlay on) → reuse+evict map; N2 pickProgram comment redundancy (race
note ×3, ratio 0.73→0.84) → one canonical copy. Fix round 2 dispatched
to a FRESH sonnet (incumbent implementer context >400k, user-flagged;
fresh>reuse). On DONE → quick verify + scoped glance (self, small diff)
→ push all 6+ commits → /feature-done.

Fix round 2 landed 80ccce4f8 (N1 reuse+evict map verified in-diff by
controller: evict-after-submit, destroy() = empty-set eviction; N2 one
canonical race note, ratio back to 0.73; suite 7831). Self-verified in
lieu of a third review seat (shape was reviewer-prescribed). PUSHED
185dba027..80ccce4f8 → PR #634 HEAD = 80ccce4f8. Final-review loop
CLOSED (residual minors parked with rulings above). NEXT: /feature-done.
