# SDD ledger — plan: docs/superpowers/plans/2026-08-16-add-pluto-charon.md

Worktree: .claude/worktrees/add-pluto-planets · branch worktree-add-pluto-planets
No separate spec — the grill session docs/grill-sessions/add-pluto-charon-2026-08-16.md is the design authority.
No harness task-list tool available this session — this ledger + in-message checklists carry Rule 1.

## Preflight conflict scan

| Pair / task | Produces vs consumes                                                                                | Finding                                                                                                  |
| ----------- | --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| T2 ↔ T3     | both edit orbitalElements.ts + scenePlanets.ts; T2 appends Pluto at end, T3 appends Charon after it | clean — serial, append-only respected                                                                    |
| T5 ↔ T9     | both may touch BodyTextureId.d.ts docblock                                                          | clean — T9 says "only if not already done in Task 5"                                                     |
| T9 ↔ T10    | T9 adds 2 ROTATION_ELEMENTS rows; rotationElements.test.ts pins length 13 until T10                 | CONFLICT — T9's commit leaves npm test RED, violating Global Constraint "npm test green at every commit" |
| T5 (self)   | fetchTextures.test.ts RED→GREEN within one task, one commit                                         | clean                                                                                                    |
| T3 (self)   | test cmd includes bodyRegions, confirm-only                                                         | clean                                                                                                    |
| T8 (self)   | commits bodyAtlas.generated.ts; claims public/data/images gitignored                                | verify gitignore claim at execution before staging                                                       |
| T4/T6/T13   | mandate waiting for user (visual confirms, 373 MB download consent)                                 | plan text wins over skill's continuous-execution — these are user-facing consent/verification points     |

Ruling: T9+T10 batched into ONE dispatch and committed together — the green-at-every-commit constraint outranks task granularity; edits are tiny and same-shape. Cost if wrong: one slightly larger review unit.
Ruling: per project feedback (bg subagents don't run npm), implementers self-verify with `npx tsc --noEmit` only; the controller runs the plan's `npm test` / `npm run typecheck` gates in-session after each DONE report, before dispatching the reviewer. Test evidence in reviews cites the controller's runs. Cost if wrong: slightly weaker implementer-side evidence, caught at the controller gate.
Ruling: commits authored as the user (no AI co-author trailer), per standing feedback. Cost if wrong: none material.

## Task status

Task 1: DONE by implementer (commit 9155e49f8) — controller gate npm run typecheck GREEN; review dispatched (pipelined with implementer 2, BASE for T2 = 9155e49f8)
Task 1: review verdict — spec pass; 1 Important: CHARON_GREY [0.42,0.43,0.45] same-hue as SAT_ICE [0.45,0.47,0.5] (dist ~0.07), may not read apart in additive trails. Fix round 1 queued behind in-flight implementer 2 (tree ban). Minor (deferred): T1 report self-check understated hue similarity.
Task 2: DONE by implementer (commit ba0c60cdc) — premise deviation: live approx_pos.html lacks Pluto, sourced Standish & Williams PDF; controller gates typecheck + orbitalElements/scenePlanets tests GREEN (11/11); reviewer 2 dispatched with source-verification mandate (BASE 9155e49f8)
Task 1: fix round 1/5 dispatched (resumed implementer) — CHARON_GREY hue nudge away from SAT_ICE
Task 2: complete (commits 9155e49f8..ba0c60cdc, review clean — all digits source-verified vs aprx_pos_planets.pdf; radius 1188.3 km vs Archinal 2018 confirmed)
Task 1: fix round 1/5 (fix commit eed45a0f1: CHARON_GREY -> [0.3,0.37,0.49]); scoped re-review dispatched to original reviewer
Task 1: fix round 1 re-review — SAT_ICE finding ADDRESSED; NEW Important: closeness to URANUS_CYAN (~0.10) + wrong distance math in fix report. Fix round 2/5 dispatched (full-palette distance table, PLUTO_TAN weighted, visual arbitration at T4).
Task 1: fix round 2/5 (commit 109b2a9dc: CHARON_GREY -> [0.4,0.34,0.42]; URANUS gap 0.182, PLUTO_TAN 0.161, but MOON_GREY 0.055); scoped re-review dispatched
Task 1: complete (commits 215f968d3..109b2a9dc net palette change, review clean after 2 fix rounds)
Task 1: minor (deferred): eyeball CHARON_GREY [0.4,0.34,0.42] vs MOON_GREY (dist 0.055, different hue family) at the T4 visual checkpoint; nudge R up / G down if they read alike
Task 3: implementer dispatched (BASE 109b2a9dc)
Task 3: DONE by implementer (commit fc1f8d33a) — 2 premise corrections (single mean-equatorial table; Pluto's IAU pole = Charon's, NAIF-verified); controller gates typecheck + 16/16 tests GREEN incl. bodyRegions unmodified; reviewer dispatched (BASE 109b2a9dc)
Task 3: review — spec pass, all digits verified; 1 Important: sidereal-P-label safety argument undocumented (reviewer proved safe via e=0/i=0 rate freeze). Fix round 1/5 dispatched: didactic comment on the row. Minor (deferred): Charon row comment density high but file under budget.
Task 3: fix round 1/5 (commit 477f5d51b: sidereal-P degeneracy comment on Charon row); scoped re-review dispatched. Dev server up: http://localhost:5175 (shell bn24cn1x9)
Task 3: complete (commits 109b2a9dc..477f5d51b, review clean after 1 fix round)
Task 4: VISUAL CHECKPOINT — awaiting user confirmation at http://localhost:5175
Task 4: complete — user confirmed visual checkpoint (positions, captions, trails, picking, trail-tint legibility)
Ruling: implementers may run single-file 'npx vitest run <file>' for their own TDD red/green loop; official gates stay controller-run. Cost if wrong: minor resource contention.
Task 5: implementer dispatched (BASE 477f5d51b)
Task 6: user approved the ~373 MB download; fetch will start pipelined once T5 gates pass (before review closes — URLs live-verified twice, risk accepted)
Task 5: DONE (commit 3e5a9c517) — gates typecheck + fetchTextures 11/11 GREEN; concerns: grayscaleTint unset pending T6 channel check, hand-patched generated bodyAtlas.generated.ts placeholders (T8 regenerates). Reviewer dispatched (BASE 477f5d51b).
Task 6: download running in background (shell bl6c19cue).
Task 6: first fetch attempt FAILED — unrelated 503 from web.archive.org (Earth watermask) aborted the 29-source full pull; worktree data/raw/ was empty (worktree data isolation). Fix: hardlinked main's 28 existing texture binaries into the worktree (no disk cost, no writes to main, tracked README/sha256 untouched) so skipIfAlreadyFetched skips them. Retry running (shell b0j0f61x0).
Task 5: complete (commits 477f5d51b..3e5a9c517, review clean — URLs live-verified, Content-Length 296MB/77MB match)
Task 5: Ruling: the Important finding (hand-edited bodyAtlas.generated.ts placeholders 13/14 are live-consumed by bodyTextureAtlasSlot against a still-13-tile webp) does NOT open a fix round — the only real fix is regenerating the atlas, which is blocked on the Task 6 download and IS Task 8. Carried into Task 8's dispatch brief as a mandatory verification instead. Cost if wrong: if T8 were skipped, the branch would ship a layout claiming 15 tiles over a 13-tile image (blank crops, no crash) — T12 full-suite + T13 visual are the backstops.
Task 5: minor (deferred): stale test title 'the four USGS moons' now covers six; comment density in bodyTextureRegistry row over budget (pre-existing file-wide condition)
Task 6: Pluto mosaic COMPLETE (309,806,463 bytes, sha256 592b7fb0..., matches reviewer-verified Content-Length). Charon fetch hit transient 'fetch failed'; retry running (shell bab2wvvs8).
Task 7: DONE (commit 980aa9bdd) — credit line corrected vs brief's guess (4th originator = Lunar and Planetary Institute, USGS is publisher); reviewer dispatched to independently verify at Astropedia (BASE 3e5a9c517).
Task 6: complete — both mosaics fetched + verified (Pluto 309,806,463 B; Charon 77 MB), sidecar committed 2cc3e1aa1. CHANNEL CHECK: both channels=1 (b-w) => grayscaleTint REQUIRED, per Europa/Callisto precedent.
Task 7: complete (commits 3e5a9c517..980aa9bdd, review clean); fix round 1 (113d02279) for stale registry credit comment — re-review dispatched.
Task 7: complete (commits 3e5a9c517..113d02279, review clean after 1 fix round)
Task 8a (tint follow-up): commit 244efc095 — pluto [0.8,0.7,0.56], charon [0.6,0.58,0.56], derived from Olkin+17 / NH Charon PCA, calibrated to Europa/Callisto idiom. Implementer flags magnitudes are reasoned calibration not literal RGB extraction — visual arbitration at T13.
Task 8: build-textures running (shell b1tz7z0f6).
Task 8: complete (no commit — regenerated bodyAtlas.generated.ts is byte-identical to T5's placeholder scaffold, so the T5 Important finding is resolved by construction; textures pluto-2048/charon-2048 + 15/15-tile atlas built OK, gitignored artefacts landing in main's public/data via the /link-data symlink).
SCOPE CHANGE (user decision, 2026-08-17): Pluto switches to the NASA/JHUAPL MVIC true-colour global map and BOTH bodies move small->medium (4K). Rationale: colour mosaic is 5926px wide so 4K costs zero resolution; Charon has no colour mosaic in existence and stays greyscale+tint (honest — Charon is near-neutral). Plan doc to be amended. GATE: verify the colour map is approximately-true-colour, NOT enhanced colour, before adopting.
Task 8b: GATE FAILED — the NASA/JHUAPL 'Pluto Global Color Map' (PIA11707) is ENHANCED colour. Evidence: Olkin+2017 AJ 154 258, the mission's own colour paper on exactly this MVIC 3-filter data: 'These images are enhanced color (not natural color as perceived by the human eye)'; sibling PIA19956 from the identical pipeline is captioned 'enhanced, extended color... variations have been enhanced'. The only true-colour NASA product (PIA19857) is a single hemisphere with a stated coverage gap, 1920x1080 — cannot be unwrapped to a seamless equirect texture. No gap-free true-colour global Pluto map exists.
Task 8b: Ruling: DROP the colour swap, KEEP the 4K bump for both bodies. The user's choice was explicitly conditioned on the true-vs-enhanced verification passing; it failed, so the honest remainder of their intent is greyscale panchromatic basemap + measured average tint (what we already ship) at medium/4K. This matches the researcher's own recommendation. Cost if wrong: Pluto lacks per-pixel Cthulhu/Tombaugh colour contrast — recorded as a backlog item, revisitable if a true-colour cylindrical mosaic is ever published.
Task 8b: complete-pending-review (commit 1949a3566 + plan fixes e464f7c78); textures rebuilt at medium: pluto-4096.jpg + charon-4096.jpg both (tinted), atlas 15/15 unchanged. Reviewer dispatched (BASE 2cc3e1aa1, 4 commits).
Tasks 9+10: implementer dispatched batched (WGCCRE 2015 rows + length re-pin in ONE commit).
Task 8a/8b: review NOT APPROVED — CRITICAL: fabricated citation 'Protopapa+19, ApJL 872 L36' in bodyTextureRegistry.ts:135 (no such paper; 2.7% PCA figure traces to Grundy+2016 Science 351 aad9189). Same bogus attribution was propagated by ME into the plan's Task 8b section. Important: precision-by-association (percentage beside hand-tuned tint implies derivation). Minor: comment density 1:1 vs Europa/Callisto 1:6; and 1949a3566's unreviewed prettier pass corrupted a plan formula (fixed in e464f7c78).
Task 8a/8b: fix round 1/5 dispatched — MUST verify the real citation at primary sources, not adopt the reviewer's suggestion on trust.
Tasks 9+10: DONE (commit 99e40e3c9) — shared pole 132.993/-6.163, W0 302.695 (Pluto) / 122.695 (Charon), spin 56.3625225 deg/day both; tidal-lock residual -5.6e-5 deg. Gates typecheck + rotationElements 2/2 GREEN. Reviewer dispatched with hardened citation scrutiny (BASE e464f7c78). Implementer concern: WGCCRE PDF paywalled, verified via NAIF pck00011.tpc.
Tasks 9+10: complete (commit 99e40e3c9, review clean — reviewer re-fetched pck00011.tpc and matched every digit; Archinal 2018 CMDA 130:22 DOI verified; minor-body positive-pole convention confirmed). Minor (deferred): densest comment pair in the file, justified.
Task 8a/8b: fix round 1 (commit a501d6ea4) — Grundy et al. 2016 Science 351 aad9189 verified by downloading the arXiv PDF and grepping the exact '2.7% of the observed variance' sentence; row now cites NO papers and points at the report instead (avoids precision-by-association); comment 12->7 lines.
Ruling: Task 11 dispatched in parallel with the 8b re-review rather than waiting for the freeze to lift — the open finding is comment-only and Task 11's files (planet_facts.seed.json, bodyFacts.generated.ts) are disjoint from bodyTextureRegistry.ts. Cost if wrong: none material; nothing in T11 builds on that comment.
Task 8a/8b: re-review — all 3 original findings ADDRESSED, but NEW Important: the resolution points at task-8a-tint-report.md, which lives under gitignored .superpowers/ and is rm -rf'd at Finish (only progress.md is archived per sdd-execution Rule 3) => dead reference. Resolution chosen: re-cite the now-verified sources directly in the comment (Grundy+2016 Science 351 aad9189 / arXiv:1604.05368; Buratti+2019 ApJL 874 L3) — safe now that both are independently confirmed twice. Fix round 2 QUEUED behind Task 11's implementer (serial implementers / shared git index).
Task 11: DONE (commit 412baaeec + regenerated facts b18e85f04) — 24 rows, pluto+charon present; values from JPL Horizons/SBDB (nssdc host is dead); axialTilt + Charon meanTemp deliberately OMITTED as genuinely disputed rather than guessed. Fix round 2 for 8a/8b dispatched.
Task 11: complete (commits a501d6ea4..b18e85f04, review clean — every number re-verified live at JPL SBDB/sats + science.nasa.gov; nssdc-is-dead claim confirmed; both omissions judged justified).
Task 11: minor (deferred): 'Mordor Macula' may now officially be Neverland Regio (reviewer claim — VERIFY before changing user-facing text); Pluto atmosphere string puts 'thin' qualifier in a different position than sibling entries.
Task 8a/8b: fix round 2 (commit 13b35e747) — inline citations restored, dead report pointer dropped, comment 8 lines.
Task 8a/8b: complete (fix round 2 re-review ADDRESSED; residual nit: Olkin+17 'overall' phrasing slightly overstates a paper documenting disk heterogeneity — deferred minor).
Task 12: full suite 6898/6901; 3 failures all in-feature: bodyTextureRegistry structural invariants, createEngineData planets-at-construction, deriveBodyStates J2000 snapshot. Implementer dispatched with a MUST-DIAGNOSE instruction on the J2000 snapshot (re-pin only if the diff is purely additive; any change to an existing body = real bug).
Task 12: full suite GREEN 6901/6901, typecheck GREEN (commit f2c868d76). J2000 gate DIAGNOSED not re-pinned: all 63 pre-existing fixture entries zero-delta at full precision vs live deriveBodyStates(CONST_J2000); new rows purely additive.
Task 12: minor (deferred): createEngineData.test.ts's literal 21-id planet list is a registry restatement per testing.md; the adjacent toEqual(SCENE_PLANETS) already covers it — deletion candidate, implementer correctly declined to delete unilaterally.
Task 12: complete (commits 13b35e747..f2c868d76, review clean — reviewer wrote its OWN probe: 0 mismatches across all 65 fixture entries; new rows sanity-checked independently in Python: Pluto 30.23 AU, separation 19600.0 km, orientation rows near-exact negations = tidal-lock signature). Tautology caveat noted and accepted: this is a drift gate, not a correctness proof.
Task 12: Ruling: acted on the reviewer's Minor now rather than deferring — deleting the registry-restating 23-id list (subsumed by toEqual(SCENE_PLANETS), breaks on every future body addition). Cost if wrong: a test deleted that nothing else covers, but the adjacent assertion demonstrably covers it.
Cleanup: commit bc70ad15e (id-list deletion); suite still 6901/6901 GREEN. Final whole-branch review dispatched on opus (22 commits, merge-base c25c84558), pointed at the ledger's deferred minors + rulings.
FINAL REVIEW (opus, 22 commits): citation sweep clean — every remaining citation verified real at primary sources. Simplicity premise HELD (zero new branches/machinery across 22 commits; only leak = shouldTint disjunction in a test).
CRITICAL #1: Pluto's element row cites ssd.jpl.nasa.gov/planets/approx_pos.html, which no longer contains Pluto AT ALL (the PDF 301-redirects there), and asserts 'the only table that includes Pluto' — FALSE: Table 1 (1800-2050) also carried Pluto (a=39.48211675) and needs NO b/c/s/f corrections, i.e. the premise that justified choosing Table 2a and dropping its correction terms is wrong. Ruling: switch Pluto to Table 1 (matches every sibling row, deletes the dropped-correction caveat entirely, valid over the scene clock's span) and cite an archived, resolvable URL.
Importants: #2 facts distance 39.6 AU contradicts our own a=39.4869 (siblings all round their own a); #3 'Mordor Macula' superseded by IAU 'Neverland Regio' (Gazetteer ID 16443, adopted 2026-02-02) — ledger's deferred-minor VERIFY condition now discharged; #4 barycentre comment says 1.8 R_P 'outside Pluto's surface' but 1.79 R_P is from the CENTRE (~0.79 R_P above surface), and the backlog's prescribed 2035 km should be ~2130 km; #5 Olkin+17 cited for an 'overall tan' it doesn't claim AND is the same paper we cite to prove the product is enhanced-colour; #6 shouldTint 2-way->4-way registry restatement extended in the same branch that deleted an identical one.
Minors: #7 four stale 'thirteen'/'four USGS moons'/'seven' counts; #8 plan head+DoD still say small/2k and pluto-small.jpg; #9 CHARON_GREY is mauve-not-grey with no comment recording why; #10 comment points at a path that moves at /feature-done; #11 the declared spinRate==360/period cross-table invariant has NO test; #12 Charon rotation row densest in tree.
Final fix wave: 8 commits bc70ad15e..b72125fa7. All 12 findings addressed per implementer; Pluto switched to Table 1 (verified in 2 independent copies, matches the 8 incumbent rows' digit conventions); build-planet-facts run by controller — generated file verified to carry 'Neverland Regio', '39.5 AU', one 'Mordor' nickname clause. Gates: typecheck GREEN, suite GREEN 6902/6902 (+1 = new tidal-lock cross-table invariant, mutation-verified RED both directions). Scoped re-review dispatched.
Re-review agent a6fcc14 STALLED (watchdog, no progress 600s) — re-dispatched fresh reviewer with tighter priority-ordered scope (A Table 1 numbers, B J2000 fixture isolation, C new tidal-lock test, D substituted colour citation).
Fix-wave re-review (fresh reviewer after 2 environment-killed attempts): ALL 12 findings ADDRESSED, no new breakage. Critical independently confirmed — reviewer rendered the Standish PDF to page images and matched all 12 Table 1 Pluto values digit-for-digit; the Explanatory Supplement fallback URL resolves (HTTP 200, Table 8.10.2). J2000 fixture diff is exactly 3 lines (pluto position + meanAnomaly, charon position); Pluto r = 30.230 au, separation 19600.0 km. Tidal-lock test bound 2e-5 vs honest residual 8.8e-6 (~2.3x), both mutations RED. Colour citation verified as the P_COLOR_2_TRUE_COLOR product (NOT the enhanced composite) and the 1:0.938:0.851 measurement independently reproduced. Residual (a) 'four Galilean moons' enumeration -> one-line fix dispatched; residual (b) prettier-dirty backlog doc confirmed pre-existing, not blocking.

== TRUE-COLOUR PLUTO (user request, 2026-08-17) ==
Spike verdict VIABLE WITH CAVEATS — and it DISPROVED the controller's proposed method. Uniform chroma scale FAILS (albedo-dependent required scale spans 3.1x; Cthulhu goes pink, dE 11). A 2x2 linear chroma matrix DOES invert the PCA stretch: M = [[1.0354,0.3565],[-0.0686,0.1579]], gain k=0.958, singular values 1.095/0.172 (6.4x anisotropy = why one scalar can't work). Held-out R2 0.876/0.884. Mean per-region dE vs the natural-colour disc: raw 10.33 -> uniform 7.01 -> matrix 3.25. Anchor residual 0.0046.
Co-registration: gradient NCC peaks at dx=0,dy=0 — plain resize, no warp. Source PIA11707.tif 5926x2963 RGB 29MB (old photojournal path now returns HTML; use assets.science.nasa.gov dam path).
Disc pose solved: sub-observer 164.6E, 31.1N, roll -3.7. LANDMINE: raw-luminance NCC locks onto a shading-driven FALSE peak at 55E; gradient matching + explicit 3D basis fixed it.
Caveats: anti-encounter hemisphere is extrapolation (no ground truth, but no colour excursion — chroma stable across longitude bands); the matrix also absorbs the true-colour product's own processing; Charon has NO colour map and stays monoTint, so the pair diverges in treatment.
Plan: A decomplect the implicit grayscaleTint discriminant into a tagged union (dispatched) -> B register+fetch sources+attribution -> C pan-sharpen build path -> D switch Pluto, rebuild, visual verify.
Colour task A: complete (commit b93a6a901) — grayscaleTint replaced by tagged ColourTreatment ({kind:'colour'} | {kind:'monoTint',tint}) with never-guard dispatch, mutation-verified. Implementer correctly renamed SurfaceTreatment->ColourTreatment ('surface' is already a TextureKind; the tag is per-body). BEHAVIOUR-NEUTRALITY PROVEN BY CONTROLLER not argued: aggregate md5 of every texture+atlas identical before/after rebuild (252f3f910f560d679008d108104dbe0e), bodyAtlas.generated.ts unchanged.
Colour task B: dispatched — register PIA11707 chroma source + true-colour reference, with an explicit instruction NOT to hack TEXTURE_SOURCES if it can't express 'second input file for one body's surface build'.
Colour task B: complete (commits e788fa523 + sidecar cd29abc9f) — PIA11707 verified 30,681,324 bytes and fetched; true-colour ref URL corrected (suggested static path 404s, live dynamicimage endpoint works). Seam: did NOT distort TEXTURE_SOURCES' one-input-per-(body,kind) shape; added CHROMA_SOURCES in fetchTextures.ts mirroring QUADRANT_SOURCES.
Colour task C: dispatched — pan-sharpen build path, with a MANDATORY empirical validation gate (must reproduce disc ratio ~1:0.934:0.853) before commit, and instructed NOT to switch Pluto's row yet so the controller can hash-verify this commit changes no output.
Colour task C: complete (commit b0ffb7fa0) — panSharpen variant added. CONTROLLER-VERIFIED no-op: aggregate texture md5 identical (252f3f910f560d679008d108104dbe0e) before and after a full rebuild on this commit, because Pluto's row was deliberately left on monoTint.
Colour task D: dispatched — flip Pluto's row to panSharpen, keep Charon on monoTint with the asymmetry explained honestly, sweep now-false prose, record as plan Task 8c.
Colour task D: complete (commit f7e7963df). CONTROLLER-VERIFIED: rebuild now logs 'pluto-2048/4096.jpg (pan-sharpened)' and 'charon (tinted)'; shipped pluto-4096.jpg visually inspected and matches the spike prototype. Full suite 6906/6906 GREEN, typecheck GREEN, tree clean, atlas layout unchanged (bodyAtlas.generated.ts byte-identical).
PROCESS FAILURE TO NOTE: tasks C and D overlapped in the same worktree — I dispatched D on seeing C's commit appear rather than waiting for C's REPORT; C then amended (b0ffb7fa0 -> 834a9b734), reshaping ChromaCalibration mid-flight. D adapted correctly and the tree is consistent, but this violated the serial-implementer rule and could have corrupted the index. Cost: none realised; the lesson is that a commit appearing is NOT a completion signal.
C also found rawDataRegistry.test.ts was RED on HEAD (true-colour ref row missing its fetcher field) — my typecheck-only spot checks between full-suite runs would not have caught it.

== OUTSTANDING AT COMPACT (2026-08-17) ==

1. BLOCKED ON USER — remote/local history diverged. Remote worktree-add-pluto-planets holds ORPHANED commit b0ffb7fa0 (amended locally to 834a9b734), so normal push is rejected and `git push --force-with-lease` was DENIED by permission prompt. Local HEAD f7e7963df is correct and complete. Options put to the user: (a) allow --force-with-lease on this draft-PR branch [controller's recommendation], (b) merge origin back in — non-destructive but conflicts between old and reshaped ChromaCalibration types and muddled history, (c) push a fresh branch and repoint PR #563. DO NOT retry force-push without an explicit answer.
2. NOT DONE — the four colour tasks (A decomplect b93a6a901, B sources e788fa523+cd29abc9f, C pan-sharpen 834a9b734, D flip f7e7963df) have had NO task-level code review. The SDD loop was not run for them. A review pass over bc70ad15e..f7e7963df is owed before /feature-done, with the same citation-scrutiny mandate used earlier on this branch (it has already produced one fabricated-citation Critical) plus: verify the fitted matrix/gain and chroma basis are documented well enough to re-derive, and that the not-a-scalar landmine is recorded.
3. NOT DONE — user's Task 13 final visual pass at http://localhost:5175 (dev server shell bn24cn1x9). Pluto now looks MATERIALLY DIFFERENT (derived colour) vs when first requested, so the ask must be re-made. Checklist: textures, InfoCards (dwarf planet / Neverland Regio / 5 moons / 39.5 AU), search + #body-pluto|#body-charon deep links, mutual tidal lock over ~6.4 d sim time, single planet toggle covers both, trail-tint legibility.
4. THEN — /feature-done audit, archive this ledger to docs/superpowers/plans/completed/2026-08-16-add-pluto-charon.ledger.md per sdd-execution Rule 3, rm -rf the workspace, undraft PR #563.
   NOTE: gitignored .superpowers/ task reports (task-colour-\*-report.md etc.) are NOT archived and vanish at Finish — anything durable must already be inline in code/plan comments.

== RESUMED 2026-08-17 (post-compact) ==
Item 1 RESOLVED: user ran `git push --force-with-lease` themselves; origin/worktree-add-pluto-planets forced b0ffb7fa0...f7e7963df. Remote now == local. Orphan gone.
Item 2 IN FLIGHT: colour-work review dispatched (opus) over b93a6a901^..f7e7963df = review-4f85deef6..f7e7963df.diff (5 commits, 73 KB). Mandate: verify every citation at primary source (guilty until proven), check panSharpenRgb's dot(LUM,c)=0 / transfer-function / div-by-zero claims, judge whether ColourTreatment+ChromaCalibration is a real split or a proxy surface, find registered-but-unconsumed dead ends. Report -> task-colour-review.md; findings feed a fix round before /feature-done.
Item 3 RESOLVED: user confirmed the Task 13 visual pass themselves (post-colour-swap, so the derived-colour Pluto is what they signed off on). Task 13's last checkbox condition — "do not mark the plan done until they've confirmed" — is now discharged. Swept one stale "Mordor Macula" left in Task 13's prose (the fact-file sweep had missed the plan's own checklist text).
Remaining before /feature-done: colour review (item 2) verdict + any fix round.

== PLUTO ATMOSPHERE (user request, 2026-08-17) ==
User noticed Pluto has no atmosphere. Correct and deliberate: it was assessment-only at Task 4, never planned. Root cause is one field — AtmosphereParams.mieScatter is a grey SCALAR, and Pluto's atmosphere is visible ONLY as blue haze whose colour is wavelength-dependent Mie scattering off sub-micron tholin aggregates. Rayleigh (the per-channel term we do have) is negligible at ~1 Pa.
User was offered backlog / rayleigh-proxy hack / real shader change, and CHOSE THE SHADER CHANGE knowing it re-opens every existing atmosphere body for visual re-verification.
Blast radius (grepped, complete): AtmosphereParams.d.ts:34, packScatteringParams.ts slot 12, atmosphereParams.ts x7 rows, scattering.wesl (struct + MediumSample.scatterM + sampleMedium 197/200), multiScatterLut.wesl:98, skyViewLut.wesl:148-149, tests/utils/gpu/packScatteringParams.test.ts.
LAYOUT FINDING (controller-derived, implementer told to verify independently): slot 12 is already 16-byte aligned, so vec3 mieScatter occupies 12..14, mieAbsorption fills the tail at 15, the four trailing scalars shift down 4, and BOTH \_pad0/\_pad1 are absorbed. Struct stays exactly 80 bytes / 20 f32; SCATTERING_PARAMS_FLOATS unchanged.
Task ATMO-1 dispatched (sonnet): the widening alone, as a PROVABLE NO-OP — all 7 existing rows splat X -> [X,X,X], NO Pluto row. Report -> task-atmo-widen-report.md.
Task ATMO-RESEARCH dispatched (opus, parallel, writes no source): derive Pluto's constants from primary literature with every value tagged MEASURED / DERIVED (working shown) / LOOK CHOICE. Report -> pluto-atmosphere-research.md. Rationale for the hard rigour bar: this branch already shipped one fabricated citation past two reviews.
Task ATMO-2 (add Pluto's row) NOT yet dispatched — gated on BOTH of the above, so the row is transcribed from researched constants rather than invented.
Task ATMO-1 COMPLETE (commit 18022ff16). Controller-verified from the diff, not the report: offsets recompute to a dense 80 B / 20 f32 with BOTH pads absorbed exactly as predicted; extinction became (mieScatter + vec3(mieAbsorption)) \* dM (correct per-channel form, mieAbsorption stays scalar); scatterM de-splatted at all 3 consumer sites. Implementer's independent alignment check agreed. Suite 6906 green.
Controller swept 2 stale 'grey Mie' comments the implementer missed (multiScatterLut.wesl:96, atmosphereParams.ts:20) + the plan's Task 13 'Mordor Macula' -> commit 2244aa669. NOTE prettier has NO wesl parser — never pass .wesl to prettier.
COLOUR REVIEW verdict CHANGES_REQUESTED, 6 should-fix + 6 nits, ZERO blocking. Reviewer measured rather than asserted (re-derived the SVD, re-ran the NCC grid, measured the clip rate) — its numbers are reusable. Report: task-colour-review.md.
Headline findings: (1) buildTextures docstring still says Pluto has 'no global colour' — the opposite of the branch; (2) 'luminance EXACTLY intact' is false at the gamut clamp — 8,659 px / 0.103% out of gamut, max 79% rel. luminance error; (3) true-colour ref missing its sha256 sidecar line; (4) ref pinned to a dynamicimage transform URL when the canonical PNG returns 200 / 57,098,975 B; (5) the ref is fetched but UNREAD and no fitting script exists, so bodyTextureRegistry's 4 calibration constants are unre-derivable in-repo; (6) the Olkin+17 'enhanced color' quote is verbatim but that paper never mentions PIA11707 and the PIA caption never says 'enhanced'.
RULING on (5): ADD THE FITTING SCRIPT, do not delete the reference — deleting leaves 4 magic numbers with no check, which is worse on a branch that already shipped a fabricated citation. Script must REPRODUCE the documented matrix/gain/R2/dE, not hardcode them; failure to reproduce = STOP, not a tolerance fudge. Cost if wrong: ~100 lines of tool code on a PR that was otherwise closing.
Colour fix round dispatched (opus) — all 12 findings + that ruling. Report -> task-colour-fix-report.md.
STALL WAVE (network, user-confirmed): colour-fix agent and atmosphere-research agent BOTH watchdog-killed at 600s mid-web-fetch, before writing anything. Tree was clean; nothing lost. This is the 3rd/4th environment kill on this branch.
Mitigations now standing for every dispatch on this branch: (a) agents write their report file INCREMENTALLY and are told to abandon a hanging fetch rather than retry it; (b) agents run NO git commands at all — the controller commits — which also lets disjoint-file agents run in parallel with zero index contention.
Re-dispatched as 4 parallel agents, file-disjoint by construction:

- fix-provenance (opus): buildTextures docstrings, textures.sha256 sidecar, rawDataRegistry URL, ATTRIBUTIONS quote accuracy, fetchTextures fold. -> fix-provenance-report.md
- fix-pansharpen (opus): the EXACTLY-intact claim + clamp figures, the y>1e-5 guard, the 15-line header, and the mirror test (mutation-verification MANDATORY). -> fix-pansharpen-report.md
- fix-fitting-script (opus): the new tool making the 4 calibration constants re-derivable; must COMPUTE the documented matrix/gain/SVD/R2/dE, not print them; mismatch = STOP and report, never widen a tolerance or edit the shipped constants to match. -> fix-fitting-script-report.md
- atmo-research (opus): re-dispatch of the literature derivation. -> pluto-atmosphere-research.md
  fix-pansharpen COMPLETE (commit 4f71c5048). Findings 1/2/3/4 all addressed; mutation-verified twice (dropped upper gamut clamp -> clamped rows RED while all 5 in-gamut assertions stayed GREEN, proving the OLD test file could not have caught it; transposed m01/m10 -> 2 rows RED, proving the frozen table pins matrix orientation).
  Implementer RULED on finding 2 and I accept it: corrected the comment, LEFT the 1e-5 threshold. Reason: a large |c| is not an error — c is re-attached to the PAN luminance, so amplified near-black chroma yields a saturated hue the clamp already handles, not a luminance blow-up. Raising it would blank real hue on 0.04% of the disc and alter an already-visually-approved texture to fix no defect.
  Header cut 15->10 lines. Two removed clauses want re-homing in the plan (controller TODO): (a) why an RGB/Y-1 carrier was chosen over photometric matching; (b) the long form of the caller's alignment obligation.
  CARRIED CONCERN: tsconfig.tools.json reports 3 pre-existing 'Object is possibly undefined' errors in tools/textures/fitPlutoChroma.ts (316,317,468) — that is the fitting-script agent's IN-PROGRESS file, not a regression. MUST re-check tools typecheck once that agent lands.
  fix-provenance COMPLETE (commit e16358916). All 6 findings fixed, every source opened and verified first-hand.
  Notable corrections to OUR OWN earlier claims: (i) the old comment said the flat BIG\_...\_1980.jpg path 404s — FALSE, it returns 200 (image/jpeg, 1,132,352 B); the dynamicimage pin was never justified. (ii) Olkin+17's "enhanced color (not natural color as perceived by the human eye)" is verbatim but describes THE PAPER'S OWN figures, and the paper never mentions PIA11707; the PIA caption never says "enhanced" either. The claim is now stated as an inference with two grounds, the second being our own measurement (independently re-derived SVD sigma = 1.09514 / 0.171619, ratio 6.381).
  PROCESS LAPSE: the agent downloaded the 57 MB canonical PNG without the size-and-go-ahead announcement the user requires. Ledger it; do not repeat in later dispatches.
  OPEN CONFLICT I CREATED BY PARALLELISING: finding 4 re-pointed textures.nasaPlutoTrueColorRef from the 1980-px JPEG to the 8000x8000 canonical PNG, but the shipped calibration constants were fitted from the JPEG. The concurrent fitting-script agent resolves through rawDataPath(), so it now fits against the PNG and may not reproduce them — and I instructed it to STOP rather than fudge on mismatch. Cannot warn it mid-run (no SendMessage to running subagents).
  RULING PREPARED for when it reports: if the re-fit shifts the constants only negligibly, keep the shipped constants and record the tolerance. If it shifts them MATERIALLY, the PNG is the better ground truth (the JPEG is a lossy resampled derivative), so re-fit, rebuild Pluto's texture, and go back to the user for a fresh visual pass — do NOT keep constants fitted from the worse source merely to protect an existing approval. Cost if wrong: one texture rebuild + one visual pass.
  ATMO-RESEARCH COMPLETE -> pluto-atmosphere-research.md. Every value tagged MEASURED / DERIVED (arithmetic shown) / LOOK CHOICE.
  Key numbers: mieScatter [1.85e-4, 3.83e-4, 8.25e-4] (anchor tau_vert 0.013 over a 50 km scale height, Gladstone+16, at LORRI pivot 607.6 nm; colour from the MEASURED MVIC blue/red I/F 0.75/0.30 = 2.5 across 475/620 nm -> lambda^-3.44, evaluated at 680/550/440). mieScaleHeightKm 50 (measured, Gladstone+16 "typical brightness scale heights of ~50 km"). miePhaseG 0.5 solved to reproduce the measured forward lobe P(165 deg) ~ 5 — NOT Earth's 0.8, because HG must hit the lobe's HEIGHT. atmosphereTopKm = R + 250. Rayleigh kept non-zero but ~300x below the haze (honest value, invisible). No ozone.
  Honest caveats recorded: one HG lobe is 3.5x too bright at 148 deg and has no backscatter lobe; the ~20 discrete haze layers are not modelled; groundAlbedo rides the seed (0.49) which is darker than the measured geometric 0.62 — a ~1% term through the multi-scatter bounce, so the SEED is the thing to fix, not this row.
  VISIBILITY EXPECTATION (sets the visual check): thin ring, BACKLIT, phase >~160 deg, camera anti-sunward, within ~8000 km (~7 Pluto radii) for ~20 px at 1080p. Below ~100 deg phase it is 1-3% of the sunlit disc = invisible. Charon gets NO row (LORRI 3-sigma limit is 7000x below Pluto's haze).
  PRE-EXISTING RENDERER BUG found by the research and CONTROLLER-VERIFIED independently: densityOzone computes 1 - abs(alt-center)/width; SIX rows (venus/mars/jupiter/saturn/uranus/neptune, verified by grep) use center=0/width=0 as the no-ozone idiom, and sampleMedium clamps altitude to EXACTLY 0 at/below the ground sphere, so 0.0/0.0 = NaN is evaluated on routine samples TODAY. Benign only by accident: WGSL max(e1,e2) returns e1 when the comparison is false and the call happens to read max(0.0, NaN). Swap the argument order and six bodies propagate NaN into extinction and blow the LUT bake.
  Dispatched (opus): Pluto's row (transcribe, do NOT re-derive) + an explicit zero-width ozone guard, with instructions to re-verify my NaN analysis from the WGSL spec before fixing, and to confirm no closed list/hardcoded id gates which bodies draw an atmosphere. -> task-atmo-row-report.md
  fix-fitting-script COMPLETE (commit 0b430f1a5, tools/textures/fitPlutoChroma.ts + npm run fit-pluto-chroma). Method reproduces; last two digits do NOT.
  Measured cause (not guessed): the fit averages only gradient-quiet tiles, so the tile population tracks the reference rendition's noise floor — 2389 tiles on the JPEG vs 2769 on the PNG; a q95 re-encode alone moves entries +/-0.002. The spike's exact reference bytes were deleted, so bit-reproduction is unrecoverable.
  Everything DERIVED does reproduce: 6.3x anisotropy, sigma2 = 0.172 exactly, dE ranking (matrix ~3.2 << uniform ~7.0 << raw ~10.3), anchor residual 0.0048 vs 0.0046, and 0 bytes' difference against the shipped panSharpenRgb (so basis + matrix orientation are confirmed identical).
  RULING 1 — do NOT re-fit on the PNG. Shipped dE 3.16 vs fresh 3.13 is invisible, and re-fitting costs a texture rebuild plus a fresh user visual pass on an already-approved image. The tool's OUTCOME gate (within half a dE of a fresh fit, and still beating uniform) is the right invariant and it passes. Recorded inline at bodyTextureRegistry so the digit drift doesn't alarm a future reader.
  RULING 2 — keep the orphaned 1980-px JPEG on disk. It is the closest surviving rendition to what was actually fitted; deleting it would destroy the only near-reproduction. Unregistered is fine, the tool takes --reference.
  ATMO-ROW COMPLETE (commits 66dba7e92 ozone guard, 6395f5225 Pluto row).
  The agent CORRECTED my ozone analysis from the normative spec (downloaded gpuweb/wgsl/index.bs rather than trusting recall): it is NOT benign-by-argument-order and NOT confined to altitude 0. WGSL's Finite Math Assumption lets implementations assume no NaN/Inf and makes any such expression an INDETERMINATE value, explicitly naming min/max as builtins that misbehave under that optimisation. Above altitude 0 the six rows produced +Inf, hitting the same clause. So EVERY ozone sample on EVERY non-Earth body was formally undefined, not just ground-level ones. Guard is now an explicit widthKm <= 0 early return.
  Draw-path gates confirmed necessary AND sufficient for Pluto: no closed list anywhere (Record lookup / Map / Object.entries); the only per-id branch in the whole path is Earth's exposure slider; near-field gate uses camera-to-TARGET distance so 39 AU is irrelevant; shell ratio 1.21 safe by construction (LUTs parametrise on ratios); summed extinction at shell top ~1.25e-6 clears EXTINCTION_EPS 1e-7 by 12.5x (Earth's Rayleigh already crosses that floor at 100 km today).
  Pre-existing staleness left alone, worth a later sweep: EngineGpuHandles.d.ts:490-507 still says "Earth today; Mars / Venus / Titan opt in later".
  ALL 4 PARALLEL AGENTS LANDED. Commits this wave: 4f71c5048 (pansharpen claims+test), e16358916 (provenance/citations), 66dba7e92 (ozone guard), 6395f5225 (Pluto haze row), 0b430f1a5 (fitPlutoChroma tool), 71eadcc26 (backlog item).
  Gates: full suite 6909/6909 GREEN (up 3 from 6906), tsc clean on BOTH tsconfigs, prettier clean. NOTE prettier has no .wesl parser — never pass one to it.
  USER QUESTION ANSWERED (are the other planets' values right?): NO, and 4 of 6 do the exact hack the user rejected for Pluto. rayleighScatter is being used as a per-body colour dial because until the vec3 widening it was the table's ONLY per-channel knob. Venus [12,10,7]e-3 and Mars [8,5,3]e-3 DECREASE toward blue, which lambda^-4 forbids; Uranus/Neptune fake methane ABSORPTION as scattering while the per-channel ozoneAbsorption vector sits at [0,0,0] in all six rows. Rows are honest about being eye-tuned and carry no numeric tests, so this is misplaced physics, not a wrong picture.
  Filed as backlog rather than expanded into this PR: docs/backlog/2026-08-17-atmosphere-params-physical-channels.md + index line under Rendering. Includes the EngineGpuHandles.d.ts:490-507 staleness and the groundAlbedo-seed question as same-pass sweeps.
  OUTSTANDING: (1) user's visual pass on Pluto's haze — backlit, phase >160 deg, camera anti-sunward, within ~7 Pluto radii; below ~100 deg phase it is CORRECTLY invisible. (2) then /feature-done, archive this ledger to docs/superpowers/plans/completed/, rm -rf workspace, undraft PR #563. (3) plan needs a Task 14 recording the atmosphere work + the two re-homed panSharpen header clauses (why RGB/Y-1 over photometric matching; the caller's alignment obligation).
  VISUAL PASS ON THE HAZE — user reports the ring is visible from the BACK side and absent from the FRONT. That is the model reproducing itself, not a bug: HG at g=0.5 gives 0.478 backlit vs 0.0177 front-lit, a 27x ratio, on top of tau_vert 0.013 competing with a lit disc at geometric albedo 0.62. It is exactly why the famous blue-haze image is the DEPARTURE shot. If it were visible from both sides, miePhaseG would be wrong.
  User RULED: leave it as-is (correct) rather than adding a two-term backscatter lobe or boosting the look. The missing backscatter lobe stays recorded in the row as a known caricature — real fractal aggregates have a modest one, so the front-lit case is likely slightly under-represented.
  Plan updated with Task 14 (commit 0fe40fb3c): the atmosphere work + the derived-colour DoD entries + the re-homed panSharpen header clause (why RGB/Y-1 over photometric matching). Plan is now the record of what shipped.
  NEXT: /feature-done audit -> archive this ledger to docs/superpowers/plans/completed/2026-08-16-add-pluto-charon.ledger.md (sdd-execution Rule 3) -> rm -rf workspace -> undraft PR #563.
