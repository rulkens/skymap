# SDD ledger — plan: docs/superpowers/plans/2026-08-20-fade-rows.md

Branch: refactor/fade-rows (worktree /Users/rulkens/Development/js/skymap/.claude/worktrees/fade-rows)
Base: 577ed2571 (main) · plan commit ac66e41d0 · draft PR #610 (already open — T8 amends, does not create)
Spec authority: the plan itself carries decisions D1–D15 + amendment log; no separate spec (rung mini-plan pattern, per decisions.md #9). Ledger rulings resolve against the plan's Decisions section.
User gates: T7 commit requires the user's visual ruling (D9). Merge only on the user's explicit word.

## Pre-flight conflict scan (2026-08-20)

Shared-file pairs:
| pair | producer/consumer | finding |
|---|---|---|
| T1/T7 · volumeLiveness.ts | T1 collapses :93 canonical call; T7 migrates :114; :87 untouched | consistent — §4 notes the split; T1 lands first so T7 edits post-T1 line numbers |
| T1/T7 · resolveLayerOpacity | T1 mints 3-arg signature; T7's 7 migrated sites call it | correct order (T1 before T7) |
| T3/T4 · visibilityActionRow.writes | T3 mints `writes`; T4's structural test reads it | correct order (T3 before T4) |
| T6/T7 · decisions.md | T6 writes #17 body; T7 appends the D9 ruling closing line | sequential appends, no overlap |
| T3 within-task | deletes FADE_ROW table+15 imports from watchFadesSaga, keeps mergeSnapshot (:44) used at :92 | self-consistent (S2 amendment) |
| T4 within-task | deletes fadeLayers.test.ts:380-408, keeps describe opener :379 | self-consistent (S1 amendment) |

Per-task self-consistency: T1–T8 each re-read — tests specified match code specified; file lists match §4 (src 19 / tests 10 / docs 4 / deleted 1). No task contradicts Global Constraints; T3's ≈+7 LOC exception is stated in §2 (new finding 1), so a positive T3 diff is NOT a defect.
Rubric-vs-plan check: T3's derived-table test and T4's two-table correspondence test could read as registry restatements; the plan pre-argues both (M5 note requirement, D3). If a reviewer flags them, adjudicate against plan text — do not pre-judge in dispatches.
Known plan typos (cosmetic, do not execute): T8 has a duplicated sentence at plan :882-883; amendment-log Result block has a prettier-mangled bold at :1153-1155. No action.
Scan verdict: clean — no contradictions requiring pre-execution rulings.

## Progress
Task 1: dispatched (BASE ac66e41d0, implementer sonnet, brief task-1-brief.md, report task-1-report.md)
Task 1: implemented — commit ede8bde62; full suite 1055 files/7114 tests green; controller re-verified typecheck clean (stale editor diagnostics in tour.integration.test.ts were mid-run snapshots, not real). Implementer deviations (flagged, mechanical): 5th direct caller in tour.integration.test.ts migrated; 8 fixtures gained a clipPlayer stub (param no longer optional). Review package review-ac66e41d0..ede8bde62.diff.
Task 1: complete — review task-1-review.md: Spec ✅ / Quality APPROVED, zero fix rounds. Deviations judged correctly handled. Parked (INFO, pre-existing): focusRecession.ts whole-file comment/code ratio ~1.3 exceeds ≤half convention — protected row comments + header-only scope make it out of T1's reach; carried for a later rung, not a defect here.
Task 2: dispatched (BASE ede8bde62, implementer sonnet, brief task-2-brief.md, report task-2-report.md)
Task 2: implemented — commit 454a03d1f; filter 33/33, full suite 7113/7113, typecheck re-verified clean by controller (another stale editor diagnostic). No concerns. Review package review-ede8bde62..454a03d1f.diff.
Task 2: complete — review task-2-review.md: Spec ✅ / Quality APPROVED, zero fix rounds, net −21 LOC, nothing parked.
Task 3: dispatched (BASE 454a03d1f, implementer sonnet, brief task-3-brief.md, report task-3-report.md). task-4-brief.md pre-written.
Task 3: implemented — commit e28c487cf; filter 43/43, full suite 7114/7114, typecheck re-verified clean by controller. No concerns. Review package review-454a03d1f..e28c487cf.diff. Briefs 5, 6, 7 pre-written (T6 points implementer at plan §0 + D1–D15 as transcription source; T7 brief carries the do-not-commit gate + failing-test-first mandate).
Task 3: complete — review task-3-review.md: Spec ✅ / Quality APPROVED, zero fix rounds. Reviewer independently verified the 15↔15 hand-table↔writes correspondence (zero mismatches), the 3 null rows, byte-identical actions bodies, all 6 call sites, net +8 LOC within the ≈+7 tolerance. One INFO (report wording imprecision, not a code defect).
Task 4: dispatched (BASE e28c487cf, implementer sonnet, brief task-4-brief.md, report task-4-report.md)
Task 4: implemented — commit 758f9cfc2; fadeLayers 28/28, full suite 7114/7114, typecheck re-verified clean by controller. No concerns. Review package review-e28c487cf..758f9cfc2.diff.
Task 4: complete — review task-4-review.md: Spec ✅ / Quality APPROVED, zero fix rounds, net −15 LOC. Reviewer confirmed the invariant is bidirectional, non-vacuous (15/3 split exercised), and would have caught the original 4-key drift.
Task 5: dispatched (BASE 758f9cfc2, implementer sonnet, brief task-5-brief.md, report task-5-report.md)
Task 5: implemented — commit 5a658d46d; fadeLayers 28/28, typecheck clean, comment-only diff verified. Review package review-758f9cfc2..5a658d46d.diff.
Task 5: review task-5-review.md — Spec ❌ / CHANGES REQUIRED, 1 blocking finding: both landmine one-liners drop the "intent path works" contrast the brief mandates (the reason the rows are kept). Fix round 1 of 5: original implementer resumed; commit AMENDED in place → 08e7755e2 (T5 stays one commit; earlier SHA 5a658d46d is dead).
Task 5: complete — re-review round 1: Finding 1 RESOLVED, no new issues; Spec ✅ / Quality APPROVED. Final commit 08e7755e2.
Task 6: dispatched (BASE 08e7755e2, implementer OPUS — research-record prose, brief task-6-brief.md, report task-6-report.md)
Task 6: implemented — commit 3dff5c55f, DONE_WITH_CONCERNS. Rulings on the 5 concerns:
- Ruling: rung 7's decision is **#18, not #17** — #17 was taken by the umbrella-deferral entry (PR #606, 0929f4648) before this plan executed; the plan's "list stops at 16" was stale at authoring. Accept #18; do NOT renumber #606's entry (two live backlog files cite it). Cost if wrong: none — self-consistent record. EVERYWHERE the plan/briefs say "decision #17" for rung-7 rulings now means **#18** (T7 ruling line appends to #18).
- Ruling: 3 landed source comments say "decision #17" and now point at the wrong entry (focusRecession.ts:9 [T1], fadeLayers.ts:151+:164 [T5]) — fixed as a standalone one-word×3 comment-only commit BEFORE T7 (not folded into gated T7, which must stay minimal). Dispatched as micro-task.
- Ruling: concern-3 extra doc edits (fade-row-totality-question link repair, contracts-map §6/§1 FADE_ROW rows) ACCEPTED — they repair claims this rung falsified, D1's spirit; reviewer verifies.
- Ruling: rg-not-empty ACCEPTED — all link-shaped references gone; remaining mentions are the plan itself + historical records, which is correct.
- Noted: decisions.md not prettier-clean pre-existing; implementer kept the diff a pure insertion. Fine (format touched-only policy).
Task 6: reviewer (OPUS — transcription-fidelity review vs plan §0+D1–D15) IN FLIGHT → task-6-review.md, package review-08e7755e2..3dff5c55f.diff. Micro-fix implementer (sonnet) IN FLIGHT concurrently: 3 comment pointers #17→#18 (focusRecession.ts:9, fadeLayers.ts:151/:164), own commit — safe alongside reviewer (reviewer reads only the frozen diff file). On both done: completion line, then dispatch T7 (brief task-7-brief.md — implementer does NOT commit; user visual gate follows; ruling line appends to **#18**).
PUSHED at user request: ac66e41d0..3dff5c55f → origin/refactor/fade-rows (fast-forward, PR #610 updated). Later commits push as they land.
Micro-fix: complete — e0f1686af (3 comment pointers #17→#18), pushed.
Task 6: review task-6-review.md — Spec ✅ / Quality CHANGES REQUIRED: 2 medium (park-safety leaks into #18's title + header ¶3; outliers row label re-asserts half the falsified clip claim), 3 low (dead-set line-number pairing, column-0 indent residue, 2 stale clauses in fade-row-totality-question.md). Reviewer independently confirmed the rest clean (21-site count, dead set, #17 untouched, register). Fix round 1 of 5: opus implementer resumed — NEW commit 663723914 (3 doc files, +25/−22, all 5 findings addressed).
Task 6: complete — re-review round 1: all 5 findings RESOLVED, no new issues; Spec ✅ / Quality APPROVED. Commits 3dff5c55f + 663723914, both pushed. Rung-7 decision entry = **#18**.
Task 7: dispatched (BASE 663723914, implementer OPUS — the one behaviour-changing task, brief task-7-brief.md, report task-7-report.md). Implementer does NOT commit; on its DONE → controller verifies suite, starts dev server, CHECKPOINTS USER for D9 visual ruling (land / re-tune / park) BEFORE any commit. Park path: discard the 7 source edits + test via git checkout --, append parked-ruling line to #18 as doc-only commit.
Task 7: implemented UNCOMMITTED — full suite 1055 files/7116 tests green, typecheck clean; failing-first evidence recorded for both new tour assertions. Working tree: 7 src + 7 test files, +177/−31 (diff snapshot review-worktree-t7.diff). Implementer concerns (5): (1) beat D never fires in harness — clip ends ~20.5s but beat D cues at 23s (authored 26s timeline; ~5.5s missing, suspect B-block settling) — CHECK ON DEV SERVER at user checkpoint; (2) focusBlend undefined ⇒ NaN alpha via lerp — PRE-EXISTING class, widened to 5 more layers, deliberately not guarded (would be out-of-scope growth) — backlog candidate; (3) renderFrameSplitBaseline fixture gained zoa:null, pinned snapshot byte-identical; (4) volumeField × volumesMaster clip channels now compose multiplicatively — no current clip cues both; (5) no perf run — one map lookup + clipOpacityOf per site per frame, point-sprites per-source not per-galaxy.
Task 7: review task-7-review.md — Spec ✅ / Quality APPROVED (disclosure findings, no diff changes required). Reviewer independently verified 7 migrated / 14 untouched sites, recession provably 1, snapshot byte-identical. Adjudications: F1 beat D UNREACHABLE — compileClip yields durationSec=20 with cue at 20 (fade cues await 0s; 26s authored → 20s compiled), fires on completion tick + reset-wiped — clip-authoring BACKLOG item, not this diff; F2 MW pick asymmetry (milkyWayLayer:95-99 pickEnabled over resolved opacity) — one recorded sentence in #18; F3 test name at tour.integration:423 — renamed pre-commit; F4 zoneOfAvoidanceLiveness:22 `=== null` lets undefined through (real cause of fixture drift) + concern-2 NaN class — BACKLOG item with F1's; F5 comment budget produceMilkyWayLabel/milkyWayCloudLiveness pre-existing over — /comment-audit at T8. Concern 3 NON-ISSUE (distinct keys), concern 4 perf ACCEPTABLE (per-source, 0-opacity now skips source).
Task 7: USER RULED **LAND** (D9 checkpoint, dev server :5176; user had the beat-D caveat in hand).
Task 7: complete — commit 3e0e4c0e6 (15 files: 7 src + 7 test + decisions.md; test renamed per F3; #18 closed with LAND ruling + beat-D compile finding + pick-asymmetry record), PUSHED. Working tree clean. decisions.md prettier-dirty is pre-existing, untouched by design.
Backlog filing: complete — 7ac9e45d7 (2 items: cosmicflows-beat-d-unreachable under UI&UX/tour, liveness-undefined-guards under Engine&State), PUSHED.
T8 in progress: smoke = attested by user's D9 LAND ruling this session; PR #610 body FINALIZED (cites #18, T7 as the one non-neutral tip commit with the ruling, code net −84 neutral / +178 T7, follow-ups listed; body file scratchpad/pr610-body.md). /comment-audit agent (OPUS) IN FLIGHT over the branch's 42 code files (protected-list in dispatch; report → comment-audit-report.md, own commit if changes). On its report → push if commit → FINAL WHOLE-BRANCH REVIEW (SDD skill mandate, most capable subagent model = opus; package 577ed2571..HEAD; one fix dispatch max + scoped re-review, residuals adjudicated) → /feature-done audit → MERGE ONLY ON USER'S WORD.
Adjacent finding (pre-existing, NOT this branch): fadeLayers.ts:32 imports BODY_IDS but nothing reads it — already unused on main 577ed2571 (only a comment at :82 mentions it). Out of rung 7 scope; surface to user at T8/PR time (candidate one-line cleanup or backlog).

## Session state (compact resume notes, 2026-08-20)
- IN-FLIGHT: Task 1 implementer (sonnet) running in this worktree; on its report → run review-package (BASE ac66e41d0 → HEAD) → task reviewer → fix loop per sdd skill → ledger completion line → dispatch T2. Briefs are hand-authored (task-brief script doesn't match "### T1" headings); write task-N-brief.md per task from the plan text.
- Controller session home: THIS worktree (fade-rows). Dev server RUNNING for THIS branch: http://localhost:5176 (shell bk854ow70); public/data symlinked → main's (13 entries, /link-data done, git-noise excluded). The :5174 server is the OLD worktree — wrong branch, don't use.
- T7 is USER-GATED (D9 visual ruling on tour beats) — checkpoint before commit; park = doc-only ruling line.
- Board artifact (ludens account, keep URL): https://claude.ai/code/artifact/f3396fdd-c9c2-4023-b890-41bb1c791988 ← republish scratchpad/engine-ladder-board.html.
- OPEN USER DECISIONS (not rung 7): (a) teardown mini-PR go/no-go — proposed shape: rebuildGpuHandles(rows,state,deps) helper collapsing buildSwapRenderers.ts:43-48 inline loop + 6 missing labels + record closures for anomalies 1 (BGLs: no destroy() in spec, cosmetic) & 2 (timingService re-stub, conceptual); evidence scratchpad/teardown-anomalies-survey.md. (b) /feature-done hardening: parked ledger findings must land as backlog entries or named carries before READY.
- MERGED THIS SESSION (user-authorized): #606 decision #17 (0929f4648), #607 backlog sweep (577ed2571). Agent worktrees agent-a488…/agent-a514… await skymap-wt-clean.sh.
Comment audit: complete — commit 6f3f5fb87 PUSHED (41 files audited, 40 fixed, ~2208 del/562 rewritten; typecheck + full suite green; diff verified comments-only). Two files stay over budget on mandated keeps (syncVisibilityFades 31/47, focusRecession 34/54). Flagged for user at wrap-up: (1) starCatalog row comment in focusRecession is 8-line spec-ish but inside keep-set; (2) prettier violations on 2 branch files (visibilityActionRow.ts FADE_ROW filter, applySceneEffect.test.ts volumeField cast) — FIX IN FINAL REVIEW FIX DISPATCH (lint gate hard-fails CI); (3) fadeLayers LANDMINE lines ~200-char, rung-8 clause is schedule state if rewrapped; (4) galaxyPointSpritesLayer 0xFFFFFFFF safety note deleted — candidate move beside constant in selectionEncoding.ts, user call. Full report comment-audit-report.md.
Final whole-branch review: dispatched (OPUS, background; package review-577ed2571..6f3f5fb87.diff, 12 commits, 489KB; report → final-review-report.md). On report: ONE fix dispatch max (fold in the 2 prettier fixes regardless) + scoped re-review, adjudicate residuals → /feature-done audit → MERGE ONLY ON USER WORD.
Final whole-branch review: report final-review-report.md — Spec ✅ / Quality CHANGES REQUIRED: 1 blocking (comment-audit tip 6f3f5fb87 shifted lines under ~18 file:line citations in #18 + contracts-map + 2 backlog files, several past EOF; mapping table in report), 3 medium (foregroundLabelsLayer demand-gate comment asserts pre-T7 invariant; galaxyPointSpritesLayer pick comment "invisible→unpickable" false + no D8 pointer left; syncVisibilityFadeItem opts dead surface), 1 low (plan says #17 in 29 places, :883 fragment, 0/48 boxes ticked, DoD rg-gate wording), 3 trivial. Reviewer verified clean: suite/types/eslint, structural neutrality, 11 canonical/15 raw partition, Track C keys, FADE_ROW derivation acyclic.
Fix round (THE one allowed): RULINGS — F4 opts param REMOVED per D11 own argument (behaviour-neutral, both callers pass {}); F5 full #17→#18 pass + header renumber note + tick 48 boxes + reword rg-gate line; pointer re-anchor prefers SYMBOL anchors in churning files, 2 historical pointers kept marked historical; prettier 2 known files folded in; starCatalog comment + 0xFFFFFFFF re-home stay OUT (user adjudication). Brief final-fix-brief.md, report final-fix-report.md, implementer SONNET, one commit, no push. Then scoped re-review → /feature-done → MERGE ONLY ON USER WORD.
Fix round: DONE — commit cc38cec22 (14 files), typecheck+suite+prettier green (controller re-verified typecheck). Implementer judgment calls, all sound: 3rd stale contracts-map pointer found+fixed; focusRecession "not-final placeholders" marked as a 3rd historical pointer (its text also audit-deleted); pre-existing markdown prettier drift on 3 files left untouched (on HEAD before the round). Commit NOT pushed — held until scoped re-review resolves (preserves amend path via resumed fixer).
Scoped re-review: dispatched (SONNET, package review-6f3f5fb87..cc38cec22.diff; verifies all re-anchors land on cited symbols, comment rewrites timeless+budgeted, opts removal neutral, plan pass complete, out-of-scope untouched). On RESOLVED → push cc38cec22 → /feature-done audit → MERGE ONLY ON USER WORD. Residuals adjudicated, no second fix dispatch.
Scoped re-review: ALL RESOLVED (6 findings + prettier), NO new defects. Every re-anchor spot-checked against today tree; 3 historical markers read as historical; opts removal complete incl. a 4th test caller (galaxyCatalogSourceRegistryFade.test.ts) the brief missed; plan diff = only the mandated edits; full suite 1055/7116 green + typecheck clean at cc38cec22. NOTE: syncVisibilityFades.test.ts lives at tests/services/engine/wiring/ (controller brief path tests/state/settings/ was wrong; harmless). cc38cec22 PUSHED. Next: /feature-done audit → MERGE ONLY ON USER WORD.
/feature-done audit: READY — tests 1055/7116 PASS, typecheck PASS, checkboxes 48/48 (DoD present), no unexpected files (whole-branch reviewer verified scope; plan has no File Structure section — rung layout), 0 new TODOs, comment audit ran (2 files over on adjudicated keeps), test parity grew (+118 T7 regression lock, structural tests verified failing-on-defect), smoke = user D9 LAND ruling this session, deferred = 3 backlog filings (beat-D, liveness guards, fade-row totality) + rung-8 caption-wire handoff per #18 D12. No spec existed (plan argues from decisions.md #18) — noted per skill backstop. Housekeeping: plan → completed/, ledger archived as completed/2026-08-20-fade-rows.ledger.md, commit + push. MERGE OF PR #610 ONLY ON USER WORD.
