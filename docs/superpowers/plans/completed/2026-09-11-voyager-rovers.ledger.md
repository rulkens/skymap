# SDD ledger — plan: docs/superpowers/plans/2026-09-11-voyager-rovers.md

Spec: docs/superpowers/specs/2026-09-11-voyager-rovers-design.md (binding). Branch worktree-voyager-rovers (worktree .claude/worktrees/voyager-rovers), based on worktree-whale-petunias-mesh-bodies bfa77e980 (PR #678 unmerged). Draft PR #691 targets that branch; retarget to main after #678 lands. Design commit 8c6d57d93 (cherry-picked from design agent; its worktree removed). User AFK; controller rules and records here.

## Standing dispatch contract
- Implementers: opus, `isolation:"worktree"` (cut from MAIN → must `git merge --ff-only worktree-voyager-rovers` or reset to the SHA given, then symlink node_modules). They commit on their own branch; controller cherry-picks onto worktree-voyager-rovers and removes their worktree+branch. Reports written in THEIR worktree at `.superpowers/sdd/2026-09-11-voyager-rovers/task-N-report.md`; controller copies here.
- Parallelism 3 (user AFK; feedback_sdd_parallel_tasks says ask — defaulted to the wave width). Reviews pipelined per sdd-execution.md Rule 2.
- Every brief carries: one symbol per file; pass files declare only the pass (passFilePurity ratchet).

## Pre-flight scan (2026-09-11)
| pair / task | shared surface | finding | ruling |
|---|---|---|---|
| 3 ↔ 5 ↔ 6 | deriveBodyStates.ts, orientationForBody.ts | 5 and 6 both build on 3's phase split; 5 adds phase 1c, 6 adds kind dispatch | serialised by waves; 5 and 6 never run together (see next row) |
| 5 ↔ 6 | orientationForBody's `surfaceLocked` arm needs the body's HOST id (row carries only `headingDeg`) and the host's pole = orientationForBody(hostId,…) col 2 | plan lists 6 as depending on 3 only — DEFECT: it also needs Task 5's `bodyHostId` | **Ruling: Task 6 moves to wave 2 (after 5 lands).** `surfaceLocked` arm resolves host via `bodyHostId(id)` from positionDrivers.ts; lookAt arm reads `positions.get(targetId)`. Cost if wrong: none (later wave) |
| 8 ↔ 10 | buildMeshes.ts, meshAssets.generated.ts | 10 depends on 8 (listed) | serial, OK |
| 9 ↔ 5 | meshBodySlabHostId uses `bodyHostId` | listed | OK; watch import cycles: src/data/bodies/hostlessMeshBodies → utils/scene/meshBodySlabHostId → data/bodies/positionDrivers → orbitalElements/sceneAnchors/surfaceFixedSites. sceneMeshBodies must not import hostlessMeshBodies |
| 11 ↔ 12 | disjoint (bodies data vs planet_facts seed) | OK | parallel |
| 1 ↔ 2 ↔ 3 | disjoint | OK | parallel |
| 4 ↔ 7 | disjoint | OK | parallel |
| 2 (self) | test "conic table excludes mesh bodies" is a default-drift guard on a real table | borderline restatement per testing.md but guards the NaN landmine | keep, plan-mandated |
| 12 (self) | `npm run build-planet-facts` — script name to verify at dispatch | — | verify |

Revised waves: W0 {1,2,3} → W1 {4,5,7,8} → W2 {6,9,10} → W3 {11,12} → W4 {13, 14, 15}.

## Asset agent (in flight, in THIS worktree)
Opus, brief asset-brief.md, owns data/raw/meshes/**, tools/meshes/prebake/**, rawDataRegistry mesh rows, package.json prebake scripts; commits directly on worktree-voyager-rovers. Report asset-report.md. Task 10 consumes it. Its uncommitted `tools/meshes/prebake/meshPrebake.py` is in the tree — do NOT stage it from the controller.

## Design-report open questions carried forward
1. Rover headings + bodyFromSource unknowable until assets exist → Task 15 is the real gate (ruling 30).
2. Voyager GLB dish/bus axis separability → asset report must say.
3. MER under 10 MB → asset report; if not, user call (coarser vs bigger).
4. Ruling 12 (dispatch in keplerianPositionMpc, not propagateElements) — accepted by controller: propagateElements' advance is conic-agnostic.
5. Optional `kind` on iau-pole rows (ruling 15) — accepted by controller; user may flip to explicit later (21 mechanical edits).
6. Asset key count 4 vs 5 → asset report.

## Execution log
- 2026-09-11 W0 dispatched: T1 (agent a97fd8f52a0132f00), T2 (ad3f13555b086bb66), T3 (a1ddda167bd7c8981) — opus, isolation worktrees, base 8c6d57d93. Handling on each: copy report from its worktree → here; cherry-pick its commit onto worktree-voyager-rovers (T1→T2→T3 order if all landed; otherwise as they arrive — disjoint files); review-package BASE = HEAD before pick; dispatch reviewer (sonnet for T1/T2, opus for T3); remove agent worktree + branch. W1 {4,5,7,8} dispatch after T1 and T3 are reviewed clean (4 needs 1; 5 needs 3; 7, 8 independent — may dispatch 7+8 immediately if a slot is free).
- T1 DONE: d6f402527 → cherry-picked as 3352cc64a. Report copied. Reviewer dispatched (sonnet, ac3b6f250e24a960f), package review-8c6d57d93..3352cc64a.diff. Agent worktree removed.
- T3 first agent (a1ddda167bd7c8981) STALLED (watchdog, no edits made) → worktree removed; re-dispatched fresh on 3352cc64a.
- Dispatched (base 3352cc64a): T3 (retry), T7 probe maker, T8 groundOffsetM — all opus isolation worktrees. T2 still running from 8c6d57d93 (disjoint files → cherry-pick fine).
- Task 1: complete (review ✅ Approved, sonnet). Minor: keplerianEllipse header pre-existing over budget (45 comment / 19 code) — candidate for deletion audit, not this task.
- T2 DONE_WITH_CONCERNS: 69ac89d2a → cherry-picked as 3c706da3f. Concern = 4 pre-existing load flakes (frameAlignedRoll, cameraRuntimeSingleWriter, galaxyIsmMapArmForcing, catalogStore) reproduced on baseline control, pass in isolation → not this task's. Reviewer dispatched (sonnet), package review-3352cc64a..3c706da3f.diff. Agent worktree removed. Branch HEAD now 3c706da3f; T3/T7/T8 agents based on 3352cc64a (disjoint files, cherry-pick fine).
- T4 dispatched (opus, isolation worktree, base 3c706da3f) — parallelism raised to 4 implementers (T3, T7, T8, T4) since isolation worktrees + disjoint files; controller ruling, user AFK.
- Task 2: complete (review ✅ Approved, sonnet). Minor: sceneOrbitConics header pre-existing over budget — deletion-audit candidate.
- T7 DONE: 3a21323e1 → cherry-picked as ab40c8a02. Reviewer dispatched (sonnet), package review-3c706da3f..ab40c8a02.diff. Agent worktree removed. Concern noted: DAYS_PER_JULIAN_CENTURY now 4 module-private copies (deletion-audit / one-symbol candidate: src/data/time/).
- Task 7: complete (review ✅ Approved, sonnet).
- ASSET AGENT DONE: cb97c4264 on branch (4 READMEs, sha256, meshPrebake.py, registry rows, prebake-mesh script). Sizes: voyager 3.69 MiB, perseverance 5.22, curiosity 5.37, mer 5.82 — all under 10 MB. Axes: rovers glTF Y-up (up +Y, forward +Z, wheels −Y); Voyager dish +Y, mag boom +Z. All in metres. Perseverance + MER pinned at deploy frames. Report asset-report.md.
- ANOMALY: uncommitted groundOffsetM edits (buildMeshes.ts, its test, meshAssets.generated.ts) appeared in the CONTROLLER worktree while T8's agent runs in isolation worktree agent-a0224721d82bea12e — investigating.
- RESOLVED: T8 agent (a0224721d82bea12e) is editing the CONTROLLER worktree (its isolation tree untouched). Files are T8-only (buildMeshes.ts, its test, meshAssets.generated.ts). Ruling: let it finish; if it cannot commit here, controller commits its named paths as the T8 commit. Never stage anything else from this tree. Lesson for later briefs: state the agent's own worktree path explicitly as `$(git rev-parse --show-toplevel)` of ITS cwd and forbid edits under .claude/worktrees/voyager-rovers.
- T4 DONE: 6788956ad → cherry-picked as 11883e245 (on top of asset commit cb97c4264). Extra third test (prograde past periapsis, guards B's sign) — controller accepts: brief's two cases cannot see B at H=0. Separate hyperbolicPositionMpc.test.ts mirrors src — fine. Reviewer dispatched (opus: math-bearing). Agent worktree removed. Note for T11 fixtures: spec's H-at-epoch pairs with the 2026 MA, not M at J2000.
- T3 DONE_WITH_CONCERNS: 4abfd4a02 → cherry-picked as b38d48739. Concern: galaxyIsmMapArmForcing test runs 4.5–5 s vs 5 s default timeout (pre-existing load flake, recurring) — deletion-audit/backlog note. Reviewer dispatched (opus). Agent worktree removed. T5 unblocked (dispatch now, base b38d48739).
- T3 reviewer dispatched (opus). T5 dispatched (opus, isolation, base b38d48739; brief now names the agent's own tree explicitly). In flight: T8 (editing controller tree), T4 review, T3 review, T5.
- Task 4: complete (review ✅ Approved, opus). Two Minor findings fixed in-context by controller (comment inverted the Q̂w sign story; P̂w sign unpinned in tests) — commit follows. Remaining Minor (keplerianPositionMpc header "single evaluation" wording) → deletion-audit pass.
- Task 3: complete (review ✅ Approved, opus). Minor: parallel positions/meanAnomalies maps (structural cost of the split; T5 adds phase 1c and may collapse into one map of {pos, M} if it stays clean) — noted for final review, not a fix round.
- T8 DONE: 91944457e → cherry-picked as 02cdbbe9c (agent had first edited the controller tree, restored it byte-identical before committing in its own; tree clean). whale groundOffsetM 2.136, petunias 0.270; bake binaries unchanged. Reviewer dispatched (sonnet). Agent worktree removed.
- Raw binaries for voyager/perseverance/curiosity/mer COPIED to MAIN checkout data/raw/meshes/<key>/ (deploy bakes from main; isolation agents symlink from there). Originals remain in this worktree (gitignored).
- T10 dispatched (opus, isolation, base 02cdbbe9c; symlinks binaries from main). Expected bodyFromSource: rovers [0,1,0, 0,0,1, 1,0,0]; voyager [0,-1,0, 1,0,0, 0,0,1] (controller derivation; implementer verifies det).
- In flight: T5 impl, T8 review, T10 impl. Next after T5 lands+reviewed: T6, T9 (both wave 2).
- Task 8: complete (review ✅ Approved, sonnet).
- T10 DONE: afab32b8a → cherry-picked as aef15fbf4. Sizes (real bake, .mesh+3 PNG): voyager 4.71 MB, perseverance 8.09 MB, curiosity 6.50 MB, mer 7.11 MB — all under 10 MB. Baked outputs copied into this worktree's public/data/meshes/ (gitignored; deploy re-bakes from main where the raw binaries now also live). Reviewer dispatched (sonnet). Agent worktree removed. Landmine: asset-report .mesh projection under-reads up to 1.75× (tangent generation splits UV-seam vertices). Follow-up candidate: Perseverance authored normal maps dropped (albedo-only prebake).
- T12 (opus) and T13 (sonnet) dispatched, isolation, base aef15fbf4 — both dependency-free now (13 needs 10). In flight: T5 impl, T10 review, T12, T13. T6 + T9 wait on T5.
- T5 DONE: 1a428bf75 → cherry-picked as 6a7ea8c75. Concern: oneMpcSeam allow-list row for deriveBodyStates (M_TO_MPC in frame/) — controller provisionally ACCEPTS (world-position add, not pose/MVP); reviewer asked to weigh. Reviewer dispatched (opus). Agent worktree removed.
- T6 + T9 dispatched (opus, isolation, base 6a7ea8c75). In flight: T5 review, T10 review, T6, T9, T12, T13. T11 waits on 6, 9 (+ 4,5,7,10 landed); T14/T15 after T11.
- Task 10: complete (review ✅ Approved, sonnet). Matrices hand-verified det +1. Watch at visual pass: mer boundingRadiusM 1.20 m (real rover ~1.6 m × 2.3 m span — plausible but check size).
- T13 DONE: 65ce32859 → cherry-picked as b24f8613b. DEPLOY.md covers public/data/meshes/ as a directory (no edit); Splash credits need no edit. Agent worktree removed.
- Task 13: complete. Ruling: docs-only diff reviewed by controller in-context (entries mirror whale/petunias shape; provenance facts match asset-report; MESH_BODY_ENTRY docblock names the category) — no separate reviewer seat; final whole-branch review covers it. Cost if wrong: prose only.
- T12 DONE: 7607a4257 → cherry-picked as 966bdb4f6. Agent worktree removed.
- Task 12: complete. Ruling: seed-data-only diff reviewed by controller in-context (six rows, factual, landing-site caveat present, generated file additive). Rover dayLength = sol kept (the rover's operational day). Ids voyager1/voyager2/curiosity/perseverance/spirit/opportunity → T11 must use exactly these.
- Task 5: complete (review ✅ Approved, opus; oneMpcSeam allow-list ruling UPHELD — a util would dodge the sweep, worse). Controller fixed in-context: surfaceFixed arm = `{kind} & SurfaceFixedSite` (excess-property safety), bodyHostId docblock points away from utils/scene/hostBodyId. DEFERRED to final review/deletion audit: collapse deriveBodyStates' parallel positions/meanAnomalies maps into one (would change orientationForBody's `positions` param type — T6 is editing that file now); positionDriverById linear find (fine, no per-frame caller yet — T9/T11 must not call it per frame).
- T9 DONE: 12d850117 → cherry-picked as 49fe44e9f. Flagged glintBandClass elementsById throw for rovers → controller FIXED in-context 594b9aaa9 (bodyHostId; last elementsById caller outside the table). Out-of-brief generic `visibleSlabBodies<T>` accepted (signature-only). Reviewer dispatched (opus). Worktree removed.
- T6 DONE: 299c0df35 → cherry-picked as 58aa1b270. Deviations accepted: 3 tests narrowed with `in`; rotationFromIau param `Extract<RotationElements, {kind?: 'iau-pole'}>`; ecliptic pole from ECLIPTIC_FRAME.normal. Reviewer dispatched (opus). Worktree removed.
- RULING (spec gap found by controller): bodyRegions' extents are J2000 snapshots assumed time-invariant; a hyperbolic row breaks that (V1 76 au / V2 60 au at J2000 vs Pluto ~30 au) and would ~2.5× SOLAR_SYSTEM_EXTENT_MPC → widens scaleFadeBands.bodyGlintBackdrop from a data append. Ruling: hyperbolic rows (orbit driver, e > 1) stay region MEMBERS (regionOfBody, palette chip) but are EXCLUDED from maxMemberDistanceMpc — a hyperbola has no envelope. T11 implements + one test. orbitReachByRegion: apoapsisMpc negative for e>1 → Math.max ignores it; left as-is (trails exclude the six), note for final review. Cost if wrong: Voyagers sit outside the solar-system fade envelope — which is physically what they do.
- T11 dispatched (opus, isolation, base 58aa1b270) with the ruling + glintBandClass note + fact ids.
- Dev server for THIS worktree: http://localhost:5176 (nohup, log scratchpad/dev-5176.log). Perf BEFORE (HEAD 58aa1b270, pre-T11 rows; prep tasks are no-ops so this is the honest baseline): earth-surface MERGED 7.9 ms (p90 8.1); solar-system MERGED 7.8 ms (p90 8.2). Saved scratchpad/perf-before-{earth-surface,solar-system}.txt. AFTER: same server + flags once T11 lands (HMR), plus a Voyager pose and a rover pose via setPose (no scenario exists; report as extra, not comparable).
- Task 6: complete (review ✅ Approved, opus; math hand-verified). Minor carried to T11/final review: dispatcher arms (lookAt/surfaceLocked in orientationForBody) untested at the integration level — T11's "rover turns with Mars" + Voyager tests exercise both through deriveBodyStates; if a column-index slip in the pole extraction would still pass those, add one orientation assertion in T11's review round. RotationElements.d.ts comment ratio 0.74 (was 25/8) — deletion-audit note.
- Task 9: complete (review ✅ Approved, opus; self-hosted near-plane path and import DAG verified). Controller fixed stale comment in meshBodiesAttachedTo.test.ts. DELETION-AUDIT: `elementsById` now has zero src consumers (tests only).
- T11 DONE: bfc9cbc4a → cherry-picked as 1097685e1. BODY_SLAB_CAPACITY 27; solar-system extent unchanged 30.23 au (76.13 without the exclusion). Deviations accepted provisionally (V2 residual 2.4e-4 au within spec's ±5e-4; quarter-turn cone angle 89.633° derived; surface-fixed sites claimed into host region; conic invariants reworked). Reviewer dispatched (opus). Worktree removed. Dev server reloaded the seed files (HMR log). NEXT: perf AFTER on :5176; headless captures of voyager1 + curiosity; then final review, deletion audit, /feature-done, user visual pass.
- Task 14 perf AFTER (HEAD 1097685e1, same server/flags): earth-surface MERGED 7.7 ms (before 7.8–7.9); solar-system 7.8 (before 7.8–7.9). Deltas inside the ~0.5 ms noise floor → no regression from the slab-capacity widening (25→27). Voyager/rover poses have no scenario; covered by headless captures below, not a perf claim. Task 14: complete (no commit).
- Task 11: complete (review ✅ Approved, opus; every digit re-verified from scratch). Important (README lacked the Horizons query the comment cites) → controller appended it. Minor fixed: test guard admits explicit kind:'iau-pole'; "same design". Deferred to deletion audit: apoapsisMpc docblock false for e>1 / ORBIT_REACH fed ORBITAL_ELEMENTS not TRAIL_ELEMENTS; `hosted` set name in bodyRegions; groundOffsetM lookup duplicated in surfaceFixedSites; spec/plan "1e-4 au" → actually ±5e-4 (V2 residual 2.4e-4).
- HEADLESS CAPTURES (:5176): voyager1 focus OK (card, ring, caption, Voyager 2 caption, 63 m) but mesh reads near-black from the follow pose — no shader host-shadow term (checked fragment.wesl), so it is night-side/albedo; user to judge. curiosity: FIRST capture camera stayed 1.53 au away → ROOT CAUSE bodyFollowsSimClock = ORBITAL_ELEMENTS membership → FIXED 689dcbab1 (POSITION_DRIVERS kind !== 'anchor', + test). Second capture: rover on Mars at 10.7 m, Mars terminator visible; orientation/heading for the user (ruling 30). Cannot reorient headlessly (focus pin overrides setPose yaw/pitch).
- Perf: complete. NEXT: final whole-branch review (fable/opus) + deletion audit in parallel → fix wave → tick plan boxes → /feature-done → PR body → push → memory. All 15 tasks dispatched; T15 = user visual pass (pending user).
- Final review (opus) + deletion audit (opus, legacy framing) dispatched over bfa77e980..5e38cf75c; package review-bfa77e980..5e38cf75c.diff; reports → final-review.md, deletion-audit-branch.md. Handling: ONE fix dispatch (or in-context if small) → scoped re-review; safe-now deletions as ONE commit; needs-ruling → user list in the final message; then tick plan boxes, /feature-done, PR body, push, memory.
- Deletion audit DONE → deletion-audit-branch.md: SAFE-NOW ~131 lines (111 src comments, 4 test docblocks, 16 ATTRIBUTIONS lines; zero code), 12 NEEDS-RULING (user), 8 traps. Apply safe-now as ONE deletion commit after the final review returns (same fix dispatch, separate commits).
- FINAL REVIEW DONE → final-review.md: Critical none; Important (1) ORBIT_REACH fed ORBITAL_ELEMENTS → TRAIL_ELEMENTS, (2) positionDriverById linear scan on per-frame path → Map index, (3) Voyager darkness = NIGHT SIDE by construction (Sun–Earth 0.33° apart at 172 au: dish always sunward, bus always dark) — no code change, Task 15 wording; Minor: probe.ts header 12 lines, 3 stale docblocks, hostless set derived twice. Ruling A (parallel maps): LEAVE. Ruling B (reach table): FIX. Verdict: With fixes.
- Controller rulings on audit needs-ruling: #1 fix (TRAIL_ELEMENTS), #4 collapse atlas knob, #9 fix drifted digit, #5 leave (explicit zeros), #7 keep elementsById (tests-only; reclassify later), #3/#2/#6/#8/#10/#11/#12 → USER.
- Fix-wave agent dispatched (opus, isolation, base 5e38cf75c): commit 1 fixes, commit 2 safe-now deletions. Then scoped re-review (sonnet), tick plan, /feature-done, PR body, push.
- FIX WAVE landed: 8f230ec86 (final-review fixes: reach←TRAIL_ELEMENTS, positionDriverById Map, 3 stale docblocks, probe test digit, atlas constant, apoapsis docblocks) + 136ccc124 (comment-only trims, −136 net). Full suite 1315 files / 9094 green; tsc clean. Plan ticked 1–14 (e5a49b0eb). PR #691 body rewritten (rulings + user ruling list). Scoped re-review dispatched (sonnet).
- /feature-done pre-audit (controller): tests PASS 9094; typecheck PASS; checkboxes 74/83 (9 open = Task 15 visual pass, user); spec Ground-preparation PRESENT; new TODO markers 0; backlog stragglers none (3 items removed in design commit); test parity OK (deriveBodyStates.test 123→130, orbitalElements.test 101→98 with invariants reworked, +23 test files); files beyond plan (all ledger-ruled): HomeFocusTarget.d.ts, OrbitalElements.d.ts, bodyRegions.ts, orbitReachByRegion.ts, glintBandClass.ts, visibleSlabBodies.ts, extractSelectionRow.ts, assetWiring.ts, apoapsisMpc.ts, rotationFromIau.ts, bodyFollowsSimClock.ts, followedBodyHome.ts, meshPrebake.py, package.json. VERDICT: NOT READY until the user attests Task 15 → no completion moves yet. Leanness: −136 net applied (136ccc124); 12 needs-ruling → user.
- Re-review (sonnet): Fix round CLEAN. Residual: probe.ts header 11 lines → controller trimmed to 9, committed, pushed. Branch complete pending Task 15 (user visual pass on http://localhost:5176) → then /feature-done moves (plan+spec → completed/, ledger archive), retarget #691 to main once #678 lands.
- NEEDS-RULING for the user (deletion audit): #2 sites resolve mesh key via SCENE_MESH_BODIES; #3 prebake bounds() keep/delete; #6 shared DAYS_PER_JULIAN_CENTURY; #7 elementsById tests-only (recommend keep); #8 rename bodyHostId/hostBodyId; #10 test helper for `'poleRaDeg' in row`; #11 rotationFromIau narration (−8); #12 frameProgram.ts comment surplus (own pass). Plus: Perseverance normal maps follow-up; spec/plan "1e-4 au" wording; visual-pass item wording (dish side is the lit side).
- 2026-09-11 visual pass (user, live): /link-data done (public/data → main; 4 new baked assets copied into main's public/data/meshes; stale dir parked). User: "zoom in more on the Voyagers?" → floor was 2 × 14.5 m bounding radius (13 m mag boom) = 29 m from a 4 m bus → MeshBodySeed.standoffRadii? override (deviation from spec ruling 26, user-driven), Voyagers 0.5 → floor ~7 m. Committed + pushed.
- RESUME MAP (2026-09-11 ~22:30): HEAD c5174bfa0 pushed, tree clean (only untracked public/data.stale.20260911-2215/ — user may delete). No agents in flight. User is mid Task-15 visual pass on http://localhost:5176 (public/data linked to main). Each visual finding → data edit in T11's files (headings, bodyFromSource, standoffRadii), commit, push, ledger line. AFTER the pass: /feature-done (plan+spec → completed/, ledger archived as docs/superpowers/plans/completed/2026-09-11-voyager-rovers.ledger.md, backlog already clean), then #678 squash-merges first, retarget #691 to main, user squash-merges, deploy from MAIN (build-meshes → sync-r2-secure). User rulings still open: deletion-audit needs-ruling list (previous line), Perseverance normal maps, spec/plan "1e-4 au" wording.
- User (visual pass): mesh bodies flipped the camera into the body-arm surface regime. Root cause: nearestBodyHR walks SCENE_BODIES incl. mesh bodies → with the Voyager standoff at 0.5 radii the eye sits inside its bounding sphere → engage. Fix: nearestBodyHR skips `'meshKey' in body` (one home; also keeps frameAlignedRoll/cameraDofAnglesOf off mesh bodies) + regimeArmFor test. Committed + pushed. Rovers were already safe (focused rover ≠ Mars blocks Mars's engage).

## 2026-09-11 visual pass — orbit drag slows near mesh bodies (user finding)
Root cause: `MeshBody.radiusM` is a bounding-sphere radius wearing the surface-radius name; every altitude-above-surface consumer (hOverR ×5 callers, orbitRadPerPixel, zoomedDistance taper, toWorldArm, anchored drag) treats it as ground. cda345b9a's `'meshKey' in body` skip is a symptom patch.
Proposed proper fix: rename to `boundingRadiusM` (trial tsc: 21 files / 26 sites), draw-footprint readers via `bodyDrawRadiusM`, surface readers narrow to `Exclude<SceneBody, MeshBody>`, `pivotFraming` builds `radiusMpc: null` for mesh rows keeping the standoff floor; delete the regime skip.
Ruling (user): land #678 (whale + petunias) FIRST. Prep refactor deferred; after #678 merges, retarget/rebase #691 onto main, then land the rename as a prep commit on #691 (own-PR vs ride-PR still to ask), then resume Task 15.
Ruling (user): the `boundingRadiusM` rename rides #691 as its OWN commit (not a separate PR), sequenced after the rebase onto main.
Ruling CORRECTED (user, same minute): `boundingRadiusM` rename gets its OWN PR off main after #678 lands; #691 then rebases onto it and deletes the regime skip there.
- 2026-09-12: #678 LANDED (main 6b6df26f4). PREP PR agent dispatched (opus, isolation wt off main): brief bounding-radius-prep-brief.md → draft PR --base main + report bounding-radius-prep-report.md in ITS tree. Handling: opus review of the PR, gate, merge (user or announced API form); then rebase #691 onto main (`git rebase --onto main bfa77e980`), drop cda345b9a (regime skip + its test — prep PR carries the equivalent), fix rover/Voyager `radiusM` seed usages, `gh pr edit 691 --base main`, full gate, resume Task 15 on :5176.
- USER RULINGS 2026-09-12 on the prep PR shape (sent to the running agent): names by the KIND OF THING, never by unit/"surface" → `CelestialBody = Exclude<SceneBody, MeshBody>`, `SCENE_CELESTIAL_BODIES`, `isMeshBody` guard; mesh arm `boundingRadiusM`. SelectionRow body arm carries NO radius/standoff (leftover from Earth-first implementation): `{ type:'body', id, label, positionMpc, focusDistanceRadii? }`; framing/pivotFraming/InfoCard resolve the body by id (sceneBodyById util or existing findByIdOrThrow); card radius row only for celestial bodies. Greenfield ideal recorded for a LATER PR: `bounds: {sphereRadiusM}` on every arm + `surface: {radiusM, oblateness}` on celestial arms only + radius as a BODY_FACTS fact — not now (45 radiusM readers).
- 2026-09-12 user "don't see the models" on :5176 → dev server predated main's 00:53 build-meshes; content-hash rename + Vite's cached dir listing → every .mesh served index.html ("bad magic" in dev log). Restarted :5176 (own nohup), verified SKMH bytes served. LANDMINE: after any bake on main, restart dev servers in worktrees that symlink public/data.
- USER RULING 2026-09-12: PROPER PBR for mesh bodies goes INTO #691 (not a follow-on): prebake metallic/roughness/normal passes for the four NASA models (today: diffuse-only atlas → 1×1 metallic 0 / roughness 0.7 / flat normal), shader metal/dielectric blend (metallic channel currently unread, DIELECTRIC_F0 only) + specular environment. Environment design (my recommendation, see chat): per-body LOCAL cubemap captured from the body's position (host planet + its atmosphere — Mars has one — + nearby bodies drawn over the existing static sky cubemap), low-res faces, mip chain for roughness, split-sum with analytic BRDF approx; Sun stays analytic as a sphere light (dynamic range); capture only while a mesh body is drawn near, faces spread across frames. Process: after the prep-PR rebase, refactor-ground → spec → plan inside this effort (own ledger section), SDD execute, visual pass, land. Placement/navigation user findings still to be described.
- USER CHECK 2026-09-12 (confirmed in code): the "static sky cubemap" is the Sgr A* lens capture only — renderFrame.ts keys it on distance to GALACTIC_CENTRE_REGION, allocates its 50 MB row only inside the sgrAStarLensing band (~500 AU around the GC), near plane 0.1 AU, 1024² faces, captured from the camera near the GC. There is NO sky cubemap at Earth/Mars/Voyager. PBR design correction: the per-body probe cannot draw over it; the star backdrop must come from (a) a solar-system sky bake made once from the mesh body's neighbourhood with the same face machinery (skyCubemapFaceContext generalised to any origin; parallax across the solar system is sub-texel at 64–128 px even at 170 au) — RECOMMENDED — or (b) omitting stars from the probe (radiance negligible vs Sun/host; visible only on mirror surfaces in shadow).
- USER RULING 2026-09-12: split-sum environment BRDF uses a PRE-BAKED 2D LUT texture (N·V × roughness → F0 scale/bias), not an analytic approximation; bake it once in tools/ (e.g. 128–256² rg16float, like the font atlas is a build artifact) or at first use via compute. Probe mip chain should be GGX-prefiltered per roughness level, not plain box mips.
- PREP PR #692 OPENED (draft, base main): 3867e72bd `refactor(bodies): a mesh body carries a bounding radius, not a surface radius`, 67 files +328/−219, 9064 tests green in agent tree. Agent concerns: Radius row hidden for mesh bodies + BodyDetailCard imports SCENE_BODIES; five camera-path sites use linear findByIdOrThrow over SCENE_BODIES (throw site on render path). Report copied: bounding-radius-prep-report.md. Agent worktree agent-a4aa264854fe98282 KEPT for now (reviewer reads it). DISPATCHED opus whole-PR review → prep-pr-review-report.md. Handling: findings → one fix dispatch on the PR branch (isolation wt, ff to 3867e72bd, push), gate, announce + merge #692 (API form), then rebase #691.
- #692 REVIEW: With fixes — 0 blocking/high/medium, 5 low (F1 pivotFraming double resolve; F2 stale docblocks claiming shared/row radiusM; F3 backlog tag `design`→`needs-design`; F4 hull-vs-ground rationale echoed ~10× → keep at MeshBody field + backlog; F5 BodyDetailCard throws in render + resolves unused seed → `.find` + narrow), 2 nits (F6 dead field via cast in fixture; F7 inert expectation), F8 informational = visual-pass items for #691 (NEAR0 near plane ~2× when hugging a mesh, scale bar ~2× step at mesh range, wheel feel on final approach). Reviewer upheld: two accessors (footprint vs draw) justified; Map-backed lookup NOT better (5–10 scans of ~30 rows per frame). CONTROLLER RULING: apply F1–F7 in one sonnet fix commit on the PR branch; F8 → user's pass. Agent worktree a4aa… removed (branch kept = PR head).
- #692 FIX 5b2c29dbe (F1–F7; F5 minimal, BodyInfo alternative rejected — contradicts no-radius ruling), CI green, **#692 SQUASH-MERGED → main b23df53b5**; branches + agent worktrees removed. NEXT: bring #691 onto main via MERGE (not rebase; one conflict pass) — brief rebase-onto-main-brief.md, opus agent, pushes to worktree-voyager-rovers; then `git pull --ff-only` here, `gh pr edit 691 --base main`, restart :5176 if needed, user visual pass (+F8 items), then PBR spec inside this effort.
- MERGE-MAIN LANDED 671546e8f (merge of b23df53b5 into cda345b9a; 59 conflicts resolved vs true base bfa77e980, 4 silently-dropped lines caught + restored, cda345b9a content dropped, 3 consumed backlog items re-deleted; 9106 tests green in agent tree). Report rebase-onto-main-report.md. **PR #691 auto-CLOSED by GitHub when its base branch was deleted — replaced by draft PR #693 (base main, same branch, old body + supersede note).** Diff vs main 87 files +3977/−498. :5176 restarted on the merged tree. Controller gate running (bg). Agent worktree removed. NEXT: user visual pass on :5176 (placement/nav findings + #692 F8 items: NEAR0 near plane, scale bar step, wheel feel), then refactor-ground → spec → plan for the PBR slice inside this effort.
- Pre-existing on main, noted by the merge agent: rotationElements.ts prettier-dirty; sceneMeshBodies.ts header still says `radiusM` → fold into the PBR/visual fix commits.
- GATE GREEN on 671546e8f here (tsc clean, 1315 files / 9106 tests). REFACTOR-GROUND for the PBR slice STARTED: two opus agents (read-only) → pbr-ground-trace.md (seams A–E: mesh pipeline, cubemap capture, build-artifact pattern, sun light, per-body state) and pbr-greenfield-shapes.md (fresh agent, requirements only). NEXT: diff the two, verdicts per touchpoint, checkpoint to user (shape, priced tensions, missing joints, prep list, adjacent, PR-packaging ask) BEFORE any spec text.
- 2026-09-12 user "can't see the voyagers and rovers" on :5176 (post-merge). Headless captures: Curiosity DRAWN but black silhouette (Mars night at Gale, no ambient in the mesh shader); Perseverance frame fully black (camera into Mars night ground); Voyager 1 NOT DRAWN. Root cause (Voyager): #692's meshBodiesPass resolves the host in SCENE_CELESTIAL_BODIES and `return`s on a miss → hostless rows (Voyagers own their row) drew nothing. FIX e2aa1e5ae (inline, pushed): host miss ⇒ `hosted = false` (unshadowed Sun, no fill). Verified headlessly: Voyager draws (black speck ~60 px at the 63 m framing distance: framing uses the 14.5 m boom sphere, camera on the anti-sun side, no ambient). OPEN presentation questions for the user: (1) AMBIENT floor in the mesh shader like bodyLighting.wesl's 0.08 (rovers at night / Voyager bus are pure black); (2) `focusDistanceRadii` seed override for the Voyagers (bus fills the frame); (3) sunlit-side arrival pose.

## RESUME MAP 2026-09-12 (pre-compaction)
- Branch worktree-voyager-rovers HEAD e2aa1e5ae (pushed), tree clean, draft PR **#693** (base main; #691 is dead/auto-closed). Dev :5176 on this tree. No agents in flight.
- AWAITING USER: (a) presentation rulings — ambient floor 0.08 in mesh shader / `focusDistanceRadii` seed override for Voyagers / sunlit-side arrival pose; (b) refactor-ground checkpoint sign-off `pbr-ground-checkpoint.md` — T1 (keep 1×1 substitutes) / T2 (committed LUT) / T3 (coarsest-mip diffuse) / T4 (constant sun irradiance) + PR packaging (prep P1–P5 separate PR(s) vs all on #693); (c) the user's own placement/navigation findings (never described yet).
- THEN: write spec `docs/superpowers/specs/2026-09-12-mesh-body-pbr-design.md` against the post-prep architecture (Ground preparation section = checkpoint §3–4) → plan via writing-plans (delegated) → SDD execute inside this ledger → user visual pass → /feature-done on both plans → merge #693 → deploy from MAIN (session must sit in main; restart :5176 after any bake).
- USER RULING: mesh ambient floor YES → 9fa9b3167 (imports lib::bodyLighting::AMBIENT, `albedo * AMBIENT` added, not scaled by sunVisibleFraction; Curiosity readable at night headlessly). Refactor-ground results written up as the SPEC SEED `docs/superpowers/specs/2026-09-12-mesh-body-pbr-design.md` (8fabf4f4d, pushed): ruled decisions + full Ground preparation section (ideal shape, T1–T4 priced, J1–J6, P1–P5, adjacent, open ask). HEAD 8fabf4f4d. Still awaiting: T1–T4 rulings, packaging, Voyager focusDistanceRadii + sunlit arrival rulings, user's placement/nav findings.
- 2026-09-12 USER RULINGS (post-compaction): **land #693 FIRST, PBR becomes its own effort off main** (reverses "PBR into #693"; P1 keyed cubemap captures = standalone prep PR, rest = feature PR — settles the packaging ask; T1–T4 still open, carried in the spec). Voyager framing WORKS AS IS (no focusDistanceRadii override, no arrival change). Visual pass DONE. Rover camera movement = WEIRD but NOT THIS PR → backlog `docs/backlog/2026-09-12-rover-surface-camera-regime.md` (user chose a NEW mesh-on-surface orbit regime over extending the Earth surface camera or tweaking the orbit driver). Placement finding: rovers sink/float BY AN AMOUNT THAT DEPENDS ON CAMERA ANGLE (all four).
- DIAGNOSIS (controller): placement data is exact (site on the 3390 km sphere, +Z radial, mesh mvp f64 camera-relative). The GROUND's depth is the analytic sphere in texturedBody/fragment.wesl solved in f32 local units: camPosLocal = 1+3e-6 (ulp 0.40 m at R), raySphere's `c=|ro|²−1` cancels, near root cancels, fragDepthFromLocal projects through an mvp with a 3.39e6 m f32 translation (ulp 0.25 m) → ground depth off by up to ~1 m, angle-dependent. Earth dodges it via the tile path. Headless captures can't reproduce (follow driver pins the camera; setPose ignored) — user attests. **TASK 16** brief `task-16-brief.md`: vpCamRelLocal + camAltitudeSq (f64) uniforms, cancellation-free near root with CPU `c`, `fragDepthFromCamRel(rd*t)`; opus implementer in an isolation worktree (ff to branch, own commit, controller cherry-picks). Report → task-16-report.md. THEN: task review, cherry-pick, gate, backlog index line + rover-camera detail file committed, user visual re-check, /feature-done, merge #693, deploy from MAIN.
- 2026-09-12 USER: "visual gate pass" — attested on HEAD 7dd897503 (pre task 16). Task 16 in flight; after it lands only the rover wheel contact needs a re-look.
- TASK 16 DONE_WITH_CONCERNS 9c0840bb6 (agent branch task16-camrel-depth; ff impossible, branched off PR head instead) → CHERRY-PICKED 26740d8c1; suite 9118 green in agent tree, naga validates 6 linked modules; tsgo clean here. Report task-16-report.md (6 judgement calls: dup 2-line depth fn, second quadratic, planet/spherePick mechanical only, analyticSphere header still over budget, no GPU visual, perf unmeasured). Opus task REVIEW in flight (review-task16.diff). User asked to eye the wheels on :5176.
- TASK 16 REVIEW (opus): spec CLEAN (maths verified: root form exact incl. miss/inside/grazing; depth equivalence exact, mvp route measured 0.02–0.10 m off vs 0 eye-relative; f64 c fine; layout 176 ok; 3 consumers only). Quality WITH FIXES: F1 UNIFORM_BUFFER_SIZE hand-copied 176 (derive from float count); F2 landmine prose ×9 (+143 comment/+42 code → trim ~30); F3 composeBodySlabCamRelVp header 14 lines; F4 wrong numbers ("a metre" from matrix; naive f64 claim); F5 vertex.wesl says 112 bytes; F6 `v = normalize(camPosLocal − ng)` cancels → `v = -rd`; F7 blank line. Concern 1 → COLLAPSE to one fragDepthFromClip; concern 2 justified. ADJACENT (ask user): lib/util raySphere + utils/math/raySphereRoots.ts still carry the −b−s cancellation for atmosphere shell rays from ground level. FIX ROUND 1 dispatched (sonnet, isolation wt, brief task-16-fix-brief.md, branch task16-fix off PR head). Suite 9118 green on 26740d8c1 here.
- TASK 16 FIX ROUND 1 0ff641494 → CHERRY-PICKED 5c5091199 (all F1–F7 + depth-helper collapse to fragDepthFromClip, v=-rd; 11 files +64/−79, ~32 comment lines out). Controller re-review of the code-only diff: clean (mechanical). tsgo clean, 460 files/2148 tests green in the touched folders (full suite 9118 green on the parent). PUSHED. Agent worktree+branches removed. **Task 16: complete.** OPEN: user wheel check; raySphere/raySphereRoots near-root adjacent finding (asked, unanswered); then /feature-done → merge #693 → deploy from MAIN.

## RESUME MAP 2026-09-12 (pre-compaction, after task 16)
- Branch worktree-voyager-rovers HEAD 5c5091199 (pushed), tree clean (only untracked public/data.stale.20260911-2215/), draft PR #693 (base main). Dev :5176 serves this tree (stray :5180 killed). NO agents in flight; all agent worktrees/branches removed.
- USER RULINGS today: land #693 first, PBR = separate effort off main (spec carries T1–T4 open; P1 = own prep PR); Voyager framing OK as is; visual gate pass attested on 7dd897503 (pre task 16); rover camera → backlog (new regime) + atmosphere-over-rover noted there; backlog also: mesh-body shadows, rover terrain region.
- DONE since: task 16 rover ground-contact depth fix (26740d8c1 + review fixes 5c5091199), suite 9118 green on parent, touched folders green on HEAD.
- AWAITING USER: (1) wheel re-check on :5176 (hard refresh; Curiosity ~10 m full orbit, plus Saturn ring shadow / textured limb / pick / close-approach precaution list); (2) ruling on the adjacent finding: lib/util.wesl raySphere + utils/math/raySphereRoots.ts still use the cancelling −b−s near root (atmosphere shell from ground level) — small commit on this branch, or backlog.
- THEN: /feature-done on docs/superpowers/plans/2026-09-11-voyager-rovers.md (task 16 is unplanned extra — note in completion) → squash-merge #693 via gh api → deploy from MAIN (session in main; bake rover/Voyager meshes; restart :5176 after bake) → close: worktree, branch, memory update. PBR effort starts fresh off main from the spec.

## Adjacent finding 2026-09-12 — scale bar frozen at 309 km (Earth) / 154 km (Mars)
Not a rover bug; a #647 (camera-pivot) consumer miss. Spec `docs/superpowers/specs/completed/2026-09-01-camera-pivot.md` §9 says the scale bar "reads the resolved range; the body arm reports metres" — `toWorldArm` (poseFrameConversion.ts:122) makes `world.distance` the eye→ground range, but `runFrame.ts` (scale snap) and `slabs.ts:275` (NEAR0 altitude) still subtract `pivotRadiusMpc(focus)`, so the effective distance goes ≤ 0 the moment the arm engages (engageHR 0.45 → Earth 2,867 km altitude ⇒ last bar 309 km; Mars 1,525 km ⇒ 154 km). `computeScaleInfo` returns null and the reducer keeps the last label. Drag/zoom in the arm route to `surfaceStep`, so `orbitRadPerPixel`/`zoomedDistance` are NOT affected.
Fix shape: derive "range to the pivot surface" ONCE, frame-aware (`displayed.frame === 'absolute' ? distance − (pivotRadius ?? 0) : distance`, frame from `projectFramePose`'s `displayed`), feed it to `computeScaleInfo` (drop its pivotRadius param) and `deriveSlabs` (replace `pivotRadiusMpc` input with altitude). Regression test: runFrame sim with an engaged arm → engineScaleChanged label in metres. Packaging (ride #693 vs own PR off main): USER RULING PENDING.

## Rulings 2026-09-12 (user)
- Scale-bar freeze: OWN PR off main after #693 lands (shape recorded above). Not on #693.
- Wheel re-check after task 16: DONE (user attested).
- raySphere near-root cleanup: LANDS ON #693 as task 17 (brief task-17-brief.md; agent dispatched, controller cherry-picks).

Task 17: complete — dc7f8b990 (cherry-pick of agent c97fa80c5), pushed; agent worktree + branches removed; report task-17-report.md. Touched folders green (981 tests). Evidence: TS near root err 4.3e-10 → 3.6e-15 (real regression test); WESL half measured NEUTRAL (3/5 f32 rays better, 2 worse — the f32 `c` dominates; form-unification only). settleGoldenTrace re-recorded (≤7 cm eye delta after ~1000 sim frames). USER RULING PENDING: keep the util.wesl hunk (consistency with hitUnitSphere) or revert it.
NEXT: /feature-done on docs/superpowers/plans/2026-09-11-voyager-rovers.md (tasks 16+17 unplanned extras) → squash-merge #693 → deploy from MAIN → scale-bar PR.

## /feature-done run 2026-09-12 (in progress)
- Baseline on dc7f8b990: tests PASS 1317 files / 9119; typecheck PASS. Checkboxes 74/83 (9 open = Task 15, user attested "visual gate pass" + wheel re-check → tick with note). PR #693 CONFLICTING vs main (#690 layer-composition prep c + #694 docs).
- AGENTS IN FLIGHT: (1) merge-main-2 (opus, isolation wt) — brief merge-main-2-brief.md, pushes merge to origin/worktree-voyager-rovers, report merge-main-2-report.md in ITS tree → handling: `git pull --ff-only` here, re-run suite+typecheck, then completion commit. (2) deletion audit over review-post-audit.diff (files changed since 5e38cf75c) → report deletion-audit-post.md in THIS tree → safe-now as ONE deletion commit before the completion moves; needs-ruling → user.
- Still-open user rulings carried from the pre-audit: deletion-audit needs-ruling #2/#3/#6/#7/#8/#10/#11/#12, Perseverance normal maps follow-up, "1e-4 au" wording; task 17 util.wesl hunk keep/revert.
