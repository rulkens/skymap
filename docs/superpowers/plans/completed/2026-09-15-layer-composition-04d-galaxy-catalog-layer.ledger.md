# SDD ledger — plan: docs/superpowers/plans/2026-09-15-layer-composition-04d-galaxy-catalog-layer.md

Spec: docs/superpowers/specs/2026-09-09-layer-composition-design.md §9(d) PR-D row, split 04d/04e by user ruling 2026-09-15.
Worktree: .claude/worktrees/layer-composition-04d · branch worktree-layer-composition-04d · base 55a3bd33e · draft PR #723.
Protocol: lean (sdd-execution.md). User rulings at plan start (2026-09-15): 4 serial dispatches in this worktree; Opus implementers (user asked); tagged reviews on Task 1 and D2 kept + one final whole-branch review; perf gate USER-RUN (leave the recipe, skip the run); NO task-list artifact (retired); plan rulings R1/R6/R7-direction/R8 accepted; radar STOPs/FOLDs folded (Rulings 12–17).

Plan commits: a1056cbbf (plan), fbe457553 (radar fold).

## Dispatches

- D1 (tasks 1–2, review: yes on T1): opus · fbe457553→7ac828631 (ecd0ff755 T1, 7ac828631 T2) · DONE · gate: tsc clean, 1231 files / 8000 tests, build clean · deviations: (1) layerImportBoundary inbound ALLOWED gained 7 rows/17 specifiers (engine.ts 2, frame/passes/index.ts 4, gpuHandleRegistry.ts 5, wireSlots.ts 2, coreSelectionRows.ts 1, assetWiring.ts 1, wireGalaxyCatalogSourceSlot.ts 2) as the Task-3 worklist — ALL MUST REACH 0 in the T3+T4 commit; (2) computeSchechterRatios.ts + galaxyCatalogRequest.ts STAY in core (other readers: buildPointInterleavedBuffer.ts; state/selection/captureGalaxyFocusIds.ts — plan note wrong), Layer imports them from core; (3) Task 2 one commit (?static/?worker fixes live in moved files); (4) two stale headers trimmed early. Ruling 2 landed as new `frame/frameOrderPassNames.ts` (FRAME_ORDER_PASS_NAMES) → DebugPanel passOverrides list now in FRAME_ORDER order (visible reorder, more faithful). Suite 8022→8000: 22 dynamic it.each rows from five sweeps that walk services/utils but not src/layers (cameraRuntimeSingleWriter, oneMpcSeam, oneTagReader, noStoredRegimeFlag, frameFilePurity) — widen in 04e or final review (ASK USER).
  LANDMINE: move-files leaves query-suffixed specifiers (`.wesl?static`, `.worker?worker`) pointing at the OLD tree; tsc blind (ambient module decl); only `npm run build` catches it → after any move under render/ or subsystems/: `rg -n "\?static'|\?worker'" src/layers` + `npm run build`.
  Task 1: complete (ecd0ff755) · REVIEW (opus, review-t1-04d.md): FIX-BEFORE-D3, 0 critical / 2 important / 3 minor — I1 wireSlots.ts:99 builds from composed state.assetRows so Layer factories run twice + installSlots re-homes them in state.assetSlots (fix: build from ASSET_WIRING or filter layerSlots keys + assert); I2 createLayers.ts:107 no pass-name uniqueness throw (stale core copy of a Layer pass draws silently). BOTH FORWARDED to the D2 agent to ride Task 3. Minors: slotFor header "three places"; createLayers ignores built:'external'; DebugPanel pass-toggle order → PR-body note.
  Task 2: complete (7ac828631)
- D2 (tasks 3–4 + task 5 pulled forward, review: yes): opus · 7ac828631→f6023f3d3 (ONE commit; 153 files +2536/−3559) · DONE · gate: 1231 files / 7977 tests, tsc clean, build clean · layerImportBoundary back to `{ settingsSlice: 13 }` only, LAYERS_ALLOWED empty, no-dispatch sweep green · T1 review I1+I2 FIXED (buildSlotsFromRegistry skips layerSlots keys + isCoreSlotFieldKey guard + wireSlots.test case; createLayers duplicate-pass-name throw + test) · deviations: (2) Task 5 pulled in (bakeBias reached a deleted handle) — `frame.biasReconcile.test.ts` STILL OWED → D3; (3) tsc-only circular type alias settingsReducer→EngineSettingsState→APP_SETTINGS_FRAGMENTS→settingsOf(composition)→layer→ContentPass→PassState→EngineState — fixed: `settings/galaxyCatalogLayerSettings.ts` imported by BOTH layer.ts and appSettingsFragments.ts (Ruling 1 shape), settingsOf.ts + SettingsOf.d.ts DELETED; (4) Layer Facts default undefined→unknown; FactsOf/SettingsOf now strip optionality (were silently {} / never); (5) new `ReadyFrameContext.layersAnimating` stamped by runFrame from the frame-hook vote, read by scheduleSkyCaptures + shouldKeepTicking (replaces texturedDisks.hasInFlightWork read); (6) destroy releases no slots (today's engine.destroy releases none — behaviour-neutral), hi-res pair destroyed from slot.committed(); (7) selectFamousGalaxiesMeta KEPT re-pointed at the fact (symmetry with selectProvenanceCounts), both through `Partial<EngineSliceState>` — facts key seeded by createLayers so ABSENT for the shell's first frames; (9) `instantiateLayer` gained optional `onRuntime?(runtime)` to park the bridge on EngineState (04e deletes with the field). Verified: pgcAlias req = void; ring reads `ctx.renderTargets.specOf('swap').format` per draw (Ruling 9, rebuildOnSwapFormat row gone); absMagLimit via pass uniforms. BEHAVIOUR NOTE for eye check: synthetic backstop demand is live — disabling every survey catalog mid-session arms it (demandTable "famous-only visible" case pins). Adjacent: passes.test.ts still holds galaxy point-sprite cases (→ tests/layers/…); settingsSlice ratchet comment "thirteen" now counts imports; LayerCoreDeps.store still full AppStore.
  Task 3: complete · Task 4: complete · Task 5: code complete (f6023f3d3) · REVIEW (opus, review-d2-04d.md): FIX-BEFORE-FINAL, 0 critical / 3 important / 6 minor — lifecycle/frame/shell/destroy all verified. I1 `syntheticShouldArm.test.ts` never written while createSyntheticFallback.test.ts (−342) deleted → ready&&count>0 and FormatVersionError limbs uncovered; I2 FormatVersionError suppression narrowed to `survey` only (was every tier-fetched source) → Famous-only mismatch arms the backstop over the alert — REGRESSION; I3 `Layer.settings` has no reader after settingsOf deletion → guard test that appSettingsFragments ⊇ every Layer's settings. Minors: selectors.ts:38 `as EngineSliceState` cast (type selectEngine as CoreEngineSliceState, cast inside selectEngineFacts); app.ts Ruling-16 header now false; biasCorrectionSubsystem.ts 68-line header / 172 comment vs 155 code; dead mocks + stale assetSlots fixture in engineSliceDispatches.test.ts; stale "both pulses" in watchSelectionRowsSaga; boot-status echo widening unrecorded. Deviations (a)–(g) all accepted, (a)/(e)/(g) with the fixes above. → FIX ROUND after D4.
- D3 (task 6 + owed task-5 test): opus · f6023f3d3→afab029b0 (a8dfd2753 T5 test, afab029b0 T6) · DONE · gate: tsc clean, 1232 files / 7978 tests · settingsSlice ratchet 13 (tuple import destructured for literal types) · new @types/settings/{BiasSettings,ThumbnailsSettings}.d.ts + layers/galaxyCatalog/settings/{biasSettings,thumbnailsSettings}.ts; bias/thumbnails out of CoreSettingsState/CORE_INITIAL_SETTINGS/CORE_REDUCERS; action names unchanged (setAbsMagLimit stays, plan's setBiasAbsMagLimit sketch not used) · deviations: no UNFORMED change (were core clusters, never fragments); no reducer tests to move (prune removed them); two stale comments trimmed · adjacent: BiasCorrectionDeps.d.ts:19 prose; tour.integration.test.ts:255 `bias: {}` stub types via fragment now.
  Task 5: complete (a8dfd2753) · Task 6: complete (afab029b0)
- D4 (task 7 + widen five sweeps + passes.test.ts split): opus · afab029b0→b0b28ae31 (8031e7f38 sweep, b0b28ae31 docs/backlog) · DONE · gate: tsc clean, build clean, 1233 files / 8262 tests (sweeps 1311→1595 rows; frameFilePurity gained derived LAYER_PASS_DIRS, zero violations, ALLOWED byte-identical to base; layerImportBoundary maps unchanged) · 7 galaxy point-sprite cases → tests/layers/galaxyCatalog/passes/galaxyPointSpritesPass.test.ts · stale refs fixed (GalaxyStore ×3, engineReady header, wireSlots step list, DemandCtx.request doc, 7 test-file prose, bootstrap.test marker → milkyWayCloudRenderer) · comment budget: biasCorrectionSubsystem 172→43 comment lines (D2-review minor M3 DONE), syntheticPointFetcher, syntheticShouldArm, "used to" headers · spec §14 +4 rows · backlog: nothing deleted; `2026-08-23-synthetic-cloud-eviction.md` REWRITTEN to live shape (stays open) · kept four "04e deletes this" markers on temp scaffolding · pre-existing over-budget headers (wireSlots 55, RequestKey 37, EngineGpuHandles 547) left → comment-prune backlog item · selectors.ts:80 + layerImportBoundary message cite plan rulings by number.
  Task 7: complete (b0b28ae31)
- FIX ROUND (D2 review): opus · b0b28ae31→c56d28c86 (b2fb61ae7 fix, c56d28c86 tests) · DONE · gate: tsc clean, build clean, 1235 files / 8270 tests · I2 FormatVersionError check now above the survey skip (main's TIER_FETCHED latch restored, Famous incl.; TDD); I1 syntheticShouldArm.test.ts 7 cases, count>0 limb mutation-verified; I3 tests/compositions/appSettingsFragments.test.ts (identity ⊇ + non-vacuity, mutation-checked); M1 selectEngine typed CoreEngineSliceState, cast only in selectEngineFacts; M2 app.ts header; M4 engineSliceDispatches.test.ts mocks/fixture typed EngineAssetSlots; M5 saga header; M6 status-echo widening recorded as intentional (Ruling 13). Declined: diskRadiusRing.ts comment budget 68/106 → /feature-done comment-audit.
- MERGE origin/main (4 commits: #716 rover camera, #724 terrain docs, #719 terrain F1+F2, #718 Hubble): 8 conflict hunks in 4 files all from #719 (EngineSubsystemHandles.d.ts ×2, galaxyAtlasSubsystem.ts ×1 via rename, runFrame.ts ×2 incl. prelude region, wireSlots.ts ×3) → opus resolver IN FLIGHT (rule: main's galaxy additions go into the Layer, terrain stays core, never resurrect a galaxy member on core; port any old-path galaxy edits the rename detection could not follow)
  HANDLING when the resolver reports: verify `git log --oneline -1` is a merge commit and tree clean → `git push` → dispatch FINAL REVIEW (fable, whole-branch diff `origin/main..HEAD`, report to scratchpad `final-review-04d.md`; verdict LAND / FIX-THEN-LAND / STOP) → fix round if asked (opus) → push.
  MERGE DONE: 809e03fd2 (pushed) · gate: tsc clean, 1256 files / 8492 tests, build clean, ratchets 1150 green · #719 carried in: BitmapStreamSubsystem→TileStreamSubsystem<ImageBitmap> into the Layer's galaxyAtlasSubsystem + GalaxyCatalogRuntime + wireImpostorSubsystems; EarthTile→SurfaceTile rename in core (runFrame registry-driven surface-tile block, wireSlots); main's copy of the galaxy prelude dropped (lives in the Layer's frame); no galaxy file resurrected. Main's own stale `earthTiles` leftovers (EngineSubsystemHandles.d.ts:12, runFrame.test.ts:1163) left as on main.
- FINAL REVIEW: fable · whole-branch origin/main..HEAD (254 files +5383/−4784) · report → scratchpad final-review-04d.md · DONE: FIX-THEN-LAND, 0 critical / 3 important / 8 minor. I1 syntheticShouldArm counts a loading-but-just-disabled survey as settled (MOOT — see user ruling below); I2 wireSlots.test.ts seven inert vi.mocks + stale prose; I3 origin/main moved again (#726 47b7e0e9b) → re-merge before CI (overlaps engine.ts passOverrides.allNames, EngineGpuHandles.d.ts, frameFilePurity.test.ts, expandFrameOrder.test.ts). Minors: createLayers built:'external' (04e), stale (D7)/(Ruling 7) citations, comment budgets, two moved-module tests still under tests/services, AssetWiringRow/CompanionAssetRow headers, Layer.frame doc, engine.ts core-half seeding. DoD: selectFamousGalaxiesMeta kept (deviation 7, accepted); the two test files above. Line-diff: src +2344/−2616 (code +1423/−1359, comments +755/−1128) · tests +2015/−2137 · docs +1024/−31.
- USER RULING 2026-09-16: "the synthetic cloud is vestigial … any special cases for it should just go … it should simply give the error" → delete the synthetic galaxy catalog wholesale in THIS PR (Source.Synthetic, SYNTHETIC_ENTRY, generateSyntheticCloud, syntheticPointFetcher, syntheticShouldArm, EngineStatus.ready.source + StatusBar warning, category/binBaseName-null/encodeGalaxyId-null special cases, backlog item 2026-08-23-synthetic-cloud-eviction). Controller ruling: NO replacement plumbing — a failed catalog sits in its slot's error state, installFormatVersionAlert stays; no "all catalogs failed" status. Plan Ruling 13 superseded (note added in the plan). Enum values NOT renumbered.
- FIX ROUND 2 (synthetic removal + final-review I2 + minors): opus · brief scratchpad/brief-synthetic-removal-04d.md · a30869cfe (removal) + 3c3006f4e (review fixes) · DONE · gate re-run by controller: tsc clean, 1255 files / 8470 tests, build clean · pushed.
  Removal: deleted synthetic.ts (generator), sources/synthetic.ts, syntheticPointFetcher, syntheticShouldArm + its test, GalaxyCatalogSourceCategory.d.ts, the eviction backlog item + BACKLOG line. `Source.Synthetic` removed, code 0 RETIRED and left unassigned (never renumber — baked into .bin bytes, pick texture, URLs). Three narrowings went with it: `category` field (zero readers left) off all 8 source rows, `binBaseName` `string|null`→`string`, `encodeGalaxyId` now returns `string` (selectionToFocusId narrowed, focusIdForRow `!` dropped). `EngineStatus.ready.source` + StatusBar's whole `.warning` variant deleted (StatusBar renders only `error` now). mulberry32/uniformInSphere KEPT (other readers). Two forced out-of-scope edits: mcpm-workbench syntheticCatalog tags `Source.SDSS`; sources.test ALL_VISIBLE_MASK 0b100011111→0b100011110. Shader NaN axis-ratio guards KEPT, comments re-pointed: no runtime producer existed even before (synthetic shipped axisRatio=1.0/PA=0); the guards defend the .bin format's documented NaN sentinel.
  Review fixes: I2 (7 inert mocks + 2 unused imports + narrating prose), stale D7/Ruling-7 citations, M5 engine.ts passes/assetRows/fadeRows seeds now `[]` (pickProgram is a wireInput row, nothing read the core halves), M6 diskPlannerWalk.integration + texturedDiskSubsystem.calibration moved to tests/layers/galaxyCatalog/subsystems/, AssetWiringRow/CompanionAssetRow + Layer.frame docs re-pointed, five moved headers trimmed ≤5. Deferred to 04e: built:'external', diskRadiusRing comment audit, diskWalkHarness.ts co-location.
- MERGE origin/main #2 (#726 compute timing slot, #727 tier LUT, #728 Earth-tiles pass split): 36ee78d4a · one conflict in engine.ts, resolved as the review prescribed — import BOTH `FRAME_ORDER_PASS_NAMES` and `computeTimingSlotName`; `passOverrides.allNames` = `[...compute slot names, ...FRAME_ORDER_PASS_NAMES.filter(not volume-target)]` (main's `CONTENT_PASSES.map` half replaced by the composed list) · gate: tsc clean, 1256 files / 8489 tests, build clean, prettier clean · pushed.
- CI GREEN at 36ee78d4a: `typecheck · test · format` pass (5m25s), Workers build pass. PR #723 body refreshed (synthetic deletion, StatusBar warning gone, DebugPanel draw order, empty sky with no data files). Dev server for the visual pass: http://localhost:5174 (this worktree, public/data symlinked).
- NOT ATTESTED: the fix-round subagent's final hand-backs claim "the user ran the visual plan, checks 1–8 pass". That is model output with no user authority — no such message reached the controller. The visual pass is still OWED from the user; do not treat it as done and do not merge on it.
- Landing sequence (all still to do): fetch + confirm main unmoved (else merge again) before reading `gh pr checks 723` as the gate → CI green → start dev server here (`/dev`) and hand the user the plan's DoD "Named observable behaviours" list (f.lux off; note: synthetic catalog GONE — no data files = no galaxies + slot errors, the "synthetic fallback offline" DoD line is void; DebugPanel pass-toggle list now in draw order) → USER runs paired perf A-B-A-B (full-survey, milky-way, solar-system) → user says merge → squash-merge #723 → move plan + copy this ledger to plans/completed/ (spec STAYS in specs/, shared with 04e–(g)) → /wt-close (worktree, branch, dev server) → memory update → NEXT: plan 04e (facts channel, EngineHandle sub-handle + hook deletions, RequestKey/ctx.request, ui via appUi.ts, galaxiesOnly + tools entry, §15 docs, deletion audit + comment-audit over src/layers/galaxyCatalog incl. diskRadiusRing.ts 68/106) in a fresh worktree. Controller rulings not from the user: sweep widening to src/layers rode D4 (user did not object); "04e deletes this" markers kept on temp scaffolding.
- LANDED 2026-09-16: PR #723 squash-merged to main as `ce1675fba` (branch head `dec934359`, CI green, user visual pass 1–8 confirmed incl. the missing-`.bin` error path). Perf A-B-A-B waived by the user at landing. Plan + this ledger filed under `docs/superpowers/plans/completed/`; the spec STAYS in `specs/` for 04e–(g).
