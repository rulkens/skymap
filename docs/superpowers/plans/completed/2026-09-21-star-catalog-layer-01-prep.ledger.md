# SDD ledger — plan: docs/superpowers/plans/2026-09-21-star-catalog-layer-01-prep.md

Spec: docs/superpowers/specs/2026-09-21-star-catalog-layer-design.md (§2.4, §2.5, §3, §9)
Worktree: .claude/worktrees/star-catalog-layer, branch worktree-star-catalog-layer, BASE 485e4980f
Protocol: lean (docs/superpowers/conventions/sdd-execution.md)

Answers at start (user "ok, go" on the proposal): grouping = dispatch A (Tasks 1+2), dispatch B (Tasks 3+4); draft PR after A; perf gate = none.
Pre-flight scan: T1 produces `CORE_TRAIL_ELEMENTS` / `guides` / `orbitTrailRows`; T2 touches only settings paths (its initialState header may cite the renamed table — consistent); T3 and T4 share no file with T1/T2 or each other. No conflict, no table.
Ruling: implementers on Opus for both dispatches (each contains a `review: yes` task) — protocol maps the tag to Opus — cost if wrong: Sonnet would have done A cheaper.
Ruling: two reviews, not four — mid-branch review after dispatch A (T2's tag, T1 alongside), and the final whole-branch review after B carries T3+T4's contracts and spec sections as their tagged review — cost if wrong: a T3/T4 defect surfaces one review later, on the same branch.
Ruling (user, mid-run): PERF GATE ON for Task 4 — "make sure we don't regress performance". Dev server for this wt: http://localhost:5174 (log in scratchpad/dev-server.log). Baselines taken at 485e4980f (T1–T3 do not touch the star path): GPU `npm run -s perf -- --url http://localhost:5174 --scenario {star-field,milky-way} --frames 60` → scratchpad/perf-before-*.txt; CPU bench scratchpad/starCutSplitBench.mts (auto-detects before/after) → scratchpad/cpu-before.txt: advance 0.765 ms, pick read 0.670 ms, 9493 nodes/frame. After = same commands at dispatch B's head; halt on a regression outside noise.
Dispatch A: Tasks 1+2, model opus, BASE 485e4980f → (running), agent ac2ee5f60b3760f3c

## Queued sequence (verbatim)
1. Dispatch A (agent ac2ee5f60b3760f3c, Opus, Tasks 1+2) reports → ledger its line; mid-branch review (one round, no re-review) over `review-package PLAN 485e4980f HEAD` with T2's contract + spec §3 commit 2, T1 alongside; fix round by the same implementer if needed.
2. Push branch, open DRAFT PR (title "Layer composition 05e-1 — starCatalog ground preparation"; body: no attribution footer, no Co-Authored-By); bg `gh pr checks --watch`.
3. Dispatch B (Opus, Tasks 3+4). Brief adds: PERF GATE — after Task 4 run `npx tsx <scratchpad>/starCutSplitBench.mts` (auto-detects AFTER) and paste the 6 output lines in the reply; controller re-runs the GPU harness (`npm run -s perf -- --url http://localhost:5174 --scenario star-field|milky-way --frames 60`) and compares MERGED medians to scratchpad/perf-before-*.txt (noise ~0.5 ms). Regression outside noise = HALT and report, do not land.
4. Final whole-branch review (top tier) over `review-package PLAN 485e4980f HEAD`, mandate spec fidelity + slop, carrying T3 (§2.5) and T4 (§3 commit 4) contracts as their tagged review; ONE fix round by the branch implementer; adjudicate residuals.
5. Land per sdd-execution.md: gh pr checks, DoD audit inline, diff breakdown once, archive ledger to docs/superpowers/plans/completed/<plan>.ledger.md, plan+spec stay (spec belongs to PR 2), squash-merge ONLY on the user's word. No deletion audit (prep PR).
6. Then: `writing-plans` for PR 2 (the Layer) against the same spec.

Background task bamz83y6z = GPU perf baseline (both scenarios, sequential) → scratchpad/perf-before-star-field.txt + perf-before-milky-way.txt; on completion just confirm the files hold MERGED tables.
Scratchpad: /private/tmp/claude-501/-Users-rulkens-Development-js-skymap/2cd58892-9e47-41f9-9d58-a324299fdf4a/scratchpad (dev-server.log, starCutSplitBench.mts, cpu-before.txt, stars-large.bin cache).
Dispatch A: DONE — Tasks 1+2, opus, 485e4980f → d6c1e7cdf (T1 923cc158e, T2 d6c1e7cdf), 66 tool uses / 8.2 min; deviations: refactor rename left the file (move-files followed), Task 2 via --manifest, runFrame.test.ts fixture gained `orbitTrails: []`. Pre-existing: typecheck:fast 5 errors in tools/structure-audit (jscpd missing in wt node_modules), unrelated.
Task 1: implemented (923cc158e). Task 2: implemented (d6c1e7cdf). Mid-branch review dispatched (one round).
Mid-branch review: agent abed8e062c69b73e8 (opus) over review-485e4980f..d6c1e7cdf.diff — on its report: findings → one fix round by resuming ac2ee5f60b3760f3c, then step 2 (push + draft PR).
Mid-branch review REPORTED: T1 spec ✅ / 5 minor prose findings (frameFilePurity rationale, createLayers header D5 clause, Layer.d.ts "three halves", orbitReachByRegion overclaim, orbitTrailsPass test name); T2 spec ✅ quality approved. Fix round 1 sent to ac2ee5f60b3760f3c (one commit, no re-review). On its reply: step 2 (push + draft PR).
Fix round 1: DONE 59c95363b (5 prose fixes, 131 tests green). No re-review.
Perf baseline RE-TAKEN at 59c95363b (first run failed: wt had no public/data link so the server returned HTML for every catalog; linked to main's public/data, Vite served it without restart): star-field MERGED hdr·NEAR0 3.6 / star-aggregates·NEAR0 2.9 / swap·NEAR0 1.0 / bloom 0.9 / hdr→swap 0.9 / swap·COSMO 0.9; milky-way MERGED hdr·COSMO 4.1 / hdr·NEAR0 1.7 / bloom 1.5 / star-aggregates·NEAR0 1.4 / mw-aggregate·NEAR0 1.4 / swap·NEAR0 1.3. Files: scratchpad/perf-before-*.txt.
Step 2 DONE: pushed, DRAFT PR #799 https://github.com/rulkens/skymap/pull/799; CI watch bg task bk6q5dmoy.
Step 3: dispatch B (Opus, Tasks 3+4) — BASE 59c95363b.
Dispatch B: Tasks 3+4, model opus, BASE 59c95363b → (running), agent a73a3f22e80a70f9a; brief carries the PERF GATE (bench lines in reply). On its report: controller runs GPU harness AFTER (star-field + milky-way, 60 frames, :5174) → scratchpad/perf-after-*.txt, compare MERGED medians to perf-before-*.txt (noise ~0.5 ms); regression outside noise = HALT. Then step 4 (final review).
CI: first `gh pr checks --watch` (bk6q5dmoy) saw only Workers Builds (pass) — the CI workflow had not queued yet; re-armed watch bpnr89j6j. Dispatch B's push will re-trigger anyway.
Dispatch B: DONE_WITH_CONCERNS — T3 fb188554d, T4 910ba5396; full suite 1371 files / 13789 tests green; CPU bench AFTER: advance 0.391 / read 0.211 / frame 0.603 (vs 0.765 before, FASTER) / pick read 0.210 (vs 0.670) / emitted 10992 (bench counts streams after the pick read overwrote them; artefact, not a drawn-set change). Deviations: cameraDrivers drops redundant `focus === null`; capture anyNodeFading test deleted; starAggregatesPass.test.ts draws gain an advance. Report: task-3-4-report.md. Pushed → PR #799 at 910ba5396.
Task 3: implemented (fb188554d). Task 4: implemented (910ba5396).
PERF GATE GPU: after-run 1 star-field hdr·NEAR0 4.4 / star-aggregates 3.8 (vs 3.6 / 2.9) — outside noise; after-run 2: 3.7 / 3.3. milky-way after within 0.1 of before. Code proof: old advanceNode (59c95363b computeStarCut.ts:203-219) has the identical drop rule and emits from the same active list → main-view set+order unchanged by construction. Running paired A/B: scratch wt scratchpad/wt-before @59c95363b (node_modules + public/data linked to MAIN checkout — this wt's node_modules is EMPTY, resolution walks up to main's), server :5180 (bg bi8wuhhmg), 3 interleaved pairs → scratchpad/ab-A{1..3}.txt / ab-B{1..3}.txt (bg b5uv7uruf). Verdict = compare medians of A vs B star passes.
Step 4: final whole-branch review dispatched, agent a0e8c0c5cb385d34a (opus) over review-485e4980f..910ba5396.diff → final-review-report.md; ONE fix round by resuming a73a3f22e80a70f9a if findings.
Cleanup owed: `git worktree remove scratchpad/wt-before` + stop bi8wuhhmg after the A/B.
PERF GATE VERDICT (old-shape T4, informational): paired A/B star-field, 3 pairs (A=59c95363b :5180, B=910ba5396 :5174): hdr·NEAR0 A 4.3/3.1/3.9 vs B 3.2/4.6/3.7; star-aggregates A 3.3/2.8/3.3 vs B 2.8/3.8/3.1 — no systematic delta, spread ~1.2 ms run-to-run; CPU frame 0.603 vs 0.765 faster. NO REGRESSION. Scratch wt removed, :5180 stopped.
HALT-AND-REPLAN: PR #799 mergeStateStatus DIRTY — main gained #769 (view rigs prep, e62c2217f) which restructured the star cut (computeStarCut(state, views, advanceFades), starCutFor, setFrameCut/getFrameCut; advanceStarCut.ts + starCutOncePerCtx.ts gone). CI never queued because GitHub does not run pull_request workflows on a conflicting PR. Ruling: rebase onto origin/main, DROP 910ba5396 (T4 old shape), re-do T4 against main from task-4-brief-v2.md (same spec intent; signatures take `views: readonly FrameView[]`). Final review a0e8c0c5cb385d34a STOPPED (would have reviewed discarded code); tsc (`npm run typecheck`) on 910ba5396: src clean.
Rebase dispatched: agent a97bb39e9ef6f9116 (opus) — rebase 8 commits, skip T4, resolve coreSettingsSlices / declutterActions (deleted on main) / runFrame (re-apply focusDriverId), full suite + typecheck:fast. On its report: (1) controller commits the plan+spec amendment for T4 v2 (plan Task 4 section ← brief v2 text; spec §3 commit 4 sentence: signatures take the frame's views), (2) push (force-with-lease) → PR #799, CI watch, (3) re-take CPU baseline with scratchpad/starCutSplitBench.mts v2 (BEFORE mode) + GPU baseline on :5174 at the rebased head, (4) dispatch T4 v2 (opus) with the perf gate, (5) final whole-branch review, (6) land on the user's word.
Rebase DONE (a97bb39e9ef6f9116): branch on origin/main ebff552b0; T1 22d6784eb, T2 a07be39b8 (+ fixups: solarSystem.ts, sceneDeclutterActions.ts import the moved cluster), T3 41ae29aab; suite 1389 files / 14094 tests green. Plan+spec amendment committed 0caa84f71 (Task 4 v2 text, DoD perf-gate line, spec §3 commit 4 sentence).
PUSH BLOCKED: `git push --force-with-lease` DENIED by permission — PR #799 still shows the pre-rebase history (910ba5396). Needs the user's go (or the user pushes). Not retried.
Baselines v2 at 0caa84f71 running (bg b9w0f6779): scratchpad/cpu-before-v2.txt (bench v2 BEFORE mode), perf-before-v2-star-field.txt, perf-before-v2-milky-way.txt. Then dispatch T4 v2 (opus, task-4-brief-v2.md, perf gate), BASE 0caa84f71.
Baselines v2 DONE at 0caa84f71: CPU advance 0.681 / read 0.479 / frame 1.161 / pick 0.476 / emitted 9493; GPU star-field MERGED hdr·NEAR0 4.2 / star-aggregates·NEAR0 3.6 / swap·NEAR0 1.0 / bloom 1.0; milky-way hdr·COSMO 4.1 / hdr·NEAR0 1.6 / bloom 1.6 / star-aggregates 1.4 / mw-aggregate 1.4.
Dispatch T4 v2: agent a5691d41d4debd8f7 (opus), BASE 0caa84f71 → (running). On its report: GPU after-run (star-field + milky-way, :5174 → perf-after-v2-*.txt), compare; if star passes move > ~0.5 ms, paired A/B via scratch wt at 0caa84f71 (node_modules + public/data → MAIN checkout's, port 5180) before ruling. Then final whole-branch review (opus) over review-package PLAN ebff562b0..HEAD [use `origin/main` as BASE], ONE fix round, then land steps (push needs the user: force-with-lease denied).
T4 v2 DONE_WITH_CONCERNS (a5691d41d4debd8f7): a7e01e0d3; suite 1389 files / 14096 tests green; typecheck:fast src clean. CPU bench AFTER: advance 0.376 / read 0.207 / frame 0.583 (vs 1.161) / pick 0.205 (vs 0.476) / emitted 10992 (vs 9493). Ruling: the `emitted 9493` gate was WRONG — the bench's read line is the pick path, which BEFORE re-walked (cut only) and AFTER emits the frame's active list (incl. nodes ramping to 0) = the sanctioned delta (spec §3 commit 4); the implementer's scratch harness shows the FRAME cut byte-identical over 300 frames on two catalogs. Concerns carried to the final review: anchor derivation + two-part source gate (~15 lines) duplicated in advanceStarFades and computeStarCut; advanceStarFades has no viewKind guard (contract: runFrame never passes a capture face). Report: task-4-v2-report.md.
Task 4: implemented (a7e01e0d3).
PERF GATE GPU v2 (0caa84f71 → a7e01e0d3, :5174, 60 frames): star-field hdr·NEAR0 4.2→3.9 / star-aggregates 3.6→3.4 / swap·NEAR0 1.0→1.0 / bloom 1.0→1.0; milky-way hdr·COSMO 4.1→4.3 / hdr·NEAR0 1.6→1.7 / star-aggregates 1.4→1.6 / bloom 1.6→1.6 / mw-aggregate 1.4→1.4. Every star pass within ±0.3 ms (< 0.5 noise, < 1.2 observed spread) → NO REGRESSION, no A/B needed. Files: perf-after-v2-*.txt.
Step 4 (v2): final whole-branch review over review-ebff552b0..a7e01e0d3.diff (10 commits, 58 files, +1450/−377) → final-review-v2-report.md; ONE fix round by resuming a5691d41d4debd8f7; adjudicate residuals; then land steps (push needs the user).
Final review v2 dispatched: agent abb353e1c1e84d349 (opus) over review-ebff552b0..a7e01e0d3.diff → final-review-v2-report.md. On its report: MUST-FIX/SHOULD-FIX → ONE fix round by resuming a5691d41d4debd8f7 (one commit, no re-review unless a must-fix touches the cut seam); rule on residuals in the ledger; then land steps 5-6 (push needs the user).
Final review v2 REPORTED (abb353e1c1e84d349): APPROVE-WITH-FIXES; all 4 tasks spec ✅; gates re-run green. 1 MUST-FIX (M1: LayerGuides.orbitTrails contract must say BOTH id and focusId resolve in the body-state snapshot — orbitTrailsPass asserts both) + 5 SHOULD-FIX (S1 computeStarCut header "runs long"/"sole owner" stale + over budget, S2 "PURE" overstated in 4 places, S3 starAggregatesPass dangling clause, S4 readStarCut ragged wrap + "walk"→"work", S5 three "PR 2" forward refs) — ALL comment wording. Rulings adopted: C(1) no shared helper (third copy already in starCatalogVisible.ts; consolidate in PR 2), C(2) no viewKind guard needed, C(3) cameraDrivers simplification approved. N1 CARRIED TO PR 2 PLAN: `LayerInstance.frame` gets ONE FrameView but advanceStarFades needs the rig's whole views list (frustum union, #769) — PR 2 must widen `Layer.frame` to the views list (or an equivalent) before moving the advance into the star Layer's frame.ts, else dome/XR silently regress to single-view pruning.
Fix round 1 sent to a5691d41d4debd8f7: M1 + S1-S5, one commit, no re-review. On its reply: land steps — DoD audit inline, diff breakdown, archive ledger; push + CI need the user.
Fix round 1 DONE: 7a2558464 (M1 + S1-S5, comments only, net −4 comment lines; computeStarCut budget 0.56 → 0.49). No re-review.
Full suite at 7a2558464: 1389 files / 14096 tests green.
DoD AUDIT (inline): LayerGuides.d.ts exists / LayerLabels.d.ts gone / Layer has guides? not labels? ✅; orbitTrailRows composed in createLayers, orbitTrailsPass imports no static table, CORE_TRAIL_ELEMENTS is the core rows' only name ✅; core/orbitTrails cluster exists, body orbitTrails dir gone, bodyLayerSettings = 3 slices ✅; focusDriverId at the six sites (bodyMovesThisFrame, stepCameraRuntime, cameraDrivers, liveBodyPosition, approachTiltedPose, runFrame) ✅; advanceStarFades.ts + frameStarCutFrustum.ts exist, zero advanceFades/anyNodeFading hits ✅; backlog detail + index line deleted ✅. Observable behaviours: user's eyes (manual pass on the main app, listed in the plan DoD). Perf gate: passed (above).
DIFF BREAKDOWN (origin/main..7a2558464, 11 commits): code +214/−133 · comment +126/−119 · test +156/−71 · doc +715/−47 (doc = grill transcript + spec + plan).
LANDING: ledger archived to docs/superpowers/plans/completed/2026-09-21-star-catalog-layer-01-prep.ledger.md; plan + spec stay (spec belongs to PR 2; plan moves at PR 2's /feature-done or on the user's word). Blocked on the user: force push of the rebased branch to PR #799, then CI, then squash-merge on their word. Next: writing-plans for PR 2 (the Layer), carrying N1 (Layer.frame must receive the rig's views list before the advance moves into the star Layer).
