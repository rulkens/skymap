# SDD ledger — plan: docs/superpowers/plans/2026-09-19-view-rigs-prep.md

Worktree: .claude/worktrees/view-rigs-prep · branch worktree-view-rigs-prep · draft PR #769 · spec docs/superpowers/specs/2026-09-19-view-rigs-dome-fisheye-design.md
Plan HEAD at start: 6cdb95124

## Plan-start answers (user, 2026-09-19)
- Parallelism: the dome feature plan (PR 2) is authored IN PARALLEL with this prep execution.
- Perf gate: NO (user overrode the recommendation).
- Protocol: lean SDD (docs/superpowers/conventions/sdd-execution.md) — 2–4 grouped dispatches in this worktree, Sonnet implementers (Opus for review: yes tasks), one mid-branch review per review:yes task, one final review, CI is the gate.

## Next actions (queued, verbatim intent)
1. Pre-flight conflict scan table (not yet run) → ledger.
2. Dispatch groups: A = Tasks 1–2 (sections + rig loop), B = Tasks 3–4 (frustum + deriveViewContext + viewKind, Opus), C = Tasks 5–6 (planning over frusta + photometry, Opus).
3. In parallel: write the dome feature plan docs/superpowers/plans/2026-09-19-dome-fisheye.md (PR 2, on top of this branch) from the spec's "Dome rig (feature PR)" section.
4. Final review → user's merge word → PR 2.

## Pre-flight scan (2026-09-19) — one conflict
| Tasks | Shared | Finding |
| T2 × T4 | ReadyFrameContext.d.ts | both list adding `output?` |
Ruling: T2 adds `ReadyFrameContext.output?`; T4 adds only `viewKind` — T2's renderFrame already reads it — cost if wrong: none (T4 sees it exists).
Other shared seams consistent: T2 produces `RenderFrameInput.views` → T5 consumes; T3 ViewFrustum → T4 ViewSpec; T4 edits starCatalogPass:825 before T5's star-cut rework (serial groups).

## Dispatches
- A (T1–2) Sonnet, 6cdb95124→03e0a932d, DONE, 130 tool uses. Task 1: complete (db19821bc). Task 2: complete pending review (03e0a932d). Deviation: mono submits 4×/frame (one per section) instead of 1.
- Dome plan committed 388b630f6, pushed.
- T2 review (Opus, bg, read-only) — focus: does the 1→4 submit split change mono pixels (buffer written in two sections), timing span, first-touch. On return: fixes done by controller inline if small, else after B lands (avoid concurrent commits).
- T2 review DONE: fix needed. (1) renderedTargets union runs at expand time but executeFrame fills it at encode (executeFrame.ts:176), batches deferred → union always empty with >1 view; (2) the three T2 contract tests missing (only call-count bumps); (3) shape; (4) endFrame skipped if no main batch; (5) byte-identity: no cross-section buffer race found; timing + swap OK; (7) inline batch type, "PR 2"/"later task" narration (engine.ts:293, ReadyFrameContext output doc, ViewRigKey.d.ts, executeFrame.ts:146-149, renderFrame.ts:119-124), renderFrame header 9 lines.
  Ruling: adopt reviewer's shape — one running encoder, submit only when the batch's view ctx changes (mono back to 1 submit, today's command stream; revert the count-only test edits); add the 3 contract tests; fix narration + header — writeBuffer race only exists between views — cost if wrong: one more fix round.
  Fix dispatch WAITS until B lands (B edits ReadyFrameContext; avoid concurrent commits). Fix = fresh Sonnet (A's agent retired: fresh>reuse), before T3/T4 review.
- B (T3–4) Opus, 388b630f6→16f364e68, DONE, 100 tool uses, pushed. T3 967d4825d, T4 16f364e68 (pending review). typecheck:fast verified clean by controller.
  Ruling: accept f64 NEAR0/body matrices within 1 ulp at [0] (tangent form can't recover f/aspect exactly; f32 COSMO exact) — sub-f32-ulp after narrowing, invisible — cost if wrong: an eye-check diff, fixable by special-casing the symmetric path.
  Ruling: accept 3rd `viewSlot !== 0` site converted (drawStream knee) + fixtures made explicit `viewKind:'capture'`; accept `viewBodyPose` 2nd export in bodyRelativePose.ts IF purity ratchet passes (reviewer checks).
  Ruling: runFrame computes views BEFORE stamping focusBlend/focus/layersSettling, which deriveViewContext copies → move the views computation after the stamp in the T2 fix dispatch — cost: none for mono.
- T3/T4 review DONE: approve with fixes; maths correct at every contracted site, same-pose guarantee holds, mono drift only m[0] ≤1ulp. Findings: (1) slab gate cone from fovY/aspect misses off-axis frustum edges (visibleSlabBodies.ts:69, frameContext.ts:161); (2) horizonShellPass:64, zoneOfAvoidancePass:41, milkyWayPass:111, milkyWayAggregatePass:65, galaxyCatalog/frame.ts:84 read un-turned ctx.cam → rig views draw main orientation; (3) frustumPerspectiveF64 dead branches → far===null means reverse-Z infinite, drop boolean; (4) deriveViewContext.ts:32 sizePx cast as HTMLCanvasElement → deriveFrameContext takes canvasSize: Readonly<Size>; (5) viewBodyPose → own file camera/viewBodyPose.ts + SEAM_FILES in oneMpcSeam.test.ts:39/:82; (6) test gaps (body-arm test needs nonzero eyeOffset asserting eyeRelBodyM; five-faces cam.position assert can't fail; asymmetric vp test tautological); (7) reflow 150+ char comment lines computeForegroundViewProj.ts:40, computeViewProj.ts:79; (8) DEV radiusPx — no action.
  Ruling: fix 1,3–7 in a T3/4 fix round (Opus, after T2 fix lands). Ruling on (2): fix in THIS PR — it's the dome's blocker and the prep's job; preferred shape = the view ctx carries a view-turned `cam` (basis rotated by spec, fov/aspect from frustum) so every ctx.cam reader is per-view for free; fall back to converting the five sites only if OrbitCamera can't express a turned view — cost if wrong: dome faces draw MW/ZoA/galaxies at main orientation (visible, caught at first still).
- T2 fix Sonnet 16f364e68→7d13ac905 DONE (78 tool uses), pushed. Mono back to 1 submit; 3 contract tests added (3rd verified failing on old logic). Task 2: complete.
- T3/4 fix (Opus, bg, BASE 7d13ac905) dispatched with findings A (turned cam) + B–G. On return: push → Task 3, Task 4 complete → dispatch C (T5–6, Opus; T5+T6 are review:yes → one mid-branch review after).
- T3/4 fix Opus 7d13ac905→3a0848dcd DONE, pushed. Task 3: complete. Task 4: complete. turnedOrbitCamera util (view cam like cubemapFaceContext); upstream only sees main ctx.
  Ruling: accept slab-gate half-diagonal few-ulp drift (aspect from projection, was canvas w/h) — only flips a body exactly on the ×1.15 threshold — cost: invisible.
  Parked for VR (not dome): horizon shell + ZoA ray-march a symmetric cone from fovY/aspect → off-axis XR eyes need the frustum passed to those two renderers. Mention at landing (backlog ask).
- C (T5–6) Opus 3a0848dcd→78fc720dc DONE (114 tool uses), pushed. T5 e65892cfb, T6 78fc720dc (pending review). Deviations: PreparedStarCut stays in starCatalogPass.ts; new util targetPxPerRad (half-res aggregate); drawStream takes ctx; pxPerRad in StarUniforms@100 / StarPointUniforms@88; 60° drift = few f32 ulps.
  ASKED USER: capture faces (sky cubemap, Sgr A* lens capture, 90°) now show stars at 1/3 old brightness (old formula 3× too bright per solid angle) — spec-correct but visible in mono. Keep, or hold captures at old brightness?
- Combined review DONE: T5–6 correct (union cull, originMpc rebase, fades once, uniform offsets verified, 60° ≈2 ulp). Branch spec-faithful, light slop. Task 5: complete. Task 6: complete.
  ASKED USER (merged with capture question): photometry now fov-dependent in mono — stars scale (tan30°/tan(fov/2))²: fov slider 30° ≈2.15× brighter, 90° = 1/3; captures 1/3 (less for knee/capped records). Correct per solid angle, breaks "mono byte-identical" at non-60° fov.
  Final fix (Sonnet, bg, BASE 78fc720dc): per-view viewportPx in surface cut, `best` alloc, slabViewOf import, planesPc grow-only, inline+delete targetPxPerRad util+test, delete tautological sections test, narration. Controller amended spec (P5 no TS twin) — UNCOMMITTED, commit after fix agent lands.
- Final fix Sonnet 78fc720dc→d9ef59757 DONE, all 7 fixed (StarCutFrustum gains viewCount). Spec amendment committed dc8c31fb3. Pushed. ALL TASKS COMPLETE; no agents live.
  USER RULED 2026-09-20: keep the fov-dependent photometry (constant per solid angle; 60° unchanged, captures 1/3). Say so in the PR description at landing.
  main merged into the branch 52e82afd3 (5 commits incl. ZoA Layer move #763 — rename carried our edits; ZoA/horizon read ctx.cam = turned cam, still correct). CI green on the merge.
  2026-09-20 also landed on this PR: README --dpr drift fixed (ea4ccaa0c, 965979a9b — default is 1; dpr 2 halves the render); dome plan updated 69aea0075 (user ruled: add `--frames N` to the recorder in dome Task 6, short film first in Task 7; dome plan open points 4 + 7 now ruled/fixed). HEAD 69aea0075, pushed, tree clean, no agents live.
  Dev server for this worktree: port 5175 (pre-existing, cwd = this worktree — do NOT start a second one; 5176 was mine and is stopped). `npm install` was run here unnecessarily (worktree has no own vite binary; the 5175 server uses main's). public/data symlinked to main.
  2026-09-20 (later): origin/main merged AGAIN → 4359a1dab (Opus merge agent; #771 star extraction relocated the whole star-cut path under T5's rewrite — multi-view semantics ported into `renderers/starCatalog/cut/*` + `utils/star/*`; `buildStarCutFrustum` takes per-view REBASED vps + widest fov/height; `starCutOncePerCtx` memoises on views[0]; `advanceStarCut(state, main, views)` registers under `[main, ...views]` (mono: main twice — harmless wart); frameFilePurity row for starCatalogPass dropped; backlog star-cut-origin-carrying consumed). typecheck clean, 1346 files / 9199 tests green. Pushed; CI watch bg.
  OPEN (user critique 2026-09-20): frameContext.ts bolt-ons (optional `view?` 12th param, 5 `view === undefined` ternaries, deriveViewContext re-pack round trip, renderedTargets fold, ctx+views on RenderFrameInput). PROPOSED (awaiting user's pick, NOT approved): split into `deriveFrameContext(state, camera: FrameCamera, clock)` (frame-wide, shared by reference, ONE renderedTargets Set) + `deriveView(frame, spec: ViewSpec)` → `FrameView { frame, cam(turned), vp, slabs, bodyPose, canvasSize, drawCamPos, drawPxPerRad, fovYRad, viewSlot, viewKind, output? }`; `mainViewSpec(cam, canvasSize)` util; `ViewSpec.kind`; `altitudeMpc` required (runFrame already computes it at :167); captures = 1 frame + 6 deriveView; deriveViewContext deleted; `RenderFrameInput { canvas, views }` (check whether `once` sections read any view field — if not, drop `canvas`); ~100 `ctx.frame.x` reader renames. Mono drift: fovYRad ≤1 ulp, view.cam yaw/pitch zeroed (no pass reader). Asked: (a) ride #769 or own PR after (lean own PR); (b) shape OK to plan against.
  USER RULED 2026-09-20: "#769 can't land in the current shape" → the frame/view split RIDES #769 as prep P6. `canvas` question settled by census (once-sections DO read view fields: composite `viewFor(dest, ctx, swapView)` → `ctx.output`; sky-view compute → atmosphereDrawList → `ctx.cam.distance`) → `RenderFrameInput { canvas, views }`. Spec amended (UNCOMMITTED: P6 bullet, "Reversed" paragraph, deriveView sentence, ViewRig.views signature). Backlog `frame-view-record` (BACKLOG.md:43 + detail file) is consumed by the plan's doc-only last task.
  Plan author (Opus, bg, doc-only, no commit) writing docs/superpowers/plans/2026-09-20-frame-view-split.md from scratchpad brief `frame-view-split-plan-brief.md` (groups A: types+split, Opus; B: callers+~100 `ctx.frame.x` renames+FrameView signatures, Sonnet; C: dome-plan re-point+backlog, Sonnet). On return: read plan, commit spec+plan together, present to user → pick → dispatch A. NO source edits until the user OKs the plan.
  Corrections sent to the plan author: no FrameCamera/FrameClock — ONE input bag `FrameContextInput { cam: OrbitCamera; arm; altitudeMpc; nowMs; simDays; visibleSourceMask }`, callers assemble the OrbitCamera. USER 2026-09-20: "no camera work without a good idea of all the camera-related types" → camera-types inventory agent (Opus, bg, read-only) writing `.superpowers/sdd/2026-09-19-view-rigs-prep/camera-types-inventory.md` (families, overlaps, where FrameView/FrameContextInput land, "frame" vocabulary collision with FrameOf/PoseFrame/EyeFrame). On return: reconcile plan against inventory (rename/reuse), then present BOTH to the user for the pick.
  Inventory DONE (86 types; file in this workspace). Rulings from it: outer type `FrameView` kept, tool's `FrameView` → `FieldFrameLanes` (Task 9); by-ref field `snapshot` (not `frame`); `FrameView` flat; `renderedTargets` PER VIEW (on FrameView, fold deleted, no dome per-face clear); fold nothing else. Plan + spec COMMITTED d07377232 (9 tasks; A=1–3 Opus, B=4–8 Sonnet, C=9 Sonnet; review:yes T3, T5; leanness gate frameContext+deriveView ≤357 lines). USER 2026-09-20: "cameraBasisWorld(cam) util - great, and go" → plan Task 3 gained the util bullet (`cameraBasisWorld(forward, rollRad, upBasis): Mat3`, replaces frameContext/poseFrameConversion/cameraDofAnglesOf sites; forward-not-cam because two sites hold CameraPose+bases), committed on top of d07377232. Parallelism = this plan only; perf gate = none (user ruled earlier).
  P6 group A (Tasks 1–3, Opus, bg) DISPATCHED — BASE = the plan-amendment commit. Brief: plan path + task headings, pin the canvas-view numbers BEFORE rewriting frameContext.ts, tree red after T1 is expected (typecheck gate closes at T8), leanness gate frameContext+deriveView ≤357, dev server 5175 untouched. On return: log model/turns/BASE→HEAD; T3 is review:yes → ONE mid-branch review (Opus, diff + T3 contract + spec P6), one fix round, then dispatch B (T4–8, Sonnet).
  Group A RETURNED (Opus, 98 tool uses, ~19 min): BASE 464d2b3be → HEAD 823f99c5b (32fb7afd7 T1 types, afd081b57 T2 spec builders, 823f99c5b T3 split). 170 assertions green incl. bit-identity pin; leanness 121+157 = 278 ≤ 357. Tree red only in T4–8 consumers (expected). Deviations recorded in the plan beside each task: (1) ReadyFrameContext also carries altitudeMpc + frame-wide rosters slabBodyCandidates/meshBodies/positionedStars (deriveView takes no EngineState); (2) bodyStates keyed by string, BodyId cast stays at armBasisCtx; (3) cameraBasisWorld upBasis param is `Readonly<Mat3> | undefined`; FACE_VIEW_ROTATIONS lives in src/data/rendering/cubeFaceBases.ts; canvasViewOf(state, input, sizePx). Ruling: deviations 1–3 accepted — (1) is the lean alternative to threading state through two stated signatures; cost if wrong = rosters re-derived per view later.
  T3 mid-branch review (Opus, bg, read-only) DISPATCHED on package `.superpowers/sdd/2026-09-19-view-rigs-prep/p6-groupA-review-package.diff` (464d2b3be..823f99c5b), report → `p6-groupA-review.md`. On return: ONE fix round (Sonnet, no re-review), then dispatch B (T4–8, Sonnet).
  T3 review RETURNED (Opus, 54 tool uses, ~10 min): Spec PASS, Quality PASS, 0 BLOCKER / 3 SHOULD-FIX / 5 NIT, report `p6-groupA-review.md`. Independently re-derived: identity multiply Object.is-exact (no-short-circuit ruling holds for any camera), cameraBasisWorld matches all three removed sites, FACE_VIEW_ROTATIONS = Cᵀ·F checks. Rulings: SF1 census on plan line 21 is wrong (zoneOfAvoidanceRenderer:154 + horizonShellRenderer:159 read ctx.cam.target/roll/upBasis on the canvas view; drift ≤4e-9 metre-scale, below f32 ε) → amend the plan text, no code change; SF2 reuse IDENTITY_MAT3 → fix; SF3 narrow assembleOrbitCamera's return type so poseBasis/upBasis are required (no `!`) → fix; NIT FACE_UP unused export → delete; NIT cameraBasisWorld Readonly<Vec3> → fix if mat3FromColumns widens trivially; NIT FrameView.d.ts doc weight → trim to budget; NIT stale deriveViewContext comments in runFrame/renderFrame → Tasks 4/6 territory; NIT faceViewSpec permutation test → keep Task 5's numeric pin. Ruling: fix round FOLDED into the group B dispatch as its first commit (saves a seat; cost if wrong = a Sonnet agent carrying 6 small fixes before its own tasks).
  P6 group B (Tasks 4–8 + T3 fix commit, Sonnet, bg) DISPATCHED — BASE 823f99c5b. On return: log model/turns/BASE→HEAD; verify `npm run typecheck:fast` is green (T8 closes the gate); then dispatch C (T9, Sonnet), then ONE final whole-branch review (Opus) + one fix round, then push + bg `gh pr checks --watch`.
  NEXT (after P6 lands + final review + CI): user smoke on :5175 (DoD list in the plan) → then landing: /feature-done (plan+spec → completed/, tick checkboxes, diff breakdown), archive this ledger to docs/superpowers/plans/completed/2026-09-19-view-rigs-prep.ledger.md, squash-merge via `gh api -X PUT repos/rulkens/skymap/pulls/769/merge -f merge_method=squash` (announce first; never `gh pr merge` from a worktree) on the user's word, then PR 2 from the dome plan. At landing also ask about a backlog item for the VR gap (horizon shell + ZoA symmetric cone).
  OLD NEXT: CI green + user rulings (photometry fov-dependence, --frames, --dpr README) → user smoke (DoD) → user's merge word → archive ledger + /feature-done moves → PR 2 (dome plan 388b630f6). At landing: ask about VR backlog item (horizon shell/ZoA symmetric cone).
- (history) Combined review (Opus, bg): T5–6 mid-branch correctness + whole-branch final (spec fidelity + slop). Ruling: merged the two into one pass — last dispatch, same diff — saves a review seat. On return: ONE fix dispatch (Sonnet), no re-review → CI → user's merge word.
- (history) T2 fix (Sonnet, bg, BASE 16f364e68) + T3/T4 review (Opus, bg, read-only, diff 388b630f6..16f364e68) dispatched in parallel. After both: T3/4 fixes (if any) → push → C (T5–6 Opus).
- Dome plan author (Opus, bg) — DONE: docs/superpowers/plans/2026-09-19-dome-fisheye.md written (7 tasks, 5 review:yes), UNCOMMITTED — controller commits after group A lands. Its T1 (dome maths) + T6 (recorder --dome) don't depend on PR 1.
  Open points (plan handles 1–3,5a): (1) array layers only via fixedSizePx → plan T2 splits `layers` field; (2) terrain-pick-marker lives in SCENE → plan T5 gates at cursor write; (3) recorder is VideoToolbox, libx264 args only under --dome; (5a) checkFrameOrder "exactly once" per rig (plan T4); galaxy disk LOD planned at main 60° density → eye-check only.
  ASKED USER: (4) loop take = full 4440-frame cycle (148 s) — add a --frames cap for the early test file? (5b) README says --dpr default 2, code uses 1 — fix README?

### 2026-09-21 — group B context rotation
- Group B agent (ae692b17d8aea2db7) hit ~400k tokens mid-T5 (user flagged). Landed so far: Commit 0 `9a8176597`, T4 `bbe07f7bd`.
- Ruling: retire after T5 — messaged it to finish T5 to green, commit, tick checkboxes, reply ≤8 lines, start nothing else. Costs one extra dispatch seat; a >350k agent on T6–8 risks the quality cliff.
- On its reply: log T5 SHA + turns; verify tree clean; dispatch group B2 (T6–8, Sonnet, fresh, BASE = T5 SHA) with the same brief pointers (plan T6–8, T3 review report irrelevant now, deviation notes T1–5). T8 closes `npm run typecheck:fast`. Then C (T9) → final review → push + CI as already recorded.
- Group B agent RETURNED (Sonnet, 202 tool uses, ~27 min, 446k tokens): Commit 0 `9a8176597`, T4 `bbe07f7bd`, T5 `9153712d5`; 149 targeted tests green; typecheck errors 332→314 (T6/T8 residue). T5 deviations recorded in plan (pin location moved to scheduleProbeCapture.test, pre-split formula reconstructed inline, renderFrame.cubemapCaptures.test updated). Tree clean. Agent retired.
- P6 group B2 (T6–8, Sonnet, fresh, bg) DISPATCHED — BASE 9153712d5. On return: log model/turns/BASE→HEAD; verify 3 commits, `npm run typecheck:fast` green, leanness ≤357, plan checkboxes; then C (T9, Sonnet) → final whole-branch review (Opus, 464d2b3be..HEAD) + one fix round → push + bg `gh pr checks --watch`.

### 2026-09-21 — group B2 KILLED at ~800k tokens (user flagged twice; rotation rule breached)
- B2 (Sonnet) committed T6 `4a8203f96`, T7 `c3ed4b751`, then ran repo-wide `npm run format` (this branch predates #780's branch-only scoping) → 796 dirty files. Killed by TaskStop.
- Salvage: full dirty tree saved to scratchpad `t8-blast-full.patch`; parked as WIP commit `e9cf67be4` (unreachable now, in reflog); 145 real sweep files restored on top of T7 via keyword filter + 3 missed files (`runFrame.ts`, `renderFrame.test.ts`, `focusRecession.test.ts`) found by typecheck; committed as T8 `50402e54d` (typecheck 0, 354 targeted test files / 4226 tests green, census clean, leanness 280/357, prettier-check clean). Deviation recorded under T8 in the plan.
- Post-mortem ruling: (1) a >100-file sweep is its OWN dispatch, never bundled with other tasks; (2) controller checks agent size at every notification via output-file bytes (2.6 MB ≈ past the cliff) and kills, not messages; (3) every brief says `npx prettier --write <files>`, never `npm run format`. Memory `feedback_bg_agent_context_rotation` updated.
- Main merged (user ask): `origin/main` b94ae687b → merge `fb648f3c2`; 2 trivial import conflicts (Layer runtime types moved to `layers/*/@types/`); `npm install` needed for #772's `jscpd`; typecheck 0 after.
- Full `npm test` post-merge: 1359 files / 13426 tests GREEN.
- Group C RETURNED (Sonnet, 53 tool uses, 126k tokens, ~4.5 min): T9 `8b654f2ab` — dome plan re-pointed, tool `FrameView`→`FieldFrameLanes`, backlog line + detail file deleted, spec P6 param-name drift fixed (deviation noted in plan). Verified: tree clean, boxes ticked, `rg FrameView tools` = only engine-type uses in flow-workbench.
- FINAL REVIEW DISPATCHED (Opus, bg, read-only): packages `p6-final-review-core.md` (T1–T7+fix, 464d2b3be..c3ed4b751) + `p6-final-review-sweep.md` (T8 + T9); report → `p6-final-review-report.md`. On return: PASS → push + bg `gh pr checks --watch`; FIX-ROUND → ONE Sonnet fix dispatch (brief = report path, one commit `fix(frame): P6 final review`), no re-review, then push. Then user smoke → /feature-done → squash (API form).
- FINAL REVIEW RETURNED (Opus, 54 tool uses, 388k tokens, ~10 min): FIX-ROUND — 0 BLOCKER / 2 SHOULD-FIX / 11 NIT. Correctness otherwise clean (canvas pin bit-identical, memos on FrameView, renderedTargets per view, captures once per row, star cut over views, purity, all 5 deviations sound). Report `p6-final-review-report.md`.
- Ruling on NIT (runFrame `altitudeMpc`): `pivotFocus` sampled from pre-dispatch `stored` while pre-split `deriveFrameContext` read live `state.selectionRows.focus` after the dispatch loop → read the live row, one call feeds scale bar + `FrameContextInput.altitudeMpc`. Cost if wrong: scale bar one frame fresher on focus-change frames; render bytes match pre-split.
- FIX ROUND DISPATCHED (Sonnet, fresh, bg, BASE 8b654f2ab): both SHOULD-FIX + ruled NIT + deletion/comment NITs; NITs that add code are skipped and listed. One commit; gates typecheck 0 + full suite + leanness. Report `p6-final-fix-report.md`. On return (no re-review): verify 1 commit + gates → push → bg `gh pr checks --watch` → report to user; then user smoke → /feature-done → squash (API form).
- FIX ROUND RETURNED (Sonnet, 111 tool uses, 190k tokens, ~15 min): `63a145f38` (15 files +45/−63): 2/2 SHOULD-FIX, ruled NIT (+ harness fallout: `makeCameraSimHarness` `selectionRows` getter, `runFrame.test` static field), 9/11 NITs; skipped: cubemapCaptures flat-renderTargets (already gone), `assembleOrbitCamera` `Readonly<Mat3>` widening (39-file ripple — candidate for a later mechanical PR). Gates: typecheck 0, full suite 1359/13426 green, leanness 280. Report `p6-final-fix-report.md`.
- PUSHED d07377232..63a145f38 → PR #769; CI GREEN on 63a145f38 (typecheck · test · format 5m59s; Workers build pass). P6 EXECUTION COMPLETE. Dev server restarted on :5175 for the user smoke (session bg shell; never start a second one). No agents live. `origin/main` still b94ae687b = already merged (user re-asked 2026-09-21, nothing to merge). Tree clean at 63a145f38.
- NEXT (user-gated): user smoke on :5175 (DoD lists in both plans) → `/feature-done` (both plans + spec → completed/, diff breakdown, fov-photometry note in PR body) → archive this ledger to `docs/superpowers/plans/completed/2026-09-19-view-rigs-prep.ledger.md` → squash via `gh api -X PUT repos/rulkens/skymap/pulls/769/merge -f merge_method=squash` on the user's word → dome PR 2.

## 2026-09-21 — main merged again (user ask, after "is main merged in?")
- origin/main had moved b94ae687b → 79615dfef (8 commits: #790 step-declared depth source, #788 commitCameraPose authoritative, #779, #789, #786, #782, #776, #778). 6 one-hunk conflicts.
- Sonnet agent resolved in-wt (report: main-merge-2-report.md): kept main's step-declared depth (`view.sampledDepth!.view`), main's cameraDofAnglesOf location, our FrameView/snapshot fixtures with main's farDepthView folded in; fixed 2 silent auto-merge landmines (flat `ctx.renderTargets` → `ctx.snapshot.renderTargets` in executeFrame.ts + its test).
- Merge commit 59fd3bacd, PUSHED. Gates: tsc 0, typecheck:fast 0, full suite 1358 files / 13493 tests green, purity ratchet green. CI watch running (bg shell blknixn3a).
- Editor diagnostics on makeCameraSimHarness.ts `committed` are STALE (tsc clean).
- NEXT unchanged: user smoke :5175 (against merged HEAD) → /feature-done → archive ledger → squash via API on user's word → dome PR 2.
- User feedback: no "say the word" — ask in plain English.
- CI GREEN at 59fd3bacd (typecheck · test · format 5m59s, Workers build pass). No agents live. Dev server :5175 still up (session bg shell bfr91ztr9). Tree clean. Waiting on user smoke (checklist = DoD "Smoke" sections of both plans).

## 2026-09-21 — entanglement radar over #769 (user ask)

3 read-only Opus slices + 1 controller finding → 9 clustered knots K1–K9, report `.superpowers/sdd/2026-09-19-view-rigs-prep/radar-report.md`. NO code edited. Waiting on user rulings: (1) which knots ride #769 / dome PR 2 / backlog; (2) K1: tangent-form pxPerRad for `starCullMargins` widens the cull slack → mono no longer bit-identical. NEXT chain unchanged until ruled: smoke :5175 → /feature-done → squash (API form) → dome PR 2.
RULING (user 2026-09-21): "accept K1" — K1 sizing half rides #769 (frustum on FrameView, one px/rad currency, tangent-form cull margin; mono star cut no longer bit-identical, accepted). Impostor/mirror retirement (A4, B1 large half) PARKED. K2–K9 still unruled. Brief `k1-brief.md`, dispatched to Sonnet in this wt; report → `k1-report.md`. After DONE: typecheck+tests here, push, bg `gh pr checks --watch`.
RULING (user 2026-09-21): "do K2 in this PR as well", "K9 as well" → K2 + K9 ride #769, ONE grouped Sonnet dispatch AFTER K1 lands (shared files: deriveView, fixtures, perf bench). Brief `k2-k9-brief.md` written; report → `k2-k9-report.md`. K3–K8 still unruled.
RULING (user 2026-09-21): "do all of them really (except K8)" → K1–K7 + K9 ride #769; K8 PARKED (backlog line at /feature-done). User AFK; "keep main context clear". Chain, one Sonnet implementer at a time (shared files):
  G1 K1 (running) → G2 K2+K9+K7 (`k2-k9-brief.md`) → G3 K5+K6 (`k5-k6-brief.md`) → G4 K3+K4 (`k3-k4-brief.md`) → final Opus review of the whole K-series diff (BASE 59fd3bacd) → ONE fix round → push → bg `gh pr checks --watch` → ledger+memory. Squash-merge and smoke remain USER-gated; stop there.
K1 COMPLETE c17dffadc (57 files, +231/−296; full suite 1358/13493 green; tsc clean). Agent retired at 371k tokens. Cascade beyond brief: apparentDiameterPx wrapper chain + starPointsPass/starSpheresPass (7 sites) — see `k1-report.md`. G2 (K2+K9+K7) dispatched to fresh Sonnet.
G2 COMPLETE: K2 8a305a914, K9 a9dccfd34, K7 8b96295ea (full suite 1360/13504 green). K7 deviation: `worldPose` stays a caller arg (deriving inside doubled deriveBodyStates calls; runFrame sim-clock test asserts one). Editor hint for final review: `renderFrame.cubemapCaptures.test.ts:194` unused `snapshot`/`cam` destructure. G3 (K5+K6) dispatched to fresh Sonnet.
G3 COMPLETE: K5 7912e4202, K6 d7f1f872e (full suite 1359/13507 green). `cubemapFaceContext.ts` DELETED (one-liner); flip lives in `frustumPerspectiveF64`; dome plan doc edited. Landmine for memory at wrap-up: a backtick inside a `.wesl` `//` comment breaks the WESL/rolldown parser. G4 (K3+K4) dispatched to fresh Sonnet.
LIVE (compact 2026-09-21): G4 Sonnet implementer (K3+K4) running in this wt; on its DONE reply: verify `git log`, ledger line, then dispatch final Opus review over BASE 59fd3bacd..HEAD (review package via sdd scripts/review-package, brief = radar-report.md + the four k*-brief.md, lens = global constraints of both plans + frame-file purity + byte-identical mono except the accepted star-cull margin), ONE fix round (Sonnet), push, bg `gh pr checks --watch`, then memory update. STOP before squash/smoke (user-gated).
G4 COMPLETE: K3 6813465a0 (frame cut on `starCatalogRenderer.setFrameCut/getFrameCut`; `readStarCut` keeps a PRIVATE per-ctx memo for capture faces — deviation, for review), K4 5e90031f0 (`renderedTargets` frame-wide on snapshot; executor `touched` local). Full suite: 2 flaky (frameAlignedRoll, noStoredRegimeFlag) + 1 hook timeout (repackAtlas), all green standalone — CI is the gate. Agent hit 450k tokens (over the retire line; finished). FINAL REVIEW dispatched (Opus) over `review-59fd3bacd..5e90031f0.diff` → `final-review-radar.md`.
FINAL REVIEW FIX-ROUND 0C/8I/7N (`final-review-radar.md`): numerics verified (captures bit-exact, mono ulp-equal, renderedTargets no-op for mono); all 8 braids removed; 3 deviations judged sound. Controller RULINGS in `final-fix-brief.md`: delete `FrameView.fovYRad` (2 sites → `frustumFovYRad` util; the "112 sites" parking premise expired — `drawCamPos` stays parked), one orientation read per frame via a named arg, delete the tautological K7 test, rewrite the K5 test through a rig row (or drop), inline `advanceStarCut`, mutable renderedTargets Set via ExecuteFrameArgs. ONE fix round dispatched to fresh Sonnet → `final-fix-report.md`. NEXT on DONE: `git log -1`, push `worktree-view-rigs-prep`, bg `gh pr checks --watch 769`, ledger+memory. STOP before squash/smoke (user-gated). User may veto the fovYRad ruling cheaply (one util + 2 sites).
FIX-ROUND agent 1 STALLED (watchdog, 600s no progress) with ~55 files modified, uncommitted, typecheck red (renderedTargets threading half-done, deriveView.test 2-arg call, unused NEAR0 imports). Fresh Sonnet dispatched to FINISH from the on-disk tree (same brief, same commit message) → `final-fix-report.md`. NEXT on DONE unchanged: push, bg CI watch, ledger+memory; STOP before squash/smoke.
FIX ROUND COMPLETE 798041e2e (fresh Sonnet finished the stalled tree; all 15 items done, none skipped; full suite 1360/13514 green; report `final-fix-report.md`). Note for the record: Item 13 landed as `runFrame` minting the mutable Set and threading it via `RenderFrameInput` (ordering: the snapshot must carry it before `renderFrame` runs) — judged sound. Adjacent: two dead `NEAR0` imports removed. PUSHED 798041e2e to origin/worktree-view-rigs-prep (#769); bg `gh pr checks --watch` running. NEXT: read CI result; green → memory update, then STOP (user smoke :5175 → /feature-done → squash via API form → dome PR 2). Red → diagnose.
CI DID NOT RUN for 798041e2e: PR CONFLICTING with main (main moved: #791 settings state, #792 @types .d.ts, #787, #781) — the "CI stale on main move" landmine. `git merge origin/main` started in-wt: 2 conflicts (`starCutCpuBench.mts` imports, `BACKLOG.md`) + 4 auto-merged tests importing the deleted `settings/defaults`. Sonnet dispatched to finish the merge → `main-merge-3-report.md`. NEXT on DONE: verify `git log -1` is a merge commit, push, bg `gh pr checks --watch` (confirm via `gh run list` that a run exists for the NEW head), ledger+memory; STOP before squash/smoke.
MAIN MERGE 3 COMPLETE f5c208cc6 (Sonnet; 2 conflicts resolved, the 4 flagged tests were already fine; full suite 1372/13805 green). PUSHED; PR now MERGEABLE; CI run 35573673376 IN PROGRESS for f5c208cc6, bg watch running. NEXT: CI green → memory line, STOP (user smoke :5175 → /feature-done → squash API form → dome PR 2). Red → diagnose.
CI GREEN for f5c208cc6 (run 35573673376). Radar series + fix + main merge all landed on #769. STOPPED at the user gate: smoke :5175 (6-item checklist) → /feature-done → squash (API form) → dome PR 2.
USER SMOKE PASSED ("yes pass", 6 items). /feature-done dispatched to Sonnet in-wt: audit (skip leanness — prep PR), tick + move BOTH plans to completed/, spec + dome plan STAY (PR 2 uses them), archive ledger, 3 backlog lines (K8, impostor OrbitCamera/drawCamPos retirement, Readonly<Mat3> widening), diff breakdown, PR body rewrite, completion commit, push, CI watch → `feature-done-report.md`. NEXT on DONE: report to user, ask the VR-gap backlog question (horizon shell + ZoA symmetric cone), then squash via `gh api -X PUT repos/rulkens/skymap/pulls/769/merge -f merge_method=squash` ONLY on the user's word; then dome PR 2.
