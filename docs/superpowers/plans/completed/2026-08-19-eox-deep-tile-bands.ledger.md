# SDD ledger — plan: docs/superpowers/plans/2026-08-19-eox-deep-tile-bands.md

Spec: docs/superpowers/specs/2026-08-19-eox-deep-tile-bands-design.md (read; binding authority).
Branch: worktree-powers-of-ten-earth-zoom · draft PR #584 · plan committed at 76a034b00.
Task list: tracked here (no harness todo tool in this session — ledger is the list).

## Pre-flight scan

| Pair / task | Produces vs consumes | Finding |
| --- | --- | --- |
| T1→T2/T3/T5 | `LonLatBounds` path+symbol | consistent (`src/@types/scene/LonLatBounds.d.ts`) everywhere |
| T2→T4/T7 | `EarthImagerySource.coverage` required | consistent; T4 reads it for manifest entries, T7 implements it |
| T3→T4 | manifest band-entry fields `bounds/min/max/builtFrom` | consistent verbatim |
| T3→T5 | `derivePlannerParams` interim `[0]` → full bands | sequential handoff, T5 rewrites; consistent |
| T4→T8 | `bakeAll(bands, outDir)`; `TILE_PREFIX` v1 in T4, v2 in T8 | consistent, deliberate staging |
| T6→T7 | tile path `<z>/<row>/<col>.jpg`, `rawDataPath('eox.dir')` | consistent |
| T6 grid | `earthTileColumns(z,256) = 2^(z+1)` = EOX columns | identity holds (verified arithmetic) |
| Global 2016-layer lock vs T6 CLI | CLI exposes bbox+`--level`, no layer knob | consistent (layer hardcoded) |
| T1..T9 self-agreement | tests vs code vs files per task | consistent, except T4/T5 findings below |

**Finding A (T4, hazard).** T4's byte-identical verification runs `build-earth-tiles -- --dev` twice; output goes through the worktree's `public/data` symlink into MAIN's live `earth-tiles/` (currently the real v1 z3–z7 bake serving dev sessions). A `--dev` bake would overwrite `index.txt`/`manifest.json` with a z3–z5 equirect bake and degrade main until Task 9. Plan says "cp aside first" but never says restore.
Ruling: T4's dispatch mandates snapshot → before-bake → snapshot → after-bake → diff → **restore main's original `earth-tiles/` from the snapshot**. Cost if wrong: user's live dev Earth tiles degraded until Task 9's real bake.

**Finding B (T5, plan defect in sketch).** The refine-site sketch (`plan:288`) replaces `required > z && z < maxTileLevel` with `earthTileBandRefineAllowed(...) && z < <overall max>` — dropping the screen-space `required > z` half, which would over-refine every node to band max regardless of projected size. The plan's own degenerate-world-band test (existing planEarthTiles suite unmodified) would catch it, but the sketch must not be transcribed literally.
Ruling: refine condition is `required > z && earthTileBandRefineAllowed(bands, z, uv)`; `required`'s `Math.min(maxTileLevel, ...)` clamp uses the overall max (deepest `max` across bands), derived once in derivePlannerParams. Spec §3's "refine gate" prose supports this reading. Cost if wrong: planner over/under-refinement, caught by existing tests.

**Finding C (T5, incomplete site list).** `minTileLevel` has a third read the plan doesn't cite: the ancestor-request gate `if (z >= minTileLevel) requests.push(...)` (planEarthTiles.ts:~172). Deleting the scalar param makes it a compile error.
Ruling: that gate becomes `earthTileBandRequestAllowed(bands, z, uv)` as well — same predicate as the leaf gate, same semantics ("a tile exists at z here"). Cost if wrong: missing ancestor-fallback requests (visible as pop-in), or a compile error the implementer resolves ad hoc.

## Model plan

T1, T2 haiku (mechanical transcription); T3–T8 sonnet; reviewers sonnet; final whole-branch review most capable available non-Fable tier per project rule (opus). Fix rounds 4–5 escalate one tier.

## Pipelining map (files-disjoint gates)

reviewer1 ∦ implementer2 (share EarthImagerySource.d.ts/bmngQuadrantSource.ts) → serial.
reviewer2 ∥ implementer3 OK (disjoint). reviewer3 ∥ implementer4 OK (disjoint). reviewer4 ∦ implementer5? disjoint (T5 doesn't touch buildEarthTiles.ts) → OK. reviewer5 ∥ implementer6 OK. reviewer6 ∥ implementer7 OK. reviewer7 ∥ implementer8 OK (T8 touches buildEarthTiles.ts — overlaps T4 only, whose review will be closed). T9 after all reviews closed (user-gated).

## Progress
Task 1: implemented (6adea3180, typecheck clean, bmngQuadrantSource 10/10)
Task 1: review — spec ❌ 1 finding (stale LonLatBox in own doc comment, LonLatBounds.d.ts:2)
Task 1: fix round 1/5 (1 addressed pending re-review, 0 open — doc-comment rename; commits 6adea3180..d2d100f81)
Task 1: complete (commits 6adea3180..d2d100f81, review clean after fix round 1)
Task 2: implemented (5a7464e10, typecheck clean) — reviewer 2 dispatched ∥ implementer 3 (pipelined, files disjoint; T3 BASE = 5a7464e10)
Task 2: complete (commits d2d100f81..5a7464e10, review clean)
Task 3: implemented (ed445b61f, targeted 14/14, full suite 1045 files/7022 green, typecheck clean)
Task 3: Ruling — implementer's compile-forced minimal edit to buildEarthTiles.ts manifest literal ACCEPTED (Global Constraint: typecheck clean per task); T4 owns the real reshape. Cost if wrong: T4 reviews a slightly-moved baseline.
Task 3: pipelining collapsed — T3 diff now touches buildEarthTiles.ts, so implementer 4 waits for T3 review (serial). T4 BASE will be post-T3-review HEAD.
Task 3: minor (deferred): EarthTileManifest.d.ts header at exactly the 10-line cap, zero margin
Task 3: minor (deferred): fetchEarthTileManifest guard comment 4 lines vs 3 code lines (load-bearing, densest spot)
Task 3: complete (commits 5a7464e10..ed445b61f, review clean)
Ruling: USER requested parallel build of T6 + harvest ("can you build the fetcher in parallel and fetch the data?"). T6 dispatched OUT OF ORDER in an ISOLATED worktree (implementers stay serial per tree; T6's files are disjoint from T4/T5) — controller cherry-picks its commit onto this branch after T4 (or current HEAD) lands, then reviews. Cost if wrong: cherry-pick conflict (unlikely — all-new files + registry/package.json rows).
Ruling: user's message IS the Task 9 harvest go-ahead (announce-gate satisfied — ~276 req @ ~2/s parameters announced repeatedly, incl. board). Harvest runs ONLY after T6 passes review (unreviewed throttle code must not touch EOX). Harvest destination: prefer MAIN's data/raw/eox per worktrees-own-data convention, symlink into this worktree; fall back to worktree-local + post-merge copy note if isolation blocks the main-side mkdir.
Task 6: dispatched in isolated worktree (base ed445b61f), running ∥ Task 4.
Task 4: implemented (fcdffc965, targeted 3/3, full suite 1045 files/7023 green, typecheck clean; Ruling A verified — bake byte-identical, main's earth-tiles restored, diff -rq clean vs snapshot)
Task 4: implementer concern (deferred to T7/T8): builtFrom.vintage sourced from module-level BMNG_VINTAGE.label, EarthImagerySource has no vintage field — EOX band task must thread vintage per-source
Task 4: reviewer dispatched (sonnet) over review-ed445b61f..fcdffc965.diff with Ruling A sanity-check in constraints
Task 6: implemented in isolated worktree (22cf837b6; targeted 6/6 TDD, full suite there 1046 files/7030 green) — cherry-picked onto this branch as 6ccd8d47d (clean, 5 files all-new/registry rows), report copied to task-6-report.md, combined-branch typecheck clean
Task 6: implementer flags literal-bbox tile count = 360 (24 cols × 15 rows, cols 8756–8779 rows 1556–1570), not spec's ≈276 estimate. Controller check: 0.5°/0.02197° ≈ 22.8→24 cols spanning, 0.3° ≈ 13.7→15 rows — 360 is the correct floor-arithmetic count; spec's number was an estimate. Still ~3 min at 2 req/s. Reviewer asked to independently verify (row inversion is the real hazard).
Task 6: reviewer dispatched (sonnet) over review-fcdffc965..6ccd8d47d.diff — running ∥ T4 reviewer (disjoint files). Isolated worktree agent-af667e0251c0ae9e7 kept until T6 review clean, then remove.
Task 5: implementer dispatched (sonnet, BASE 6ccd8d47d) ∥ both reviewers per pipelining map — dispatch carries Rulings B+C verbatim. If T4 review finds blockers, its fix round WAITS until T5's implementer lands (one implementer per tree at a time).
Task 4: review — spec ✅ quality ✅, 0 blocking (1 non-blocking nit: dense NonNullable<>[number][] indexed-access type in manifest entry). Reviewer independently re-ran tests/typecheck and read main's live manifest.json through the symlink — pre-SDD scalar shape confirmed, Ruling A restore corroborated.
Task 4: complete (commits ed445b61f..fcdffc965, review clean)
Task 6: review — spec ✅ (layer lock, row-before-col, throttle, resume, HTML-abort all verified; 360-tile count independently re-derived CORRECT, rows not inverted), quality ❌ 1 blocking (module header ~20 lines vs ≤10 cap) + minor (parseCliArgs breaks if --level precedes bbox positionals; package.json placement pre-existing → skip)
Task 6: fix round 1/5 — implementer resumed via SendMessage in ITS worktree (new commit on top of 22cf837b6, no rebase/amend; header cut + arg-order fix; package.json explicitly out of scope). On DONE: cherry-pick fix commit here, scoped re-review (haiku/sonnet) over the fix diff.
Task 6: fix round 1 DONE — commit 242729013 (agent worktree; 7/7 targeted, typecheck clean). Cherry-pick DEFERRED until T5 implementer lands (no concurrent index use in this tree); scoped re-review (haiku) dispatched over review-t6-fix1.diff built via the shared object store (git -C to sibling worktree is guard-blocked; plain `git diff <sha>..<sha>` here works).
Sequencing note: harvest needs BOTH re-review clean AND 242729013 cherry-picked here (fetch-eox runs from THIS tree; prefer MAIN's data/raw/eox + symlink).
Task 6: re-review — ALL ADDRESSED (header 21→9 lines, all 3 load-bearing facts kept, file comment ratio ~36%; parseCliArgs single-pass order-independent, both orderings tested). Review clean.
Task 6: COMPLETE pending mechanical cherry-pick of 242729013 (waits for T5 implementer to land — no concurrent git in this tree). Then: remove isolated worktree agent-af667e0251c0ae9e7 + its branch, run harvest.
Task 5: implemented (76669022e; full suite 1048 files/7041 green, typecheck clean). Two concerns handed to reviewer to adjudicate: (a) EarthTileBand + EarthTilePlannerParams co-located per brief's literal file list vs one-type-per-file; (b) overall-max hoisted once inside planEarthTiles instead of returned from derivePlannerParams (Ruling B "derived once" — equivalent if outside recursion).
Task 6: fix commit cherry-picked as 8bd223448; Task 6 COMPLETE (6ccd8d47d + 8bd223448, review clean after fix round 1). Isolated worktree + branch removed (commits preserved via cherry-picks).
Task 5: reviewer dispatched (sonnet) over review-6ccd8d47d..76669022e.diff with Rulings B+C + both concerns in constraints.
HARVEST RUNNING (b5787tjet, background): npm run fetch-eox -- 12.4 55.5 12.9 55.8 (level defaults 13). Destination: main's data/raw/eox/13 via symlink data/raw/eox/13 → main (main-side mkdir was allowed). Expect 360 tiles ~3 min; HTML/non-image response aborts. On completion: verify count via ls | wc -l ≈ 360 under the symlink.
Task 5: review — spec ✅ quality ✅, 0 blocking. Rulings B+C followed exactly; reviewer hand-proved `required > z` implies `z < overallMax` (clamp makes the dropped conjunct redundant) and confirmed south-first v0/v1 consistent both sides. Adjudications: (1) one-type-per-file convention WINS over brief's literal file list — EarthTileBand split = minor fast-follow; (2) overall-max hoist inside planEarthTiles = functionally equivalent to "derived once in derivePlannerParams", accepted.
Task 5: fix round 1/5 (minor) — implementer resumed to split EarthTileBand → src/@types/scene/EarthTileBand.d.ts (mechanical, on top of 8bd223448). On DONE: scoped re-review (haiku) over the fix diff → Task 5 complete → dispatch T7 implementer. (T7 does NOT need the harvest — its tests use fixture dirs; real tiles only matter at T9's bake.)
HARVEST COMPLETE: 360/360 tiles fetched, 0 retries-needed, exit 0. Verified: find count = 360, corner tiles 1556/8756 + 1570/8779 both valid 256×256 JPEGs, 2.7 MB total in main's data/raw/eox/13 (via worktree symlink).
Task 5: fix round 1 DONE — 8ab42c68e (EarthTileBand → own .d.ts, typecheck clean, targeted 25/25). Scoped re-review (haiku) dispatched over review-t5-fix1.diff ∥ implementer 7 (disjoint files: T7 all-new tools/textures source + test).
Task 7: implementer dispatched (sonnet, BASE 8ab42c68e) — dispatch carries harvest-data location (READ-ONLY symlink), sharp composite-after-resize landmine, T4's vintage-threading concern pointer.
Dev server for this session: http://localhost:5175 (shell bzcqh38mr) — user ran /dev.
Task 5: re-review — ALL ADDRESSED (split mechanically perfect, 8 importers verified by grep, zero stray old-path imports). Task 5 COMPLETE (commits 6ccd8d47d..76669022e + split 8ab42c68e, review clean after 1 minor fix round).
Task 7: implemented (6055ea942; new 3/3, full suite 1049 files/7045 green, typecheck clean). Concerns: (a) vintage '2016' has no landing field on EarthImagerySource — third sighting of the T4 gap; (b) readBox throws on non-512 requests (loud-failure guard, accepted).
**Ruling D (plan gap, spec-grounded):** spec §manifest mandates per-band builtFrom with each source's OWN identity (BMNG entry ≠ EOX entry), but bakeAll stamps BMNG_VINTAGE on every band and EarthImagerySource has no provenance field — the plan never threaded it. Ruling: `EarthImagerySource` gains REQUIRED `provenance: EarthTileProvenance`; both BMNG sources populate it from the BMNG constants, eoxTileSource from its spec constants (already present as unused); bakeAll reads band.source.provenance per band and the module-level BMNG assumption in buildEarthTiles dies. Lands in T8 (its blast radius already includes buildEarthTiles; adds EarthImagerySource.d.ts, bmngQuadrantSource.ts, eoxTileSource.ts + fixture sources in tests). Cost if wrong: T8 diff slightly wider than its brief. Cost if NOT done: T9 ships a manifest violating the spec's provenance contract (EOX band credited to BMNG 2004).
Task 7: reviewer dispatched (sonnet) with Ruling D noted as settled context. T8 SERIALIZED behind T7 review (Ruling D makes T8 touch eoxTileSource.ts — no longer disjoint from the review's test runs).
Task 7: review — spec ✅ (composite/orientation/coverage independently verified), quality ❌ 1 blocking (coverage test MIRRORS boundsForRowColRect token-for-token — proves nothing) + 3 minors (header 11 lines; readBox guard after composite work; eoxTilePath duplicated private in eoxTileSource+fetchEoxTiles).
Ruling: eoxTilePath duplication PARKED as-is — 3-line stable path template; extracting would re-open T6's reviewed file mid-run for marginal drift protection. Final whole-branch review may re-raise; accept there if it insists.
Task 7: fix round 1/5 — implementer resumed (blocking mirror test → hand-derived literals at exact z13 step 0.02197265625°/tile; header trim; guard-before-composite; fetchEoxTiles explicitly off-limits). On DONE: scoped re-review (haiku) → T8 dispatch with Ruling D.
Task 7: fix round 1 DONE — 014ea2d22 (literal coverage expectations, header ≤10, guard-first; targeted 3/3, full suite 7045 green, typecheck clean). Scoped re-review (haiku) dispatched over review-t7-fix1.diff with instruction to hand-verify the literals independently. T8 dispatches after its verdict (T8 touches eoxTileSource.ts per Ruling D — keep test runs unentangled).
Task 7: re-review — ALL ADDRESSED (all 4 literals hand-recomputed and matched exactly; header 8 lines; guard now at entry; fetchEoxTiles untouched). Task 7 COMPLETE (commits 8ab42c68e..6055ea942 + fix 014ea2d22, review clean after 1 fix round).
Task 8: implementer dispatching (sonnet, BASE 014ea2d22) with Ruling D verbatim (EarthImagerySource gains required provenance; bakeAll reads band.source.provenance; BMNG module-level vintage assumption dies).
Task 8: implemented (3b3510076; full suite 1049 files/7045 green, typecheck clean, no concerns). Ruling D executed: required provenance field, sources populate it, two-band test strengthened to assert per-band builtFrom. TILE_PREFIX now earth-tiles/v2, EOX_MIN_LEVEL=8 named constant, --dev single-band, ATTRIBUTIONS.md EOX subsection added. No bake run.
Task 8: reviewer dispatched (sonnet) over review-014ea2d22..3b3510076.diff — constraints include grep for stale earth-tiles/v1, BMNG manifest strings byte-identical, Ruling D completeness. On review clean → T9: real bake (npm run build-earth-tiles from THIS tree writes through public/data symlink into MAIN's live earth-tiles — v2 prefix means NEW keys, v1 untouched, so no snapshot dance needed this time; verify manifest has 2 surface bands + EOX provenance) → STOP for user visual pass at http://localhost:5175.
Task 8: review — spec ✅ quality ✅, 0 blocking (1 non-blocking: main() startup log lost source.id). Ruling D verified fully executed; no stale earth-tiles/v1 in production code; BMNG manifest strings byte-identical. Task 8 COMPLETE (commits 014ea2d22..3b3510076, review clean, no fix rounds).
T9 STARTED: earth-tiles live at public/data/images/earth-tiles/ (NOT public/data/earth-tiles); pre-bake snapshot of main's manifest.json+index.txt → workspace pre-t9-snapshot/ (rollback if visual pass fails; v1 tile bytes untouched by v2 bake). First bake attempt FAILED: worktree data/raw/textures lacks the 8 BMNG quadrant JPGs (worktrees-own-data landmine, same as T4's agent hit) → fixed with 8 individual symlinks to main's files (leaves tracked README/sha256 alone; REMOVE after T9 or leave for /feature-done cleanup). Bake re-running in background (b3gggei83). User asked "can i see anything yet" → told: after bake, hard refresh :5175, fly to Copenhagen.

T9 BAKE DONE (exit 0, 11,042 tiles indexed): manifest VERIFIED — levels.surface = 2 entries, prefix earth-tiles/v2, per-band builtFrom correct (BMNG nasa-bmng-200408-quadrants z3–z7 global; EOX eox-s2cloudless-2016 z8–z13 Copenhagen, CC BY 4.0 attribution verbatim). Tile counts: z3–z7 = 32/128/512/2048/8192 (global), z8–z13 = 2/4/4/12/24/84 (regional). NOTE: z13 = 84 not 90 — 24×15 EOX grid pairs into 12×7 full 2×2 composites; the odd 15th EOX row (southernmost ~2.7 km strip) emits no z13 pyramid tile, manifest coverage still spans it → that strip 404s at z13 and rests at z12 detail (graceful, accepted). NOW BLOCKED ON USER VISUAL PASS (http://localhost:5175, hard refresh; Copenhagen z7→z8 seam, z12–z13 LOD, elsewhere unchanged). User asked about bake parallelization → answered: possible (bounded concurrency; deepest level only within a tile row to respect the 4-band LRU), captured as potential follow-up, NOT on this branch.

VISUAL PASS ROUND 1 — user found 2 defects (both user-adjudicated):
**Ruling E (spec gap, user-confirmed fix):** EOX band's coarse tiles (z8 spans ~1.4° vs 0.5° patch) carry transparent no-data margins; page table names finest resident → EOX tile shadows BMNG z7; fragment.wesl:235 blends transparent texels to BASE texture. Fix = bake-side underfill (user chose over runtime band layering): a baked tile is ALWAYS fully opaque, "no coverage" appears only as an absent file (404 → resident-ancestor chain, which crosses bands correctly). Mechanics: eoxTileSource emits PARTIAL 2×2 composites (null only when all 4 EOX tiles absent — also rescues the harvest's odd 15th row, z13 84→96 tiles); new combinator underfillImagerySource(primary, filler) composites primary OVER filler (primary null → null WITHOUT reading filler — bakeDeepestLevel probes 58M z13 boxes); bakeCoarserLevel gains optional underfill (partial parent → canvas = filler readBox of parent box instead of transparent); bakeAll band gains underfill?; main() passes the shared BMNG instance as the EOX band's underfill. Spec must gain this fallback-semantics section at wrap-up. Cost if wrong: re-bake.
**Ruling F (user chose depth):** SURFACE_STANDOFF_RADII 1.02 → 1.00015 (~1 km altitude; z13 ~13× magnified at floor). Sanctioned by the shipped-landmine note ("only becomes worth lowering if the pyramid gets deeper" — it now is). Watch on next visual pass: atmosphere-from-inside at 1 km, depth precision. Consumers: comments in orbitRadPerPixel/zoomedDistance/bodyLikeFraming + hardcoded 1.02 in orbitControls.test.ts:487 + comment in orbitRadPerPixel.test.ts:32.
Task 10 (fix round vs visual pass): BASE 3b3510076, implementer dispatching (sonnet, this tree) — two commits (tools underfill; camera floor). Then re-bake → visual pass round 2.

Task 10 COMPLETE: Fix 2 (zoom floor 1.00015) done INLINE by controller at user's request — e52db160b; Fix 1 (underfill) by implementer — 8857fabe3 (full suite 1050 files/7052 green; implementer correctly threaded underfill through every bakeCoarserLevel call, not just deepest — anticipated deviation). Review (sonnet): spec ✅ quality ✅, 0 blocking 0 minor; hand-recomputed Porter-Duff + quadrant literals. 1 out-of-scope note: runFrame.ts:125-129 idle-tick rationale cites stale 127 km floor — geometry changed (at 1 km the 500 ms live-clock tick = ~130 m ground jumps), PARKED pending drift adjudication.
VISUAL PASS ROUND 2 findings (user, testing the 1 km floor): (a) Earth VANISHES at max zoom, stars show through — undiagnosed; static analysis exhausted (earthLayer enabled-gates all pass at 1 km, MVP f64-composed, no discard in earth WGSL, NEAR0 near floors at ~3 m); suspects: camera below cloud shell (12.7 km) / inside atmosphere shell, or f32 vertex precision; AWAITING user observations (pop-vs-fade + altitude). (b) camera drifts while pinned+focused — prime suspect: LIVE sim clock rotates Earth under a centre-pivoted camera (~0.26 km/s ground speed at Copenhagen lat; sub-pixel at 127 km, obvious now); AWAITING clock-pause test; fix if confirmed = surface-fixed camera follow, a small feature to spec, NOT a hotfix. (c) user noted rebake does all bands — by design (index written whole); backlog item at wrap-up: --only band incremental bake.
VANISH SOLVED (user's sliced-triangle observation → near plane): MIN_NEAR_MPC 1e-19 Mpc = 3.09 KM (controller earlier misread as 3 m), above the ground at the new floor → whole visible cap near-clipped at nadir. Fix 20fed8e31 (inline): MIN_NEAR_MPC → 1e-21 so the distance ratio governs (~637 m near vs ~956 m altitude at Earth's floor); 576 camera/frame tests green. COUPLING recorded in the comment: near floor must stay below min altitude implied by SURFACE_STANDOFF_RADII.
Re-bake with underfill RUNNING (task b2wpqws2v); on DONE verify z13 = 96 tiles (12×8, underfill rescues the 15th harvest row), z8-z12 counts may shift, all-opaque; then visual pass round 2.

Re-bake DONE (11,054 tiles; z13 = 96 as predicted; ALL 122 EOX-band tiles at z8/z12/z13 verified min-alpha 255; manifest 2 bands intact). **VISUAL GATE PASSED (user, round 2): "tiles around are not blurry any more, and the zoom properly works."** BMNG symlinks removed (eox/13 symlink left — worktree-local, dies with the tree). Drift ruled OUT OF SCOPE → backlog (surface-fixed camera follow; clock-pause confirmation still pending, geometry hypothesis high-confidence).
WRAP-UP RUNNING: docs subagent (spec Ruling-E section + 2 backlog items: surface-fixed follow incl. runFrame tick note, --only incremental bake; one docs(eox) commit). Attempt 1 DIED to an API-error stall (tree left clean, nothing committed); attempt 2 LIVE. API is flaky this session (user reported "api error" twice) — if attempt 2 also dies, write the three doc files INLINE from the dispatch brief (it is self-contained; full text in this ledger's transcript is unnecessary — the brief's facts are all in the rulings above). On docs commit landed → final whole-branch review (OPUS, never Fable) over full branch vs main (base for diff: merge-base with main; branch code commits so far: fcdffc965..20fed8e31 + docs) → ONE fix dispatch max → archive this ledger to docs/superpowers/plans/completed/2026-08-19-eox-deep-tile-bands.ledger.md BEFORE workspace deletion → finishing-a-development-branch → /feature-done (must also delete the 2 new backlog items? NO — they are new UNSTARTED work, they stay; /feature-done sweeps only items this feature started) → R2 sync POST-MERGE from MAIN checkout only.

DOCS COMMIT LANDED (attempt 2): 1e17258a6 — spec §10 "Band fallback semantics (post-visual-pass ruling)" + docs/backlog/2026-08-19-surface-fixed-camera-follow.md + docs/backlog/2026-08-19-incremental-band-bake.md + 2 terse BACKLOG.md index lines. Verified: 4 files, 127 insertions, no code touched, scratch files unstaged.
FINAL WHOLE-BRANCH REVIEW DISPATCHED (opus, background): base = merge-base 6d8edfd26, head 1e17258a6 (19 commits); package .superpowers/sdd/2026-08-19-eox-deep-tile-bands/review-6d8edfd26..1e17258a6.diff; report → final-review-report.md in this workspace. Dispatch carried: licence lock, the 4 adjudicated deviation commits (judge correctness not plannedness), MIN_NEAR/STANDOFF coupling, comment-budget/one-symbol/type-not-interface, read-only + no network + no subagents. HANDLING on verdict: "Yes" → archive this ledger to docs/superpowers/plans/completed/2026-08-19-eox-deep-tile-bands.ledger.md, delete workspace, finishing-a-development-branch, /feature-done (the 2 NEW backlog items STAY), R2 sync post-merge from MAIN checkout. "With fixes"/"No" → adjudicate each finding, ONE fix dispatch max, ONE scoped re-review, park residuals with rulings, then proceed as above.

FINAL REVIEW VERDICT (opus): **With fixes** — 0 Critical / 6 Important / 13 Minor; typecheck clean, full suite 1050/7052 green, prettier fails on 4 touched files. Report: final-review-report.md. ADJUDICATION (controller rulings):
- ACCEPT → single fix dispatch (sonnet): (I1) clampDistance docblock gains near-plane coupling clause + honest "validated over Earth only" note on the body-global ratio (also covers I6's docblock half); (I2 option b) eoxTileSource contiguity guard (rect tile count vs file count → throw) + existsSync guard for missing z13 dir (minor) + backlog detail file for proper multi-region coverage; (I4) stale docs RENDERER.md:27 / DATA.md:79 / DEPLOY.md:41 / EarthTileManifest.d.ts v1 example; (I5 docs half) DEPLOY.md note: manifest SHAPE change = tiles off between merge and sync; (M) prettier 4 files, 58M→33.5M magnitude, extract earthTileBandOverlapsUv (scalar args, kills planEarthTiles per-node uv alloc), derivePlannerParams malformed-band guard + test, eox README harvested-regions table (Copenhagen 12.4/55.5/12.9/55.8, 2026-08-19, 24×15=360), orbitRadPerPixel wording, bakeAll deepest-underfill test, BACKLOG index line for EarthImagerySource id/provenance collapse.
- PARK with ruling: (I3) bakeDeepestLevel coverage clamp → folded into incremental-band-bake backlog file per reviewer rec 4 (perf-only, bake works, real test surface — not a merge blocker); (I5 process half) R2 sync becomes a BLOCKING checklist line in PR #584 body — controller does this at finishing, NOT the fix agent; (I6 verification half) Moon/gas-giant/Sun approach at new standoff → ask user at merge gate, two minutes; (M) buildEarthTiles 0.62 comment ratio (pre-existing overage, needs judgment audit not a blind trim), fetchEarthTileManifest 'surface' hardcode (existing EarthTileKind-singularity backlog), refactor-ground commit riding branch (shipped, user knows), v1 dead R2 keys (explicitly not this branch's problem).
Then ONE scoped re-review (sonnet) over the fix diff → archive ledger → finishing-a-development-branch (PR body gains blocking R2-sync line) → /feature-done → R2 sync from MAIN.

FIX ROUND DONE: 611633dae (code) + ad6539c2b (docs); typecheck clean, full suite 7058 green (+6 new tests), eslint clean. One sanctioned deviation: eoxTileSource's "leaves a missing one transparent" test re-modeled (write 4 tiles, rmSync SE before readBox) because the contiguity guard correctly rejects a 3-of-4 construction tree — intent preserved. Editor diagnostics on the two band-predicate test files are STALE (files exist, typecheck clean). SCOPED RE-REVIEW DISPATCHED (sonnet, background): package review-1e17258a6..ad6539c2b.diff, report → final-rereview-report.md. HANDLING: both verdicts clean → archive ledger to docs/superpowers/plans/completed/2026-08-19-eox-deep-tile-bands.ledger.md, delete workspace, finishing-a-development-branch (add blocking R2-sync checklist to PR #584 body + Moon/Sun standoff note to user), /feature-done (the NEW backlog items — surface-fixed follow, incremental bake, multi-region coverage, EarthImagerySource identity line — all STAY), R2 sync post-merge from MAIN. Unaddressed finding or new defect → adjudicate; residuals park with rulings (breaker: this was the one fix dispatch).

SCOPED RE-REVIEW CLEAN: all 15 items addressed at the right sites; parked items correctly absent; prettier/typecheck clean, targeted vitest 34 files/121 tests green. One residual (stale `earthTileSubsystem.ts:123` citation in RENDERER.md) fixed inline by controller as 16749082f — line number dropped entirely so it cannot go stale again. FINAL REVIEW LOOP COMPLETE. Branch tip 16749082f (22 commits over merge-base 6d8edfd26). Remaining: archive this ledger → /feature-done → finishing-a-development-branch (PR #584 body gains BLOCKING R2-sync checklist line + Moon/Sun standoff visual note; merge itself is the user's call) → post-merge R2 sync from MAIN checkout (npm run sync-r2-secure).

## In-flight handling (compact-safe snapshot, REFRESHED after T8)
- CURRENT STATE: T1–T8 ALL COMPLETE (reviews clean). HEAD 3b3510076. Harvest DONE (360 tiles in main's data/raw/eox/13). T9 bake RUNNING in background (task b3gggei83; first attempt failed on missing BMNG quadrants, fixed via 8 symlinks in data/raw/textures — remove them at wrap-up).
- On bake DONE: verify main's public/data/images/earth-tiles/manifest.json has levels.surface = 2 entries, prefix earth-tiles/v2, per-band builtFrom (EOX entry sourceId eox-s2cloudless-2016). Confirm v2 tile files exist z3–z13. Then STOP — user visual pass at http://localhost:5175 (hard refresh; Copenhagen z7→z8 seam, z12–z13 LOD, elsewhere unchanged). Rollback if pass fails: restore pre-t9-snapshot/{manifest.json,index.txt} to main's public/data/images/earth-tiles/.
- After user pass: final whole-branch review (opus, most capable non-Fable) over full branch → ONE fix dispatch max → archive ledger to docs/superpowers/plans/completed/2026-08-19-eox-deep-tile-bands.ledger.md BEFORE workspace deletion → finishing-a-development-branch → /feature-done (backlog sweep). R2 sync is POST-MERGE from MAIN checkout only (npm run sync-r2-secure) + purge caveat in reference_misc.
- Task board artifact (same file path redeploys): https://claude.ai/code/artifact/c7e4e046-06fa-44af-9f54-a6b049addc9f · source: /private/tmp/claude-501/-Users-rulkens-Development-js-skymap/7a3ff70e-70d5-415f-840d-6b7f790b9b63/scratchpad/eox-task-board.html — board currently shows T8 running; refresh it to 8-done + bake state on next transition.

## In-flight handling (older snapshot)
- T4 implementer LIVE in THIS tree (BASE ed445b61f). On DONE: verify report confirms main's earth-tiles RESTORED (Ruling A) + byte-identical diff verdict → review-package ed445b61f..HEAD → reviewer (sonnet) with Ruling A in constraints. On BLOCKED: assess per SDD.
- T6 implementer LIVE in ISOLATED worktree (base ed445b61f). On DONE: cherry-pick its commit(s) onto this branch at then-HEAD (all-new files + registry/package.json rows; resolve trivial conflicts), cp its .superpowers/sdd/task-6-report.md into this workspace as task-6-report.md, review-package over the cherry-picked range → reviewer (sonnet). AFTER T6 review clean: run the harvest (go-ahead GIVEN, see rulings) — prefer MAIN's data/raw/eox + symlink here; fallback worktree-local + post-merge copy note. ~276 req @ ~2/s, npm run fetch-eox -- (bbox 12.4/55.5/12.9/55.8), verify ~276 tiles under data/raw/eox/13/.
- Queue after T4 review clean: T5 (sonnet, MUST carry Rulings B+C verbatim in dispatch) → T7 (needs T6 cherry-picked) → T8 → T9 (bake + USER visual pass gate; R2 sync post-merge from main only).
- Task board artifact (update on task transitions, same file path): https://claude.ai/code/artifact/c7e4e046-06fa-44af-9f54-a6b049addc9f · source: /private/tmp/claude-501/-Users-rulkens-Development-js-skymap/7a3ff70e-70d5-415f-840d-6b7f790b9b63/scratchpad/eox-task-board.html
