# SDD ledger — plan: docs/superpowers/plans/2026-09-03-scene-workbench-1-lidar-end-to-end.md

Worktree: .claude/worktrees/scene-workbench-1-lidar (branch worktree-scene-workbench-1-lidar), base main d33744869.
Spec: docs/superpowers/specs/2026-09-02-scene-workbench-design.md (binding). Preps P1 #659 / P2 #658 on main — verified.
public/data → symlink to main's; data/raw/geodanmark/soendermarken-z19-jpg → symlink to main's. data/raw/dhm is worktree-local (copy to main at close-out).
Harness task-list tool unavailable in this session — the task table in the controller's chat message + this ledger stand in for it.

## Pre-flight scan

| Pair / task                | Produces vs consumes                                                               | Finding                                                                                    |
| -------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| T2 → T3                    | GroupAnchor type → SOENDERMARKEN.anchor                                            | agree                                                                                      |
| T4 → T6                    | ScenePoint → readPdalCsv yields ScenePoint                                         | agree                                                                                      |
| T4 → T12                   | parsePoints(ArrayBuffer) → saga                                                    | agree                                                                                      |
| T5, T6, T7 → T9            | orthoVrtXml / lidarPipelineStages / writeJsonAtomic+upsert\* → bakeLidar           | signatures agree                                                                           |
| T8 → T9                    | none (fetch writes files only)                                                     | agree; T9 depends on fetched LAZ at run time                                               |
| T10 → T11 → T15            | App.tsx created, then <Provider>, then components                                  | sequential edits of one file, fine                                                         |
| T11 → T12                  | SceneSagaContext.resources: RenderResources — but renderResources.ts is a T12 file | CONFLICT: forward reference breaks typecheck-green-at-every-commit                         |
| T11 → T13                  | SceneCamera, SceneStore, commitCameraPose → rig/input                              | agree                                                                                      |
| T12 → T14                  | RenderResources gains `lidar` field; Viewport draw                                 | agree (T14 lists both mods)                                                                |
| T13 → T14                  | SceneCameraView → writeSceneCamera/draw                                            | agree                                                                                      |
| T12 → T16                  | `?probe` synthetic registry lives in watchRegistrySaga                             | T16 Files omits the saga modification                                                      |
| T10 tsconfig               | include lacks "@types" (flow-workbench adds it)                                    | not a defect: .d.ts reached via relative imports; root tsconfig.tools.json includes tools/ |
| Global vs T11/T15          | react-redux only in store/hooks.ts + ui/App/App.tsx                                | tests under tests/ may use Provider — outside the constraint's scope                       |
| T6 tests                   | plan calls them contract tests, not restatements                                   | reviewer may disagree — adjudicate if raised                                               |
| Each task self-consistency | tests vs code vs files                                                             | T1–T16 internally consistent                                                               |

Ruling: T11 also creates `src/render/renderResources.ts` holding the `LidarGpuAsset`/`RenderResources` types + `createRenderResources()`; T12 adds `disposeScene`/`uploadPointCloud` + tests there — keeps every commit typecheck-green. Costs if wrong: a file-ownership shuffle between two adjacent tasks.
Ruling: T16 may modify `src/state/registry/watchRegistrySaga.ts` (and add a small synthetic-registry helper file) — a Files-list omission, not a design change. Costs if wrong: nothing.

## Progress

Draft PR #665 opened on worktree-scene-workbench-1-lidar.
Task 1: implementer DONE_WITH_CONCERNS (16d88d55b) — concerns are observations: brief's literal cs2cs fails on PROJ 9.8 (equivalent pipeline used, documented), heightMDvr90 ≈ 33 m PROVISIONAL (re-read from tile in T9), Fildownload ignores HTTP Range (fetcher must not resume mid-file). Entitlement CONFIRMED. Reviewer dispatched.
Ruling: parallel execution (user request 2026-09-03) — independent tasks run in their own agent worktrees reset to origin/worktree-scene-workbench-1-lidar; controller cherry-picks -x onto the execution branch in task order. Waves: A = T2,T4,T5,T10 (dispatched) · B = T3,T6,T7,T11 · C = T8,T12,T13 · D = T9,T14,T15 · E = T16. Costs if wrong: a cherry-pick conflict on an overlooked shared file — resolved by re-running that task serially.
Task 1: review — Approved, 1 Important: heightMDvr90 from encyclopedia estimate not DHM data (README:44,58-66); Minor: README long (deferred). Fix round 1 dispatched to original implementer (compute from ground-class points of the anchor tile via PDAL).
Task 1: minor (deferred): README 144 lines vs sibling's 82 — fold cs2cs transcript into a summary once T3 exists to cross-reference.
Task 4: implementer DONE (agent worktree abc60a183 → cherry-picked 3728625ca; 3/3 tests). Reviewer dispatched.
Task 5: implementer DONE_WITH_CONCERNS, process only (agent worktree be79658ce → cherry-picked ed29ee842; 2/2 tests). Reviewer dispatched.
Note: agent worktrees cannot write into the controller worktree — reports land in the agent worktree's mirrored .superpowers path; controller copies. `git reset --hard` is sandbox-denied for agents; `git checkout -B <branch> origin/<exec>` works. Future dispatches say so.
Task 2: implementer DONE (agent worktree 7dfab614e → cherry-picked e5a0147ca; typecheck green). Reviewer dispatched.
Task 10: implementer DONE_WITH_CONCERNS, process only (agent worktree 96615298b → cherry-picked 62918a4b2; smoke 1/1, serves on :5600). Reviewer dispatched.
Wave B: T6, T7 implementers dispatched (base e5a0147ca); T11 dispatched (base 62918a4b2); T3 waits for T1 fix (anchor height).
Task 5: review clean — Approved. minor (deferred): no XML escaping of tile path in <SourceFilename> (orthoVrtXml.ts:52; path is numeric-only in practice).
Task 5: complete (commits 3728625ca..ed29ee842, review clean)
Task 4: review — Approved, 1 Important: report claimed an 8-byte-buffer truncation case that does not exist; sub-header branch (parsePoints.ts:182) untested. Fix round 1 dispatched to original implementer. minor (deferred): no 0..255 clamp in packPoints (silent ToUint8 wrap); toBeCloseTo(…,2) loose; magic decode hardcodes 4 getUint8.
Task 2: review clean — Approved. minor (deferred): GroupAnchor.d.ts comment ratio ~1.9 (cut the "v1 only ever constructs it as 0" sentence); PointCloudAsset field comment reworded from spec's inline form.
Task 2: complete (commits ed29ee842..e5a0147ca, review clean)
Task 1: fix round 1/5 (1 addressed pending re-review — heightMDvr90 now 18.5 m from lowest of 429 returns within 20 m of anchor in punktsky_1km_6175_721; tile reported ZERO class-2 ground points, SMRF no-op — flagged for re-reviewer scrutiny; commit de1bb65fd). Re-review dispatched.
Task 4: fix round 1/5 (1 addressed pending re-review — 8-byte truncation assertion added; agent 9d5269527 → cherry-picked 2e0dc96e3). Re-review pending.
Task 3: implementer dispatched (base de1bb65fd).
Task 10: review — Approved, 1 Important: vite.config.ts header 13 lines / comments > half code. Fix round 1 dispatched to original implementer. minor (deferred): main.tsx 5-line header over ratio on a 6-line file.
Task 6: implementer DONE (agent 2ef534105 → cherry-picked c4098b9e5; 6/6 tests — a 6th `filters.range limits` test beyond the brief's five, reviewer to judge). Reviewer dispatched.
Task 7: implementer DONE (agent 754a724e8 → cherry-picked b9579aa8d; 6/6 tests; upsertGroup untested, mirror of upsertAsset). Reviewer dispatched.
Task 4: fix round 1/5 (1 addressed, 0 open; commits 2e0dc96e3)
Task 4: complete (commits 3728625ca + 2e0dc96e3, review clean after fix round 1)
Task 1: fix round 1/5 (1 addressed, 0 open; commit de1bb65fd). Re-reviewer flag: probe tile likely TRUNCATED (0.45 pts/m² vs ~4–5 expected, ~13 MB vs ~100+ MB, garbage header Z) — would explain zero class-2 points.
Ruling: T1 complete as a docs task; the truncation concern is carried forward — T8 fetchDhm MUST verify each tile's on-disk size against Content-Length (and LAS header point_count × record_length) and reject/redo partial downloads; T9 bakeLidar re-derives heightMDvr90 from class-2 ground points near the anchor across the full tiles and updates README + SOENDERMARKEN (one-number edit) before writing the manifest. Costs if wrong: an ~15 m vertical offset of the group frame origin, corrected by a re-bake.
Task 1: complete (commits 16d88d55b + de1bb65fd, review clean after fix round 1)
Task 3: implementer DONE (agent 1b6910c8b → cherry-picked 036540905; typecheck clean; values byte-checked vs README). Reviewer dispatched.
Task 10: fix round 1/5 (1 addressed pending re-review — header 8 lines, comments 9 vs 19 code; agent 5ad0f462e → cherry-picked 1559f0724). Re-review dispatched.
Task 8: implementer dispatched (base 1559f0724) carrying the truncation ruling.
Task 7: review — Needs fixes, 3 Important: no mkdir of parent dir (writeJsonAtomic.ts:22-23; precedent saveJsonCache.ts:13); fixed `.tmp` name defeats two-writer scenario; upsertGroup untested. Fix round 1 dispatched to original implementer. minor (deferred): stranded .tmp if rename() fails; corrupt-JSON rethrow undocumented.
Ruling: freeze applies to consumers of T7 only (T9 waits for T7's loop to close); independent tasks (T8 in flight, T12/T13 after T11) continue — worktree isolation means they cannot build on T7's tree. Costs if wrong: none beyond the serial alternative.
Task 10: fix round 1/5 (1 addressed, 0 open; commit 1559f0724)
Task 10: complete (commits 62918a4b2 + 1559f0724, review clean after fix round 1)
Task 6: review — Needs fixes. CRITICAL: filters.range comma-joined exclusions OR together → no-op for [7,18] (syntax was the controller's dispatch note). IMPORTANT: Number('') → 0 silently; header skipped by position. Fix round 1 dispatched. minor (deferred): explicit in_srs redundant; unreachable empty-line guard.
Ruling: the classification drop uses ONE `filters.expression` stage (`Classification != 7 && Classification != 18`) instead of the plan's `filters.range` — range clauses on one dimension OR together in PDAL; expression has AND semantics. Order test updated accordingly. Costs if wrong: a stage-type string in one test and the bake pipeline JSON; verified at first real bake (T9) by point counts with/without noise classes.
Task 3: review clean — Approved (values byte-checked vs README; rows mirror eox.\* pair). minor (deferred): slight module/field comment overlap.
Task 3: complete (commits b9579aa8d..036540905, review clean)
Task 11: implementer DONE (agent 79ca61ab5 → cherry-picked 9e44b5c94; 4/4 tests, full suite 8130 green; no registrySlice.test.ts — brief's checklist has no registry-only assertion, judged authoritative). Reviewer dispatched.
Task 7: fix round 1/5 (3 addressed pending re-review; agent 9578ea961 → cherry-picked 8cbc0b820; 7/7 tests). Re-review dispatched.
Wave C: T12, T13 implementers dispatched (base 8cbc0b820).
Task 7: fix round 1/5 (3 addressed, 0 open; commit 8cbc0b820)
Task 7: complete (commits b9579aa8d + 8cbc0b820, review clean after fix round 1)
Task 6: fix round 1/5 (3 addressed pending re-review; agent 4cf087ef9 → cherry-picked c4757e8a4; 7/7 tests). Re-review dispatched.
Task 11: review clean — Approved (registrySlice.test.ts omission accepted per testing.md). minor (deferred): comment ratio in hooks.ts / sagaContextRegistered.ts (parity with mcpm); serve check only proves HTTP 200.
Task 11: complete (commits 1559f0724..9e44b5c94, review clean)
Task 6: fix round 1/5 (3 addressed, 0 open; commit c4757e8a4)
Task 6: complete (commits c4098b9e5 + c4757e8a4, review clean after fix round 1). minor (deferred): empty dropClassifications → empty filters.expression (guard: omit stage when empty); truly-empty-file throw untested.
Task 13: implementer DONE (agent e27480837 → cherry-picked 408da9a2f; 2/2 new tests, suite 6/6; concerns: fovYRad hardcoded π/4, drain() marks dirty on any applied step). Reviewer dispatched.
Task 13: review clean — Approved (rightM/pan re-derived by hand, identical to mcpm's pan). minor (deferred): pitch clamp applied both mid-drag and on commit (shared PITCH_LIMIT, mirrors mcpm); fovYRad π/4 = T14 hook; drain() marks dirty unconditionally — re-judge in T14's loop.
Task 13: complete (commits c4757e8a4..408da9a2f, review clean)
Task 12: implementer DONE (agent c6204b218 → cherry-picked cc960a34b; 9/9 tool tests, suite 8148 green; headless Chromium: no GPU/page errors). Landmine: vite dev serves 200 text/html for a missing scenes.json (SPA fallback) — saga treats 404 OR non-JSON content-type as empty state; T15's empty-state condition must match. Saga context registered after initGpu resolves. Reviewer dispatched. T14/T15 wait for T12's review (they edit T12's files).
Task 8: implementer DONE (agent b9da5b173 → cherry-picked f42e04129; 34 tests incl. validateLasHeader; fetched 8/8 tiles = 105 MB, all pass byte-formula completeness; re-run skips). Reviewer dispatched.
Ruling: the T1 "truncation" hypothesis was WRONG — tiles are complete (offset + count × recordLength == size); this DHM product is ~0.45 pts/m² here (~3.9 M points total, served as uncompressed .las ~13–15 MB each). The Z-plausibility band from the earlier ruling was dropped by the implementer on live evidence (every tile carries noise outliers) — accepted. Tiles moved to main checkout data/raw/dhm/ (shared); worktree symlinks per file. T9 must re-derive heightMDvr90 from the full tiles and, if the 1 m thinning drops pointCount below 1e6, reduce minPointSpacingM (one-number edit) and ledger it. Costs if wrong: a re-bake.
Task 12: review — Approved with 1 Important to fix (headers: watchGroupSaga.ts 11 lines, acceptLoadedAsset.ts 13; report misstated) + 1 Important for the controller (group-load path never executed against real data → T14's visual check is the integration gate; T16 probe also exercises it). Fix round 1 dispatched. minor (deferred): registry reload auto-selects groups[0] (yanks user's selection — fix in T15/T16 or later); buffer label lacks asset id; content-type sniff would misread octet-stream JSON as empty (prod only); Viewport takes store as prop though useAppStore exists; store.subscribe marks dirty on EVERY action — setFps in the frame loop would self-sustain redraws (T14 must guard); duplicate asset ids overwrite without destroy; pointCount 0 untested; Viewport 100vw/100vh vs future panels (T15).
Task 12: fix round 1/5 (1 addressed pending re-review — headers 9 and 10 lines; agent 0f249552d → cherry-picked 67a729a51). Re-review dispatched.
Ruling: T14/T15 dispatched before T12's re-review closes — the open item is a comment trim in two files (watchGroupSaga.ts, acceptLoadedAsset.ts) neither T14 nor T15 touches; the files they DO touch (Viewport.tsx, renderResources.ts, App.tsx) were approved. Costs if wrong: none.
Task 12: fix round 1/5 (1 addressed, 0 open; commit 67a729a51)
Task 12: complete (commits cc960a34b + 67a729a51, review clean after fix round 1; group-load path first exercised on real data at T14/T16)
Task 8: review — Approved, 1 Important: headers over budget (validateLasHeader.ts 17 lines, fetchDhm.ts 13). Fix round 1 dispatched. minor (deferred): legacy u32 point count only (no LAS 1.4 extended-count fallback); orphaned .tmp after SIGKILL not pre-cleaned; Content-Length mismatch message not wrapped (contains no secret).
Task 8: fix round 1/5 (1 addressed pending re-review — headers 9 and 10 lines; agent 25c0b8438 → cherry-picked d198d9012). Re-review dispatched.
Task 8: fix round 1/5 (1 addressed, 0 open; commit d198d9012)
Task 8: complete (commits f42e04129 + d198d9012, review clean after fix round 1)
Task 15: implementer DONE (agent 70a2acfbd → cherry-picked 75782b307; LayerList test RED→GREEN, suite 8160 green; panel overlays Viewport absolutely because Viewport.module.css is T14-owned — visual check pending real points). Reviewer dispatched.
Task 15: review clean — Approved (empty-state condition byte-exact vs saga; narrow selectors avoid the fps re-render trap). ⚠️ hiddenAssetIds not yet read by any renderer — T14 must consume it (its brief says so). minor (deferred): no exported Props types (components take none); aria-label duplicates visible label text.
Task 15: complete (commits d198d9012..75782b307, review clean)
Task 14: implementer DONE (agent 69a963484 → cherry-picked 993aa66eb; parity RED→GREEN, 309 tests across the three tool suites; synthetic 360k-pt cloud in headless Chromium with depth + zero GPU errors). Escalation: bake frame is ENU Z-up, T13 rig is Y-up (yawPitchToDir + frameUp(undefined)). setFps deliberately not dispatched (no reader in plan 1). Reviewer dispatched.
Ruling: the group frame is ENU (Z-up) per spec §4 — the plan's T13 test expectations (Y-up) were the defect. Fix = T13 fix round on a fresh implementer: sceneCameraView becomes Z-up (yaw about +Z, pitch toward +Z, up reference +Z) via a proper rotation (never a y/z swap — that flips handedness and mirrors pan), test expectations recomputed by hand; createSceneInput unchanged (consumes rightM/upM). Spec §7.3 wording to be corrected at close-out. Costs if wrong: a mirrored pan or tipped horizon, visible immediately in the operator check.
Task 14: review — Approved, 1 Important: parity test never executes writeSceneCamera and its offset model ignores WGSL alignment (inherited from the mcpm sibling test). Fix round 1 dispatched (scene-workbench test only). minor (deferred): POINT_SIZE_PX=2 is DEVICE px (1 CSS px on Retina — bump to 3–4 after operator check); viewportH written but unread (like eyeM); depth view allocated per frame; visibleAssets allocates per frame; color varying could be @interpolate(flat); renderer is device-lifetime but rides scene-lifetime RenderResources (pipeline recompiles per group switch — plan 2 un-braid); black speckle on near faces = expected constant-px gaps (plan 2). Adjacent (backlog/final review): tests/tools/mcpm-workbench/render/mcpmCamera.parity.test.ts has the same two blind spots.
Task 14: fix round 1/5 (1 addressed pending re-review — writer executed with sentinels, alignment-aware layoutOf, mutation out[19]→out[20] demonstrated failing; agent edf0f5958 → cherry-picked 389236d03). Re-review dispatched.
Task 14: fix round 1/5 (1 addressed, 0 open; commit 389236d03)
Task 14: complete (commits 993aa66eb + 389236d03, review clean after fix round 1; operator visual check on real data pending T13 Z-up fix)
Task 13: fix round 1/5 (1 addressed pending re-review — Z-up via ENU_UP_BASIS det+1 rotation, handedness test added; real-data screenshots show level horizon through an orbit; agent b128d28b0 → cherry-picked 77485acc5). Re-review dispatched.
Task 13: fix round 1/5 (1 addressed, 0 open; commit 77485acc5 — det +1 basis verified by hand, handedness invariant real)
Task 13: complete (commits 408da9a2f + 77485acc5, review clean after the Z-up fix round)
Dev server started for the operator: http://localhost:5600 (shell batis6fee, this worktree).
Task 9: implementer DONE (agent 2368bba60 + 746f2b553 → cherry-picked ..544768acc; bake 1,615,969 pts / 25.86 MB / ~170 s, re-run idempotent; anchor 18.53 m via 5th-percentile fallback (no class-2 in this DHM product); class drop verified 391,668→391,661; FIXED three latent T6 bugs found only under real PDAL: LAS in_srs, filters.projpipeline for +proj=topocentric, writers.text header quoting — documented as README landmines). 101/1.6M Z outliers noted (deferred). Reviewer dispatched. T16 dispatched in parallel (disjoint files).
Task 9: review — Needs fixes. Important: (1) manifest group fields (anchor/name/id) never refreshed on re-bake; (2) CSV not removed when the read throws; (3) pack step buffers all points (correct given packPoints' array contract) — Ruling: document only; (4) lidarPipelineStages.ts header ~21 lines — Ruling: T16's comment sweep owns it (avoids a cherry-pick conflict). Fix round 1 dispatched for (1)(2)(3). minor (deferred): default_srs injected in bakeLidar not lidarPipelineStages (tested output ≠ written pipeline); spawn vs execFile (array args, fine); README flight-date GPS-UTC offset 18 s vs historical 15 s. ⚠️ Ruling 3 (class-drop delta 391,668→391,661) rests on the report only — accepted.
Task 9: fix round 1/5 (3 addressed pending re-review — nextManifest.ts extracted + tested, try/finally CSV cleanup, buffering documented; real re-bake 162 s confirmed anchor refresh; agent 883fc8854 → cherry-picked 83de3a00d). Re-review dispatched.
Task 9: fix round 1/5 (3 addressed, 0 open; commit 83de3a00d)
Task 9: complete (commits 544768acc + 83de3a00d, review clean after fix round 1; header trim of lidarPipelineStages.ts delegated to T16's sweep)
Task 16: implementer DONE (agent 013908c79 + c2a5a30ed → cherry-picked ..ec3c6b2a4; probe PASS 6 steps, injected shader error → FAIL → revert → PASS; 318 tests across three suites; sweep trimmed lidarPipelineStages.ts 24→10 + SceneAsset.d.ts). Note for review: a one-time fetch shim in watchRegistrySaga for blob: URLs through dataUrl() — judge. Reviewer dispatched. Full-branch typecheck + vitest run started in the controller worktree.
Whole-branch gate at ec3c6b2a4: `npm run typecheck` clean (both projects); `npx vitest run` 1213 files / 8166 tests passed (73.8 s).
Task 16: review — Approved, 2 Important carried into the final fix wave (Ruling: one fix dispatch after the whole-branch review, not a separate round): (a) ?probe blob fetch shim is a page-global window.fetch patch gated by ?probe — replace with the group saga skipping dataUrl() for absolute/blob: URLs and drop the shim; (b) no unit test for syntheticProbeScene's packer — add a parsePoints round-trip. minor (deferred): README says slices live in src/store/ (they live in src/state/).
Task 16: complete (commits 83de3a00d..ec3c6b2a4, review approved; 2 Importants queued for the final fix wave)
Final whole-branch review dispatched (opus) over d33744869..ec3c6b2a4.
FINAL REVIEW (opus, d33744869..ec3c6b2a4): Ready with fixes. No Critical. Important: #1 ?probe global fetch shim → resolve absolute/blob URLs in watchGroupSaga (both manifestUrl + artifactUrl), delete shim; #2 syntheticProbeScene duplicates the points.bin writer → import packPoints (header's "Node-only" claim was false); #3 default_srs injected in bakeLidar → LidarBakeSpec.defaultSrs emitted by lidarPipelineStages + order test; #4 dead store surfaces fps/setFps + reloadRegistryRequested. Minors to fix in-wave: #5 README slices path, #6 "LAZ"→".las" (README + LidarBakeSpec.lazFiles + fixture), #7 README points.bin path, #8 dhm README task-number references + stale line 80, #14 devPorts stale "reserved" comment. Deferred triage: all other ledger minors STAY DEFERRED (table in the review); POINT_SIZE_PX = user's call; mcpm parity-test blind spots = adjacent finding to offer. Spec/plan amendments 1–8 recorded for close-out.
Ruling: delete fps/setFps and reloadRegistryRequested (plan-sanctioned but nothing dispatches/reads them; code is liability; setFps is the live end of the every-action-dirty landmine). Costs if wrong: plan 2 re-adds them with the HUD that reads them.
Fix wave dispatched (single agent, opus).
Spec/plan amended to as-built (agent 34d8ae461 → cherry-picked eb3970a38): spec §5/§6/§7.1/§7.3/§11 + plan "As built" section. Plan line 24 points at the workspace ledger path — repoint to docs/superpowers/plans/completed/<plan>.ledger.md in the archive commit.
Final fix wave landed (agent d1c6b7bdb..0057209bd → cherry-picked eb3970a38..f359f7067): #1 resolveAssetUrl + shim deleted; #2 probe scene uses packPoints (cast required: TS2322 on BlobPart); #3 LidarBakeSpec.defaultSrs + SceneGroupDefinition.sourceSrs, order test extended; #4 fps/setFps + reloadRegistryRequested removed; #5/#7/#8/#14 docs (dhm README line ~205 extra refs fixed too). Probe PASS with no baked data; 324 tests; typecheck/eslint/prettier clean. Left: soendermarken.ts still says "task 1's list"/"task 9's real bake" (one-line follow-up). Scoped re-review dispatched.
Whole-branch gate at f359f7067: typecheck clean; `npx vitest run` 8169 tests passed (60.9 s).
Final fix wave re-review: all 8 addressed, no new breakage. Residual: stale plan-task references in bakeLidar.ts:7-8 header and groups/soendermarken.ts comments — Ruling: comment-only, folded into the ledger-archive commit (not a second fix wave). Costs if wrong: none.
Final review clean at f359f7067 → archive ledger, /feature-done, finishing-a-development-branch.
