# SDD ledger — plan: docs/superpowers/plans/2026-09-19-dome-fisheye.md

Spec: docs/superpowers/specs/2026-09-19-view-rigs-dome-fisheye-design.md (live; sections "Dome rig (feature PR)", "Testing", "Risks and eye-checks").
Worktree: .claude/worktrees/dome-fisheye, branch worktree-dome-fisheye, base ab5fcc744 (origin/main, includes prep #769 e62c2217f and #798).
Protocol: lean SDD (docs/superpowers/conventions/sdd-execution.md) — grouped Sonnet dispatches, one final Opus review, one fix round, CI is the gate.
public/data symlinked to main; dev server NOT started yet (user declined at setup; start for Task 5 eye-check).

## Standing user rulings carried into this PR

- Keep fov-dependent star photometry (constant per solid angle).
- Recorder `--frames N` lands in Task 6 (applies to every take); Task 7 takes the 150-frame test film FIRST and hands the file to the user for wisdome-teknik.kf@malmo.se.
- Overlays off in the dome rig is a content decision (no OVERLAYS section in the dome program).
- H.264 level 6.1 for 4096² (libx264 under --dome only; VideoToolbox stays for non-dome).
- Deletion audit runs at THIS feature's /feature-done (skipped on the prep PR).
- OPEN (user unanswered): VR-gap backlog item — horizon shell + ZoA symmetric cone.

## Pre-flight (2026-09-21, report: preflight.md in this workspace)

Symbol-level scan against ab5fcc744: 33 of 35 assumed contracts match; the 2 "differs" are the expected pre-Task-2 `fixedSizePx.layers` state and one stale field path. Pairwise table and per-task self-consistency rows are in preflight.md §4–§5. PRELUDE-before-face-submit ordering CONFIRMED (renderFrame accumulate/flush). Compositor uniform-per-key landmine CONFIRMED.

Rulings:
- Ruling P1: Task 1 creates ALL five `src/utils/dome/*.ts` files (the plan's missing create-markers imply stubs that never existed) — trivial — no cost if wrong.
- Ruling P2: Task 5's terrain-pick-marker gate reads `ctx.snapshot.cursorTexPx` (post-P6 split), not `ctx.cursorTexPx`; brief carries the correct path — no code change beyond what the plan already asks.
- Ruling P3: Task 4's "check this PR 1 assumption" bullet is a no-op verification (ordering already holds); the brief says so, so the implementer does not re-order submits.
- Ruling P4: Task 2's layered-row test ALSO asserts `viewOf('dome-cube')` returns one whole 2d-array view for a 5-layer (non-cube) row, since Task 3's pass depends on that codepath and only 6-layer rows exercise it today — cost if wrong: one extra assertion.
- Note: `checkFrameOrder`'s caller still passes the flat legacy `FRAME_ORDER`; Task 4's `startLoop` edit is a real behaviour change (Open point #5 as written).

## Plan-start rulings (user, 2026-09-21)

- Grouping: A = T1+T2 · B = T3+T4 · C = T5+T6 (one Sonnet each, sequential) · T7 film run by the controller.
- Perf gate: NONE.
- VR-gap backlog item (horizon shell + ZoA cone): DROPPED — do not record.
- Draft PR opens after group A's first commit (branch has no diff vs main before that).
- Dev server: start before Task 5's eye-check.

## Tasks

Group A (T1+T2): BASE ab5fcc744 — DONE_WITH_CONCERNS. Task 1: complete 7ee467fa9 (8 tests). Task 2: complete 62eac7290 (full suite 14004 green). Report: group-a-report.md.
- Ruling A1: implementer widened `ViewRigKey` to `'mono' | 'dome'` and added a placeholder `VIEW_RIGS.dome = { views: () => null, program: [] }` so `allocateWhen: state.viewRig === 'dome'` typechecks — ACCEPTED as a transitional state; Task 4 replaces the placeholder with the real row (and adds `pickable`). Inert at runtime (nothing seeds 'dome' until Task 5). Cost if wrong: none, Task 4 overwrites it.
- Ruling A2: `dome-cube` joined the "clears to opaque alpha" invariant test beside hdr/swap — accepted (the brief's clear colour has a = 1).
Draft PR #800 opened at 62eac7290.

Group B (T3+T4): BASE 62eac7290 — DONE. Task 3: complete 94df7ac5e (resample shader/renderer/pass, parity test). Task 4: complete 0d5ae3ab8 (dome rig, pickable, checkFrameOrder over every rig; `frameOrderBoot.test.ts` adapted to the new signature). Full suite 14029 green. PRELUDE-before-face ordering verified, no change. Report: group-b-report.md. Pushed to #800.
Dev server: http://localhost:5175 (started 2026-09-22 on user ask).

Group C (T5+T6): BASE 0d5ae3ab8 — DONE. Task 5: complete 137723964 (?dome seed, square canvas CSS, pick + cursor gates; EYE-CHECK PENDING on :5175). Task 6: complete af8ac6a19 (--dome, --frames, ffprobe fields, README; collateral `dataset: {}` on the destroyOrder test's stub canvas). Suite 14033 green. Report: group-c-report.md. Pushed to #800.

CI green on af8ac6a19.
Coordination (2026-09-22, star-catalog-layer session, PR #799): dome branch touches NEITHER runFrame's star block NOR LayerInstance.frame NOR any star-cut/focus/orbitTrails file (49-file diff, all dome/frame-step/recorder). Rig views reach computeStarCut/advanceStarFades through the existing `VIEW_RIGS[state.viewRig].views` seam, capture faces never enter that list. Agreed: #799 lands first, then main is merged into this branch (merge, not rebase — same as #769; squash at the end). Star PR 2 owns widening LayerInstance.frame to the views list; no parallel mechanism here.

2026-09-22: #799 merged; origin/main (29470a2d9) merged into the branch, clean, typecheck:fast clean, suite 14154 green, pushed.

## Smoke 2026-09-22 — HALTED for architecture work (user ruling)

User smoke on :5175 `?dome`: by design — no 2D labels (OVERLAYS off), no picking (`pickable: false`). DEFECTS: A Sgr A* cube maps misaligned; B structure circles do not line up past the MW (and they should not draw at all if overlays); C MW blobs misaligned across face seams (3D labels fine); D froxel atmosphere (from #798) wrong per face (Mars/Perseverance pose); E `aerial-perspective` BODY pass does not render at Søndermarken under dome.
Opus diagnosis DONE → `smoke-diagnosis.md` (585 lines): 4 root causes, ALL latent multi-view bugs outside the dome diff. R1 aerial-perspective compute in PRELUDE (once) bakes froxels + shellUniformBuffer from the canvas vp → D + E-visual. R2 `worldLenToPx` in `lib/billboard.wesl` omits cot(fovY/2) → rings 0.577× in mono vs 1.0× in 90° faces → B (and `structure-markers` is in SCENE, not OVERLAYS). R3 MW sprites view-plane-aligned → flip across seams → C. R4 body-slab index baked into frame-wide timing slot → E's panel reading. A not reproduced by reading (likely R2 + `edgeFadeEndRs` keyed on drawPxPerRad); two eye-checks pending with the user. Missing joint: scope decides where a step RUNS, nothing decides where its consumed data is PLANNED; 9 class-3 items (§ Architecture); captures already give 3 primitives (view-keyed GPU slot, view-keyed CPU memo, view-keyed identity string); the compute case is the one new joint. (root causes A–E + "Architecture: frame-scoped vs view-scoped inputs" inventory).
USER RULING: "treat this as an insight into the engine architecture. I don't want to cut corners with fixes, I'd rather do some proper refactoring to reach an architecture that is sound and well designed" → NO point fixes. Next: diagnosis → entanglement-radar/refactor-ground over the frame-vs-view seam → design with the user (brainstorm) → spec/plan → prep refactor (own PR or riding #800, user's call) → then finish dome. Deadline Fri 2026-09-25 is at risk; the user chose architecture over speed.

USER RULING 2026-09-22: labels ARE wanted in the dome film, lower priority than the per-view joint. Scope for refactor-ground: the joint (R1–R4 + class-3 items) first; the label directors (3.3) join as a consumer of the joint; where dome text is drawn (per face vs on the fisheye) is a design question for the brainstorm, sequenced after.

## Refactor-ground IN PROGRESS (2026-09-22)

- Controller sketch written: `ideal-shape.md` (plan step kind + ContentPlanner registry + ViewMemo; aerial compute → SCENE; pxPerRad in CameraUniforms; MW eye-facing basis; timing identity by view slot; prep P1–P5; tensions T1–T3).
- LIVE AGENT: greenfield cross-check (Opus, no repo access) → writes `greenfield-shape.md`. HANDLING: diff it against `ideal-shape.md`; every divergence is either a priced real constraint (goes to checkpoint item 2) or inherited shape (reshape the sketch). Then present the refactor-ground CHECKPOINT to the user: ideal shape, tensions priced, missing joints with file:line, prep list, adjacent findings (3.8, 3.9 → backlog), THE ASK (shape sign-off + "prep as separate PR or riding #800?"). No spec text before sign-off. If the shape has real branches → grill-me instead of yes/no.
- Two Sgr A* eye-checks still owed by the user (mono FOV 60→90 rings grow?; Sgr A* across a seam moves with it?).
- After sign-off: spec "Ground preparation" section (spec stays live) → prep plan → lean SDD → dome smoke → T7 film → dome text design (OVERLAYS per view vs fisheye space) later.

Queued (after the refactor): user eye-check `?dome` on :5175 → Task 7 film (`--frames 150` first) → final Opus review → one fix round → CI → smoke checklist → /feature-done (deletion audit runs here) → squash via API on user's word.

## Refactor-ground CHECKPOINT SIGNED OFF (user, 2026-09-22)

Shape as in ideal-shape.md § Cross-check diff (D1–D6 adopted, D7–D9 not). Rulings:
- Planner joint: `PlanStepSpec` + `ContentPlanner<T>` with `scope: 'once' | 'perView'` returning `{ value, animating }`; ONE `Plans` store on the frame, `get(planner, view)` THROWS on a miss; `LayerInstance.planners` list, `LayerInstance.frame` hook RETIRED (galaxyCatalog `frame.ts` splits into a once reconcile row + a perView disk-lod row); `ContentCompute` gains `scope`; `checkFrameOrder` rejects a perView row in a once section. OVERLAYS becomes a perView section (one view in mono).
- Ring size: FIX `worldLenToPx` via `pxPerRad` in the CameraUniforms prefix (`_pad0`) AND RETUNE constants ×1.73 so mono at 60° looks as today. Five per-renderer pxPerRad copies → deletion audit at feature-done (not prep).
- Aerial-perspective compute → `scope: 'perView'`, line moves to SCENE head; volume stays shared.
- Timing: `ViewSpec.id`/`FrameView.id` named by the rig; slot = `work@view.id`; update `src/utils/perf/foldCaptureFaceRows.ts`.
- MW sprites: eye-facing per-instance basis, drop camRight/camUp.
- Backlog: captures-as-views (D7), v2 field per-view uniforms (3.8), off-axis fovY reconstruction (3.9).
- PACKAGING: SEPARATE PREP PR off main, lands before #800; #800 merges main afterwards (as with #799).
- Coordination: tell the star-catalog session `Layer.frame` retires → planner rows (replaces its N1).
Next: spec "Ground preparation 2" section + prep plan in the prep worktree → lean SDD → merge → main into #800 → dome smoke → T7.
- 2026-09-22 HAND-OFF WRITTEN (this session is isolated to dome-fisheye; cannot edit/commit in `dome-planning-prep`): scratchpad `prep-handoff/{HANDOFF.md, spec-ground-preparation-2.md, 2026-09-22-per-view-planning-prep.md}` (7 tasks). Plan-time amendments to the checkpoint: planning precedes encoding (plan rows lead their section; once rows run before capture scheduling — captures read the settling vote); disk LOD = ONCE row over views (2.5M-row walk, not per face); label directors/3-D labels/star cut/surface cut stay in runFrame (OVERLAYS stays once until dome text). Retune table per worldLenToPx consumer (ring band ×1.732, worldEmMpc ×0.577, aggregate glow factor ×0.577, STAR_SPHERE_RESOLVE_PX ×1.732). NEXT: user opens a session in `dome-planning-prep` and follows HANDOFF.md; this session resumes at "merge main into #800" afterwards.
- USER RULED 2026-09-22: the prep PR runs in a NEW session opened in `.claude/worktrees/dome-planning-prep` (not via isolation agents from here). Hand-off files copied to `.superpowers/sdd/2026-09-19-dome-fisheye/prep-handoff/` (HANDOFF.md, spec section, plan) — durable copy; the scratchpad copy is disposable. THIS SESSION IS PARKED until the user says "prep merged"; then: merge main into #800 → resolve `checkFrameOrder` signature + add `ViewSpec.id` for the five dome faces (`dome:front|left|right|back|top`) → typecheck/suite → push, watch CI → user re-smokes `?dome` on :5175 (defects A–E) → T7 film. Still owed by the user: two Sgr A* eye-checks.
- 2026-09-22 coordination note SENT to peer session `skymap-ba` (the only peer listed; asked to confirm it is the star-catalog session, wt `star-catalog-layer`, draft #802 whose T1 = N1 hook widening): Layer.frame/LayerInstance.frame/LayerFrameVote/layersSettling deleted → Layer.planners?(runtime) rows, ContentPlanner scope once|perView, plan rows lead their section, Plans.get throws; N1 = a once-scope star-cut row; whichever PR merges second rebases the hook onto a row. Awaiting its reply (record which case it is in).
- REPLY from star-catalog session (skymap-ba, #802): CONFIRMED. Its T1 already landed (69032619b widens Layer.frame to (views, state)); T3 mid-flight moves star cut + fade advance into that hook. #802 continues as is; whichever PR merges SECOND rebases the star hook onto a scope-'once' ContentPlanner row (mechanical). Its vote is { awake: anyNodeFading, settling: false } — PlanResult fits unchanged. Recorded in both ledgers.
- 2026-09-22 (later): prep PR is LIVE as draft #805 in session `skymap-94` (per memory, written by that session; ledger wt `dome-planning-prep/.superpowers/sdd/2026-09-22-per-view-planning-prep/progress.md`, group A dispatched). Cross-session messaging works (ListAgents/SendMessage): star-catalog session = `skymap-ba`; the prep session was NOT listed at last check — re-list before messaging it. No agents live here; nothing uncommitted. Still PARKED until "prep merged".
- 2026-09-22 USER: "prep merged" (#805 → main 43ff07f19). UNPARKED. `git merge origin/main` started (base 0bdee7588): 4 conflicts (checkFrameOrder.ts, startLoop.ts, checkFrameOrder.test.ts, frameOrderBoot.test.ts) — union resolution = `checkFrameOrder(programs: FrameSection[][], passes, computes, planners, targets)`, per-program draw/compute/plan counts, across-program presence; dome faces get `id: dome:<name>` via new `DOME_FACE_NAMES` in `domeFaces.ts`. Brief `main-merge-3-brief.md`, Sonnet agent dispatched; report → `main-merge-3-report.md`. Then: push → bg `gh pr checks 800 --watch` → user re-smokes `?dome` on :5175 → T7 film. Note (memory): star #802 still has to merge main and port its frame hook to a planner row before ITS merge; if #802 lands first, merge main into #800 once more.
- MERGE DONE: 4b56847c6 (typecheck clean, suite 1399 files / 14227 tests, report `main-merge-3-report.md`), PUSHED; CI run 35732507744 in progress, bg watch running. NEXT: user re-smokes `?dome` on :5175 (defects A–E from smoke-diagnosis.md: Sgr A* cubemaps, structure rings, MW blobs, froxel atmosphere, aerial-perspective body) → T7 film.
- CI GREEN on 4b56847c6 (run 35732507744). USER: "also merge in main" → main had moved to b1e453e02 (#802 starCatalog Layer). Merged: 3 conflicts (EngineGpuHandles.d.ts, engine.ts, gpuHandleRegistry.ts — star-catalog renderer rows moved into the Layer on main; kept only the dome rows), resolved by hand; typecheck clean; suite 1405 files / 14474 tests. Merge commit dcd547116 PUSHED; CI run 35737408359 in progress, bg watch. Branch now carries #805 + #802. NEXT unchanged: user re-smokes `?dome` on :5175 (A–E) → T7 film.
- CI GREEN on dcd547116. USER RE-SMOKE after #805+#802: D FIXED (cannot reproduce), E GOOD; B, C, A STILL THERE. Diagnosis (read-only, this session):
  - B rings: planner per view is clean (no culling, same eye/pxPerRad on all faces). `structureMarker/ring.wesl` + `halo.wesl` + `ringPick` expand a SCREEN-ALIGNED quad (`pxToClipOffset`, radius from `centerClip.w`) — a circle in face A's image plane and the same ring in face B's are different curves on the sphere → halves never meet. Fix shape: eye-facing WORLD disc (basis like `sgrAStarLensing/vertex.wesl:24-31`), band width from screen derivatives (`fwidth(d)`), drop the `sizePx` smuggle. Mono: on-axis identical, off-axis rings become the correct perspective ellipse (≤ ~20% radial stretch at 60° corners). Awaiting ruling.
  - C MW sprites: basis IS eye-facing now (prep), but `stars.wesl:97-131` px clamp + `apparent` LOD + `clampFluxScale` are measured by projecting the corner through THIS face's viewProj — a sprite straddling a seam sits at different off-axis angles in the two faces (1/cos² scale differs) → different size/brightness either side. Fix shape: measure apparent size angularly (`worldHalfExtent / dist · pxPerRad`), view-independent across a rig. `dust.wesl` same.
  - A Sgr A*: screenshot = a dark strip (no lens quad drawn) between the front face's near-plane cut and the front/top seam. `bodySlabRow` (`slabs.ts:172-193`) sets near = viewZ − margin with margin = max(1.05·footprint, drawRadius) = ~1 Rs for Sgr A*, while the lens quad (`lensQuadPlaneRadiusRs`, up to 8·dist) is a flat disc facing the anchor: off-axis by θ its near side reaches viewZ − R·sinθ → clipped. Mono never shows it because the hole is centred (θ≈0). Fix shape: the body row's drawn envelope must include the lens quad for the lens body (distance- and pxPerRad-dependent, so not a static `bodyDrawRadiusM`) — joint to design. Awaiting ruling.
- USER RULED 2026-09-22: "show the world-space solution for structure discs first, then I'll say if good enough"; "fix these things structurally and neatly, ideally no special cases"; overlays must honour SCREEN SPACE in mono AND dome (dome output = the fisheye image, not a face) — open design thread, see below.
  - B LANDED locally be39db16a (brief `fix-b-brief.md`, report `fix-b-report.md`): `expandBillboardWorld` (eye-facing, lib/billboard.wesl), ring/halo world discs projected per corner, band width from dpdx/dpdy, `camPosMpc` at byte 80 of the marker uniform, `MARKER_RADIUS_RETUNE`. Suite 14475 green. NOT pushed; user eye-check on :5175 pending.
  - C + A DISPATCHED (Sonnet, brief `fix-c-a-brief.md`, report `fix-c-a-report.md`, two commits): C = MW sprites sized ANGULARLY (halfRad·pxPerRad), exact per-corner projection, `apparent` reproduces the old on-axis NDC value; A = `bodyDrawRadiusM(body, distM, pxPerRad)` + `BODY_DRAW_ENVELOPES` registry (earth cloud shell, Sgr A* lens quad via `lensEdgeFadeEndRs` + `lensQuadPlaneRadiusRs` + margin const with WESL parity), Sgr A* cull bypass in `visibleSlabBodies` DELETED.
  - OPEN DESIGN (user's screen-space point): pixel-tuned widths (ring band, sprite px clamp) are currently in FACE pixels; the dome OUTPUT is equidistant (N/π px/rad uniform) while a face is N/2 px/rad on axis and 1/cos²θ denser off axis → a 1.5 face-px band reads ~1 px on axis and ~0.5 px at a seam after the resample. Candidate joint: `FrameView.outputPxPerRad` (mono = drawPxPerRad; dome = N/π) carried in the camera prefix for TUNING while `pxPerRad` stays the view's own GEOMETRIC scale; true overlays (labels, selection ring, marker lines) draw ONCE against a rig OUTPUT VIEW whose projection kind is perspective|equidistant. Not started; brainstorm with the user after the three fixes are judged.
- C LANDED 545e722ca, A LANDED 32fc00408 (report `fix-c-a-report.md`; agent's ruling: slabs test at dM=1000 r_s not 30 r_s — at 30 r_s the LUT floor (50 r_s) exceeds the distance so the quad hits the 8·dist cap and near floors at MIN_NEAR_M, invariant trivially true). Suite 14489 green after each. All three fixes LOCAL ONLY (HEAD 32fc00408, 3 ahead of origin); user eye-check on :5175 (mono + ?dome) pending → on the user's word: push, bg `gh pr checks 800 --watch`, then brainstorm the output-space/overlay design.
- USER VERDICTS 2026-09-22: B ACCEPTED ("eye-facing world discs do actually work, also in mono; looks slightly different, that's ok"); A RESOLVED ("black hole lensing issue is resolved"); C STILL A PROBLEM "with very large sprites" at pose `?dome#pose=a,0.001882453192800851,-0.001646175001440048,-0.005080232161920116,-1.5946237214103498,-0.5062349232051033,0.007096374902799988,0` (camera ~9 kpc from Earth, inside the MW). Hypothesis: the flat eye-facing disc's own RIM — a cloud the camera is inside/near covers at most atan(r/d) and shows a bounded blob with an edge in a 180° view (hidden at 60°); alternative: a residual seam mismatch. Screenshot requested from the user; the fix shape depends on which (rim → spherical/angular glow profile in the fragment, cover the full solid angle; seam → re-diagnose). Nothing pushed yet (HEAD 32fc00408, 3 commits ahead); no agents live. NEXT: user screenshot → fix C properly → push → bg `gh pr checks 800 --watch` → output-space/overlay brainstorm → T7 film.

## 2026-09-22 — re-smoke round 2 (user poses), C fixed, A re-diagnosed

- Headless shot tool for any share URL: `npx tsx <scratchpad>/shot.mts '<url with ?perf&cinema[&dome]#...>' out.png [size] [disabledPasses] [tuningJSON] [fovDeg]` (scratchpad `/private/tmp/claude-501/-Users-rulkens-Development-js-skymap/8f07f5d3-ffe8-4071-b6bc-9822f1414e9b/scratchpad/`). Isolation guard refuses `$VAR` in the npx line — spell the URL literally.
- **C root cause + fix (commit 98a5a0748)**: the far-plane clip-z clamp in `milkyWay/sprites/{stars,dust}.wesl` is inert on NEAR0 (reversed-Z, infinite far) and, combined with exact per-corner projection, clipped sprites straddling a face's eye plane along z=0 at the wrong parameter → each face lost a different sprite set → brightness step along face frustum outlines (pose: Earth focus, 1.34 kpc, `#t=2026-09-22T19:01:53.926Z&pose=a,4.865669104983766e-12,-4.531343324827035e-14,-1.9643178596805234e-14,-1.162064034583676,-0.25078222710665427,0.0013385866921616898,2.7755575615628914e-17`). Verified seamless after deletion. The first pose (9 kpc) was NOT a reproduction — sprites there never cross an eye plane.
- ADJACENT (unruled): the same idiom sits in 6 more shaders: constellations, bodies/starPoints, bodies/bodyGlint, markerLines, labels, starCatalog — all inert on reversed-Z slabs (NEAR0 + body rows), harmful for any primitive straddling a view's eye plane (constellation lines!). labels also draw on COSMO (forward-Z, finite far) where it is still load-bearing.
- **A re-diagnosed** (pose: Sgr A* focus, 5 r_s, `#focus=body-sgr-a-star&t=2026-09-22T19:03:01.453Z&pose=a,-0.0004469860712306233,-0.007138118108456056,-0.0039657480154262886,-2.6503909651238726,0.11195544194044754,1.9301377281777415e-12,-2.7757188644932276e-17`): the lens quad is a FLAT disc (radius capped 8·dist → angular radius ≤ 83°) so in a 180° view its rim shows as a straight cut; at 5 r_s every ray has b < edgeFadeEnd (50 r_s), the lens covers the whole sphere. Proposed shape (awaiting ruling): per-view FULLSCREEN triangle — clip = fixed NDC, `posRelCamM` = the view's frustum-corner directions in the body frame (from `pose.basisM` + `view.frustum` tangents, 3 vec4 in the uniform, or basis+tangents), fragment early-discards `impactParamRs >= edgeFadeEndRs`; envelope for cull/slab becomes the impact-parameter sphere `edgeFadeEndRs·r_s` (rays with b ≤ e hit that sphere) → DELETE lensQuadPlaneRadiusRs(+test), SGR_A_STAR_LENS_QUAD_MARGIN, LENS_QUAD_MARGIN (+parity row), `quadPlaneRadiusRs` uniform field; hdr has no depth attachment and the pass is premultiplied-over, so a fixed clip z is safe. Files: listed by grep in session (14).
- HEAD 98a5a0748, 4 ahead of origin, unpushed.
- USER: "C seems fixed now" (accepted). Pushed 98a5a0748 → origin; PR #800 head = 98a5a0748, one check-run registered; bg `gh pr checks 800 --watch` (shell b29gcbjn2) — on completion: report green/red; red → read the failed log and diagnose. Never merge on green.
- QUEUED: (1) user ruling on A's fullscreen-lens shape (above) → Sonnet brief `fix-a2-brief.md`, verify with the shot tool at the Sgr A* pose; (2) user ruling on the six remaining clip-z clamp copies (delete here vs backlog); (3) output-space/overlay brainstorm; (4) T7 film; final Opus review; /feature-done (deletion audit incl. five per-renderer pxPerRad copies); API squash on the user's word.
- **A landed (df1330334, amended for timeless comments, pushed)**: per-view fullscreen lens triangle (ray from `viewBasis` mat3 + `frustumTan` in the uniform, bytes 176..239, 60 floats), fragment early-discard past edgeFadeEndRs, envelope = impact-parameter sphere `sgrAStarLensEnvelopeM` (file renamed from sgrAStarLensQuad.ts); lensQuadPlaneRadiusRs + both margins + quadPlaneRadiusRs deleted. Perf flat (11.8→11.7 ms, lens slot 0.7→0.6). Suite 14482 green. Dome shot continuous to the rim; mono identical. Report: fix-a2-report.md.
- CI ANOMALY: no GitHub Actions `pull_request` run has registered for 98a5a0748 nor df1330334 (last PR run 19:12Z on another branch); only the Cloudflare Workers build ran (green). bg `gh pr checks 800 --watch` = shell bb1iwwluw. If still absent: close+reopen the PR or push an empty commit to re-trigger, then watch again.
- Adjacent (ELABORATED to user, unruled): delete dead clip-z clamp in starPoints/starCatalog/bodyGlint (mechanical); keep in labels/markerLines (COSMO forward-Z path); constellations need a near-plane segment clip in lib/segmentQuad.wesl before expansion (design item) — ride this PR or backlog = user's call.
- HEAD df1330334 == origin.
- CI anomaly RESOLVED: the PR was CONFLICTING with main (#808/#809), and GitHub skips pull_request runs it cannot build a merge commit for. Merged origin/main (one conflict, frameOrderBoot.test.ts imports, union) → 9460a68ee, pushed, PR MERGEABLE. bg watch = new shell; report when settled.
- COMPACT POINT: HEAD 9460a68ee == origin, tree clean. Live bg: `gh pr checks 800 --watch` (shell bnmvqcivt) on 9460a68ee — on completion report green/red (red → read the failed log, diagnose); never merge on green. AWAITING USER: browser verification of C (Earth pose) and A (Sgr A* pose); ruling on the adjacent clip-z clamp finding (recommendation: delete in starPoints/starCatalog/bodyGlint, keep labels/markerLines, constellations = near-plane segment clip in lib/segmentQuad.wesl as its own item — ride PR or backlog). THEN: output-space/overlay brainstorm → T7 film (`npm run record-clip -- earthUniverseLoop --dome --frames 150 --serve --rebuild` first) → final Opus review → /feature-done (deletion audit incl. five per-renderer pxPerRad copies) → API squash on the user's word.
- CI GREEN on 9460a68ee (typecheck·test·format 4m57s + Workers build). No live agents/shells.
- USER ATTESTED 2026-09-22: "sgr a* lensing now works in both mono and dome" — A CLOSED in the browser. C accepted earlier ("C seems fixed now"). STILL AWAITING: ruling on the adjacent clip-z clamp finding (recommendation unchanged: delete in starPoints/starCatalog/bodyGlint, keep labels/markerLines, constellations near-plane segment clip as its own item — ride PR or backlog); go for the output-space/overlay brainstorm vs straight to T7 film.
- Clamp deletion LANDED da9807602 (brief clamp-delete-brief.md, report clamp-delete-report.md): CLIP_Z_EPS gone from starPoints/bodyGlint/constellations/starCatalog + pickDepthBands comment + backlog item 1 retired; labels/markerLines keep theirs (COSMO forward-Z). Suite 14476 green; Earth-pose shots mono+dome render normally. PUSHED; bg `gh pr checks 800 --watch` = shell b40x66ujt. Constellation near-plane segment clip = still open, own item.
- USER 2026-09-22: "before we do the labels, i think we should make a video" → T7 film NOW, overlay/labels brainstorm AFTER. Short take running: `npm run record-clip -- earthUniverseLoop --dome --frames 150 --serve --rebuild --out recordings/dome-short-150.mp4` (bg shell bu9d1cxuc). On completion: ffprobe (h264/Main/61/yuv420p/4096²/30), stills at 2 s, then FULL take (4440 frames) — report the per-frame rate first; then hand the path to the user for wisdome-teknik.kf@malmo.se.
- SHORT TAKE DONE: recordings/dome-short-150.mp4 (150 frames, ~0.6 s/frame incl. boot; build ~1 min). First probe showed pix_fmt yuvj420p / color_range=pc (JPEG full-range passed through libx264) → FIX 4c4e0eceb (buildFfmpegArgs dome argv: `-vf scale=in_range=full:out_range=limited -color_range tv`, test updated), re-take probes h264/Main/61/yuv420p/tv/4096²/30. Stills at 2 s + 4.9 s: black surround, Earth low-centre, no seams. CI GREEN on da9807602; 4c4e0eceb pushed, bg watch running.
- FULL TAKE RUNNING (bg shell): recordings/earthUniverseLoop-dome-4096-30fps.mp4, 4440 frames, ETA ~45 min from 22:32. On completion: ffprobe, stills at 2/35/74 s, hand path to user for wisdome-teknik.kf@malmo.se. Then overlay/labels brainstorm.
- USER: "stars are doing their firefly thing" in the short film → diagnosed as resample minification aliasing (single bilinear tap at mip 0 over a 1.6–3× shrink; 1.5 face-px star floor lands sub-pixel in the output). USER RULED option 1 (multi-tap resample) now, option 2 (output-space floors, outputPxPerRad) for the brainstorm. LANDED 1b27b2d60: domeResample.wesl samples a 3×3 box over the pixel footprint (footprint from dpdx/dpdy of uv, read before any branch), rim antialiased. Full take STOPPED on user request (482 frames, 1.5 fps = 0.67 s/frame, ~50 min projected; partial file deleted). Re-take recordings/dome-short-150-taps.mp4 (37 MB vs 52 MB before, same probe values) opened for the user. CI GREEN on 4c4e0eceb; 1b27b2d60 pushed, bg watch running.
- OPEN (offered, unruled): per-stage timers in record.ts frame loop (grant/render, CDP capture, pipe write) printed on the progress line — ~15 lines, before the full take so it doubles as the measurement.
- USER 2026-09-22: multi-tap resample "slightly better, not a lot of difference"; firefly experiments (crf 8 encode; star floor doubled in face px) DEFERRED on user word — "ok for now, render the full length one". Films live in ~/Movies/skymap (both short takes moved there). FULL TAKE RUNNING detached (nohup, log scratchpad/full-take.log): ~/Movies/skymap/earthUniverseLoop-dome-4096-30fps.mp4, 4440 frames, ~50 min from 22:46. On completion: ffprobe, stills 2/35/74 s, tell the user. Profiling timers in record.ts still unruled.
- FULL TAKE DONE 23:31: ~/Movies/skymap/earthUniverseLoop-dome-4096-30fps.mp4 — 4440 frames / 148 s, 2.0 GB, ffprobe h264 Main level 61 yuv420p tv 4096x4096 30/1 (T7 DoD values recorded). Rate 0.62 s/frame, 46 min wall. Stills 2/35/74 s + native crop at 35 s in scratchpad (full-still-*.png, full-crop-35.png). NEXT: user views; firefly experiments (crf 8; star floor ×2) and record.ts stage timers still unruled; then overlay/labels brainstorm → final Opus review → /feature-done → API squash.
- USER: "the video looks pretty cool" → T7 film ACCEPTED. File too big to mail → R2 upload IN FLIGHT (bg shell bzp4f9ik3 polls; scratchpad/r2-upload-film.sh via rclone from keychain S3 keys) to r2:skymap-data/films/2026-09-22-earthUniverseLoop-dome-4096-30fps.mp4 → public https://skymap-data.rulkens.com/films/2026-09-22-earthUniverseLoop-dome-4096-30fps.mp4 (HEAD 404 until multipart completes). On completion: curl -sI → report link to user for wisdome-teknik.kf@malmo.se. USER RULED ("ok nice"): 90-day lifecycle rule on films/ + DEPLOY.md Films section. Doc LANDED cbbc5a2b7 (pushed; CI watch NOT started for it — docs-only, start one at next push). LIFECYCLE RULE NOT APPLIED: permission classifier blocked `wrangler r2 bucket lifecycle add`; the user must run it (command given in chat: CLOUDFLARE_API_TOKEN from keychain `skymap-cloudflare-api-token`, `npx wrangler r2 bucket lifecycle add skymap-data films-expire-90d films/ --expire-days 90 --force`); verify with `lifecycle list`.
- HEAD cbbc5a2b7 == origin, tree clean. STILL UNRULED: firefly experiments (crf 8 take; star floor ×2 in face px), record.ts stage timers, resumable-take backlog entry (offered, not filed). THEN: overlay/labels brainstorm (outputPxPerRad, overlays once against the rig output view; firefly option 2 = output-space star floor belongs here) → final Opus review → /feature-done (deletion audit incl. five per-renderer pxPerRad copies) → API squash on the user word.
- UPLOAD DONE 23:48: films/2026-09-22-earthUniverseLoop-dome-4096-30fps.mp4 on R2, public HEAD 200 video/mp4 2008873975 bytes. Link given to the user for wisdome-teknik.kf@malmo.se. Lifecycle rule STILL the user's to run (command in chat).

## 2026-09-23 — overlay brainstorm → landing
- BRAINSTORM RULINGS: scope = labels + pixel floors (firefly option 2), NOT marker lines/selection ring; constellations + rings already draw per face (no work); label orientation = upright toward the zenith (front-at-bottom dome master → identical to mono at the front); USER RULED "labels can be a follow up, we could already land this work" → #800 LANDS NOW, labels+floors = ONE follow-up on an outputPxPerRad joint. Filed docs/backlog/2026-09-23-dome-output-space-overlays.md + 2026-09-23-constellation-segment-near-plane-clip.md (index lines added), commit 0e8161b3d PUSHED; bg `gh pr checks 800 --watch` = shell be3ci0f2n.
- LANDING SEQUENCE: final Opus whole-branch review (package review-71477b91d..0e8161b3d.diff, 20 commits, 89 files +2205/-682) → one fix dispatch if findings → /feature-done (deletion audit incl. five per-renderer pxPerRad copies) → API squash on the user word. Still unruled: record.ts stage timers, resumable-take backlog entry, crf 8 / star floor ×2 experiments (superseded by the follow-up item).
- CI GREEN on 0e8161b3d. USER REVIEW 2026-09-23: finding 1 = domeResample.wesl is one file, convention is io/vertex/fragment (compositor shape); "no other findings". ENFORCEMENT: rule was never written outside compositor/io.wesl header → prompt for a SEPARATE session written to shader-layout-ratchet-session-prompt.md (skill section + CLAUDE.md line + ratchet test tests/services/gpu/shaders/shaderDirLayout.test.ts + split the cheap offenders on main; domeResample excluded, split here). OPUS FINAL REVIEW DONE: Ready YES, 0/0/13 minor (final-review-report.md). FIX DISPATCH (Sonnet, brief final-fix-brief.md → report final-fix-report.md): commit 1 = domeResample split (split-dome-resample-brief.md), commit 2 = minors #1 #2 #4 #5 #7 #8 #9 #10 #11 #12. Skipped: #3 (note only), #6 (self-resolves at /feature-done spec move), #13 → deletion audit list with the five pxPerRad copies and the billboard.wesl essay.
- KNOWN LIMITS (recorded, not fixed, from the review): GPU timings silently unavailable under ?dome (slot names carry @<view.id>, MAX_PROGRAM expands only FRAME_ORDER); VIEW_SLOT_COUNT=24 for every rig (mono allocates 5 unused ring slots); resample clamp-to-edge sampler duplicates a border texel at seams (hidden by the 3x3 box at 4096); the dome square canvas is enforced only by the #c[data-view-rig=dome] CSS rule, nothing asserts it against symmetricFrustum(pi/2,1).
- ON FIX COMPLETION: read report, no re-review needed per reviewer; push; bg gh pr checks 800 --watch; then /feature-done (deletion audit list: five per-renderer pxPerRad copies, slabs.test.ts Sgr A* near-plane test, billboard.wesl essay residue); API squash on the user word.
- FIX ROUND LANDED: 4703029d3 (domeResample split io/vertex/fragment) + 644a875bf (review minors), full suite 14480 green per agent, PUSHED; bg `gh pr checks 800 --watch` = shell bt69bu58t (report when settled).
- /feature-done IN PROGRESS on plan docs/superpowers/plans/2026-09-19-dome-fisheye.md: typecheck PASS (tsc, both projects); `npm test` running (bg shell bi0zvk2r7); DELETION AUDIT running (Opus, brief deletion-audit-brief.md → report deletion-audit-branch.md; must rule on the five pxPerRad copies, slabs.test.ts Sgr A* near-plane test, billboard.wesl residue). Inventory: every planned file present; unplanned files = the five ruled smoke fixes + review round + mat3Columns (all ledgered); only tests/utils/lensing/lensQuadPlaneRadiusRs.test.ts deleted (with its function); no TODO markers; no dome straggler in BACKLOG (captures-as-views is a separate item, stays). Plan has 33 open checkboxes, 0 ticked (lean protocol; tick all in the completion commit). Spec has Ground preparation sections. Smoke attested in-session: dome A/B/C fixes, film accepted; mono unchanged NOT explicitly re-attested (flag in report).
- ON AUDIT + TESTS DONE: apply SAFE-NOW bin as its own deletion commit (Sonnet); needs-ruling items → user; then completion commit: tick checkboxes, git mv plan → plans/completed/, spec → specs/completed/, copy ledger → plans/completed/2026-09-19-dome-fisheye.ledger.md, stage by path, `docs(plan): mark dome-fisheye complete` (NO Co-Authored-By), push, bg CI watch; then `gh pr ready 800` + API squash ONLY on the user word.
- TESTS PASS 1405/14480, CI GREEN on 644a875bf. DELETION AUDIT DONE (deletion-audit-branch.md): SAFE BIN APPLIED 74ac5e13e (S1 billboard.wesl comments, S2 mat3Columns.test.ts deleted, S3 DOME_FACE_COUNT derived, S4, S6, S7, N2 slabs.test.ts Sgr A* near-plane test deleted), net -95, full suite 14478 green, LOCAL ONLY (not pushed).
- AWAITING USER RULING on four audit items (recommendation first): (1) N1 dead 80-byte cam prefix in SgrAStarLensingUniforms → backlog item; (2) packGalaxyPointUniforms.ts:91-96 hand-written prefix leaves cam.pxPerRad zero (labelled _pad0) → fix now in this PR, 2 lines; (3) N3+N4 five per-renderer pxPerRad copies (proceduralDisks deletes cleanly incl. unread camPosWorld; starPoints/milkyWay-pick/starCatalog need u.pxPerRad→u.cam.pxPerRad; galaxyCatalog/points blocked on item 2) → one backlog item; (4) S5 instancedQuadRenderer.ts:386 dead pxPerRad write into texturedDisks _pad2 → pick up here or fold into item 3.
- THEN: apply rulings (Sonnet if code; backlog files if filed), completion commit (tick 33 plan checkboxes, git mv plan+spec → completed/, copy ledger → plans/completed/2026-09-19-dome-fisheye.ledger.md, `docs(plan): mark dome-fisheye complete`, NO Co-Authored-By), push 74ac5e13e + completion, bg CI watch, report DoD verdict (READY expected; smoke: dome attested, mono-unchanged not explicitly re-attested this session); `gh pr ready 800` + API squash ONLY on the user word.
- 2026-09-23 USER RULED audit items "as recommended": galaxy-point prefix fix LANDED 8fc7e37a9 (writeCameraPrefix; test now pins cam.pxPerRad); lens prefix + five pxPerRad copies + instancedQuad dead pad write → one backlog item 9839eb1d6 (docs/backlog/2026-09-23-camera-uniform-surplus.md). Also on branch: eba2da040 (--dome accepts --fps 60, DOME_FPS), 08719d15a (sondermarkenFlyout clip, rides #800 because this session cannot branch). 60 fps take running detached → ~/Movies/skymap/earthUniverseLoop-dome-4096-60fps.mp4 (log scratchpad full-take-60.log). Completion commit next; then API squash only on the user's word.
