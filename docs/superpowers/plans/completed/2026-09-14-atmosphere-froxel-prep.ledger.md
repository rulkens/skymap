# SDD ledger — plan: docs/superpowers/plans/2026-09-14-atmosphere-froxel-prep.md

Spec: docs/superpowers/specs/2026-09-14-atmosphere-froxel-aerial-perspective-design.md
Execution branch: worktree-atmosphere-froxel-aerial-perspective (this worktree), PR #702 (draft)
BASE for all lanes: 0b79deb13
Parallelism (user ruling 2026-09-14): 5 lanes in agent worktrees, cherry-picked onto the execution branch in plan order 1..7 after each task's review is clean. Lane {2→3→4} serial.
Task board artifact: https://claude.ai/code/artifact/4d940816-1280-4d12-a39d-c5dddff8b359

## Pre-flight scan

| Pair / task | Shared file or interface | Produces vs consumes | Finding |
| --- | --- | --- | --- |
| 2 ↔ 3 | atmosphereDrawList.ts | 2 adds entry fields; 3 wraps in a WeakMap keyed on ctx | Serial by design; 3 must keep 2's signature. OK |
| 2 ↔ 4 | atmosphereShellPass.ts, AtmosphereDrawEntry | 4 consumes entry.camLocal/sunLocal/atmosphereTopM/inside from 2 | Consistent; 4 after 2. OK |
| 2 ↔ 5 | none (5 touches renderFrame/expand/check/execute/frameOrder/timing) | — | Disjoint. OK |
| 1 ↔ 5 | slabs.ts (1) vs 5 imports NEAR0/isBodySlabIndex read-only | — | Disjoint edits. OK |
| 6 ↔ all | scattering.wesl, skyViewLut.wesl only | — | Disjoint. OK |
| 7 ↔ all | renderTargets.ts only | — | Disjoint. OK |
| 4 self | atmosphereShellUniforms(entry, slab: Slab, ctx, state) vs pass has view.slab: Slab | — | Consistent. OK |
| 5 self | depthLoadOpFor('sample') falls to first-touch; 'sample' attaches no depth so the op is unused | — | Self-consistent (dead value by design, documented). OK |
| 5 self | sameGroup keys on depth; foreground rows carry 'clear' | — | Consistent with expandFrameOrder test expectations. OK |
| 1 self | test relies on makePlanet/baseInput helpers in slabs.test.ts | — | Implementer verifies helper names; brief says "the file's helper". OK |
| 6 self | perf paired A/B needs two dev servers (base + head); an agent worktree holds one commit | — | CONFLICT → Ruling below |
| 8 | visual attestation is the user's; PR = #702 | — | OK |

Ruling: Task 6's paired perf A/B moves to Task 8 as ONE whole-branch paired A/B (this worktree's server vs the main checkout's server), since only task 6 touches GPU work and an agent worktree cannot host base+head servers — cost if wrong: a regression found at task 8 needs a one-commit bisect, cheap because prep 6 is the sole shader change. Task 6 instead runs the perf harness once as a headless compile/dispatch smoke.
Ruling: agent worktrees symlink node_modules and public/data from the main checkout (/Users/rulkens/Development/js/skymap), never npm install — cost if wrong: none observed in prior efforts.

## Dispatch log
2026-09-14 dispatch wave 1 (BASE 0b79deb13, agent worktrees): Task 1 sonnet [a5592981], Task 2 opus [a9bfb7fc] (lane 2 head; tasks 3+4 resume this agent), Task 5 opus [a53dccde], Task 6 opus [aae9c1b6], Task 7 sonnet [acba85a8].
NOTE: agent worktrees branch from origin/main (35463c237), not this branch; execution branch REBASED onto 35463c237 → doc commits now ff1ae7b16, d2acdcb7b, 6a097c331 (force-pushed). Cherry-pick base for every lane = 35463c237. Agent reports live in each agent worktree's mirrored `.superpowers/sdd/<plan>/task-N-report.md` (agents cannot write outside their worktree).
Task 7: implementer DONE_WITH_CONCERNS, commit fd21e6c6d on worktree-agent-acba85a8eebcf319e. Concern: deleted `expect(fgDepth.usage).toBe(RENDER_ATTACHMENT)` in tests/services/gpu/renderTargets.test.ts.
Ruling: that deletion stands — the assertion is a constant restatement (testing.md), and the plan's "stop if an assertion must change" guard exists to catch behaviour drift, not usage-flag mirrors — cost if wrong: none, the reviewer sees the deletion in the diff.
Task 1: implementer DONE, commit 70c2d9eb3 on worktree-agent-a55929819cf7d5a6c (+6/−1 code, +7 comment, +17 test). Reviewer dispatched.
Task 7: complete (commit fd21e6c6d on agent branch, review clean; ⚠️ commit body verified by controller via git log). Cherry-pick deferred until tasks 1-6 land, to keep plan order.
Task 2: implementer BLOCKED on atmosphereShellPass.test.ts:189 `toHaveBeenCalledTimes(1)` (now 2: derivation runs once per live entry; fixture seeds earth+mars).
Ruling: drop the call-count line, keep the args assertions (a mock call count pins the call site, not the seam) — cost if wrong: a regression to per-draw re-derivation would go unnoticed by this suite; the reviewer + Task 4's extraction cover that path. Secondary: encodeAtmosphereSkyView.test.ts's null-pose-guard case → rename to what it now proves, or delete if it asserts nothing. Implementer resumed.
Task 1: review clean; ⚠️ commit body verified by controller. Reviewer flagged plan-mandated tension: the tie-break can change live painter/pick order on tied rows.
Ruling: accepted as the one intentional, scoped exception to the behavioural no-op rule — a tie at 0 means the camera is inside two bodies' draw radii, where today's order is input order (luck); ties at non-zero values are measure-zero. Attested at Task 8's inside-atmosphere pose — cost if wrong: a visible occlusion swap between two enclosing bodies at that pose.
Task 1: complete (cherry-picked 70c2d9eb3 → f13ced75d on execution branch, review clean)
Task 5: implementer DONE_WITH_CONCERNS, commit 68ba8eab7 on worktree-agent-a53dccdee23d08bc8 (+40/−37 code, +42/−25 comment, +63/−17 test). Concerns: passSlabOf.ts widened too (not in brief's file list; its own lens arm); RenderStepSpec.depth unauthored in this PR; refactor CLI fallbacks hit.
Ruling: RenderStepSpec.depth stays — spec §4.2 mandates it and the feature line authors it; a prep that removed it would make the feature's FRAME_ORDER literal a type error — cost if wrong: one unused optional field for one PR. Reviewer dispatched (opus).
Task 2: implementer DONE, commit 418386486 on worktree-agent-a9bfb7fc6c61d6767 (+31/−37 code, +39/−57 comment, +50/−27 test). Ruling applied: count line dropped; sky-view null-pose case DELETED (asserted only the removed guard; subject now covered by atmosphereDrawList's `excludes a body with no pose this frame`). Reviewer dispatched (opus).
Ruling: Task 3 starts on the lane while Task 2's review is open (files overlap on atmosphereDrawList.ts). Pipelining rule's freeze exists so fix edits don't collide with an in-flight implementer; on one lane with one implementer a Task 2 fix simply lands as a later commit — cost if wrong: Task 2's commit is not self-contained on the lane; the squash-merge erases that.
Task 6: implementer DONE_WITH_CONCERNS, commit cbe7f05c9 on worktree-agent-aae9c1b6f36ac10e9 (+46/−21 code, +30/−19 comment). Concerns: eye check not attested (Task 8); 'let step' shadows WGSL builtin step(); build + full suite + perf smoke clean. Reviewer dispatched (opus).
Task 5: review clean (Approved). Important finding = the already-ruled unauthored RenderStepSpec.depth surface (no action).
Task 5: minor (deferred): executeFrame marks `touched` for a 'sample' step that attaches no depth — if a 'sample' step is ever the FIRST pass on a depth target, the next step loads uncleared depth; not live (nothing emits 'sample'); MUST be carried into the feature plan's Task 4 dispatch (the aerial step always follows the foreground chain, so it is never first — record the invariant in depthLoadOpFor's doc then).
Task 5: minor (deferred): depthAttachment takes both `depth` and `depthLoadOp` (derived pair) — brief-mandated; don't widen further.
Task 5: minor (deferred): "the frame used to pass as separate program parameters" survives in expandFrameOrder.ts:5-6 and FrameStepSpec.d.ts:7-9 headers — timelessness; drop at the comment audit (Task 8 step 3).
Task 5: complete (commit 68ba8eab7 on agent branch, review clean; cherry-pick after lane 2 lands to keep plan order)
Task 2: review Approved with 2 Important: (1) rewritten comment falsely says "neither consumer guards" (pass still guards for TS narrowing); (2) positionMpc/orientation on the entry are now write-only.
Ruling: DELETE positionMpc + orientation from AtmosphereDrawEntry (no consumer in tasks 3/4 or the feature; sceneBodyStates stays as sunDirLocal's input) — cost if wrong: re-adding two fields later.
Task 2: fix round 1/5 dispatched (resume lane implementer after its Task 3 commit; fix = own commit).
Task 2: minor (deferred): derivation paid before the sub-pixel cull (bodyPose unmemoized) — plan-mandated shape; Task 3's memo removes the per-call repeat.
Task 2: minor (deferred): encodeAtmosphereSkyView.test.ts header still says sunDirLocal where src says sunLocal — comment audit.
Task 6: review clean (Approved). ⚠️ commit body verified by controller; eye check = Task 8; scattering.wesl comment count grew ~+11 (brief-mandated field/param docs) — trimmable line noted.
Task 6: minor (deferred): `var out: ScatterStep; ...; return out;` could be `return ScatterStep(sInt, sampleTransmittance);` — leanness pass at Task 8.
Task 6: minor (deferred): scatterStep header sentence restates the inScatter field doc — trim at the comment audit.
Task 6: complete (commit cbe7f05c9 on agent branch, review clean; cherry-pick after lane 2 + task 5)
Task 3: implementer DONE, commit 03f6a8d1c on lane (+14/−2 code, +19/−8 comment, +16/−4 test); atmosphereShellPass.test.ts inside case now uses two ctxs (memo collapses re-mocks on one ctx). Reviewer dispatched (sonnet).
Task 2: fix round 1 landed as 5994c79e0 (after Task 3's commit on the lane). Scoped re-review dispatched (sonnet).
Task 4: dispatched to the lane implementer (context ~180k at dispatch; est. end < 300k).
Task 2: fix round 1/5 (2 addressed, 0 open — guard note narrowed to the bake + TS-narrowing line at the pass; positionMpc/orientation deleted; commit 5994c79e0)
Task 2: complete (commits 418386486 + fix 5994c79e0 on lane, review clean; cherry-pick with lane 2 after tasks 3 + 4 land)
Task 3: review clean (Approved); ⚠️ commit body checked by controller (no trailer). minor (deferred): atmosphereDrawListCache.ts comment ratio 6/6 on a six-line file — comment audit judges.
Task 3: complete (commit 03f6a8d1c on lane, review clean; cherry-pick with lane 2)
Task 4: implementer DONE, commit 7c46e86c2 on lane. CLI extract refused (statement block, not exported decl) → hand-move + refs. Pass file now 68 comment / 25 code (over ratio; header keeps the frame-position prose). Reviewer dispatched (opus).
Cherry-picked lane 2 (2, 3, fix) → ab031b447, d8b0e86be, 12132c61d on execution branch. Task 4 commit follows its review; then 5 (68ba8eab7), 6 (cbe7f05c9), 7 (fd21e6c6d).

## Resume map (written 2026-09-14 for compaction)
Execution branch HEAD 12132c61d (main 35463c237 + 3 docs + tasks 1,2,3,fix). PR #702 draft (not yet pushed past 6a097c331 — push after next cherry-pick).
IN FLIGHT: Task 4 reviewer [ab07494519f793bd5] over review-5994c79e0..7c46e86c2.diff. Handling: clean → cherry-pick 7c46e86c2, then 68ba8eab7 (task 5), cbe7f05c9 (task 6), fd21e6c6d (task 7), push, mark 4/5/6/7 complete; findings → resume lane implementer [a9bfb7fc6c61d6767] (context ~215k, small fix only; ≥ round 4 → fresh opus agent on its worktree via cherry-pick) with the findings verbatim, scoped re-review, then the same cherry-pick sequence.
THEN Task 8 (brief task-8-brief.md): npm test + npm run typecheck (not :fast) + comment audit (deferred minors below) + deletion-audit-lite on the branch + whole-branch paired perf A/B vs the main checkout's server (Task 6 ruling) + ASK USER for the 3-pose visual attestation (Earth from space terminator, inside atmosphere down+up, Sgr A* lens band) pixel-identical to main + final whole-branch review (fable/opus) + PR #702 body rewrite + relocate the PREP plan (not spec) to plans/completed/ + ledger archive per sdd-execution Rule 3 + mark PR ready.
Agent worktrees to remove at close: agent-a55929819cf7d5a6c, agent-a9bfb7fc6c61d6767, agent-a53dccdee23d08bc8, agent-aae9c1b6f36ac10e9, agent-acba85a8eebcf319e (+ their worktree-agent-* branches).
Task board artifact: https://claude.ai/code/artifact/4d940816-1280-4d12-a39d-c5dddff8b359 (scratchpad froxel-board.html; republish on state change).
Task 4: review Needs fixes — 1 Important: pass header keeps ~33 lines whose subject is the renderer/shader/frameOrder (63 comment / 25 code). Fix round 1/5 dispatched (resume lane implementer; cut to pointers, no re-homing prose).
Task 4: minor (deferred): atmosphereShellUniforms.ts:2-4 future-tense "more than one consumer coming" — make timeless at the comment audit.
Task 4: minor (deferred): atmosphereShellPass.test.ts:8,167,182 prose still says the pass reads the pose / composes mvp — touch up at the comment audit.
Task 4: note: f64-seam section deleted from the pass header (brief listed it as a keeper) — accepted: the pose read moved to the builder, whose header now carries it.
Task 4: fix round 1 landed as 054d8bd75 on lane (comment-only, 63→35 comment / 25 code; still >half — implementer stopped at reviewer keep-list). Scoped re-review dispatched (sonnet) over review-7c46e86c2..054d8bd75.diff; re-reviewer asked to judge whether the surviving 35 lines are load-bearing.
Task 4: fix round 1/5 — re-review: 1 open (ratio 35/25, pre-heading block 17 lines not 9, MULTIPLY/ADD sentence duplicates atmosphereShellRenderer.ts:11-12). Fix round 2/5 dispatched to lane implementer (~235k ctx; small own-diff) with hard targets: header ≤10 lines, ≤12 comment lines total, section heading dropped, MULTIPLY/ADD cut.
Task 4: fix round 2 committed 35ef843da on lane BEFORE the implementer died (rate limit 429, no report). Controller verified: prettier clean, passes suite 35/306 green. Header now 10 lines, 11 comment / 25 code. Cherry-picked 4 (+2 fixes) → 323bbd241, 6e8cbff5b, 7ccc923be; then 5 → 0ce894c7d, 6 → 249f78b89, 7 → 37744805a. Execution HEAD 37744805a. Round-2 scoped re-review dispatched (sonnet); any residual fix lands as a commit on the execution branch directly (lane implementer retired at ~240k ctx + rate-limited).
Push DENIED by user this turn (force-with-lease refused) — branch local only past 6a097c331; retry only when the user says so.
Task 8 step 1 started: npm test + npm run typecheck in background on 37744805a.
Task 4: fix round 2/5 (1 addressed, 0 open — header 10 lines, 11 comment / 25 code; pointers verified at atmosphereShellRenderer.ts:10-13 and atmosphereDrawList.ts:3-5). Out-of-scope minor accepted: earthPass pick-surface elaboration dropped, core non-pickable reason kept.
Task 4: complete (323bbd241 + 6e8cbff5b + 7ccc923be on execution branch)
Task 5: complete (0ce894c7d on execution branch)
Task 6: complete (249f78b89 on execution branch)
Task 7: complete (37744805a on execution branch)
Task 8: typecheck (tsc, both projects) exit 0 on 37744805a; npm test running.
Task 8: npm test green on 37744805a (1336 files / 9242 tests). Dev server for this worktree started on http://localhost:5173 (background task, leave running). Comment audit (opus, edits + 2 commits incl. scatterStep constructor return) and deletion audit (opus, read-only → task-8-deletion-audit.md) dispatched in parallel. Perf A/B waits for the audit commit (HMR mid-measure). Then final whole-branch review, then ASK USER for the 3-pose attestation.
Task 8: deletion audit → 6 findings (task-8-deletion-audit.md). Rulings: F1 ACT (delete expandFrameOrder.test.ts:272-291, shrink :247-270 to the multi-row-order case via program()) — after the lens fold the pre-existing lens tests ARE the BodyRowSource tests; spec §9 titles are testing prose, not Ground-prep contract — cost if wrong: one test re-added. F2 ACT (depthLoadOpFor returns GPULoadOp | null, depthAttachment takes the op alone; 'sample' → null) — overrides the Task-5 'don't widen' deferral because it narrows — cost if wrong: an executor edit the suite + final review guard. F3 ACT (delete the forwarding test at atmosphereShellPass.test.ts:230-245; spec §4.1 removes the inside arg next PR). F4 ACT (replace the throw with a non-null assertion + one-line why: an entry exists only where bodyPose resolved) — the plan's throw is the third copy of the guard prep 2 deletes; symmetry wins — cost if wrong: a less descriptive crash on a future decoupling. F5/F6 → verify covered by the comment audit; else in the fix wave. eyeRelBodyM-on-entry variant NOT acted (spec §10 signature). CARRY to feature Task 4 dispatch: maxFrameInputs.ts insideAtmosphere must be sized to BODY_SLAB_CAPACITY when the AERIAL step exists. Fix wave dispatches AFTER the comment-audit commit (shared test file); perf A/B after the fix wave.
Task 8: comment audit landed 7663d9915 (12 files, +76/−158 comment-only; minors 1–6 resolved, 7 left) + 35f860171 (ScatterStep constructor return; build linked it). Deletion fix wave F1–F4 (+F5/F6 verify) dispatched (opus, one commit refactor(frame): deletion audit for the froxel prep). Next after it: perf A/B (isolation worktree agent = base 35463c237 server vs this worktree's :5173), then final whole-branch review (fable), then ASK USER 3-pose attestation, then PR body + relocate + ledger archive.
Task 8: deletion fix wave landed 98a5e3b74 (+21/−84; F1–F4 applied, F5/F6 already clean). HEAD 98a5e3b74. Final whole-branch review (fable) dispatched over review-35463c237..98a5e3b74.diff → task-8-final-review.md; perf A/B (sonnet, isolation worktree at base 35463c237, B = :5173) dispatched → its worktree's task-8-perf-ab.md. Handling: review Needs fixes → ONE fix dispatch + scoped re-review; perf B slower > 0.5 ms → HALT and put land/park to the user; both clean → ASK USER for the 3-pose attestation, then PR body + relocate plan + ledger archive + mark ready. One more agent worktree to remove at close (the perf A/B one).
NOTE: origin/main advanced to 536329ea8 (d954fa747 EOX s2cloudless band, 2956c4425 PBR P1 #697, 536329ea8 backlog docs). Perf agent's worktree was cut there; told it to detach to 35463c237 — the A/B baseline stays the branch base. Before marking #702 ready: merge origin/main into the branch (never from a worktree per memory? — the branch is a worktree branch; do the merge here, it is the PR's own branch) and re-run the suite.
LANDMINE: an isolation:worktree agent's worktree is auto-cleaned when it stops unchanged; resuming it re-homes the agent into the CONTROLLER worktree — never resume such an agent with git instructions (the first perf agent correctly refused a detach that would have hit this branch). Fresh perf A/B agent dispatched with the detach-to-35463c237 step in the brief and the report echoed in its reply.
Task 8: perf A/B NEUTRAL within noise (earth-surface 3 pairs: median A 21.14 / B 20.05 ms, no per-pass delta > 0.5 ms; solar-system 1 pair A 22.09 / B 19.76). Report copied to task-8-perf-ab.md. Perf agent worktree agent-aa66837cafe70b9a6 (+ agent-ae14d51c4b903fa6f if present) → remove at close.
Task 8: perf A/B NEUTRAL within noise (earth-surface 3 pairs: median A 21.14 / B 20.05 ms, no per-pass delta > 0.5 ms; solar-system 1 pair A 22.09 / B 19.76). Report rewritten at task-8-perf-ab.md (agent worktree auto-cleaned with its JSON). Remaining: final review verdict → user 3-pose attestation → merge origin/main → PR body/relocate/archive/ready.
Task 8: final whole-branch review (fable) APPROVED — 1 Important (comment budget: atmosphereShellUniforms.ts 21/36, atmosphereDrawListCache.ts 5/6, encodeAtmosphereSkyView.ts 22/27), 6 minors. Ruling: ONE fix wave (sonnet) = I1 trims + 'let step' builtin-shadow rename + two forward-referencing froxel-bake comments; NOT acted: TEXTURE_BINDING on every depth target (harmless usage flag, spec's hand-set intent kept; narrow only if a review of the feature PR asks), passSlabOf ratio, long .d.ts headers (per-field contracts). 35f860171 WGSL compile gate = the perf A/B ran on a server serving 98a5e3b74 (earth-surface + solar-system rendered) — passed. Then scoped re-review (sonnet), then ASK USER attestation.
Task 8: final-review fix wave landed 614f1ab5a (+20/−38; counts 16/36, 2/6, 12/27; step→s rename; build linked). Scoped re-review dispatched (sonnet). Merge origin/main (536329ea8) STARTED: 10 frame-program conflicts (main renamed capture inputs under PBR P1 #697 vs our lens fold) → opus agent resolving in this worktree (both sides survive), then typecheck + full suite + merge commit → task-8-merge-main.md. Editor diagnostics mid-merge are expected noise. The :5173 server serves a conflicted tree until the merge commits — do not ask the user to look before then.
Task 8: final fix-wave re-review CLEAN (I1 + 2 minors addressed, no breakage). Final review gate PASSED at 614f1ab5a. Remaining: merge commit (agent in flight) → user attestation → PR body + relocate + archive + ready.
Task 8: merge origin/main COMMITTED c11e146ce (10 conflicts resolved; tsc exit 0 re-verified by controller; suite 1340/9264 per agent). Resolver choices: depthLoadOpFor folded into main's depthFor() -> {target, loadOp} | undefined; main's new lensBodySlabs.ts renamed bodyRowSlabs.ts returning the whole Record (git mv inside the merge — move-files not used; tsc clean). Scoped review of the resolution dispatched (sonnet). HEAD c11e146ce; :5173 serves it. NOW ASKING USER for the 3-pose attestation vs a main server. Board republished.
Task 8: merge-resolution review APPROVED (depthFor reproduces both parents; bodyRowSlabs.ts = main's lensBodySlabs + insideAtmosphere []; purity allow-list byte-identical to main; no markers/trailers). All automated gates passed at c11e146ce. BLOCKED ON USER: 3-pose attestation (asked 2026-09-14). On pass: PR body rewrite, relocate prep plan + archive ledger in final commit, push (ask — denied once), mark ready, remove agent worktrees a55929819cf7d5a6c a9bfb7fc6c61d6767 a53dccdee23d08bc8 aae9c1b6f36ac10e9 acba85a8eebcf319e + branches.
Task 8: VISUAL ATTESTATION PASSED by user 2026-09-14 (3 poses vs main: Earth terminator, inside atmosphere down+up, Sgr A* band). Task 8: complete pending the final docs commit.
