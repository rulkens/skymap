# SDD ledger — plan: docs/superpowers/plans/2026-08-01-frame-invariant-camera-poses.md

Task 1: complete pending review (commits eb0a368, docs corrected in HEAD)
Task 1: review 1 — spec OK, quality NEEDS FIXES (2 Important): (a) rotateByFrame at buildPathTrack.ts:180-188 duplicates updatePosition.ts:69-71 verbatim — extract a shared util; (b) test 1 name overclaims: it is invariant to the both-sites-bare bug, guards site-consistency only.
Task 1: fix round 1 queued behind in-flight Task 2 implementer (freeze rule: fixes edit the tree).
Task 2: complete (commits eb0a368..41ff35c3, review clean)
Task 2: minor (deferred): sampleClipPath test samples only t=0, so it exercises eyeOf rotation but not frameBasis reaching evaluateClip tangent-aim encoding; covered by Task 1 tests.
Task 1: fix round 1/5 (2 addressed, 0 open; commits 41ff35c3..de7da90f)
Task 1: complete (commits 6081b52..de7da90f, review clean)
Task 3: complete (commits de7da90f..047e8870, review clean — routing audit: 11 sites, no mis-route)
Task 3: minor (deferred): stale comment tests/services/engine/frame/frameContext.test.ts:50 still shows the 3-arg assembleOrbitCamera call shape; header at line 13 is correct.
Task 4: review 1 — spec mostly OK, quality NEEDS FIXES (3 Important): (a) 19-line module header vs the plan-stated <=10 budget, no justification line; (b) target aliased by reference at reencodePose.ts:59, departing from the copy-defensively convention every sibling CameraPose producer follows; (c) mixed-undefined basis case (one identity, one not) correct by construction but untested.
Task 4: fix round 1 queued behind in-flight Task 5 implementer (freeze rule).
Task 4: fix round 1/5 (3 addressed pending re-review; commits 184b6658..90448629)
Task 5: review 1 — spec OK, all 5 named risks verified correct, quality NEEDS FIXES (2 Important, both documentation): (a) assembleOrbitCamera.ts:32-34 comment says every caller passes the same basis for both — this task falsified it; (b) pre-existing runFrame.test.ts:402-476 asserts the eye MOVES during a roll and claims that is the produce-path invariant — now the opposite; passes only because it feeds one basis into both slots manually.
Task 5: pickFrameContext steady-basis-for-both confirmed PRE-EXISTING by review, not widened here — deferred, pick during a roll uses a pole the draw does not.
Task 4: fix round 1/5 (3 addressed + report claim, 0 open; commits 184b6658..90448629)
Task 4: complete (commits 047e8870..90448629, review clean)
Task 5: fix round 1/5 (2 addressed, 0 open; commits 90448629..dc9bf6d4)
Task 5: complete (commits 68db1873..dc9bf6d4, review clean)
Task 5: minor (deferred): runFrame.ts:340-355 comment block still ~16 comment lines over ~12 code lines, above the <=half budget; judged non-redundant.
Task 6: fix round 1/5 (brief was WRONG — from must be the outgoing REGISTRY frame, not the live basis; implementer derived it and flagged. 6 fixes addressed; commits dc9bf6d4..fd54ed13. Spec+plan corrected in 28127438.)
Task 6: review 1 — spec OK against the corrected contract, all 5 named risks verified, quality APPROVED. 1 Minor: comment budget at exactly half (16/32), no headroom.
Task 6: OPEN ITEM — reviewer left a WARN: the RED-against-old-code claim is a process claim not visible in the diff. Controller must mutation-verify (swap `from` to the live basis, confirm the mid-roll test fails, restore) once the tree is quiet. Task 6 stays in_progress until then.
IN FLIGHT at compaction: Task 7 implementer (clip pins its frame) editing engine.ts / visitBeatSaga.ts / cameraSlice + CameraState.d.ts — mid-edit, tree NOT clean, do not run the gate until it reports.
RESUME: tasks 1-5 complete. Task 6 committed fd54ed13, review APPROVED, blocked only on the controller mutation-check noted above. Tasks 7-12 remain. Branch worktree-fix-tour-camera-roll, draft PR #538.
Task 6: OPEN ITEM RESOLVED — controller mutation-verified. Moving the `previous` select to AFTER setOrientation (the silent no-op re-encode) fails the mid-roll test on the eye-direction assertion itself (0.776 vs -0.517, delta 1.29 on a unit vector), and the steady test too. Restored; 6/6 green. The tests have real power.
Task 6: complete (commits dc9bf6d4..fd54ed13, review clean, mutation-verified)
Task 7: implemented, committed bdbc9a81, awaiting review. Concern flagged: watchReplayInspectedPathSaga selects the LIVE orientation at replay-dispatch rather than the inspector compute-time frame (debug-only path).
Task 7: review 1 — core contract OK, all 5 named risks verified (incl. the surprising RED claim: evaluateBaseScalar short-circuits on empty segments so frameBasis genuinely has zero effect on a base-track clip; the pre-fix bug is the raw angles staying frozen while the render decode moves). NEEDS FIXES, 1 Important: watchReplayInspectedPathSaga:49-53 re-selects the LIVE orientation at Play time, but watchClipPathInspectSaga:73-74 baked the pinned clip bearings under the Calculate-time frame. Change orientation between Calculate and Play and the replay decodes under the wrong pole — the exact defect this task fixes, relocated. Every other dispatch site is safe because resolveClipFoci and the playClip frame arg read selectOrientation in the SAME saga step; that invariant does not hold here.
Task 7: fix round 1 queued behind in-flight Task 8 implementer (freeze rule).
Task 7: minor (deferred): no regression test for the compile-cache-stability half of the fix (flyPath clip not recompiling across a switch once frame is pinned); only the re-encode half is covered.
Task 7: minor (deferred): CameraState.d.ts header 19 lines, pre-existing over the <=10 budget, +2 from this task.
Task 8: implemented, committed f1230674, awaiting review. 6202 tests green.
Task 8: review 1 — spec OK, all 5 named risks verified (incl. independent commit-path trace and clip-precedent consistency). Reviewer labelled Approved but raised 1 Important, which per the SDD rule enters the fix loop: tests/state/selection/watchGoHomeSaga.test.ts:82 asserts tween.frame === orientation without ever dispatching setOrientation away from DEFAULT_ORIENTATION, so it passes identically if the saga hardcoded the default. Cannot distinguish live-capture from hardcoded.
Task 8: fix round 1 queued behind in-flight Task 7 fix implementer (freeze rule).
Task 8: minor (deferred): byId/worldAim helpers duplicated between the clip and tween world-aim tests in cameraDrivers.test.ts; hoist to module scope now that a second copy exists.
Task 7: fix round 1/5 (1 addressed, 0 open; commits f1230674..b6614834). Verified: id stored as a string not a Mat3; pinnedFrameId written in the SAME synchronous call as pinned (gap closed, not moved); selectOrientation import removed from the replay path entirely; recompute re-stamps at its own time; the stale pinned-without-frame state is unreachable in production and guarded where it is reachable; the new test genuinely varies orientation between compute and replay.
Task 7: complete (commits 28127438..b6614834, review clean)
Task 7: minor (deferred): computeClipPath.test.ts does not unit-test pinnedFrame() at the seam level (compute-then-read, recompute-restamps); a reordering bug inside computeClipPath.ts itself would not be caught — the saga-level test mocks the seam wholesale.
Task 8: fix round 1/5 (1 addressed, 0 open; commits b6614834..be703962). DEFAULT_ORIENTATION confirmed ecliptic, test dispatches galactic, assertion still compares a live-read value, RED transcript matches the test as written.
Task 8: complete (commits bdbc9a81..be703962, review clean)
MILESTONE: camera mechanism complete (tasks 1-8). Every pose-producing site respects the rule. Remaining: 9, 9b, 10, 11, 12 — the tour-authoring half.
Task 9: complete (commits be703962..ba48a612, review clean). Cross-basis test verified genuinely independent (expectation from cross(poleEcliptic, poleGalactic), not resolver self-comparison); reviewer hand-verified the resolver arithmetic reproduces aim [1,0,0] for the yaw=0.5 / Virgo fixture.
Task 9: minor -> BACKLOG (not deferred): silent default: arms in clipFociReady + stubResolveClipFoci, filed in docs/BACKLOG.md Engine & State.
Task 9b: complete (commits fb8d4e44..14ea1776, review clean). Byte-identical default path verified structurally: the original spin() call expression is untouched, lifted into a const behind a ternary; no pre-existing test body edited. turns is DELEGATED to spinToId rather than re-defaulted, so no second default can drift.
Task 9b: minor (deferred): throw message says "Drop one" rather than naming which knob to drop — the brief asked for "say which to drop".
Task 9b: minor (deferred): the mutual-exclusion test only asserts .toThrow(), not the message, so a regression blanking the explanation would pass.
Task 9b: minor (deferred): dwellDrift.ts header is 54 lines (pre-existing over budget; this task added 5).
Task 10: review 1 — NEEDS FIXES, 1 CRITICAL + 3 Important. CRITICAL: moving the M81 landing onto localGroupDwell relocated the flythrough launch ~6.7 Mpc around the 4.5 Mpc orbit, so neighbourhoodFlythrough now opens with a ~41 deg eased whip pan over DEFAULT_ALIGN_SEC 1.35s — against its own documented "no opening swing" design — and its header still asserts the invariant. The implementers 96 deg figure was the YAW delta; the visible swing is ~41 deg once the eye offset is accounted for. Also localGroupDwell now sweeps 35% faster in the same 14s.
Task 10: REMEDY (reviewer derived, disproving the implementers "no alternative" claim): move spinTo from localGroupDwell to neighbourhoodReveal dwellDrift — its target is static across that dwell, so it resolves cleanly and restores the launch invariant FRAME-INVARIANTLY; localGroupDwell reverts to a plain cruiseRate (a rate, already frame-invariant).
Task 10: Important: GALACTIC_DISC_FORWARD has ZERO test coverage — the report claimed it was covered; it is not. resolveClipFoci.test.ts:798 hand-copies the literal instead of importing it, so a mistyped digit passes the whole suite. cameraFraming.test.ts was named in the brief and untouched.
Task 10: Important: resolveClipFoci module header omits aimAlong while its Walk-invariants paragraph asserts all non-id arms pass through unchanged.
Task 10: fix round 1 queued behind in-flight Task 11 implementer (freeze rule).
Task 11: complete (commits ca788e33..c3a14e35, review clean). All 5 named risks verified independently: restore destructures orientation OUT of the mergeSnapshot payload then dispatches requestOrientationChange, so no raw scalar write reaches settings.orientation and no double re-encode is possible; capture still precedes any beat; header policy now states the scalar exception; mergeSettingsSnapshot returns a new object (no in-place mutation); the deleted guidedTourSaga clause was confirmed already false pre-task.
Task 11: minor (deferred): restoreSceneSaga header ~40 prose lines over ~9 code lines (pre-existing, grew here; matches de-facto house style in neighbouring files).
Task 11: NOTE for Task 12: requestOrientationChange now fires on every tour restore even when no beat touched the frame — a harmless no-op tween today, but Task 12 makes tours that DO change the frame the norm. Implementer deferred a guard deliberately rather than adding an unrequested select.
Task 10: fix round 1/5 (2 Critical + 2 Important addressed, 0 open; commits c3a14e35..822a3fa5). Launch angle now 5.09e-14 deg (0 to float precision) vs ~41 deg; reveal arc matches the deleted REVEAL_NET_YAW_RAD to 7 sig figs; REVEAL_NET_YAW_RAD grepped clean across src/tests/tools.
Task 10: regression test VERIFIED NOT A MIRROR — chains the real unmodified beat ClipData through the real resolver against synthetic deps; expected bearing computed independently; asserts localGroupDwell alone does NOT land on M81 (>0.5 rad gap), so moving spinTo back would fail immediately. Re-reviewer reran it independently rather than trusting the transcript.
Task 10: complete (commits 14ea1776..822a3fa5, review clean)
DOC STALENESS for the final sweep: docs/superpowers/plans/2026-08-01-frame-invariant-camera-poses.md:8 still names REVEAL_NET_YAW_RAD (now deleted); docs/backlog/2026-07-22-grand-tour-earth-start.md names GALACTIC_DISC_YAW_RAD/PITCH_RAD (now deleted).
Task 12: review 1 — spec OK, ladder complete across all 13 beats, NEEDS FIXES (2 Important): (a) FRAME_ROLL_SEC declared three times with the same value and same derivation — one knob wearing three names, right before the user retunes it; (b) no test pins the frame literals, and they ARE testable (frameTo emits a plain serializable object on the exported timeline) — a future edit to another VALID OrientationFrameId would be caught by nothing.
Task 12: both traced claims CONFIRMED by independent trace: cold-open ordering genuinely inert (foci baked and clip.frame pinned before the timeline ticks); ladder coverage complete with no beat inheriting the wrong pole.
Task 12: reviewer CORRECTED the implementer rationale: home-again cannot "fight" the restore (no overlap possible, and startFrameTween reseeds from the live mid-slerp basis so an overlap would compose anyway). Early placement still right, for the other reason given.
VISUAL-PASS NOTE: a viewer WILL see a second ~1s roll at the tour end (galactic -> their pre-tour frame) unless already on galactic. Inherent to the unconditional restore, by design, not a regression.
Task 12: fix round 1/5 (2 addressed + rationale correction, 0 open; commits 77070970..584cdb7b)
Task 12: complete (commits 822a3fa5..584cdb7b, review clean)
ALL 13 TASKS COMPLETE. Docs refreshed in d4ea48e9. Next: whole-branch review, then the user visual pass.
WHOLE-BRANCH REVIEW 1 — NEEDS FIXES BEFORE MERGE. 1 Critical, 3 Important, 5 Minor.
CRITICAL: guidedTourSaga.ts:132 merges computeSceneEntering(...) BEFORE EVERY BEAT, and computeSceneEntering:85 returns captureSettings(...) which now carries orientation (Task 11). mergeSettingsSnapshot:42 spreads every key, so the PRE-TOUR frame is raw-written into settings.orientation at each beat start. frameTo(supergalactic) in beat 02 is undone entering beat 03 — beats 03-09 play under the viewers pre-tour pole. The DoD line fails and the dwell-roll problem returns for 7 of 11 beats. Verified at RUNTIME by the reviewer with a tsx probe, not inferred. frameLadder.test.ts passes throughout because it asserts authored literals, not runtime effect.
CRITICAL is a TASK-INTERACTION bug: Task 11 (orientation into SettingsSnapshot) and Task 12 (frameTo cues) were each correct in isolation. Only the whole-branch view could see it.
Important: applySceneEffect frameTo arm does not re-express camera.base — invariant upheld by driver-priority accident, and SceneEffect.ts:41-44 claims a symmetry that no longer holds.
Important: hashParamSources.ts:230-233 comment now FALSE (orientation is in SettingsSnapshot now); writesOn does not include mergeSnapshot, so a raw snapshot write also silently desyncs the URL hash.
Important: docs/animation/clip-primitives.md never updated — spinToId, aimAlong, dwellDrift spinTo/turns all undocumented in the authoring reference.
BACKLOG HYGIENE MISS: docs/BACKLOG.md:144 + docs/backlog/2026-07-29-tour-snapshot-orientation.md are the item Task 11 implemented; convention requires deleting index line AND detail file in the change that picks it up.
WHOLE-BRANCH FIX WAVE re-review: Critical 1 ADDRESSED (verified at RUNTIME — reviewer drove all 14 beats through the real boundary dispatch + real frameTo cues: ladder is galactic/galactic/supergalactic x11/galactic, exactly the DoD; re-adding orientation to the payload reproduces the broken ladder). Important 2/3/4 and Findings 5/6 ADDRESSED. No new breakage. Test arithmetic confirmed 6227->6226 (frameLadder 3 its -> 1, guidedTourSaga +1); no coverage dropped.
Important 2 conclusion INDEPENDENTLY TRACED and found stronger than claimed: applySceneEffect has exactly one caller (clipPlayer fireCue), clipPlayer defers completion by two frames so a cue can never fire on the deactivation frame, and commit-on-edge overwrites base with lastPose wholesale — so the requested third dispatch is a no-op on every reachable path.
R1 (Important) RESOLVED by controller in da847266: spec + plan + DoD said the opposite of what shipped.
R2 parked — minor: frameTo itself still absent from clip-primitives.md scene-effects table. Ruling: real, one line, but outside the three items the finding named; the branch documented what it added. Filed as follow-up rather than fixed post-review.
R3 parked — minor: frameLadder.test.ts duplicates the beat-boundary fold rather than driving the saga. Ruling: rationale sound (full saga needs catalog fixtures for resolveClipFoci) and the paired guidedTourSaga guard covers the saga itself; accepted.
R4 parked — minor: liveFrameBasisQuat/LiveCameraRuntime.frameBasisQuat keep the old noun after CameraRuntime.frameBasis -> upBasis. Ruling: scope-compliant (Finding 6 named the field only) but a half-rename against the project naming rule; follow-up.
R5 parked — minor: comment budget moving the wrong way in @types headers. Ruling: pre-existing house style, added material is load-bearing; not this branch to sweep.
