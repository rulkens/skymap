# SDD ledger — plan: docs/superpowers/plans/2026-08-19-wake-vote-fold.md

Branch: refactor/wake-vote-fold · BASE (plan commit): 91b493f0d · main: e34ca2a6c · draft PR #591.
Spec authority: the plan itself is the rung-5 mini-plan (rulings D1–D10 inline); parent decisions at docs/research/engine/decisions.md #7/#9/#13/#14. Surveys at scratchpad/rung5-*.md.
User directive this session: maximize parallel agents. Ruling: implementers stay sequential (shared git index — concurrent commits can stage each other's files); parallelism extracted by pipelining each task's review beside the next task's implementation and batching Tasks 4+5 into one dispatch. — cost if wrong: some wall-clock left on the table, no correctness risk.

## Pre-flight conflict scan (2026-08-19)

| Pair / task | Shared surface | Checked | Finding |
|---|---|---|---|
| T1 × T2 | none — label fold files vs volume files disjoint; T2's grep gate (`grep -rn requestRender src/services/engine/volume/`) unaffected by T1 | file lists | clean |
| T1 × T3 | none — T3 touches biasCorrectionSubsystem/startLoop/wireInput only | file lists | clean |
| T1 × T5 | runFrame.ts line citations in doc maps — T5 must cite post-fold numbers | T5 brief already cites :708-711 (post-T1 shape); plan orders T5 after T3 | clean, ordering honoured |
| T2 × T6 | volume smoke (mcpm/cf4-density fade-in) is T2's only behavioural coverage | T6 brief lists it explicitly | clean |
| T3 × T4 | D8/D9 wording must match between comments and decision #15 | both quote the same two-clause rule ("covers the same fact") | clean |
| T4 × T5 | disjoint docs (decisions.md vs contract maps) | file lists | clean |
| T1 self | tests it rewrites vs code it changes: vote sequence true,true,false,true,false at nowMs 0/150/300/400/700 matches director envelope; grep gates (2 hits runFrame, 0 hits subsystems.scheduler) consistent with "capture at :639 + bag at :708-711" | brief re-read | clean |
| T2 self | deletes :84 assertion only, keeps :82-83 + spy — matches Global Constraint "exactly one assertion deleted" | brief re-read | clean |
| T6 self | gate lists typecheck+test then 5 user-eyes checks; camera-at-rest precondition stated | brief re-read | clean |

Scan clean — plan was adversarially reviewed pre-commit (17 amendments applied); no execution-blocking conflict found.

## Task log

Task 1: complete — commit 35b1e1e33 (fold label-director wake vote into the anim bag). 39/39 targeted, full suite 1045 files / 7029 tests green, typecheck clean, both grep gates pass, no citation drift. Review: SPEC ✅ / QUALITY APPROVED, no findings (task-1-review.md); reviewer independently reran typecheck + targeted tests and read all four untouchable invariants intact.

Task 2: implementer DONE — commit baed11ac2 (delete the two redundant volume-ingest wakes). Typecheck clean, uploadVolumeField 3/3 + volumeSlotIngest 4/4, grep gate 0 hits. Implementer wording deviation noted: "wake rides the settings-row dispatch" (not "line above") because the dispatch sits two lines above in uploadVolumeField — handed to reviewer to judge. Review (haiku): SPEC ✅ / QUALITY NEEDS-FIXES — (1) unify the two replacement comments' wording (uploadVolumeField "settings-row dispatch" vs unloadVolumeField "settings dispatch above"); (2) correct the report's false "identical wording" claim. Fix round 1 QUEUED behind Task 3's implementer (commit serialization) — will resume the Task 2 implementer agent.

Task 3: implementer DONE — commit 7176351f7 (record D8/D9 keep rationale at the three kept wake sites). Comment-only verified by green run with no test edits (biasCorrection 12, startLoop 5, wireInput 9). Review dispatched (haiku, package review-baed11ac2..7176351f7.diff).

Task 2 fix round 1: DONE — commit a62fa9f6a; unified comment in both files: "// Wake rides the settings-row dispatch; watchWakeSaga's route table renders it (#14 D2)."; report corrected. Scoped re-review: both findings ADDRESSED (task-2-re-review.md). Task 2: complete.

Task 3 review (haiku): SPEC ✅ / QUALITY NEEDS-FIXES — length only: startLoop comment 2 lines vs brief's "one line", wireInput 4 lines vs "one or two". Substance + citations verified sound. Ruling: run the fix round — the comment-budget convention is load-bearing in this repo (user's standing over-commenting audit), and the trim is cheap; cost if wrong: a few minutes. Fix QUEUED behind Tasks 4+5 implementer (commit serialization) — will resume the Task 3 implementer agent.

Tasks 4+5 (batched): implementer DONE — commits 433d3b530 (decision #15) + 5bd90a275 (map sweep). Census sums verified by implementer; extra citation drift found+fixed (uploadVolumeField :27→:25, T3 comment sites). Judgment call flagged: W7 mermaid node moved "future walker"→"out" class, reading D4 as permanent — handed to reviewer to adjudicate. Review (sonnet): SPEC ✅ / QUALITY NEEDS-FIXES — two dead citation pairs in decision #15 (wireInput :382→:380 + span :378-379; startLoop :151→:150 + span :149) — root cause: Task 3's trim (002917037) landed after the docs commits and shifted those spans. W7 adjudication: reviewer overruled "out" — D4's own text is an open re-deferral to the umbrella reassessment. Ruling: accept both — fix citations against current tree, revert W7 to deferred class. Fix round 1: DONE — commit 63f62bd09 (citations corrected to :380/:378-379 and :150/:149; W7 reverted to deferred class with label reflecting D4's open re-deferral). Scoped re-review: ALL ADDRESSED, new citations independently verified against the tree (task-4-5-re-review.md). Tasks 4+5: complete.

Task 6 gate: typecheck GREEN (both tsconfigs) + full suite GREEN (1045 files / 7029 tests, 0 skipped-added). Branch pushed to PR #591 (8 commits 35b1e1e33..63f62bd09). USER VISUAL SMOKE — check 3 FLAGGED (2026-08-20): "the mcpm volume doesn't always fade in, sometimes it pops in when enabled." This is the check covering Task 2's deletions. PIPELINE HALTED pending adjudication: regression vs pre-existing. Investigation dispatched (sonnet, read-only) → smoke-mcpm-pop-investigation.md; must trace enable→fade path, the saga-vs-synchronous wake interleaving, and pre-existing suspects (rapid off/on fade state, cached-cube re-enable path, animate:false branch). USER ATTESTED (2026-08-20): checks 1, 2, 4, 5 GOOD (label envelope keeps easing at rest; label enter/leave fades; bias repaint; FrameStatsRow sleeps) + cf4-density fade good; only mcpm intermittent pop open, user tentatively correlates it with quick off/on toggles ("not 100% sure"). Investigation verdict (smoke-mcpm-pop-investigation.md): PRE-EXISTING — fadeRegistry.fadeTo (fadeRegistry.ts:125-133) stamps fade start from stale lastTickNowMs after loop sleep ≥ fade duration → first post-wake frame clamps to full opacity. Deletion provably orthogonal: coalescing token claimed by earlier wakes (settings dispatch + fadeTo's own requestRender) before the deleted line's position on both enable paths. Ruling: NOT a rung-5 regression; smoke check 3's flag adjudicated pre-existing; captured as backlog item commit a60a6b289 (docs/backlog/2026-08-20-fade-pop-after-idle.md + index line). UNHALTED. Cost if wrong: the browser experiment (idle→pop, nudge→fade) can falsify cheaply; user has it.

FINAL REVIEW (opus, final-review.md): NEEDS-FIXES — 3 must-fix (shouldKeepTicking header enumerates 2-of-3 bag contributors; decisions.md:520 dead syncVisibilityFadeItem.ts pointer; backlog file cites gitignored workspace path) + 7 nits. All mechanism/neutrality/invariant/test/DoD checks PASS ("fix those five lines and this is APPROVED"). Rulings: fix must-fix 1-3 + N1 (decisions.md off-by-one spans) + N2 (contracts-map :441-481→:438-476) + N3 (biasCorrection trim 5→2 lines) + N7 (wrap long comment lines); PARK N4 (pre-existing header length, plan ruled out-of-scope — future comment-audit), N5 (plan's "flat comment count" prediction wrong by +10, history records it, N3 recovers 3), N6 (exception-path nuance, reviewer's own no-action). ONE fix dispatch (sonnet): DONE — commit 9d9c76fba (all 7 findings + declared collateral line-shift corrections in the two living docs). Scoped re-review: ALL 7 ADDRESSED, no unexplained hunks, N4/N5/N6 untouched (final-fix-re-review.md; one minor report inaccuracy noted — startLoop line is 100 chars, not 98, still within budget). Final review: closed APPROVED-after-fixes.

/feature-done audit on final tree (HEAD 9d9c76fba): typecheck PASS, full suite PASS (1045 files / 7029 tests), plan checkboxes 55 unticked but ALL substantively verified by the opus final review (DoD 10/10) — ticked in the completion commit; smoke attestation IN SESSION (user: 4 checks good + mcpm pop adjudicated pre-existing, captured a60a6b289); no unexpected files vs plan (backlog capture + fix sweeps are review/adjudication products, ledgered); no new TODOs; deferred items: caption wake → rung 8 (D6), wake-walker question → umbrella reassessment (D4), fade-pop bug → backlog. VERDICT: READY.

USER GAVE EXPLICIT MERGE WORD 2026-08-20 ("feature-done and merge"). Executing: completion commit (tick boxes, mv plan to completed/, archive this ledger) → push → gh pr ready 591 → squash merge. This is the ledger's final state before archival.

Task 3 fix round 1: DONE — commit 002917037 (startLoop 1 line, wireInput 2 lines, biasCorrection untouched). Scoped re-review: ALL ADDRESSED (task-3-re-review.md). Task 3: complete.
