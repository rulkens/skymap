/** `GalaxyFieldProbe.requestDustPlacementReadback`'s resolved shape. */
export type GalaxyFieldDustPlacementReadback = {
  readonly count: number;
  readonly records: Float32Array;
  readonly mass: Float32Array;
  readonly renormScale: number;
};
