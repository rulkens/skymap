/** `GalaxyFieldProbe.requestDigVeilPlacementReadback`'s resolved shape. */
export type GalaxyFieldDigVeilPlacementReadback = {
  readonly count: number;
  readonly offset: number;
  readonly amplitudeBase: number;
  readonly records: Float32Array;
};
