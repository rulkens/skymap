/**
 * PlaceId — an authored anchor that is positioned but is not a body: no
 * registry row, no surface, nothing selectable. It exists so a slab row (and
 * the regions and bands keyed on it) can hang off a location rather than off
 * whichever object happens to sit there.
 */

export type PlaceId = 'galactic-centre';
