/**
 * EngineBiasHandle — Malmquist-bias correction cluster.
 *
 * Mode writes dispatch through the settings store; the reconcile saga drives
 * the async bake. No handle methods remain.
 */
export type EngineBiasHandle = {};
