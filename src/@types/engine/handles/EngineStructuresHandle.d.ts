/**
 * EngineStructuresHandle — structure overlay cluster
 * (clusters / superclusters / voids / groups).
 *
 * Marker and label visibility writes dispatch through the settings store;
 * no handle methods remain.
 */
export type EngineStructuresHandle = {};
