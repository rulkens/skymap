/**
 * EngineFlowFieldsHandle — CF4++ peculiar-velocity flow overlay cluster.
 *
 * Flow settings dispatch through the store; the reconcile saga drives
 * demand evaluation and fade side-effects. No handle methods remain.
 */
export type EngineFlowFieldsHandle = {};
