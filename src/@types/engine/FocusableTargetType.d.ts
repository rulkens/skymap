// Mirrors the SOURCE_REGISTRY `type` for the focusable arms: a galaxy-catalog
// point, an extended structure anchor, or the Milky Way singleton.  Every
// FocusableTarget dispatch table is keyed on this tag.
export type FocusableTargetType = 'galaxyCatalog' | 'structure' | 'milkyWay';
