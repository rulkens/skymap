/** One per `MESH_TEXTURE_SLOTS` row, and the key that map arrives under on a
 *  `MeshAsset`, so bake, fetch and bind all name a slot the same way. */
export type MeshTextureField = 'albedo' | 'metalRough' | 'normalMap';
