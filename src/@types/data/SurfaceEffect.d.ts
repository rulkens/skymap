/**
 * SurfaceEffect — one fragment-shading feature a `SurfaceTileSpec` row can
 * list; the tile shader variant is keyed off a row's `effects` set.
 */
export type SurfaceEffect = 'materialMap' | 'nightLights' | 'cloudShadows';
