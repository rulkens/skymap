/**
 * BLACK_HOLE_FOCUS_PREFIX — the durable `#focus=<id>` prefix for a black hole
 * (`blackhole-sgr-a-star`). Without it a bare id would slip through the composed
 * resolver's famous-galaxy branch first. The one home for the literal: the
 * encoder and the selection row's `claims` both read it.
 */

export const BLACK_HOLE_FOCUS_PREFIX = 'blackhole-';
