import type { MeshBody } from '../../@types/scene/MeshBody';

/**
 * SCENE_MESH_BODIES — seeded mesh-drawn bodies (the fifth `SceneBody` arm).
 * Empty until Task 18 adds the whale/petunias entries once their assets
 * clear curation (`data/raw/meshes/**`) and Task 10's tool bakes matching
 * `MESH_ASSETS` rows.
 */
export const SCENE_MESH_BODIES: readonly MeshBody[] = [];
