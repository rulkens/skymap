/**
 * CONTACT_SHADOW_HALF_HEIGHT_M — the contact decal box's half-extent along the
 * ground normal, metres. Sized by how far the drawn terrain strays from the
 * bake's flat plane (seat error, drawn-post stride), not by the model, so it is
 * one constant rather than a row field.
 */
export const CONTACT_SHADOW_HALF_HEIGHT_M = 0.25;
