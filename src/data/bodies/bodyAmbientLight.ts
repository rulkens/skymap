/** Night-side ambient floor of every lit body except Earth (whose floor is the
 *  `settings.earth.ambientLight` slider). Mirrors `AMBIENT` in
 *  `lib/bodyLighting.wesl`; a parity test keeps the two equal. */
export const BODY_AMBIENT_LIGHT = 0.08;
