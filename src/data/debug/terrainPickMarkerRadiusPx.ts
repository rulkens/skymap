/**
 * Screen radius of the terrain-pick debug marker, in backing-store pixels.
 * Chosen so the sphere reads at a glance yet leaves the terrain it intersects
 * visible around it — the intersection silhouette IS the measurement.
 */
export const TERRAIN_PICK_MARKER_RADIUS_PX = 12;
