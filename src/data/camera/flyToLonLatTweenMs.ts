/** How long a `camera/flyToLonLat` flight takes by default, in ms: long enough to
 * read as travel across a globe, where the 600 ms focus tween reads as a cut. */
export const FLY_TO_LON_LAT_TWEEN_MS = 1500;
