/**
 * Minimum apparent half-extent (px) of the Milky Way pick billboard, so the
 * galactic centre stays clickable from intergalactic distances. Equals the
 * default point size 2.5 px plus `PICK_PADDING_PX` 4 px; a literal so the
 * Milky Way's hit target does not follow the galaxy size setting.
 */
export const MILKY_WAY_PICK_MIN_SIZE_PX = 6.5;
