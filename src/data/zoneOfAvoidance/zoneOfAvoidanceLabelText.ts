/**
 * zoneOfAvoidanceLabelText — the curved on-band lettering's text and repeat
 * count. Repeated at evenly-spaced galactic longitudes — 3 rather than 2, so
 * the worst-case gap to the nearest copy is smaller — so the label is
 * legible from most camera headings without tracking the camera.
 */
export const ZONE_OF_AVOIDANCE_LABEL_TEXT = 'Zone of Avoidance';
export const ZONE_OF_AVOIDANCE_LABEL_REPEAT_COUNT = 3;
