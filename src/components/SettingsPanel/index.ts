/**
 * Barrel re-export for the SettingsPanel component.
 *
 * Lets consumers `import { SettingsPanel } from '../components/SettingsPanel'` regardless of
 * whether SettingsPanel is a single file today or split into multiple sub-components later.
 */
export { SettingsPanel } from './SettingsPanel';
