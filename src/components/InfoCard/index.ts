/**
 * Public surface of the InfoCard module.
 *
 * Only the top-level routing component is exported here.  Internal
 * sub-components (FullCard, CompactCard, Thumbnail) are not part of the
 * public API — they are implementation details that may change independently.
 * Import them directly from their files only if you have a strong reason.
 */
export { InfoCard } from './InfoCard';
