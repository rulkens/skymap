/**
 * Barrel re-export for the ScaleBar component.
 *
 * Lets consumers `import { ScaleBar } from '../components/ScaleBar'` regardless of
 * whether ScaleBar is a single file today or split into multiple sub-components later.
 */
export { ScaleBar } from './ScaleBar';
