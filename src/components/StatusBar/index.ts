/**
 * Barrel re-export for the StatusBar component.
 *
 * Lets consumers `import { StatusBar } from '../components/StatusBar'` regardless of
 * whether StatusBar is a single file today or split into multiple sub-components later.
 */
export { StatusBar } from './StatusBar';
