/**
 * b_c = 3root3/2 r_s, the photon-sphere impact parameter: a ray aimed inside it
 * is captured. Also the black-hole SHADOW's angular radius at distance d
 * (b_c * r_s / d) — one home for the number.
 */
export const CRITICAL_IMPACT_PARAM_RS = (3 * Math.sqrt(3)) / 2;
