import type { makeCameraSimHarness } from './makeCameraSimHarness';

/** The value {@link makeCameraSimHarness} returns — always derived, never restated. */
export type CameraSimHarness = ReturnType<typeof makeCameraSimHarness>;
